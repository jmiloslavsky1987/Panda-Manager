# Environment secrets — NEVER commit
.env
credentials/
!credentials/.gitkeep

# Google Calendar OAuth token — contains user credentials, never commit
server/calendar-token.json

# Service account keys (JSON files in credentials/ directory)
credentials/*.json

# Dependencies
node_modules/
server/node_modules/
client/node_modules/

# Build output
client/dist/
dist/

# OS artifacts
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
