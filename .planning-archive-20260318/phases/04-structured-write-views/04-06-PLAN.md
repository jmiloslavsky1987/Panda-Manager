---
phase: 04-structured-write-views
plan: "06"
type: execute
wave: 3
depends_on: [04-02, 04-03, 04-04, 04-05]
files_modified: []
autonomous: false
requirements: [UPD-01, UPD-02, UPD-03, UPD-04, UPD-05, ART-01, ART-02, ART-03, ART-04, ART-05]

must_haves:
  truths:
    - "All server tests pass: artifacts.test.js (7) + history.test.js (3) + all prior suites"
    - "WeeklyUpdateForm renders at /customer/:id/update with today's date pre-filled and 11 workstream sections"
    - "Submitting the form creates a new history entry and redirects to CustomerOverview; the new entry's week_ending appears in the header"
    - "ArtifactManager table renders at /customer/:id/artifacts with all artifact columns"
    - "Inline editing in ArtifactManager shows Saving... indicator and value persists after Drive write"
    - "Add Artifact saves and reloads table with server-assigned X-### ID"
  artifacts:
    - path: "server/routes/artifacts.test.js"
      provides: "7 passing tests for ART-02 through ART-05"
    - path: "server/routes/history.test.js"
      provides: "3 passing tests for UPD-04"
  key_links:
    - from: "WeeklyUpdateForm submit"
      to: "CustomerOverview header week_ending"
      via: "POST → history[0].week_ending read by CustomerOverview"
      pattern: "latest\\.week_ending"
    - from: "ArtifactManager Add row"
      to: "table re-render with X-### id"
      via: "onSuccess invalidateQueries refetch"
      pattern: "invalidateQueries"
---

<objective>
Run the full automated test suite and conduct the human visual verification checkpoint for Phase 4.

Purpose: This is the phase gate. All 10 Phase 4 requirements (UPD-01 through ART-05) must be confirmed working end-to-end before Phase 5 begins.
Output: Phase 4 signed off; SUMMARY.md records test results and any post-checkpoint fixes.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-structured-write-views/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite</name>
  <files></files>
  <action>
Run the full server test suite. All tests must pass before the human checkpoint proceeds.

Expected pass counts from prior phases plus Phase 4:
- routes/risks.test.js: 5
- routes/milestones.test.js: 5
- routes/actions.test.js: 12
- routes/workstreams.test.js: 5
- routes/artifacts.test.js: 7 (new in Phase 4)
- routes/history.test.js: 3 (new in Phase 4)
- client/deriveCustomer.test.js: 39
- Total: 76+ tests

If any test fails: fix the failing test or implementation before proceeding to the checkpoint. Do not skip failures.

After suite is green, confirm the dev server starts without errors:
`npm run dev` — verify no import errors in the console related to Phase 4 files (ArtifactManager, WeeklyUpdateForm, InlineEditField, InlineSelectField).
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/*.test.js && cd .. && node --test client/src/lib/deriveCustomer.test.js</automated>
  </verify>
  <done>All 76+ tests pass; zero failures; full suite exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual checkpoint — Phase 4 end-to-end verification</name>
  <files></files>
  <action>
Start dev server and verify all Phase 4 views end-to-end per the how-to-verify steps below. Fix any issues found before marking approved.
  </action>
  <verify>
    <automated>MISSING — visual verification required; no jsdom in project</automated>
  </verify>
  <done>User types "approved" confirming all verification steps pass</done>
  <what-built>
Phase 4 complete implementation:
- WeeklyUpdateForm at /customer/:id/update (UPD-01 through UPD-05)
- ArtifactManager at /customer/:id/artifacts (ART-01 through ART-05)
- InlineEditField + InlineSelectField extracted to client/src/components/
- Server routes: POST /artifacts (X-### ID), PATCH /artifacts/:id, POST /history (prepend)
  </what-built>
  <how-to-verify>
Start the dev server: `npm run dev` from the project root.

**Weekly Update Form verification (UPD-01 through UPD-05):**
1. Navigate to any customer → click "Weekly Update" in the sidebar nav
2. Confirm the URL is /customer/:id/update
3. Verify: week_ending date input is pre-filled with today's date (2026-03-05 or current)
4. Verify: ADR fieldset shows 6 rows (Inbound Integrations, Outbound Integrations, Normalization, Platform Configuration, Correlation, Training & UAT)
5. Verify: Biggy fieldset shows 5 rows (Biggy App Integration, UDC, Real-Time Integrations, Action Plans Configuration, Workflows Configuration)
6. Verify: each row has status select (Green/Yellow/Red), percent complete input, progress notes textarea, blockers textarea
7. Verify: Summary section has progress, decisions, and outcomes text areas
8. Fill in a few workstream statuses and progress notes, add text to all three summary fields
9. Click "Submit Weekly Update"
10. Verify: redirected to CustomerOverview; the "Last Updated" header shows the week_ending date you submitted (confirms history[0].week_ending read by CustomerOverview)

**Artifact Manager verification (ART-01 through ART-05):**
11. Navigate to any customer → click "Artifacts" in the sidebar nav
12. Verify: table shows columns id, type, title, description, status, owner, last_updated
13. Click a title cell → edit the text → click away (blur)
14. Verify: "Saving..." indicator appears briefly, then the updated value persists
15. Click the status dropdown for an artifact → change to "Superseded"
16. Verify: Saving... indicator appears, value updates to Superseded
17. Scroll to Add Artifact row at the bottom
18. Fill in type (select), title, description, status, owner → click Save
19. Verify: new artifact appears in table with X-### ID (X-002 or next sequential)

**ActionManager regression check:**
20. Navigate to Action Manager → verify it still loads correctly (InlineEditField/InlineSelectField still work after extraction)
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full automated suite green before checkpoint:
`cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/*.test.js`

Manual checkpoint approved by user.
</verification>

<success_criteria>
- All 76+ automated tests pass (7 new artifacts + 3 new history + 65 prior = 75 minimum, likely 76 with deriveCustomer.test.js)
- Human verifier approves:
  - WeeklyUpdateForm: date pre-fill, 11 workstream sections, submit → redirect → CustomerOverview reflects new entry
  - ArtifactManager: table renders, inline editing with Saving..., status dropdowns, Add Artifact with X-### ID
  - ActionManager: regression-free after InlineEditField/InlineSelectField extraction
- Phase 4 requirements UPD-01 through ART-05: all confirmed working end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-structured-write-views/04-06-SUMMARY.md`
Update STATE.md: Phase 4 complete, stopped_at "Completed 04-structured-write-views", progress percent_complete updated
Update ROADMAP.md Phase 4 plan checkboxes to complete
</output>
