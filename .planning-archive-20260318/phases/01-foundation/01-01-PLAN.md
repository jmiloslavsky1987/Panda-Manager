---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/services/yamlService.test.js
  - server/fixtures/sample.yaml
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Running `node --test server/services/yamlService.test.js` exits 0 with all stubs passing"
    - "A sample.yaml fixture exists that is a valid minimal customer YAML (no Drive access needed in tests)"
    - "Test stubs exist for coercion prevention, schema validation, and ID assignment — all marked TODO"
  artifacts:
    - path: "server/services/yamlService.test.js"
      provides: "Failing test stubs for INFRA-03, INFRA-04, INFRA-05"
      min_lines: 60
    - path: "server/fixtures/sample.yaml"
      provides: "Minimal valid customer YAML for use in tests without Drive"
      contains: "customer:"
  key_links:
    - from: "server/services/yamlService.test.js"
      to: "server/services/yamlService.js"
      via: "require('../services/yamlService')"
      pattern: "require.*yamlService"
---

<objective>
Create the Wave 0 test scaffold that all subsequent Wave 1 tasks depend on for their verify commands.

Purpose: The VALIDATION.md requires `node --test server/services/yamlService.test.js` to exist before any implementation task can report a green verify. Wave 0 creates stubs that are syntactically valid (pass immediately because they test nothing yet) and that will fail as soon as real assertions are added. This is the Nyquist rule: no plan proceeds without an automated verify path.

Output: `server/services/yamlService.test.js` with test stubs for all yamlService behaviors, and `server/fixtures/sample.yaml` with a minimal valid customer YAML fixture.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sample.yaml fixture</name>
  <files>server/fixtures/sample.yaml</files>
  <action>
Create `server/fixtures/sample.yaml` — a minimal valid customer YAML that matches all required top-level keys. This is used in yamlService tests without touching Drive.

The fixture must contain exactly these top-level keys (no others):
- customer
- project
- status
- workstreams
- actions
- risks
- milestones
- artifacts
- history

Use real-looking but minimal values. For workstreams, include both `adr` and `biggy` sub-keys with their exact sub-workstream names (research confirmed: ADR has Inbound Integrations, Configuration, Outbound Integrations, Workflow Configuration; Biggy has Integrations, Workflow Configuration).

CRITICAL for test validity: include at least one field value that would be coerced by default js-yaml schema, e.g. `status: on_track` (a safe string) AND a comment in the file noting that bare `on` or `yes` values must NOT appear in this fixture (they would coerce). The fixture is parsed with JSON_SCHEMA; just ensure it's valid YAML with string values.

Include one action with id `A-003` so assignNextId tests can verify the `A-004` return value. Include one risk with id `R-001` and one artifact with id `X-001`. History array must have at least one entry. All arrays must be present (not null/empty for items that need to exist for ID tests).

Write the file in `server/fixtures/sample.yaml`.
  </action>
  <verify>
    <automated>node -e "const yaml = require('js-yaml'); const fs = require('fs'); const data = yaml.load(fs.readFileSync('server/fixtures/sample.yaml','utf8'), {schema: yaml.JSON_SCHEMA}); const keys=['customer','project','status','workstreams','actions','risks','milestones','artifacts','history']; const missing = keys.filter(k=>!(k in data)); if(missing.length) throw new Error('Missing: '+missing); console.log('fixture OK')" 2>/dev/null || echo "js-yaml not yet installed — fixture will be validated in Plan 03"</automated>
  </verify>
  <done>server/fixtures/sample.yaml exists, contains all 9 required top-level keys, has an action with id A-003, a risk with id R-001, an artifact with id X-001, and at least one history entry.</done>
</task>

<task type="auto">
  <name>Task 2: Create yamlService test stubs</name>
  <files>server/services/yamlService.test.js</files>
  <action>
Create `server/services/yamlService.test.js` using Node.js built-in `node:test` module (no install required — available in Node 18+). Do NOT use vitest, jest, or mocha. Do NOT use any test framework that requires npm install.

The test file requires `../services/yamlService` — this module will not exist yet, so wrap the require in a try/catch so the stub file runs without crashing before Plan 03 creates yamlService.js. Use a module-level try/catch pattern: attempt to require; if it fails, set a flag; in each test, skip with `t.skip()` if the flag is set.

Write the following test stubs using `node:test` and `node:assert`:

```
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
```

(Use ESM or CJS — since server uses CJS (require()), use CJS: `const { test, describe } = require('node:test'); const assert = require('node:assert/strict');`)

Test groups to create (all stubs — they use `t.todo()` so they pass but mark as TODO):

**Group: parseYaml — INFRA-03 coercion prevention**
- `parseYaml() parses "status: on" as string "on" not boolean true`
- `parseYaml() parses "active: yes" as string "yes" not boolean true`
- `parseYaml() parses "flag: no" as string "no" not boolean false`

**Group: serializeYaml — INFRA-03 round-trip fidelity**
- `serializeYaml() preserves key insertion order (sortKeys: false)`
- `serializeYaml() does not wrap long lines (lineWidth: -1)`
- `serializeYaml() round-trips sample.yaml without data loss`

**Group: validateYaml — INFRA-04 schema enforcement**
- `validateYaml() passes for object with all 9 required top-level keys`
- `validateYaml() throws ValidationError when a required key is missing`
- `validateYaml() throws ValidationError when an extra key is present`
- `validateYaml() throws ValidationError when actions is not an array`
- `validateYaml() throws ValidationError when history is not an array`

**Group: assignNextId — INFRA-05 sequential ID assignment**
- `assignNextId("A", []) returns "A-001"`
- `assignNextId("A", items with A-003 as max) returns "A-004"`
- `assignNextId("R", []) returns "R-001"`
- `assignNextId("X", items including completed items) never reuses IDs`

Use `test('description', (t) => { t.todo('implement in Plan 03'); })` for all stubs. This makes them syntactically valid and the runner exits 0 (todo tests do not fail).

The file must include a top comment:
```
// Wave 0 test stubs — behaviors defined here, implementations added in Plan 03
// Run: node --test server/services/yamlService.test.js
// Requires: Node 18+ (node:test built-in)
```
  </action>
  <verify>
    <automated>node --test "server/services/yamlService.test.js" 2>&1 | tail -5</automated>
  </verify>
  <done>`node --test server/services/yamlService.test.js` runs without crashing. Output shows test count. All tests are marked as TODO (not failures). Exit code is 0.</done>
</task>

</tasks>

<verification>
Both artifacts exist and the test runner exits 0:
1. `ls server/fixtures/sample.yaml server/services/yamlService.test.js` — both files present
2. `node --test server/services/yamlService.test.js` — exits 0, shows TODO markers
</verification>

<success_criteria>
- server/fixtures/sample.yaml exists with all 9 required top-level keys and seeded IDs (A-003, R-001, X-001)
- server/services/yamlService.test.js exists with 14 test stubs covering INFRA-03, INFRA-04, INFRA-05
- `node --test server/services/yamlService.test.js` exits 0 (stubs are all TODO, not failures)
- No npm install required for Wave 0 (node:test is built-in)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
