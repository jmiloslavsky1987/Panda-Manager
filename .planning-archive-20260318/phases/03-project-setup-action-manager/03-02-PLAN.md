---
phase: 03-project-setup-action-manager
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - server/routes/actions.js
  - server/routes/actions.test.js
autonomous: true
requirements: [ACT-02, ACT-03, ACT-04, ACT-05, ACT-06, ACT-09, ACT-11, ACT-12]

must_haves:
  truths:
    - "POST /api/customers/:id/actions returns 201 with server-assigned A-004 ID given sample.yaml A-003 max"
    - "PATCH /api/customers/:id/actions/:actionId returns 200 with the patched action"
    - "PATCH with status:completed writes completed_date as YYYY-MM-DD string (not full ISO timestamp)"
    - "PATCH with status:open and completed_date:'' reopens an action"
    - "Returns 404 when actionId not found in YAML"
    - "writeYamlFile called exactly once per mutation (atomic write verified)"
    - "node --test server/routes/actions.test.js exits 0 with all tests green"
  artifacts:
    - path: "server/routes/actions.js"
      provides: "Full POST and PATCH implementations replacing 501 stubs"
      exports: []
    - path: "server/routes/actions.test.js"
      provides: "All stubs filled with real supertest assertions"
  key_links:
    - from: "server/routes/actions.js"
      to: "server/services/yamlService"
      via: "require('../services/yamlService')"
      pattern: "yamlService.assignNextId"
    - from: "server/routes/actions.js"
      to: "server/services/driveService"
      via: "require('../services/driveService')"
      pattern: "driveService.readYamlFile|writeYamlFile"
---

<objective>
Wave 1: Implement POST and PATCH handlers in server/routes/actions.js (replacing 501 stubs) and fill
the test assertions in actions.test.js.

Purpose: All client-side action mutations (complete, edit, reopen, delay, add) depend on these
endpoints. GET is omitted — client reads actions from the full customer object via useOutletContext.

Output: Working POST /api/customers/:id/actions and PATCH /api/customers/:id/actions/:actionId
endpoints; all actions.test.js assertions green.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-project-setup-action-manager/03-01-SUMMARY.md
@server/routes/actions.js
@server/routes/actions.test.js
@server/routes/risks.js
@server/services/yamlService.js
@server/fixtures/sample.yaml

<interfaces>
From server/services/yamlService.js:
  parseYaml(content): parses with JSON_SCHEMA (no boolean coercion)
  serializeYaml(data): dump with sortKeys:false, lineWidth:-1, noRefs:true
  validateYaml(data): throws ValidationError(422) if required keys missing or arrays not arrays
  normalizeForSerialization(data): ensures array fields are [] not null/undefined
  assignNextId(prefix, allItems): scans item.id fields, returns next sequential e.g. "A-004"

From server/routes/risks.js (atomic PATCH pattern to replicate exactly):
  const content = await driveService.readYamlFile(fileId);
  const data = yamlService.parseYaml(content);
  yamlService.validateYaml(data);
  // ... modify data ...
  const normalized = yamlService.normalizeForSerialization(data);
  const yamlString = yamlService.serializeYaml(normalized);
  await driveService.writeYamlFile(fileId, yamlString);
  res.json({ fileId, ... });

actions.js: router = require('express').Router({ mergeParams: true }) — mergeParams already set
  GET / stub (501) — LEAVE UNCHANGED (not needed in Phase 3, client uses customer object)
  POST / stub (501) — replace with full implementation
  PATCH /:actionId stub (501) — replace with full implementation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement POST /api/customers/:id/actions</name>
  <files>server/routes/actions.js, server/routes/actions.test.js</files>
  <behavior>
    - POST with {description, owner, due, workstream} returns 201 with action including id:"A-004" (sample.yaml max is A-003)
    - New action gets status:"open" and completed_date:"" set by server (not from request body)
    - writeYamlFile called exactly once
    - Returns 422 when YAML validation fails (use mockReadYamlFile.mock.mockImplementationOnce to return broken YAML)
  </behavior>
  <action>
Replace the POST / stub in server/routes/actions.js with:

  router.post('/', asyncWrapper(async (req, res) => {
    const { id: fileId } = req.params;
    const { description, owner, due, workstream } = req.body;

    const content = await driveService.readYamlFile(fileId);
    const data = yamlService.parseYaml(content);
    yamlService.validateYaml(data);

    const newId = yamlService.assignNextId('A', data.actions);
    const newAction = {
      id: newId,
      description: description ?? '',
      owner: owner ?? '',
      due: due ?? '',
      status: 'open',
      workstream: workstream ?? '',
      completed_date: '',
    };

    data.actions.push(newAction);

    const normalized = yamlService.normalizeForSerialization(data);
    const yamlString = yamlService.serializeYaml(normalized);
    await driveService.writeYamlFile(fileId, yamlString);

    res.status(201).json({ fileId, action: newAction });
  }));

Add require statements at top of file (after existing asyncWrapper require):
  const driveService = require('../services/driveService');
  const yamlService = require('../services/yamlService');

Fill the POST describe block in actions.test.js with real assertions (replacing t.todo()):
  - 'returns 201 with new action including server-assigned A-004 id':
      const res = await request.post(`/api/customers/${FAKE_FILE_ID}/actions`)
        .send({ description: 'Test action', owner: 'Alice', due: '2026-04-01', workstream: 'adr' })
        .set('Content-Type', 'application/json');
      assert.equal(res.status, 201);
      assert.equal(res.body.action.id, 'A-004');
      assert.equal(res.body.action.status, 'open');
      assert.equal(res.body.action.completed_date, '');

  - 'assignNextId increments correctly when existing max is A-003':
      Same test as above — assert id is 'A-004' (max A-003 in sample.yaml)

  - 'writes updated YAML to Drive (writeYamlFile called once)':
      mockWriteYamlFile.mock.resetCalls();
      await request.post(...).send({...});
      assert.equal(mockWriteYamlFile.mock.calls.length, 1);

  - '422 when YAML validation fails':
      mockReadYamlFile.mock.mockImplementationOnce(() =>
        Promise.resolve('customer:\n  name: "Broken"\nstatus: "on_track"\n')
      );
      const res = await request.post(...).send({...});
      assert.equal(res.status, 422);
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/actions.test.js 2>&1 | grep -E "POST|pass|fail|error" | head -20</automated>
  </verify>
  <done>POST describe block: 4 assertions pass; no t.todo skips remain in POST block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PATCH /api/customers/:id/actions/:actionId</name>
  <files>server/routes/actions.js, server/routes/actions.test.js</files>
  <behavior>
    - PATCH with partial fields returns 200 with merged action (other fields preserved)
    - PATCH {status:'completed'} also requires completed_date in the patch body (set client-side)
    - PATCH {status:'open', completed_date:''} reopens action (completed_date becomes "")
    - PATCH with unknown actionId returns 404
    - writeYamlFile called exactly once per PATCH
  </behavior>
  <action>
Replace the PATCH /:actionId stub in server/routes/actions.js with:

  router.patch('/:actionId', asyncWrapper(async (req, res) => {
    const { id: fileId, actionId } = req.params;
    const patch = req.body;

    const content = await driveService.readYamlFile(fileId);
    const data = yamlService.parseYaml(content);
    yamlService.validateYaml(data);

    const idx = data.actions.findIndex(a => a.id === actionId);
    if (idx === -1) {
      return res.status(404).json({ error: `Action ${actionId} not found` });
    }

    data.actions[idx] = { ...data.actions[idx], ...patch };

    const normalized = yamlService.normalizeForSerialization(data);
    const yamlString = yamlService.serializeYaml(normalized);
    await driveService.writeYamlFile(fileId, yamlString);

    res.json({ fileId, actionId, action: data.actions[idx] });
  }));

Note on completed_date: The server accepts whatever the client sends in the patch body. The client
is responsible for sending completed_date: new Date().toISOString().split('T')[0] when completing
an action. Do NOT set completed_date server-side — this keeps the endpoint generic for all patches.

Fill the PATCH describe block in actions.test.js (replacing all t.todo()):
  - 'returns 200 with updated action when patching description':
      res = await request.patch(`.../actions/A-002`).send({ description: 'Updated desc' });
      assert.equal(res.status, 200);
      assert.equal(res.body.action.description, 'Updated desc');
      assert.equal(res.body.action.id, 'A-002'); // preserved

  - 'returns 200 when patching owner':
      res = await request.patch(`.../actions/A-002`).send({ owner: 'New Owner' });
      assert.equal(res.status, 200);
      assert.equal(res.body.action.owner, 'New Owner');

  - 'marks action complete: sets status=completed and completed_date=today':
      const today = new Date().toISOString().split('T')[0];
      res = await request.patch(`.../actions/A-002`)
        .send({ status: 'completed', completed_date: today });
      assert.equal(res.status, 200);
      assert.equal(res.body.action.status, 'completed');
      assert.equal(res.body.action.completed_date, today);

  - 'reopens action: sets status=open and completed_date empty':
      res = await request.patch(`.../actions/A-001`)
        .send({ status: 'open', completed_date: '' });
      assert.equal(res.status, 200);
      assert.equal(res.body.action.status, 'open');
      assert.equal(res.body.action.completed_date, '');

  - 'sets status=delayed':
      res = await request.patch(`.../actions/A-002`).send({ status: 'delayed' });
      assert.equal(res.status, 200);
      assert.equal(res.body.action.status, 'delayed');

  - 'returns 404 when actionId not found':
      res = await request.patch(`.../actions/A-999`).send({ status: 'open' });
      assert.equal(res.status, 404);
      assert.ok(res.body.error);

  - 'applies partial patch — other fields preserved':
      res = await request.patch(`.../actions/A-002`).send({ owner: 'Patched' });
      assert.equal(res.status, 200);
      assert.equal(res.body.action.owner, 'Patched');
      assert.equal(res.body.action.description, 'Schedule kickoff call with customer team'); // original

  - 'writeYamlFile called exactly once':
      mockWriteYamlFile.mock.resetCalls();
      await request.patch(`.../actions/A-002`).send({ status: 'delayed' });
      assert.equal(mockWriteYamlFile.mock.calls.length, 1);
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/actions.test.js 2>&1 | tail -8</automated>
  </verify>
  <done>All 12 tests in actions.test.js pass (exit 0); no t.todo skips; 404 for unknown ID; writeYamlFile called once per mutation.</done>
</task>

</tasks>

<verification>
Run full suite to confirm no regressions:
cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/risks.test.js routes/milestones.test.js routes/actions.test.js
</verification>

<success_criteria>
- server/routes/actions.js: GET / stub preserved (501), POST / implemented, PATCH /:actionId implemented
- POST returns 201 + action with server-assigned A-004 ID
- PATCH returns 200 + merged action, 404 for missing ID
- writeYamlFile called once per mutation (atomic write pattern)
- All 12 actions.test.js assertions pass (exit 0)
- risks.test.js and milestones.test.js still green (no regressions)
</success_criteria>

<output>
After completion, create .planning/phases/03-project-setup-action-manager/03-02-SUMMARY.md
</output>
