---
phase: 06-ux-polish-and-feature-enhancements
plan: "03"
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - client/src/views/YAMLEditor.jsx
  - client/src/views/ReportGenerator.jsx
autonomous: true
requirements:
  - UX-04
  - UX-05
  - UX-06

must_haves:
  truths:
    - "YAML Editor shows a blocking confirmation dialog when user navigates away with isDirty=true"
    - "YAML Editor shows an amber banner warning that saving strips YAML comments"
    - "Report Generator Weekly Status section has a working Download .txt button that triggers browser file save"
  artifacts:
    - path: "client/src/views/YAMLEditor.jsx"
      provides: "useBlocker for navigate-away guard + amber comments-stripping banner"
    - path: "client/src/views/ReportGenerator.jsx"
      provides: "DownloadTxtButton component + integration in Weekly Status output section"
  key_links:
    - from: "client/src/views/YAMLEditor.jsx"
      to: "react-router-dom useBlocker"
      via: "import { useBlocker } from 'react-router-dom'"
      pattern: "useBlocker"
    - from: "client/src/views/ReportGenerator.jsx"
      to: "Blob URL.createObjectURL"
      via: "same pattern as existing downloadPptx function in ReportGenerator"
      pattern: "URL\\.createObjectURL"
---

<objective>
Implement three Phase 5 requirements that were specified but never built: YAML-04 (navigate-away guard), YAML-05 (comments-stripping banner), and RPT-05 (.txt download for Weekly Status).

Purpose: These are completeness gaps, not polish — they are required behaviors specified in the original brief. Completing them closes the last open requirements from Phase 5.

Output: YAMLEditor with useBlocker confirmation dialog and amber banner. ReportGenerator Weekly Status output with Download .txt button.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ux-polish-and-feature-enhancements/06-RESEARCH.md

@client/src/views/YAMLEditor.jsx
@client/src/views/ReportGenerator.jsx

<interfaces>
<!-- From RESEARCH.md: React Router v7 useBlocker pattern -->
import { useBlocker } from 'react-router-dom';

const blocker = useBlocker(
  ({ currentLocation, nextLocation }) =>
    isDirty && currentLocation.pathname !== nextLocation.pathname
);

// Render confirmation UI when blocker.state === 'blocked'
if (blocker.state === 'blocked') {
  return (
    <div className="fixed inset-0 bg-black/40 flex items-center justify-center z-50">
      <div className="bg-white rounded-lg p-6 max-w-sm w-full shadow-lg border border-gray-200">
        <p className="text-sm font-medium text-gray-900 mb-1">Unsaved Changes</p>
        <p className="text-sm text-gray-600 mb-4">You have unsaved YAML changes. Leave anyway?</p>
        <div className="flex gap-3 justify-end">
          <button onClick={() => blocker.reset()} className="px-3 py-1.5 text-sm rounded border border-gray-200 text-gray-600 hover:bg-gray-50">Stay</button>
          <button onClick={() => blocker.proceed()} className="px-3 py-1.5 text-sm rounded bg-red-600 text-white hover:bg-red-700">Leave anyway</button>
        </div>
      </div>
    </div>
  );
}

<!-- From RESEARCH.md: .txt download pattern (reuses downloadPptx pattern already in ReportGenerator) -->
function downloadTxt(text, filename) {
  const blob = new Blob([text], { type: 'text/plain' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = filename;
  a.click();
  URL.revokeObjectURL(url);
}

<!-- From RESEARCH.md: comments-stripping banner -->
<div className="bg-amber-50 border border-amber-200 rounded-lg px-4 py-3 text-sm text-amber-800 shrink-0">
  <strong>Comments will be stripped on save.</strong>{' '}
  js-yaml does not preserve YAML comments when serializing.
  Copy any comments you want to keep before saving.
</div>

<!-- YAMLEditor.jsx key state: -->
const [isDirty, setIsDirty] = React.useState(false);
// isDirty is true when CodeMirror content differs from savedContentRef.current
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useBlocker navigate-away guard and comments banner to YAMLEditor</name>
  <files>client/src/views/YAMLEditor.jsx</files>
  <action>
    Read YAMLEditor.jsx fully before editing. The file uses React Router hooks — confirm `useBlocker` is not already imported. The `isDirty` state already exists.

    **Step 1 — Add useBlocker import:**
    Find the existing react-router-dom import line (it imports `useParams` at minimum). Add `useBlocker` to the same import:
    ```javascript
    import { useParams, useBlocker } from 'react-router-dom';
    ```

    **Step 2 — Add blocker hook call** (inside the component, after the existing useState hooks):
    ```javascript
    const blocker = useBlocker(
      ({ currentLocation, nextLocation }) =>
        isDirty && currentLocation.pathname !== nextLocation.pathname
    );
    ```

    **Step 3 — Add blocker confirmation dialog rendering:**
    This must be rendered as an overlay, NOT as an early return that replaces the whole editor. Add as a conditional overlay in the JSX return, inside the outermost div but AFTER all other content:
    ```jsx
    {blocker.state === 'blocked' && (
      <div className="fixed inset-0 bg-black/40 flex items-center justify-center z-50">
        <div className="bg-white rounded-lg p-6 max-w-sm w-full shadow-lg border border-gray-200">
          <p className="text-sm font-medium text-gray-900 mb-1">Unsaved Changes</p>
          <p className="text-sm text-gray-600 mb-4">You have unsaved YAML changes. Leave anyway?</p>
          <div className="flex gap-3 justify-end">
            <button
              type="button"
              onClick={() => blocker.reset()}
              className="px-3 py-1.5 text-sm rounded border border-gray-200 text-gray-600 hover:bg-gray-50"
            >
              Stay
            </button>
            <button
              type="button"
              onClick={() => blocker.proceed()}
              className="px-3 py-1.5 text-sm rounded bg-red-600 text-white hover:bg-red-700"
            >
              Leave anyway
            </button>
          </div>
        </div>
      </div>
    )}
    ```

    **Step 4 — Add comments-stripping banner:**
    The existing view has a warning banner (the "Use with care" structural warning). Add the comments banner as a SECOND, SEPARATE amber banner below it (do not modify the existing warning banner):
    ```jsx
    <div className="bg-amber-50 border border-amber-200 rounded-lg px-4 py-3 text-sm text-amber-800 shrink-0">
      <strong>Comments will be stripped on save.</strong>{' '}
      js-yaml does not preserve YAML comments when serializing.
      Copy any comments you want to keep before saving.
    </div>
    ```

    All className values above are complete literal strings — no dynamic construction (Tailwind v4 constraint satisfied).

    CRITICAL — Pitfall 2 from research: `useBlocker` must be called directly inside the YAMLEditor component function (not in a utility hook or outside the route tree). It is already under a route, so this is fine.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test --test-reporter spec routes/*.test.js services/*.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>YAMLEditor.jsx imports useBlocker; blocker hook called with isDirty condition; confirmation overlay renders when blocker.state === 'blocked'; amber comments banner present below existing warning banner. Server tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add Download .txt button to ReportGenerator Weekly Status output</name>
  <files>client/src/views/ReportGenerator.jsx</files>
  <action>
    Read ReportGenerator.jsx fully. Locate the Weekly Status output section — it already has a `CopyButton` component for "Copy Email". The `.txt` download button should appear alongside it.

    **Step 1 — Add downloadTxt helper function** (add after the existing `CopyButton` component definition, before the main component):
    ```javascript
    function downloadTxt(text, filename) {
      const blob = new Blob([text], { type: 'text/plain' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = filename;
      a.click();
      URL.revokeObjectURL(url);
    }
    ```

    **Step 2 — Find the Weekly Status output section.** It renders when `report.type === 'weekly_status'` (or similar). There is a `CopyButton text={text} label="Copy Email"` or similar. After that CopyButton, add:
    ```jsx
    <button
      type="button"
      onClick={() => downloadTxt(text, `weekly-status-${customerId || 'report'}.txt`)}
      className="text-xs px-2.5 py-1 rounded border border-gray-200 text-gray-500 hover:border-teal-300 hover:text-teal-600 transition-colors whitespace-nowrap"
    >
      ↓ Download .txt
    </button>
    ```

    The `text` variable in scope should be the same string passed to `CopyButton`. If the text is not in an obvious variable, read the surrounding JSX carefully to find where the Weekly Status string content is held (likely a prop or state value like `report.content` or a variable derived from the report result).

    The `customerId` is available via `useParams()` — confirm it's already destructured or add it.

    All className strings are complete literals (Tailwind v4 safe).
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test --test-reporter spec routes/*.test.js services/*.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>ReportGenerator.jsx has downloadTxt helper. Weekly Status output section has a "Download .txt" button that calls downloadTxt with the report text content. Server tests pass.</done>
</task>

</tasks>

<verification>
Manual browser verification (after `npm run dev`):
1. Open YAML Editor for any customer. Make a change (type any character). Click a Sidebar nav link. Confirmation overlay must appear with "Stay" and "Leave anyway" buttons. "Stay" dismisses. "Leave anyway" navigates.
2. YAML Editor shows TWO banners: the existing structural warning AND the new amber "Comments will be stripped on save" banner.
3. Generate a Weekly Status report. The output section shows both "Copy Email" and "Download .txt" buttons. Clicking Download .txt triggers a browser file save.

Server suite still green:
`cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test --test-reporter spec routes/*.test.js services/*.test.js`
</verification>

<success_criteria>
- YAML-04: useBlocker fires when isDirty=true and user attempts navigation; confirmation dialog has Stay/Leave buttons
- YAML-05: Amber banner "Comments will be stripped on save" visible in YAML Editor
- RPT-05: Download .txt button present in Weekly Status output; triggers browser download with .txt MIME type
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-and-feature-enhancements/06-03-SUMMARY.md`
</output>
