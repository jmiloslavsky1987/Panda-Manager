---
phase: 06-ux-polish-and-feature-enhancements
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - server/routes/risks.test.js
  - server/routes/milestones.test.js
  - client/src/lib/reportGenerator.test.js
autonomous: true
requirements:
  - UX-08
  - UX-07

must_haves:
  truths:
    - "POST /api/customers/:id/risks test cases exist as t.todo() stubs that run and exit 0"
    - "POST /api/customers/:id/milestones test cases exist as t.todo() stubs that run and exit 0"
    - "client/src/lib/reportGenerator.test.js exists with a t.todo() stub for buildPanel workstream matching"
    - "Full server test suite runs and exits 0 with no failures"
  artifacts:
    - path: "server/routes/risks.test.js"
      provides: "POST /risks test stubs appended to existing PATCH tests"
    - path: "server/routes/milestones.test.js"
      provides: "POST /milestones test stubs appended to existing PATCH tests"
    - path: "client/src/lib/reportGenerator.test.js"
      provides: "Wave 0 unit test stub for reportGenerator buildPanel regression guard"
  key_links:
    - from: "server/routes/risks.test.js"
      to: "POST /api/customers/:id/risks"
      via: "supertest + t.todo() stubs — filled in Plan 06-04"
      pattern: "t\\.todo\\("
    - from: "client/src/lib/reportGenerator.test.js"
      to: "reportGenerator.js buildPanel()"
      via: "t.todo() stub — filled in Plan 06-02"
      pattern: "t\\.todo\\("
---

<objective>
Establish Wave 0 test stubs required by the Nyquist validation contract before any implementation begins.

Purpose: Three test gaps exist from the Phase 6 validation plan — POST risks endpoint, POST milestones endpoint, and a regression guard for the reportGenerator buildPanel bug fix. These stubs must exist and run clean before implementation plans execute, so every subsequent task has an automated verify path.

Output: Three files updated/created with t.todo() stubs that exit 0. No implementations yet.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ux-polish-and-feature-enhancements/06-VALIDATION.md

@server/routes/risks.test.js
@server/routes/milestones.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST stubs to risks.test.js and milestones.test.js</name>
  <files>server/routes/risks.test.js, server/routes/milestones.test.js</files>
  <action>
    Append a new describe block to EACH of the two existing test files. Do NOT modify any existing tests.

    For risks.test.js — append AFTER the closing of the existing PATCH describe block:

    ```javascript
    describe('POST /api/customers/:id/risks', () => {
      it('creates a new risk and returns 201 with R-### id', t.todo);
      it('assigns next sequential R-### id (max+1) when risks already exist', t.todo);
      it('rejects missing required fields with 400', t.todo);
      it('writes the new risk to Drive atomically (writeYamlFile called once)', t.todo);
    });
    ```

    For milestones.test.js — append AFTER the closing of the existing PATCH describe block:

    ```javascript
    describe('POST /api/customers/:id/milestones', () => {
      it('creates a new milestone and returns 201 with M-### id', t.todo);
      it('assigns next sequential M-### id (max+1) when milestones already exist', t.todo);
      it('rejects missing required fields with 400', t.todo);
      it('writes the new milestone to Drive atomically (writeYamlFile called once)', t.todo);
    });
    ```

    The `t.todo` shorthand works because `t` is already destructured from `require('node:test')` as `{ describe, it, before, mock }`. If `it` is destructured but not `t` itself, use the full form `it('...', (t) => { t.todo(); })` — but check: sample pattern in the file uses `t.todo` as bare second arg (consistent with node:test docs).

    Verify `const { describe, it, before, mock } = require('node:test')` is at the top — if `it` is already destructured there is no `t` in scope. In that case use: `it('...', () => {});` with a comment `// TODO: implement`. Actually the correct node:test todo pattern without a t-context is to pass `undefined` as the second arg — but the safest approach matching existing test files is `it('description')` with no callback (node:test treats missing callback as todo). Use `it('creates a new risk and returns 201 with R-### id')` (no second arg) which node:test treats as a pending test.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test --test-reporter spec routes/risks.test.js routes/milestones.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files run; existing PATCH tests still pass; new POST describe blocks appear with pending tests; exit code 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create reportGenerator.test.js stub</name>
  <files>client/src/lib/reportGenerator.test.js</files>
  <action>
    Create a new file at `client/src/lib/reportGenerator.test.js`. This is a server-side node:test file — even though the file lives in the client tree, it is a standalone node:test script (not a Vite/browser test). The reportGenerator.js is a pure JS module with no DOM dependencies, so it can be required directly.

    ```javascript
    // client/src/lib/reportGenerator.test.js
    // Regression guard for buildPanel() workstream-group filter bug (Phase 6 fix).
    // Run: node --test client/src/lib/reportGenerator.test.js
    'use strict';
    const { describe, it } = require('node:test');

    // NOTE: reportGenerator.js is an ESM module (uses export). To test it from node:test
    // (CJS runner), we use a dynamic import inside an async test.
    // When the buildPanel fix is implemented in Plan 06-02, replace t.todo stubs below
    // with real assertions using the dynamically-imported module.

    describe('reportGenerator buildPanel() workstream filter', () => {
      it('matches actions whose workstream is a sub-key of the ELT group (not the group key itself)');
      it('returns non-empty lookingAhead bullets when actions exist for the group sub-workstreams');
      it('falls back to "Continue current work items" only when no actions match the group sub-workstreams');
    });
    ```

    Use single-argument `it('description')` (no callback) so node:test treats them as pending — same pattern as Task 1.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && node --test --test-reporter spec client/src/lib/reportGenerator.test.js 2>&1 | tail -15</automated>
  </verify>
  <done>File exists; node:test runs it; 3 pending tests shown; exit code 0.</done>
</task>

</tasks>

<verification>
Full server suite still green after stub additions:

```bash
cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test --test-reporter spec routes/*.test.js services/*.test.js
```

Exit code must be 0.
</verification>

<success_criteria>
- risks.test.js has a POST describe block with 4 pending (t.todo) cases — existing PATCH tests unchanged
- milestones.test.js has a POST describe block with 4 pending (t.todo) cases — existing PATCH tests unchanged
- client/src/lib/reportGenerator.test.js exists with 3 pending cases
- Full server suite (routes/*.test.js services/*.test.js) exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-polish-and-feature-enhancements/06-01-SUMMARY.md`
</output>
