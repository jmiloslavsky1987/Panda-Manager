---
phase: 37-actions-inline-editing-foundation
plan: "04"
type: execute
wave: 2
depends_on:
  - "37-02"
  - "37-03"
files_modified:
  - bigpanda-app/app/customer/[id]/actions/page.tsx
  - bigpanda-app/components/ActionsTableClient.tsx
autonomous: true
requirements:
  - ACTN-01
  - ACTN-02
  - ACTN-03
  - ACTN-04
  - ACTN-05
  - SRCH-03

must_haves:
  truths:
    - "Actions tab renders as a table with columns: Checkbox | ID | Description | Owner | Due Date | Status | Source"
    - "Clicking an Owner, Due Date, or Status cell opens the inline editor in-place (no modal)"
    - "Filter bar row contains: search input, status chips, owner dropdown, date range inputs — all wired to URL query params"
    - "Typing in search box filters visible actions client-side by description text (URL param ?q=)"
    - "Selecting 1+ rows via checkbox shows floating bulk-action bar; selecting status applies bulk PATCH"
    - "After inline save or bulk save, router.refresh() re-fetches server data"
  artifacts:
    - path: "bigpanda-app/app/customer/[id]/actions/page.tsx"
      provides: "Server Component that fetches actions + stakeholders, renders ActionsTableClient"
      min_lines: 40
    - path: "bigpanda-app/components/ActionsTableClient.tsx"
      provides: "Client island with filter bar, bulk select, inline editing"
      min_lines: 150
      exports: ["ActionsTableClient"]
  key_links:
    - from: "ActionsTableClient.tsx"
      to: "InlineSelectCell"
      via: "import and render for status column"
      pattern: "InlineSelectCell.*onSave.*status"
    - from: "ActionsTableClient.tsx"
      to: "DatePickerCell"
      via: "import and render for due date column"
      pattern: "DatePickerCell.*onSave.*due_date"
    - from: "ActionsTableClient.tsx"
      to: "OwnerCell"
      via: "import and render for owner column"
      pattern: "OwnerCell.*onSave.*owner"
    - from: "ActionsTableClient.tsx"
      to: "/api/actions/[id]"
      via: "PATCH fetch for single-cell inline saves"
      pattern: "fetch.*api/actions.*PATCH"
    - from: "ActionsTableClient.tsx"
      to: "/api/actions/bulk-update"
      via: "POST fetch for bulk status change"
      pattern: "fetch.*bulk-update.*POST"
    - from: "ActionsTableClient.tsx"
      to: "useSearchParams"
      via: "reads ?q=, ?status=, ?owner=, ?from=, ?to= for filtering"
      pattern: "useSearchParams.*q.*status.*owner"
---

<objective>
Refactor the Actions page from card layout to a full table with inline cell editing, URL-driven filters, text search, and a floating bulk action bar. This is the largest plan in Phase 37.

Purpose: Delivers ACTN-01 through ACTN-05 and SRCH-03 in a single vertical slice.
Output: A working Actions table with all interactive features.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/PROJECT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/ROADMAP.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-CONTEXT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-RESEARCH.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-02-SUMMARY.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-03-SUMMARY.md

<interfaces>
<!-- Shared components from Plan 02 -->
import { InlineSelectCell } from '@/components/InlineSelectCell'
import { DatePickerCell } from '@/components/DatePickerCell'
import { OwnerCell } from '@/components/OwnerCell'

<!-- shadcn Table -->
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import { Checkbox } from '@/components/ui/checkbox'

<!-- SourceBadge — already used in existing actions page -->
import SourceBadge from '@/components/SourceBadge'

<!-- Next.js client hooks -->
'use client'
import { useRouter, useSearchParams } from 'next/navigation'
import { useCallback, useState } from 'react'

<!-- Action status enum — confirmed from API Plan 03 -->
const ACTION_STATUS_OPTIONS = [
  { value: 'open', label: 'Open' },
  { value: 'in_progress', label: 'In Progress' },
  { value: 'completed', label: 'Completed' },
  { value: 'cancelled', label: 'Cancelled' },
] as const

<!-- Action type shape (from existing page.tsx / db schema) -->
type Action = {
  id: number
  description: string | null
  owner: string | null
  due_date: string | null
  status: string
  source: string | null
  project_id: number
  // ... other fields
}

<!-- ActionsTableClient props -->
interface ActionsTableClientProps {
  actions: Action[]
  projectId: number
  // stakeholders NOT needed as prop — OwnerCell fetches internally via /api/stakeholders?project_id=
}

<!-- Filter pitfall avoidance (from RESEARCH.md Pitfall 1) -->
// WRONG: Pass pre-filtered actions to client
// RIGHT: Pass full unfiltered list; client reads useSearchParams() and filters in-memory
// Filtering: useMemo over actions list using q, status, owner, from/to values from useSearchParams()

<!-- URL param update without navigation -->
// Use router.push with updated URLSearchParams:
const params = new URLSearchParams(searchParams.toString())
params.set('q', value)
router.push(`?${params.toString()}`, { scroll: false })

<!-- PATCH single action inline save -->
async function patchAction(id: number, patch: Record<string, unknown>) {
  const res = await fetch(`/api/actions/${id}`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(patch),
  })
  if (!res.ok) throw new Error('Save failed')
  router.refresh()
}
// Each inline cell: onSave={async (val) => patchAction(action.id, { status: val })}

<!-- Bulk action POST -->
async function bulkUpdateStatus(selectedIds: Set<number>, status: string) {
  await fetch('/api/actions/bulk-update', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ action_ids: [...selectedIds], patch: { status } }),
  })
  setSelectedIds(new Set())
  router.refresh()
}

<!-- Floating bulk bar pattern (from RESEARCH.md Pattern 5) -->
// Render above <Table> when selectedIds.size > 0
// "N selected — [Status dropdown] [Clear]"

<!-- Existing ActionEditModal — retain for description/notes cells -->
import ActionEditModal from '@/components/ActionEditModal'
// Trigger from: description cell click OR a small edit icon at row end
// NOT triggered from status, owner, or due_date cells

<!-- Server Component data fetch pattern — from RESEARCH.md Pitfall 7 -->
// searchParams is a Promise in Next.js 16 — must await
// But ActionsTableClient reads useSearchParams() client-side, so server just passes unfiltered data
type SearchParams = Promise<Record<string, string | string[] | undefined>>
export default async function ActionsPage({
  params,
  searchParams,
}: {
  params: Promise<{ id: string }>
  searchParams: SearchParams
}) {
  const { id } = await params
  const projectId = parseInt(id, 10)
  // Fetch full unfiltered actions list
  // Return <ActionsTableClient actions={...} projectId={projectId} />
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor actions/page.tsx to Server Component + data fetch</name>
  <files>
    bigpanda-app/app/customer/[id]/actions/page.tsx
  </files>
  <action>
    READ the existing `bigpanda-app/app/customer/[id]/actions/page.tsx` file first to understand the current card layout and data fetching.

    Refactor to:
    1. Keep it as a Server Component (no 'use client' directive)
    2. Fetch the FULL unfiltered actions list via the existing data access pattern (db query or getWorkspaceData)
    3. Remove all card rendering, status filter chips, and any pre-filtering logic from the server component
    4. Render only `<ActionsTableClient actions={actions} projectId={projectId} />` in the return
    5. Preserve `searchParams: Promise<...>` and `await searchParams` (Next.js 16 async searchParams — pitfall 7) but do NOT pre-filter the data — just await it to avoid the warning
    6. Keep `requireSession()` or session check if it exists in the current page
    7. The existing `ActionEditModal` import can be removed from the page — it will be imported inside ActionsTableClient instead

    Do NOT create ActionsTableClient.tsx yet — that is Task 2.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "actions/page" | head -10</automated>
  </verify>
  <done>page.tsx is a Server Component, fetches full action list, renders ActionsTableClient (will error until Task 2 creates the component — that is expected). No pre-filtering on server.</done>
</task>

<task type="auto">
  <name>Task 2: Build ActionsTableClient with table, filters, inline editing, and bulk bar</name>
  <files>
    bigpanda-app/components/ActionsTableClient.tsx
  </files>
  <action>
    Create `bigpanda-app/components/ActionsTableClient.tsx` as a 'use client' component. Implement in this order:

    1. FILTER BAR (above table):
       - Search input: onChange pushes `?q=` URL param; filters by description.toLowerCase().includes(q.toLowerCase())
       - Status chips (buttons for each of 4 statuses + "All"): pushes `?status=` URL param
       - Owner dropdown: `<select>` populated with unique owner values from the actions list prop; pushes `?owner=` URL param
       - Date range: two `<input type="date">` inputs for `?from=` and `?to=` URL params; filter by action.due_date within range
       - All filters use useMemo to derive filteredActions from the full list + current URL params

    2. BULK ACTION FLOATING BAR:
       - Show when selectedIds.size > 0: `<div>` with "N selected", status `<select>`, "Clear" button
       - Status select onChange: calls bulkUpdateStatus(selectedIds, newStatus)
       - Clear button: setSelectedIds(new Set())

    3. TABLE (shadcn Table):
       - Columns: [Checkbox] [ID] [Description] [Owner] [Due Date] [Status] [Source]
       - Header row with "select all" checkbox that toggles all filteredActions
       - Each row:
         - Checkbox cell: controlled by selectedIds Set, toggles id on change
         - ID cell: plain text (action.id)
         - Description cell: clicking opens ActionEditModal (retain for description editing per CONTEXT.md)
         - Owner cell: `<OwnerCell value={action.owner} projectId={projectId} onSave={async (v) => patchAction(action.id, { owner: v })} />`
         - Due Date cell: `<DatePickerCell value={action.due_date} onSave={async (v) => patchAction(action.id, { due_date: v })} />`
         - Status cell: `<InlineSelectCell value={normalisedStatus} options={ACTION_STATUS_OPTIONS} onSave={async (v) => patchAction(action.id, { status: v })} />`
         - Source cell: `<SourceBadge source={action.source} />` (existing component)

    4. PATCH HELPER:
       - async function patchAction(id, patch): fetch PATCH /api/actions/${id}, on error throw, router.refresh() on success
       - onSave callbacks are async — InlineSelectCell/DatePickerCell/OwnerCell handle optimistic revert + toast on rejection

    5. ACTION STATUS NORMALISATION:
       - const VALID_ACTION_STATUSES = ['open', 'in_progress', 'completed', 'cancelled']
       - Normalise to 'open' if value not in list (safe default for legacy data)

    Key constraints from CONTEXT.md (NON-NEGOTIABLE):
    - No row-level edit mode — each cell is independently clickable
    - Status, Owner, Due Date → inline only (no modal)
    - Description → ActionEditModal only
    - All filters → URL query params (shareable)
    - Bulk actions → Actions-only (no checkboxes on Risks/Milestones tables)
    - After save → router.refresh() (not full page reload)
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "ActionsTableClient|actions/page" | head -20</automated>
  </verify>
  <done>ActionsTableClient.tsx exists. TypeScript compilation clean for both files. Component imports InlineSelectCell, DatePickerCell, OwnerCell, ActionEditModal, shadcn Table, and Checkbox correctly. All 7 table columns present. Filter bar uses URL params. Floating bulk bar renders when selectedIds.size > 0.</done>
</task>

</tasks>

<verification>
1. TypeScript: `npx tsc --noEmit` clean for actions/page.tsx and ActionsTableClient.tsx
2. Manual: Start dev server, open any project's Actions tab — table renders with all 7 columns
3. Manual: Click a Status cell — inline select appears in-place (no modal)
4. Manual: Click a Due Date cell — calendar popover opens
5. Manual: Click a Description cell — ActionEditModal opens
6. Manual: Type in search box — URL updates to ?q=xxx, rows filter live
7. Manual: Select 2 rows — floating bulk bar appears
</verification>

<success_criteria>
Actions tab renders as a full table. All inline editing, filtering, search, and bulk selection work. No TypeScript errors. router.refresh() fires after every save.
</success_criteria>

<output>
After completion, create `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-04-SUMMARY.md`
</output>
