---
phase: 37-actions-inline-editing-foundation
plan: "02"
type: execute
wave: 1
depends_on:
  - "37-01"
files_modified:
  - bigpanda-app/package.json
  - bigpanda-app/package-lock.json
  - bigpanda-app/app/globals.css
  - bigpanda-app/components/InlineSelectCell.tsx
  - bigpanda-app/components/DatePickerCell.tsx
  - bigpanda-app/components/OwnerCell.tsx
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
  - ACTN-02
  - IEDIT-01
  - IEDIT-02

must_haves:
  truths:
    - "react-day-picker and @radix-ui/react-popover are installed and importable"
    - "InlineSelectCell renders a status badge and becomes a native <select> on click, saving via onSave callback optimistically"
    - "DatePickerCell renders a date string and opens a Radix Popover with DayPicker calendar on click, with Clear/TBD button"
    - "OwnerCell renders owner name and becomes a text input with <datalist> autocomplete on click, fetching stakeholders on first edit"
    - "All three components use sonner toast.error() for save failures and revert optimistic state"
  artifacts:
    - path: "bigpanda-app/components/InlineSelectCell.tsx"
      provides: "Generic inline select component for status and severity fields"
      exports: ["InlineSelectCell"]
    - path: "bigpanda-app/components/DatePickerCell.tsx"
      provides: "Date picker cell using react-day-picker + Radix Popover"
      exports: ["DatePickerCell"]
    - path: "bigpanda-app/components/OwnerCell.tsx"
      provides: "Owner text input with datalist autocomplete from stakeholders API"
      exports: ["OwnerCell"]
  key_links:
    - from: "DatePickerCell.tsx"
      to: "react-day-picker"
      via: "named import { DayPicker }"
      pattern: "import.*DayPicker.*from.*react-day-picker"
    - from: "DatePickerCell.tsx"
      to: "@radix-ui/react-popover"
      via: "namespace import * as Popover"
      pattern: "Popover.Root.*Popover.Trigger.*Popover.Content"
    - from: "OwnerCell.tsx"
      to: "/api/stakeholders"
      via: "fetch on edit focus"
      pattern: "fetch.*stakeholders.*project_id"
---

<objective>
Install required packages and build the three shared inline-edit components (`InlineSelectCell`, `DatePickerCell`, `OwnerCell`) that are reused across Actions, Risks, Milestones, and Tasks in later plans.

Purpose: These are the building blocks. Plans 04 and 05 import and wire these directly — no duplication.
Output: 3 new component files + packages installed + CSS loaded.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/PROJECT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/ROADMAP.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-CONTEXT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-RESEARCH.md

<interfaces>
<!-- Sonner toast — already in bigpanda-app/components/ContextTab.tsx -->
import { toast } from 'sonner'
// Usage: toast.error('Save failed — please try again')
// <Toaster> already mounted in app/layout.tsx

<!-- shadcn Table in bigpanda-app/components/ui/table.tsx -->
export { Table, TableBody, TableCell, TableHead, TableHeader, TableRow }

<!-- Existing stakeholders API (GET to be added in Plan 03, but OwnerCell must handle 404/405 gracefully until then) -->
GET /api/stakeholders?project_id={N} → [{ id: number, name: string, role: string }]

<!-- React-day-picker v9 — NAMED EXPORTS ONLY -->
import { DayPicker } from 'react-day-picker'  // NOT: import DayPicker from 'react-day-picker'
import 'react-day-picker/style.css'            // Load in globals.css @import, NOT in component

<!-- Radix Popover -->
import * as Popover from '@radix-ui/react-popover'
// Usage: <Popover.Root> <Popover.Trigger asChild> <Popover.Portal> <Popover.Content> <Popover.Arrow>

<!-- Existing useRouter + router.refresh() pattern -->
import { useRouter } from 'next/navigation'
const router = useRouter()
// router.refresh() after successful mutation — re-fetches server component data
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and wire CSS</name>
  <files>
    bigpanda-app/package.json
    bigpanda-app/package-lock.json
    bigpanda-app/app/globals.css
  </files>
  <action>
    Run: `cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npm install react-day-picker @radix-ui/react-popover`

    Then add the react-day-picker CSS import to `bigpanda-app/app/globals.css`. Add `@import 'react-day-picker/style.css';` as the FIRST line of globals.css (before any existing Tailwind directives).

    IMPORTANT CSS pitfall: The project uses Tailwind 4 with a custom PostCSS pipeline. If adding `@import` to globals.css causes a build error (run `cd bigpanda-app && npm run next-only` briefly to check), use the fallback: copy the minified CSS from `node_modules/react-day-picker/dist/style.css` to `bigpanda-app/public/rdp-style.css` and add `<link rel="stylesheet" href="/rdp-style.css" />` in `bigpanda-app/app/layout.tsx` (same pattern as frappe-gantt CSS). Do NOT proceed to Task 2 until the CSS loads without build errors.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && node -e "require('react-day-picker')" 2>&1 && node -e "require('@radix-ui/react-popover')" 2>&1 && echo "packages OK"</automated>
  </verify>
  <done>react-day-picker and @radix-ui/react-popover appear in package.json dependencies. CSS is loaded via globals.css @import or layout.tsx link without build error. `npm run next-only` starts cleanly (no PostCSS errors).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build InlineSelectCell, DatePickerCell, and OwnerCell components</name>
  <files>
    bigpanda-app/components/InlineSelectCell.tsx
    bigpanda-app/components/DatePickerCell.tsx
    bigpanda-app/components/OwnerCell.tsx
  </files>
  <behavior>
    InlineSelectCell:
    - Renders optimisticValue as a styled span (cursor-pointer, hover:bg-zinc-100)
    - On click: swaps to a native <select> with autoFocus showing all options
    - onChange: optimistic update → setEditing(false) → await onSave(newValue)
    - On onSave rejection: revert optimisticValue → toast.error('Save failed — please try again')
    - Props: value: T, options: { value: T; label: string }[], onSave: (v: T) => Promise<void>, className?: string
    - Generic over T extends string

    DatePickerCell:
    - Renders date display (value ?? '—') as a Popover.Trigger span
    - Popover opens on click, contains <DayPicker mode="single" selected={parsedDate} onSelect={handleDaySelect} />
    - "Clear / TBD" button below calendar: sets value to null via onSave(null)
    - Selecting a date: optimistic update → close popover → await onSave(isoDate)
    - On onSave rejection: revert → toast.error
    - Props: value: string | null, onSave: (isoDate: string | null) => Promise<void>
    - Use named import: import { DayPicker } from 'react-day-picker'

    OwnerCell:
    - Renders optimisticValue (or '—') as a clickable span
    - On click: setEditing(true) — also triggers useEffect to fetch stakeholders if not yet loaded
    - Renders <datalist id={datalistId}> with stakeholder names + <input type="text" list={datalistId} autoFocus defaultValue={optimisticValue}>
    - onBlur: optimistic update → setEditing(false) → await onSave(newValue)
    - On onSave rejection: revert → toast.error
    - Props: value: string | null, projectId: number, onSave: (owner: string) => Promise<void>
    - Use useId() for datalistId (avoids collisions when multiple cells on screen)
    - Fetch from /api/stakeholders?project_id=${projectId} on first edit focus (empty datalist is acceptable fallback per CONTEXT.md)
  </behavior>
  <action>
    Implement all three components exactly per the RESEARCH.md code examples (Pattern 2, 3, 4). Key rules:
    - Use 'use client' directive at top of each file
    - Use native <select> for InlineSelectCell (NOT Radix Select — per RESEARCH.md anti-patterns)
    - Use named DayPicker import (NOT default import — pitfall 3 in RESEARCH.md)
    - Use useId() from React for datalist id uniqueness in OwnerCell
    - Each component is self-contained — owns its own editing boolean, optimisticValue state
    - No row-level edit state — each cell is independent (anti-pattern per CONTEXT.md)
    - Add a `saving` boolean to InlineSelectCell and OwnerCell to prevent double-save on rapid clicks
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "InlineSelectCell|DatePickerCell|OwnerCell" | head -20</automated>
  </verify>
  <done>All 3 component files exist. TypeScript compilation reports zero errors in these files. Each file exports its named component. Components match the RESEARCH.md patterns exactly.</done>
</task>

</tasks>

<verification>
1. `npm install` completed — react-day-picker and @radix-ui/react-popover in package.json
2. CSS loads without PostCSS build error
3. All 3 component files exist with correct exports
4. `npx tsc --noEmit` passes for the 3 new component files
</verification>

<success_criteria>
Packages installed. CSS loads. InlineSelectCell, DatePickerCell, OwnerCell are TypeScript-valid, exported, and follow the RESEARCH.md patterns exactly. Ready for Plans 04 and 05 to import and wire.
</success_criteria>

<output>
After completion, create `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-02-SUMMARY.md`
</output>
