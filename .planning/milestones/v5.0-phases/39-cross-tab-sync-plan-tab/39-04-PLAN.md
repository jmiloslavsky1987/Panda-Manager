---
phase: 39-cross-tab-sync-plan-tab
plan: 04
type: execute
wave: 3
depends_on:
  - "39-02"
  - "39-03"
files_modified: []
autonomous: false
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - PLAN-01
  - PLAN-02

must_haves:
  truths:
    - "All 5 phase requirements verified working end-to-end in the browser"
    - "Automated test suite is fully GREEN before verification begins"
    - "No regressions in previously passing tests"
  artifacts:
    - path: "bigpanda-app/tests/sync/metrics-invalidate.test.tsx"
      provides: "GREEN automated coverage for SYNC-01"
      exports: []
    - path: "bigpanda-app/tests/sync/chart-drill-down.test.tsx"
      provides: "GREEN automated coverage for SYNC-02"
      exports: []
    - path: "bigpanda-app/tests/sync/active-blockers.test.tsx"
      provides: "GREEN automated coverage for SYNC-03"
      exports: []
    - path: "bigpanda-app/tests/plan/overdue-visual.test.tsx"
      provides: "GREEN automated coverage for PLAN-01"
      exports: []
    - path: "bigpanda-app/tests/plan/bulk-status.test.tsx"
      provides: "GREEN automated coverage for PLAN-02"
      exports: []
  key_links:
    - from: "bigpanda-app/components/ActionsTableClient.tsx"
      to: "bigpanda-app/components/OverviewMetrics.tsx"
      via: "metrics:invalidate CustomEvent triggers in-place re-fetch"
      pattern: "metrics:invalidate"
---

<objective>
Run the full automated test suite and perform a structured 12-step UI verification of all Phase 39 requirements. Human confirms each behavior is working as specified.

Purpose: Phase gate — confirms all 5 requirements work end-to-end before the phase is marked complete.
Output: User confirmation that Phase 39 is ship-ready.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-cross-tab-sync-plan-tab/39-02-SUMMARY.md
@.planning/phases/39-cross-tab-sync-plan-tab/39-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite and TypeScript check</name>
  <files></files>
  <action>
Run the full test suite and TypeScript compiler check. Fix any failures before proceeding to the human verification checkpoint.

1. Run full suite: `cd bigpanda-app && npm test -- --run --reporter=verbose`
   - Expected: All 5 new test files (10+ tests) are GREEN
   - Expected: All pre-existing tests remain in their previous state (no regressions)

2. Run TypeScript check: `cd bigpanda-app && npx tsc --noEmit`
   - Expected: No new errors introduced by Phase 39 changes

If tests fail: diagnose, fix the implementation (not the tests unless a test has a genuine bug), re-run.

Report the test results summary before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>All tests GREEN; tsc --noEmit exits with no errors; test output shows 0 failing tests</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all 5 requirements end-to-end</name>
  <files></files>
  <action>
Present the 12-step verification checklist below to the user. Wait for the user to complete each step manually in their browser. Resume only after the user types "approved" or reports issues.
  </action>
  <what-built>
Phase 39 — Cross-Tab Sync and Plan Tab improvements:

SYNC-01: Editing a Risk, Action, or Milestone dispatches `metrics:invalidate` custom event; OverviewMetrics and HealthDashboard re-fetch in place (no loading spinner).

SYNC-02: Risk distribution pie chart segments are clickable (cursor: pointer); clicking navigates to `/customer/[id]/risks?severity=[value]`; Risks tab filters to that severity on load.

SYNC-03: HealthDashboard Active Blockers section shows a list of blocked task titles (not a count), each linking to `/customer/[id]/plan/tasks`. Truncates at 5 with "and N more" message.

PLAN-01: TaskCard and PhaseCard show red border + light red background (`border-red-500 bg-red-50`) for overdue tasks (due date in past AND status not done).

PLAN-02: TaskBoard BulkToolbar has a "Change Status" mode with 4 options. PhaseBoard has checkboxes on each card; selecting 2+ shows a bulk status toolbar.
  </what-built>
  <how-to-verify>
Start the dev server: `cd bigpanda-app && npm run next-only`

Open a project with existing Risks, Actions, Milestones, and Tasks in your browser.

**SYNC-01 — Metrics invalidation (3 steps):**

Step 1. Navigate to the Overview tab. Note the current risk count or task completion percentage displayed in OverviewMetrics.

Step 2. Open a new browser tab to the same project's Risks tab. Change a risk's status via inline edit (click the status cell, select a new value). Save the edit.

Step 3. Switch back to the Overview tab. Expected: The metrics numbers update in place within 1-2 seconds without any page navigation, loading spinner, or manual refresh. Verify via browser DevTools Network tab if needed.

**SYNC-02 — Chart drill-down (2 steps):**

Step 4. On the Overview tab, locate the Risk Distribution pie chart. Hover over a colored segment. Expected: Cursor changes to a pointer (hand cursor).

Step 5. Click a segment (e.g., the "high" severity segment). Expected: Browser navigates to the Risks tab with URL containing `?severity=high`. The Risks table shows only risks with that severity.

**SYNC-03 — Active blockers list (2 steps):**

Step 6. On the Overview tab, find the HealthDashboard section. Expected: The "Active Blockers" area shows a list of task titles (not a count number). Each title is a clickable link.

Step 7. Click one of the blocked task links. Expected: Navigates to the Task Board page (`/customer/[id]/plan/tasks`). If no tasks are blocked, the section says "No blocked tasks".

**PLAN-01 — Overdue highlighting (2 steps):**

Step 8. Navigate to Plan tab — Task Board. Find tasks with a past due date that are not marked "Done". Expected: Those task cards have a red border and light red background. Done tasks (even with past dates) are NOT highlighted.

Step 9. Navigate to Plan tab — Phase Board. Expected: Same red highlighting for overdue phase-level items.

**PLAN-02 — Bulk status update (3 steps):**

Step 10. On the Task Board, check the checkbox on 2 or more tasks. Expected: A bulk action toolbar appears with multiple action buttons including "Change Status".

Step 11. Click "Change Status". Expected: A dropdown appears with 4 options: To Do, In Progress, Blocked, Done. Select one and click Apply. Expected: The selected tasks update to that status; the toolbar disappears.

Step 12. Navigate to Phase Board. Check 2 or more phase card checkboxes. Expected: A bulk toolbar appears with a status-only dropdown. Select a status and click Apply. Expected: Those phases update; toolbar clears.
  </how-to-verify>
  <resume-signal>Type "approved" if all 12 steps pass, or describe any issues found for fixing</resume-signal>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; automated verification was completed in Task 1</automated>
  </verify>
  <done>User types "approved" confirming all 12 verification steps pass</done>
</task>

</tasks>

<verification>
All automated tests GREEN before checkpoint.
Human verifies 12 steps covering all 5 requirements.
</verification>

<success_criteria>
- [ ] Full test suite GREEN (0 failing tests)
- [ ] TypeScript check clean (0 new errors)
- [ ] Human verifies SYNC-01: metrics update in-place after entity edit
- [ ] Human verifies SYNC-02: pie chart segments clickable + navigate to filtered Risks tab
- [ ] Human verifies SYNC-03: blocked tasks list with links in HealthDashboard
- [ ] Human verifies PLAN-01: red card styling on overdue tasks in both boards
- [ ] Human verifies PLAN-02: bulk status update functional on TaskBoard and PhaseBoard
- [ ] User types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/39-cross-tab-sync-plan-tab/39-04-SUMMARY.md`
</output>
