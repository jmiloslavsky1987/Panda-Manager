---
phase: 42-ingestion-field-coverage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bigpanda-app/tests/ingestion/write.test.ts
  - bigpanda-app/tests/ingestion/extraction-job.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Vitest test suite has RED failing cases for every new behavior before implementation begins"
    - "coerceRiskSeverity mapping cases are defined and failing (function not yet implemented)"
    - "insertItem risk/task/milestone test cases assert severity/dates/FKs and fail (fields not yet written)"
    - "mergeItem fill-null-only guards are specified and failing (fields not yet guarded)"
    - "Cross-entity resolution test cases exist and fail (resolveEntityRef not yet implemented)"
    - "Unresolved ref description append cases exist and fail"
    - "unresolvedRefs response field case exists and fails"
    - "Extraction prompt field coverage cases exist and fail"
  artifacts:
    - path: "bigpanda-app/tests/ingestion/write.test.ts"
      provides: "Failing tests for approve route new behaviors"
      contains: "coerceRiskSeverity"
    - path: "bigpanda-app/tests/ingestion/extraction-job.test.ts"
      provides: "Failing tests for extraction prompt field coverage"
      contains: "milestone_name"
  key_links:
    - from: "bigpanda-app/tests/ingestion/write.test.ts"
      to: "bigpanda-app/app/api/ingestion/approve/route.ts"
      via: "POST handler import"
      pattern: "import.*POST.*approve/route"
    - from: "bigpanda-app/tests/ingestion/extraction-job.test.ts"
      to: "bigpanda-app/worker/jobs/document-extraction.ts"
      via: "processDocumentExtraction import"
      pattern: "import.*processDocumentExtraction"
---

<objective>
Extend existing ingestion test files with failing RED test cases for every new behavior introduced in Phase 42. This is Nyquist Wave 0 compliance — tests define the contracts before implementation begins.

Purpose: Ensure no implementation ships without a failing test first. All new behaviors have an automated verification gate.
Output: Extended `write.test.ts` with 9 new failing test cases; extended `extraction-job.test.ts` with new prompt-field assertion cases.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-ingestion-field-coverage/42-CONTEXT.md
@.planning/phases/42-ingestion-field-coverage/42-RESEARCH.md
@bigpanda-app/tests/ingestion/write.test.ts
@bigpanda-app/tests/ingestion/extraction-job.test.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend write.test.ts with RED cases for approve route new behaviors</name>
  <files>bigpanda-app/tests/ingestion/write.test.ts</files>
  <behavior>
    - coerceRiskSeverity: 'critical' → 'critical', 'crit' → 'critical', 'high' → 'high', 'medium' → 'medium', 'med' → 'medium', 'moderate' → 'medium', 'low' → 'low', 'minor' → 'low', '' → 'medium', undefined → 'medium', 'nonsense' → 'medium'
    - insertItem('risk'): POST with approved risk item having f.severity='high' must call db.insert().values() with severity field present
    - insertItem('task'): POST with approved task having start_date, due_date, description, priority, milestone_name, workstream_name fields — inserted row must include start_date, due, description, priority in values
    - insertItem('milestone'): POST with approved milestone having owner — inserted row must include owner field
    - mergeItem('risk') fill-null-only: when beforeRecord.severity is 'high' (non-null) → patch does NOT update severity; when beforeRecord.severity is null → patch includes coerced severity
    - mergeItem('task') fill-null-only: when beforeRecord.start_date is non-null → patch omits start_date; when null → patch includes start_date from extraction
    - Cross-entity resolution (resolveEntityRef): when db.select returns exactly 1 milestone → milestone_id FK is set on inserted task; when db.select returns 0 → milestone_id is null and "Milestone ref: [name]" appended to description; when db.select returns 2+ → milestone_id is null
    - Unresolved ref description: both milestone and workstream unresolved → description includes "Milestone ref: [m] | Workstream ref: [w]"
    - unresolvedRefs in API response: when at least one task had unresolved milestone ref → response JSON includes unresolvedRefs string (not null)
  </behavior>
  <action>
    Append a new describe block `describe('Phase 42 — new field coverage', () => { ... })` to `bigpanda-app/tests/ingestion/write.test.ts`.

    IMPORTANT: These tests must fail RED when first run — do not import any function that does not yet exist. Import the POST handler (already imported in the file) and use the existing mock infrastructure.

    For coerceRiskSeverity tests: the function is not exported from the route — test it indirectly via POST endpoint behavior (insert values call receives coerced severity). If direct export becomes available, update later.

    For insertItem tests: construct minimal approved items and assert on `mockValues.mock.calls[0][0]` fields after POST call. The mock DB infrastructure already exists in the file header — follow the same stub pattern used in the existing ING-09 tests (lines 102-148).

    For mergeItem fill-null-only tests: stub `db.select` to return a beforeRecord with a non-null field (e.g., `severity: 'high'`), trigger a conflict by making the conflict-check select return the same existing id, then verify the `db.update().set()` mock was called WITHOUT the severity field in the patch object.

    For resolveEntityRef tests: stub `db.select` to return 1 row / 0 rows / 2 rows for the milestone lookup query, then verify the final task insert values include or omit `milestone_id` accordingly, and verify description append behavior.

    All test cases MUST fail on first run. They will be marked RED in VALIDATION.md. Do not implement any production code in this task.

    Run: `cd bigpanda-app && npx vitest run tests/ingestion/write.test.ts` — expect failures.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/ingestion/write.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>New describe block present in write.test.ts with 9+ new it() cases. Test run shows some FAIL results (RED) confirming tests are not trivially passing. No syntax errors preventing execution.</done>
</task>

<task type="tdd">
  <name>Task 2: Extend extraction-job.test.ts with RED cases for prompt field coverage</name>
  <files>bigpanda-app/tests/ingestion/extraction-job.test.ts</files>
  <behavior>
    - Extraction prompt EXTRACTION_SYSTEM constant contains "milestone_name" for task entity guidance
    - Extraction prompt EXTRACTION_SYSTEM constant contains "workstream_name" for task entity guidance
    - Extraction prompt EXTRACTION_SYSTEM constant contains "start_date" for task entity guidance
    - Extraction prompt EXTRACTION_SYSTEM constant contains "due_date" for task entity guidance
    - Extraction prompt EXTRACTION_SYSTEM constant contains "priority" for task entity guidance
    - Extraction prompt EXTRACTION_SYSTEM constant contains "description" for task entity guidance
    - Extraction prompt EXTRACTION_SYSTEM constant contains "owner" for milestone entity guidance
    - Extraction prompt EXTRACTION_SYSTEM constant contains "verbatim" instruction (per locked decisions)
  </behavior>
  <action>
    Append a new describe block `describe('Phase 42 — extraction prompt field coverage', () => { ... })` to `bigpanda-app/tests/ingestion/extraction-job.test.ts`.

    The existing test file imports `processDocumentExtraction` from `../../worker/jobs/document-extraction`. The `EXTRACTION_SYSTEM` constant is defined in the same file. To test it, export `EXTRACTION_SYSTEM` from `document-extraction.ts` as a named export (or test via the processDocumentExtraction behavior, but string inspection is simpler).

    Add test cases that import the `EXTRACTION_SYSTEM` string from the worker and assert the expected substrings are present. Example:

    ```typescript
    import { EXTRACTION_SYSTEM } from '../../worker/jobs/document-extraction';

    describe('Phase 42 — extraction prompt field coverage', () => {
      it('task guidance includes milestone_name', () => {
        expect(EXTRACTION_SYSTEM).toContain('milestone_name');
      });
      // ...one it() per field
    });
    ```

    If `EXTRACTION_SYSTEM` is not currently exported, the import will fail and the tests will be RED — which is the desired Wave 0 state.

    Run: `cd bigpanda-app && npx vitest run tests/ingestion/extraction-job.test.ts` — expect failures (import error or assertion failures).
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/ingestion/extraction-job.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>New describe block in extraction-job.test.ts with 8 it() cases for prompt field presence. Tests are RED (import error or assertion fails). No syntax errors.</done>
</task>

</tasks>

<verification>
Run full ingestion suite after both tasks: `cd bigpanda-app && npx vitest run tests/ingestion/` — must complete without syntax errors, with new test cases present and failing RED.
</verification>

<success_criteria>
- write.test.ts has a new describe block with 9+ failing test cases for Phase 42 behaviors
- extraction-job.test.ts has a new describe block with 8 failing test cases for prompt coverage
- Both files run without syntax errors (failures are assertion/import failures, not parse errors)
- The failing tests define the exact contracts that Plans 02, 03, and 04 will implement against
</success_criteria>

<output>
After completion, create `.planning/phases/42-ingestion-field-coverage/42-01-SUMMARY.md`
</output>
