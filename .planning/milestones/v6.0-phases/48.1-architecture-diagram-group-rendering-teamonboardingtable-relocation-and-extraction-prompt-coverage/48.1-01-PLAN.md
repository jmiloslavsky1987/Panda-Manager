---
phase: 48.1-architecture-diagram-group-rendering-teamonboardingtable-relocation-and-extraction-prompt-coverage
plan: "01"
subsystem: architecture-visualization, teams-tab, extraction-pipeline
requirements: [ARCH-04, TEAM-03, TEAM-04]
wave_strategy: sequential
---

# Phase 48.1 Plan 01: Architecture Diagram Improvements + TeamOnboardingTable Relocation + Extraction Prompt Coverage

## Objective

Three coordinated improvements after Phase 48 UAT review:
1. Architecture diagram gets `integration_group` column for grouped rendering (dashed boxes), wider columns (220px), and phase-aware color strips
2. TeamOnboardingTable moves from Architecture tab to Teams tab Section 4
3. Extraction prompt updated to capture all new entity fields (integration_group, onboarding status fields, focus_area, e2e_workflow)
4. Comprehensive seed data for AMEX architecture integrations and E2E workflows

## Tasks

### Wave 1: Database + Schema

1. Add `integration_group` column to `architecture_integrations` (migration + schema)
2. Update POST/PATCH API routes to accept `integration_group`
3. Update IntegrationEditModal with Group field

### Wave 2: Visual + Layout

4. InteractiveArchGraph: wider columns (220px), group rendering (dashed boxes), phase-aware column header colors
5. Move TeamOnboardingTable from CurrentFutureStateTab → TeamEngagementMap (Section 4)
6. Remove `onOnboardingUpdate` prop from WorkflowDiagram/CurrentFutureStateTab chain

### Wave 3: Data + Extraction

7. Seed comprehensive AMEX architecture integrations (37 entries, with groups)
8. Seed AMEX E2E workflows derived from team_onboarding_status
9. Update extraction prompt: add `integration_group`, onboarding status fields, `focus_area`, `e2e_workflow` entity types
10. Update Phase 50 ROADMAP goal with 6 extraction commit handler gaps

## Verification

- Architecture tab: group boxes render, wider columns, phase colors
- Teams tab: Section 4 shows TeamOnboardingTable + team cards; Section 3 shows E2E workflows
- Modal: Group field present
- No TypeScript errors
