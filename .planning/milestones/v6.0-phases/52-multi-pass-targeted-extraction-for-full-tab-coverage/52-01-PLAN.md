---
phase: 52-multi-pass-targeted-extraction-for-full-tab-coverage
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/worker/jobs/__tests__/document-extraction-passes.test.ts
  - bigpanda-app/worker/jobs/__tests__/document-extraction-dedup.test.ts
  - bigpanda-app/__tests__/ingestion-modal-pass-progress.test.ts
autonomous: true
requirements:
  - MULTI-PASS-01
  - MULTI-PASS-02
  - MULTI-PASS-03

must_haves:
  truths:
    - "Three test files exist before any implementation begins (Wave 0 RED)"
    - "Pass structure test stubs document 3-pass PDF path, 3-pass text path, pass prompt isolation, global progress scale, and merge behavior"
    - "Dedup test stubs document same-type dedup removal and cross-type preservation"
    - "IngestionModal source inspection test stub documents PASS_LABELS constant and pass-index math requirements"
  artifacts:
    - path: "bigpanda-app/worker/jobs/__tests__/document-extraction-passes.test.ts"
      provides: "RED stubs: pass prompts isolation, PDF 3 passes, text 3 passes, progress global scale, merge all passes, isAlreadyIngested import"
    - path: "bigpanda-app/worker/jobs/__tests__/document-extraction-dedup.test.ts"
      provides: "RED stubs: intra-batch dedup same-type removal, cross-type preservation, key field coverage per entity type"
    - path: "bigpanda-app/__tests__/ingestion-modal-pass-progress.test.ts"
      provides: "RED source-inspection stub: PASS_LABELS and pass-index math in IngestionModal.tsx"
  key_links:
    - from: "document-extraction-passes.test.ts"
      to: "worker/jobs/document-extraction.ts"
      via: "import of PASS_PROMPTS, ExtractionPass, deduplicateWithinBatch"
      pattern: "import.*document-extraction"
    - from: "ingestion-modal-pass-progress.test.ts"
      to: "components/IngestionModal.tsx"
      via: "fs.readFileSync source inspection"
      pattern: "PASS_LABELS"
---

<objective>
Create Wave 0 RED test stubs that document the exact behaviors Phase 52 must implement. All three test files will fail (RED) because the implementation does not yet exist. They establish the behavioral contract that Wave 1 and Wave 2 plans must satisfy.

Purpose: Test-first discipline — write the contract before implementation so that GREEN = done. Following the established Wave 0 TDD pattern from Phases 46, 47, 48, 49, 50, and 51.
Output: Three RED test files covering all Phase 52 behaviors.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-multi-pass-targeted-extraction-for-full-tab-coverage/52-CONTEXT.md
@.planning/phases/52-multi-pass-targeted-extraction-for-full-tab-coverage/52-RESEARCH.md
</context>

<interfaces>
<!-- Key types from worker/jobs/document-extraction.ts (lines 83–111) -->

```typescript
export type EntityType =
  | 'action' | 'risk' | 'decision' | 'milestone' | 'stakeholder'
  | 'task' | 'architecture' | 'history' | 'businessOutcome' | 'team'
  | 'note' | 'team_pathway' | 'workstream' | 'onboarding_step' | 'integration'
  | 'wbs_task' | 'arch_node' | 'focus_area' | 'e2e_workflow'
  | 'before_state' | 'weekly_focus';

export interface ExtractionItem {
  entityType: EntityType;
  fields: Record<string, string>;
  confidence: number;
  sourceExcerpt: string;
}
```

<!-- Pass structure that Plan 02 will implement (from RESEARCH.md) -->
<!-- PASS_PROMPTS[1]: action, risk, task, milestone, decision, note, history -->
<!-- PASS_PROMPTS[2]: architecture, arch_node, integration, before_state -->
<!-- PASS_PROMPTS[3]: team, wbs_task, workstream, focus_area, e2e_workflow, team_pathway, weekly_focus, stakeholder, businessOutcome, onboarding_step -->

<!-- Intra-batch dedup key fields (from RESEARCH.md): -->
<!-- action → description | risk → description | milestone → name | decision → decision -->
<!-- history/note → content ?? context | stakeholder → email ?? name | task → title -->
<!-- businessOutcome → title | team → team_name | architecture → tool_name -->
<!-- workstream → name | onboarding_step → step_name | integration → tool_name -->
<!-- wbs_task → title+track (composite) | arch_node → node_name+track (composite) -->
<!-- focus_area → title | e2e_workflow → workflow_name+team_name (composite) -->
<!-- team_pathway → team_name | before_state → aggregation_hub_name -->
<!-- weekly_focus → skip dedup (singletons) -->

<!-- Global progress scale (from RESEARCH.md): -->
<!-- Pass 1: 0–33% | Pass 2: 34–66% | Pass 3: 67–100% -->
<!-- Formula: globalPct = Math.round((passIndex / 3) * 100 + (passProgressPct / 3)) -->

<!-- IngestionModal current message logic (lines 115–121 of IngestionModal.tsx): -->
```typescript
if (total_chunks > 0) {
  setExtractionMessage(`${progress_pct}% — Processing chunk ${current_chunk} of ${total_chunks}`)
} else {
  setExtractionMessage(`Extracting ${file.name}...`)
}
```
<!-- Target message format: "Pass N of 3 — Label (withinPassPct%)" -->
<!-- PASS_LABELS = ['Project data', 'Architecture', 'Teams & delivery'] -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 stub — document-extraction-passes.test.ts</name>
  <files>bigpanda-app/worker/jobs/__tests__/document-extraction-passes.test.ts</files>
  <behavior>
    - Test "pass prompts contain only their entity types (no cross-pass types)": imports PASS_PROMPTS from worker; asserts PASS_PROMPTS[1] contains 'action' but not 'arch_node'; PASS_PROMPTS[2] contains 'arch_node' but not 'action'; PASS_PROMPTS[3] contains 'wbs_task' but not 'risk' — RED because PASS_PROMPTS does not exist yet
    - Test "PDF 3 passes": mocks runClaudeCall; runs PDF extraction; asserts runClaudeCall called 3 times — RED because 3-pass loop does not exist yet
    - Test "text 3 passes": mocks runClaudeCall; runs text chunk extraction; asserts runClaudeCall called 3 * chunkCount times — RED because 3-pass loop does not exist yet
    - Test "merge all passes before staging": asserts allRawItems is a merge of items from all 3 passes before isAlreadyIngested — RED because merge logic not yet implemented
    - Test "progress global scale": asserts that after pass 1 completion progress_pct <= 33; after pass 2 completion progress_pct <= 66; after pass 3 progress_pct = 100 — RED because global scale not yet implemented
    - Test "isAlreadyIngested import from lib": imports isAlreadyIngested from lib/extraction-types; asserts it is a function — this should turn GREEN in Wave 1 when the import is added
  </behavior>
  <action>
    Create `bigpanda-app/worker/jobs/__tests__/document-extraction-passes.test.ts`.

    Use Vitest (vi.mock, vi.fn, describe, it, expect). All tests MUST be RED on creation — do not implement any production code.

    Structure:
    1. Import `PASS_PROMPTS` from `'../document-extraction'` (will fail until Plan 02 exports it) — wrap in try/catch or use dynamic import to control the failure mode gracefully
    2. Mock `@anthropic-ai/sdk` with a streaming client stub that returns a static JSON array string per call
    3. Mock `../../db` and `../../lib/document-extractor` and `../../lib/settings-core` as needed for test isolation
    4. Write all 6 test stubs listed in the behavior section. Each test should be a clear stub that fails with a descriptive message (not just `expect(true).toBe(false)`)

    Key: test "isAlreadyIngested import from lib" imports from `../../lib/extraction-types` and asserts `typeof isAlreadyIngested === 'function'` — this will be the first test to turn GREEN in Wave 1.

    Note: The test file must be in the `worker/jobs/__tests__/` directory. Confirm that directory exists or create it — Phase 51 created `worker/jobs/__tests__/` in Plan 51-01 so it should exist.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run worker/jobs/__tests__/document-extraction-passes.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>File exists at `worker/jobs/__tests__/document-extraction-passes.test.ts`. Tests run (do not skip/error on import). Tests fail RED on the core assertions. "isAlreadyIngested import from lib" may be GREEN (lib export already exists).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 stub — document-extraction-dedup.test.ts</name>
  <files>bigpanda-app/worker/jobs/__tests__/document-extraction-dedup.test.ts</files>
  <behavior>
    - Test "intra-batch dedup removes same entityType+key duplicates": calls deduplicateWithinBatch with two 'action' items sharing description; asserts result has length 1 — RED because deduplicateWithinBatch does not exist yet
    - Test "cross-type preserved: same key under different entityTypes is kept": calls deduplicateWithinBatch with one 'action' item and one 'workstream' item both having the same name value; asserts result has length 2 — RED because deduplicateWithinBatch does not exist yet
    - Test "entity key field coverage — composite keys": tests wbs_task deduplication uses title+track composite; tests e2e_workflow uses workflow_name+team_name composite; tests arch_node uses node_name+track composite — RED
    - Test "weekly_focus passes through without dedup": two weekly_focus items always produce 2 items (singletons, no dedup key) — RED
    - Test "unkeyed items (missing primary field) pass through": item with null/empty description field passes through without filtering — RED
  </behavior>
  <action>
    Create `bigpanda-app/worker/jobs/__tests__/document-extraction-dedup.test.ts`.

    Import `deduplicateWithinBatch` from `'../document-extraction'` (will fail until Plan 02 exports it).

    Write 5 test stubs from the behavior section. Use inline ExtractionItem objects to keep tests self-contained. Key structure for test items:
    ```typescript
    const makeItem = (entityType: EntityType, fields: Record<string, string>): ExtractionItem => ({
      entityType, fields, confidence: 0.9, sourceExcerpt: 'test'
    })
    ```

    All tests RED until Plan 02 implements `deduplicateWithinBatch`. Tests must compile (use type assertions if needed for type-narrowing on the import) so they fail on runtime assertions, not on import errors.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run worker/jobs/__tests__/document-extraction-dedup.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>File exists at `worker/jobs/__tests__/document-extraction-dedup.test.ts`. Tests run and fail RED on assertions. No syntax errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 stub — ingestion-modal-pass-progress.test.ts</name>
  <files>bigpanda-app/__tests__/ingestion-modal-pass-progress.test.ts</files>
  <behavior>
    - Test "PASS_LABELS constant exists in IngestionModal.tsx": reads IngestionModal.tsx source as string; asserts source contains 'PASS_LABELS' — RED because constant not yet added
    - Test "pass-index math for progress_pct <= 33 produces Pass 1": reads source; asserts formula for passIdx when progress_pct <= 33 returns index 0 — RED because math not yet added
    - Test "pass-aware message format string present": reads source; asserts source contains the pattern `Pass \${passNum} of 3` or similar — RED because format string not yet added
  </behavior>
  <action>
    Create `bigpanda-app/__tests__/ingestion-modal-pass-progress.test.ts`.

    Use the source inspection pattern established in Phase 48 Plan 02 and Phase 51 Plan 04: read the component source file with `fs.readFileSync`, then assert on string content.

    ```typescript
    import { readFileSync } from 'fs'
    import { resolve } from 'path'
    import { describe, it, expect } from 'vitest'

    const modalSrc = readFileSync(
      resolve(__dirname, '../components/IngestionModal.tsx'),
      'utf-8'
    )
    ```

    Write 3 source inspection tests. Each asserts on the raw string content of `IngestionModal.tsx`. All will be RED until Plan 03 adds the pass-aware message logic.

    Place this file at `bigpanda-app/__tests__/ingestion-modal-pass-progress.test.ts` (same directory as other `__tests__/` files in the app root).
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run "__tests__/ingestion-modal-pass-progress.test.ts" --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>File exists at `bigpanda-app/__tests__/ingestion-modal-pass-progress.test.ts`. Tests run and fail RED on source assertions. File at `components/IngestionModal.tsx` is found by readFileSync (path is correct).</done>
</task>

</tasks>

<verification>
After all 3 tasks:
```
cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run worker/jobs/__tests__/document-extraction-passes.test.ts worker/jobs/__tests__/document-extraction-dedup.test.ts __tests__/ingestion-modal-pass-progress.test.ts --reporter=verbose 2>&1 | tail -40
```

Expected result: Tests run (not skipped, not compilation errors). Most fail RED. The "isAlreadyIngested import from lib" test in document-extraction-passes may be GREEN since the lib export exists.
</verification>

<success_criteria>
- Three test files exist in correct directories
- All tests run (no syntax or import errors that prevent execution)
- Core behavioral assertions fail RED — the implementation does not exist yet
- Wave 0 nyquist compliance: every test in VALIDATION.md has a corresponding RED stub here
</success_criteria>

<output>
After completion, create `.planning/phases/52-multi-pass-targeted-extraction-for-full-tab-coverage/52-01-SUMMARY.md`
</output>
