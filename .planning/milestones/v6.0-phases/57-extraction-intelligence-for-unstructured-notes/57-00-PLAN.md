---
phase: 57-extraction-intelligence-for-unstructured-notes
plan: "00"
type: tdd
wave: 1
depends_on: []
files_modified:
  - bigpanda-app/tests/ingestion/extraction-prompts.test.ts
autonomous: true
requirements: [SYNTH-01, SYNTH-02, SYNTH-03, SYNTH-04, SYNTH-05]

must_haves:
  truths:
    - "8 new RED test stubs exist in extraction-prompts.test.ts documenting Phase 57 behavioral contracts"
    - "All 8 stubs fail with clear failure messages (no syntax errors, no import failures)"
    - "Existing 12 Phase 53 tests remain GREEN (no regressions)"
  artifacts:
    - path: "bigpanda-app/tests/ingestion/extraction-prompts.test.ts"
      provides: "RED stubs for SYNTH-01 through SYNTH-05 behavioral contracts"
      contains: "SYNTH-01"
  key_links:
    - from: "bigpanda-app/tests/ingestion/extraction-prompts.test.ts"
      to: "bigpanda-app/worker/jobs/document-extraction.ts"
      via: "import { EXTRACTION_BASE, PASS_PROMPTS, PASS_0_PROMPT }"
      pattern: "import.*PASS_0_PROMPT.*document-extraction"
---

<objective>
Create Wave 0 RED test stubs for Phase 57 synthesis-first extraction contracts.

Purpose: Establish behavioral contracts as failing tests before any prompt changes. Tests document exactly what must be true about the new prompts, enabling TDD-style verification in Plan 57-01.

Output: 8 new RED stubs appended to extraction-prompts.test.ts, covering SYNTH-01 through SYNTH-05. Existing 12 GREEN tests untouched.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-extraction-intelligence-for-unstructured-notes/57-CONTEXT.md
@.planning/phases/57-extraction-intelligence-for-unstructured-notes/57-RESEARCH.md

# Existing test file — append to this, do NOT replace
@bigpanda-app/tests/ingestion/extraction-prompts.test.ts

# Export surface being tested — PASS_0_PROMPT is NOT currently exported
@bigpanda-app/worker/jobs/document-extraction.ts
</context>

<feature>
  <name>Phase 57 RED Test Stubs</name>
  <files>bigpanda-app/tests/ingestion/extraction-prompts.test.ts, bigpanda-app/worker/jobs/document-extraction.ts</files>
  <behavior>
    SYNTH-01: EXTRACTION_BASE contains global inference posture instruction
      - EXTRACTION_BASE must contain language like "unstructured" AND ("infer" OR "inference")
      - RED: current EXTRACTION_BASE says "extract all structured project data" — no inference posture
    SYNTH-02: PASS_0_PROMPT outputs document_type XML tag
      - PASS_0_PROMPT must contain the literal string "document_type"
      - RED: current PASS_0_PROMPT has no document classification output
    SYNTH-03: PASS_0_PROMPT outputs likely_entity_types XML tag
      - PASS_0_PROMPT must contain the literal string "likely_entity_types"
      - RED: current PASS_0_PROMPT has no entity type prediction
    SYNTH-04: All pass prompts include transcript-mode conditional instructions
      - Each of PASS_PROMPTS[1], PASS_PROMPTS[2], PASS_PROMPTS[3] must contain "transcript"
      - RED: current pass prompts have no conditional document-type behavior
    SYNTH-05a: Confidence calibration rubric present in EXTRACTION_BASE
      - EXTRACTION_BASE must contain "0.5" AND "0.7" (the inferred confidence range)
      - RED: current EXTRACTION_BASE confidence guidance says only "use &lt;0.5 for ambiguous"
    SYNTH-05b: weekly_focus marked SINGLETON in prompt
      - PASS_PROMPTS[3] must contain "SINGLETON" near "weekly_focus"
      - RED: current PASS_PROMPTS[3] weekly_focus description has no SINGLETON marker
    SYNTH-05c: before_state marked SINGLETON in prompt
      - PASS_PROMPTS[2] must contain "SINGLETON" near "before_state"
      - RED: current PASS_PROMPTS[2] before_state description has no SINGLETON marker
    SYNTH-05d: e2e_workflow description includes assembly-from-scattered-mentions guidance
      - PASS_PROMPTS[3] must contain "scattered" near "e2e_workflow"
      - RED: current e2e_workflow description has no assembly guidance
  </behavior>
  <implementation>
    STEP 1: Export PASS_0_PROMPT from document-extraction.ts
    - Verify it is already exported (has "export const PASS_0_PROMPT" — confirmed in source)
    - If not exported, add export keyword (do NOT change prompt content in this plan)

    STEP 2: Update the import in extraction-prompts.test.ts to include PASS_0_PROMPT
    - Change: import { EXTRACTION_BASE, PASS_PROMPTS } from '...'
    - To: import { EXTRACTION_BASE, PASS_PROMPTS, PASS_0_PROMPT } from '...'

    STEP 3: Append 8 RED test stubs (below the existing 12 tests):

    // ── Phase 57: Synthesis-first extraction for unstructured notes ──────────

    describe('SYNTH-01: Global inference posture in EXTRACTION_BASE', () => {
      it('EXTRACTION_BASE contains inference-first posture instruction for unstructured documents', () => {
        // RED: fails until Plan 01 rewrites EXTRACTION_BASE global posture
        const hasInferencePosture = /unstructured/i.test(EXTRACTION_BASE) &&
          /infer/i.test(EXTRACTION_BASE);
        expect(hasInferencePosture).toBe(true);
      });
    });

    describe('SYNTH-02: Pass 0 document type classification', () => {
      it('PASS_0_PROMPT outputs document_type XML tag', () => {
        // RED: fails until Plan 01 rewrites PASS_0_PROMPT with classification step
        expect(PASS_0_PROMPT).toContain('document_type');
      });
    });

    describe('SYNTH-03: Pass 0 entity type prediction', () => {
      it('PASS_0_PROMPT outputs likely_entity_types XML tag', () => {
        // RED: fails until Plan 01 rewrites PASS_0_PROMPT with entity prediction step
        expect(PASS_0_PROMPT).toContain('likely_entity_types');
      });
    });

    describe('SYNTH-04: Transcript-mode conditional instructions in all pass prompts', () => {
      it('PASS_PROMPTS[1] contains transcript-mode conditional extraction instruction', () => {
        // RED: fails until Plan 01 adds conditional behavior to Pass 1
        expect(PASS_PROMPTS[1].toLowerCase()).toContain('transcript');
      });
      it('PASS_PROMPTS[2] contains transcript-mode conditional extraction instruction', () => {
        // RED: fails until Plan 01 adds conditional behavior to Pass 2
        expect(PASS_PROMPTS[2].toLowerCase()).toContain('transcript');
      });
      it('PASS_PROMPTS[3] contains transcript-mode conditional extraction instruction', () => {
        // RED: fails until Plan 01 adds conditional behavior to Pass 3
        expect(PASS_PROMPTS[3].toLowerCase()).toContain('transcript');
      });
    });

    describe('SYNTH-05: Confidence calibration and singleton enforcement', () => {
      it('EXTRACTION_BASE includes confidence rubric with 0.5-0.7 range for inferred entities', () => {
        // RED: fails until Plan 01 adds confidence calibration rubric
        const hasCalibrationRange = EXTRACTION_BASE.includes('0.5') && EXTRACTION_BASE.includes('0.7');
        expect(hasCalibrationRange).toBe(true);
      });
      it('PASS_PROMPTS[3] weekly_focus description includes SINGLETON marker', () => {
        // RED: fails until Plan 01 adds SINGLETON enforcement to weekly_focus
        const weeklyFocusSection = PASS_PROMPTS[3];
        const weeklyFocusIdx = weeklyFocusSection.indexOf('weekly_focus');
        const singletonIdx = weeklyFocusSection.indexOf('SINGLETON');
        expect(weeklyFocusIdx).toBeGreaterThan(-1);
        expect(singletonIdx).toBeGreaterThan(-1);
      });
      it('PASS_PROMPTS[2] before_state description includes SINGLETON marker', () => {
        // RED: fails until Plan 01 adds SINGLETON enforcement to before_state
        const beforeStateSection = PASS_PROMPTS[2];
        const singletonIdx = beforeStateSection.indexOf('SINGLETON');
        expect(singletonIdx).toBeGreaterThan(-1);
      });
      it('PASS_PROMPTS[3] e2e_workflow description includes assembly-from-scattered-mentions guidance', () => {
        // RED: fails until Plan 01 adds assembly guidance to e2e_workflow description
        expect(PASS_PROMPTS[3].toLowerCase()).toContain('scattered');
      });
    });
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export PASS_0_PROMPT and add 8 RED test stubs to extraction-prompts.test.ts</name>
  <files>
    bigpanda-app/worker/jobs/document-extraction.ts
    bigpanda-app/tests/ingestion/extraction-prompts.test.ts
  </files>
  <behavior>
    - PASS_0_PROMPT is already defined with `export const PASS_0_PROMPT` (confirmed in source at line 312)
    - Update the import line in extraction-prompts.test.ts to add PASS_0_PROMPT to the destructuring
    - Append the 8 RED test stubs as described in the &lt;feature&gt; block above
    - All 8 new tests MUST fail (RED) — do NOT change document-extraction.ts prompt content yet
    - All 12 existing tests MUST remain GREEN
  </behavior>
  <action>
1. Open `bigpanda-app/tests/ingestion/extraction-prompts.test.ts`.

2. Update the import on line 6 to include PASS_0_PROMPT:
   ```typescript
   import { EXTRACTION_BASE, PASS_PROMPTS, PASS_0_PROMPT } from '../../worker/jobs/document-extraction';
   ```

3. Append the following block at the bottom of the file (after the last closing `});`):

```typescript
// ── Phase 57: Synthesis-first extraction for unstructured notes ──────────────

describe('SYNTH-01: Global inference posture in EXTRACTION_BASE', () => {
  it('EXTRACTION_BASE contains inference-first posture instruction for unstructured documents', () => {
    // RED: fails until Plan 01 rewrites EXTRACTION_BASE global posture
    const hasInferencePosture = /unstructured/i.test(EXTRACTION_BASE) &&
      /infer/i.test(EXTRACTION_BASE);
    expect(hasInferencePosture).toBe(true);
  });
});

describe('SYNTH-02: Pass 0 document type classification', () => {
  it('PASS_0_PROMPT outputs document_type XML tag', () => {
    // RED: fails until Plan 01 rewrites PASS_0_PROMPT with classification step
    expect(PASS_0_PROMPT).toContain('document_type');
  });
});

describe('SYNTH-03: Pass 0 entity type prediction', () => {
  it('PASS_0_PROMPT outputs likely_entity_types XML tag', () => {
    // RED: fails until Plan 01 rewrites PASS_0_PROMPT with entity prediction step
    expect(PASS_0_PROMPT).toContain('likely_entity_types');
  });
});

describe('SYNTH-04: Transcript-mode conditional instructions in all pass prompts', () => {
  it('PASS_PROMPTS[1] contains transcript-mode conditional extraction instruction', () => {
    // RED: fails until Plan 01 adds conditional behavior to Pass 1
    expect(PASS_PROMPTS[1].toLowerCase()).toContain('transcript');
  });
  it('PASS_PROMPTS[2] contains transcript-mode conditional extraction instruction', () => {
    // RED: fails until Plan 01 adds conditional behavior to Pass 2
    expect(PASS_PROMPTS[2].toLowerCase()).toContain('transcript');
  });
  it('PASS_PROMPTS[3] contains transcript-mode conditional extraction instruction', () => {
    // RED: fails until Plan 01 adds conditional behavior to Pass 3
    expect(PASS_PROMPTS[3].toLowerCase()).toContain('transcript');
  });
});

describe('SYNTH-05: Confidence calibration and singleton enforcement', () => {
  it('EXTRACTION_BASE includes confidence rubric with 0.5-0.7 range for inferred entities', () => {
    // RED: fails until Plan 01 adds confidence calibration rubric
    const hasCalibrationRange = EXTRACTION_BASE.includes('0.5') && EXTRACTION_BASE.includes('0.7');
    expect(hasCalibrationRange).toBe(true);
  });

  it('PASS_PROMPTS[3] weekly_focus description includes SINGLETON marker', () => {
    // RED: fails until Plan 01 adds SINGLETON enforcement to weekly_focus
    const weeklyFocusIdx = PASS_PROMPTS[3].indexOf('weekly_focus');
    const singletonIdx = PASS_PROMPTS[3].indexOf('SINGLETON');
    expect(weeklyFocusIdx).toBeGreaterThan(-1);
    expect(singletonIdx).toBeGreaterThan(-1);
  });

  it('PASS_PROMPTS[2] before_state description includes SINGLETON marker', () => {
    // RED: fails until Plan 01 adds SINGLETON enforcement to before_state
    expect(PASS_PROMPTS[2].indexOf('SINGLETON')).toBeGreaterThan(-1);
  });

  it('PASS_PROMPTS[3] e2e_workflow description includes assembly-from-scattered-mentions guidance', () => {
    // RED: fails until Plan 01 adds assembly guidance to e2e_workflow description
    expect(PASS_PROMPTS[3].toLowerCase()).toContain('scattered');
  });
});
```

4. Run the test to confirm 8 new failures and 12 passing:
   ```bash
   cd bigpanda-app && npm test -- extraction-prompts --run 2>&1 | tail -20
   ```
   Expected: 12 pass, 8 fail. If all 20 pass — STOP, the prompts already contain the expected content (inspect and confirm or adjust test).
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- extraction-prompts --run 2>&1 | grep -E "Tests.*passed|Tests.*failed|pass|fail" | tail -5</automated>
  </verify>
  <done>
    - 20 total tests in extraction-prompts.test.ts
    - 12 passing (existing Phase 53 tests GREEN)
    - 8 failing (new Phase 57 stubs RED)
    - PASS_0_PROMPT included in import
    - No TypeScript compile errors
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- extraction-prompts --run
```
Expected result: 12 tests pass (Phase 53 contracts), 8 tests fail (Phase 57 RED stubs). Failure messages are clear assertions about missing content — no import errors or syntax failures.
</verification>

<success_criteria>
- extraction-prompts.test.ts has 20 tests total (12 pre-existing + 8 new)
- 8 new stubs are RED (fail) with descriptive assertion messages
- 12 pre-existing Phase 53 tests remain GREEN
- PASS_0_PROMPT imported successfully (export already present in document-extraction.ts)
- No TypeScript errors in test file
</success_criteria>

<output>
After completion, create `.planning/phases/57-extraction-intelligence-for-unstructured-notes/57-00-SUMMARY.md`
</output>
