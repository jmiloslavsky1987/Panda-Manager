---
phase: 27-ui-overhaul-templates
plan: 02
type: execute
wave: 1
depends_on:
  - 27-01
files_modified:
  - bigpanda-app/lib/tab-template-registry.ts
autonomous: true
requirements:
  - UI-03
must_haves:
  truths:
    - "TAB_TEMPLATE_REGISTRY has entries for exactly 11 tab types (overview, actions, risks, milestones, teams, architecture, decisions, history, stakeholders, plan, skills)"
    - "Adding a new key to TabType union without a registry entry causes a TypeScript compile error (npx tsc --noEmit fails)"
    - "skills entry exists with empty sections array — no DB rows seeded for this tab"
    - "plan entry exists with one section named 'Business Outcomes'"
    - "Each non-skills section has name, requiredFields, and placeholderText with instructional text"
  artifacts:
    - path: "bigpanda-app/lib/tab-template-registry.ts"
      provides: "Typed registry of all 11 tab section structures"
      exports: ["TAB_TEMPLATE_REGISTRY", "TabType", "SectionDef", "TabTemplate"]
      contains: "satisfies Record<TabType, TabTemplate>"
  key_links:
    - from: "bigpanda-app/lib/tab-template-registry.ts"
      to: "TypeScript compiler"
      via: "satisfies Record<TabType, TabTemplate>"
      pattern: "satisfies Record<TabType"
    - from: "bigpanda-app/lib/seed-project.ts"
      to: "bigpanda-app/lib/tab-template-registry.ts"
      via: "import TAB_TEMPLATE_REGISTRY"
      pattern: "TAB_TEMPLATE_REGISTRY"
---

<objective>
Create `lib/tab-template-registry.ts` — the TypeScript-enforced registry that defines required section structure for all 11 tab types, with instructional placeholder text for each section.

Purpose: This is the single source of truth consumed by both Phase 27 seeding (Plan 04) and Phase 30 Context Hub completeness analysis. The registry uses `satisfies Record<TabType, TabTemplate>` so adding a new tab type without a registry entry is a compile-time error.
Output: One new file — `lib/tab-template-registry.ts` — fully typed, all 11 entries, instructional placeholder text.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-ui-overhaul-templates/27-CONTEXT.md
@.planning/phases/27-ui-overhaul-templates/27-RESEARCH.md

<interfaces>
<!-- Pattern: satisfies Record<K, V> — TypeScript 4.9+, enforces exhaustive key coverage -->
<!-- Overview tab: no DB table, completeness calculated from project record fields -->
<!-- Skills tab: read-only execution log, registry entry needed for exhaustiveness but empty sections -->
<!-- Plan tab: one section "Business Outcomes" — seeding inserts 1 placeholder business_outcomes row -->
<!-- Instructional placeholder style: "Add your first X — field1, field2, and description" -->
<!-- source='template' field on seeded rows (Plan 04 uses this, not the registry itself) -->

From bigpanda-app/db/schema.ts — tables available for seeding:
  actions:           description, owner, due, status, source, source_artifact_id
  risks:             description, severity, owner, mitigation, status, source
  milestones:        name, status, target, date, notes, owner, source
  engagementHistory: date, content, source  (APPEND ONLY — no update/delete)
  keyDecisions:      date, decision, context, source  (APPEND ONLY — no update/delete)
  stakeholders:      name, role, company, email, notes, source
  businessOutcomes:  (see app/api/projects/[projectId]/business-outcomes/route.ts for shape)
  architectureIntegrations: (see app/api/projects/[projectId]/architecture-integrations/route.ts)
  — teams table: see db/schema.ts for shape
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/tab-template-registry.ts with all 11 tab types</name>
  <files>bigpanda-app/lib/tab-template-registry.ts</files>
  <behavior>
    - TAB_TEMPLATE_REGISTRY has exactly 11 keys: overview, actions, risks, milestones, teams, architecture, decisions, history, stakeholders, plan, skills
    - skills entry: sections is an empty array []
    - plan entry: sections has exactly 1 entry with name='Business Outcomes'
    - All non-skills, non-overview entries: sections has at least 1 entry
    - Each SectionDef has name (string), requiredFields (string[]), placeholderText (string)
    - placeholderText is instructional, non-empty, contains action verb ("Add", "Define", "Document")
    - TypeScript: `satisfies Record<TabType, TabTemplate>` — missing a key is a compile error
  </behavior>
  <action>
Create `bigpanda-app/lib/tab-template-registry.ts`. Use the research-documented pattern exactly.

**TabType union** — 11 values:
```typescript
export type TabType =
  | 'overview'
  | 'actions'
  | 'risks'
  | 'milestones'
  | 'teams'
  | 'architecture'
  | 'decisions'
  | 'history'
  | 'stakeholders'
  | 'plan'
  | 'skills'
```

**SectionDef and TabTemplate interfaces:**
```typescript
export interface SectionDef {
  name: string
  requiredFields: string[]
  placeholderText: string
}

export interface TabTemplate {
  sections: SectionDef[]
}
```

**TAB_TEMPLATE_REGISTRY** — implement all 11 entries using `satisfies Record<TabType, TabTemplate>`:

Section definitions to implement (Claude's discretion on exact section names per CONTEXT.md):

- **overview**: sections=[
    { name: 'Project Summary', requiredFields: ['description'], placeholderText: 'Describe the project scope and objectives — include customer name, go-live target, and primary goal' },
    { name: 'Success Metrics', requiredFields: ['metric', 'target'], placeholderText: 'Define measurable success criteria — what does good look like at go-live?' },
    { name: 'Key Contacts', requiredFields: ['name', 'role'], placeholderText: 'Add primary contacts — BigPanda CSM, customer project owner, and executive sponsor' }
  ]

- **actions**: sections=[
    { name: 'Action Items', requiredFields: ['description', 'owner', 'due'], placeholderText: 'Add your first action — include owner, due date, and a clear description of the expected outcome' }
  ]

- **risks**: sections=[
    { name: 'Risk Register', requiredFields: ['description', 'severity', 'mitigation'], placeholderText: 'Document a risk — severity level (low/medium/high/critical), owner, and mitigation plan' }
  ]

- **milestones**: sections=[
    { name: 'Project Milestones', requiredFields: ['name', 'target', 'owner'], placeholderText: 'Add a milestone — name, target date, owner, and current status' }
  ]

- **teams**: sections=[
    { name: 'Customer Team', requiredFields: ['name', 'role', 'company'], placeholderText: 'Add a team member — name, role, company, and email or Slack handle' },
    { name: 'BigPanda Team', requiredFields: ['name', 'role'], placeholderText: 'Add a BigPanda team member assigned to this project — name, role, and Slack handle' }
  ]

- **architecture**: sections=[
    { name: 'Integration Track', requiredFields: ['name', 'status', 'type'], placeholderText: 'Add an integration — name, current status (live/in_progress/pilot/planned), and integration type' },
    { name: 'Workflow', requiredFields: ['name', 'description'], placeholderText: 'Document an E2E workflow — name and description of the end-to-end process being configured' }
  ]

- **decisions**: sections=[
    { name: 'Key Decisions', requiredFields: ['decision', 'date'], placeholderText: 'Record a key decision — decision text, date, context, and who made it (append-only)' }
  ]

- **history**: sections=[
    { name: 'Engagement History', requiredFields: ['content', 'date'], placeholderText: 'Log an engagement update — date, content summary, and source (append-only)' }
  ]

- **stakeholders**: sections=[
    { name: 'Stakeholder Map', requiredFields: ['name', 'role', 'company'], placeholderText: 'Add a stakeholder — name, role, company, and preferred contact method' }
  ]

- **plan**: sections=[
    { name: 'Business Outcomes', requiredFields: ['outcome', 'owner'], placeholderText: 'Define a business outcome this project must deliver — outcome description and business owner' }
  ]

- **skills**: sections=[]  ← empty; Skills tab is read-only execution log; no DB writes during seeding

End file with:
```typescript
export type RegistryKey = keyof typeof TAB_TEMPLATE_REGISTRY
```
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test tests/ui/tab-registry.test.ts -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - lib/tab-template-registry.ts file exists
    - npm test tests/ui/tab-registry.test.ts -- --run: all 4 tests GREEN
    - npx tsc --noEmit exits 0 with no errors
    - File uses `satisfies Record<TabType, TabTemplate>` (grep confirms)
    - skills entry has sections: [] (grep confirms)
    - plan entry sections[0].name === 'Business Outcomes' (grep confirms)
  </done>
</task>

</tasks>

<verification>
1. File exists: bigpanda-app/lib/tab-template-registry.ts
2. Tests green: cd bigpanda-app && npm test tests/ui/tab-registry.test.ts -- --run
3. Type check: cd bigpanda-app && npx tsc --noEmit (exit 0)
4. Exhaustiveness: grep -n "satisfies Record" bigpanda-app/lib/tab-template-registry.ts returns a match
5. All 11 keys present: grep -c "sections:" bigpanda-app/lib/tab-template-registry.ts >= 11
</verification>

<success_criteria>
- TAB_TEMPLATE_REGISTRY exported from lib/tab-template-registry.ts
- All 11 TabType keys covered: overview, actions, risks, milestones, teams, architecture, decisions, history, stakeholders, plan, skills
- skills.sections is an empty array
- plan.sections[0].name is 'Business Outcomes'
- satisfies Record<TabType, TabTemplate> enforces exhaustive coverage (TypeScript compile error if key missing)
- All 4 tests in tests/ui/tab-registry.test.ts pass GREEN
- npx tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/27-ui-overhaul-templates/27-02-SUMMARY.md`
</output>
