---
phase: 27-ui-overhaul-templates
plan: 05
type: execute
wave: 3
depends_on:
  - 27-02
  - 27-03
  - 27-04
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-03
  - UI-04
must_haves:
  truths:
    - "Top-level nav shows exactly 6 items (Overview, Delivery, Team, Intel, Skills, Admin)"
    - "Clicking Delivery shows secondary tab row with Actions, Risks, Milestones, Plan"
    - "URL changes to ?tab=delivery&subtab=actions after Delivery click — visible in browser address bar"
    - "Browser back button returns to previous tab and URL reflects it"
    - "Overview tab shows no secondary row — just content directly"
    - "Plan's existing Phase Board / Task Board / Gantt / Swimlane navigation is intact"
    - "Creating a new project via wizard and launching seeds placeholder rows in Actions, Risks, Milestones"
    - "TAB_TEMPLATE_REGISTRY export in lib/tab-template-registry.ts has all 11 tab types"
    - "Full test suite passes: npm test -- --run exits 0"
  artifacts:
    - path: "bigpanda-app/components/WorkspaceTabs.tsx"
      provides: "6-group nav component"
    - path: "bigpanda-app/components/SubTabBar.tsx"
      provides: "Secondary tab row component"
    - path: "bigpanda-app/lib/tab-template-registry.ts"
      provides: "11-tab TypeScript registry"
    - path: "bigpanda-app/lib/seed-project.ts"
      provides: "Project seeding function"
  key_links:
    - from: "browser"
      to: "bigpanda-app/components/WorkspaceTabs.tsx"
      via: "visual inspection at /customer/[id]?tab=delivery"
      pattern: "secondary nav visible"
---

<objective>
Human browser verification of all Phase 27 deliverables before marking the phase complete.

Purpose: Automated tests cover component behavior and API wiring, but URL deep-linking, sticky header scroll behavior, and browser back/forward state require a real browser session to verify.
Output: User confirms navigation works, seeding works, and Plan's internal nav is preserved.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-ui-overhaul-templates/27-CONTEXT.md
@.planning/phases/27-ui-overhaul-templates/27-03-SUMMARY.md
@.planning/phases/27-ui-overhaul-templates/27-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and type check before verification</name>
  <files></files>
  <action>
Run final automated checks before presenting verification to user:

1. Full test suite: cd bigpanda-app && npm test -- --run
   - All tests must pass GREEN. If any test is RED, stop and fix before proceeding.

2. TypeScript type check: cd bigpanda-app && npx tsc --noEmit
   - Must exit 0 with no errors.

3. Apply migration if not already applied:
   If the DB is accessible (check with: psql $DATABASE_URL -c "\d projects" 2>/dev/null | grep seeded), run:
   psql $DATABASE_URL -f db/migrations/0022_project_seeded_flag.sql

4. Start the dev server: npm run next-only
   - Verify server starts without errors.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | tail -15</automated>
  </verify>
  <done>Full test suite exits 0, all tests GREEN, tsc --noEmit exits 0, dev server starts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human browser verification of all Phase 27 deliverables</name>
  <what-built>
    Two-level workspace navigation (14 flat tabs → 6 grouped with sub-tabs), TypeScript template registry for 11 tab types, and new-project seeding with instructional placeholder content on status='active'.
  </what-built>
  <how-to-verify>
    Run the dev server: cd bigpanda-app && npm run next-only

    **Test 1 — Primary nav grouping (UI-01):**
    1. Open any project workspace (e.g., http://localhost:3000/customer/1)
    2. Count the top-level tab items — should be exactly 6: Overview, Delivery, Team, Intel, Skills, Admin
    3. Confirm you do NOT see: Actions, Risks, Milestones, Teams, Architecture, Decisions, etc. at the top level

    **Test 2 — Sub-tab navigation and URL (UI-01):**
    1. Click "Delivery" — verify a second row of tabs appears: Actions, Risks, Milestones, Plan
    2. Check the browser address bar — URL should contain ?tab=delivery&subtab=actions
    3. Click "Risks" in the sub-tab row — URL should change to ?tab=delivery&subtab=risks
    4. Click "Team" in primary bar — second row changes to: Teams, Architecture, Stakeholders
    5. URL should contain ?tab=team&subtab=teams

    **Test 3 — Standalone tabs (UI-01):**
    1. Click "Overview" — verify NO secondary tab row appears; content renders directly
    2. Click "Skills" — verify NO secondary tab row appears; skills content renders

    **Test 4 — Deep-linking (UI-01):**
    1. While on any tab, copy the full URL (e.g., /customer/1/risks?tab=delivery&subtab=risks)
    2. Open a new browser tab and paste the URL
    3. Verify the correct tab and sub-tab are active (Delivery highlighted, Risks sub-tab active)

    **Test 5 — Browser back/forward (UI-01):**
    1. Navigate: Delivery → Team → Intel
    2. Press browser back button — should return to Team (with Team sub-tabs visible)
    3. Press back again — should return to Delivery
    4. Press forward — should go to Team again
    Confirm URL updates correctly at each step.

    **Test 6 — Plan's internal navigation preserved (UI-01):**
    1. Click "Delivery" then click "Plan" in the sub-tab row
    2. Inside the Plan view, verify the Phase Board / Task Board / Gantt / Swimlane switcher is still present
    3. Click between Plan's internal views — they should work as before

    **Test 7 — Sticky headers on scroll (UI-01):**
    1. Navigate to a tab with lots of content (e.g., Delivery > Actions)
    2. Scroll down past the visible viewport
    3. Verify BOTH the primary tab bar AND the sub-tab row remain visible/sticky at the top

    **Test 8 — Seeding verification (UI-04):**
    1. Create a new project via the wizard (http://localhost:3000 → New Project)
    2. Complete the wizard steps and click Launch / set status to active
    3. Open the new project's Actions tab — verify 1 placeholder row with instructional text
       (e.g., "Add your first action — include owner, due date, and a clear description...")
    4. Open Risks tab — verify 1 placeholder row
    5. Open Milestones tab — verify 1 placeholder row
    6. Verify placeholder rows are labeled/tagged as template content (not real data)
    If you cannot run the full wizard, test via: curl -X PATCH http://localhost:3000/api/projects/[NEW_ID] -H "Content-Type: application/json" -d '{"status":"active"}' (with auth cookie)

    **Test 9 — Template registry type safety (UI-03):**
    In your terminal, run: cd bigpanda-app && npx tsc --noEmit
    Should exit 0 with no errors.
    Optional manual check: In lib/tab-template-registry.ts, temporarily comment out one entry (e.g., // risks: ...) and run npx tsc --noEmit again — should show a TypeScript error. Restore the entry.
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 tests pass, or describe any issues found for each test that failed.</resume-signal>
</task>

</tasks>

<verification>
Automated gate (Task 1): npm test -- --run exits 0
Human gate (Task 2): User confirms Tests 1-9 pass
Phase complete when: user types "approved"
</verification>

<success_criteria>
- npm test -- --run: all tests GREEN (exit 0)
- 6-item primary nav confirmed in browser
- Sub-tab row renders for Delivery, Team, Intel, Admin
- URL uses ?tab=X&subtab=Y pattern (confirmed in address bar)
- Browser back/forward works
- Plan's internal navigation unaffected
- Seeded placeholder rows visible in Actions/Risks/Milestones for new project
- npx tsc --noEmit exits 0
- User types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/27-ui-overhaul-templates/27-05-SUMMARY.md`
</output>
