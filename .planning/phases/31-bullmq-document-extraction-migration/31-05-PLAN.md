---
phase: 31-bullmq-document-extraction-migration
plan: 05
type: execute
wave: 4
depends_on:
  - 31-04
files_modified: []
autonomous: false
requirements:
  - EXTR-01
  - EXTR-02
  - EXTR-03

must_haves:
  truths:
    - "User can upload a document, navigate away, return, and see extraction progress"
    - "Extraction survives a full browser refresh mid-job"
    - "Progress shows correct % and chunk label while running"
    - "Toast appears exactly once when all files in a batch complete"
    - "Review modal opens at reviewing stage with staged items pre-loaded"
    - "Failed extraction leaves no data in workspace tabs"
  artifacts:
    - path: "bigpanda-app/app/api/ingestion/extract/route.ts"
      provides: "Thin enqueue endpoint (verified working)"
    - path: "bigpanda-app/worker/jobs/document-extraction.ts"
      provides: "Background job handler (verified running)"
    - path: "bigpanda-app/components/IngestionModal.tsx"
      provides: "Polling-based extracting stage (SSE removed)"
    - path: "bigpanda-app/components/ContextTab.tsx"
      provides: "Inline progress + review card (verified rendering)"
  key_links:
    - from: "browser Context Hub"
      to: "BullMQ worker process"
      via: "POST /api/ingestion/extract → queue → worker"
      pattern: "end-to-end: upload → enqueue → job runs → progress visible → review"
---

<objective>
Human verification checkpoint. Confirms the full end-to-end BullMQ extraction flow works
as specified in Phase 31 requirements before marking the phase complete.

Purpose: Unit tests cover individual components. This checkpoint verifies the integrated
system behavior: browser navigation, refresh resilience, toast timing, and review handoff.

Output: Phase 31 UAT sign-off.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/PROJECT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/ROADMAP.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/STATE.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/31-bullmq-document-extraction-migration/31-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build check</name>
  <files></files>
  <action>
    Before human verification, run the full automated checks:

    1. Full test suite: `npx vitest run` from bigpanda-app/ — must be GREEN (or same failures as pre-phase)
    2. TypeScript check: `npx tsc --noEmit` — must be clean
    3. Production build check: `npx next build` from bigpanda-app/ — must succeed with no new errors

    If any new test failures appear (beyond the 13 pre-existing failures in tests/teams-arch/),
    fix them before proceeding to the human verification checkpoint.

    Document results in the phase summary.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run tests/ingestion/ 2>&1 | tail -10</automated>
  </verify>
  <done>
    tests/ingestion/ all GREEN. TypeScript clean. Build succeeds (no new errors beyond pre-existing).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — end-to-end extraction flow</name>
  <files></files>
  <action>Pause for human verification of end-to-end BullMQ extraction flow per the instructions in how-to-verify.</action>
  <verify>
    <automated>CHECKPOINT — human verification required. See how-to-verify section below.</automated>
  </verify>
  <done>Human types "approved" after all 6 tests pass.</done>
  <what-built>
    BullMQ document extraction migration — complete replacement of SSE transport with background job:
    - POST /api/ingestion/extract: thin enqueue endpoint returning { jobIds, batchId }
    - worker/jobs/document-extraction.ts: background job handler with per-chunk progress updates
    - GET /api/ingestion/jobs/[jobId]: polling endpoint with stale detection
    - GET /api/projects/[projectId]/extraction-status: batch status with batch_complete flag
    - IngestionModal: polling replaces SSE; shows "60% — Processing chunk 3 of 5"
    - ContextTab: inline progress card while running; "Ready for review" card when complete; toast on batch completion
  </what-built>
  <how-to-verify>
    Prerequisites: `npm run dev` from bigpanda-app/ (starts Next.js + worker process).
    Use a small-medium test document (PDF or PPTX, ~10-50 pages).

    Test 1 — EXTR-02: Progress visibility while modal is open
    1. Go to Context Hub for any project
    2. Upload a document and click Extract
    3. Observe the extracting stage in IngestionModal
    PASS: Modal shows "X% — Processing chunk Y of Z" updating in real time (not SSE stream)
    FAIL: No progress shown, or modal hangs, or shows old SSE-style streaming output

    Test 2 — EXTR-01: Navigation away and return
    1. Start a document extraction (click Extract in modal)
    2. While IngestionModal shows "extracting", navigate to a different page (e.g., click on a project tab)
    3. Come back to Context Hub (same project)
    PASS: Upload area is replaced by an inline "Extraction in progress" card showing per-file progress rows with % complete
    FAIL: Upload area shows as normal; no progress card

    Test 3 — EXTR-01: Browser full refresh resilience
    1. Start extraction on a document
    2. While extraction is running, press Cmd+R (hard refresh)
    3. Navigate back to Context Hub
    PASS: Extraction progress card appears, showing the job still running (or completed if fast enough)
    FAIL: No card shown; job appears to have stopped

    Test 4 — EXTR-01: Toast and review handoff
    1. Start extraction and navigate away
    2. Wait for extraction to complete (watch for toast notification)
    PASS: Exactly ONE toast appears saying "Extraction complete — review N items" with a "Review" action button
    FAIL: No toast, or multiple toasts, or toast fires per-file instead of once for the batch

    Test 5 — EXTR-01: Review modal reopens correctly
    1. From the toast or the "Ready for review" card on Context Hub, click "Review"
    PASS: IngestionModal opens at the reviewing stage (not uploading) with extraction items pre-loaded and ready for approve/reject
    FAIL: Modal opens at uploading stage; or modal is empty; or review items are missing

    Test 6 — EXTR-03: Atomicity (workspace tabs unchanged until approval)
    1. Start extraction on a document
    2. While extraction is running, check any workspace tab (Actions, Risks, etc.)
    PASS: Workspace tabs show only previously approved data — no partial extraction items appear mid-job
    FAIL: New items appear in workspace tabs before user has approved anything
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 6 tests pass.
    Or describe which test(s) failed and what behavior was observed.
  </resume-signal>
</task>

</tasks>

<verification>
- All automated tests GREEN (npx vitest run tests/ingestion/)
- TypeScript clean
- Production build succeeds
- Human verification: all 6 manual tests pass
</verification>

<success_criteria>
- EXTR-01: User can navigate away and return to see progress — VERIFIED
- EXTR-01: Browser refresh does not kill the job — VERIFIED
- EXTR-02: Progress % and chunk label display correctly — VERIFIED
- EXTR-03: Workspace tabs unchanged until user approves — VERIFIED
- Toast fires exactly once per batch — VERIFIED
- Review modal opens at reviewing stage with pre-loaded items — VERIFIED
</success_criteria>

<output>
After completion, create `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/31-bullmq-document-extraction-migration/31-05-SUMMARY.md`
</output>
