---
phase: 19.1-source-integrations
plan: 08
subsystem: discovery
tags: [drizzle, postgresql, claude, discovery, deduplication, react, nextjs]

# Dependency graph
requires:
  - phase: 19.1-05
    provides: discovery_items table, runDiscoveryScan scanner, scan route SSE, ReviewQueue UI
  - phase: 19.1-07
    provides: adapter pattern for source fetching, source_credentials, userTokens in DiscoveryScanParams
provides:
  - likely_duplicate BOOLEAN column in discovery_items (migration 0015)
  - Claude dedup signal: DISCOVERY_SYSTEM + prompt instructs Claude to set likely_duplicate
  - Scan route builds existingProjectSummary from DB (actions/risks/stakeholders) before scan
  - Worker builds same project context for scheduled daily scans
  - ReviewQueue splits items into primary list vs collapsed "May already exist" section
affects:
  - 19.1-source-integrations (queue UX, all future scan consumers)
  - Phase 22 (source badges + audit log — may read likely_duplicate for audit context)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Project context summary pattern: fetch existing DB rows, build compact text, inject into Claude prompt for cross-reference"
    - "Collapsed duplicate section: <details> element with amber badge on items Claude flagged"

key-files:
  created:
    - bigpanda-app/db/migrations/0015_discovery_dedup_flag.sql
  modified:
    - bigpanda-app/db/schema.ts
    - bigpanda-app/lib/discovery-scanner.ts
    - bigpanda-app/app/api/discovery/scan/route.ts
    - bigpanda-app/worker/jobs/discovery-scan.ts
    - bigpanda-app/components/ReviewQueue.tsx
    - bigpanda-app/components/QueueItemRow.tsx
    - bigpanda-app/tests/discovery/scan.test.ts

key-decisions:
  - "likely_duplicate stored in DB rather than computed client-side: persists Claude's cross-reference signal through queue refresh cycles"
  - "Collapsed <details> section (not filtered-out): flagged items remain reviewable, just de-emphasized — user can override Claude's judgment"
  - "50-row limit per entity type for project context: keeps token budget bounded while covering typical project size"
  - "existingProjectSummary is required (not optional) in DiscoveryScanParams: callers must opt-out explicitly by passing empty string, preventing accidental omission"

patterns-established:
  - "Dedup via Claude cross-reference: pass compact existing-data summary in analysis prompt, instruct likely_duplicate boolean in output schema"
  - "Worker mirrors scan route logic: both build existingProjectSummary before calling runDiscoveryScan"

requirements-completed:
  - SRC-09

# Metrics
duration: 6min
completed: 2026-03-26
---

# Phase 19.1 Plan 08: Discovery Dedup Flag Summary

**Claude cross-references each discovered item against existing project actions/risks/stakeholders, stores a likely_duplicate flag in discovery_items, and ReviewQueue surfaces flagged items in a collapsed amber-badged section separate from actionable discoveries.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-03-26T20:00:24Z
- **Completed:** 2026-03-26T20:05:58Z
- **Tasks:** 3
- **Files modified:** 7 (+ 1 created)

## Accomplishments
- Added `likely_duplicate BOOLEAN NOT NULL DEFAULT FALSE` via migration 0015 and Drizzle schema
- Updated `DISCOVERY_SYSTEM` prompt and analysis user message to instruct Claude to set `likely_duplicate` when an item matches existing project data
- Scan route (SSE) and worker both fetch existing actions/risks/stakeholders and build compact summary before calling `runDiscoveryScan`
- `DiscoveryScanParams.existingProjectSummary` added as required field, propagated through all callers including tests
- `ReviewQueue` splits `items` into `newItems` + `likelyDuplicates`, renders duplicates in a collapsed `<details>` section at 60% opacity with amber "May already exist" badge

## Task Commits

Each task was committed atomically:

1. **Task 1: DB migration + schema** - `514c749` (feat)
2. **Task 2: Enhance discovery-scanner** - `c9df2c9` (feat)
3. **Task 3: Scan route + queue UI** - `cf5e104` (feat)

**Plan metadata:** _(docs commit pending)_

## Files Created/Modified
- `bigpanda-app/db/migrations/0015_discovery_dedup_flag.sql` - ALTER TABLE adds likely_duplicate column
- `bigpanda-app/db/schema.ts` - discoveryItems table gets likely_duplicate boolean column
- `bigpanda-app/lib/discovery-scanner.ts` - DiscoveryItem.likely_duplicate, DiscoveryScanParams.existingProjectSummary, updated DISCOVERY_SYSTEM + analysis prompt
- `bigpanda-app/app/api/discovery/scan/route.ts` - fetches existingProjectSummary, passes all params to scanner, stores likely_duplicate on insert
- `bigpanda-app/worker/jobs/discovery-scan.ts` - same project context fetch + likely_duplicate on insert for scheduled scans
- `bigpanda-app/components/ReviewQueue.tsx` - splits items, renders collapsed duplicate section
- `bigpanda-app/components/QueueItemRow.tsx` - adds likely_duplicate?: boolean to DiscoveryQueueItem
- `bigpanda-app/tests/discovery/scan.test.ts` - baseParams extended with source_credentials, userTokens, existingProjectSummary

## Decisions Made
- `existingProjectSummary` is required (not optional) in `DiscoveryScanParams` — callers must explicitly pass empty string to opt out, preventing silent omission
- Items flagged as `likely_duplicate` are kept in DB with status `pending` and shown in a collapsed section rather than auto-dismissed — user can override Claude's judgment
- 50-row cap per entity type (actions/risks/stakeholders) keeps project context within token budget while covering typical project sizes

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Extended worker to fetch project context and pass all required params**
- **Found during:** Task 3 (scan route update)
- **Issue:** After adding `existingProjectSummary` as required field in `DiscoveryScanParams`, the worker job (`discovery-scan.ts`) called `runDiscoveryScan` without `source_credentials`, `userTokens`, or `existingProjectSummary`, causing TypeScript errors
- **Fix:** Updated worker to fetch settings, userSourceTokens, and project context in parallel; pass all to scanner; store `likely_duplicate` on insert
- **Files modified:** bigpanda-app/worker/jobs/discovery-scan.ts
- **Verification:** TypeScript compiles with 0 new errors
- **Committed in:** cf5e104 (Task 3 commit)

**2. [Rule 2 - Missing Critical] Updated test baseParams to include new required fields**
- **Found during:** Task 3 (TypeScript verification)
- **Issue:** `scan.test.ts` called `runDiscoveryScan` without `source_credentials`, `userTokens`, `existingProjectSummary` — TypeScript type error
- **Fix:** Extended `baseParams` in test file with empty defaults for all three fields
- **Files modified:** bigpanda-app/tests/discovery/scan.test.ts
- **Verification:** TypeScript compiles clean; tests still structurally valid
- **Committed in:** cf5e104 (Task 3 commit)

**Note:** During Task 2, a linter/auto-formatter restored `discovery-scanner.ts` to the adapter-pattern version from plan 19.1-07 (with `source_credentials`/`userTokens` added). My changes were re-applied on top of that canonical version, resulting in a richer but fully compatible interface.

---

**Total deviations:** 2 auto-fixed (both Rule 2 — missing critical propagation of new required interface field)
**Impact on plan:** Both fixes required for type correctness and runtime correctness. No scope creep.

## Issues Encountered
- Linter restored `discovery-scanner.ts` to adapter-pattern version mid-execution (plan 19.1-07 had updated this file). Changes were re-applied on the updated version without conflict.

## Next Phase Readiness
- `likely_duplicate` flag is stored per scan item; ReviewQueue surfaces it clearly
- Migration 0015 must be run manually before the flag appears in production DB
- Phase 19.1 complete — all 8 plans executed

---
*Phase: 19.1-source-integrations*
*Completed: 2026-03-26*
