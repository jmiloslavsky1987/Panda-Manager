---
phase: 19.1-source-integrations
plan: 05
subsystem: api
tags: [adapter-pattern, source-credentials, discovery-scanner, resolveAdapter, slack, gong, glean, gmail, mcp]

# Dependency graph
requires:
  - phase: 19.1-02
    provides: SlackAdapter, GongAdapter with creds-object constructors
  - phase: 19.1-03
    provides: GleanAdapter, MCPAdapter implementations
  - phase: 19.1-04
    provides: GmailAdapter with UserSourceToken constructor
provides:
  - resolveAdapter factory: REST-priority adapter selection for all four sources
  - discovery-scanner.ts: refactored to use adapter pattern instead of direct anthropic.beta MCP calls
  - DiscoveryScanParams: includes source_credentials and userTokens fields
  - /api/settings/source-credentials: GET (masked) + POST (Zod-validated) route
  - Scan route: reads settings + userTokens from DB before calling scanner
affects:
  - 19.1-06 (Source Connections UI)
  - 19.1-07 (Gmail OAuth)
  - 19.1-08 (queue dedup UI)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "resolveAdapter factory: REST credentials take priority over MCP fallback; null returned when neither configured"
    - "DiscoveryScanParams: all credential types flow through single params object to scanner"
    - "Adapter constructor pattern: creds object (not positional args) for all REST adapters"

key-files:
  created: []
  modified:
    - bigpanda-app/lib/source-adapters/index.ts
    - bigpanda-app/lib/source-adapters/index.test.ts
    - bigpanda-app/lib/discovery-scanner.ts
    - bigpanda-app/app/api/discovery/scan/route.ts
    - bigpanda-app/app/api/settings/source-credentials/route.ts
    - bigpanda-app/tests/discovery/scan.test.ts

key-decisions:
  - "REST credentials take priority over MCP fallback when both configured — resolveAdapter checks REST first"
  - "All REST adapter constructors take a single creds object (not positional args) — actual Wave 1 impl differs from plan spec"
  - "DiscoveryScanParams adds source_credentials + userTokens as required fields — backward compat maintained via existingProjectSummary optional-then-required"
  - "Source-credentials route uses Zod validation + selective merge (only updates keys present in POST body)"

patterns-established:
  - "resolveAdapter(source, orgCredentials, userToken, mcpServer): null means skip with SSE warning"
  - "Scan route reads readSettings() + DB userSourceTokens in parallel before scanner call"
  - "GET /api/settings/source-credentials masks secrets (••••last4) before returning to client"

requirements-completed: [SRC-06, SRC-07, SRC-08]

# Metrics
duration: 6min
completed: 2026-03-26
---

# Phase 19.1 Plan 05: Adapter Factory Integration Summary

**resolveAdapter factory fully wired into discovery-scanner — REST adapters (Slack/Gong/Glean/Gmail) take priority over MCP fallback, direct anthropic.beta MCP calls removed from scanner**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-26T19:59:57Z
- **Completed:** 2026-03-26T20:06:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Implemented `resolveAdapter` factory in `index.ts` — selects SlackAdapter/GongAdapter/GleanAdapter/GmailAdapter (REST path) or MCPAdapter (fallback), returns null when no credentials
- Refactored `discovery-scanner.ts` Phase 1 loop to use `resolveAdapter` instead of direct `anthropic.beta.messages.create` MCP calls
- Updated `DiscoveryScanParams` to include `source_credentials`, `userTokens`, and `existingProjectSummary`
- Updated scan route to read settings and DB userSourceTokens before calling scanner
- Source-credentials API route (GET with masking + POST with Zod validation) already in place from prior wave
- index.test.ts expanded from 3 to 9 tests — covers all adapters, MCP fallback, null case, disabled MCP, and REST-priority rule

## Task Commits

Each task was committed atomically:

1. **Task 1: resolveAdapter factory + index.test.ts GREEN** - `a54487c` (feat)
2. **Task 2: discovery-scanner refactor + scan route update** - already in HEAD via 19.1-08 prior work (`c9df2c9`, `cf5e104`)

## Files Created/Modified
- `bigpanda-app/lib/source-adapters/index.ts` - Replaced stub with full resolveAdapter implementation
- `bigpanda-app/lib/source-adapters/index.test.ts` - Expanded from 3 to 9 tests, all GREEN
- `bigpanda-app/lib/discovery-scanner.ts` - Phase 1 loop uses resolveAdapter; DiscoveryScanParams extended
- `bigpanda-app/app/api/discovery/scan/route.ts` - Reads source_credentials + userTokens, passes to scanner
- `bigpanda-app/app/api/settings/source-credentials/route.ts` - GET (masked) + POST (Zod-validated) — already implemented
- `bigpanda-app/tests/discovery/scan.test.ts` - Updated baseParams with new required DiscoveryScanParams fields

## Decisions Made
- REST credentials take priority over MCP fallback — `resolveAdapter` checks REST first; MCP only if no REST creds
- Adapter constructors use creds object pattern, not positional args — the plan spec showed `(token, channels[])` but actual Wave 1 implementations use `({ token, channels })`. Factory updated to match real constructors.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Adapter constructor signature mismatch**
- **Found during:** Task 1 (resolveAdapter implementation)
- **Issue:** Plan spec showed `new SlackAdapter(token, channels[])` but actual Wave 1 SlackAdapter, GongAdapter, and GleanAdapter all use `constructor(creds: { ... })` object pattern
- **Fix:** Updated resolveAdapter to pass creds objects matching actual constructors
- **Files modified:** `bigpanda-app/lib/source-adapters/index.ts`
- **Verification:** All 9 index.test.ts tests pass GREEN
- **Committed in:** a54487c (Task 1 commit)

**2. [Rule 1 - Bug] scan.test.ts missing required DiscoveryScanParams fields**
- **Found during:** Task 2 (TypeScript check)
- **Issue:** `tests/discovery/scan.test.ts` baseParams missing `source_credentials`, `userTokens`, `existingProjectSummary` after DiscoveryScanParams was extended
- **Fix:** Added default values for all new fields to baseParams
- **Files modified:** `bigpanda-app/tests/discovery/scan.test.ts`
- **Verification:** TypeScript compiles clean for this file; 110 tests pass
- **Committed in:** Part of Task 2 staged changes

---

**Total deviations:** 2 auto-fixed (2 Rule 1 bugs)
**Impact on plan:** Both fixes required for compilation correctness. No scope creep.

## Issues Encountered
- Discovery-scanner.ts and scan route Task 2 changes were already present in HEAD from a prior 19.1-08 agent commit that ran before this plan. The adapter factory (Task 1) was new work; Task 2 changes merged cleanly since the prior agent had already implemented the same spec.
- `dedup.test.ts` fails due to pre-existing `server-only` import infrastructure issue unrelated to this plan — confirmed pre-existing before plan execution.

## Next Phase Readiness
- Adapter factory operational and tested GREEN
- Scanner uses adapter pattern end-to-end — no direct MCP calls in scanner
- Source credentials flow: settings.json (org) + DB (user OAuth) → scanner → adapters
- Ready for 19.1-06 (Source Connections UI) and 19.1-07 (Gmail OAuth flow)

---
*Phase: 19.1-source-integrations*
*Completed: 2026-03-26*
