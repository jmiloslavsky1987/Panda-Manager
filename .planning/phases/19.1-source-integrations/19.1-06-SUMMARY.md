---
phase: 19.1-source-integrations
plan: "06"
subsystem: ui
tags: [react, next.js, settings, oauth, credentials, source-connections, radix-ui]

# Dependency graph
requires:
  - phase: 19.1-05
    provides: source-credentials API schema, user_source_tokens DB table, Gmail OAuth flow
provides:
  - Settings page Source Connections tab with Slack/Gong/Glean credential forms and Gmail OAuth connect/disconnect
  - GET /api/oauth/gmail/status — gmail connection status + email
  - DELETE /api/oauth/gmail/status — gmail disconnect
  - GET /api/settings/source-credentials — masked credential hints for Slack/Gong/Glean
  - POST /api/settings/source-credentials — saves credentials to settings.json
affects: [19.1-07, 19.1-08, discovery-scan]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Source Connections tab pattern: per-source card with ConnectedBadge, setup instructions when unconnected, form fields, Save handler"
    - "POST source-credentials merges partial updates — only keys present in POST body are overwritten"
    - "Gmail OAuth: initiate via anchor href (no JS), success detected via ?success=gmail + history.replaceState"

key-files:
  created:
    - bigpanda-app/app/api/oauth/gmail/status/route.ts
    - bigpanda-app/app/api/settings/source-credentials/route.ts
  modified:
    - bigpanda-app/app/settings/page.tsx

key-decisions:
  - "Source Connections rendered as a third tab in Settings (Jobs / MCP Servers / Source Connections) — consistent UX, no new page needed"
  - "Gmail connect via anchor href to /api/oauth/gmail rather than JS fetch — necessary because OAuth flow is a browser redirect, not an API call"
  - "POST /api/settings/source-credentials merges partial credentials — Slack save does not clobber Gong config"
  - "Credential fields cleared after successful save — avoids stale plaintext in browser state"

patterns-established:
  - "ConnectedBadge component: green Connected / gray Not connected — reuse for future source cards"
  - "Setup instructions p block shown only when !source.connected — keeps UI clean when configured"

requirements-completed: [SRC-01, SRC-08]

# Metrics
duration: 4min
completed: 2026-03-26
---

# Phase 19.1 Plan 06: Source Connections UI Summary

**Settings page extended with Source Connections tab: Slack/Gong/Glean credential forms with masked status hints, Gmail OAuth connect/disconnect flow, and two new API routes (gmail status + source-credentials CRUD)**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-03-26T20:00:23Z
- **Completed:** 2026-03-26T20:03:41Z
- **Tasks:** 1
- **Files modified:** 3

## Accomplishments
- Added `GET /api/oauth/gmail/status` + `DELETE /api/oauth/gmail/status` — reads/removes gmail row from user_source_tokens
- Added `GET /api/settings/source-credentials` returning masked token hints; `POST` merging partial credentials into settings.json
- Extended Settings page with a third "Source Connections" tab containing four source cards (Slack, Gong, Glean, Gmail)
- Each card shows ConnectedBadge (green/gray), setup instructions when unconnected, and credential forms with Save buttons
- Gmail card uses anchor href for OAuth initiation; shows "Connected as [email]" + Disconnect button when connected
- Success banner shown after OAuth redirect (`?success=gmail`) with URL cleanup via `history.replaceState`

## Task Commits

1. **Task 1: Add Gmail status API + Source Connections panel** - `289dc01` (feat)

**Plan metadata:** TBD (docs commit)

## Files Created/Modified
- `bigpanda-app/app/api/oauth/gmail/status/route.ts` — GET (connection status) + DELETE (disconnect) for gmail tokens
- `bigpanda-app/app/api/settings/source-credentials/route.ts` — GET (masked hints) + POST (merge + save credentials)
- `bigpanda-app/app/settings/page.tsx` — Source Connections tab with Slack/Gong/Glean/Gmail cards

## Decisions Made
- Source Connections added as a new tab in existing Settings page (not a separate route) — avoids nav changes
- Gmail connect uses anchor href to `/api/oauth/gmail` — browser redirect required, JS fetch cannot follow OAuth redirects
- POST /api/settings/source-credentials uses merge semantics — each source's save only updates its own keys

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Settings page path in the plan was `app/(app)/settings/page.tsx` but the actual file is `app/settings/page.tsx` (no route group). Discovered via glob search, executed against the correct path.

## User Setup Required
None - no external service configuration required for this plan. Users configure sources via the new UI, which uses already-deployed API routes and DB tables.

## Self-Check: PASSED

All files confirmed present. Commit 289dc01 verified in git log.

## Next Phase Readiness
- Source Connections UI complete — users can now configure Slack/Gong/Glean credentials and connect Gmail
- All four source panels show correct connected/unconnected state from live API
- Ready for 19.1-07 (scanner integration with REST adapters using stored credentials)

---
*Phase: 19.1-source-integrations*
*Completed: 2026-03-26*
