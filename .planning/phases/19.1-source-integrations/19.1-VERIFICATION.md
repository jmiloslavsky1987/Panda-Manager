---
phase: 19.1-source-integrations
verified: 2026-03-26T21:00:00Z
status: passed
score: 7/7 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Gmail OAuth end-to-end flow"
    expected: "Connect Gmail button redirects to Google consent page (or shows a clear env-var-missing error, not a crash). After OAuth completes, /settings shows 'Connected as [email]'."
    why_human: "Requires a real Google Cloud project with GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET / GOOGLE_REDIRECT_URI configured. Cannot exercise the live OAuth token exchange with grepping alone."
  - test: "Source Connections panel visual layout"
    expected: "All 4 source cards (Slack, Gong, Glean, Gmail) render at /settings#source-connections with 'Not connected' badges when no credentials are saved. Save actions show 'Saved.' confirmation without console errors."
    why_human: "UI visual confirmation of tab navigation, badge rendering, and save feedback is human-only."
---

# Phase 19.1: Source Integrations — Verification Report

**Phase Goal:** Team members can connect the discovery scanner to real data sources — Slack, Gong, and Glean via shared org-level credentials stored in app Settings, and Gmail via per-user OAuth with guided setup instructions. MCP remains available as an alternate backend when configured in settings.json.

**Verified:** 2026-03-26
**Status:** PASSED (2 items flagged for human confirmation — all automated checks passed)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Slack/Gong/Glean bot tokens and API keys can be entered and saved in Settings UI | VERIFIED | `app/settings/page.tsx` — full Slack/Gong/Glean form state + `handleSaveSlack/Gong/Glean` handlers POST to `/api/settings/source-credentials`; route validates with Zod and merges into settings.json |
| 2 | Gmail per-user OAuth connect flow is implemented end-to-end | VERIFIED | `app/api/oauth/gmail/route.ts` (initiate), `app/api/oauth/gmail/callback/route.ts` (exchange + upsert), `app/api/oauth/gmail/status/route.ts` (GET/DELETE); CSRF state cookie verified; `userSourceTokens` table in schema |
| 3 | Settings UI shows connection status for each source | VERIFIED | `ConnectedBadge` component used for Slack/Gong/Glean status; `gmailStatus.connected` drives Gmail badge; `fetchSourceStatus()` called on mount from both `/api/settings/source-credentials` and `/api/oauth/gmail/status` |
| 4 | Discovery scanner uses REST adapters when credentials are configured | VERIFIED | `resolveAdapter()` in `source-adapters/index.ts` — REST path checked before MCP fallback. `discovery-scanner.ts` calls `resolveAdapter()` per source. `runDiscoveryScan` accepts `source_credentials` and `userTokens` |
| 5 | MCP fallback still works when no REST credentials configured | VERIFIED | `resolveAdapter()` falls through to `MCPAdapter` when `mcpServer?.enabled` is true. `MCPAdapter` in `mcp-adapter.ts` preserves exact original call pattern with `anthropic-beta: mcp-client-2025-11-20` |
| 6 | Users without credentials see actionable setup instructions | VERIFIED | Settings UI shows inline instruction blocks per source card when `!sourceStatus.slack` / `!sourceStatus.gong` / etc. Scanner logs `"Configure in Settings > Source Connections."` warning and skips (no silent failure) |
| 7 | Discovery scanner flags likely-duplicate items; queue groups them separately | VERIFIED | `DISCOVERY_SYSTEM` instructs Claude to set `likely_duplicate`. `existingProjectSummary` injected into prompt. DB column `likely_duplicate BOOLEAN NOT NULL DEFAULT FALSE` in schema + migration 0015. `ReviewQueue.tsx` splits items into `newItems` vs `likelyDuplicates`; duplicates rendered in collapsed `<details>` with amber "May already exist" badge |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact | Provides | Status | Details |
|----------|----------|--------|---------|
| `bigpanda-app/lib/source-adapters/index.ts` | `SourceAdapter` interface, `SourceCredentials` type, `resolveAdapter` factory | VERIFIED | 133 lines; exports interface, types, and fully-implemented factory with REST-priority logic |
| `bigpanda-app/lib/source-adapters/slack-adapter.ts` | REST Slack adapter (conversations.history) | VERIFIED | 64 lines; real fetch to `slack.com/api/conversations.history`; oldest param, multi-channel, non-fatal error handling |
| `bigpanda-app/lib/source-adapters/gong-adapter.ts` | REST Gong adapter (call list + transcripts) | VERIFIED | 85 lines; two-step: `/v2/calls` pagination then `/v2/calls/transcript` batch; Basic Auth |
| `bigpanda-app/lib/source-adapters/glean-adapter.ts` | REST Glean adapter (search API) | VERIFIED | 73 lines; Bearer token, `X-Glean-ActAs` header, 403 guard, `actAsEmail` warning |
| `bigpanda-app/lib/source-adapters/gmail-adapter.ts` | Gmail adapter using googleapis OAuth | VERIFIED | 63 lines; uses `google.auth.OAuth2` with stored refresh_token; auto-refresh; date-filtered query |
| `bigpanda-app/lib/source-adapters/mcp-adapter.ts` | MCPAdapter wrapping existing MCP beta call | VERIFIED | 60 lines; preserves original `anthropic.beta.messages.create` call with `mcp-client-2025-11-20` header |
| `bigpanda-app/app/api/oauth/gmail/route.ts` | Gmail OAuth initiate — redirect to consent screen | VERIFIED | 36 lines; env-var guard returns JSON error (not crash); CSRF state cookie; `access_type: 'offline'`, `prompt: 'consent'` |
| `bigpanda-app/app/api/oauth/gmail/callback/route.ts` | Gmail OAuth callback — exchange code + store token | VERIFIED | 86 lines; CSRF check; `onConflictDoUpdate` upsert; email extracted from id_token; redirects to `/settings?success=gmail` |
| `bigpanda-app/app/api/oauth/gmail/status/route.ts` | Gmail status GET + DELETE (disconnect) | VERIFIED | 32 lines; DB lookup by user_id='default' + source='gmail'; DELETE removes token row |
| `bigpanda-app/app/api/settings/source-credentials/route.ts` | GET status hints / POST save org credentials | VERIFIED | 98 lines; Zod validation; masks raw tokens before returning; merge-only (no clobber unrelated keys) |
| `bigpanda-app/app/settings/page.tsx` | Settings page with Source Connections tab | VERIFIED | 837 lines; full Slack/Gong/Glean form state + handlers; Gmail OAuth button + disconnect; `fetchSourceStatus` on mount; success banner after OAuth redirect |
| `bigpanda-app/db/migrations/0014_source_tokens.sql` | `user_source_tokens` DDL | VERIFIED | `CREATE TABLE IF NOT EXISTS user_source_tokens` with UNIQUE(user_id, source) |
| `bigpanda-app/db/migrations/0015_discovery_dedup_flag.sql` | `likely_duplicate` column DDL | VERIFIED | `ALTER TABLE discovery_items ADD COLUMN IF NOT EXISTS likely_duplicate BOOLEAN NOT NULL DEFAULT FALSE` |
| `bigpanda-app/db/schema.ts` | Drizzle `userSourceTokens` + `likely_duplicate` in `discoveryItems` | VERIFIED | Both confirmed present at lines 458-463 (likely_duplicate) and 582-594 (userSourceTokens) |
| `bigpanda-app/lib/settings-core.ts` | `AppSettings.source_credentials` field | VERIFIED | `source_credentials?: SourceCredentials` in interface; merged on read with DEFAULTS |
| `bigpanda-app/lib/discovery-scanner.ts` | Adapter-pattern scanner with dedup analysis | VERIFIED | `resolveAdapter` per source; `existingProjectSummary` in prompt; `likely_duplicate` in `DiscoveryItem` and `DISCOVERY_SYSTEM` |
| `bigpanda-app/app/api/discovery/scan/route.ts` | Scan route passing credentials + project context | VERIFIED | `source_credentials`, `userTokens`, `existingProjectSummary` all built and passed; `likely_duplicate` stored on insert |
| `bigpanda-app/worker/jobs/discovery-scan.ts` | Worker mirrors scan route credential + dedup logic | VERIFIED | Parallel fetch of settings/user tokens/project context; `likely_duplicate` stored on insert |
| `bigpanda-app/components/ReviewQueue.tsx` | Queue with collapsed duplicate section | VERIFIED | `newItems` vs `likelyDuplicates` split; `<details>` collapsed section with amber badge |
| `bigpanda-app/components/QueueItemRow.tsx` | `DiscoveryQueueItem.likely_duplicate` field | VERIFIED | `likely_duplicate?: boolean` in interface at line 16 |

---

### Key Link Verification

| From | To | Via | Status | Evidence |
|------|----|-----|--------|----------|
| `settings/page.tsx` | `/api/settings/source-credentials` | `handleSaveSlack/Gong/Glean` POST + `fetchSourceStatus` GET | WIRED | Lines 155-162, 274, 293, 315 |
| `settings/page.tsx` | `/api/oauth/gmail` | `<a href="/api/oauth/gmail">` Connect Gmail link | WIRED | Line 808 |
| `settings/page.tsx` | `/api/oauth/gmail/status` | `fetchSourceStatus` GET + `handleDisconnectGmail` DELETE | WIRED | Lines 156-162, 336 |
| `discovery-scanner.ts` | `source-adapters/index.ts` | `import { resolveAdapter }` + call per source in loop | WIRED | Lines 13-18, 111-116 |
| `app/api/discovery/scan/route.ts` | `db/schema.ts` | `db.insert(discoveryItems).values({ ..., likely_duplicate: item.likely_duplicate ?? false })` | WIRED | Line 202 |
| `app/api/discovery/scan/route.ts` | `db` (actions/risks/stakeholders) | `db.select()` fetches per-project data, builds `existingProjectSummary` | WIRED | Lines 113-149 |
| `worker/jobs/discovery-scan.ts` | `runDiscoveryScan` | Passes `source_credentials`, `userTokens`, `existingProjectSummary`; stores `likely_duplicate` | WIRED | Lines 111-163 |
| `ReviewQueue.tsx` | `QueueItemRow.tsx` | `item.likely_duplicate` filter; renders `QueueItemRow` in duplicate section | WIRED | Lines 118-119, 177-181 |
| `resolveAdapter` (index.ts) | `MCPAdapter` | `mcpServer?.enabled` fallback to `new MCPAdapter(mcpServer, toolName)` | WIRED | Lines 123-129 |

---

### Requirements Coverage

SRC requirements are defined in `19.1-CONTEXT.md` (not in global `REQUIREMENTS.md`, which uses DISC-* for the broader discovery feature set). Cross-reference below is against `19.1-CONTEXT.md` definitions.

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SRC-01 | 06, 07 | Settings UI shows "Source Connections" panel with status for Slack, Gong, Glean, Gmail | SATISFIED | `settings/page.tsx` Source Connections tab with `ConnectedBadge` per source; human-verified per SUMMARY-07 |
| SRC-02 | 01, 02, 05, 06, 07 | Slack bot token can be entered/saved; scanner uses it | SATISFIED | `slack-adapter.ts` implements REST fetch; `resolveAdapter` selects `SlackAdapter` when token present; settings UI saves token |
| SRC-03 | 01, 02, 05, 06, 07 | Gong API key can be entered/saved; scanner uses it for transcripts | SATISFIED | `gong-adapter.ts` implements two-step transcript fetch; `resolveAdapter` selects `GongAdapter` when credentials present |
| SRC-04 | 01, 03, 05, 06, 07 | Glean API token can be entered/saved; scanner uses it | SATISFIED | `glean-adapter.ts` implements search API with bearer + actAs; `resolveAdapter` selects `GleanAdapter` when credentials present |
| SRC-05 | 03, 04, 06, 07 | Gmail OAuth connect flow works; per-user token stored and used | SATISFIED (human pending) | OAuth initiate/callback/status routes implemented; `gmail-adapter.ts` uses stored refresh_token; human verification needed for live flow |
| SRC-06 | 01, 04, 05, 07 | Discovery scanner uses REST adapters when credentials configured | SATISFIED | `resolveAdapter` REST-priority logic; scanner calls adapters per source; `source_credentials` and `userTokens` passed through full call chain |
| SRC-07 | 03, 05, 07 | MCP adapters still work when MCP configured in settings.json (no regression) | SATISFIED | `MCPAdapter` extracted verbatim; `resolveAdapter` falls through to MCP when no REST creds and `mcpServer?.enabled`; original call pattern preserved |
| SRC-08 | 05, 06, 07 | Users without credentials see clear setup instructions, not silent failure | SATISFIED | Settings UI shows per-source instruction blocks when not connected; scanner logs `"Configure in Settings > Source Connections."` and skips (non-fatal) |
| SRC-09 | 08, 07 | Discovery scanner cross-references items against existing project data; likely duplicates flagged in queue | SATISFIED | `likely_duplicate` in `DiscoveryItem`, `DISCOVERY_SYSTEM`, `existingProjectSummary` injected into Claude prompt, DB column, ReviewQueue collapsed section |

**Orphaned requirements check:** SRC-01 through SRC-09 are all claimed by at least one plan. No orphaned requirements found.

**Note on REQUIREMENTS.md traceability:** SRC-01 through SRC-09 are phase-internal requirements defined in `19.1-CONTEXT.md` and ROADMAP.md. They do not appear in the global `REQUIREMENTS.md` traceability table, which maps only DISC-*/SCHEMA-*/ING-* etc. to phases. This is expected — Phase 19.1 was an inserted sub-phase; its requirements were documented in the phase context file rather than the global requirements registry. This is an informational observation, not a gap.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `source-adapters/index.ts` | 131 | `return null` | Info | Intentional sentinel — documented behavior (caller emits SSE warning and skips source) |

No stubs, TODOs, empty implementations, or unconnected artifacts found.

---

### Test Suite Status

**Full suite run (2026-03-26):** 118 tests passed, 0 failed across 26 test files.

```
Test Files  26 passed (26)
Tests  118 passed (118)
Duration  2.92s
```

Adapter-level tests confirmed: `slack-adapter.test.ts`, `gong-adapter.test.ts`, `glean-adapter.test.ts`, `index.test.ts` all part of the passing suite.

---

### Commits Verified

All commits claimed in SUMMARYs confirmed in git log and exist with expected file changes:

| Hash | Commit | Verified |
|------|--------|---------|
| `8a825a0` | feat(19.1-01): DB migration + Drizzle schema + AppSettings extension | EXISTS |
| `2a4dca4` | feat(19.1-02): implement GongAdapter (GREEN) | EXISTS |
| `1612da9` | feat(19.1-03): implement GleanAdapter | EXISTS |
| `3bbaef7` | feat(19.1-03): extract MCPAdapter from discovery-scanner.ts | EXISTS |
| `50cb540` | feat(19.1-04): install googleapis + Gmail OAuth initiate route | EXISTS |
| `a53e02b` | feat(19.1-04): Gmail OAuth callback route + GmailAdapter | EXISTS |
| `a54487c` | feat(19.1-05): implement resolveAdapter factory | EXISTS |
| `289dc01` | feat(19.1-06): add Source Connections panel to Settings page | EXISTS |
| `514c749` | feat(19.1-08): DB migration + schema — add likely_duplicate | EXISTS |
| `c9df2c9` | feat(19.1-08): enhance discovery-scanner with project context + dedup flag | EXISTS |
| `cf5e104` | feat(19.1-08): scan route fetches project context + queue renders duplicate section | EXISTS |
| `16a39d3` | chore(19.1-07): add test script + server-only mock for vitest | EXISTS |

---

### Human Verification Required

#### 1. Gmail OAuth End-to-End Flow (SRC-05)

**Test:** Navigate to `/settings`, click the "Source Connections" tab, then click "Connect Gmail".
**Expected:** Either (a) browser redirects to Google OAuth consent screen requesting `gmail.readonly` scope (when env vars are set), OR (b) a clear JSON error appears with instructions about `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI` (when env vars are absent) — NOT a 500 crash page. If completing the full OAuth flow: verify `/settings` shows "Connected as [email]" and a "Disconnect" button.
**Why human:** Requires live Google Cloud project credentials and a real Google account. The OAuth token exchange cannot be exercised with static analysis.

#### 2. Settings UI Source Connections Panel Visual (SRC-01, SRC-08)

**Test:** Navigate to `/settings` → click "Source Connections" tab. Verify all 4 source cards (Slack, Gong, Glean, Gmail) are visible with gray "Not connected" badges when no credentials are configured. Enter a dummy Slack token (`xoxb-test`) and click Save — verify "Saved." text appears briefly without console errors.
**Why human:** Tab navigation, badge rendering, and save-confirmation feedback are UI behaviors requiring visual inspection.

---

### Summary

Phase 19.1 goal is achieved. All seven observable truths are verified by direct code inspection:

- Four REST adapters (Slack, Gong, Glean, Gmail) are fully implemented and wired to the adapter factory.
- The `resolveAdapter` factory correctly prioritizes REST credentials over MCP, with `MCPAdapter` as a preserved fallback.
- The Settings page has a complete "Source Connections" tab with forms for each source, status badges, setup instructions when not connected, and a Gmail OAuth connect/disconnect flow.
- All API routes (`/api/settings/source-credentials`, `/api/oauth/gmail`, `/api/oauth/gmail/callback`, `/api/oauth/gmail/status`) are substantive implementations — no stubs.
- The `user_source_tokens` table and migration exist for Gmail per-user token storage.
- `AppSettings.source_credentials` is extended and propagated to the scanner and worker.
- The dedup signal (SRC-09) is fully wired: `likely_duplicate` column, Claude prompt instruction, project context injection, DB storage, and ReviewQueue UI collapse.
- 118 automated tests pass green.

Two items require human confirmation (Gmail OAuth live flow and Settings UI visual), which is expected and was planned as a human-gated checkpoint in Plan 07.

---

_Verified: 2026-03-26_
_Verifier: Claude (gsd-verifier)_
