---
phase: 19.1-source-integrations
plan: 07
type: execute
wave: 3
depends_on:
  - 19.1-06
  - 19.1-08
files_modified: []
autonomous: false
requirements:
  - SRC-01
  - SRC-05
  - SRC-08
  - SRC-09

must_haves:
  truths:
    - "Settings Source Connections panel renders and is navigable in the browser"
    - "Slack credential form saves without console errors"
    - "Gmail Connect button initiates OAuth flow (or shows expected env-var-missing error)"
    - "Full adapter test suite passes GREEN"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the Settings Source Connections panel and end-to-end adapter test suite green-light before Phase 19.1 is marked complete.

Purpose: Visual UI verification (SRC-01, SRC-08) and Gmail OAuth flow (SRC-05) cannot be automated — requires a human to confirm the UI renders correctly and the OAuth redirect initiates.
Output: Phase 19.1 human-verified complete.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-source-integrations/19.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite green-light</name>
  <files></files>
  <action>
Run the complete test suite to confirm all adapter tests pass and no regressions exist:

```bash
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run --coverage
```

Expected: All tests PASS. Report any failures.

Also run TypeScript compile check:
```bash
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx tsc --noEmit
```

Expected: No errors.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>Full test suite PASS. No TypeScript errors. Output logged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Source Connections panel in Settings page with:
- Slack: bot token + channel IDs inputs with Save + status badge + setup instructions
- Gong: access key + secret + base URL inputs with Save + status badge + setup instructions
- Glean: token + instance URL + actAs email inputs with Save + status badge + setup instructions
- Gmail: Connect Gmail button (OAuth initiate) + connected status when token stored
  </what-built>
  <how-to-verify>
1. Start the dev server: `cd bigpanda-app && npm run dev`
2. Navigate to http://localhost:3001/settings
3. Scroll to "Source Connections" section — verify all 4 source cards are visible
4. Verify each card shows "Not connected" status badge (gray) when no credentials configured
5. Enter a test Slack bot token (or dummy value like `xoxb-test`) and click Save — verify no console errors, page shows saved state
6. Click "Connect Gmail" — verify it either: (a) redirects to Google consent page (if env vars set), or (b) shows a clear error message about missing GOOGLE_CLIENT_ID env var (not a 500 crash page)
7. Verify Gong card shows Base URL field with placeholder "https://us-XXXX.api.gong.io"
8. Verify Glean card shows ActAs Email field with helper text about global tokens
9. If Gmail OAuth env vars are configured: complete the full OAuth flow and verify "Connected as [email]" appears after redirect back to /settings?success=gmail
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which items failed with specific observations</resume-signal>
</task>

</tasks>

<verification>
- Full test suite GREEN with coverage output
- TypeScript compiles with no errors
- Human confirmed: Source Connections panel renders with 4 source cards
- Human confirmed: Save actions don't crash
- Human confirmed: Gmail OAuth button behavior (redirect or clear env-var error)
</verification>

<success_criteria>
Phase 19.1 complete: all 8 requirements (SRC-01 through SRC-08) satisfied. Adapter tests GREEN. Settings UI human-verified. Discovery scanner refactored to use adapter pattern with MCP backward compatibility preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-source-integrations/19.1-07-SUMMARY.md`
</output>
