---
phase: 072-feature-unification
plan: "04"
type: execute
wave: 2
depends_on:
  - "072-01"
  - "072-02"
  - "072-03"
files_modified: []
autonomous: false
requirements:
  - RFCTR-04
must_haves:
  truths:
    - "Production build passes with zero TypeScript errors"
    - "Full Vitest test suite passes (148+ tests)"
    - "Risks table has a visible text search input that filters results"
    - "Milestones table has a visible text search input that filters results"
    - "WorkstreamTableClient shows EmptyState component (not paragraph text) when no workstreams exist"
    - "DB migration file 0036 exists and contains normalization UPDATEs"
    - "No dead files named SearchBar.tsx, GlobalSearchBar.tsx, or InlineEditModal.tsx remain"
  artifacts:
    - path: "bigpanda-app/db/migrations/0036_risk_milestone_status_enums.sql"
      provides: "Forward-only SQL migration for status enum enforcement"
    - path: "bigpanda-app/tests/schema/severity-enum-parity.test.ts"
      provides: "Severity parity guard test"
  key_links:
    - from: "all modified components"
      to: "production build"
      via: "npm run build"
      pattern: "Compiled successfully"
---

<objective>
Human verification gate for Phase 72 Feature Unification. Confirm all 13 audit findings are resolved — either by code change (9 findings) or documented as intentional (4 findings). Verify the production build and test suite are clean before closing the phase.

Purpose: Phase 72 gate. All duplicates and inconsistencies from the Phase 71 audit must be confirmed resolved before proceeding to Phase 73.
Output: Human-approved confirmation that all findings are addressed. Phase 72 complete.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/072-feature-unification/072-CONTEXT.md
@.planning/phases/072-feature-unification/072-01-SUMMARY.md
@.planning/phases/072-feature-unification/072-02-SUMMARY.md
@.planning/phases/072-feature-unification/072-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build and test suite validation</name>
  <files></files>
  <action>
Run the full production build:
```bash
cd bigpanda-app && npm run build 2>&1 | tail -20
```

Run the full Vitest test suite:
```bash
cd bigpanda-app && npx vitest run 2>&1 | tail -30
```

Confirm:
- Build exits with zero errors
- All tests pass (count should be 148 or higher)
- No references to old component names remain:
```bash
grep -rn "from.*components/SearchBar\b\|from.*GlobalSearchBar\b\|from.*teams/InlineEditModal\b" bigpanda-app --include="*.tsx" --include="*.ts" | grep -v node_modules
```

If any failures, diagnose and fix them before the checkpoint.
  </action>
  <verify>
    <automated>cd bigpanda-app && npm run build 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0 && echo "BUILD CLEAN" || echo "BUILD HAS ERRORS"</automated>
  </verify>
  <done>Build exits clean. All tests pass. grep returns no stale component references.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all 13 Phase 71 findings resolved</name>
  <files></files>
  <action>Present verification checklist to user. All prior automated work in Task 1 must pass before this checkpoint executes.</action>
  <verify>
    <automated>echo "Human verification required — see how-to-verify steps"</automated>
  </verify>
  <done>User types "approved" confirming all 9 verification steps pass</done>
  <what-built>
Phase 72 Feature Unification — all 13 audit findings from Phase 71 addressed:

CODE CHANGES (9 findings):
1. DB enums: riskStatusEnum + milestoneStatusEnum added to schema.ts; migration 0036 with data normalization
2. migrate-local.ts: status validation guards for risks and milestones (matches severity pattern)
3. RisksTableClient: text search input (description + mitigation fields)
4. MilestonesTableClient: text search input (name + notes fields)
5. WorkstreamTableClient: EmptyState component replaces inline paragraph
6. DecisionsTableClient: filter-zero-results wrapped in <TableRow><TableCell colSpan={N}> (in-table empty row pattern spanning all columns)
7. SearchBar.tsx renamed to GlobalProjectSearchBar.tsx; all import sites updated
8. GlobalSearchBar.tsx renamed to WorkspaceSearchBar.tsx; all import sites updated
9. InlineEditModal.tsx renamed to TeamMetadataEditModal.tsx; all import sites updated

DOCUMENTED AS INTENTIONAL (4 findings — inline comments added):
10. Decisions: no bulk actions (append-only design) — comment in DecisionsTableClient.tsx
11. Workstreams: no bulk actions (progress-slider UX) — comment in WorkstreamTableClient.tsx
12. Decisions: no edit modal (immutable records) — comment in DecisionsTableClient.tsx
13. Severity enum duplication: parity test added (tests/schema/severity-enum-parity.test.ts)
  </what-built>
  <how-to-verify>
1. Start dev server: `cd bigpanda-app && npm run next-only`
2. Open any project workspace, Delivery tab, Risks sub-tab
   - Verify a text search input appears in the filter bar (placeholder "Search risks...")
   - Type a keyword — confirm table filters in real time
3. Open Delivery tab, Milestones sub-tab
   - Verify a text search input appears in the filter bar (placeholder "Search milestones...")
   - Type a keyword — confirm table filters in real time
4. Open Delivery tab, Workstreams sub-tab on a project with no workstreams
   - Verify "No workstreams yet" EmptyState component appears (styled card, not plain text)
5. Open Delivery tab, Decisions sub-tab, apply a filter with no matching results
   - Verify the no-results message appears as an in-table row spanning all columns — not a standalone paragraph below the table (TableRow/TableCell pattern matching Actions, Risks, Milestones)
6. Navigate to global search (top header bar) — confirm component still works (now GlobalProjectSearchBar)
7. Use workspace search within a project — confirm live dropdown still works (now WorkspaceSearchBar)
8. Open Teams tab, Business Outcomes or Focus Areas section — confirm edit modal still opens correctly (now TeamMetadataEditModal internally)
9. Open bigpanda-app/components/WorkstreamTableClient.tsx — confirm two intentional comments are present: one for bulk-actions and one for no-filter-bar (finding 3.3 documented as intentional: workstream counts remain small)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `cd bigpanda-app && npm run build` — clean exit
2. `cd bigpanda-app && npx vitest run` — all tests pass
3. `grep -rn "from.*SearchBar\b\|from.*GlobalSearchBar\b\|from.*InlineEditModal\b" bigpanda-app --include="*.tsx" --include="*.ts"` — empty output
4. Human approval of all 8 functional verification steps
</verification>

<success_criteria>
- Production build passes with zero errors
- Full test suite passes (148+ tests)
- All 13 Phase 71 audit findings are resolved (9 code changes + 4 documented intentional)
- Human approves all verification steps
- Phase 72 is complete; RFCTR-04 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/072-feature-unification/072-04-SUMMARY.md`
</output>
