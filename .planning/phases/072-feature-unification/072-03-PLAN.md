---
phase: 072-feature-unification
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - bigpanda-app/components/SearchBar.tsx
  - bigpanda-app/components/GlobalProjectSearchBar.tsx
  - bigpanda-app/components/GlobalSearchBar.tsx
  - bigpanda-app/components/WorkspaceSearchBar.tsx
  - bigpanda-app/components/teams/InlineEditModal.tsx
  - bigpanda-app/components/teams/TeamMetadataEditModal.tsx
  - bigpanda-app/app/layout.tsx
  - bigpanda-app/app/customer/[id]/layout.tsx
  - bigpanda-app/components/AppChrome.tsx
  - bigpanda-app/components/HeaderBar.tsx
  - bigpanda-app/components/teams/BusinessOutcomesSection.tsx
  - bigpanda-app/components/teams/E2eWorkflowsSection.tsx
  - bigpanda-app/components/teams/FocusAreasSection.tsx
  - bigpanda-app/tests/search/global-search.test.tsx
  - bigpanda-app/tests/schema/severity-enum-parity.test.ts
autonomous: true
requirements:
  - RFCTR-04
must_haves:
  truths:
    - "SearchBar.tsx is renamed to GlobalProjectSearchBar.tsx; all import sites updated"
    - "GlobalSearchBar.tsx is renamed to WorkspaceSearchBar.tsx; all import sites (including test) updated"
    - "InlineEditModal.tsx in components/teams/ is renamed to TeamMetadataEditModal.tsx; all import sites updated"
    - "A Vitest test asserts SEVERITY_OPTIONS values exactly match the DB severity enum values"
    - "Each renamed file has an inline comment documenting its scope"
  artifacts:
    - path: "bigpanda-app/components/GlobalProjectSearchBar.tsx"
      provides: "Renamed from SearchBar.tsx — all-projects search, routes to /search?q="
      contains: "intentional: GlobalProjectSearchBar searches across all projects"
    - path: "bigpanda-app/components/WorkspaceSearchBar.tsx"
      provides: "Renamed from GlobalSearchBar.tsx — single-workspace search with live results"
      contains: "intentional: WorkspaceSearchBar searches within a single project workspace"
    - path: "bigpanda-app/components/teams/TeamMetadataEditModal.tsx"
      provides: "Renamed from InlineEditModal.tsx — edits project-level team metadata (business outcomes, focus areas)"
    - path: "bigpanda-app/tests/schema/severity-enum-parity.test.ts"
      provides: "Vitest test asserting SEVERITY_OPTIONS values === DB enum values"
  key_links:
    - from: "bigpanda-app/app/layout.tsx"
      to: "bigpanda-app/components/GlobalProjectSearchBar.tsx"
      via: "Import renamed from SearchBar"
    - from: "bigpanda-app/app/customer/[id]/layout.tsx"
      to: "bigpanda-app/components/WorkspaceSearchBar.tsx"
      via: "Import renamed from GlobalSearchBar"
    - from: "bigpanda-app/tests/search/global-search.test.tsx"
      to: "bigpanda-app/components/WorkspaceSearchBar.tsx"
      via: "Test import updated to WorkspaceSearchBar"
---

<objective>
Rename three components to clarify their distinct scopes, add intentional-scope comments to each, add a Vitest test asserting SEVERITY_OPTIONS parity with the DB enum, and add intentional-scope comments to each renamed file. This resolves the low-priority naming confusion and severity duplication findings from the Phase 71 audit.

Purpose: Eliminate misleading names. "GlobalSearchBar" currently means project-scoped, "SearchBar" currently means global — the naming is backwards. Rename to reflect actual scope.
Output: Three renamed component files, all import sites updated, one new test file.

Note on file count: This plan touches 15 files and is at the scope threshold. Splitting is NOT required: every change is a mechanical rename or import-path update — no logic changes, no new abstractions, no shared mutable state between tasks. Per-file complexity is minimal (1-3 line changes per import site). Tasks are naturally ordered: Task 1 handles SearchBar/GlobalSearchBar (7 files), Task 2 handles InlineEditModal + new test (5 files), Task 3 is build+test verification (0 new files). Each task has its own grep-clean check to verify no stale references before proceeding. Quality degradation risk is lower than for 15-file logic changes because the executor only needs to track "old name → new name" — no API contracts or state to reason about.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/072-feature-unification/072-CONTEXT.md

<interfaces>
<!-- Import sites discovered during planning. -->

Files importing SearchBar (to be renamed GlobalProjectSearchBar):
- bigpanda-app/app/layout.tsx:4  → `import { SearchBar } from '../components/SearchBar'`
- bigpanda-app/components/AppChrome.tsx → comments reference SearchBar
- bigpanda-app/components/HeaderBar.tsx → references SearchBar in comments + renders component

Files importing GlobalSearchBar (to be renamed WorkspaceSearchBar):
- bigpanda-app/app/customer/[id]/layout.tsx:6 → `import GlobalSearchBar from '../../../components/GlobalSearchBar'`
- bigpanda-app/tests/search/global-search.test.tsx:11 → `import GlobalSearchBar from '@/components/GlobalSearchBar'`

Files importing InlineEditModal (to be renamed TeamMetadataEditModal):
- bigpanda-app/components/teams/BusinessOutcomesSection.tsx
- bigpanda-app/components/teams/E2eWorkflowsSection.tsx
- bigpanda-app/components/teams/FocusAreasSection.tsx

Existing test directory for new test: bigpanda-app/tests/schema/ (contains project-dependencies.test.ts, wbs-items.test.ts)

SEVERITY_OPTIONS is in bigpanda-app/components/RisksTableClient.tsx lines 29-34.
DB severityEnum values are in bigpanda-app/db/schema.ts lines 44-49: ['low', 'medium', 'high', 'critical']
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename SearchBar → GlobalProjectSearchBar and GlobalSearchBar → WorkspaceSearchBar</name>
  <files>bigpanda-app/components/GlobalProjectSearchBar.tsx, bigpanda-app/components/WorkspaceSearchBar.tsx, bigpanda-app/app/layout.tsx, bigpanda-app/app/customer/[id]/layout.tsx, bigpanda-app/components/AppChrome.tsx, bigpanda-app/components/HeaderBar.tsx, bigpanda-app/tests/search/global-search.test.tsx</files>
  <action>
**Step 1 — Rename SearchBar.tsx to GlobalProjectSearchBar.tsx:**
- Copy bigpanda-app/components/SearchBar.tsx to bigpanda-app/components/GlobalProjectSearchBar.tsx
- In the new file: rename the exported function/const from `SearchBar` to `GlobalProjectSearchBar`
- Add a file-level comment at the top (after any "use client" directive):
  `// intentional: GlobalProjectSearchBar searches across all projects — routes to /search?q=`
- Delete bigpanda-app/components/SearchBar.tsx

**Step 2 — Update all SearchBar import sites:**
- bigpanda-app/app/layout.tsx: change `import { SearchBar } from '../components/SearchBar'` to `import { GlobalProjectSearchBar } from '../components/GlobalProjectSearchBar'`; update the JSX usage from `<SearchBar />` to `<GlobalProjectSearchBar />`
- bigpanda-app/components/AppChrome.tsx: update any comments or references to SearchBar
- bigpanda-app/components/HeaderBar.tsx: update any comments or references to SearchBar; update import and JSX if it renders SearchBar

**Grep-clean check after SearchBar rename:**
Run `grep -rn "SearchBar\b" bigpanda-app --include="*.tsx" --include="*.ts" | grep -v "GlobalProject\|Workspace\|node_modules\|intentional"` — must return no results before proceeding to Step 3.

**Step 3 — Rename GlobalSearchBar.tsx to WorkspaceSearchBar.tsx:**
- Copy bigpanda-app/components/GlobalSearchBar.tsx to bigpanda-app/components/WorkspaceSearchBar.tsx
- In the new file: rename the exported default or named export from `GlobalSearchBar` to `WorkspaceSearchBar`
- Add a file-level comment:
  `// intentional: WorkspaceSearchBar searches within a single project workspace — scoped to one projectId`
- Delete bigpanda-app/components/GlobalSearchBar.tsx

**Step 4 — Update all GlobalSearchBar import sites:**
- bigpanda-app/app/customer/[id]/layout.tsx: change import path and component name from `GlobalSearchBar` to `WorkspaceSearchBar`
- bigpanda-app/tests/search/global-search.test.tsx: update import from `@/components/GlobalSearchBar` to `@/components/WorkspaceSearchBar`; update all references to the component name from `GlobalSearchBar` to `WorkspaceSearchBar`

**Grep-clean check after GlobalSearchBar rename:**
Run `grep -rn "GlobalSearchBar\b" bigpanda-app --include="*.tsx" --include="*.ts" | grep -v "node_modules\|intentional"` — must return no results before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd bigpanda-app && ls components/GlobalProjectSearchBar.tsx components/WorkspaceSearchBar.tsx && ! test -f components/SearchBar.tsx && ! test -f components/GlobalSearchBar.tsx && echo "RENAMED OK"</automated>
  </verify>
  <done>GlobalProjectSearchBar.tsx and WorkspaceSearchBar.tsx exist; SearchBar.tsx and GlobalSearchBar.tsx do not exist; all import sites updated; intentional comments in both new files; grep-clean checks pass (no stale references)</done>
</task>

<task type="auto">
  <name>Task 2: Rename InlineEditModal → TeamMetadataEditModal and add severity parity test</name>
  <files>bigpanda-app/components/teams/TeamMetadataEditModal.tsx, bigpanda-app/components/teams/BusinessOutcomesSection.tsx, bigpanda-app/components/teams/E2eWorkflowsSection.tsx, bigpanda-app/components/teams/FocusAreasSection.tsx, bigpanda-app/tests/schema/severity-enum-parity.test.ts</files>
  <action>
**Step 1 — Rename InlineEditModal.tsx to TeamMetadataEditModal.tsx:**
- Copy bigpanda-app/components/teams/InlineEditModal.tsx to bigpanda-app/components/teams/TeamMetadataEditModal.tsx
- In the new file: rename the exported component from `InlineEditModal` to `TeamMetadataEditModal`
- Add a file-level comment: `// intentional: TeamMetadataEditModal edits project-level team metadata (business outcomes, focus areas) — not entity rows`
- Delete bigpanda-app/components/teams/InlineEditModal.tsx

**Step 2 — Update all InlineEditModal import sites in the teams/ directory:**
- bigpanda-app/components/teams/BusinessOutcomesSection.tsx: update import path and component name
- bigpanda-app/components/teams/E2eWorkflowsSection.tsx: update import path and component name
- bigpanda-app/components/teams/FocusAreasSection.tsx: update import path and component name

**Grep-clean check after InlineEditModal rename:**
Run `grep -rn "InlineEditModal\b" bigpanda-app --include="*.tsx" --include="*.ts" | grep -v "node_modules\|intentional"` — must return no results before proceeding to Step 3.

**Step 3 — Create severity enum parity test:**

Create bigpanda-app/tests/schema/severity-enum-parity.test.ts:
```typescript
import { describe, it, expect } from 'vitest'
import { SEVERITY_OPTIONS } from '@/components/RisksTableClient'

/**
 * Asserts that SEVERITY_OPTIONS component constant values exactly match
 * the DB severityEnum values. If the DB enum changes, this test fails,
 * preventing silent divergence between DB storage and UI labels.
 *
 * DB enum (db/schema.ts): severityEnum = pgEnum('severity', ['low', 'medium', 'high', 'critical'])
 */
describe('Severity enum parity', () => {
  const DB_SEVERITY_VALUES = ['low', 'medium', 'high', 'critical'] as const

  it('SEVERITY_OPTIONS values exactly match DB enum values', () => {
    const componentValues = SEVERITY_OPTIONS.map(o => o.value)
    expect(componentValues).toEqual(expect.arrayContaining([...DB_SEVERITY_VALUES]))
    expect(DB_SEVERITY_VALUES).toEqual(expect.arrayContaining(componentValues))
    expect(componentValues).toHaveLength(DB_SEVERITY_VALUES.length)
  })

  it('No SEVERITY_OPTIONS value is missing from DB enum', () => {
    SEVERITY_OPTIONS.forEach(opt => {
      expect(DB_SEVERITY_VALUES).toContain(opt.value)
    })
  })
})
```

NOTE: This test requires SEVERITY_OPTIONS to be exported from RisksTableClient. Check if it is already exported. If not, add `export` to the const declaration (change `const SEVERITY_OPTIONS` to `export const SEVERITY_OPTIONS`).

Run `cd bigpanda-app && npx vitest run tests/schema/severity-enum-parity.test.ts` to verify the test passes.
  </action>
  <verify>
    <automated>cd bigpanda-app && npx vitest run tests/schema/severity-enum-parity.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>TeamMetadataEditModal.tsx exists in components/teams/; InlineEditModal.tsx does not exist; all three teams section components import TeamMetadataEditModal; grep-clean check shows no stale InlineEditModal references; severity parity test passes with 2 test cases passing</done>
</task>

<task type="auto">
  <name>Task 3: Build verification and full test run</name>
  <files></files>
  <action>
Run the full production build and test suite to confirm all renames, import updates, and the new test are clean:

```bash
cd bigpanda-app && npm run build 2>&1 | tail -20
```

Then run the full test suite:
```bash
cd bigpanda-app && npx vitest run 2>&1 | tail -30
```

If any test references the old component names (SearchBar, GlobalSearchBar, InlineEditModal), update those test files. The test at tests/search/global-search.test.tsx should already have been updated in Task 1.

If the build reveals any remaining references to old component names, fix them before proceeding.
  </action>
  <verify>
    <automated>cd bigpanda-app && npm run build 2>&1 | grep -E "^Error|error TS|Module not found" | grep -v "node_modules" | head -10</automated>
  </verify>
  <done>Production build passes with zero errors. All vitest tests pass (148+ tests). No references to SearchBar, GlobalSearchBar, or InlineEditModal in source files (grep clean).</done>
</task>

</tasks>

<verification>
1. `ls bigpanda-app/components/GlobalProjectSearchBar.tsx bigpanda-app/components/WorkspaceSearchBar.tsx` — both exist
2. `! ls bigpanda-app/components/SearchBar.tsx bigpanda-app/components/GlobalSearchBar.tsx 2>/dev/null` — old files gone
3. `ls bigpanda-app/components/teams/TeamMetadataEditModal.tsx` — exists; `! ls bigpanda-app/components/teams/InlineEditModal.tsx 2>/dev/null` — gone
4. `cd bigpanda-app && npx vitest run tests/schema/severity-enum-parity.test.ts` — passes
5. `cd bigpanda-app && npm run build 2>&1 | grep "error" | grep -v node_modules` — zero errors
</verification>

<success_criteria>
- GlobalProjectSearchBar.tsx replaces SearchBar.tsx; all 3 import sites updated; intentional comment in file
- WorkspaceSearchBar.tsx replaces GlobalSearchBar.tsx; all 2 import sites updated (including test); intentional comment in file
- TeamMetadataEditModal.tsx replaces InlineEditModal.tsx; all 3 teams section import sites updated; intentional comment in file
- tests/schema/severity-enum-parity.test.ts exists and passes (2 test cases)
- Production build passes; full vitest suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/072-feature-unification/072-03-SUMMARY.md`
</output>
