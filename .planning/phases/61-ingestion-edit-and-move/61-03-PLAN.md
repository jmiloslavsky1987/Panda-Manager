---
phase: 61-ingestion-edit-and-move
plan: 03
type: execute
wave: 3
depends_on:
  - 61-02
files_modified: []
autonomous: false
requirements:
  - INGEST-01
  - INGEST-05

must_haves:
  truths:
    - "Type dropdown appears only for note entities in ExtractionItemEditForm"
    - "Reclassified item moves to correct tab immediately after type change"
    - "Inline error indicator shown on row with empty primary field when approve is attempted"
    - "Error indicators clear after correcting the empty field and re-attempting approve"
  artifacts: []
  key_links: []
---

<objective>
Human verification gate: confirm that the four UI behaviors from VALIDATION.md that cannot be automated are working correctly in the running app.

Purpose: Visual and interactive behaviors — type dropdown rendering, tab-switch on reclassification, error indicator display, error clearing — require human eyes on a live browser session.
Output: Human sign-off or issue descriptions for gap closure.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-ingestion-edit-and-move/61-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server</name>
  <files></files>
  <action>
    Start the Next.js dev server so the human can verify the UI:
    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run next-only
    ```
    Server runs on http://localhost:3000. Confirm it starts without TypeScript or build errors.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Dev server running at http://localhost:3000, TypeScript build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify ingestion edit and reclassification UI behaviors</name>
  <action>Human verifies the four UI behaviors in the running app (see how-to-verify below).</action>
  <verify>
    <automated>MISSING — UI rendering and interactive behaviors require human verification</automated>
  </verify>
  <done>Human confirms all 4 UI tests pass: type dropdown, tab move, inline error, error clearing.</done>
  <what-built>
    Four UI changes to the ingestion extraction review modal:
    1. Type dropdown appears at the top of ExtractionItemEditForm for note entities only
    2. Reclassifying a note moves it to the target entity type tab immediately
    3. Attempting to approve items with empty primary fields shows inline "Required field empty" error
    4. Correcting an empty field and re-approving clears the error and proceeds
  </what-built>
  <how-to-verify>
    Navigate to any project at http://localhost:3000, go to Document Ingestion (the Context Hub or the "Ingest" button in the workspace).

    To test with real extraction results, upload a document that produces note entities. Alternatively, if test documents are not available, you can manually test by temporarily changing an entity's type in the browser console or checking the edit form expansion behavior.

    **Test 1 — Type dropdown for notes only (INGEST-05):**
    1. In the extraction review modal, go to the "Notes" tab
    2. Click any note item row to expand the edit form
    3. Verify: A "Reclassify as" dropdown appears at the TOP of the edit form with options: Note (keep as-is), Action, Task, Milestone, Decision, Risk
    4. Now click any non-note item row (e.g., an Action)
    5. Verify: The edit form does NOT show the reclassify dropdown — instead shows "Type: action" as a plain text label

    **Test 2 — Reclassification moves item to correct tab (INGEST-05):**
    1. In the Notes tab, expand a note item's edit form
    2. Change the "Reclassify as" dropdown from "Note (keep as-is)" to "Action"
    3. Verify: The note item immediately disappears from the Notes tab
    4. Switch to the Actions tab
    5. Verify: The item appears in the Actions tab with its content mapped to the description field
    6. Verify: The Notes tab badge count decreased by 1

    **Test 3 — Inline validation error on empty primary field (INGEST-01):**
    1. In the review modal, find any approved item
    2. Expand its edit form and clear the primary field (e.g., clear "Description" for an action)
    3. Save the edit
    4. Click "Submit Approved" button
    5. Verify: The submit is BLOCKED (no network request sent)
    6. Verify: The offending row shows a red "Required field empty" label inline on the row
    7. Verify: Other rows without errors are NOT affected

    **Test 4 — Error clears after correction (INGEST-01):**
    1. Continuing from Test 3 with the error visible
    2. Click the offending row to expand its edit form
    3. Type a value into the primary field and Save
    4. Verify: The "Required field empty" label disappears from the row
    5. Click "Submit Approved" again
    6. Verify: The approve proceeds normally (submission goes through)
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 tests pass. Describe any failures (e.g., "Test 2 failed: item did not move to Actions tab") so they can be addressed in a gap closure plan.</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all 4 UI behaviors listed in VALIDATION.md Manual-Only Verifications section.
</verification>

<success_criteria>
- Type dropdown visible for notes, absent for non-notes
- Reclassified note tab moves immediately
- Empty primary field blocks submit with inline error
- Corrected field clears error and allows approve
</success_criteria>

<output>
After completion, create `.planning/phases/61-ingestion-edit-and-move/61-03-SUMMARY.md`
</output>
