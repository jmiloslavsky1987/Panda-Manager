---
phase: 05.1-onboarding-dashboard
plan: 08
subsystem: ui, api
tags: [next.js, react, drizzle-orm, postgres, sticky-header]

# Dependency graph
requires:
  - phase: 05.1-onboarding-dashboard
    provides: onboarding_phases, onboarding_steps, projects table with status_summary + go_live_target columns
provides:
  - GET /api/projects/[projectId] returning project summary fields (name, customer, status_summary, go_live_target)
  - OnboardingDashboard sticky header displaying customer name, executive summary, go-live target badge
affects: [05.1-onboarding-dashboard, OVER-01 requirement]

# Tech tracking
tech-stack:
  added: []
  patterns: [project summary fetch added to existing Promise.all in useEffect, read-only header fields pattern]

key-files:
  created:
    - bigpanda-app/app/api/projects/[projectId]/route.ts
  modified:
    - bigpanda-app/components/OnboardingDashboard.tsx

key-decisions:
  - "No RLS on GET /api/projects/[projectId] — projects table has no row-level security policy; simple SELECT by id is correct"
  - "desired_outcomes skipped — no DB column exists; deferred to future schema extension"
  - "status_summary used as executive summary text — per plan spec (no separate engagement_summary column)"

patterns-established:
  - "Project summary fetch added as 5th item in existing Promise.all — avoids separate useEffect, keeps loading state unified"

requirements-completed: [OVER-01]

# Metrics
duration: 8min
completed: 2026-03-23
---

# Phase 05.1 Plan 08: Sticky Header Project Summary Summary

**GET /api/projects/[projectId] route + OnboardingDashboard sticky header updated to show customer name, executive summary (status_summary), and go-live target badge from DB**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-23T20:40:00Z
- **Completed:** 2026-03-23T20:48:00Z
- **Tasks:** 2/2
- **Files modified:** 2

## Accomplishments
- Created new GET /api/projects/[projectId]/route.ts — returns id, name, customer, status_summary, go_live_target; 400 on bad id, 404 on missing project
- Extended OnboardingDashboard's Promise.all with a 5th fetch for project summary data
- Sticky header now shows customer name (bold, truncated), step count, status_summary (line-clamp-2), and go-live badge — all read-only
- Added `data-testid="project-summary"` to the header div per OVER-01 spec
- Preserved all pre-existing testids (`data-testid="progress-ring"`, `data-testid="onboarding-dashboard"`)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create GET /api/projects/[projectId]/route.ts** - `ce7ed0c` (feat)
2. **Task 2: Add project summary fields to OnboardingDashboard sticky header** - `3e4ec2f` (feat)

## Files Created/Modified
- `bigpanda-app/app/api/projects/[projectId]/route.ts` - New GET handler returning project summary fields
- `bigpanda-app/components/OnboardingDashboard.tsx` - ProjectSummary interface, projectSummary state, 5th fetch, updated sticky header

## Decisions Made
- No RLS needed on this route — projects table has no row-level security policies (child tables do; projects itself does not)
- `desired_outcomes` field skipped — no DB column; deferred per plan note
- Pre-existing TypeScript errors in ioredis/bullmq type clash and settings.ts are out-of-scope (not introduced by this plan)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- TypeScript check revealed pre-existing TS errors (ioredis version mismatch, settings import) — confirmed pre-existing and out of scope; no errors from files created/modified in this plan.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- OVER-01 requirement fulfilled: GET route exists, sticky header renders project-summary testid, customer name, status_summary, and go_live_target badge
- All 4 OVER E2E tests should remain GREEN (no testid regressions introduced)
- OnboardingDashboard is ready for future schema extensions (desired_outcomes can be added when DB column exists)

---
*Phase: 05.1-onboarding-dashboard*
*Completed: 2026-03-23*
