---
phase: 05.1-onboarding-dashboard
plan: 03
type: execute
wave: 2
depends_on: [05.1-02]
files_modified:
  - bigpanda-app/app/api/projects/[projectId]/onboarding/route.ts
  - bigpanda-app/app/api/projects/[projectId]/onboarding/steps/[stepId]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/integrations/route.ts
  - bigpanda-app/app/api/projects/[projectId]/integrations/[integId]/route.ts
autonomous: true
requirements: [OVER-01, OVER-02, OVER-03]

must_haves:
  truths:
    - "GET /api/projects/1/onboarding returns JSON with phases array (empty array if no data)"
    - "GET /api/projects/1/integrations returns JSON with integrations array (empty array if no data)"
    - "PATCH /api/projects/1/onboarding/steps/1 with {status: 'in-progress'} returns {ok: true} or 400/422"
    - "PATCH /api/projects/1/integrations/1 with {status: 'configured'} returns {ok: true} or 400/422"
    - "All routes use @/ alias for DB imports (not relative paths) — consistent with existing route pattern"
  artifacts:
    - path: "bigpanda-app/app/api/projects/[projectId]/onboarding/route.ts"
      provides: "GET — phases + steps in RLS transaction"
      exports: [GET]
    - path: "bigpanda-app/app/api/projects/[projectId]/onboarding/steps/[stepId]/route.ts"
      provides: "PATCH — step status/owner + JSONB updates append"
      exports: [PATCH]
    - path: "bigpanda-app/app/api/projects/[projectId]/integrations/route.ts"
      provides: "GET — integrations list in RLS transaction"
      exports: [GET]
    - path: "bigpanda-app/app/api/projects/[projectId]/integrations/[integId]/route.ts"
      provides: "PATCH — integration status/notes"
      exports: [PATCH]
  key_links:
    - from: "bigpanda-app/app/api/projects/[projectId]/onboarding/steps/[stepId]/route.ts"
      to: "bigpanda-app/db/schema.ts onboardingSteps.updates"
      via: "sql template tag for JSONB array append"
      pattern: "sql`.*\\|\\|.*jsonb`"
    - from: "bigpanda-app/app/api/projects/[projectId]/onboarding/route.ts"
      to: "bigpanda-app/db/index.ts"
      via: "@/db import + RLS transaction SET LOCAL"
      pattern: "SET LOCAL app.current_project_id"
---

<objective>
Create all 4 API route files for onboarding phases/steps and integrations. Two GET routes return data in RLS-scoped transactions. Two PATCH routes validate with Zod and handle special cases: JSONB array append for step update notes, status cycling for integrations.

Purpose: The OnboardingDashboard client component (Plan 04, parallel) fetches data from these routes.
Output: 4 new API route files under app/api/projects/[projectId]/
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05.1-onboarding-dashboard/05.1-RESEARCH.md
@bigpanda-app/app/api/risks/[id]/route.ts
@bigpanda-app/lib/queries.ts

<interfaces>
<!-- Existing PATCH route pattern — follow exactly (from bigpanda-app/app/api/risks/[id]/route.ts): -->
import { NextRequest, NextResponse } from 'next/server'
import { db } from '@/db'
import { risks } from '@/db/schema'
import { eq } from 'drizzle-orm'
import { sql } from 'drizzle-orm'
import { z } from 'zod'

const patchSchema = z.object({
  severity: z.enum(['low', 'medium', 'high', 'critical']).optional(),
  mitigation_append: z.string().optional(),
})

export async function PATCH(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params
  const numericId = parseInt(id, 10)
  if (isNaN(numericId)) return NextResponse.json({ error: 'Invalid id' }, { status: 400 })
  const parsed = patchSchema.safeParse(await request.json())
  if (!parsed.success) return NextResponse.json({ error: parsed.error.flatten() }, { status: 422 })
  // ... DB update ...
  return NextResponse.json({ ok: true })
}

<!-- RLS transaction pattern (from bigpanda-app/lib/queries.ts): -->
return await db.transaction(async (tx) => {
  await tx.execute(sql.raw(`SET LOCAL app.current_project_id = ${projectId}`))
  // ... queries ...
})

<!-- JSONB array append pattern (from RESEARCH.md Pitfall 1): -->
import { sql } from 'drizzle-orm'
await db.update(onboardingSteps)
  .set({
    updates: sql`${onboardingSteps.updates} || ${JSON.stringify([newEntry])}::jsonb`,
    updated_at: new Date(),
  })
  .where(eq(onboardingSteps.id, stepId))

<!-- New table exports (from schema.ts after Plan 02): -->
export const onboardingStepStatusEnum  // pgEnum: 'not-started' | 'in-progress' | 'complete' | 'blocked'
export const integrationStatusEnum     // pgEnum: 'not-connected' | 'configured' | 'validated' | 'production' | 'blocked'
export const onboardingPhases          // table: id, project_id, name, display_order, created_at
export const onboardingSteps           // table: id, phase_id, project_id, name, description, status, owner, dependencies, updates (jsonb), display_order, timestamps
export const integrations              // table: id, project_id, tool, category, status, color, notes, display_order, timestamps
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create onboarding GET + steps PATCH routes</name>
  <files>
    bigpanda-app/app/api/projects/[projectId]/onboarding/route.ts,
    bigpanda-app/app/api/projects/[projectId]/onboarding/steps/[stepId]/route.ts
  </files>
  <action>
Create the directory structure `bigpanda-app/app/api/projects/[projectId]/onboarding/steps/[stepId]/` first (Next.js App Router requires the full folder tree).

**GET route** at `app/api/projects/[projectId]/onboarding/route.ts`:
- Parse `projectId` from params (await params, parseInt, isNaN guard → 400)
- Run RLS transaction: `SET LOCAL app.current_project_id = ${projectId}`
- Query `onboardingPhases` where project_id = projectId, ordered by display_order
- For each phase, query `onboardingSteps` where phase_id = phase.id, ordered by display_order
- Return `{ phases: [{ ...phase, steps: [...] }] }`
- Import: `import { db } from '@/db'`, `import { onboardingPhases, onboardingSteps } from '@/db/schema'`

**PATCH route** at `app/api/projects/[projectId]/onboarding/steps/[stepId]/route.ts`:
- Parse both `projectId` and `stepId` from params
- Zod schema:
  ```typescript
  const patchSchema = z.object({
    status: z.enum(['not-started', 'in-progress', 'complete', 'blocked']).optional(),
    owner: z.string().optional(),
    updates_append: z.string().optional(),  // new update note text to append
  })
  ```
- Build update object: `{ updated_at: new Date() }`
- If `status` provided: add to update object
- If `owner` provided: add to update object
- If `updates_append` provided: use the JSONB append pattern (sql template tag):
  ```typescript
  const newEntry = { timestamp: new Date().toISOString(), text: data.updates_append }
  // Use sql template: updates: sql`${onboardingSteps.updates} || ${JSON.stringify([newEntry])}::jsonb`
  ```
  Do NOT use `.set()` alone for JSONB append — it overwrites. Use the sql template pattern.
- Use RLS transaction for the update (same `SET LOCAL app.current_project_id = ${projectId}`)
- Return `{ ok: true }`
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | grep "onboarding" | head -10</automated>
  </verify>
  <done>Both files exist; TypeScript shows no errors in the new route files; curl GET /api/projects/1/onboarding returns JSON (or 500 if DB unavailable — not 404)</done>
</task>

<task type="auto">
  <name>Task 2: Create integrations GET + PATCH routes</name>
  <files>
    bigpanda-app/app/api/projects/[projectId]/integrations/route.ts,
    bigpanda-app/app/api/projects/[projectId]/integrations/[integId]/route.ts
  </files>
  <action>
Create the directory structure `bigpanda-app/app/api/projects/[projectId]/integrations/[integId]/` first.

**GET route** at `app/api/projects/[projectId]/integrations/route.ts`:
- Parse `projectId` from params (isNaN guard → 400)
- Run RLS transaction: `SET LOCAL app.current_project_id = ${projectId}`
- Query `integrations` where project_id = projectId, ordered by display_order
- Return `{ integrations: [...] }`

**PATCH route** at `app/api/projects/[projectId]/integrations/[integId]/route.ts`:
- Parse both `projectId` and `integId` from params
- Zod schema:
  ```typescript
  const patchSchema = z.object({
    status: z.enum(['not-connected', 'configured', 'validated', 'production', 'blocked']).optional(),
    notes: z.string().optional(),
  })
  ```
- Build update: `{ updated_at: new Date(), ...data }` (filter undefined fields)
- Use RLS transaction for the update
- Return `{ ok: true }`

Note on import depth: These routes are at `app/api/projects/[projectId]/integrations/`. Using `@/db` and `@/db/schema` avoids relative path depth issues entirely (confirmed in RESEARCH.md Open Question 2 — `@/` alias resolves to bigpanda-app/).
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | grep "integrations" | head -10</automated>
  </verify>
  <done>Both files exist; TypeScript shows no errors in the new route files; all 4 new API route files exist under app/api/projects/[projectId]/</done>
</task>

</tasks>

<verification>
find "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app/app/api/projects" -name "route.ts" | sort
Expected: 4 route.ts files (onboarding/route.ts, onboarding/steps/[stepId]/route.ts, integrations/route.ts, integrations/[integId]/route.ts)
</verification>

<success_criteria>
- 4 new route files exist in the correct directory structure
- All export GET or PATCH as named exports (Next.js App Router requirement)
- JSONB append in steps PATCH uses sql template tag (not plain .set())
- TypeScript compiles without new errors in these files
- GET routes return { phases: [] } or { integrations: [] } shape
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-onboarding-dashboard/05.1-03-SUMMARY.md`
</output>
