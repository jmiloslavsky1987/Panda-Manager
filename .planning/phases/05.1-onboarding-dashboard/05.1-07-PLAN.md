---
phase: 05.1-onboarding-dashboard
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/phase5.1.spec.ts
autonomous: true
gap_closure: true
requirements:
  - OVER-04

must_haves:
  truths:
    - "OVER-04 E2E test POSTs to /api/projects/1/yaml-export (correct path)"
    - "OVER-04 E2E test clicks step-owner span before calling fill()"
  artifacts:
    - path: "tests/e2e/phase5.1.spec.ts"
      provides: "Corrected OVER-04 test with right endpoint and click-before-fill"
      contains: "/api/projects/1/yaml-export"
  key_links:
    - from: "tests/e2e/phase5.1.spec.ts"
      to: "/api/projects/[projectId]/yaml-export"
      via: "page.request.post"
      pattern: "/api/projects/1/yaml-export"
---

<objective>
Fix two line-level bugs in the OVER-04 E2E test that are hidden by the assert-if-present guard on an empty DB but would cause test failure with real data.

Purpose: The two bugs are blockers — they render OVER-04 non-functional with DB data. Both are in the same file and can be fixed atomically.
Output: tests/e2e/phase5.1.spec.ts with corrected endpoint path and click-before-fill sequence.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix OVER-04 test — correct endpoint path and add click before fill</name>
  <files>tests/e2e/phase5.1.spec.ts</files>
  <action>
    Two targeted fixes to the OVER-04 test block (lines 62–77):

    Fix 1 — Wrong endpoint (line 71):
    Change: `page.request.post('/api/yaml-export', {`
    To:     `page.request.post('/api/projects/1/yaml-export', {`

    Fix 2 — fill() on span without click (lines 68–69):
    Change:
    ```
    const ownerField = page.locator('[data-testid="step-owner"]').first();
    await ownerField.fill('Test Owner');
    ```
    To:
    ```
    const ownerField = page.locator('[data-testid="step-owner"]').first();
    await ownerField.click();
    await ownerField.fill('Test Owner');
    ```

    The StepOwnerField component renders a `<span data-testid="step-owner">` in display mode
    and switches to `<input data-testid="step-owner">` only after a click. Playwright's fill()
    requires an input — the click() triggers the state transition before fill() runs.

    No other changes to this file.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/phase5.1.spec.ts --grep "OVER-04" 2>&1 | tail -20</automated>
  </verify>
  <done>
    OVER-04 test passes (or is skipped on empty DB — assert-if-present guard still applies).
    File contains string "/api/projects/1/yaml-export" at the POST call site.
    File contains `await ownerField.click()` immediately before `await ownerField.fill('Test Owner')`.
  </done>
</task>

</tasks>

<verification>
npx playwright test tests/e2e/phase5.1.spec.ts 2>&1 | tail -10
All 4 OVER tests pass (or assert-if-present blocks skip gracefully on empty DB).
</verification>

<success_criteria>
- tests/e2e/phase5.1.spec.ts line for yaml-export POST uses `/api/projects/1/yaml-export`
- ownerField.click() appears before ownerField.fill('Test Owner')
- All 4 OVER-0x tests GREEN (empty-DB run)
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-onboarding-dashboard/05.1-07-SUMMARY.md`
</output>
