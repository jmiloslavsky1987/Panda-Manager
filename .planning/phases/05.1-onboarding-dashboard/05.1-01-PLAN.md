---
phase: 05.1-onboarding-dashboard
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/phase5.1.spec.ts
autonomous: true
requirements: [OVER-01, OVER-02, OVER-03, OVER-04]

must_haves:
  truths:
    - "tests/e2e/phase5.1.spec.ts exists with 4 RED stub tests (one per OVER-0x requirement)"
    - "Running the full suite produces FAILED results (not skip/fixme — stub assertions are expect(false).toBe(true))"
    - "Each test name contains its requirement ID (OVER-01 etc.) for --grep targeting"
  artifacts:
    - path: "tests/e2e/phase5.1.spec.ts"
      provides: "RED baseline E2E stubs for all 4 OVER requirements"
      contains: "OVER-01"
  key_links:
    - from: "tests/e2e/phase5.1.spec.ts"
      to: "playwright.config.ts"
      via: "standard import — no config changes needed"
      pattern: "import.*playwright"
---

<objective>
Create the Wave 0 RED baseline E2E test stubs for Phase 5.1. Four stub tests, one per OVER-0x requirement, using the assert-if-present pattern established in phase5.spec.ts. Tests must be RED (failing) before any implementation begins.

Purpose: Nyquist compliance — every subsequent plan has an automated verify command targeting its requirement ID.
Output: tests/e2e/phase5.1.spec.ts with 4 failing tests
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-onboarding-dashboard/05.1-VALIDATION.md

<interfaces>
<!-- Pattern to follow exactly — from tests/e2e/phase5.spec.ts -->
<!-- assert-if-present: structural UI assertions always pass, DB-dependent wrapped in if(count > 0) -->
<!-- Stub RED baseline: expect(false, 'stub: OVER-XX not yet implemented').toBe(true) placed FIRST in each test -->
<!-- Existing pattern from phase2.spec.ts and phase5.spec.ts: -->

import { test, expect } from '@playwright/test'

// Stub test structure used in prior phases:
test('OVER-01: ...', async ({ page }) => {
  expect(false, 'stub: OVER-01 not yet implemented').toBe(true)
  // ... structural assertions below stub line (never reached until stub removed)
})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase5.1.spec.ts E2E stubs</name>
  <files>tests/e2e/phase5.1.spec.ts</files>
  <action>
Create `tests/e2e/phase5.1.spec.ts` with 4 stub tests matching the assert-if-present pattern from `tests/e2e/phase5.spec.ts`. Each stub must:

1. Have `expect(false, 'stub: OVER-XX not yet implemented').toBe(true)` as the FIRST line — this keeps tests RED and visible
2. Include the requirement ID in the test name string for `--grep "OVER-0X"` targeting
3. Include the structural + assert-if-present assertions below the stub line (unreachable until stub removed — documents the intended final test)

Test coverage:

**OVER-01** — navigate to `/customer/1/overview`, assert `[data-testid="onboarding-dashboard"]` visible, `[data-testid="progress-ring"]` visible, `[data-testid="integration-tracker"]` visible; assert-if-present: `[data-testid="phase-card"]` count > 0 → first card visible.

**OVER-02** — navigate to `/customer/1/overview`; assert-if-present: if phase-card count > 0, locate first `[data-testid="step-status-badge"]`, click it, assert it has changed class (badge reflects new status string different from original); assert `[data-testid="step-update-notes"]` is present.

**OVER-03** — navigate to `/customer/1/overview`; assert `[data-testid="integration-tracker"]` visible; assert-if-present: if `[data-testid="integration-card"]` count > 0, first card has `[data-testid="pipeline-bar"]` visible and `[data-testid="integration-notes"]` element present.

**OVER-04** — navigate to `/customer/1/overview`; assert-if-present: if `[data-testid="phase-card"]` count > 0, locate first step's owner field, update it, POST to yaml-export endpoint; verify response 200.

Run verification after creation: `npx playwright test tests/e2e/phase5.1.spec.ts` — all 4 tests must FAIL (RED due to stub line).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/phase5.1.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>4 tests in tests/e2e/phase5.1.spec.ts, all RED (failing on stub assertion), grep targets OVER-01 through OVER-04 work individually</done>
</task>

</tasks>

<verification>
npx playwright test tests/e2e/phase5.1.spec.ts 2>&1 | grep -E "passed|failed|Error"
Expected: 0 passed, 4 failed
</verification>

<success_criteria>
- tests/e2e/phase5.1.spec.ts exists
- 4 tests, 0 passing, 4 failing (RED stubs)
- Each test name contains OVER-0X for grep targeting
- `npx playwright test tests/e2e/phase5.1.spec.ts --grep "OVER-01"` runs exactly 1 test and fails
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-onboarding-dashboard/05.1-01-SUMMARY.md`
</output>
