---
phase: 05.1-onboarding-dashboard
plan: 02
subsystem: database
tags: [drizzle, postgres, schema, migrations, jsonb, enums]

# Dependency graph
requires:
  - phase: 05.1-01
    provides: phase 5.1 research and planning context for onboarding dashboard tables
provides:
  - onboardingPhases Drizzle table definition with FK to projects
  - onboardingSteps Drizzle table definition with jsonb updates + TEXT[] dependencies
  - integrations Drizzle table definition with integrationStatusEnum
  - onboardingStepStatusEnum pgEnum (not-started, in-progress, complete, blocked)
  - integrationStatusEnum pgEnum (not-connected, configured, validated, production, blocked)
  - 0005_onboarding_dashboard.sql manual migration with 2 CREATE TYPE + 3 CREATE TABLE
affects:
  - 05.1-03 (API routes use these type exports)
  - 05.1-04 (OnboardingDashboard component uses these table types)
  - 05.1-05 (YAML round-trip reads/writes onboarding_steps)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - jsonb column via drizzle-orm/pg-core jsonb() helper (first use in this schema)
    - TEXT[] array column via .array().default([]) on text() column
    - Manual migration SQL (no drizzle-kit generate) — DB unavailable in dev environment

key-files:
  created:
    - bigpanda-app/db/migrations/0005_onboarding_dashboard.sql
  modified:
    - bigpanda-app/db/schema.ts

key-decisions:
  - "jsonb added to existing drizzle-orm/pg-core import — no second import block"
  - "Enum values use hyphens (not-started, in-progress) matching reference HTML data-status attributes per CONTEXT.md"
  - "onboarding_steps.phase_id has ON DELETE CASCADE in SQL migration; schema.ts mirrors with plain FK (cascade not specified in Drizzle definition — intentional for Phase 5.1)"

patterns-established:
  - "Manual migration pattern: write SQL by hand following 0004 pattern, never run drizzle-kit generate when DB unavailable"
  - "jsonb column pattern: jsonb('col').default([]).notNull() for arrays, import jsonb from drizzle-orm/pg-core"

requirements-completed: [OVER-01, OVER-02, OVER-03]

# Metrics
duration: 1min
completed: 2026-03-23
---

# Phase 5.1 Plan 02: Onboarding Dashboard Schema Summary

**Drizzle ORM schema + manual SQL migration adding onboarding_phases, onboarding_steps (jsonb updates, TEXT[] dependencies), and integrations tables with two new pgEnum types**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-03-23T20:06:26Z
- **Completed:** 2026-03-23T20:07:27Z
- **Tasks:** 2/2
- **Files modified:** 2

## Accomplishments
- Created `0005_onboarding_dashboard.sql` with 2 CREATE TYPE + 3 CREATE TABLE statements matching the plan specification exactly
- Extended `schema.ts` with `jsonb` import and 5 new exports (2 enums, 3 tables) without disturbing any existing definitions
- Confirmed TypeScript compiles with no new errors — all errors shown are pre-existing ioredis/bullmq version conflicts from prior phases

## Task Commits

Each task was committed atomically:

1. **Task 1: Write migration SQL 0005_onboarding_dashboard.sql** - `3c4ca6e` (feat)
2. **Task 2: Extend schema.ts with 3 new Drizzle table definitions** - `88b2ed2` (feat)

**Plan metadata:** (see final commit below)

## Files Created/Modified
- `bigpanda-app/db/migrations/0005_onboarding_dashboard.sql` - Manual SQL migration: CREATE TYPE onboarding_step_status, integration_status; CREATE TABLE onboarding_phases, onboarding_steps, integrations
- `bigpanda-app/db/schema.ts` - Added jsonb import + 2 enums + 3 table definitions at bottom of file

## Decisions Made
- Enum hyphen values (`not-started`, `in-progress`) preserved exactly per plan — valid PostgreSQL and required for CONTEXT.md HTML data-status attribute compatibility
- Pre-existing TypeScript errors (ioredis/bullmq dual version conflict, settings module) are out of scope and not fixed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None — both tasks completed on first attempt with clean verification.

## User Setup Required
None — no external service configuration required. DB migration runs when PostgreSQL is available: `cd bigpanda-app && DATABASE_URL=postgresql://localhost:5432/bigpanda_app npx drizzle-kit migrate`

## Next Phase Readiness
- Schema exports `onboardingPhases`, `onboardingSteps`, `integrations`, `onboardingStepStatusEnum`, `integrationStatusEnum` — ready for 05.1-03 API routes to import
- Migration file ready to run once PostgreSQL is available
- No blockers

---
*Phase: 05.1-onboarding-dashboard*
*Completed: 2026-03-23*
