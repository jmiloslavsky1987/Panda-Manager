---
phase: 05.1-onboarding-dashboard
plan: 06
type: execute
wave: 4
depends_on: [05.1-03, 05.1-04, 05.1-05]
files_modified: []
autonomous: false
requirements: [OVER-01, OVER-02, OVER-03, OVER-04]

must_haves:
  truths:
    - "All 4 E2E tests (OVER-01 through OVER-04) pass with no stub assertion remaining"
    - "Human has verified animated progress ring renders smoothly in browser"
    - "Human has verified collapsible phase cards expand/collapse correctly"
    - "Human has verified integration pipeline bar highlights correct segment"
    - "Human has verified YAML write-back produces valid YAML (manual file check)"
  artifacts:
    - path: "tests/e2e/phase5.1.spec.ts"
      provides: "4 GREEN E2E tests"
      contains: "OVER-01"
  key_links:
    - from: "tests/e2e/phase5.1.spec.ts"
      to: "bigpanda-app/app/customer/[id]/overview/page.tsx"
      via: "Playwright navigation to /customer/1/overview"
      pattern: "goto.*overview"
---

<objective>
Run the full E2E suite for Phase 5.1, activate all 4 tests (remove stub lines), and perform human verification of the visual/interactive elements that Playwright cannot verify in headless mode.

Purpose: Phase gate — all OVER requirements verified before marking 5.1 complete.
Output: 4/4 E2E GREEN + human sign-off
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05.1-onboarding-dashboard/05.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate all E2E stubs and run full suite</name>
  <files>tests/e2e/phase5.1.spec.ts</files>
  <action>
Remove the stub lines (`expect(false, 'stub: OVER-XX not yet implemented').toBe(true)`) from all 4 tests in `tests/e2e/phase5.1.spec.ts`. The structural assertions below each stub line now become the actual test body.

After removing stubs, run the full suite:
```bash
npx playwright test tests/e2e/phase5.1.spec.ts
```

If any structural assertions fail (e.g., `[data-testid="onboarding-dashboard"]` not visible), investigate and fix in the component — do not skip or skip:fixme the test. Common issues to check:
- Is the dev server running? Playwright config points to localhost:3000
- Are all data-testid attributes present in OnboardingDashboard.tsx as specified in Plan 04?
- Does the overview page.tsx correctly import and render OnboardingDashboard?

Target: 4 passed, 0 failed.

If any assert-if-present blocks fail (DB-dependent with no data), check that the `if (count > 0)` guard is correctly wrapping the assertion — these should pass on an empty DB.
  </action>
  <verify>
    <automated>npx playwright test "/Users/jmiloslavsky/Documents/Project Assistant Code/tests/e2e/phase5.1.spec.ts" 2>&1 | tail -15</automated>
  </verify>
  <done>4/4 tests passing; 0 failed; no stub assertions remaining in phase5.1.spec.ts</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 5.1 Onboarding Dashboard:
- Overview tab replaced with dynamic onboarding dashboard
- Sticky header with animated SVG progress ring
- Collapsible phase cards with step status badges (click to cycle)
- Integration tracker with 4-stage pipeline bar (click to advance)
- Risks & Blockers section (read-only cards)
- Milestone Timeline section (read-only)
- Filter bar (status pills + search)
- YAML import script at scripts/import-onboarding.ts
- YAML write-back at POST /api/projects/:id/yaml-export
  </what-built>
  <how-to-verify>
1. Open http://localhost:3000/customer/1/overview — confirm the onboarding dashboard loads (not the old workstreams/milestones view)
2. Verify the progress ring SVG is visible in the sticky header; if any steps exist, ring should show % complete
3. Verify filter pills (All / Not Started / In Progress / Complete / Blocked) are visible and respond to clicks (filtered steps change)
4. If phase cards are visible: click a phase header to collapse it, click again to expand — confirm smooth behavior
5. If step status badges are visible: click a badge and confirm the displayed status changes immediately (optimistic update)
6. Navigate to Integration Tracker section — confirm each integration card shows a 4-segment pipeline bar
7. If integration cards exist: click a pipeline segment to advance status; confirm bar updates
8. Verify no console errors in browser DevTools Network tab when the page loads
9. (If PostgreSQL is available) Run: `npx tsx bigpanda-app/scripts/import-onboarding.ts --project-id 1 --file ~/your-context-doc.md` — confirm "Imported X phases" output
10. (If PostgreSQL + file available) Make a step status change in-app, then check the YAML file — confirm onboarding_phases section reflects the change
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix before marking the phase complete</resume-signal>
</task>

</tasks>

<verification>
npx playwright test "/Users/jmiloslavsky/Documents/Project Assistant Code/tests/e2e/phase5.1.spec.ts" 2>&1 | grep -E "passed|failed"
Expected: 4 passed, 0 failed
</verification>

<success_criteria>
- 4/4 E2E tests GREEN (OVER-01 through OVER-04)
- Human verified progress ring, collapsible cards, pipeline bar, and YAML write-back
- No stub assertions remaining in phase5.1.spec.ts
- Phase 5.1 marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-onboarding-dashboard/05.1-06-SUMMARY.md`

Update STATE.md: Phase 5.1 = COMPLETE (6/6 plans done).
Update ROADMAP.md: Phase 5.1 plans list with 6 plan entries, status = Complete.
</output>
