---
phase: 05.1
slug: onboarding-dashboard
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-23
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Playwright (`@playwright/test`) |
| **Config file** | `playwright.config.ts` (project root) |
| **Quick run command** | `npx playwright test tests/e2e/phase5.1.spec.ts --grep "OVER-01"` |
| **Full suite command** | `npx playwright test tests/e2e/phase5.1.spec.ts` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx playwright test tests/e2e/phase5.1.spec.ts --grep "OVER-01"` (structural assertion always passes)
- **After every plan wave:** Run `npx playwright test tests/e2e/phase5.1.spec.ts`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 5.1-01-01 | 01 | 1 | OVER-01 | E2E structural | `npx playwright test tests/e2e/phase5.1.spec.ts --grep "OVER-01"` | ❌ W0 | ⬜ pending |
| 5.1-02-01 | 02 | 1 | OVER-01 | E2E structural | `npx playwright test tests/e2e/phase5.1.spec.ts --grep "OVER-01"` | ❌ W0 | ⬜ pending |
| 5.1-03-01 | 03 | 2 | OVER-02 | E2E assert-if-present | `npx playwright test tests/e2e/phase5.1.spec.ts --grep "OVER-02"` | ❌ W0 | ⬜ pending |
| 5.1-04-01 | 04 | 2 | OVER-03 | E2E assert-if-present | `npx playwright test tests/e2e/phase5.1.spec.ts --grep "OVER-03"` | ❌ W0 | ⬜ pending |
| 5.1-05-01 | 05 | 3 | OVER-04 | E2E assert-if-present | `npx playwright test tests/e2e/phase5.1.spec.ts --grep "OVER-04"` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/e2e/phase5.1.spec.ts` — stubs for OVER-01, OVER-02, OVER-03, OVER-04 using assert-if-present pattern (matches `tests/e2e/phase5.spec.ts`)

*Pattern to follow (from phase5.spec.ts):*
```typescript
test('OVER-01: Overview tab shows onboarding dashboard', async ({ page }) => {
  await page.goto('/customer/1/overview')
  // Structural: always passes
  await expect(page.locator('[data-testid="onboarding-dashboard"]')).toBeVisible()
  await expect(page.locator('[data-testid="progress-ring"]')).toBeVisible()
  // Assert-if-present: DB-dependent
  const phaseCount = await page.locator('[data-testid="phase-card"]').count()
  if (phaseCount > 0) {
    await expect(page.locator('[data-testid="phase-card"]').first()).toBeVisible()
  }
})
```

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Animated progress ring renders smoothly | OVER-01 | CSS animation not reliably testable in Playwright headless | Visual inspection in browser |
| Collapsible phase cards expand/collapse with animation | OVER-01 | Animation timing | Click phase header, confirm smooth expand |
| YAML write-back produces valid YAML | OVER-04 | File system side-effect | Edit a step in-app, check YAML file content |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
