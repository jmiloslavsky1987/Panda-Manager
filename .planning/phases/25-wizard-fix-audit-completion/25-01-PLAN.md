---
phase: 25-wizard-fix-audit-completion
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/tests/wizard/ai-preview-filter.test.ts
  - bigpanda-app/tests/audit/ingestion-approve-audit.test.ts
  - bigpanda-app/tests/audit/discovery-approve-audit.test.ts
autonomous: true
requirements:
  - WIZ-03
  - AUDIT-02

must_haves:
  truths:
    - "Three test files exist and contain failing tests (RED state) that precisely describe the bugs being fixed"
    - "ai-preview-filter.test.ts documents both the broken filter and the fixed filter behavior"
    - "ingestion-approve-audit.test.ts verifies insertItem and mergeItem call db.transaction with an auditLog insert"
    - "discovery-approve-audit.test.ts verifies insertDiscoveredItem wraps entity write + audit insert in db.transaction"
  artifacts:
    - path: "bigpanda-app/tests/wizard/ai-preview-filter.test.ts"
      provides: "Unit tests for WIZ-03 filter behavior"
      exports: []
    - path: "bigpanda-app/tests/audit/ingestion-approve-audit.test.ts"
      provides: "Unit tests for ingestion/approve audit transaction"
      exports: []
    - path: "bigpanda-app/tests/audit/discovery-approve-audit.test.ts"
      provides: "Unit tests for discovery/approve audit transaction"
      exports: []
  key_links:
    - from: "tests/wizard/ai-preview-filter.test.ts"
      to: "components/wizard/AiPreviewStep.tsx"
      via: "tests the filter logic inline (not importing the component)"
      pattern: "filter.*artifactId"
    - from: "tests/audit/ingestion-approve-audit.test.ts"
      to: "app/api/ingestion/approve/route.ts"
      via: "mocked db verifying transaction call pattern"
      pattern: "db.transaction"
---

<objective>
Create the three Wave 0 test stub files that define failing tests (RED) for the two Phase 25 fixes before any implementation begins.

Purpose: Nyquist compliance — every implementation task in Wave 1+ must have an existing automated test file to verify against. These tests document the precise bug being fixed (WIZ-03) and the missing audit pattern (AUDIT-02).

Output:
- `tests/wizard/ai-preview-filter.test.ts` — RED tests for filter bug fix
- `tests/audit/ingestion-approve-audit.test.ts` — RED tests for bulk ingestion audit
- `tests/audit/discovery-approve-audit.test.ts` — RED tests for discovery approve audit
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-wizard-fix-audit-completion/25-CONTEXT.md
@.planning/phases/25-wizard-fix-audit-completion/25-VALIDATION.md

<interfaces>
<!-- Key types the test files reference. Do not import AiPreviewStep directly — test the pure filter logic inline. -->

From bigpanda-app/components/wizard/AiPreviewStep.tsx (lines 12-16):
```typescript
interface FileStatus {
  name: string
  status: 'pending' | 'extracting' | 'done' | 'error'
  artifactId?: number
}
```

From bigpanda-app/lib/audit.ts:
```typescript
export async function writeAuditLog(params: {
  entityType: string;
  entityId: number | null;
  action: 'create' | 'update' | 'delete';
  actorId?: string;
  beforeJson?: Record<string, unknown> | null;
  afterJson?: Record<string, unknown> | null;
}): Promise<void>
```

Vitest config: bigpanda-app/vitest.config.ts
Test runner command: cd bigpanda-app && npx vitest run tests/wizard/ai-preview-filter.test.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write ai-preview-filter test stub (RED)</name>
  <files>bigpanda-app/tests/wizard/ai-preview-filter.test.ts</files>
  <behavior>
    - Test 1 (documents the bug): Given fileStatuses where all files have artifactId and status 'done', the BROKEN filter `f.artifactId && f.status !== 'done'` returns zero files.
    - Test 2 (documents the fix): Given the same fileStatuses, the FIXED filter `f.artifactId` (no status check) returns all files with an artifactId.
    - Test 3: Files with no artifactId are excluded by both filters.
    - Test 4: Files with artifactId and status 'pending' or 'extracting' are also included by the fixed filter.
    These tests are pure data tests — no component import needed. Define the filter logic inline in test cases.
  </behavior>
  <action>
    Create `bigpanda-app/tests/wizard/ai-preview-filter.test.ts` using Vitest (`import { describe, it, expect } from 'vitest'`).

    The test file works with `FileStatus` objects defined inline (no import needed — just use the shape):
    ```typescript
    type FileStatus = { name: string; status: 'pending' | 'extracting' | 'done' | 'error'; artifactId?: number }
    ```

    Write a `describe('AiPreviewStep filter logic')` block with 4 tests as described in `<behavior>`.

    Test setup: create an array of FileStatus items:
    - `{ name: 'file1.pdf', status: 'done', artifactId: 101 }` — uploaded successfully
    - `{ name: 'file2.pdf', status: 'done', artifactId: 102 }` — uploaded successfully
    - `{ name: 'file3.pdf', status: 'pending', artifactId: undefined }` — no artifactId yet

    Test 1 (broken filter): `fileStatuses.filter(f => f.artifactId && f.status !== 'done')` → length 0 (because both files with artifactId have status 'done')
    Test 2 (fixed filter): `fileStatuses.filter(f => f.artifactId)` → length 2
    Test 3 (no artifactId excluded): both filters exclude file3.pdf (no artifactId)
    Test 4 (pending/extracting included by fix): add `{ name: 'file4.pdf', status: 'pending', artifactId: 103 }` — fixed filter returns it, broken filter also returns it (status !== 'done')

    These tests PASS after writing (they test pure filter logic with no mocks needed). This is not a RED/GREEN cycle — the tests document behavior and will pass immediately. They serve as regression guards after the WIZ-03 fix is applied in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/wizard/ai-preview-filter.test.ts</automated>
  </verify>
  <done>File exists with 4 passing tests. All assertions use `expect(...).toHaveLength(N)` or equivalent. Tests run in under 5 seconds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write ingestion-approve-audit test stub (RED)</name>
  <files>bigpanda-app/tests/audit/ingestion-approve-audit.test.ts</files>
  <behavior>
    - Test 1: After the fix, `insertItem()` (for an 'action' type) calls `db.transaction()` exactly once and the transaction callback inserts into auditLog.
    - Test 2: After the fix, `mergeItem()` (for an 'action' type) calls `db.transaction()` exactly once and the transaction callback inserts into auditLog.
    - Test 3: The audit entry for an insert has `before_json: null` and `after_json` containing the inserted record.
    - Test 4: The audit entry for a merge has `before_json` (the before-state object) and `after_json` from the returned updated record.
    These tests must start in RED state (failing) because `insertItem` and `mergeItem` currently do NOT call `db.transaction` or insert into auditLog.
  </behavior>
  <action>
    Create `bigpanda-app/tests/audit/ingestion-approve-audit.test.ts`.

    Strategy: The route file's helper functions (`insertItem`, `mergeItem`) are not exported. Write tests that mock `db` at module level and then import the POST handler to drive the helpers indirectly, OR extract the relevant logic into testable helper functions.

    Since the helpers are module-private, the cleanest approach for these unit tests is to mock the `db` module and invoke the route's POST handler with a minimal approved item payload, then assert on the mock calls.

    Use `vi.mock('@/db', ...)` or `vi.mock('../../../db', ...)` to mock the db object with:
    - `db.transaction` as a `vi.fn()` that calls the callback with a mock tx
    - `tx.insert().values().returning()` returning a fake inserted row

    Write 2 tests:
    1. POST with one approved 'action' item (no conflict) — assert `db.transaction` was called once and the mock tx received an insert into auditLog table
    2. POST with one approved 'action' item + merge conflict resolution — assert `db.transaction` called and auditLog insert present

    Mark these tests with `it.todo(...)` or write them to fail with a clear assertion error (`expect(db.transaction).toHaveBeenCalled()` — this FAILS currently because the route doesn't use transactions yet). This creates the RED state.

    Use the full route path for import: `import { POST } from '@/app/api/ingestion/approve/route'` or the relative equivalent from the test file location. Check the existing test files in `bigpanda-app/tests/audit/` for import pattern conventions.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/audit/ingestion-approve-audit.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>File exists with at least 2 tests. Tests fail (RED) with assertions like "expected db.transaction to have been called". The test file parses without syntax errors (vitest can collect the test). Running the full audit suite still passes existing tests: `cd bigpanda-app && npx vitest run tests/audit/audit-helper.test.ts` is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write discovery-approve-audit test stub (RED)</name>
  <files>bigpanda-app/tests/audit/discovery-approve-audit.test.ts</files>
  <behavior>
    - Test 1: After the fix, `insertDiscoveredItem()` for a 'action' type item calls `db.transaction()` and the transaction callback inserts into auditLog.
    - Test 2: The audit entry has `before_json: null` (insert) and `after_json` with the inserted entity fields.
    These must start RED — `insertDiscoveredItem` currently does not use transactions.
  </behavior>
  <action>
    Create `bigpanda-app/tests/audit/discovery-approve-audit.test.ts`.

    Same mocking strategy as Task 2: mock `db` with `vi.mock`, import and call the POST handler with a minimal payload (one itemId), mock the `discoveryItems` SELECT to return a fake discovery item with `suggested_field: 'action'`.

    Write 2 tests:
    1. POST with one itemId for an 'action' discovery item — assert `db.transaction` was called once and auditLog insert was included in the transaction.
    2. The audit entry produced has `before_json: null` and `after_json` that includes the `description` field from the discovery item's `content`.

    Tests should fail (RED) because `insertDiscoveredItem` currently does not call `db.transaction` or insert into auditLog.

    Look at `bigpanda-app/tests/audit/audit-helper.test.ts` for the test file structure conventions before writing.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/audit/discovery-approve-audit.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>File exists with at least 2 tests. Tests fail (RED) with clear assertion errors. Existing audit-helper.test.ts remains GREEN after adding this file.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
```
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/audit/audit-helper.test.ts tests/wizard/
```
Expected: audit-helper.test.ts GREEN (5 tests pass), ai-preview-filter.test.ts GREEN, ingestion-approve-audit.test.ts RED, discovery-approve-audit.test.ts RED.
</verification>

<success_criteria>
- All 3 test stub files exist at specified paths
- ai-preview-filter.test.ts passes (documents filter behavior with passing assertions)
- ingestion-approve-audit.test.ts fails with assertion errors (RED — no db.transaction yet)
- discovery-approve-audit.test.ts fails with assertion errors (RED — no db.transaction yet)
- audit-helper.test.ts (existing) remains GREEN
- No syntax errors in any file
</success_criteria>

<output>
After completion, create `.planning/phases/25-wizard-fix-audit-completion/25-01-SUMMARY.md`
</output>
