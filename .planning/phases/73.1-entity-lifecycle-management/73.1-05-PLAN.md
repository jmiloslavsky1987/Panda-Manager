---
phase: 73.1
plan: "05"
type: execute
wave: 3
depends_on:
  - "73.1-03"
  - "73.1-04"
files_modified: []
autonomous: false
requirements:
  - LIFECYCLE-P1
  - LIFECYCLE-P2
must_haves:
  truths:
    - "User can delete a risk, milestone, action, or workstream from the UI and it disappears"
    - "Deleting an entity writes an audit log entry visible in the DB"
    - "Uploading a document with update/close intent surfaces 'Proposed Changes' in IngestionModal"
    - "User can approve a proposed close and the entity status changes"
    - "User can reject a proposed change and the entity is NOT modified"
    - "BusinessOutcomesSection has a working delete button for each outcome"
    - "E2eWorkflowsSection has a working delete button for each workflow"
    - "engagement_history and key_decisions have no edit/delete buttons (append-only preserved)"
    - "Full test suite passes (npm test -- --run is green)"
  artifacts:
    - path: "bigpanda-app/app/api/actions/[id]/route.ts"
      provides: "DELETE handler verified working"
    - path: "bigpanda-app/app/api/risks/[id]/route.ts"
      provides: "DELETE handler verified working"
    - path: "bigpanda-app/worker/jobs/change-detection.ts"
      provides: "Pass 5 change detection verified working"
    - path: "bigpanda-app/components/IngestionModal.tsx"
      provides: "Proposed Changes section verified working"
  key_links:
    - from: "IngestionModal proposed change approval"
      to: "approve route approvedChanges handler"
      via: "POST /api/ingestion/approve with approvedChanges array"
---

<objective>
Human verification gate — confirm all Phase 73.1 behaviors work end-to-end in the running application.

Purpose: Pillar 1 (document-driven updates) and Pillar 2 (manual CRUD) are both verified by the user in the actual app before the phase is marked complete.
Output: Human sign-off that all behaviors in both pillars are working.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73.1-entity-lifecycle-management/73.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite verification + production build</name>
  <files>No files modified — verification task only</files>
  <action>
    Run full test suite and production build. Fix any failures before proceeding to human verification.

    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | tail -30
    ```

    Then:
    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run build 2>&1 | tail -20
    ```

    If tests fail: identify failing test, fix the underlying code, re-run. Do not proceed to human verification with failing tests.

    Also start the dev server for the user to verify:
    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run next-only
    ```
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass. Production build succeeds. Dev server is running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Pillar 1 (document-driven updates) and Pillar 2 (manual delete)</name>
  <what-built>
    Phase 73.1 implements two pillars of entity lifecycle management:

    Pillar 2 — Manual CRUD (verify first, simpler):
    - DELETE handlers added for actions (/api/actions/[id]), risks (/api/risks/[id]), milestones (/api/milestones/[id]), workstreams (/api/workstreams/[id])
    - Delete buttons added to ActionsTableClient, RisksTableClient, MilestonesTableClient, WorkstreamTableClient
    - Delete buttons added to BusinessOutcomesSection and E2eWorkflowsSection in the Teams tab

    Pillar 1 — Document-driven updates:
    - Pass 5 change detection added to extraction pipeline
    - IngestionModal shows "Proposed Changes" section when extraction job has proposed changes
    - Approve route extended to handle update/close/remove intents
  </what-built>
  <how-to-verify>
    Start dev server: cd bigpanda-app && npm run next-only

    **Pillar 2 — Manual delete:**
    1. Open any project, go to Risks tab
       - Confirm each risk row has a delete button (trash icon or "X" in the row)
       - Click delete on one risk — confirm it disappears immediately
       - Refresh the page — confirm it is gone permanently

    2. Repeat for Actions tab and Milestones tab

    3. Go to Teams tab — Business Value & Expected Outcomes section
       - Confirm each outcome has a delete button
       - Click delete on one outcome — confirm it disappears immediately

    4. Go to Teams tab — End-to-End Workflows section
       - Confirm each workflow has a delete button
       - Click delete on one workflow — confirm it disappears

    5. Confirm engagement_history (Engagement History section in Teams tab) has NO delete buttons — it is append-only by design.

    **Pillar 1 — Document-driven updates (requires document upload with update intent):**
    6. Upload a document that references an existing entity with an update or closure.
       Example: Create a short text file that says "Action A-001 [description of existing action] has been completed and closed."
       Upload it as a document in the Document Ingestion tab.

    7. After extraction completes, open the IngestionModal review
       - If the action text is similar enough to an existing action, a "Proposed Changes" section should appear at the top of the review screen
       - You should see the action listed with "CLOSE" intent badge
       - Confidence % should be greater than 60%

    8. Click Approve on the proposed change, then submit
       - Go to Actions tab and confirm the action's status changed to 'closed'

    9. Upload a document with an update intent for a different entity, then click Reject on the proposed change
       - Submit the approval anyway — confirm the entity was NOT modified

    Note: If Pass 5 produces no proposed changes because the document text does not achieve 70% similarity, this is expected behavior (conservative threshold). Report what you observed.
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors work as expected. Type "issues: [description]" to describe any problems found.</resume-signal>
</task>

</tasks>

<verification>
Human sign-off received on both pillars.
</verification>

<success_criteria>
- All tests green (npm test -- --run passes)
- Production build succeeds
- Pillar 2: User can delete actions, risks, milestones, workstreams, business outcomes, and e2e workflows
- Pillar 1: IngestionModal shows Proposed Changes section with approve/reject per change
- User confirms "approved" after verifying both pillars
</success_criteria>

<output>
After completion, create `.planning/phases/73.1-entity-lifecycle-management/73.1-05-SUMMARY.md`
</output>
