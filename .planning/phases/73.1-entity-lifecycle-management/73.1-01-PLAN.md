---
phase: 73.1
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - bigpanda-app/worker/jobs/__tests__/entity-lifecycle.test.ts
  - bigpanda-app/worker/jobs/__tests__/change-detection.test.ts
  - bigpanda-app/lib/__tests__/entity-matcher.test.ts
  - bigpanda-app/components/__tests__/IngestionModal-changes.test.tsx
autonomous: true
requirements:
  - LIFECYCLE-P1
  - LIFECYCLE-P2
must_haves:
  truths:
    - "All Wave 0 test files exist and import their target modules"
    - "All tests fail with 'not implemented' or import errors (RED state)"
    - "Full test suite still passes — new stubs do not break existing 148 tests"
  artifacts:
    - path: "bigpanda-app/worker/jobs/__tests__/entity-lifecycle.test.ts"
      provides: "RED stubs for PATCH/DELETE handler unit tests"
    - path: "bigpanda-app/worker/jobs/__tests__/change-detection.test.ts"
      provides: "RED stubs for Pass 5 change detection"
    - path: "bigpanda-app/lib/__tests__/entity-matcher.test.ts"
      provides: "RED stubs for PostgreSQL trigram entity matching"
    - path: "bigpanda-app/components/__tests__/IngestionModal-changes.test.tsx"
      provides: "RED stubs for IngestionModal Changes stage rendering"
  key_links:
    - from: "bigpanda-app/worker/jobs/__tests__/entity-lifecycle.test.ts"
      to: "bigpanda-app/app/api/risks/[id]/route.ts"
      via: "imports DELETE handler to test"
    - from: "bigpanda-app/lib/__tests__/entity-matcher.test.ts"
      to: "bigpanda-app/lib/entity-matcher.ts"
      via: "import findSimilarEntities (file does not exist yet — RED)"
---

<objective>
Create Wave 0 RED test stubs for all entity lifecycle behaviors. Tests must exist and fail before implementation begins — this is the TDD contract that subsequent plans fulfill.

Purpose: Establishes the behavioral contracts for CRUD DELETE handlers, Pass 5 change detection, fuzzy entity matching, and the IngestionModal "Changes" stage. Prevents implementation drift.
Output: 4 test files with failing stubs.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73.1-entity-lifecycle-management/73.1-CONTEXT.md
@.planning/phases/73.1-entity-lifecycle-management/73.1-RESEARCH.md
@.planning/phases/73.1-entity-lifecycle-management/73.1-VALIDATION.md
</context>

<interfaces>
<!-- Existing test patterns from codebase -->

From bigpanda-app/worker/jobs/__tests__/document-extraction-dedup.test.ts (pattern):
```typescript
import { describe, it, expect } from 'vitest';
// Tests import the function under test; if file missing, test fails at import (RED)
```

From bigpanda-app/lib/__tests__/require-project-role.test.ts (pattern):
```typescript
import { describe, it, expect, vi } from 'vitest';
```

Vitest config: bigpanda-app/vitest.config.ts
Quick run: cd bigpanda-app && npm test -- --run
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED stubs — entity-lifecycle and change-detection test files</name>
  <files>bigpanda-app/worker/jobs/__tests__/entity-lifecycle.test.ts, bigpanda-app/worker/jobs/__tests__/change-detection.test.ts</files>
  <behavior>
    entity-lifecycle.test.ts:
    - Test: DELETE /api/risks/[id] removes record and writes audit log
    - Test: DELETE /api/milestones/[id] removes record and writes audit log
    - Test: DELETE /api/actions/[id] removes record and writes audit log
    - Test: DELETE /api/workstreams/[id] removes record and writes audit log
    - Test: PATCH/DELETE on entity from different project returns 403

    change-detection.test.ts:
    - Test: runPass5ChangeDetection returns empty array when no matches found
    - Test: runPass5ChangeDetection returns ProposedChange with intent 'close' when extracted action matches existing open action
    - Test: runPass5ChangeDetection returns ProposedChange with intent 'update' when fields differ from existing record
    - Test: runPass5ChangeDetection skips append-only entity types (engagement_history, key_decisions)
  </behavior>
  <action>
    Create both test files in bigpanda-app/worker/jobs/__tests__/.

    For entity-lifecycle.test.ts: Import the DELETE handler from the route files that don't exist yet (e.g., `import { DELETE } from '../../app/api/risks/[id]/route'`). Write stub tests using `it.todo()` or `expect(false).toBe(true)` so they fail RED. Tests should verify: (1) DELETE handler is exported, (2) returns 200 on valid delete, (3) returns 403 when project doesn't match session user.

    For change-detection.test.ts: Import `runPass5ChangeDetection` from `../../worker/jobs/change-detection` (file does not exist — import RED). Write stubs covering the four behaviors listed. Use `it.todo()` format so the test file is valid TypeScript but all tests fail.

    These files must be compilable TypeScript — no syntax errors. The imports may fail (file not found) which is the RED state.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- worker/jobs/__tests__/entity-lifecycle.test.ts worker/jobs/__tests__/change-detection.test.ts --run 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist, TypeScript compiles, tests are in failing/todo state (RED). No test passes green yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED stubs — entity-matcher and IngestionModal-changes test files</name>
  <files>bigpanda-app/lib/__tests__/entity-matcher.test.ts, bigpanda-app/components/__tests__/IngestionModal-changes.test.tsx</files>
  <behavior>
    entity-matcher.test.ts:
    - Test: findSimilarEntities('action', description, projectId) returns array of MatchCandidates
    - Test: findSimilarEntities returns empty array when no results above threshold
    - Test: findSimilarEntities only returns results from the correct projectId (isolation)
    - Test: findSimilarEntities threshold defaults to 0.7

    IngestionModal-changes.test.tsx:
    - Test: When proposedChanges is non-empty, IngestionModal renders "Proposed Changes" section
    - Test: Proposed change item shows before/after comparison (entity name + changed fields)
    - Test: Approve button on proposed change calls onApproveChange(change)
    - Test: Reject button on proposed change calls onRejectChange(change)
  </behavior>
  <action>
    Create bigpanda-app/lib/__tests__/entity-matcher.test.ts: Import `findSimilarEntities` from `../entity-matcher` (file does not exist — RED). Write stubs using `it.todo()` for all four behaviors.

    Create bigpanda-app/components/__tests__/IngestionModal-changes.test.tsx: Import `IngestionModal` from `../IngestionModal`. Test the "Proposed Changes" section rendering. This component exists — tests will fail because the proposedChanges prop and "Proposed Changes" section don't exist yet (RED).

    Use React Testing Library for the component test:
    ```typescript
    import { render, screen } from '@testing-library/react';
    ```
    Check if @testing-library/react is in package.json first — if not, use it.todo() only (no render calls).
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- lib/__tests__/entity-matcher.test.ts components/__tests__/IngestionModal-changes.test.tsx --run 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist, TypeScript compiles, tests fail RED. Full suite (npm test -- --run) still passes (existing 148 tests unaffected).</done>
</task>

</tasks>

<verification>
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | grep -E "pass|fail|error" | tail -5
</verification>

<success_criteria>
- 4 test files exist in their correct paths
- All 4 files are valid TypeScript (no syntax errors, no import-breaking changes to existing code)
- New test cases are RED (todo or failing)
- Existing 148 tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/73.1-entity-lifecycle-management/73.1-01-SUMMARY.md`
</output>
