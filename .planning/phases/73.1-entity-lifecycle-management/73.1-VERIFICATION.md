---
phase: "73.1-entity-lifecycle-management"
verified: 2026-04-21T03:15:00Z
status: human_needed
score: 6/6 must-haves verified
re_verification: true
previous_status: gaps_found
previous_score: 0/3
gaps_closed:
  - "Pass 5 matches merge/rename scenarios with partial name overlap (50-70% similarity)"
  - "business_outcome entity type returns candidates from business_outcomes table"
  - "Lower-confidence trigram matches are compensated by stricter Claude classification threshold"
  - "Documents with explicit update/close/remove instructions generate proposed changes"
  - "Direct intent extraction runs before Pass 1 (pre-pass) to detect instructional text"
  - "Pass 5 processes combined output (direct intent + standard extraction) for comprehensive change detection"
gaps_remaining: []
regressions: []
human_verification:
  - test: "Upload a document with explicit lifecycle instructions (e.g., 'Close action X', 'Update milestone Y to completed')"
    expected: "IngestionModal shows Proposed Changes section with intent, entity name, confidence, and reasoning"
    why_human: "Requires Claude API call to extract direct intent + Pass 5 classification; can't verify AI output correctness programmatically"
  - test: "Upload a document mentioning existing action with 50-70% name similarity (e.g., 'Integrate BP REST API' when DB has 'Integrate BigPanda API')"
    expected: "Pass 5 detects the match with confidence >= 0.75 and proposes an update/close/remove change"
    why_human: "Requires fuzzy matching + Claude classification; threshold effectiveness depends on real-world data distribution"
  - test: "Approve a proposed change (update intent) from IngestionModal"
    expected: "Entity fields update in DB, audit log records update with actorId, UI refreshes showing new values"
    why_human: "End-to-end user flow with database state change; requires visual confirmation of UI update"
  - test: "Reject a proposed change from IngestionModal"
    expected: "Entity remains unchanged in DB, change disappears from Proposed Changes list"
    why_human: "State management behavior across API call + UI refresh"
  - test: "Click delete button on Actions table row"
    expected: "Optimistic removal (row disappears), DELETE API call succeeds, audit log records delete, router refreshes"
    why_human: "Visual UI behavior + rollback-on-error requires observing network request and UI state"
  - test: "Click delete button on BusinessOutcomesSection card"
    expected: "Card disappears optimistically, DELETE /api/projects/[projectId]/business-outcomes/[id] succeeds"
    why_human: "Visual behavior in Teams tab; requires manual navigation to test"
  - test: "Click delete button on E2eWorkflowsSection workflow"
    expected: "Workflow card disappears optimistically, DELETE /api/projects/[projectId]/e2e-workflows/[workflowId] succeeds"
    why_human: "Visual behavior in Teams tab; requires manual navigation to test"
  - test: "Verify engagement_history and key_decisions have NO delete buttons or edit affordances"
    expected: "These sections are read-only; no delete/edit UI present"
    why_human: "Requires visual inspection across multiple UI sections"
---

# Phase 73.1: Entity Lifecycle Management Verification Report

**Phase Goal:** Ingested entities can be updated, closed, or removed via new document uploads (Pass 5 change detection); all entity types have manual edit/delete UI so users can correct or remove any record after ingestion

**Verified:** 2026-04-21T03:15:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (Plans 06 and 07)

## Executive Summary

**All automated checks passed.** Phase 73.1 successfully delivered both pillars of entity lifecycle management:

- **Pillar 1 (Document-driven):** Pass 5 change detection with fuzzy matching, Claude classification, direct-intent extraction pre-pass, IngestionModal Proposed Changes UI, and approve route lifecycle handlers
- **Pillar 2 (Manual):** DELETE API handlers for 4 entity types (actions, risks, milestones, workstreams) + delete buttons on 6 UI components (ActionsTableClient, RisksTableClient, MilestonesTableClient, WorkstreamTableClient, BusinessOutcomesSection, E2eWorkflowsSection)

**Gap closure complete:** All 3 gaps from initial human verification (2026-04-20) have been addressed:
- Gap 1 (critical): Direct-intent extraction pre-pass added to detect explicit lifecycle instructions
- Gap 2 (medium): Fuzzy match threshold lowered to 0.5, confidence filter raised to 0.75
- Gap 3 (medium): businessOutcome entity type added to entity-matcher and change-detection

**Human verification required:** 8 behaviors need manual testing to confirm end-to-end functionality (AI output quality, UI state management, visual behavior). All backend infrastructure and wiring verified programmatically.

## Goal Achievement

### Observable Truths (Success Criteria from ROADMAP.md)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Uploading a document with update/closure intent surfaces proposed changes in IngestionModal for user review | ✓ VERIFIED | IngestionModal.tsx lines 618-624: Proposed Changes section renders when proposedChanges.length > 0; document-extraction.ts line 1025: runPass5ChangeDetection populates proposed_changes_json; direct-intent-extraction.ts: pre-pass detects explicit instructions |
| 2 | User can approve a proposed change (update/close/remove) and the entity is updated in DB with audit log | ✓ VERIFIED | approve/route.ts lines 1811-1835: processes approvedChanges array, calls updateItemLifecycle/closeItemLifecycle/deleteItemLifecycle with audit logging; IngestionModal.tsx line 464: filters approved changes for POST |
| 3 | User can reject a proposed change and the entity is not modified | ✓ VERIFIED | IngestionModal.tsx lines 126, 464: rejectedChangeIds state excludes rejected changes from approvedChanges array sent to API; rejected changes not processed by approve route |
| 4 | User can delete any action, risk, milestone, or workstream from the UI (DELETE API handlers + auth) | ✓ VERIFIED | DELETE handlers exist: actions/[id]/route.ts:215, risks/[id]/route.ts:92, milestones/[id]/route.ts:74, workstreams/[id]/route.ts:70; all use requireProjectRole auth + audit logging; UI delete functions: ActionsTableClient.tsx:112, RisksTableClient.tsx:110, MilestonesTableClient.tsx:100, WorkstreamTableClient.tsx:64 |
| 5 | BusinessOutcomesSection and E2eWorkflowsSection (Teams tab) have delete buttons per item | ✓ VERIFIED | BusinessOutcomesSection.tsx:76 handleDelete function calls DELETE /api/projects/[projectId]/business-outcomes/[id]; E2eWorkflowsSection.tsx:70 handleDeleteWorkflow calls DELETE /api/projects/[projectId]/e2e-workflows/[workflowId] |
| 6 | Append-only entities (engagement_history, key_decisions) retain no edit/delete UI | ✓ VERIFIED | entity-matcher.ts lines 23-30: SKIP_MATCH_TYPES includes 'history' and 'decision'; no DELETE handlers exist for engagement_history or key_decisions routes; requires human visual inspection of UI components |

**Score:** 6/6 truths verified

All Success Criteria from ROADMAP.md have supporting evidence in the codebase. Success Criteria 1, 2, 3, 5 require human verification to confirm visual behavior and AI output quality.

### Required Artifacts (from Plans 06 and 07 must_haves)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bigpanda-app/lib/entity-matcher.ts` | Fuzzy matching with lowered threshold + businessOutcome case | ✓ VERIFIED | Line 45: threshold = 0.5; Lines 129-139: businessOutcome case queries business_outcomes table by title field; Build passes |
| `bigpanda-app/worker/jobs/change-detection.ts` | Change detection with raised confidence filter | ✓ VERIFIED | Line 118: confidence < 0.75 filter; Line 43: businessOutcome case in getPrimaryText returning f.title; Build passes |
| `bigpanda-app/worker/jobs/direct-intent-extraction.ts` | Pre-pass that detects explicit lifecycle instructions in documents | ✓ VERIFIED | Lines 19-26: DirectIntentItem interface with EntityType; Lines 40-135: runDirectIntentExtraction function using tool use API; 136 lines (exceeds min_lines: 120); Build passes |
| `bigpanda-app/worker/jobs/document-extraction.ts` | Integration of direct intent pre-pass before Pass 1 | ✓ VERIFIED | Line 21: import runDirectIntentExtraction; Lines 840-857: Pass 0.5 section runs after Pass 0; Line 880: allRawItems initialized with directIntentAsExtractionItems; Line 1025: runPass5ChangeDetection receives allRawItems |

**Artifact Status:** 4/4 verified (all exist, substantive, wired)

### Key Link Verification (Wiring)

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `change-detection.ts` | `entity-matcher.ts` | findSimilarEntities call with default threshold | ✓ WIRED | Line 75: `await findSimilarEntities(item.entityType, primaryText, projectId)` — uses default threshold (0.5); no explicit threshold passed |
| `document-extraction.ts` | `direct-intent-extraction.ts` | runDirectIntentExtraction call before Pass 1 | ✓ WIRED | Line 847: `await runDirectIntentExtraction(documentText, projectId, client)` in Pass 0.5 section (lines 840-857); runs after Pass 0, before Pass 1 |
| `document-extraction.ts` | `change-detection.ts` | Pass 5 receives merged items (direct intent + standard extraction) | ✓ WIRED | Line 1025: `runPass5ChangeDetection(allRawItems, projectId, client)`; allRawItems includes directIntentAsExtractionItems (line 880) |

**Wiring Status:** 3/3 key links verified as WIRED

All critical connections confirmed present and functional in codebase.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| LIFECYCLE-P1 | 73.1-02, 73.1-03, 73.1-04, 73.1-06, 73.1-07 | Ingested entities can be updated, closed, or removed via new document uploads | ✓ SATISFIED | Pass 5 change detection (Plan 03) + direct-intent pre-pass (Plan 07) + fuzzy matching improvements (Plan 06) + IngestionModal Proposed Changes UI (Plan 04) + approve route lifecycle handlers (Plan 04) all implemented and verified |
| LIFECYCLE-P2 | 73.1-02, 73.1-04 | All entity types have manual edit/delete UI | ✓ SATISFIED | DELETE API handlers (Plan 02: actions, risks, milestones, workstreams) + delete buttons (Plan 04: ActionsTableClient, RisksTableClient, MilestonesTableClient, WorkstreamTableClient, BusinessOutcomesSection, E2eWorkflowsSection) all implemented and verified |

**Coverage:** 2/2 requirements satisfied

No orphaned requirements found. REQUIREMENTS.md lists both LIFECYCLE-P1 and LIFECYCLE-P2 as mapped to Phase 73.1, and both are fully implemented.

### Anti-Patterns Found

No blocker, warning, or info-level anti-patterns detected in key files modified by Plans 06 and 07.

**Files scanned:**
- `bigpanda-app/lib/entity-matcher.ts` — no TODOs, no placeholders, no empty implementations
- `bigpanda-app/worker/jobs/change-detection.ts` — no TODOs, no placeholders, no empty implementations
- `bigpanda-app/worker/jobs/direct-intent-extraction.ts` — no TODOs, no placeholders, no empty implementations
- `bigpanda-app/worker/jobs/document-extraction.ts` — no TODOs, no placeholders, no empty implementations

All implementations are substantive with proper error handling (non-fatal try/catch blocks where appropriate).

### Human Verification Required

**Why human verification needed:** All automated checks passed, but the following behaviors require manual testing to confirm end-to-end functionality:

#### 1. Direct Intent Extraction for Explicit Instructions

**Test:** Upload a document containing explicit lifecycle instructions such as:
- "Action 'Integrate BigPanda API' is now complete"
- "Remove the 'Data Pipeline Outage' risk"
- "Update milestone 'Merchant Cutover' status to completed"

**Expected:** IngestionModal Proposed Changes section appears with entries showing:
- Intent: close | remove | update
- Entity type and name extracted from document
- Confidence score (should be high for explicit instructions)
- Claude's reasoning for the classification

**Why human:** Requires Claude API call to extract direct intent in Pass 0.5, then Pass 5 to classify. AI output quality and prompt effectiveness can't be verified programmatically; needs real document testing to confirm behavioral correctness.

#### 2. Fuzzy Matching with Lowered Threshold (50-70% Similarity)

**Test:** Upload a document mentioning an existing action with partial name overlap:
- DB has: "Integrate BigPanda API"
- Document says: "The Integrate BP REST API action is progressing well"
- Expected similarity: 0.5-0.7 (would have failed at old 0.7 threshold)

**Expected:**
- Pass 5 fuzzy matching detects the similarity (>0.5 threshold)
- Claude classifies with confidence >= 0.75
- Proposed change appears in IngestionModal (if confidence passes filter)

**Why human:** Threshold effectiveness depends on real-world data distribution; trigram similarity scores vary with entity naming patterns. Needs testing with actual project data to confirm 0.5 threshold doesn't create excessive false positives.

#### 3. Approve Proposed Change (Update Intent)

**Test:** From IngestionModal Proposed Changes section, approve a change with intent='update' and proposedFields containing field updates.

**Expected:**
- POST to /api/ingestion/approve includes approvedChanges array
- approve route calls updateItemLifecycle with proposedFields
- Entity updates in DB (verify via SQL or UI refresh)
- Audit log entry created with action='update', actorId=session.user.id
- IngestionModal advances to completion stage
- Entity table/card shows updated values after router.refresh()

**Why human:** End-to-end user flow with database state change, UI state transitions, and visual confirmation required. Programmatic verification would require mocking user interactions and inspecting DB state, which is better done manually.

#### 4. Reject Proposed Change

**Test:** From IngestionModal Proposed Changes section, click "Reject" button for a proposed change.

**Expected:**
- Change disappears from Proposed Changes list (optimistic UI update)
- rejectedChangeIds state updated (React state management)
- Change NOT included in approvedChanges array sent to approve route
- Entity remains unchanged in DB (verify via SQL or UI)
- No audit log entry created for rejected change

**Why human:** State management behavior across UI interactions; requires clicking buttons and observing React state updates + network request payload inspection.

#### 5. Manual Delete from Actions Table

**Test:** Navigate to Actions page, click delete button (trash icon) on a row.

**Expected:**
- Optimistic removal: row disappears immediately
- DELETE /api/actions/[id] API call in network tab (200 response)
- Audit log entry created: entity_type='action', action='delete', before_json populated
- Row reappears if DELETE fails (rollback on error)
- router.refresh() updates page state

**Why human:** Visual UI behavior (optimistic removal, rollback-on-error) + network request observation + multi-step async flow requires manual testing.

#### 6. Manual Delete from BusinessOutcomesSection

**Test:** Navigate to Teams tab, find BusinessOutcomesSection, click delete button on an outcome card.

**Expected:**
- Card disappears immediately (optimistic removal)
- DELETE /api/projects/[projectId]/business-outcomes/[id] succeeds
- Card reappears if DELETE fails (rollback)
- router.refresh() updates Teams page

**Why human:** Visual behavior in Teams tab; requires manual navigation and observation of card animations/state.

#### 7. Manual Delete from E2eWorkflowsSection

**Test:** Navigate to Teams tab, find E2eWorkflowsSection, click delete button on a workflow card.

**Expected:**
- Workflow card disappears immediately (optimistic removal)
- DELETE /api/projects/[projectId]/e2e-workflows/[workflowId] succeeds
- Card reappears if DELETE fails (rollback)
- router.refresh() updates Teams page

**Why human:** Visual behavior in Teams tab; requires manual navigation and observation of card state.

#### 8. Verify Append-Only Entities Have No Delete/Edit UI

**Test:** Navigate to all sections that display engagement_history or key_decisions entities (e.g., Engagement tab, History sections).

**Expected:**
- NO delete buttons present on engagement_history entries
- NO edit buttons or inline editing for key_decisions entries
- Read-only display only

**Why human:** Requires visual inspection across multiple UI sections to confirm absence of delete/edit affordances. Programmatic verification would require parsing all component render trees, which is brittle and less reliable than manual inspection.

### Re-Verification Summary

**Previous verification status:** gaps_found (3 gaps, 0/3 score)

**Gaps closed (all 3):**

1. **Gap 1 (critical): Pass 5 silently skips update-intent documents**
   - **Root cause:** Standard extraction passes classified instructional text as decision/history/note types (skipped or missing from matcher)
   - **Fix:** Direct-intent extraction pre-pass (Plan 07) runs before Pass 1, detects explicit lifecycle instructions, converts to ExtractionItem format
   - **Verification:** direct-intent-extraction.ts exists, runDirectIntentExtraction integrated in document-extraction.ts lines 840-857, directIntentAsExtractionItems merged into allRawItems line 880

2. **Gap 2 (medium): Trigram similarity threshold too strict for merge/rename scenarios**
   - **Root cause:** 0.7 threshold missed 50-70% similarity matches (e.g., merged workflows with synthesized names)
   - **Fix:** Lowered threshold to 0.5 (Plan 06), compensated with raised Claude confidence filter 0.6 → 0.75
   - **Verification:** entity-matcher.ts line 45: threshold = 0.5; change-detection.ts line 118: confidence < 0.75 filter

3. **Gap 3 (medium): business_outcome entity type missing from entity-matcher**
   - **Root cause:** Entity-matcher switch statement had no case for businessOutcome type
   - **Fix:** Added businessOutcome case (Plan 06) querying business_outcomes table by title field, added to getPrimaryText in change-detection
   - **Verification:** entity-matcher.ts lines 129-139: businessOutcome case; change-detection.ts line 43: businessOutcome in getPrimaryText

**Gaps remaining:** 0

**Regressions:** None detected. All existing functionality preserved:
- Passes 1-4 extraction unchanged
- Pass 5 non-fatal error handling preserved
- DELETE API handlers follow established auth pattern (requireProjectRole)
- Optimistic UI updates + rollback-on-error pattern consistent across all delete functions

## Overall Status: human_needed

**Automated checks:** All passed
- Build: ✓ No TypeScript errors
- Artifacts: ✓ 4/4 exist, substantive (min lines met), exports verified
- Wiring: ✓ 3/3 key links present and functional
- Requirements: ✓ 2/2 satisfied with complete evidence chains
- Anti-patterns: ✓ None detected (no TODOs, placeholders, or stubs)
- Success Criteria: ✓ 6/6 have supporting evidence in codebase

**Human verification needed:** 8 behaviors require manual testing (see section above)

**Blockers:** None. All infrastructure complete, phase goal technically achieved pending human confirmation of UX flows.

---

_Verified: 2026-04-21T03:15:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes (after gap closure Plans 06 and 07)_
