---
phase: 14-time-+-project-analytics
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/phase14.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "tests/e2e/phase14.spec.ts exists and all 6 tests fail (RED) with descriptive stub messages"
    - "npx playwright test tests/e2e/phase14.spec.ts exits non-zero with 6 failures"
  artifacts:
    - path: "tests/e2e/phase14.spec.ts"
      provides: "E2E stubs for all Phase 14 success criteria"
      min_lines: 60
  key_links:
    - from: "tests/e2e/phase14.spec.ts"
      to: "data-testid attributes"
      via: "Playwright locators"
      pattern: "data-testid=\"weekly-summary\""
---

<objective>
Create the Wave 0 E2E test stubs for Phase 14. Six failing tests — one per success criterion — that drive all subsequent implementation tasks to GREEN.

Purpose: RED-first discipline ensures every UI element has a testid contract before the component is written. These stubs are the acceptance gate for Plans 14-03 through 14-05.
Output: tests/e2e/phase14.spec.ts with 6 failing stubs covering SC-1 through SC-4.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-time-+-project-analytics/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 14 E2E stubs</name>
  <files>tests/e2e/phase14.spec.ts</files>
  <action>
Create `tests/e2e/phase14.spec.ts` with 6 failing stub tests. Each stub uses `expect(false, 'stub — SC-X not yet implemented').toBe(true)` (the project-established stub pattern from STATE.md: "E2E stubs use expect(false, 'stub').toBe(true) not test.fixme()").

Six tests, each in its own `test()` block within a single describe block `'Phase 14: Time + Project Analytics'`:

1. `'SC-1: weekly summary table shows last 8 weeks'` — navigates to `/customer/1/time`, asserts `[data-testid="weekly-summary"]` is visible, asserts the table has at least 1 row with `[data-testid="weekly-summary-row"]`
2. `'SC-1: total hours header is visible'` — navigates to `/customer/1/time`, asserts `[data-testid="total-hours"]` is visible (extends existing TIME-01 testid, confirms it still passes)
3. `'SC-2: velocity chart shows 4 bars on HealthCard'` — navigates to `/`, asserts `[data-testid="velocity-chart"]` is visible, asserts exactly 4 bars (`[data-testid="velocity-bar"]` count === 4)
4. `'SC-2: action trend indicator shows directional arrow'` — navigates to `/`, asserts `[data-testid="action-trend"]` is visible with text matching `/[↑↓→]/`
5. `'SC-3: risk trend indicator visible on HealthCard'` — navigates to `/`, asserts `[data-testid="risk-trend"]` is visible with text matching `/[\d]+ open risks [↑↓→]/`
6. `'SC-4: weekly target field is editable in Time tab'` — navigates to `/customer/1/time`, asserts `[data-testid="weekly-target"]` is visible and clickable (the inline-editable capacity field)

All 6 tests must FAIL on first run — do NOT implement any UI changes in this plan.

Import pattern (use existing phase structure, e.g. phase13.spec.ts for reference if it exists):
```typescript
import { test, expect } from '@playwright/test';
```

Each test must stub out gracefully:
```typescript
test('SC-1: weekly summary table shows last 8 weeks', async ({ page }) => {
  expect(false, 'stub — SC-1 weekly summary not yet implemented').toBe(true);
});
```
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase14.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, 6 tests present, test run exits non-zero (all RED). No test passes — if any test passes, the stub is incorrect.</done>
</task>

</tasks>

<verification>
npx playwright test tests/e2e/phase14.spec.ts must report 6 failed, 0 passed.
File line count: wc -l tests/e2e/phase14.spec.ts should be >= 40 lines.
</verification>

<success_criteria>
tests/e2e/phase14.spec.ts exists with 6 stub tests, all RED. The file is the acceptance contract for Phase 14 — every subsequent plan targets making these tests GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/14-time-+-project-analytics/14-01-SUMMARY.md`
</output>
