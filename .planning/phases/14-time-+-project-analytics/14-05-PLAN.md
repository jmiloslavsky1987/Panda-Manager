---
phase: 14-time-+-project-analytics
plan: 05
type: execute
wave: 3
depends_on: ["14-03", "14-04"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "All 6 phase14.spec.ts tests pass GREEN"
    - "Full Playwright suite passes with no regressions from prior phases"
    - "User has visually confirmed velocity chart renders proportionally with real data"
    - "User has confirmed inline weekly target field saves and persists on page reload"
  artifacts:
    - path: "tests/e2e/phase14.spec.ts"
      provides: "6 passing E2E tests"
  key_links:
    - from: "tests/e2e/phase14.spec.ts"
      to: "live application UI"
      via: "Playwright browser automation"
      pattern: "data-testid"
---

<objective>
Drive all 6 Phase 14 E2E tests to GREEN and get human sign-off on visual behavior that automated tests cannot verify.

Purpose: Phase gate — ensures all analytics UX works end-to-end before Phase 14 is marked complete.
Output: Full test suite green, human-verified visual quality.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-time-+-project-analytics/14-VALIDATION.md
@.planning/phases/14-time-+-project-analytics/14-03-SUMMARY.md
@.planning/phases/14-time-+-project-analytics/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive phase14.spec.ts to GREEN</name>
  <files>tests/e2e/phase14.spec.ts</files>
  <action>
Run the Phase 14 E2E suite and fix any remaining test failures:

```bash
cd "/Users/jmiloslavsky/Documents/Project Assistant Code"
npx playwright test tests/e2e/phase14.spec.ts
```

For each failing test, diagnose the root cause:
- If `data-testid` is missing → fix the component file (HealthCard.tsx or TimeTab.tsx)
- If API returns wrong shape → fix the analytics route
- If TypeScript compile error → fix the type mismatch

After all 6 phase14 tests pass, run the full suite:
```bash
npx playwright test
```

Fix any regressions introduced by Phase 14 changes (particularly if getDashboardData() changes break existing DASH-* tests).

Common regressions to check:
- HealthCard existing testids (RAG badge, health data) still present after component changes
- TimeTab existing testid `[data-testid="total-hours"]` still present after adding summary section
- Dashboard page load performance not degraded (computeProjectAnalytics adds 2 DB queries per project)
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase14.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <done>npx playwright test tests/e2e/phase14.spec.ts exits 0. All 6 tests GREEN. Full suite (npx playwright test) shows 0 regressions from prior phases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — analytics UX sign-off</name>
  <files></files>
  <action>Human verifies visual quality of analytics UX (automated tests cannot cover proportional bar heights, color coding, and interactive editing UX).</action>
  <verify>
    <automated>MISSING — visual/interactive verification requires human</automated>
  </verify>
  <done>User types "approved" after confirming all visual behaviors below.</done>
  <what-built>
    - Time tab weekly summary table (last 8 weeks of hours, collapsible)
    - Time tab capacity planning header: "Weekly Target: X hrs | This week: Y hrs | ±Z"
    - Dashboard HealthCard velocity chart: 4 CSS bars with proportional heights + trend arrow
    - Dashboard HealthCard risk trend indicator: "N open risks ↑/↓/→"
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000 — Dashboard loads
    2. Check each HealthCard: confirm 4 velocity bars are visible at the bottom of each card. Bars should have varying heights if any actions were completed in the last 4 weeks. If no actions completed, bars should be flat at minimum height with "No completions yet" label.
    3. Check each HealthCard: confirm risk trend indicator shows "N open risks ↑/↓/→"
    4. Click through to any project → Time tab
    5. Confirm "Weekly Summary" collapsible section appears ABOVE the existing time entries table. Click the toggle to collapse/expand — confirm it works.
    6. Confirm capacity planning header: "Weekly Target" field shows either a number or "Set target". Click it — confirm it becomes an editable input. Type a number (e.g., 20), press Enter. Reload the page — confirm the target persisted.
    7. Confirm the flat entries table below the summary section is unchanged from Phase 5.2.
    8. Confirm no console errors at http://localhost:3000 (F12 → Console → no red errors).
  </how-to-verify>
  <resume-signal>Type "approved" if all items look correct, or describe any issues for Claude to fix before marking complete</resume-signal>
</task>

</tasks>

<verification>
All 6 phase14 E2E tests GREEN.
Full Playwright suite: 0 regressions.
Human checkpoint: approved.
</verification>

<success_criteria>
Phase 14 complete: all 4 success criteria met (SC-1 weekly rollup, SC-2 velocity chart, SC-3 risk trend, SC-4 capacity planning), all 6 E2E tests GREEN, human-verified visual quality.
</success_criteria>

<output>
After completion, create `.planning/phases/14-time-+-project-analytics/14-05-SUMMARY.md`
</output>
