---
phase: 35-overview-tab-weekly-focus-integration-tracker
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/tests/overview/weekly-focus.test.ts
  - bigpanda-app/tests/overview/integration-tracker.test.ts
autonomous: true
requirements:
  - WKFO-01
  - WKFO-02
  - OINT-01

must_haves:
  truths:
    - "Tests fail RED with stub pattern (not import errors)"
    - "weekly-focus.test.ts covers job, API GET, API POST, and ProgressRing behaviors"
    - "integration-tracker.test.ts covers ADR/Biggy/Unassigned grouping, type grouping, PATCH validation"
    - "Redis is mocked via vi.mock so tests run without a real Redis instance"
  artifacts:
    - path: "bigpanda-app/tests/overview/weekly-focus.test.ts"
      provides: "RED test stubs for WKFO-01 and WKFO-02"
      min_lines: 40
    - path: "bigpanda-app/tests/overview/integration-tracker.test.ts"
      provides: "RED test stubs for OINT-01"
      min_lines: 50
  key_links:
    - from: "tests/overview/weekly-focus.test.ts"
      to: "WKFO-01, WKFO-02"
      via: "stub tests covering weekly focus job and WeeklyFocus component"
      pattern: "expect.*toBeDefined\\(\\)"
    - from: "tests/overview/integration-tracker.test.ts"
      to: "OINT-01"
      via: "stub tests covering grouped tracker rendering and PATCH route"
      pattern: "expect.*toBeDefined\\(\\)"
---

<objective>
Create Wave 0 failing test scaffolds for Phase 35. Establish the RED baseline that implementation plans will turn GREEN.

Purpose: Nyquist compliance — every requirement has an automated test before any implementation begins.
Output: 2 failing test files with stubs for WKFO-01, WKFO-02, OINT-01.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-overview-tab-weekly-focus-integration-tracker/35-CONTEXT.md
@.planning/phases/35-overview-tab-weekly-focus-integration-tracker/35-RESEARCH.md
</context>

<interfaces>
<!-- Stub pattern established in Phase 32, 33, 34 — fail RED without brittle import errors -->

Wave 0 stub pattern (from State.md decisions):
```typescript
const x: any = undefined
expect(x).toBeDefined() // always fails RED — safe even when module doesn't exist yet
```

Redis mock pattern (adapt from tests/ingestion/extraction-enqueue.test.ts):
```typescript
vi.mock('ioredis', () => ({
  default: vi.fn().mockImplementation(() => ({
    get: vi.fn().mockResolvedValue(null),
    setex: vi.fn().mockResolvedValue('OK'),
    quit: vi.fn().mockResolvedValue(undefined),
    connect: vi.fn().mockResolvedValue(undefined),
  })),
}))
```

Recharts mock already present in bigpanda-app/vitest.config.ts — no new mock needed for ProgressRing.

Test framework: Vitest — run with `cd bigpanda-app && npx vitest run tests/overview/`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create weekly-focus test scaffolds (WKFO-01, WKFO-02)</name>
  <files>bigpanda-app/tests/overview/weekly-focus.test.ts</files>
  <behavior>
    - Stub 1: weeklyFocusJob generates 3-5 bullets and writes to Redis (WKFO-01 job)
    - Stub 2: GET /api/.../weekly-focus returns bullets from Redis cache (WKFO-01 GET)
    - Stub 3: GET /api/.../weekly-focus returns null bullets when cache empty (WKFO-01 empty state)
    - Stub 4: POST /api/.../weekly-focus enqueues job and returns { queued: true } (WKFO-01 on-demand)
    - Stub 5: WeeklyFocus component renders ProgressRing with overall completion % (WKFO-02)
    - Stub 6: ProgressRing pct is average of ADR + Biggy stepCounts from overview-metrics (WKFO-02)
  </behavior>
  <action>
    Create `bigpanda-app/tests/overview/weekly-focus.test.ts`.

    Use the Wave 0 stub pattern: `const x: any = undefined; expect(x).toBeDefined()` for each stub.
    This pattern always fails RED but never throws import errors when the modules don't exist yet.

    Add a Redis mock at the top of the file using vi.mock('ioredis') — see interfaces section for pattern.

    Structure with 6 test cases across 2 describe blocks:
    - describe('weeklyFocusJob') — 4 stubs covering job behavior and API route
    - describe('WeeklyFocus component') — 2 stubs covering WKFO-02 progress ring

    DO NOT import the actual modules yet — stubs must work without any source files existing.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/overview/weekly-focus.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, all 6 tests fail RED (not ERROR/syntax), command exits non-zero</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create integration-tracker test scaffolds (OINT-01)</name>
  <files>bigpanda-app/tests/overview/integration-tracker.test.ts</files>
  <behavior>
    - Stub 1: integrations with track='ADR' render in ADR section
    - Stub 2: integrations with track='Biggy' render in Biggy section
    - Stub 3: integrations with track=null render in Unassigned section
    - Stub 4: ADR integrations group by type: Inbound / Outbound / Enrichment
    - Stub 5: Biggy integrations group by type: Real-time / Context / Knowledge / UDC
    - Stub 6: PATCH /integrations/[id] accepts track + integration_type fields
    - Stub 7: Integration edit form/select filters type options by selected track
  </behavior>
  <action>
    Create `bigpanda-app/tests/overview/integration-tracker.test.ts`.

    Use the Wave 0 stub pattern: `const x: any = undefined; expect(x).toBeDefined()` for each stub.

    Structure with 7 test cases across 3 describe blocks:
    - describe('Integration grouping by track') — stubs 1-3 (section rendering)
    - describe('Integration grouping by type') — stubs 4-5 (type group headers)
    - describe('Integration API and edit form') — stubs 6-7 (PATCH + UI validation)

    DO NOT import actual modules — stubs must work without source files existing.
    No Redis mock needed for this file (integration tracker is pure DB/UI).
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/overview/integration-tracker.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, all 7 tests fail RED (not ERROR/syntax), command exits non-zero</done>
</task>

</tasks>

<verification>
Both test files must fail RED — not throw syntax errors or import errors.
Run full overview suite to confirm no regressions: `cd bigpanda-app && npx vitest run tests/overview/`
</verification>

<success_criteria>
- tests/overview/weekly-focus.test.ts exists with 6 RED stubs
- tests/overview/integration-tracker.test.ts exists with 7 RED stubs
- All existing tests/overview/ tests still pass (completeness-removal, metrics-health, timeline-replacement, track-separation)
- Combined: `npx vitest run tests/overview/` shows 13 new failures + all previous passes intact
</success_criteria>

<output>
After completion, create `.planning/phases/35-overview-tab-weekly-focus-integration-tracker/35-01-SUMMARY.md`
</output>
