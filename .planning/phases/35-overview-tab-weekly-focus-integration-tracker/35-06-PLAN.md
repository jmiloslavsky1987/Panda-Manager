---
phase: 35-overview-tab-weekly-focus-integration-tracker
plan: 06
type: execute
wave: 3
depends_on:
  - 35-04
  - 35-05
files_modified: []
autonomous: false
requirements:
  - WKFO-01
  - WKFO-02
  - OINT-01

must_haves:
  truths:
    - "All 13 new test stubs (6 weekly-focus + 7 integration-tracker) pass GREEN"
    - "Production build succeeds with no errors or warnings"
    - "User confirms Weekly Focus section visible in Overview tab with ProgressRing"
    - "User confirms Integration Tracker shows ADR / Biggy / Unassigned sections in browser"
    - "Generate Now button triggers job and shows appropriate feedback"
  artifacts:
    - path: "bigpanda-app/tests/overview/weekly-focus.test.ts"
      provides: "6 GREEN tests for WKFO-01, WKFO-02"
      min_lines: 40
    - path: "bigpanda-app/tests/overview/integration-tracker.test.ts"
      provides: "7 GREEN tests for OINT-01"
      min_lines: 50
  key_links:
    - from: "browser Overview tab"
      to: "WeeklyFocus component + weekly-focus API"
      via: "visual confirmation of bullets or empty state + ProgressRing"
      pattern: "data-testid=weekly-focus-section"
    - from: "browser Overview tab"
      to: "OnboardingDashboard integration-tracker section"
      via: "visual confirmation of ADR/Biggy/Unassigned sections"
      pattern: "data-testid=integration-tracker"
---

<objective>
Final verification gate: automated test suite must be fully GREEN, production build clean, and human UAT confirms WKFO-01, WKFO-02, OINT-01 requirements in the browser.

Purpose: Ensures Phase 35 is production-ready before closure.
Output: Verification sign-off for all three requirements.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-overview-tab-weekly-focus-integration-tracker/35-CONTEXT.md
@.planning/phases/35-overview-tab-weekly-focus-integration-tracker/35-04-SUMMARY.md
@.planning/phases/35-overview-tab-weekly-focus-integration-tracker/35-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated test suite + production build verification</name>
  <files>bigpanda-app/tests/overview/weekly-focus.test.ts, bigpanda-app/tests/overview/integration-tracker.test.ts</files>
  <action>
    Run the full automated verification sequence:

    1. Full test suite:
       `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run`
       All 13 new tests must pass GREEN. No pre-existing tests may regress.

    2. Production build:
       `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run build`
       Build must complete with 0 errors. TypeScript compilation must be clean.

    3. Fix any failures found. Common issues to watch for:
       - Redis mock not cleaning up between tests (use vi.clearAllMocks() in afterEach)
       - TypeScript errors from Integration interface update (check all Integration usages in OnboardingDashboard)
       - Missing async/await in API route handlers
       - ProgressRing circumference constant mismatch (use 138.23 from OverviewMetrics.tsx)

    If tests require mock updates to turn GREEN (e.g., updating the stub assertions to real assertions), update the test files:
    - `tests/overview/weekly-focus.test.ts` — replace stub assertions with real test logic
    - `tests/overview/integration-tracker.test.ts` — replace stub assertions with real test logic

    Do NOT use `--forceExit` or suppress test failures. Resolve root causes only.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>Full test suite passes. Production build succeeds. Zero errors or suppressed failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — WKFO-01, WKFO-02, OINT-01</name>
  <files>bigpanda-app/app/customer/[id]/overview/page.tsx</files>
  <what-built>
    - Weekly Focus section in Overview tab with AI-generated bullets, ProgressRing, and Generate Now button (WKFO-01, WKFO-02)
    - Integration Tracker redesigned with ADR / Biggy / Unassigned sections grouped by type (OINT-01)
    - BullMQ weekly-focus job registered and ready for scheduling
  </what-built>
  <how-to-verify>
    Start the app: `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run next-only`

    **WKFO-01 + WKFO-02 — Weekly Focus:**
    1. Navigate to any project's Overview tab (e.g., /customer/1/overview or similar)
    2. Verify: Weekly Focus section appears at the top of the Overview page
    3. Verify: ProgressRing is visible with a completion percentage in the section header area
    4. If bullets are present (Redis has cached data): confirm 3-5 bullet points display in the section
    5. If no bullets (empty state): confirm the placeholder message and "Generate Now" button appear
    6. Click "Generate Now" button: confirm it shows loading state and calls the API (check browser Network tab)
    7. Verify: ProgressRing percentage reflects actual ADR + Biggy onboarding completion (not 0% unless truly empty)

    **OINT-01 — Integration Tracker:**
    1. Scroll down to the Integration Tracker section in the Overview tab
    2. Verify: Integrations are no longer in a flat grid — confirm ADR, Biggy, or Unassigned section headers are present
    3. If project has integrations with track set: confirm they appear under the correct workstream section
    4. If project has integrations with track=null: confirm they appear under "Unassigned"
    5. Verify: Pipeline bar (status segments) and notes textarea still work (click a pipeline segment to cycle status)
    6. Navigate to an integration card and look for track/type assignment controls — confirm dropdown options for track (ADR/Biggy) and filtered type options appear

    **Cross-check:** All existing Overview tab content (Onboarding, Metrics, Health, Timeline) still renders correctly.
  </how-to-verify>
  <action>Human verifies the browser UI against the criteria in how-to-verify.</action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run next-only -- --help 2>&1 | head -5 || echo "start app manually"</automated>
  </verify>
  <done>User typed "approved" confirming WKFO-01, WKFO-02, OINT-01 verified in browser.</done>
  <resume-signal>Type "approved" when all criteria are verified, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Automated: `cd bigpanda-app && npx vitest run` — all tests pass.
Build: `cd bigpanda-app && npm run build` — clean.
Human: UAT sign-off on WKFO-01, WKFO-02, OINT-01.
</verification>

<success_criteria>
- npx vitest run: all tests pass (no failures, no skips)
- npm run build: 0 errors, 0 critical warnings
- Human UAT approved: Weekly Focus section visible with ProgressRing
- Human UAT approved: Integration Tracker shows workstream sections
- WKFO-01, WKFO-02, OINT-01 requirements verified in browser
</success_criteria>

<output>
After completion, create `.planning/phases/35-overview-tab-weekly-focus-integration-tracker/35-06-SUMMARY.md`
</output>
