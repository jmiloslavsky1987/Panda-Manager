---
phase: 21-teams-tab-+-architecture-tab
plan: "06"
type: execute
wave: 4
depends_on:
  - "21-03"
  - "21-04"
  - "21-05"
files_modified: []
autonomous: false
requirements:
  - TEAMS-01
  - TEAMS-02
  - TEAMS-03
  - TEAMS-04
  - TEAMS-05
  - TEAMS-06
  - TEAMS-07
  - TEAMS-08
  - TEAMS-09
  - TEAMS-10
  - TEAMS-11
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - ARCH-06
  - ARCH-07
  - ARCH-08
  - ARCH-09
  - ARCH-10
  - ARCH-11
  - ARCH-12

must_haves:
  truths:
    - "All 5 Teams tab sections render with correct labels and DB data (or yellow warning banners — no generic copy)"
    - "All 2 Architecture tab sub-tabs render without page reload on tab switch"
    - "Design tokens are visually correct: ADR blue, Biggy purple, E2E green, amber divider, status pill colors"
    - "Inline edit modals open and changes persist after page reload"
    - "Running team-engagement-map skill produces a downloadable HTML file that mirrors the Teams tab"
    - "Running workflow-diagram skill produces a downloadable HTML file that mirrors the Architecture tab"
  artifacts:
    - path: "bigpanda-app/app/customer/[id]/teams/page.tsx"
      provides: "Teams tab rendering 5 sections"
    - path: "bigpanda-app/app/customer/[id]/architecture/page.tsx"
      provides: "Architecture tab with 2-tab Workflow Diagram"
---

<objective>
Human verification checkpoint for the complete Phase 21 Teams Tab + Architecture Tab implementation. Confirms all visual rendering, design tokens, inline editing, and skill exports are working correctly before marking the phase complete.

Purpose: Phase 21 delivers customer-facing quality UI views that will be used in live customer demos and document exports. Visual verification is required — automated TypeScript checks are not sufficient for hex color accuracy, tab switching behavior, or skill HTML output quality.

Output: Human approval or itemized bug list for remediation.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-teams-tab-+-architecture-tab/21-03-SUMMARY.md
@.planning/phases/21-teams-tab-+-architecture-tab/21-04-SUMMARY.md
@.planning/phases/21-teams-tab-+-architecture-tab/21-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of Teams tab + Architecture tab + skill exports</name>
  <what-built>
    Complete Phase 21 implementation:
    - Teams tab: 5-section Team Engagement Map (Business Outcomes, Architecture Overview, E2E Workflows, Teams & Engagement Status, Top Focus Areas) replacing the old workstream table
    - Architecture tab: 2-tab Workflow Diagram (Before BigPanda + Current & Future State with ADR/Biggy tracks separated by amber divider + Team Onboarding Status table) replacing the old workstream cards
    - Inline editing with optimistic UI for both tabs
    - Updated team-engagement-map and workflow-diagram skills that export self-contained HTML using live DB data
  </what-built>
  <action>No automated action — human visual verification required. Proceed directly to how-to-verify steps.</action>
  <how-to-verify>
    Run `npm run dev` in bigpanda-app/ if not already running. Navigate to a project (e.g., /customer/1).

    **Teams Tab verification (navigate to /customer/1/teams):**
    1. Confirm 5 sections are visible: "Business Value & Expected Outcomes", "Architecture Overview", "End-to-End Workflows", "Teams & Engagement Status", "Top Focus Areas"
    2. If any section has no data, confirm a YELLOW warning banner appears (not generic placeholder text)
    3. Confirm ADR track pills are #1e40af (deep blue) and Biggy track pills are #6d28d9 (purple) — check by hovering over a pill and inspecting color
    4. Click a "+ Add" button in any section — confirm modal opens with correct fields
    5. Add a test business outcome — confirm it appears immediately (optimistic UI) and persists after page reload
    6. If testing with AMEX project: confirm canonical 8-team order in "Teams & Engagement Status"

    **Architecture Tab verification (navigate to /customer/1/architecture):**
    1. Confirm two tabs: "Before BigPanda" (grey dot) and "Current & Future State" (green dot)
    2. Click "Current & Future State" tab — confirm no page reload; content switches
    3. Confirm full-width amber divider with text "↓ BIGGY AI TRACK ↓" is visible between ADR and Biggy sections
    4. Confirm Team Onboarding Status table is present below both tracks with ADR (blue header) and Biggy (amber header) sections
    5. Confirm status pills show correct colors: green=Live, amber=In Progress/Pilot, gray=Planned
    6. "Before BigPanda" tab: confirm 5-phase horizontal flow is visible; confirm pain point cards render (or yellow warning if no data)
    7. If AMEX customer: confirm Aggregation Hub is orange. If Kaiser: confirm "Live in Production" badge.
    8. Click "Edit Before State" button — confirm modal opens

    **Skill export verification:**
    1. Navigate to /customer/1/skills
    2. Run "Team Engagement Map" skill — confirm it completes and produces an HTML output
    3. Open the HTML output — confirm it contains all 5 sections with design token colors (not generic grey)
    4. Run "Workflow Diagram" skill — confirm it completes and produces an HTML output
    5. Open the HTML output — confirm "Before BigPanda" and "Current & Future State" tabs are present with tab-switching JavaScript, amber divider visible
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues (section, component, expected vs actual behavior)</resume-signal>
</task>

</tasks>

<verification>
Human visual verification of all TEAMS and ARCH requirements. Phase 21 is complete when human types "approved".
</verification>

<success_criteria>
- All 5 Teams tab sections render correctly with DB data or yellow warning banners
- Architecture 2-tab view renders with correct design tokens and tab switching
- Inline editing persists changes after reload
- Both skill exports produce self-contained HTML that mirrors the tab views
- Design tokens visually match spec: #1e40af ADR, #6d28d9 Biggy, #d97706 amber divider, status pill colors
</success_criteria>

<output>
After human approval, create `.planning/phases/21-teams-tab-+-architecture-tab/21-06-SUMMARY.md` confirming Phase 21 complete.
</output>
