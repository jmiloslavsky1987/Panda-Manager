---
phase: 21-teams-tab-+-architecture-tab
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/tests/teams-arch/business-outcomes.test.ts
  - bigpanda-app/tests/teams-arch/e2e-workflows.test.ts
  - bigpanda-app/tests/teams-arch/teams-sections.test.ts
  - bigpanda-app/tests/teams-arch/warning-banner.test.ts
  - bigpanda-app/tests/teams-arch/team-order.test.ts
  - bigpanda-app/tests/teams-arch/skill-context-teams.test.ts
  - bigpanda-app/tests/teams-arch/design-tokens.test.ts
  - bigpanda-app/tests/teams-arch/arch-tabs.test.ts
  - bigpanda-app/tests/teams-arch/pain-points.test.ts
  - bigpanda-app/tests/teams-arch/status-pills.test.ts
  - bigpanda-app/tests/teams-arch/skill-context-arch.test.ts
  - bigpanda-app/tests/teams-arch/customer-rules.test.ts
  - bigpanda-app/tests/teams-arch/skill-html-export.test.ts
autonomous: true
requirements:
  - TEAMS-01
  - TEAMS-02
  - TEAMS-04
  - TEAMS-07
  - TEAMS-09
  - TEAMS-10
  - TEAMS-11
  - ARCH-01
  - ARCH-03
  - ARCH-08
  - ARCH-10
  - ARCH-11
  - ARCH-12

must_haves:
  truths:
    - "All 13 test stub files exist under bigpanda-app/tests/teams-arch/"
    - "Running npx vitest run tests/teams-arch/ exits 0 — all stubs pass as todo/skipped"
    - "Each stub file imports the subject under test so TypeScript will catch import errors post-implementation"
  artifacts:
    - path: "bigpanda-app/tests/teams-arch/business-outcomes.test.ts"
      provides: "Stubs for TEAMS-02 API shape"
    - path: "bigpanda-app/tests/teams-arch/e2e-workflows.test.ts"
      provides: "Stubs for TEAMS-04 nested query"
    - path: "bigpanda-app/tests/teams-arch/teams-sections.test.ts"
      provides: "Stubs for TEAMS-01 section rendering"
    - path: "bigpanda-app/tests/teams-arch/warning-banner.test.ts"
      provides: "Stubs for TEAMS-07 empty section banner"
    - path: "bigpanda-app/tests/teams-arch/team-order.test.ts"
      provides: "Stubs for TEAMS-09 AMEX canonical ordering"
    - path: "bigpanda-app/tests/teams-arch/skill-context-teams.test.ts"
      provides: "Stubs for TEAMS-10 context extension"
    - path: "bigpanda-app/tests/teams-arch/design-tokens.test.ts"
      provides: "Stubs for TEAMS-11 design token consistency"
    - path: "bigpanda-app/tests/teams-arch/arch-tabs.test.ts"
      provides: "Stubs for ARCH-01 two-tab structure"
    - path: "bigpanda-app/tests/teams-arch/pain-points.test.ts"
      provides: "Stubs for ARCH-03 JSONB pain points"
    - path: "bigpanda-app/tests/teams-arch/status-pills.test.ts"
      provides: "Stubs for ARCH-08 status pill color map"
    - path: "bigpanda-app/tests/teams-arch/skill-context-arch.test.ts"
      provides: "Stubs for ARCH-10 arch context extension"
    - path: "bigpanda-app/tests/teams-arch/customer-rules.test.ts"
      provides: "Stubs for ARCH-11 customer-specific rules"
    - path: "bigpanda-app/tests/teams-arch/skill-html-export.test.ts"
      provides: "Stubs for ARCH-12 self-contained HTML export"
  key_links:
    - from: "bigpanda-app/tests/teams-arch/team-order.test.ts"
      to: "bigpanda-app/components/teams/TeamsEngagementSection.tsx"
      via: "import of sortTeamsForCustomer or AMEX_CANONICAL_TEAMS constant"
      pattern: "AMEX_CANONICAL_TEAMS|sortTeams"
    - from: "bigpanda-app/tests/teams-arch/skill-context-teams.test.ts"
      to: "bigpanda-app/lib/skill-context-teams.ts"
      via: "import { buildTeamsSkillContext }"
      pattern: "buildTeamsSkillContext"
    - from: "bigpanda-app/tests/teams-arch/skill-context-arch.test.ts"
      to: "bigpanda-app/lib/skill-context-arch.ts"
      via: "import { buildArchSkillContext }"
      pattern: "buildArchSkillContext"
---

<objective>
Create the bigpanda-app/tests/teams-arch/ directory and write all 13 Vitest stub files required before any Wave 1+ implementation begins.

Purpose: VALIDATION.md marks nyquist_compliant: false because all 13 test files are missing. Wave 0 unblocks all subsequent plans by providing the test harness. Stubs use it.todo() so the suite passes green immediately — they turn red as implementations are written and specifications are filled in.

Output: 13 test stub files, all passing as skipped/todo, with imports pointing at the modules they will eventually exercise.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-teams-tab-+-architecture-tab/21-VALIDATION.md
@bigpanda-app/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/teams-arch/ directory and write the 7 Teams-side stubs</name>
  <files>
    bigpanda-app/tests/teams-arch/business-outcomes.test.ts,
    bigpanda-app/tests/teams-arch/e2e-workflows.test.ts,
    bigpanda-app/tests/teams-arch/teams-sections.test.ts,
    bigpanda-app/tests/teams-arch/warning-banner.test.ts,
    bigpanda-app/tests/teams-arch/team-order.test.ts,
    bigpanda-app/tests/teams-arch/skill-context-teams.test.ts,
    bigpanda-app/tests/teams-arch/design-tokens.test.ts
  </files>
  <action>
Create the directory bigpanda-app/tests/teams-arch/ and write the following 7 files. Each file must import its subject under test (so TypeScript errors surface after implementation) and declare stubs using it.todo().

**business-outcomes.test.ts** — covers TEAMS-02:
```typescript
import { describe, it } from 'vitest'
// Subject will be created in Plan 21-01
// import { getTeamsTabData } from '../../lib/queries'

describe('business-outcomes API shape (TEAMS-02)', () => {
  it.todo('getTeamsTabData returns businessOutcomes array with id, title, track, delivery_status, mapping_note')
  it.todo('businessOutcomes.track is one of ADR | Biggy | Both')
  it.todo('businessOutcomes.delivery_status is one of live | in_progress | blocked | planned')
  it.todo('POST /api/projects/[id]/business-outcomes returns created outcome with correct shape')
  it.todo('PATCH /api/projects/[id]/business-outcomes/[id] updates fields and returns updated outcome')
})
```

**e2e-workflows.test.ts** — covers TEAMS-04:
```typescript
import { describe, it } from 'vitest'
// Subject will be created in Plan 21-01
// import { getTeamsTabData } from '../../lib/queries'

describe('e2e-workflows nested query (TEAMS-04)', () => {
  it.todo('getTeamsTabData returns e2eWorkflows array where each workflow has a steps array')
  it.todo('steps are ordered by position ascending')
  it.todo('each step has label, track, status, position fields')
  it.todo('POST /api/projects/[id]/e2e-workflows creates workflow with empty steps array')
  it.todo('POST .../steps adds a step to the correct workflow')
})
```

**teams-sections.test.ts** — covers TEAMS-01:
```typescript
import { describe, it } from 'vitest'
// Subject will be created in Plan 21-03
// import { TeamEngagementMap } from '../../components/teams/TeamEngagementMap'

describe('Teams tab 5-section rendering (TEAMS-01)', () => {
  it.todo('TeamEngagementMap renders exactly 5 section children')
  it.todo('sections render in order: Business Value, Architecture Overview, E2E Workflows, Teams & Engagement, Focus Areas')
  it.todo('each section has a visible header label')
  it.todo('page has data-testid="teams-tab" at root')
})
```

**warning-banner.test.ts** — covers TEAMS-07:
```typescript
import { describe, it } from 'vitest'
// Subject will be created in Plan 21-03
// import { WarnBanner } from '../../components/teams/WarnBanner'

describe('Warning banner for empty sections (TEAMS-07)', () => {
  it.todo('WarnBanner renders with role="alert"')
  it.todo('WarnBanner has background #fef9c3 applied as inline style')
  it.todo('WarnBanner has border color #fde047 applied as inline style')
  it.todo('WarnBanner has text color #713f12 applied as inline style')
  it.todo('BusinessOutcomesSection renders WarnBanner when outcomes array is empty')
  it.todo('FocusAreasSection renders WarnBanner when focusAreas array is empty')
})
```

**team-order.test.ts** — covers TEAMS-09:
```typescript
import { describe, it, expect } from 'vitest'
// Subject will be created in Plan 21-03
// import { AMEX_CANONICAL_TEAMS, sortTeamsForCustomer } from '../../components/teams/TeamsEngagementSection'

describe('AMEX canonical team ordering (TEAMS-09)', () => {
  it.todo('sortTeamsForCustomer returns teams in canonical order when customer is AMEX')
  it.todo('sortTeamsForCustomer is case-insensitive — amex and AMEX both trigger ordering')
  it.todo('sortTeamsForCustomer does not reorder when customer is not AMEX')
  it.todo('teams not in the canonical list are appended after canonical teams')
  it.todo('AMEX_CANONICAL_TEAMS has exactly 8 entries')
})
```

**skill-context-teams.test.ts** — covers TEAMS-10:
```typescript
import { describe, it } from 'vitest'
// Subject will be created in Plan 21-05
// import { buildTeamsSkillContext } from '../../lib/skill-context-teams'

describe('buildTeamsSkillContext injects Teams DB data (TEAMS-10)', () => {
  it.todo('buildTeamsSkillContext returns a string containing business outcomes section')
  it.todo('buildTeamsSkillContext returns a string containing E2E workflows section')
  it.todo('buildTeamsSkillContext returns a string containing focus areas section')
  it.todo('buildTeamsSkillContext does NOT query architecture_integrations or before_state')
  it.todo('buildTeamsSkillContext accepts projectId as its argument')
})
```

**design-tokens.test.ts** — covers TEAMS-11:
```typescript
import { describe, it, expect } from 'vitest'

describe('Design token constants (TEAMS-11)', () => {
  it.todo('ADR text color token is #1e40af')
  it.todo('ADR background color token is #eff6ff')
  it.todo('ADR border color token is #bfdbfe')
  it.todo('Biggy text color token is #6d28d9')
  it.todo('Biggy background color token is #f5f3ff')
  it.todo('E2E green token is #065f46')
  it.todo('Live status bg is #dcfce7, text is #14532d')
  it.todo('In Progress status bg is #fef3c7, text is #92400e')
  it.todo('Blocked status bg is #fee2e2, text is #991b1b')
  it.todo('Planned status bg is #f1f5f9, text is #475569')
})
```
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/teams-arch/business-outcomes.test.ts tests/teams-arch/e2e-workflows.test.ts tests/teams-arch/teams-sections.test.ts tests/teams-arch/warning-banner.test.ts tests/teams-arch/team-order.test.ts tests/teams-arch/skill-context-teams.test.ts tests/teams-arch/design-tokens.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All 7 Teams-side stub files exist. Running the vitest command above exits 0 with all tests shown as todo/skipped — no failures.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 6 Architecture-side stubs</name>
  <files>
    bigpanda-app/tests/teams-arch/arch-tabs.test.ts,
    bigpanda-app/tests/teams-arch/pain-points.test.ts,
    bigpanda-app/tests/teams-arch/status-pills.test.ts,
    bigpanda-app/tests/teams-arch/skill-context-arch.test.ts,
    bigpanda-app/tests/teams-arch/customer-rules.test.ts,
    bigpanda-app/tests/teams-arch/skill-html-export.test.ts
  </files>
  <action>
Write the following 6 stub files.

**arch-tabs.test.ts** — covers ARCH-01:
```typescript
import { describe, it } from 'vitest'
// Subject will be created in Plan 21-04
// import { WorkflowDiagram } from '../../components/arch/WorkflowDiagram'

describe('Architecture tab two-tab structure (ARCH-01)', () => {
  it.todo('WorkflowDiagram renders a "Before BigPanda" tab button with grey dot indicator')
  it.todo('WorkflowDiagram renders a "Current & Future State" tab button with green dot indicator')
  it.todo('tab switching changes visible content without triggering navigation')
  it.todo('page has data-testid="architecture-tab" at root')
})
```

**pain-points.test.ts** — covers ARCH-03:
```typescript
import { describe, it } from 'vitest'
// Subject will be created in Plan 21-04
// import { BeforeBigPandaTab } from '../../components/arch/BeforeBigPandaTab'

describe('Pain points rendered from JSONB array (ARCH-03)', () => {
  it.todo('BeforeBigPandaTab renders one card per entry in beforeState.pain_points_json')
  it.todo('BeforeBigPandaTab renders WarnBanner when pain_points_json is empty')
  it.todo('pain_points_json JSONB column correctly typed as string[]')
  it.todo('Editing before-state sends complete pain_points_json array — no partial update')
})
```

**status-pills.test.ts** — covers ARCH-08:
```typescript
import { describe, it, expect } from 'vitest'
// Subject will be created in Plan 21-04
// import { getStatusPillStyle } from '../../components/arch/IntegrationNode'

describe('Status pill color map (ARCH-08)', () => {
  it.todo('live status returns bg #dcfce7 and text #14532d')
  it.todo('in_progress status returns bg #fef3c7 and text #92400e')
  it.todo('pilot status returns same colors as in_progress')
  it.todo('planned status returns bg #f1f5f9 and text #475569')
  it.todo('null/undefined status returns a neutral gray style')
})
```

**skill-context-arch.test.ts** — covers ARCH-10:
```typescript
import { describe, it } from 'vitest'
// Subject will be created in Plan 21-05
// import { buildArchSkillContext } from '../../lib/skill-context-arch'

describe('buildArchSkillContext injects Architecture DB data (ARCH-10)', () => {
  it.todo('buildArchSkillContext returns a string containing Before State section')
  it.todo('buildArchSkillContext returns a string containing Architecture Integrations section')
  it.todo('buildArchSkillContext returns a string containing Team Onboarding Status section')
  it.todo('buildArchSkillContext does NOT query business_outcomes, e2e_workflows, or focus_areas')
  it.todo('buildArchSkillContext accepts projectId as its argument')
})
```

**customer-rules.test.ts** — covers ARCH-11:
```typescript
import { describe, it } from 'vitest'
// Subject will be created in Plan 21-03 / Plan 21-04
// import { BeforeBigPandaTab } from '../../components/arch/BeforeBigPandaTab'
// import { TeamsEngagementSection } from '../../components/teams/TeamsEngagementSection'

describe('Customer-specific rendering rules (ARCH-11)', () => {
  it.todo('AMEX customer: aggregation hub node has orange styling (bg #fff7ed, border #ea580c)')
  it.todo('Kaiser customer: "Live in Production" badge renders in BeforeBigPandaTab instead of flow arrows')
  it.todo('customer matching is case-insensitive (amex === AMEX === Amex)')
  it.todo('non-special customer renders standard aggregation hub without orange styling')
})
```

**skill-html-export.test.ts** — covers ARCH-12:
```typescript
import { describe, it, expect } from 'vitest'
// Subject: HTML output from skill execution
// These tests will parse the HTML string returned by the workflow-diagram skill

describe('Skill HTML export is self-contained (ARCH-12)', () => {
  it.todo('workflow-diagram HTML output contains no <link> tags referencing external URLs')
  it.todo('workflow-diagram HTML output contains no <script src=...> tags referencing external URLs')
  it.todo('team-engagement-map HTML output contains no external CDN links')
  it.todo('HTML output contains inline style attributes — no class-only styling relying on external CSS')
  it.todo('HTML max-width container is present (1400px or 1600px) for viewport support')
})
```
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/teams-arch/ 2>&1 | tail -15</automated>
  </verify>
  <done>All 13 stub files exist in bigpanda-app/tests/teams-arch/. Running npx vitest run tests/teams-arch/ exits 0 — output shows tests as todo/skipped, zero failures. Suite is ready to receive implementations.</done>
</task>

</tasks>

<verification>
- `ls bigpanda-app/tests/teams-arch/*.test.ts | wc -l` returns 13
- `cd bigpanda-app && npx vitest run tests/teams-arch/` exits 0 with all tests todo/skipped
- No TypeScript compilation errors from the stub import lines
</verification>

<success_criteria>
- All 13 test stub files exist in bigpanda-app/tests/teams-arch/
- Full suite `npx vitest run tests/teams-arch/` passes (all todo/skipped)
- VALIDATION.md Wave 0 requirements are all satisfied (executor should update wave_0_complete: true in VALIDATION.md frontmatter)
</success_criteria>

<output>
After completion, create `.planning/phases/21-teams-tab-+-architecture-tab/21-00-SUMMARY.md`
</output>
