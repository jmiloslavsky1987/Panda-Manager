---
phase: 17-schema-extensions
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/tests/schema-v2.test.ts
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04
  - SCHEMA-05
  - SCHEMA-06
  - SCHEMA-07
  - SCHEMA-08
  - SCHEMA-09
  - SCHEMA-10
  - SCHEMA-11

must_haves:
  truths:
    - "A Vitest test file exists that imports and asserts every new table export from schema.ts"
    - "Tests are RED (fail) because schema.ts does not yet have the new exports"
    - "Test output identifies exactly which table/enum exports are missing"
  artifacts:
    - path: "bigpanda-app/tests/schema-v2.test.ts"
      provides: "Vitest test assertions for all 10 new Drizzle table exports and 5 enum exports"
      exports: []
  key_links:
    - from: "bigpanda-app/tests/schema-v2.test.ts"
      to: "bigpanda-app/db/schema.ts"
      via: "import of named exports"
      pattern: "import.*from.*db/schema"
---

<objective>
Create the Wave 0 test scaffold for Phase 17. This file asserts that all 10 new Drizzle table definitions and 5 new enum definitions are exported from schema.ts. Tests must fail RED before Wave 1 writes those exports.

Purpose: Nyquist compliance — automated verification command exists before any production code is written.
Output: bigpanda-app/tests/schema-v2.test.ts (RED)
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-schema-extensions/17-CONTEXT.md
@.planning/phases/17-schema-extensions/17-VALIDATION.md
@bigpanda-app/db/schema.ts
@bigpanda-app/vitest.config.ts
</context>

<feature>
  <name>Schema v2 export verification</name>
  <files>bigpanda-app/tests/schema-v2.test.ts</files>
  <behavior>
    - Test: discoveryItems export exists and is not undefined
    - Test: auditLog export exists and is not undefined
    - Test: businessOutcomes export exists and is not undefined
    - Test: e2eWorkflows export exists and is not undefined
    - Test: workflowSteps export exists and is not undefined
    - Test: focusAreas export exists and is not undefined
    - Test: architectureIntegrations export exists and is not undefined
    - Test: beforeState export exists and is not undefined
    - Test: teamOnboardingStatus export exists and is not undefined
    - Test: scheduledJobs export exists and is not undefined
    - Test: discoveryItemStatusEnum export exists and is not undefined
    - Test: ingestionStatusEnum export exists and is not undefined
    - Test: jobRunOutcomeEnum export exists and is not undefined
    - Test: deliveryStatusEnum export exists and is not undefined
    - Test: integrationTrackStatusEnum export exists and is not undefined
    - All 15 tests MUST fail RED before schema.ts is updated in Wave 1
  </behavior>
  <implementation>
    Write bigpanda-app/tests/schema-v2.test.ts using Vitest (import { describe, it, expect } from 'vitest').

    Import all 15 names from '../db/schema'. Because schema.ts does not yet export these names, the imports will resolve as undefined.

    Use this assertion pattern for each export:
      expect(discoveryItems, 'discoveryItems table must be exported from schema.ts').toBeDefined()

    Group into two describe blocks:
    1. "New table exports" — 10 table assertions (discoveryItems, auditLog, businessOutcomes, e2eWorkflows, workflowSteps, focusAreas, architectureIntegrations, beforeState, teamOnboardingStatus, scheduledJobs)
    2. "New enum exports" — 5 enum assertions (discoveryItemStatusEnum, ingestionStatusEnum, jobRunOutcomeEnum, deliveryStatusEnum, integrationTrackStatusEnum)

    Do NOT add any DB connection or SQL execution — this is a Drizzle schema export check only.

    RED phase check: run the test and confirm all 15 assertions fail with "received: undefined". If any pass, it means schema.ts already exports that name (unexpected — do not delete it, just note in the commit message).

    Commit: test(17-01): add failing schema-v2 export tests (RED)
  </implementation>
</feature>

<verification>
  <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run tests/schema-v2.test.ts 2>&1 | tail -20</automated>
</verification>

<success_criteria>
- bigpanda-app/tests/schema-v2.test.ts exists
- Running the test suite produces 15 failing tests (all RED)
- No syntax errors in the test file itself
- Commit message: test(17-01): add failing schema-v2 export tests (RED)
</success_criteria>

<output>
After completion, create `.planning/phases/17-schema-extensions/17-01-SUMMARY.md`
</output>
