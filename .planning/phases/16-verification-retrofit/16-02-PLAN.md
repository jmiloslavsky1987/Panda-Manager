---
phase: 16-verification-retrofit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-job-infrastructure/04-VERIFICATION.md
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05
  - SCHED-06
  - SCHED-07
  - SCHED-08

must_haves:
  truths:
    - ".planning/phases/04-job-infrastructure/04-VERIFICATION.md exists with status passed, human_needed, or gaps_found"
    - "All 8 requirement IDs (SCHED-01..08) appear in the Requirements Coverage table"
    - "Frontmatter includes score, verified timestamp, and human_verification list"
  artifacts:
    - path: ".planning/phases/04-job-infrastructure/04-VERIFICATION.md"
      provides: "Formal audit record for Phase 04 requirements"
      contains: "SCHED-01"
  key_links:
    - from: "gsd-verifier agent"
      to: ".planning/phases/04-job-infrastructure/04-VERIFICATION.md"
      via: "Write tool"
      pattern: "status: (passed|human_needed|gaps_found)"
---

<objective>
Run gsd-verifier retroactively on Phase 04 (Job Infrastructure) to produce a VERIFICATION.md covering SCHED-01..08 — the 8 requirements that were assigned to Phase 04 but never formally verified.

Purpose: Close the audit gap for Phase 04 so these requirements appear in the verified column of REQUIREMENTS.md.
Output: `.planning/phases/04-job-infrastructure/04-VERIFICATION.md`
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-job-infrastructure/04-VALIDATION.md
@.planning/phases/04-job-infrastructure/04-01-SUMMARY.md
@.planning/phases/04-job-infrastructure/04-02-SUMMARY.md
@.planning/phases/04-job-infrastructure/04-03-SUMMARY.md
@.planning/phases/04-job-infrastructure/04-04-SUMMARY.md
@.planning/phases/04-job-infrastructure/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run gsd-verifier on Phase 04 — Job Infrastructure</name>
  <files>.planning/phases/04-job-infrastructure/04-VERIFICATION.md</files>
  <action>
Spawn gsd-verifier as a sub-agent to produce `04-VERIFICATION.md` for Phase 04: Job Infrastructure.

**Verifier inputs:**
- Phase: 04
- Phase slug: job-infrastructure
- Phase directory: `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/04-job-infrastructure/`
- Codebase root: `/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app/`
- Requirement IDs to cover: SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05, SCHED-06, SCHED-07, SCHED-08
- Evidence files: 04-01..05-PLAN.md, 04-01..05-SUMMARY.md, 04-VALIDATION.md

**Critical verifier guidance (from research pitfall analysis):**

PITFALL 1 — SCHED-01 and SCHED-08 vs SCHED-02..07 must be assessed separately:
Phase 04 built the BullMQ infrastructure (worker process, Redis connection, scheduler registration) and job status UI — SCHED-01 and SCHED-08 are satisfied by this infrastructure. However, SCHED-02 through SCHED-07 require specific named jobs with specific cron schedules. Phase 15 REMOVED phantom entries `action-sync` and `weekly-briefing` from the scheduler. The verifier MUST inspect the current state of `bigpanda-app/worker/scheduler.ts`:

1. Check `JOB_SCHEDULE_MAP` — list which job names are actually registered
2. Known currently registered (per Phase 15): `morning-briefing`, `weekly-customer-status`
3. Check `bigpanda-app/worker/jobs/` directory — list actual job handler files
4. For each SCHED-02..07: determine whether a handler EXISTS in worker/jobs/ and whether it is in JOB_SCHEDULE_MAP

The correct verdict per requirement:
- If handler file exists AND in JOB_SCHEDULE_MAP AND cron matches spec: SATISFIED
- If handler file exists but NOT in JOB_SCHEDULE_MAP: gaps_found for that item
- If no handler file exists: gaps_found for that item (may need implementation phase)
- Do NOT apply blanket failure to the entire phase — assess each SCHED item individually

PITFALL 2 — SCHED-01 BullMQ dedicated process:
Verify `bigpanda-app/worker/index.ts` exists and starts a Worker (not an in-process cron). STATE.md confirms: `maxRetriesPerRequest: null` required, `createRedisConnection()` factory pattern, `concurrency:1` on Worker. Inspect the file directly.

PITFALL 3 — SCHED-08 schedule configurability:
STATE.md records that cron schedules were hardcoded in `JOB_DISPLAY` map (UI constants) in Phase 04 with editable via Settings deferred to Phase 5+. Verify whether the current codebase wired schedule configurability to Settings. If hardcoded, flag as gaps_found for SCHED-08's configurability clause.

PITFALL 4 — Phase 15 changes are the current ground truth:
Phase 15 modified `scheduler.ts`. The Phase 04 PLAN.md and SUMMARY.md describe what was built originally, but Phase 15 changed the scheduler. The verifier must read the LIVE `scheduler.ts` file, not rely solely on Phase 04 SUMMARYs.

**Output format** must match the confirmed VERIFICATION.md schema:
```yaml
---
phase: 04-job-infrastructure
verified: {ISO-8601}
status: passed | human_needed | gaps_found
score: {N}/8 automated must-haves verified
re_verification: false
human_verification:
  - test: "..."
    expected: "..."
    why_human: "..."
---
```

Body sections (in this order):
1. ## Goal Achievement — Observable Truths table
2. ### Required Artifacts — Expected file → Status → Details
3. ### Key Link Verification — From → To → Via → Status
4. ### Requirements Coverage — Req ID | Source Plan | Description | Status | Evidence
5. ### Anti-Patterns Found
6. ### TypeScript Compilation — pre-existing vs new errors
7. ### Human Verification Required
8. ### Gaps Summary

Requirements Coverage Status values: `SATISFIED`, `SATISFIED (indirect)`, `SATISFIED (enhanced)`, `NEEDS HUMAN`, `NOT FOUND`

Accept any status in {passed, human_needed, gaps_found} — do NOT force PASSED. Record the actual code state. gaps_found for individual SCHED items is expected and acceptable.
  </action>
  <verify>
    <automated>test -f "/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/04-job-infrastructure/04-VERIFICATION.md" && grep -q "phase: 04-job-infrastructure" "/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/04-job-infrastructure/04-VERIFICATION.md" && echo "VERIFICATION.md exists and has correct phase" || echo "MISSING or malformed"</automated>
  </verify>
  <done>04-VERIFICATION.md exists in .planning/phases/04-job-infrastructure/ with valid frontmatter and a Requirements Coverage table containing all 8 requirement IDs: SCHED-01 through SCHED-08, each with individual status verdicts</done>
</task>

</tasks>

<verification>
After task completes:

```bash
test -f ".planning/phases/04-job-infrastructure/04-VERIFICATION.md" && echo "EXISTS" || echo "MISSING"
grep -c "SCHED-0[1-8]" ".planning/phases/04-job-infrastructure/04-VERIFICATION.md"
```

Expected: file EXISTS, grep count >= 8 (all SCHED requirement IDs appear).
</verification>

<success_criteria>
- 04-VERIFICATION.md exists at the correct path
- Frontmatter has status in {passed, human_needed, gaps_found}
- All 8 requirement IDs (SCHED-01 through SCHED-08) appear in Requirements Coverage table with individual verdicts
- Individual gaps_found verdicts for SCHED items that are missing handlers or schedule entries are recorded (not blanket phase failure)
- No remediation tasks created within this plan — gaps recorded only
</success_criteria>

<output>
After completion, create `.planning/phases/16-verification-retrofit/16-02-SUMMARY.md`
</output>
