---
phase: 16-verification-retrofit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-data-foundation/01-VERIFICATION.md
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - DATA-08
  - SET-01
  - SET-03
  - SET-04

must_haves:
  truths:
    - ".planning/phases/01-data-foundation/01-VERIFICATION.md exists with status passed, human_needed, or gaps_found"
    - "All 11 requirement IDs (DATA-01..08, SET-01/03/04) appear in the Requirements Coverage table"
    - "Frontmatter includes score, verified timestamp, and human_verification list"
  artifacts:
    - path: ".planning/phases/01-data-foundation/01-VERIFICATION.md"
      provides: "Formal audit record for Phase 01 requirements"
      contains: "DATA-01"
  key_links:
    - from: "gsd-verifier agent"
      to: ".planning/phases/01-data-foundation/01-VERIFICATION.md"
      via: "Write tool"
      pattern: "status: (passed|human_needed|gaps_found)"
---

<objective>
Run gsd-verifier retroactively on Phase 01 (Data Foundation) to produce a VERIFICATION.md covering DATA-01..08 and SET-01/03/04 — the 11 requirements that were implemented in Phase 01 but never formally verified.

Purpose: Close the audit gap for Phase 01 so these requirements appear in the verified column of REQUIREMENTS.md.
Output: `.planning/phases/01-data-foundation/01-VERIFICATION.md`
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
@.planning/phases/01-data-foundation/01-02-SUMMARY.md
@.planning/phases/01-data-foundation/01-03-SUMMARY.md
@.planning/phases/01-data-foundation/01-04-SUMMARY.md
@.planning/phases/01-data-foundation/01-05-SUMMARY.md
@.planning/phases/01-data-foundation/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run gsd-verifier on Phase 01 — Data Foundation</name>
  <files>.planning/phases/01-data-foundation/01-VERIFICATION.md</files>
  <action>
Spawn gsd-verifier as a sub-agent to produce `01-VERIFICATION.md` for Phase 01: Data Foundation.

**Verifier inputs:**
- Phase: 01
- Phase slug: data-foundation
- Phase directory: `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/01-data-foundation/`
- Codebase root: `/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app/`
- Requirement IDs to cover: DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08, SET-01, SET-03, SET-04
- Evidence files: 01-01..06-PLAN.md, 01-01..06-SUMMARY.md, 01-VALIDATION.md

**Critical verifier guidance (from research pitfall analysis):**

PITFALL 1 — RED tests are expected, not failures:
The 01-VALIDATION.md documents that ALL migration and schema tests (schema.test.ts, append-only.test.ts, migration.test.ts, pool.test.ts, outputs.test.ts, settings.test.ts, yaml-roundtrip.test.ts) remain RED with ECONNREFUSED because PostgreSQL is NOT installed on this machine. This is BY DESIGN. Requirements DATA-01..08 and SET-01/03/04 must be verified via CODE STRUCTURE inspection, not test execution. The verifier should inspect: bigpanda-app/db/schema.ts (tables, RLS), bigpanda-app/db/migrations/ (trigger SQL), bigpanda-app/lib/settings.ts (settings service), bigpanda-app/lib/yaml-export.ts (YAML round-trip). A requirement CAN be SATISFIED via code evidence even when its test is RED due to missing infrastructure.

PITFALL 2 — SET-04 API key secrecy cannot be verified by reading source files:
SET-04 (Anthropic API key stored securely, not committed to git) requires git history inspection. Flag SET-04 as `human_needed` with instructions: run `git log --all -- .env*` and confirm no API key values appear. Do NOT mark it SATISFIED from code inspection alone.

PITFALL 3 — DATA-01 RLS must be verified as FORCE ROW LEVEL SECURITY:
STATE.md records that `FORCE ROW LEVEL SECURITY` was added to all 8 RLS tables (Phase 01-02 decision). The verifier should confirm this in schema.ts or migration SQL — standard `ENABLE ROW LEVEL SECURITY` alone does not satisfy the "enforced at DB layer" clause.

**Output format** must match the confirmed VERIFICATION.md schema:
```yaml
---
phase: 01-data-foundation
verified: {ISO-8601}
status: passed | human_needed | gaps_found
score: {N}/11 automated must-haves verified
re_verification: false
human_verification:
  - test: "..."
    expected: "..."
    why_human: "..."
---
```

Body sections (in this order):
1. ## Goal Achievement — Observable Truths table
2. ### Required Artifacts — Expected file → Status → Details
3. ### Key Link Verification — From → To → Via → Status
4. ### Requirements Coverage — Req ID | Source Plan | Description | Status | Evidence
5. ### Anti-Patterns Found
6. ### TypeScript Compilation — pre-existing vs new errors
7. ### Human Verification Required
8. ### Gaps Summary

Requirements Coverage Status values: `SATISFIED`, `SATISFIED (indirect)`, `SATISFIED (enhanced)`, `NEEDS HUMAN`, `NOT FOUND`

Accept any status in {passed, human_needed, gaps_found} — do NOT force PASSED. Record the actual code state.
  </action>
  <verify>
    <automated>test -f "/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/01-data-foundation/01-VERIFICATION.md" && grep -q "phase: 01-data-foundation" "/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/01-data-foundation/01-VERIFICATION.md" && echo "VERIFICATION.md exists and has correct phase" || echo "MISSING or malformed"</automated>
  </verify>
  <done>01-VERIFICATION.md exists in .planning/phases/01-data-foundation/ with valid frontmatter (phase, verified, status, score fields present) and a Requirements Coverage table containing all 11 requirement IDs: DATA-01..08, SET-01, SET-03, SET-04</done>
</task>

</tasks>

<verification>
After task completes:

```bash
test -f ".planning/phases/01-data-foundation/01-VERIFICATION.md" && echo "EXISTS" || echo "MISSING"
grep -c "DATA-0[1-8]\|SET-0[134]" ".planning/phases/01-data-foundation/01-VERIFICATION.md"
```

Expected: file EXISTS, grep count >= 11 (all requirement IDs appear at least once).
</verification>

<success_criteria>
- 01-VERIFICATION.md exists at the correct path
- Frontmatter has status in {passed, human_needed, gaps_found}
- All 11 requirement IDs (DATA-01 through DATA-08, SET-01, SET-03, SET-04) appear in Requirements Coverage table
- SET-04 is marked NEEDS HUMAN with git inspection instructions
- No remediation tasks created within this plan — gaps recorded only
</success_criteria>

<output>
After completion, create `.planning/phases/16-verification-retrofit/16-01-SUMMARY.md`
</output>
