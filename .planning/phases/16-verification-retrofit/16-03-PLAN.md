---
phase: 16-verification-retrofit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-skill-engine/05-VERIFICATION.md
autonomous: true
requirements:
  - SKILL-02
  - SKILL-14
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04

must_haves:
  truths:
    - ".planning/phases/05-skill-engine/05-VERIFICATION.md exists with status passed, human_needed, or gaps_found"
    - "All 6 requirement IDs (SKILL-02, SKILL-14, OUT-01..04) appear in the Requirements Coverage table"
    - "Frontmatter includes score, verified timestamp, and human_verification list"
  artifacts:
    - path: ".planning/phases/05-skill-engine/05-VERIFICATION.md"
      provides: "Formal audit record for Phase 05 requirements"
      contains: "SKILL-02"
  key_links:
    - from: "gsd-verifier agent"
      to: ".planning/phases/05-skill-engine/05-VERIFICATION.md"
      via: "Write tool"
      pattern: "status: (passed|human_needed|gaps_found)"
---

<objective>
Run gsd-verifier retroactively on Phase 05 (Skill Engine) to produce a VERIFICATION.md covering SKILL-02, SKILL-14, and OUT-01..04 — the 6 requirements that were assigned to Phase 05 but never formally verified.

Purpose: Close the audit gap for Phase 05 so these requirements appear in the verified column of REQUIREMENTS.md.
Output: `.planning/phases/05-skill-engine/05-VERIFICATION.md`
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-skill-engine/05-VALIDATION.md
@.planning/phases/05-skill-engine/05-01-SUMMARY.md
@.planning/phases/05-skill-engine/05-02-SUMMARY.md
@.planning/phases/05-skill-engine/05-03-SUMMARY.md
@.planning/phases/05-skill-engine/05-04-SUMMARY.md
@.planning/phases/05-skill-engine/05-05-SUMMARY.md
@.planning/phases/05-skill-engine/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run gsd-verifier on Phase 05 — Skill Engine</name>
  <files>.planning/phases/05-skill-engine/05-VERIFICATION.md</files>
  <action>
Spawn gsd-verifier as a sub-agent to produce `05-VERIFICATION.md` for Phase 05: Skill Engine.

**Verifier inputs:**
- Phase: 05
- Phase slug: skill-engine
- Phase directory: `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/05-skill-engine/`
- Codebase root: `/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app/`
- Requirement IDs to cover: SKILL-02, SKILL-14, OUT-01, OUT-02, OUT-03, OUT-04
- Evidence files: 05-01..06-PLAN.md, 05-01..06-SUMMARY.md, 05-VALIDATION.md

**Critical verifier guidance (from research pitfall analysis):**

PITFALL 1 — OUT-01..04 bug was fixed in Plan 05-06, not the earlier plans:
The 05-06-SUMMARY.md explicitly records that the `skill-run.ts` generic handler had a bug: it did NOT write to `outputs` or `drafts` tables. This was discovered during human verification in Plan 05-06 and fixed before Phase 05 closed. The verifier MUST read the LIVE `bigpanda-app/app/api/skills/run/route.ts` (or equivalent) to confirm the fix is present. OUT-01..04 should come back SATISFIED in the current codebase. Do NOT mark them NOT FOUND based on earlier plan descriptions.

PITFALL 2 — SKILL-02 token budget guard — verify implementation depth, not just test coverage:
Phase 05 VALIDATION.md confirms `phase5.spec.ts --grep "SKILL-02"` was GREEN. However, the verifier must inspect WHERE the token budget logic actually lives. Look in `bigpanda-app/lib/` or `bigpanda-app/app/api/skills/` for:
- Token count estimation before Claude API call
- Truncation or summarization of low-priority context when over budget
If the E2E test is green but the implementation is a no-op stub (e.g., `estimateTokens()` returns 0 or always passes), flag SKILL-02 as gaps_found with specific evidence. A passing test is necessary but not sufficient — the implementation must actually guard.

PITFALL 3 — SKILL-14 SKILL.md runtime loading:
SKILL-14 requires that SKILL.md files are read from disk at runtime and skill_path is configurable in settings. Phase 15 confirmed this was fixed for 3 handlers using `resolveSkillsDir()`. Verify the current skill dispatch path reads skill files via the settings-driven path, not hardcoded __dirname-relative paths. Check `bigpanda-app/worker/jobs/` handler files.

PITFALL 4 — OUT-02 Output Library view filtering:
OUT-02 requires the Output Library view to be filterable by account, skill type, and date range. Verify a UI component at or reachable from `bigpanda-app/app/outputs/` (or similar) renders with filter controls. Check both the route existence and that filter query params are wired to the DB query.

**Output format** must match the confirmed VERIFICATION.md schema:
```yaml
---
phase: 05-skill-engine
verified: {ISO-8601}
status: passed | human_needed | gaps_found
score: {N}/6 automated must-haves verified
re_verification: false
human_verification:
  - test: "..."
    expected: "..."
    why_human: "..."
---
```

Body sections (in this order):
1. ## Goal Achievement — Observable Truths table
2. ### Required Artifacts — Expected file → Status → Details
3. ### Key Link Verification — From → To → Via → Status
4. ### Requirements Coverage — Req ID | Source Plan | Description | Status | Evidence
5. ### Anti-Patterns Found
6. ### TypeScript Compilation — pre-existing vs new errors
7. ### Human Verification Required
8. ### Gaps Summary

Requirements Coverage Status values: `SATISFIED`, `SATISFIED (indirect)`, `SATISFIED (enhanced)`, `NEEDS HUMAN`, `NOT FOUND`

Accept any status in {passed, human_needed, gaps_found} — do NOT force PASSED. Record the actual code state.
  </action>
  <verify>
    <automated>test -f "/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/05-skill-engine/05-VERIFICATION.md" && grep -q "phase: 05-skill-engine" "/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/05-skill-engine/05-VERIFICATION.md" && echo "VERIFICATION.md exists and has correct phase" || echo "MISSING or malformed"</automated>
  </verify>
  <done>05-VERIFICATION.md exists in .planning/phases/05-skill-engine/ with valid frontmatter and a Requirements Coverage table containing all 6 requirement IDs: SKILL-02, SKILL-14, OUT-01, OUT-02, OUT-03, OUT-04</done>
</task>

</tasks>

<verification>
After task completes:

```bash
test -f ".planning/phases/05-skill-engine/05-VERIFICATION.md" && echo "EXISTS" || echo "MISSING"
grep -c "SKILL-02\|SKILL-14\|OUT-0[1-4]" ".planning/phases/05-skill-engine/05-VERIFICATION.md"
```

Expected: file EXISTS, grep count >= 6 (all requirement IDs appear).
</verification>

<success_criteria>
- 05-VERIFICATION.md exists at the correct path
- Frontmatter has status in {passed, human_needed, gaps_found}
- All 6 requirement IDs (SKILL-02, SKILL-14, OUT-01..04) appear in Requirements Coverage table
- OUT-01..04 reflect the post-bug-fix state of skill-run.ts (should be SATISFIED)
- SKILL-02 is assessed by implementation depth, not just test coverage
- No remediation tasks created within this plan — gaps recorded only
</success_criteria>

<output>
After completion, create `.planning/phases/16-verification-retrofit/16-03-SUMMARY.md`
</output>
