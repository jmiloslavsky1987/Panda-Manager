---
phase: 40-search-traceability-skills-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bigpanda-app/tests/search/global-search.test.tsx
  - bigpanda-app/tests/search/decisions-filter.test.tsx
  - bigpanda-app/tests/artifacts/extracted-entities.test.tsx
  - bigpanda-app/tests/history/audit-log-feed.test.tsx
  - bigpanda-app/tests/skills/job-progress.test.tsx
  - bigpanda-app/tests/skills/job-cancel.test.ts
autonomous: true
requirements: [SRCH-01, SRCH-02, ARTF-01, HIST-01, SKLS-01, SKLS-02]

must_haves:
  truths:
    - "All 6 test files exist and contain meaningful failing assertions (not just empty describe blocks)"
    - "Running the test suite exits RED for all phase-40 tests"
    - "Tests cover the key behaviors each requirement specifies"
  artifacts:
    - path: "bigpanda-app/tests/search/global-search.test.tsx"
      provides: "SRCH-01 test contract — debounce, grouping, navigation"
    - path: "bigpanda-app/tests/search/decisions-filter.test.tsx"
      provides: "SRCH-02 test contract — URL params, client-side filter"
    - path: "bigpanda-app/tests/artifacts/extracted-entities.test.tsx"
      provides: "ARTF-01 test contract — reverse lookup, tab rendering, links"
    - path: "bigpanda-app/tests/history/audit-log-feed.test.tsx"
      provides: "HIST-01 test contract — unified feed merge, field-level diffs"
    - path: "bigpanda-app/tests/skills/job-progress.test.tsx"
      provides: "SKLS-01 test contract — elapsed timer, polling auto-stop"
    - path: "bigpanda-app/tests/skills/job-cancel.test.ts"
      provides: "SKLS-02 test contract — cancel endpoint, DB status update"
  key_links:
    - from: "tests/search/global-search.test.tsx"
      to: "components/GlobalSearchBar.tsx"
      via: "import and render assertions"
    - from: "tests/skills/job-cancel.test.ts"
      to: "app/api/skills/runs/[runId]/cancel/route.ts"
      via: "fetch mock assertions"
---

<objective>
Create six failing test scaffolds (Wave 0) covering all Phase 40 requirements before any implementation begins.

Purpose: Nyquist TDD compliance — tests written RED first so each Wave 1 plan has a concrete pass/fail target.
Output: Six test files with meaningful assertions that fail because the implementation does not yet exist.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-search-traceability-skills-ux/40-CONTEXT.md
@.planning/phases/40-search-traceability-skills-ux/40-RESEARCH.md

<interfaces>
<!-- Test infrastructure: vitest 4.1.1 + @testing-library/react 16.3.2, config in bigpanda-app/vitest.config.ts -->
<!-- Test root: bigpanda-app/tests/ (not __tests__/) — see vitest.config.ts root is bigpanda-app/ -->
<!-- jsdom environment: add // @vitest-environment jsdom at top of .tsx test files -->
<!-- Node environment: default for API tests (.ts files without jsdom comment) -->

<!-- Standard mocks needed (from existing tests like tests/sync/metrics-invalidate.test.tsx): -->
vi.mock('next/navigation', () => ({
  useRouter: () => ({ push: vi.fn(), refresh: vi.fn() }),
  useSearchParams: () => ({ get: vi.fn(() => null), toString: vi.fn(() => '') }),
}))

<!-- SearchResult interface (from lib/queries.ts): -->
interface SearchResult {
  id: number
  table: string    // 'actions' | 'risks' | 'key_decisions' | ...
  section: string  // 'Actions' | 'Risks' | 'Key Decisions' | ...
  project_id: number | null
  project_name: string
  customer: string
  date: string | null
  title: string
  snippet: string
}

<!-- SkillRun interface (from components/SkillsTabClient.tsx): -->
interface SkillRun {
  id: number
  run_id: string
  project_id: number | null
  skill_name: string
  status: 'pending' | 'running' | 'completed' | 'failed'
  started_at: Date | null
  completed_at: Date | null
  created_at: Date
}

<!-- KeyDecision type (from lib/queries.ts): -->
type KeyDecision = typeof keyDecisions.$inferSelect
// Relevant fields: id, decision, context, date, created_at, source, source_artifact_id

<!-- AuditLogEntry (to be created in lib/queries.ts): -->
interface AuditLogEntry {
  id: number
  entity_type: string
  entity_id: number
  external_id: string | null
  action: string    // 'create' | 'update' | 'delete'
  actor_id: string | null
  before_json: Record<string, any> | null
  after_json: Record<string, any> | null
  created_at: Date
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Search and Decisions filter test scaffolds (SRCH-01, SRCH-02)</name>
  <files>bigpanda-app/tests/search/global-search.test.tsx, bigpanda-app/tests/search/decisions-filter.test.tsx</files>
  <behavior>
    - global-search.test.tsx: GlobalSearchBar renders a text input; does NOT call fetch when query is less than 2 chars; calls fetch with debounce when query reaches 2+ chars; results are grouped by section with labelled headers; clicking a result calls router.push with the correct tab path; Escape or clicking outside closes the dropdown
    - decisions-filter.test.tsx: DecisionsTableClient renders all decisions when no filter active; text filter on `q` narrows results to only matching decisions; `from` date filter excludes decisions before the date; `to` date filter excludes decisions after the date; filter inputs call updateParam which updates URL search params
  </behavior>
  <action>
    Create `bigpanda-app/tests/search/` directory. Write `global-search.test.tsx` with `// @vitest-environment jsdom` header.

    Import GlobalSearchBar from `@/components/GlobalSearchBar` (does not exist yet — this import will fail, making the test RED).

    Mock `next/navigation` with standard pattern. Mock `global.fetch` to return `{ results: [{ id: 1, table: 'actions', section: 'Actions', title: 'Deploy fix', snippet: '', project_id: 1, project_name: 'BigPanda', customer: 'BigPanda', date: null }] }`.

    Test cases:
    1. `renders search input` — render `<GlobalSearchBar projectId={1} />`, expect input present
    2. `does not fetch for 1-char query` — type 'a', advance timers, expect fetch NOT called
    3. `fetches after 300ms debounce for 2+ char query` — use `vi.useFakeTimers()`, type 'ri', advance 300ms, expect fetch called with `/api/search?q=ri&account=1`
    4. `groups results by section` — resolve fetch, expect section header "Actions (1)"
    5. `navigates on result click` — click result, expect `router.push` called with `/customer/1/actions`
    6. `closes dropdown on Escape` — open dropdown, press Escape, expect dropdown closed

    Write `decisions-filter.test.tsx` with `// @vitest-environment jsdom` header.

    Import DecisionsTableClient from `@/components/DecisionsTableClient` (does not exist yet).

    Test cases:
    1. `renders all decisions without filter` — pass 3 decisions, expect all 3 rendered
    2. `text filter narrows to matching decisions` — set `q` searchParam to 'budget', expect only matching decision shown
    3. `from date filter excludes earlier decisions` — set `from` to '2026-03-01', expect decisions before that date hidden
    4. `to date filter excludes later decisions` — set `to` to '2026-02-28', expect decisions after that date hidden
    5. `combined filters apply together` — set both `q` and `from`, expect intersection

    Both test files must import non-existent components so they fail on import. Confirm failure mode with an import-only test.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run tests/search/ 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist, run without syntax errors, and fail RED because the imported components do not exist yet</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Artifact, History, and Skills test scaffolds (ARTF-01, HIST-01, SKLS-01, SKLS-02)</name>
  <files>bigpanda-app/tests/artifacts/extracted-entities.test.tsx, bigpanda-app/tests/history/audit-log-feed.test.tsx, bigpanda-app/tests/skills/job-progress.test.tsx, bigpanda-app/tests/skills/job-cancel.test.ts</files>
  <behavior>
    - extracted-entities.test.tsx: ArtifactEditModal renders two tabs when opened; Extracted Entities tab shows risks, actions, milestones, decisions grouped by type with counts; each entity item has a link to the correct tab path; clicking a link closes the modal
    - audit-log-feed.test.tsx: computeAuditDiff returns 'Created' when before_json is null; returns changed fields string when fields differ; history page merges audit entries with notes in descending created_at order; audit entries show "Activity" badge; notes show source badge unchanged
    - job-progress.test.tsx: SkillsTabClient shows elapsed time counter on skill card when job is running; polling stops when job reaches completed state; polling stops when job reaches failed state; elapsed time increments each second
    - job-cancel.test.ts: POST /api/skills/runs/[runId]/cancel returns 200 with { success: true, status: 'cancelled' }; updates skill_runs DB row status to 'cancelled'; calls queue.remove() on the BullMQ queue
  </behavior>
  <action>
    Create `bigpanda-app/tests/artifacts/`, `bigpanda-app/tests/history/`, `bigpanda-app/tests/skills/` directories.

    **extracted-entities.test.tsx** (jsdom): Import ArtifactEditModal from `@/components/ArtifactEditModal`. Import getEntitiesExtractedFromArtifact from `@/lib/queries` (does not exist yet). Mock fetch to return `{ risks: [{id:1, external_id:'R-001', description:'Test risk'}], actions: [], milestones: [], decisions: [] }`. Test: (1) modal has two tabs "Details" and "Extracted Entities"; (2) Extracted Entities tab shows "Risks (1)"; (3) risk item renders as a link to `/customer/1/risks`; (4) clicking link triggers modal close.

    **audit-log-feed.test.tsx** (jsdom): Import computeAuditDiff from `@/lib/queries` (does not exist yet). Import HistoryPage logic or the unified feed rendering. Test: (1) `computeAuditDiff(null, {...})` returns 'Created'; (2) `computeAuditDiff({status:'open'}, {status:'mitigated'})` returns 'status: open → mitigated'; (3) merged feed sorts audit entries and notes descending by created_at; (4) audit entries render with "Activity" badge class or text; (5) audit entry shows the diff string.

    **job-progress.test.tsx** (jsdom): Import SkillsTabClient from `@/components/SkillsTabClient`. Mock fetch: status endpoint returns `{ status: 'running' }` first, then `{ status: 'completed' }`. Use vi.useFakeTimers(). Test: (1) after triggerSkill, elapsed time "0m 0s" appears; (2) after 61 seconds, displays "1m 1s"; (3) after polling returns completed, elapsed timer disappears; (4) after polling returns failed, elapsed timer disappears.

    **job-cancel.test.ts** (node): Import the cancel route handler from `@/app/api/skills/runs/[runId]/cancel/route` (does not exist yet). Mock db and BullMQ Queue. Test: (1) POST returns `{ success: true, status: 'cancelled' }`; (2) db.update called with status 'cancelled'; (3) queue.remove called with 'skill-run-{runId}'; (4) queue.close() called in finally block.

    All 4 files must fail RED because the imported modules/functions do not yet exist.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run tests/artifacts/ tests/history/ tests/skills/ 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 test files exist, run without syntax errors, and fail RED because the imported components/functions do not exist yet. Running the full phase test suite shows 6 failing test files.</done>
</task>

</tasks>

<verification>
All 6 test files exist in the correct directories. Full test command:
`cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run tests/search/ tests/artifacts/ tests/history/ tests/skills/ 2>&1 | tail -30`

Expected: tests collected, assertions fail on import (component not found) or on assertion mismatch. Exit code non-zero.
</verification>

<success_criteria>
- 6 test files created across 4 directories: tests/search/, tests/artifacts/, tests/history/, tests/skills/
- Each test file has at least 3 meaningful failing assertions
- `npm test -- --run tests/search/ tests/artifacts/ tests/history/ tests/skills/` exits RED
- No syntax errors in any test file (only runtime assertion failures)
- Each test file has a comment indicating which requirement it covers
</success_criteria>

<output>
After completion, create `.planning/phases/40-search-traceability-skills-ux/40-01-SUMMARY.md`
</output>
