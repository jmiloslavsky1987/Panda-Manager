---
phase: 40-search-traceability-skills-ux
plan: 06
type: execute
wave: 3
depends_on: [40-02, 40-03, 40-04, 40-05]
files_modified: []
autonomous: false
requirements: [SRCH-01, SRCH-02, ARTF-01, HIST-01, SKLS-01, SKLS-02]

must_haves:
  truths:
    - "Full test suite is GREEN before human verification begins"
    - "Global search bar is visible in the workspace header and returns grouped results"
    - "Decisions tab has working text and date-range filters"
    - "ArtifactEditModal shows Extracted Entities tab with clickable entity links"
    - "Engagement History shows audit log entries with field-level diffs"
    - "Skills tab shows elapsed timer, spinner, and Cancel button for running jobs"
  artifacts:
    - path: "bigpanda-app/components/GlobalSearchBar.tsx"
      provides: "Search bar in header"
    - path: "bigpanda-app/components/DecisionsTableClient.tsx"
      provides: "Filtered decisions list"
    - path: "bigpanda-app/components/ArtifactEditModal.tsx"
      provides: "Two-tab modal with Extracted Entities"
    - path: "bigpanda-app/app/customer/[id]/history/page.tsx"
      provides: "Unified audit + notes feed"
    - path: "bigpanda-app/components/SkillsTabClient.tsx"
      provides: "Progress indicator and cancel button"
    - path: "bigpanda-app/app/api/skills/runs/[runId]/cancel/route.ts"
      provides: "Cancel endpoint"
  key_links: []
---

<objective>
Run the full test suite to confirm all phase-40 tests pass GREEN, then pause for human verification of all 5 success criteria via the running application.

Purpose: Phase gate — no phase ships until both automated tests AND human walkthrough confirm all requirements are met.
Output: Phase 40 verified complete, ready to mark as done in STATE.md.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-search-traceability-skills-ux/40-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite gate</name>
  <files></files>
  <action>
    Run the full test suite. All tests must be GREEN before proceeding to human verification.

    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | tail -40
    ```

    If any Phase 40 tests are RED, diagnose and fix before continuing. Phase 40 test files:
    - tests/search/global-search.test.tsx
    - tests/search/decisions-filter.test.tsx
    - tests/artifacts/extracted-entities.test.tsx
    - tests/history/audit-log-feed.test.tsx
    - tests/skills/job-progress.test.tsx
    - tests/skills/job-cancel.test.ts

    If non-phase-40 tests fail, check if they were already failing before this phase (regression vs pre-existing).

    Also run TypeScript check:
    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx tsc --noEmit 2>&1 | head -30
    ```
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run tests/search/ tests/artifacts/ tests/history/ tests/skills/ 2>&1 | tail -20</automated>
  </verify>
  <done>All phase-40 test files pass GREEN. Full suite exit code 0 (or only pre-existing failures). TypeScript compiles without errors in modified files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all 5 success criteria</name>
  <files></files>
  <action>
    Start the dev server: `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run next-only`
    Open a project workspace: http://localhost:3000/customer/{any-project-id}
    Walk through all 5 verification scenarios described in how-to-verify below.
  </action>
  <what-built>
    Phase 40 delivers 5 UX improvements across the BigPanda Project Assistant workspace:
    1. Global search bar in the workspace header (SRCH-01)
    2. Decisions tab text + date-range filtering (SRCH-02)
    3. Artifact detail modal shows extracted entities with links (ARTF-01)
    4. Engagement History unified feed with audit log entries (HIST-01)
    5. Skills tab job progress indicator + cancel button (SKLS-01 + SKLS-02)
  </what-built>
  <how-to-verify>
    Start the dev server: `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run next-only`

    Open a project workspace: http://localhost:3000/customer/{any-project-id}

    **1. SRCH-01 — Global search bar:**
    - Verify a search input is visible in the workspace header (near the project name/health badge)
    - Type a single character — confirm NO dropdown appears
    - Type 2+ characters matching a known entity (e.g. a risk description keyword) — confirm dropdown appears within ~400ms
    - Confirm results are grouped by section with labels like "Risks (2)" and "Actions (1)"
    - Click a result — confirm navigation to the correct tab (risk to /risks, action to /actions, etc.) and dropdown closes
    - Press Escape — confirm dropdown closes

    **2. SRCH-02 — Decisions tab filtering:**
    - Navigate to the Decisions tab
    - Verify search input and two date inputs appear above the decisions list
    - Type a word that matches one decision's text — confirm the list narrows to only that decision
    - Clear the text, set a "From" date — confirm only decisions after that date are shown
    - Set a "To" date to restrict the range — confirm the list filters correctly
    - Refresh the page — confirm filters are preserved in the URL

    **3. ARTF-01 — Artifact reverse lookup:**
    - Navigate to the Context tab (artifacts list)
    - Click Edit on an artifact that has been used in ingestion (has extracted entities)
    - Confirm the modal now has two tabs: "Details" and "Extracted Entities"
    - Click "Extracted Entities" tab — confirm it shows entities grouped by type (Risks (N), Actions (N), etc.)
    - Click one of the entity links — confirm navigation to the correct entity tab and modal closes
    - For an artifact with no extracted entities — confirm "No entities extracted from this artifact yet."

    **4. HIST-01 — Engagement History unified feed:**
    - Navigate to the History tab
    - Confirm the feed now contains both note entries (existing) and audit log entries
    - Find an audit entry — confirm it has an "Activity" badge (not a source badge)
    - Confirm the audit entry shows a field-level diff like "status: open → mitigated" or "Created"
    - Confirm audit entries include who made the change (actor_id shown if present)
    - Confirm the feed is sorted newest-first (most recent audit action at top)

    **5. SKLS-01 + SKLS-02 — Skills job progress:**
    - Navigate to the Skills tab
    - Click "Run" on a wired skill (e.g. "Morning Briefing")
    - Confirm the skill card immediately shows a spinner + elapsed timer starting at "0m 0s"
    - Confirm a "Cancel" button appears on the card
    - Wait a few seconds — confirm elapsed timer increments (e.g. "0m 3s")
    - Click "Cancel" — confirm the card returns to idle state
    - Check the Recent Runs table — confirm the cancelled run shows status "cancelled"
    - Run the skill again and let it complete — confirm elapsed timer disappears and Recent Runs updates
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a human verification checkpoint</automated>
  </verify>
  <resume-signal>Type "approved" if all 5 criteria pass. Describe any failures in detail so they can be fixed.</resume-signal>
  <done>User confirms all 5 requirements verified in the running application.</done>
</task>

</tasks>

<verification>
Automated gate: `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | tail -10`
Human gate: all 5 verification steps above pass.
</verification>

<success_criteria>
- Full test suite passes GREEN (or only pre-existing failures)
- TypeScript compiles without new errors
- Human approves all 5 walkthrough criteria
- Phase 40 marked complete in STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/40-search-traceability-skills-ux/40-06-SUMMARY.md`

Update STATE.md:
- status: completed
- stopped_at: Phase 40 complete
- last_activity: "2026-04-XX — Completed 40-06: Full test gate + human verification"
- Phase 40 row in Phase Progress table: "Complete (6/6 plans)"
</output>
