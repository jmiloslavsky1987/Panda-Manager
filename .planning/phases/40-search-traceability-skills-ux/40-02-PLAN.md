---
phase: 40-search-traceability-skills-ux
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - bigpanda-app/components/GlobalSearchBar.tsx
  - bigpanda-app/components/ProjectHeader.tsx
autonomous: true
requirements: [SRCH-01]

must_haves:
  truths:
    - "A search input is visible in the workspace header on every project page"
    - "Typing 2+ characters triggers a fetch to /api/search and results appear in a dropdown"
    - "Results are grouped by entity type with section label and count"
    - "Clicking a result navigates to the correct tab and closes the dropdown"
    - "Typing fewer than 2 characters shows no results and closes the dropdown"
  artifacts:
    - path: "bigpanda-app/components/GlobalSearchBar.tsx"
      provides: "Debounced search input with Radix Popover dropdown"
      exports: ["GlobalSearchBar"]
    - path: "bigpanda-app/components/ProjectHeader.tsx"
      provides: "Workspace header with GlobalSearchBar mounted"
  key_links:
    - from: "components/GlobalSearchBar.tsx"
      to: "/api/search"
      via: "fetch with ?q=&account= params after 300ms debounce"
      pattern: "fetch.*api/search.*q=.*account="
    - from: "components/ProjectHeader.tsx"
      to: "components/GlobalSearchBar.tsx"
      via: "import and render <GlobalSearchBar projectId={project.id} />"
---

<objective>
Build GlobalSearchBar — a debounced search input component that queries the existing FTS API and shows grouped results in a Radix Popover dropdown — and wire it into the workspace header.

Purpose: SRCH-01 — users can find any project entity by keyword from the workspace header without navigating away.
Output: GlobalSearchBar component + updated ProjectHeader mounting it.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-search-traceability-skills-ux/40-CONTEXT.md
@.planning/phases/40-search-traceability-skills-ux/40-RESEARCH.md
@.planning/phases/40-search-traceability-skills-ux/40-01-SUMMARY.md

<interfaces>
<!-- ProjectHeader current signature (bigpanda-app/components/ProjectHeader.tsx): -->
interface ProjectHeaderProps {
  project: ProjectWithHealth  // from lib/queries — has .id: number, .customer: string, .health: 'green'|'yellow'|'red'
}
export function ProjectHeader({ project }: ProjectHeaderProps)
// Currently renders: project name + health badge + go-live date
// MODIFY: add <GlobalSearchBar projectId={project.id} /> to the right side of the flex row

<!-- SearchResult interface (lib/queries.ts line 509): -->
export interface SearchResult {
  id: number
  table: string    // 'actions' | 'risks' | 'key_decisions' | 'tasks' | 'stakeholders' | 'artifacts' | ...
  section: string  // 'Actions' | 'Risks' | 'Key Decisions' | 'Tasks' | 'Stakeholders' | ...
  project_id: number | null
  project_name: string
  customer: string
  date: string | null
  title: string
  snippet: string
}

<!-- /api/search usage (app/api/search/route.ts): -->
// GET /api/search?q={query}&account={projectId}
// projectId passed as 'account' param (it does ILIKE match on project.customer, but filters to project)
// Returns: { results: SearchResult[] }
// Note: 'account' param does an ILIKE match on projects.customer — use project.id and pass as numeric string
// Minimum 2 chars enforced by API (returns empty results for <2 chars)

<!-- Radix Popover (already installed @radix-ui/react-popover@1.1.15): -->
import { Popover, PopoverTrigger, PopoverContent } from '@/components/ui/popover'
// PopoverContent renders below trigger, click-outside closes it automatically via Radix

<!-- Tab navigation map for result clicks: -->
// result.table → tab path segment
const TABLE_TO_TAB: Record<string, string> = {
  'actions': 'actions',
  'risks': 'risks',
  'key_decisions': 'decisions',
  'milestones': 'milestones',
  'tasks': 'plan',
  'stakeholders': 'stakeholders',
  'artifacts': 'context',
  'engagement_history': 'history',
}
// Navigation: router.push(`/customer/${projectId}/${TABLE_TO_TAB[result.table] ?? result.table}`)

<!-- Established patterns from Phase 37/39: -->
// - 'use client' directive for Client Components
// - useRouter from next/navigation for navigation
// - No external debounce library — useEffect + setTimeout + clearTimeout cleanup
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GlobalSearchBar component</name>
  <files>bigpanda-app/components/GlobalSearchBar.tsx</files>
  <behavior>
    - Input does not call fetch when query length is less than 2
    - Input calls fetch to /api/search?q={query}&account={projectId} after 300ms debounce when query has 2+ chars
    - Results are grouped by section using groupBySection helper
    - Each section shows label + count in format "Actions (3)"
    - Each result shows title and snippet (truncated)
    - Clicking result calls router.push with correct tab path and closes dropdown (setOpen(false), setQuery(''))
    - useEffect cleanup cancels pending setTimeout to avoid double-fetch on rapid typing
    - Polling stops (clearInterval) and dropdown closes when query drops below 2 chars
  </behavior>
  <action>
    Create `bigpanda-app/components/GlobalSearchBar.tsx` as a 'use client' component.

    Implement the full GlobalSearchBar component exactly as specified in the research code example (40-RESEARCH.md "Global Search API Call with Debounce" section), with these specifics:

    1. State: `query` (string), `results` (SearchResult[]), `loading` (boolean), `open` (boolean)
    2. useEffect on [query, projectId]: if query.length < 2, clear results and setOpen(false), return. Create timer = setTimeout(async () => { setLoading(true); fetch; setResults; setLoading(false); setOpen(true) }, 300). Return () => clearTimeout(timer).
    3. handleResultClick: setOpen(false), setQuery(''), router.push using TABLE_TO_TAB map. Fallback to result.table if not in map.
    4. Popover controlled with open/onOpenChange. PopoverTrigger wraps a styled `<input>` (w-64, placeholder "Search...").
    5. PopoverContent: max-h-96 overflow-y-auto, w-96. Shows: loading state text, no-results text (only when query.length >= 2 and results empty and not loading), grouped results via groupBySection helper.
    6. groupBySection: reduces SearchResult[] to Record<string, SearchResult[]> keyed by result.section.
    7. Section headers: `<h3 className="text-xs font-semibold text-zinc-700 mb-1">{section} ({items.length})</h3>`
    8. Each result item: `<button>` with title (font-medium) + snippet (text-xs text-zinc-500 truncate). data-testid="search-result-{result.table}-{result.id}".
    9. Import SearchResult from '@/lib/queries'.

    Use Tailwind classes consistent with existing components. No new dependencies.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run tests/search/global-search.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All global-search.test.tsx tests pass GREEN. Component renders input, debounces correctly, groups results by section, navigates on click.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GlobalSearchBar into ProjectHeader</name>
  <files>bigpanda-app/components/ProjectHeader.tsx</files>
  <action>
    Read the current ProjectHeader component. It renders a `<div className="flex items-center gap-3">` with project name, health badge, and go-live date.

    Convert ProjectHeader to a 'use client' component by adding `'use client'` directive at the top (it currently has no directive, meaning it's a Server Component — but it needs to be a Client Component to include GlobalSearchBar which uses hooks).

    Wait — check if ProjectHeader is called from layout.tsx as a Server Component. It IS called from `app/customer/[id]/layout.tsx` which is a Server Component. We cannot make ProjectHeader itself a Client Component if it receives `project: ProjectWithHealth` from a Server Component parent.

    Instead: Keep ProjectHeader as a Server Component. Add `<Suspense>` boundary around GlobalSearchBar in the layout OR add GlobalSearchBar as a sibling in the layout header section, not inside ProjectHeader.

    Better approach: Add GlobalSearchBar directly into `app/customer/[id]/layout.tsx` alongside `<ProjectHeader>`, not inside the component. The layout's header section currently renders:
    ```
    <div className="px-6 pt-6 pb-2 border-b border-zinc-200 bg-white">
      {project ? <ProjectHeader project={project} /> : ...}
    </div>
    ```
    Change to:
    ```
    <div className="px-6 pt-6 pb-2 border-b border-zinc-200 bg-white flex items-center justify-between">
      <div>{project ? <ProjectHeader project={project} /> : ...}</div>
      {project && <GlobalSearchBar projectId={project.id} />}
    </div>
    ```

    This keeps ProjectHeader pure (no 'use client'), and GlobalSearchBar is a separate Client Component mounted directly in the layout.

    Files to modify: `bigpanda-app/app/customer/[id]/layout.tsx` (not ProjectHeader.tsx directly — update the files_modified list in your thinking, but you must edit layout.tsx).

    NOTE: The plan frontmatter lists ProjectHeader.tsx — if the correct approach is to edit layout.tsx instead, edit layout.tsx and update the SUMMARY to reflect this. The goal is: search bar visible in the workspace header on every project page.

    Import GlobalSearchBar in layout.tsx: `import { GlobalSearchBar } from '../../../components/GlobalSearchBar'`
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run next-only 2>&1 &amp; sleep 5 &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 2>/dev/null || echo "Build check: run npm test -- --run tests/search/ to verify component exists"</automated>
  </verify>
  <done>GlobalSearchBar is imported and rendered in the workspace layout header. The search input appears alongside the project name on every /customer/[id]/* page. `npm test -- --run tests/search/` passes GREEN.</done>
</task>

</tasks>

<verification>
Run: `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run tests/search/ 2>&1 | tail -20`
Expected: all global-search.test.tsx tests GREEN.

TypeScript check: `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx tsc --noEmit 2>&1 | grep -E "GlobalSearchBar|ProjectHeader|layout" | head -10`
</verification>

<success_criteria>
- GlobalSearchBar.tsx exists as a 'use client' component with debounced search, grouped results dropdown
- Search bar is mounted in the workspace header (layout.tsx) and visible on every project page
- All tests/search/global-search.test.tsx assertions pass GREEN
- No TypeScript errors in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/40-search-traceability-skills-ux/40-02-SUMMARY.md`
</output>
