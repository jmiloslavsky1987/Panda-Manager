---
phase: 42-ingestion-field-coverage
plan: 05
type: execute
wave: 4
depends_on: [42-03, 42-04]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Full test suite is GREEN before human verification begins"
    - "User has visually confirmed new fields appear in the approval card edit form"
    - "User has confirmed unresolved ref notice appears in the ingestion done stage"
    - "Phase 42 is complete and ready for v5.0 milestone sign-off"
  artifacts:
    - path: "bigpanda-app/tests/ingestion/write.test.ts"
      provides: "All Wave 0 cases GREEN"
      contains: "Phase 42"
    - path: "bigpanda-app/tests/ingestion/extraction-job.test.ts"
      provides: "Prompt coverage cases GREEN"
      contains: "Phase 42"
  key_links:
    - from: "human reviewer"
      to: "ingestion approval modal"
      via: "browser upload flow"
      pattern: "manual"
---

<objective>
Gate Plan: run full test suite to confirm all Phase 42 behaviors are GREEN, then guide the user through a brief manual verification of the two behaviors that require a real browser: approval card new fields and unresolved-refs notice.

Purpose: Ensure everything shipped in Phase 42 works end-to-end before v5.0 milestone sign-off.
Output: GREEN test suite + user-approved checkpoint.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-ingestion-field-coverage/42-CONTEXT.md
@.planning/phases/42-ingestion-field-coverage/42-03-SUMMARY.md
@.planning/phases/42-ingestion-field-coverage/42-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite gate</name>
  <files></files>
  <action>
    Run the complete test suite and report results.

    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run
    ```

    Report total pass/fail count. If any ingestion tests fail, diagnose and fix before proceeding to checkpoint.

    Also run TypeScript check:
    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx tsc --noEmit
    ```

    All ingestion tests must be GREEN. Other suite regressions from pre-existing failures are acceptable (note them but do not block).
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/ingestion/ 2>&1 | tail -15</automated>
  </verify>
  <done>All tests/ingestion/ cases pass. TypeScript compilation clean. Pre-existing failures in other test directories noted but not blocking.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — approval card fields and unresolved refs notice</name>
  <files></files>
  <action>Human verification — see how-to-verify steps below. Start dev server with: cd bigpanda-app && npm run next-only</action>
  <verify>
    <automated>MANUAL — human verification required; see how-to-verify steps</automated>
  </verify>
  <done>User confirms all 3 verifications pass (approval card fields, unresolved refs notice, clean ingest)</done>
  <what-built>
    Phase 42 complete — all ingestion pipeline improvements:
    - Risk approved items now include severity (coerced to low/medium/high/critical)
    - Task approved items now include start_date, due, description, priority, milestone_id (auto-linked), workstream_id (auto-linked)
    - Milestone approved items now include owner
    - Action approved items now include notes and type
    - Re-ingestion never overwrites user-edited fields (fill-null-only on all new fields)
    - Unresolved milestone/workstream references append "Milestone ref: [name]" to task description
    - API response includes unresolvedRefs notice when any refs were unresolved
    - IngestionModal done stage shows unresolved refs notice and suppresses auto-close when present
    - Extraction prompt teaches Claude to extract all new fields verbatim
    - Approval card edit form shows new fields for task (6 new), milestone (owner), action (notes, type)
  </what-built>
  <how-to-verify>
    Start the dev server: `cd bigpanda-app && npm run next-only`

    **Verification 1 — Approval card new fields (3 min):**
    1. Open any project → Context Hub tab → upload a sample document (or re-process an existing artifact)
    2. After extraction completes, open the approval modal
    3. Click to expand a Task entity card → confirm: description, start_date, due_date, milestone_name, workstream_name, priority fields appear in the edit form
    4. Click to expand a Milestone entity card → confirm: owner field appears
    5. Click to expand an Action entity card → confirm: notes, type fields appear
    6. Click to expand a Risk entity card → confirm: severity field still appears (regression check)

    **Verification 2 — Unresolved ref notice (2 min):**
    1. Approve a document that contains task entities with milestone or workstream name references that do NOT match existing DB records
    2. After approval completes → confirm: done stage shows the unresolved refs notice message with count and "link them manually via the Plan tab" instruction
    3. Confirm: modal does NOT auto-close while notice is visible

    **Verification 3 — Clean ingest (1 min):**
    1. Approve a document with tasks that DO have matching milestone/workstream names in the DB (or no milestone/workstream references)
    2. Confirm: done stage shows no notice (or modal auto-closes normally)

    **Pass criteria:** All 3 verifications pass without errors.
  </how-to-verify>
  <resume-signal>Type "approved" when all 3 verifications pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full test suite green for ingestion directory.
Human-verified: new fields in approval cards, unresolved refs notice, and clean ingest flow.
</verification>

<success_criteria>
- All tests/ingestion/ cases GREEN
- TypeScript compilation clean
- User confirms new fields visible in approval card edit form for task, milestone, action entity types
- User confirms unresolved ref notice appears in IngestionModal done stage when applicable
- User confirms clean ingest (no notice) when all refs resolve
- Phase 42 ready for v5.0 milestone close
</success_criteria>

<output>
After completion, create `.planning/phases/42-ingestion-field-coverage/42-05-SUMMARY.md`
</output>
