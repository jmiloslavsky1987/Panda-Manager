---
phase: 05.2-time-tracking
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/phase5.2.spec.ts
autonomous: true
requirements:
  - TIME-01
  - TIME-02
  - TIME-03

must_haves:
  truths:
    - "E2E spec file exists at tests/e2e/phase5.2.spec.ts with 3 stub tests"
    - "All 3 stubs are visibly RED (fail without app running) to enforce Nyquist rule"
    - "Each stub includes the requirement ID in its test name for --grep targeting"
  artifacts:
    - path: "tests/e2e/phase5.2.spec.ts"
      provides: "Playwright E2E stubs for TIME-01, TIME-02, TIME-03"
      contains: "expect(false, 'stub"
  key_links:
    - from: "tests/e2e/phase5.2.spec.ts"
      to: "playwright.config.ts"
      via: "npx playwright test"
      pattern: "phase5\\.2\\.spec\\.ts"
---

<objective>
Create the Wave 0 E2E test stub file for Phase 5.2 so every subsequent plan has a failing-by-default automated verify target.

Purpose: Nyquist rule enforcement — every implementation task in Waves 1-3 verifies against a named test. Stubs are visibly RED before implementation begins.
Output: `tests/e2e/phase5.2.spec.ts` with 3 RED stub tests (TIME-01, TIME-02, TIME-03).
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-time-tracking/05.2-CONTEXT.md
@.planning/phases/05.2-time-tracking/05.2-VALIDATION.md

<interfaces>
<!-- Stub pattern established in Phase 5.1 — copy exactly -->
<!-- From tests/e2e/phase5.1.spec.ts -->
import { test, expect } from '@playwright/test';

test.describe('Phase 5.1: Onboarding Dashboard', () => {
  test('OVER-01: ...', async ({ page }) => {
    // Wave 0 stubs: expect(false, 'stub: OVER-01 not yet implemented').toBe(true) as FIRST LINE
    expect(false, 'stub: OVER-01 not yet implemented').toBe(true);
  });
});

<!-- Decision from STATE.md [Phase 02-01] and [Phase 05.1-01]: -->
<!-- Wave 0 stub assertion placed as FIRST line in each test — visibly RED without server running -->
<!-- Requirement IDs in test names — implementation plans use --grep to target specific tests -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase5.2.spec.ts E2E stubs</name>
  <files>tests/e2e/phase5.2.spec.ts</files>
  <action>
Create `tests/e2e/phase5.2.spec.ts` with exactly 3 stub tests. Pattern matches phase5.1.spec.ts exactly.

Key requirements:
- `expect(false, 'stub: TIME-XX not yet implemented').toBe(true)` MUST be the FIRST LINE of each test body — this makes the test fail before any navigation, ensuring RED status even without a running server
- Each test name includes the requirement ID for `--grep` targeting
- Tests are wrapped in `test.describe('Phase 5.2: Time Tracking', () => { ... })`

The three stubs:

**TIME-01:** "TIME-01: Time tab renders with entry table and total hours header"
- URL: `/customer/1/time`
- After stub line removed: will assert `[data-testid="time-tab"]` visible and `[data-testid="total-hours"]` visible

**TIME-02:** "TIME-02: Log Time button opens form; entry appears in table; edit and delete work"
- URL: `/customer/1/time`
- After stub line removed: will use assert-if-present pattern for DB-dependent checks

**TIME-03:** "TIME-03: Export CSV button triggers download with correct filename"
- URL: `/customer/1/time`
- After stub line removed: will verify `[data-testid="export-csv"]` button is present

Write the stub file now — do NOT write the implementation assertions yet. The stub line `expect(false, 'stub: TIME-XX not yet implemented').toBe(true)` is the entire test body for all 3 tests.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase5.2.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
`tests/e2e/phase5.2.spec.ts` exists. Running `npx playwright test tests/e2e/phase5.2.spec.ts` shows 3 FAILED tests (stubs are RED). Zero passing tests is correct at Wave 0.
  </done>
</task>

</tasks>

<verification>
Run: `npx playwright test tests/e2e/phase5.2.spec.ts`
Expected: 3 failed, 0 passed. All failures show "stub: TIME-XX not yet implemented" message.
</verification>

<success_criteria>
- `tests/e2e/phase5.2.spec.ts` file exists
- `npx playwright test tests/e2e/phase5.2.spec.ts` exits non-zero with 3 failures
- Each failure message contains the stub text (not a runtime error or import error)
- `npx playwright test tests/e2e/phase5.2.spec.ts --grep "TIME-01"` runs 1 test and fails
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-time-tracking/05.2-01-SUMMARY.md`
</output>
