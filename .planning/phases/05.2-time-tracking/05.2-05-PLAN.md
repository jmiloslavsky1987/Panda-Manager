---
phase: 05.2-time-tracking
plan: "05"
type: execute
wave: 3
depends_on:
  - "05.2-03"
  - "05.2-04"
files_modified:
  - tests/e2e/phase5.2.spec.ts
autonomous: false
requirements:
  - TIME-01
  - TIME-02
  - TIME-03

must_haves:
  truths:
    - "All 3 E2E tests pass (3/3 green)"
    - "Time tab is visible in workspace navigation for any customer"
    - "Table renders with total hours header"
    - "Log Time inline form is accessible"
    - "Export CSV button is present and triggerable"
    - "User has confirmed the full time tracking flow end-to-end"
  artifacts:
    - path: "tests/e2e/phase5.2.spec.ts"
      provides: "Active (non-stub) E2E assertions for TIME-01, TIME-02, TIME-03"
      contains: "data-testid"
  key_links:
    - from: "tests/e2e/phase5.2.spec.ts"
      to: "bigpanda-app/components/TimeTab.tsx"
      via: "Playwright browser navigation to /customer/1/time"
      pattern: "goto.*customer.*time"
---

<objective>
Activate the E2E stubs with real assertions and run the full suite green, then get human verification of the complete time tracking flow.

Purpose: Phase gate — confirms the entire 5.2 feature works end-to-end before moving to Phase 6.
Output: 3/3 E2E tests green; human-approved time tracking feature.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05.2-time-tracking/05.2-CONTEXT.md
@.planning/phases/05.2-time-tracking/05.2-VALIDATION.md
@.planning/phases/05.2-time-tracking/05.2-03-SUMMARY.md
@.planning/phases/05.2-time-tracking/05.2-04-SUMMARY.md

<interfaces>
<!-- data-testid attributes in TimeTab.tsx (from Plan 04) -->
data-testid="time-tab"           — outer wrapper
data-testid="total-hours"        — total hours display ("X.XX hrs total")
data-testid="export-csv"         — export CSV button
data-testid="log-time-btn"       — toggle add form
data-testid="add-time-form"      — inline add form (visible after log-time-btn click)
data-testid="from-date"          — date range filter start
data-testid="to-date"            — date range filter end
data-testid="time-entry-row"     — each table row
data-testid="empty-state"        — empty state message

<!-- E2E assert-if-present pattern (from STATE.md Phase 03-09, 04-05, 05.1-06) -->
// Structural assertions always run; DB-dependent assertions guarded by count check
const rowCount = await page.locator('[data-testid="time-entry-row"]').count()
if (rowCount > 0) {
  // DB-dependent assertions here
}

<!-- Playwright download test pattern for FILE-03 CSV export -->
// Download assertions: check button is present and clickable (structural)
// Actual file content check is manual (headless download dialog not testable in CI)
await expect(page.locator('[data-testid="export-csv"]')).toBeVisible()
await expect(page.locator('[data-testid="export-csv"]')).toBeEnabled()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate E2E stubs with real assertions</name>
  <files>tests/e2e/phase5.2.spec.ts</files>
  <action>
Replace the stub `expect(false, 'stub: TIME-XX not yet implemented').toBe(true)` lines in each test with real Playwright assertions. Keep the assert-if-present pattern for DB-dependent checks.

**TIME-01 test** — structural assertions:
- `await page.goto('/customer/1/time')`
- `await expect(page.locator('[data-testid="time-tab"]')).toBeVisible()`
- `await expect(page.locator('[data-testid="total-hours"]')).toBeVisible()`
- `await expect(page.locator('[data-testid="log-time-btn"]')).toBeVisible()`
- `await expect(page.locator('[data-testid="export-csv"]')).toBeVisible()`
- Assert-if-present: `const rowCount = await page.locator('[data-testid="time-entry-row"]').count()` — if > 0, assert first row visible
- Assert-if-present: `await expect(page.locator('[data-testid="empty-state"]').or(page.locator('[data-testid="time-entry-row"]'))).toBeVisible()` — either empty state or rows must be present

**TIME-02 test** — assert-if-present for CRUD:
- Navigate to `/customer/1/time`
- Structural: `await expect(page.locator('[data-testid="log-time-btn"]')).toBeVisible()`
- Click "Log Time" button: `await page.click('[data-testid="log-time-btn"]')`
- Structural: `await expect(page.locator('[data-testid="add-time-form"]')).toBeVisible()`
- Assert-if-present for edit: only test edit flow if `time-entry-row` count > 0
  - If rows exist: click first edit button (role="button" near pencil icon in row), verify modal or inline edit appears

**TIME-03 test** — structural export check:
- Navigate to `/customer/1/time`
- `await expect(page.locator('[data-testid="export-csv"]')).toBeVisible()`
- `await expect(page.locator('[data-testid="export-csv"]')).toBeEnabled()`
- Trigger the click and verify no JS error thrown (wrap in try/catch is NOT needed — just click and verify page still shows time-tab):
  ```typescript
  await page.click('[data-testid="export-csv"]')
  await expect(page.locator('[data-testid="time-tab"]')).toBeVisible()
  ```

Run the tests before declaring done: `npx playwright test tests/e2e/phase5.2.spec.ts`

Fix any assertion failures (missing testids, wrong selectors) before moving to checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase5.2.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
`npx playwright test tests/e2e/phase5.2.spec.ts` exits 0 with 3 passed, 0 failed. No stub assertion lines remain in the file.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete time tracking flow</name>
  <action>Pause for human verification of the complete Phase 5.2 feature. See how-to-verify steps below.</action>
  <what-built>
Complete Phase 5.2: Time Tracking feature —
- 12th "Time" workspace tab in navigation for every customer
- Time log table with date/hours/description columns and total hours header
- Inline "Log Time" form to add entries
- Edit (pencil icon → modal) and delete (trash icon → immediate) per row
- Date range filter updating the visible entries and total
- Client-side CSV export downloading as time-log-{project}-{date}.csv
- Manual migration SQL at bigpanda-app/db/migrations/0006_time_tracking.sql
  </what-built>
  <how-to-verify>
1. Navigate to any customer workspace (e.g. http://localhost:3000/customer/1)
2. Confirm "Time" appears as the 12th tab in the nav bar (after Skills)
3. Click the Time tab — confirm you land on /customer/1/time
4. Confirm the page shows: total hours header ("0.00 hrs total" or actual total), "Log Time" button, "Export CSV" button, and either the empty state message or a table

**If DB is running (PostgreSQL with migration 0006 applied):**
5. Click "Log Time" — confirm the inline form appears with Date (default today), Hours, Description fields
6. Fill in: Date = today, Hours = 1.5, Description = "Test entry" — submit
7. Confirm the new entry appears in the table immediately
8. Confirm the total hours updates to "1.50 hrs total"
9. Click the pencil/edit icon on the row — confirm the edit modal opens with pre-filled values
10. Change hours to 2.0 — save — confirm row updates and total becomes "2.00 hrs total"
11. Set date range filter From/To to exclude the entry — confirm table shows empty state and total = "0.00 hrs total"
12. Clear date filter — confirm entry returns
13. Click "Export CSV" — confirm file downloads as "time-log-{project}-{today's date}.csv"
14. Open the CSV — confirm columns: date, hours, description, project name

**If DB is not running:**
5. Confirm the Time tab loads without errors and shows the empty state message
6. Click "Export CSV" on empty table — should either download an empty CSV (header only) or be disabled
  </how-to-verify>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase5.2.spec.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Human types "approved" confirming all verification steps pass.</done>
  <resume-signal>Type "approved" when verification passes, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
`npx playwright test tests/e2e/phase5.2.spec.ts` — 3 passed, 0 failed.
</verification>

<success_criteria>
- 3/3 E2E tests green
- Human verified: Time tab visible in nav, CRUD works, CSV export downloads correctly
- No console errors in browser when using the Time tab
- Phase 5.2 COMPLETE
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-time-tracking/05.2-05-SUMMARY.md`
</output>
