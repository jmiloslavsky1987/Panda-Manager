---
phase: 28-interactive-visuals
plan: "05"
type: execute
wave: 3
depends_on:
  - "28-03"
  - "28-04"
files_modified: []
autonomous: false
requirements:
  - VIS-01
  - VIS-02

must_haves:
  truths:
    - "All 10 visual tests (tests/visuals/) pass GREEN"
    - "Teams tab shows interactive engagement graph above existing sections"
    - "Clicking a team node opens the NodeDetailDrawer; clicking X closes it"
    - "Architecture tab Current & Future State shows hub-and-spoke graph with BigPanda center"
    - "Clicking an integration node opens the IntegrationDetailDrawer; clicking X closes it"
    - "Browser console shows zero hydration warnings after next build && next start"
    - "Before BigPanda tab remains as static narrative (no regression)"
  artifacts:
    - path: "bigpanda-app/tests/visuals/"
      provides: "10 GREEN tests covering VIS-01 and VIS-02"
  key_links:
    - from: "browser console"
      to: "production build"
      via: "next build && next start hydration check"
      pattern: "zero.*Hydration failed"
---

<objective>
Verify the complete Phase 28 implementation: run all tests, perform a production build, and do an in-browser walkthrough confirming both graphs work without hydration errors.

Purpose: React Flow hydration errors do NOT appear in development mode (`npm run next-only`) — they only surface in a production build. This checkpoint is mandatory per the research findings and VALIDATION.md.
Output: Phase 28 sign-off: all tests GREEN + zero browser hydration warnings.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-interactive-visuals/28-CONTEXT.md
@.planning/phases/28-interactive-visuals/28-VALIDATION.md
@.planning/phases/28-interactive-visuals/28-03-SUMMARY.md
@.planning/phases/28-interactive-visuals/28-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite green pass</name>
  <files></files>
  <action>
    Run the full test suite from the bigpanda-app directory. All 10 visual tests must be GREEN. No regressions in any existing tests.

    Run: `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run`

    If any visual test is FAILING, investigate and fix the underlying component or test before proceeding. Do NOT skip or comment out failing tests.

    Expected GREEN count in tests/visuals/: engagement-graph (3), node-detail-drawer (2), arch-graph (3), dagre-layout (2) = 10 total.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>Full test suite GREEN; tests/visuals/ shows 10/10 passing; no regressions in any other test file</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production build + in-browser hydration verification</name>
  <action>Run `npm run build && npm run start` from bigpanda-app. Open browser and verify both graphs render with zero hydration console errors.</action>
  <what-built>
    Two interactive React Flow graphs:
    1. Teams tab — Engagement Map prepended above existing sections; team nodes clickable with NodeDetailDrawer; empty state message when no data
    2. Architecture tab Current and Future State — hub-and-spoke with BigPanda center + integration nodes; integration nodes clickable with IntegrationDetailDrawer; Before BigPanda tab unchanged
    Both components use dynamic import with ssr: false — production build required to verify no hydration errors.
  </what-built>
  <how-to-verify>
    Step 1: Build and start production server
    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app
    npm run build && npm run start
    ```
    Wait for "Ready" output (typically 10-15 seconds).

    Step 2: Open browser developer tools (Cmd+Option+I console tab). Filter for "Hydration" warnings.

    Step 3: Navigate to any project then Teams tab
    - Verify: React Flow engagement graph appears above Business Outcomes section
    - Verify: Nodes are visible (or empty state message if no team data)
    - If nodes exist: Click a node — drawer opens on the right showing team/workflow details
    - Click the X button — drawer closes
    - Click same node again — drawer closes
    - Verify: Business Outcomes, E2E Workflows, Teams Engagement, Focus Areas sections still visible below graph

    Step 4: Navigate to any project then Architecture tab then "Current and Future State" tab
    - Verify: React Flow hub-and-spoke graph appears with BigPanda as center node (dark card)
    - Verify: Integration nodes radiate outward from BigPanda (or empty state message if no integrations)
    - If integration nodes exist: Click one — IntegrationDetailDrawer opens on the right showing status, track, phase, notes
    - Click the X button — drawer closes
    - Verify: TeamOnboardingTable is still visible below the graph
    - Verify: "+ ADR Integration" and "+ Biggy Integration" buttons are present and open the edit modal

    Step 5: Navigate to Architecture tab then "Before BigPanda" tab
    - Verify: Tab content is unchanged (static narrative, no React Flow graph)

    Step 6: Check browser console
    - Pass: ZERO messages containing "Hydration failed" or "Expected server HTML to contain"
    - Pass: No console errors related to @xyflow/react
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass. Or describe any issues found (e.g., "hydration warning in console", "drawer doesn't close", "Before BigPanda tab broken")</resume-signal>
  <verify>
    <automated>MISSING — hydration errors are runtime browser-only; requires manual browser console inspection after `npm run build && npm run start`</automated>
  </verify>
  <done>Human confirms: both graphs interactive, zero hydration warnings in production build, Before BigPanda tab unchanged</done>
</task>

</tasks>

<verification>
- `npm test -- --run` exits 0, all 10 visual tests GREEN
- Production build (`npm run build`) succeeds with no errors
- In-browser: Teams engagement graph renders and click-to-drawer works
- In-browser: Architecture hub-and-spoke renders and click-to-drawer works
- In-browser: Zero hydration warnings in browser console
- In-browser: Before BigPanda tab unchanged
</verification>

<success_criteria>
Phase 28 complete: VIS-01 and VIS-02 verified in production build. All 10 automated tests GREEN. Zero hydration errors. Both graphs interactive with working drawers. Human sign-off received.
</success_criteria>

<output>
After completion, create `.planning/phases/28-interactive-visuals/28-05-SUMMARY.md`
</output>
