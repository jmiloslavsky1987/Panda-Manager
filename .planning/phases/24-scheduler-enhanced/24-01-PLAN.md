---
phase: 24-scheduler-enhanced
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/tests/scheduler/jobs-crud.test.ts
  - bigpanda-app/tests/scheduler/frequency-to-cron.test.ts
  - bigpanda-app/tests/scheduler/trigger.test.ts
  - bigpanda-app/tests/scheduler/notifications.test.ts
  - bigpanda-app/tests/scheduler/run-history.test.ts
  - bigpanda-app/tests/scheduler/sidebar.test.ts
  - bigpanda-app/tests/scheduler/wizard-step.test.ts
  - bigpanda-app/tests/scheduler/skill-list.test.ts
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05, SCHED-06, SCHED-07, SCHED-08, SCHED-09, SCHED-10, SCHED-11, SCHED-12]

must_haves:
  truths:
    - "All 8 test scaffold files exist and are importable by Vitest"
    - "Every test is in FAILING state (describes the behavior but implementation does not exist yet)"
    - "npx vitest run tests/scheduler/ completes without import errors"
  artifacts:
    - path: "bigpanda-app/tests/scheduler/jobs-crud.test.ts"
      provides: "Stubs for SCHED-01, SCHED-03, SCHED-04, SCHED-05, SCHED-07"
    - path: "bigpanda-app/tests/scheduler/frequency-to-cron.test.ts"
      provides: "Stubs for SCHED-02 — frequencyToCron utility"
    - path: "bigpanda-app/tests/scheduler/trigger.test.ts"
      provides: "Stubs for SCHED-06 — manual trigger endpoint"
    - path: "bigpanda-app/tests/scheduler/notifications.test.ts"
      provides: "Stubs for SCHED-08 — failure notifications"
    - path: "bigpanda-app/tests/scheduler/run-history.test.ts"
      provides: "Stubs for SCHED-09 — run history append logic"
    - path: "bigpanda-app/tests/scheduler/sidebar.test.ts"
      provides: "Stubs for SCHED-10 — sidebar link presence"
    - path: "bigpanda-app/tests/scheduler/wizard-step.test.ts"
      provides: "Stubs for SCHED-11 — wizard step logic"
    - path: "bigpanda-app/tests/scheduler/skill-list.test.ts"
      provides: "Stubs for SCHED-12 — all 12 skills present in SKILL_LIST"
  key_links:
    - from: "bigpanda-app/tests/scheduler/"
      to: "bigpanda-app/app/api/jobs/route.ts"
      via: "import mock"
      pattern: "vi.mock.*api/jobs"
---

<objective>
Create the Wave 0 test scaffold — 8 stub test files for the scheduler feature. All tests must exist in failing state before any implementation code is written.

Purpose: TDD Nyquist compliance. Every subsequent implementation task has a pre-existing automated verify command that runs in < 15 seconds.
Output: 8 test files in bigpanda-app/tests/scheduler/ with failing stubs.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-scheduler-enhanced/24-VALIDATION.md

@bigpanda-app/vitest.config.ts
@bigpanda-app/tests/scheduler-map.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scheduler test scaffold (8 files)</name>
  <files>
    bigpanda-app/tests/scheduler/jobs-crud.test.ts,
    bigpanda-app/tests/scheduler/frequency-to-cron.test.ts,
    bigpanda-app/tests/scheduler/trigger.test.ts,
    bigpanda-app/tests/scheduler/notifications.test.ts,
    bigpanda-app/tests/scheduler/run-history.test.ts,
    bigpanda-app/tests/scheduler/sidebar.test.ts,
    bigpanda-app/tests/scheduler/wizard-step.test.ts,
    bigpanda-app/tests/scheduler/skill-list.test.ts
  </files>
  <behavior>
    jobs-crud.test.ts:
    - "POST /api/jobs returns 201 with a created job row containing id, name, skill_name, cron_expression"
    - "POST /api/jobs with missing name returns 400"
    - "POST /api/jobs stores timezone from request body"
    - "POST /api/jobs stores skill_params_json from request body"
    - "PATCH /api/jobs/[id] with {enabled: false} sets enabled=false"
    - "GET /api/jobs returns array; enabled jobs appear before disabled jobs"
    - "GET /api/jobs returns last_run_at, last_run_outcome, next_run fields"

    frequency-to-cron.test.ts:
    - "frequencyToCron('daily', {hour:9, minute:0}) returns '0 0 9 * * *'"
    - "frequencyToCron('weekly', {hour:9, minute:30, dayOfWeek:1}) returns '0 30 9 * * 1'"
    - "frequencyToCron('monthly', {hour:8, minute:0, dayOfMonth:1}) returns '0 0 8 1 * *'"
    - "frequencyToCron('custom', {cron:'0 9 * * MON'}) returns '0 9 * * MON' unchanged"
    - "frequencyToCron('once', ...) returns null"
    - "frequencyToCron('biweekly', ...) returns null (handled separately via BullMQ every:ms)"

    trigger.test.ts:
    - "POST /api/jobs/trigger with {jobId, skillName} enqueues the job and returns 200"
    - "POST /api/jobs/trigger with unknown skillName returns 400"

    notifications.test.ts:
    - "insertSchedulerFailureNotification(jobId, jobName, errorMsg) inserts row into app_notifications with type='scheduler_failure'"
    - "Notification title includes jobName"
    - "Notification body is truncated to 500 chars"

    run-history.test.ts:
    - "appendRunHistoryEntry(jobId, entry) calls db.update on scheduled_jobs"
    - "appendRunHistoryEntry keeps only the last 10 entries when array exceeds 10"
    - "entry shape: {timestamp, outcome, duration_ms, artifact_link?}"

    sidebar.test.ts:
    - "Sidebar renders a link with href='/scheduler'"
    - "Sidebar link has data-testid='sidebar-scheduler-link'"

    wizard-step.test.ts:
    - "getWizardSteps(skillName) returns ['skill','schedule','params'] for 'discovery-scan'"
    - "getWizardSteps('morning-briefing') returns ['skill','schedule'] (no params step)"
    - "SKILLS_WITH_PARAMS contains 'discovery-scan', 'customer-project-tracker', 'weekly-customer-status', 'context-updater'"
    - "SKILLS_WITH_PARAMS does not contain 'morning-briefing', 'meeting-summary'"

    skill-list.test.ts:
    - "SKILL_LIST has exactly 12 entries"
    - "SKILL_LIST contains all 12: morning-briefing, customer-project-tracker, weekly-customer-status, elt-external, elt-internal, biggy-weekly-briefing, context-updater, meeting-summary, workflow-diagram, team-engagement-map, discovery-scan, timesheet-reminder"
    - "Each SKILL_LIST entry has: id, label, description, hasParams fields"
  </behavior>
  <action>
    Create the tests/scheduler/ directory if it does not exist. Write 8 stub test files.

    Each stub file:
    - Imports the unit under test from the path where it WILL live (not yet existing — that is intentional; tests fail with import error first, then with assertion failures)
    - Uses `describe` + `it` blocks with explicit `expect()` assertions matching the behavior list above
    - Uses `vi.mock` for DB and BullMQ Queue to avoid real I/O in unit tests

    Key import paths stubs should expect (write these paths exactly — implementation tasks will create them here):
    - `frequencyToCron` → `../../lib/scheduler-utils`
    - `insertSchedulerFailureNotification`, `appendRunHistoryEntry` → `../../lib/scheduler-notifications`
    - `SKILL_LIST`, `SKILLS_WITH_PARAMS`, `getWizardSteps` → `../../lib/scheduler-skills`
    - API routes → mock via `vi.mock` — test the handler function directly if exported, or use fetch mocking

    For sidebar.test.ts: mock the RSC Sidebar component — test only that the rendered output contains the scheduler href. Use `@testing-library/react` consistent with existing tests in tests/wizard/ for reference.

    Do NOT create implementation files in this task. Tests must FAIL (import errors or assertion failures count as failing — both are valid RED state).

    Run `npx vitest run tests/scheduler/ --reporter=verbose` after creating all 8 files. Confirm test runner starts and exits with failures (not import/syntax panics that prevent the suite from running at all). Fix any syntax errors until the suite runs and shows failures.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run tests/scheduler/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>8 test files exist; npx vitest run tests/scheduler/ exits with test failures (not syntax errors); test runner processes all 8 files and reports them as failing</done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/scheduler/ --reporter=verbose` from bigpanda-app/ — all 8 test files are processed, test runner exits with failures (RED state confirmed).
</verification>

<success_criteria>
- All 8 test scaffold files exist under bigpanda-app/tests/scheduler/
- Each file has at least the stub tests matching the behavior map in 24-VALIDATION.md
- Suite runs without import/syntax panics (failures are expected and required)
- Wave 1 and Wave 2 plans can now reference automated verify commands that point to these files
</success_criteria>

<output>
After completion, create `.planning/phases/24-scheduler-enhanced/24-01-SUMMARY.md`
</output>
