---
phase: 24-scheduler-enhanced
plan: 03
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - bigpanda-app/components/Sidebar.tsx
  - bigpanda-app/app/scheduler/page.tsx
  - bigpanda-app/components/SchedulerJobTable.tsx
  - bigpanda-app/components/SchedulerJobRow.tsx
autonomous: true
requirements: [SCHED-05, SCHED-06, SCHED-07, SCHED-08, SCHED-09, SCHED-10]

must_haves:
  truths:
    - "A Scheduler link appears in the sidebar alongside Dashboard, Knowledge Base, Settings"
    - "A notification badge count appears on the Scheduler sidebar link when unread scheduler_failure notifications exist"
    - "Navigating to /scheduler renders a page titled 'Scheduler' with a '+ Create Job' button in the header"
    - "The table renders one row per job: Name, Skill, Schedule, Next Run, Last Outcome, Enabled toggle, Trigger button"
    - "Enabled jobs appear before disabled jobs"
    - "Clicking a row expands it inline — shows Edit, Disable/Enable, Delete controls + last 10 run history entries"
  artifacts:
    - path: "bigpanda-app/components/Sidebar.tsx"
      provides: "Scheduler link with href=/scheduler, data-testid=sidebar-scheduler-link, and NotificationBadge count for unread scheduler_failure notifications"
    - path: "bigpanda-app/app/scheduler/page.tsx"
      provides: "RSC page; fetches jobs server-side; renders SchedulerJobTable"
    - path: "bigpanda-app/components/SchedulerJobTable.tsx"
      provides: "Client component — table with column headers, job rows, Create Job button placeholder"
    - path: "bigpanda-app/components/SchedulerJobRow.tsx"
      provides: "Individual row; inline-expand panel; enable/disable toggle; trigger button; run history list"
  key_links:
    - from: "bigpanda-app/app/scheduler/page.tsx"
      to: "GET /api/jobs"
      via: "fetch in RSC"
      pattern: "fetch.*api/jobs"
    - from: "bigpanda-app/components/Sidebar.tsx"
      to: "app_notifications (type=scheduler_failure, read=false)"
      via: "NotificationBadge count query"
      pattern: "NotificationBadge.*scheduler_failure"
    - from: "bigpanda-app/components/SchedulerJobRow.tsx"
      to: "PATCH /api/jobs/[id]"
      via: "fetch on enable/disable toggle"
      pattern: "fetch.*api/jobs.*PATCH"
    - from: "bigpanda-app/components/SchedulerJobRow.tsx"
      to: "POST /api/jobs/trigger"
      via: "fetch on Trigger button click"
      pattern: "fetch.*api/jobs/trigger"
---

<objective>
Build the Scheduler page UI: sidebar link with failure notification badge, RSC page scaffold, job table with inline-expand rows. The Create Job wizard button renders as a placeholder (wired in Plan 04). All table interactions (enable/disable, trigger, run history display) are fully functional.

Purpose: Delivers the core read/control surface of the scheduler feature — users can view, enable/disable, trigger, and inspect run history for all jobs. The notification badge on the sidebar link surfaces job failures passively.
Output: 4 files; sidebar-scheduler-link test and parts of jobs-crud test pass GREEN.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-scheduler-enhanced/24-CONTEXT.md
@.planning/phases/24-scheduler-enhanced/24-RESEARCH.md

@bigpanda-app/components/Sidebar.tsx
@bigpanda-app/components/ProjectWizard.tsx

<interfaces>
<!-- Locked decisions from CONTEXT.md — implement exactly as specified. -->

Table columns (locked): Name | Skill | Schedule | Next Run | Last Outcome (✓/✗) | Enabled toggle | Trigger button

Row expand (locked): [Edit] [Disable/Enable] [Delete] controls + run history log (last 10 entries, scrollable)

Sidebar (locked): standalone link alongside Dashboard, Knowledge Base, Settings — add data-testid="sidebar-scheduler-link"

Sidebar badge (locked): notification badge count appears on the Scheduler link for unread scheduler_failure notifications — follow the NotificationBadge.tsx pattern already used elsewhere in Sidebar.tsx for other badge counts

Page header (locked): "+ Create Job" button top-right — renders as a button that will open the CreateJobWizard (not yet wired; passes `onOpen` prop placeholder for Plan 04)

<!-- Job shape from GET /api/jobs (Plan 02 output): -->
```typescript
interface ScheduledJob {
  id: string
  name: string
  skill_name: string
  cron_expression: string | null
  enabled: boolean
  timezone: string | null
  skill_params_json: Record<string, unknown>
  last_run_at: string | null
  last_run_outcome: 'success' | 'failure' | 'partial' | null
  run_history_json: RunHistoryEntry[]
  next_run: string | null   // derived in API
}

interface RunHistoryEntry {
  timestamp: string
  outcome: 'success' | 'failure' | 'partial'
  duration_ms: number
  artifact_link: string | null
  error?: string
}
```

<!-- Discretion areas (from CONTEXT.md) — make your own choices: -->
- Exact column widths and table styling
- Enabled toggle design (switch vs checkbox)
- Trigger button loading state (spinner or toast)
- Error text truncation in run history entries
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar Scheduler link + notification badge</name>
  <files>bigpanda-app/components/Sidebar.tsx</files>
  <action>
    Read the existing Sidebar.tsx. Add a new navigation link for `/scheduler` following the exact same pattern as the existing Dashboard, Knowledge Base, and Settings links.

    Requirements:
    - `href="/scheduler"`
    - `data-testid="sidebar-scheduler-link"`
    - Label: "Scheduler"
    - Use a CalendarClock icon from lucide-react (consistent with other sidebar icons)
    - Position: between Settings and Knowledge Base (or at the bottom of the nav list — match the visual hierarchy of the sidebar)
    - Active state styling: use the same active/inactive class pattern as existing links

    Notification badge (locked decision from CONTEXT.md — "badge appears on the Scheduler sidebar link"):
    - Render a `<NotificationBadge>` count on the Scheduler link, showing the number of unread `scheduler_failure` notifications from `app_notifications`
    - Query the count using the same pattern as any existing notification badge rendering in Sidebar.tsx — look at how NotificationBadge.tsx is already used in the file and follow that exact import/usage pattern
    - Filter: `type = 'scheduler_failure'` AND `read = false`
    - Badge renders inline alongside the "Scheduler" label, consistent with other badge placements in the sidebar
    - If count is 0, the badge is hidden (follow NotificationBadge.tsx's existing zero-count behavior — do not override it)

    Do NOT change any other part of Sidebar.tsx.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run tests/scheduler/sidebar.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>sidebar-scheduler-link test passes GREEN; Sidebar.tsx has new /scheduler link with data-testid and NotificationBadge showing unread scheduler_failure count</done>
</task>

<task type="auto">
  <name>Task 2: /scheduler RSC page + SchedulerJobTable + SchedulerJobRow</name>
  <files>
    bigpanda-app/app/scheduler/page.tsx,
    bigpanda-app/components/SchedulerJobTable.tsx,
    bigpanda-app/components/SchedulerJobRow.tsx
  </files>
  <action>
    Create three files.

    **bigpanda-app/app/scheduler/page.tsx** (RSC):
    - Fetch jobs server-side: `const res = await fetch('http://localhost:3000/api/jobs', { cache: 'no-store' })`
    - Handle empty array and fetch errors gracefully (show empty state: "No scheduled jobs yet.")
    - Render page with `<h1>Scheduler</h1>` and `<SchedulerJobTable initialJobs={jobs} />`
    - Use `'use server'` is NOT needed — this is an RSC (no directive needed in App Router)

    **bigpanda-app/components/SchedulerJobTable.tsx** (Client component — `'use client'`):
    - Props: `{ initialJobs: ScheduledJob[] }`
    - State: `jobs` (updated optimistically), `expandedId` (string|null — which row is expanded)
    - Header: `<h2>` or page sub-header with `<button>+ Create Job</button>` — button fires `onCreateJob` prop (stub; will be wired in Plan 04); add `data-testid="create-job-button"`
    - Table with columns: Name, Skill, Schedule, Next Run, Last Outcome, Enabled, Actions
    - Renders `<SchedulerJobRow>` for each job, enabled jobs first then disabled (sort client-side)
    - Handles `onJobUpdate(updatedJob)` and `onJobDelete(id)` callbacks from rows to update local state

    **bigpanda-app/components/SchedulerJobRow.tsx** (Client component — `'use client'`):
    - Props: `{ job: ScheduledJob; expanded: boolean; onToggleExpand: () => void; onJobUpdate: (j: ScheduledJob) => void; onJobDelete: (id: string) => void }`
    - Table row cells matching the column order: name, skill_name (human-readable label from SKILL_LIST), cron_expression (show frequency label), next_run (formatted or "—"), last outcome (✓ green / ✗ red / — for null), enabled toggle (Radix UI Switch or native checkbox), Trigger button
    - Last Outcome column: use ✓ (text-green-600) for success, ✗ (text-red-600) for failure, "~" for partial, "—" for null
    - Enabled toggle: on change, call `PATCH /api/jobs/[job.id]` with `{ enabled: newValue }`, then call `onJobUpdate` with the updated job — optimistic update (flip immediately, revert on error)
    - Trigger button: on click, call `POST /api/jobs/trigger` with `{ jobId: job.id, skillName: job.skill_name }`, show a sonner toast on success/failure (use `toast.success` / `toast.error` from 'sonner')
    - Delete button (in expanded panel): show confirmation inline ("Are you sure?" with Confirm/Cancel), on confirm call `DELETE /api/jobs/[job.id]`, then `onJobDelete(job.id)`
    - Expanded panel (inline below row — use a `<tr>` with `colSpan={7}` or a CSS approach): shows [Edit] button (opens CreateJobWizard pre-filled — stub for Plan 04; renders as disabled button for now), [Enable/Disable] button, [Delete] button + run history section
    - Run history section: scrollable list (max-height ~200px, overflow-y-auto) of last 10 entries; each entry shows: timestamp (formatted), outcome badge, duration (e.g., "1.2s"), artifact link if present; error text truncated to 80 chars
    - Import SKILL_LIST from `../../lib/scheduler-skills` to display human-readable skill labels

    Do NOT import from Plan 04 files (CreateJobWizard does not exist yet — use a disabled button placeholder).

    After creating all three files, confirm the fetch calls to GET /api/jobs, PATCH /api/jobs/[id], and POST /api/jobs/trigger are present by running:
    `grep -n "api/jobs" bigpanda-app/app/scheduler/page.tsx bigpanda-app/components/SchedulerJobRow.tsx`
    Output should show the fetch calls in both files before reporting done.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | grep -E "scheduler|Scheduler" | head -20 && grep -c "api/jobs" components/SchedulerJobRow.tsx app/scheduler/page.tsx 2>/dev/null</automated>
  </verify>
  <done>app/scheduler/page.tsx, SchedulerJobTable.tsx, SchedulerJobRow.tsx created with no TypeScript errors; grep confirms fetch calls to GET /api/jobs, PATCH /api/jobs/[id], and POST /api/jobs/trigger exist in the component files; sidebar link test passes GREEN</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/scheduler/sidebar.test.ts --reporter=verbose` — GREEN
- `npx tsc --noEmit` from bigpanda-app/ — no errors in scheduler files
- `grep -n "scheduler_failure" components/Sidebar.tsx` — confirms badge query is wired
- Visually: navigate to /scheduler in browser — table renders with column headers; sidebar shows Scheduler link with badge
</verification>

<success_criteria>
- Sidebar has /scheduler link with correct data-testid
- Sidebar Scheduler link renders NotificationBadge for unread scheduler_failure notifications
- /scheduler page exists and renders SchedulerJobTable
- SchedulerJobTable renders jobs with correct 7 columns
- SchedulerJobRow: inline expand works, enable/disable toggle calls PATCH, Trigger button calls POST /api/jobs/trigger, run history panel renders
- sidebar-scheduler-link test passes GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/24-scheduler-enhanced/24-03-SUMMARY.md`
</output>
