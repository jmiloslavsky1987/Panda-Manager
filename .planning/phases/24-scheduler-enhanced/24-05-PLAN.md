---
phase: 24-scheduler-enhanced
plan: 05
type: execute
wave: 3
depends_on: [24-04]
files_modified: []
autonomous: false
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05, SCHED-06, SCHED-07, SCHED-08, SCHED-09, SCHED-10, SCHED-11, SCHED-12]

must_haves:
  truths:
    - "All 12 SCHED requirements are confirmed working end-to-end"
    - "Full test suite (npx vitest run) is green with no failures"
    - "Scheduler page is accessible from sidebar and renders correctly"
    - "Create Job wizard completes without errors for at least 2 skill types (with and without params)"
    - "Enable/disable, trigger, and delete operations all work correctly in the UI"
  artifacts:
    - path: "bigpanda-app/tests/scheduler/"
      provides: "All 8 test files GREEN"
    - path: "bigpanda-app/app/scheduler/page.tsx"
      provides: "Verified working page"
  key_links:
    - from: "Manual trigger button"
      to: "POST /api/jobs/trigger → BullMQ queue"
      via: "user action"
      pattern: "manual"
---

<objective>
Final verification checkpoint: confirm all 12 SCHED requirements are satisfied end-to-end in the browser with a working local dev environment.

Purpose: Human sign-off gates Phase 24 as complete before REQUIREMENTS.md is updated.
Output: Phase 24 approved and all SCHED requirements marked complete.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-scheduler-enhanced/24-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated test run + TypeScript check</name>
  <files></files>
  <action>
    Run the full test suite and TypeScript check. Fix any remaining failures before surfacing the checkpoint.

    1. `npx vitest run --reporter=verbose` from bigpanda-app/ — all tests must pass GREEN
    2. `npx tsc --noEmit` from bigpanda-app/ — no TypeScript errors
    3. If any scheduler tests fail: diagnose and fix the implementation (do NOT modify the tests)
    4. Confirm the existing scheduler-map.test.ts still passes (backward-compat guard for JOB_SCHEDULE_MAP)
    5. Output the test results summary for the checkpoint review
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass GREEN; npx tsc --noEmit reports no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 24 complete Scheduler Enhanced feature:
    - Scheduler page at /scheduler with sidebar link
    - Job table: Name, Skill, Schedule, Next Run, Last Outcome, Enabled toggle, Trigger button
    - Inline row expand: Edit, Disable/Enable, Delete, run history (last 10 entries)
    - Create Job wizard: 3-step (Skill → Schedule → Params), Step 3 auto-skipped for no-param skills
    - All 12 skills selectable via wizard
    - Enable/disable, manual trigger, delete all working
    - Worker hooks: run history written on job completion/failure
    - Failure notifications written to app_notifications
    - DB-driven scheduler registration (registerDbSchedulers)
  </what-built>
  <how-to-verify>
    **SCHED-10:** Open http://localhost:3000 — confirm "Scheduler" appears in the sidebar. Click it. Confirm /scheduler page loads.

    **SCHED-07:** Verify the job table shows columns: Name, Skill, Schedule, Next Run, Last Outcome (✓/✗), Enabled, and a Trigger button per row.

    **SCHED-01 + SCHED-11:** Click "+ Create Job". Confirm a full-screen wizard opens with stepper header "1 Skill • 2 Schedule • 3 Params".

    **SCHED-12:** In Step 1, confirm 12 skill cards appear in a 3×4 grid. Cards include: Morning Briefing, Customer Project Tracker, Weekly Customer Status, ELT External, ELT Internal, Biggy Weekly Briefing, Context Updater, Meeting Summary, Workflow Diagram, Team Engagement Map, Discovery Scan, Timesheet Reminder.

    **SCHED-11 (step skip):** Select "Morning Briefing". Complete Step 1 (enter a job name). Click Next — confirm you go directly to Submit (Step 3 is skipped). Cancel.

    **SCHED-04 + SCHED-11 (params):** Start a new job, select "Discovery Scan". Advance to Step 3. Confirm project picker and Slack channels input appear.

    **SCHED-02 + SCHED-03:** In Step 2, confirm frequency options: Once, Daily, Weekly, Bi-Weekly, Monthly, Custom Cron. Select "Weekly" — confirm day-of-week picker appears. Confirm timezone defaults to your local timezone.

    **SCHED-01:** Complete the full wizard for any skill (e.g., Morning Briefing, daily at 9am). Confirm the new job appears in the table after submit.

    **SCHED-05:** Toggle the Enabled switch off for a job. Confirm the row moves to the bottom of the table (disabled section). Toggle back on.

    **SCHED-06:** Click the Trigger button on any job. Confirm a success toast appears (or loading state).

    **SCHED-09:** Click a job row to expand it. Confirm run history panel appears (may be empty if no runs yet — "No runs yet" empty state is acceptable).

    **SCHED-07 (last outcome):** After triggering a job, reload /scheduler. Confirm Last Run and Last Outcome columns show updated values.

    Type "approved" when all the above checks pass, or describe any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All 12 SCHED requirements verified in browser. Full test suite GREEN.
</verification>

<success_criteria>
- All 12 SCHED requirements (SCHED-01 through SCHED-12) confirmed working
- npx vitest run reports all tests GREEN
- Human approves at checkpoint
- Phase 24 marked COMPLETE in STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/24-scheduler-enhanced/24-05-SUMMARY.md`

Update REQUIREMENTS.md: mark SCHED-01 through SCHED-12 as complete (phase 24).
Update STATE.md: Phase 24 Scheduler Enhanced → COMPLETE.
</output>
