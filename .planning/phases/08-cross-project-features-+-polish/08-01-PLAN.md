---
phase: 08-cross-project-features-+-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/phase8.spec.ts
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - KB-01
  - KB-02
  - KB-03
must_haves:
  truths:
    - "A failing E2E test exists for SRCH-01 (global search returns cross-project results)"
    - "A failing E2E test exists for SRCH-02 (search results filterable by account, date, type)"
    - "A failing E2E test exists for SRCH-03 (each result shows project and section context)"
    - "A failing E2E test exists for KB-01 (knowledge base entry can be created and viewed)"
    - "A failing E2E test exists for KB-02 (KB entry linkable to a risk or history entry)"
    - "A failing E2E test exists for KB-03 (KB entry carries source_trace fields)"
  artifacts:
    - path: "tests/e2e/phase8.spec.ts"
      provides: "RED baseline E2E stubs for all Phase 8 requirements"
      exports: []
  key_links:
    - from: "tests/e2e/phase8.spec.ts"
      to: "/search"
      via: "page.goto"
      pattern: "SRCH-01"
    - from: "tests/e2e/phase8.spec.ts"
      to: "/knowledge-base"
      via: "page.goto"
      pattern: "KB-01"
---

<objective>
Establish the RED baseline for Phase 8 by writing failing Playwright E2E stubs for all six requirements (SRCH-01/02/03 and KB-01/02/03). These stubs are intentionally failing — implementation plans will activate them.

Purpose: Ensures every requirement has a verifiable automated test before any code is written. Consistent with the Wave 0 stub pattern used in every prior phase.
Output: tests/e2e/phase8.spec.ts with 6 failing tests.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Pattern: Wave 0 stub from prior phases. Use expect(false, 'stub').toBe(true) as the FIRST line of each test. -->
<!-- Requirement IDs in test names for --grep targeting: e.g. test('[SRCH-01] global search returns results', ...) -->
<!-- Consistent pattern from phases 02-01, 03-01, 04-01, 05-01, 06-01, 07-01 -->

From tests/e2e/phase6.spec.ts (reference pattern):
```typescript
import { test, expect } from '@playwright/test';
test('[DASH-04] Risk Heat Map renders on dashboard', async ({ page }) => {
  expect(false, 'stub').toBe(true); // RED baseline
  // ... actual assertions added in activation plan
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 8 E2E stubs (Wave 0 RED baseline)</name>
  <files>tests/e2e/phase8.spec.ts</files>
  <action>
Create tests/e2e/phase8.spec.ts with 6 failing Playwright tests.

**Critical:** `expect(false, 'stub').toBe(true)` must be the FIRST line of each test body — this keeps tests visibly RED without a running server.

Include all six requirement IDs in test names:

```
test('[SRCH-01] global search bar returns cross-project results', ...)
test('[SRCH-02] search results filterable by account, date, and type', ...)
test('[SRCH-03] each search result shows project name and section context', ...)
test('[KB-01] knowledge base entry can be created and appears in list', ...)
test('[KB-02] knowledge base entry can be linked to a risk or history entry', ...)
test('[KB-03] knowledge base entry carries source_trace (project, event, date)', ...)
```

After the `expect(false, 'stub').toBe(true)` RED line, add commented-out assertions describing the full intended behavior (these become the activation spec in plan 08-07):

SRCH-01: Navigate to `/search?q=testterm`, expect results list `data-testid="search-results"` to contain at least one item, each with project name visible.
SRCH-02: Navigate to `/search?q=risk&account=KAISER`, expect all visible results to show KAISER project; add `?type=risk` filter and expect only risk-type results.
SRCH-03: Expect each result card to show `data-testid="result-project"` (project name) and `data-testid="result-section"` (e.g. "risks", "actions") and `data-testid="result-date"` when a date is available.
KB-01: Navigate to `/knowledge-base`, click `data-testid="add-kb-entry-btn"`, fill title and content, submit, expect new entry visible in list with title shown.
KB-02: Navigate to `/knowledge-base`, open an entry, click `data-testid="link-risk-btn"`, expect a risk picker and a saved link ID visible on the entry.
KB-03: Navigate to `/knowledge-base`, open an entry, expect `data-testid="source-trace"` to show project name, event reference, and captured date.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase8.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>6 tests in tests/e2e/phase8.spec.ts, all RED (failing at the stub assertion), no syntax errors.</done>
</task>

</tasks>

<verification>
Run: `cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase8.spec.ts --reporter=list 2>&1 | grep -E "passed|failed|error"`
Expected: 0 passed, 6 failed (stub assertions).
</verification>

<success_criteria>
tests/e2e/phase8.spec.ts exists with 6 tests, all failing at the Wave 0 stub assertion. Every requirement ID (SRCH-01/02/03, KB-01/02/03) appears in a test name.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-project-features-+-polish/08-01-SUMMARY.md`
</output>
