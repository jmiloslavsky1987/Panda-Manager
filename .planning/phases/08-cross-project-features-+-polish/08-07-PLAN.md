---
phase: 08-cross-project-features-+-polish
plan: 07
type: execute
wave: 6
depends_on:
  - "08-05"
  - "08-06"
files_modified:
  - tests/e2e/phase8.spec.ts
autonomous: false
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - KB-01
  - KB-02
  - KB-03
must_haves:
  truths:
    - "All 6 Phase 8 E2E tests pass (GREEN)"
    - "Global search bar is visible on the dashboard"
    - "Searching a term shows results with project and section context"
    - "Knowledge Base page loads and shows entries (or empty state)"
    - "Add KB Entry creates a new entry visible in the list"
    - "source_trace is displayed on KB entries"
  artifacts:
    - path: "tests/e2e/phase8.spec.ts"
      provides: "Activated Phase 8 E2E tests — all 6 GREEN"
      exports: []
  key_links:
    - from: "tests/e2e/phase8.spec.ts"
      to: "/search"
      via: "page.goto + assertions on data-testid"
      pattern: "search-results"
    - from: "tests/e2e/phase8.spec.ts"
      to: "/knowledge-base"
      via: "page.goto + assertions on data-testid"
      pattern: "add-kb-entry-btn"
---

<objective>
Activate the Phase 8 E2E test suite (remove stub assertions, write real behavior assertions), get all 6 tests to GREEN, then present the complete Phase 8 feature set for human verification.

Purpose: Final gate before Phase 8 is declared complete. Tests verify all six requirements end-to-end against a running app.
Output: Green E2E suite + human sign-off.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-cross-project-features-+-polish/08-05-SUMMARY.md
@.planning/phases/08-cross-project-features-+-polish/08-06-SUMMARY.md
</context>

<interfaces>
<!-- assert-if-present pattern: structural assertions always pass; live-data assertions only run when data is present -->
<!-- Pattern from Phase 07-07: check element exists first, then conditionally assert content -->
<!-- data-testids from plan 08-01 stubs:
  "search-bar", "search-results", "result-project", "result-section", "result-date",
  "add-kb-entry-btn", "source-trace", "link-risk-btn"
-->

From tests/e2e/phase7.spec.ts (assert-if-present pattern):
```typescript
const btn = page.getByTestId('generate-plan-btn');
await expect(btn).toBeVisible();
// assert-if-present: only check content if API key is available
const hasApiKey = process.env.ANTHROPIC_API_KEY;
if (hasApiKey) {
  // live assertions
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Activate Phase 8 E2E tests</name>
  <files>tests/e2e/phase8.spec.ts</files>
  <action>
Replace the `expect(false, 'stub').toBe(true)` RED line in each test with real Playwright assertions. Use the assert-if-present pattern for tests that require seeded DB data.

**[SRCH-01] global search bar returns cross-project results:**
- Navigate to `/` (dashboard)
- `await expect(page.getByTestId('search-bar')).toBeVisible()` — structural: always passes
- Type "risk" in search bar, press Enter
- Expect URL to contain `/search?q=risk`
- `await expect(page.getByTestId('search-results')).toBeVisible()` — structural: always passes (even if empty state shows)
- assert-if-present: count results; if > 0, expect first result to have data-testid="result-project" visible

**[SRCH-02] search results filterable by account, date, and type:**
- Navigate to `/search?q=action&type=actions`
- `await expect(page.getByTestId('search-results')).toBeVisible()` — structural
- Check that a type filter select is visible (use `page.locator('select')` or label text)
- assert-if-present: if results > 0, no result should have section "risks" visible

**[SRCH-03] each search result shows project name and section context:**
- Navigate to `/search?q=test`
- `await expect(page.getByTestId('search-results')).toBeVisible()` — structural
- assert-if-present: if results > 0, first result must have data-testid="result-project" and data-testid="result-section" visible

**[KB-01] knowledge base entry can be created and appears in list:**
- Navigate to `/knowledge-base`
- `await expect(page.getByTestId('add-kb-entry-btn')).toBeVisible()` — structural: always passes
- Click add-kb-entry-btn, fill title "Test KB Entry E2E", fill content "E2E test content", click submit
- Expect entry with title "Test KB Entry E2E" to appear in the list (within 3s)

**[KB-02] knowledge base entry can be linked to a risk or history entry:**
- Navigate to `/knowledge-base`
- assert-if-present: if any entry is visible, click first entry's link-risk-btn (data-testid="link-risk-btn"), expect a risk ID input to appear

**[KB-03] knowledge base entry carries source_trace:**
- Navigate to `/knowledge-base`
- `await expect(page.locator('[data-testid="add-kb-entry-btn"]')).toBeVisible()` — structural
- assert-if-present: if entries list > 0, expect first entry to have data-testid="source-trace" visible with non-empty text

Run all 6 tests after activation. Fix any failures. All 6 must be GREEN before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase8.spec.ts --reporter=list 2>&1 | tail -10</automated>
  </verify>
  <done>6/6 Phase 8 E2E tests pass. No stub assertions remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 8 complete: Full-text search across all project records (actions, risks, decisions, history, stakeholders, tasks, artifacts, knowledge base) with filter panel; Knowledge Base page with create, view, link, source_trace display; sidebar navigation link; PostgreSQL tsvector/GIN indexes with auto-update triggers.
  </what-built>
  <how-to-verify>
Start the app: `cd bigpanda-app && npm run dev`

**Search verification:**
1. Look for the search bar in the top of the app on any page — verify it's visible
2. Type any word in the search bar and press Enter — verify you land on /search with a results page
3. If you have seeded DB data: verify result cards show project name, section label, and date
4. Verify the filter panel shows: account input, type dropdown, from/to date fields
5. Select "Actions" in the type filter — verify page re-fetches (loading flash)

**Knowledge Base verification:**
6. Click "Knowledge Base" in the sidebar — verify /knowledge-base loads
7. Click "Add Entry" — verify the modal opens with title, content, project, source_trace fields
8. Create a test entry — verify it appears in the list immediately after saving
9. Verify the entry card shows source_trace text (auto-generated or entered)
10. Click "Link to Risk" on an entry — verify an input appears for entering a risk ID

**E2E verification:**
11. `cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase8.spec.ts --reporter=list`
    Verify: 6/6 tests pass

Report any broken flows or visual issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
`cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase8.spec.ts --reporter=list 2>&1 | grep -E "passed|failed"`
Expected: 6 passed, 0 failed.
</verification>

<success_criteria>
6/6 Phase 8 E2E tests GREEN. Human has verified search UI, Knowledge Base page, and E2E results. Phase 8 declared COMPLETE.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-project-features-+-polish/08-07-SUMMARY.md`
</output>
