---
phase: 08-cross-project-features-+-polish
plan: 05
type: execute
wave: 4
depends_on:
  - "08-03"
files_modified:
  - bigpanda-app/app/search/page.tsx
  - bigpanda-app/components/SearchBar.tsx
  - bigpanda-app/app/layout.tsx
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
must_haves:
  truths:
    - "A search input is visible in the app's top navigation on every page"
    - "Typing in the search bar and pressing Enter navigates to /search?q=term"
    - "The /search page shows a list of result cards, each showing project name, section, date, and a text snippet"
    - "A filter panel on /search lets user select account, date range, and data type"
    - "Changing a filter re-fetches results from /api/search with the new params"
    - "When no results are found, a friendly empty state is shown"
  artifacts:
    - path: "bigpanda-app/components/SearchBar.tsx"
      provides: "Global search input with Enter-key navigation"
      exports: ["SearchBar"]
    - path: "bigpanda-app/app/search/page.tsx"
      provides: "Search results page with filter panel and result cards"
      exports: ["default (SearchPage)"]
  key_links:
    - from: "bigpanda-app/app/layout.tsx"
      to: "bigpanda-app/components/SearchBar.tsx"
      via: "import SearchBar in root layout"
      pattern: "SearchBar"
    - from: "bigpanda-app/app/search/page.tsx"
      to: "/api/search"
      via: "client-side fetch on mount and on filter change"
      pattern: "fetch.*api/search"
---

<objective>
Build the global search UI: a persistent search bar in the app header and a dedicated /search results page with filter controls. Users can search across all project records from anywhere in the app.

Purpose: Implements SRCH-01/02/03 from the user's perspective. The search API from plan 08-03 is the data layer.
Output: SearchBar component in layout + /search results page.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@bigpanda-app/app/layout.tsx
</context>

<interfaces>
<!-- UI stack: React 19, Tailwind 4, shadcn/ui components already installed -->
<!-- Client components: "use client" at top, use useState/useEffect for fetch -->
<!-- SearchPage is a Client Component — fetches from /api/search via useEffect on searchParams change -->
<!-- layout.tsx is the root RSC layout — SearchBar must be "use client" imported into it -->
<!-- SearchResult shape from plan 08-03 queries.ts:
  { id, table, section, project_id, project_name, customer, date, title, snippet }
-->

From bigpanda-app/app/layout.tsx (existing structure — add SearchBar to header):
The layout already has a sidebar and main content area. SearchBar goes in the top header bar (if exists) or as a sticky top bar above <main>.

Established data-testid patterns (E2E stubs from plan 08-01):
- data-testid="search-results"   — the results list container
- data-testid="result-project"   — project name span per result
- data-testid="result-section"   — section name span per result
- data-testid="result-date"      — date span per result (omit if null)
- data-testid="search-bar"       — the input element in SearchBar
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create SearchBar component and wire into layout</name>
  <files>bigpanda-app/components/SearchBar.tsx, bigpanda-app/app/layout.tsx</files>
  <action>
**SearchBar.tsx:**
Client component ("use client"). Renders a text input with:
- `data-testid="search-bar"`
- placeholder "Search all projects..."
- Controlled value state
- On Enter keydown: `router.push('/search?q=' + encodeURIComponent(value))` using next/navigation useRouter
- Tailwind: rounded input, border, px-3 py-1.5, w-64 on desktop, full-width on mobile

**layout.tsx:**
Read the existing layout.tsx first. Add SearchBar to the top of the main content area (not inside the sidebar). Import as a Client Component. Placement: before the `{children}` render, in a sticky top bar div with `className="flex items-center justify-between border-b px-6 py-2 bg-white sticky top-0 z-10"`.

Do NOT modify the sidebar structure — only add the top bar with SearchBar.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | grep "SearchBar\|layout" | head -10</automated>
  </verify>
  <done>SearchBar.tsx exists. layout.tsx imports SearchBar. TypeScript compiles clean. SearchBar has data-testid="search-bar".</done>
</task>

<task type="auto">
  <name>Task 2: Create /search results page with filter panel</name>
  <files>bigpanda-app/app/search/page.tsx</files>
  <action>
Create bigpanda-app/app/search/page.tsx as a Client Component ("use client").

**State:**
- `query` — from URL searchParams (useSearchParams hook)
- `results: SearchResult[]` — fetched from API
- `loading: boolean`
- `filterAccount: string` — filter select
- `filterType: string` — filter select
- `filterFrom: string` — date input
- `filterTo: string` — date input

**Behavior:**
- On mount and whenever query/filter state changes: fetch `/api/search?q={query}&account={filterAccount}&type={filterType}&from={filterFrom}&to={filterTo}` (omit empty params)
- Debounce filter changes by 300ms to avoid excessive fetches
- Show loading spinner (a simple "Searching..." text) while fetching

**Layout (Tailwind):**
- Two-column grid: left 1/4 = filter panel, right 3/4 = results
- Filter panel contains: account text input, type select (All Types / Actions / Risks / Key Decisions / Engagement History / Stakeholders / Tasks / Artifacts / Knowledge Base), from date, to date
- Results list: `data-testid="search-results"` on the container

**Result card per item:**
```tsx
<div key={r.id + r.table} className="border rounded-lg p-4 mb-3 bg-white hover:shadow-sm">
  <div className="flex items-center gap-2 text-xs text-zinc-500 mb-1">
    <span data-testid="result-project">{r.project_name}</span>
    <span>/</span>
    <span data-testid="result-section" className="capitalize">{r.section}</span>
    {r.date && <span data-testid="result-date" className="ml-auto">{r.date}</span>}
  </div>
  <p className="font-medium text-zinc-900 text-sm">{r.title}</p>
  {r.snippet && <p className="text-zinc-600 text-sm mt-1 line-clamp-2">{r.snippet}</p>}
</div>
```

**Empty state:** When results.length === 0 and !loading: show `<p className="text-zinc-500 text-center py-12">No results for "{query}"</p>`

**Zero-query state:** When query is empty: show `<p className="text-zinc-500 text-center py-12">Enter a search term above</p>`
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | grep "search/page" | head -10</automated>
  </verify>
  <done>/search/page.tsx exists as "use client" component. data-testid="search-results" container present. Filter inputs for account, type, from, to present. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- Components exist: `ls bigpanda-app/components/SearchBar.tsx bigpanda-app/app/search/page.tsx`
- data-testids present: `grep -c "data-testid" bigpanda-app/app/search/page.tsx` returns >= 4
- TypeScript clean: `cd bigpanda-app && npx tsc --noEmit 2>&1 | grep -v node_modules | grep error | wc -l`
</verification>

<success_criteria>
SearchBar renders in app header on every page. /search page fetches from /api/search, shows result cards with project/section/date context, shows filter panel. Empty state shown when no results.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-project-features-+-polish/08-05-SUMMARY.md`
</output>
