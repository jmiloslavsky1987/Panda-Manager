---
phase: 08-cross-project-features-+-polish
plan: 06
type: execute
wave: 5
depends_on:
  - "08-04"
  - "08-05"
files_modified:
  - bigpanda-app/app/knowledge-base/page.tsx
  - bigpanda-app/components/KnowledgeBaseEntry.tsx
  - bigpanda-app/components/AddKbEntryModal.tsx
  - bigpanda-app/app/layout.tsx
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-03
must_haves:
  truths:
    - "Navigating to /knowledge-base shows a list of all KB entries with title, source_trace, and project name"
    - "Clicking 'Add Entry' opens a modal to create a new KB entry (title, content, project, source_trace)"
    - "Each entry shows its linked risk or engagement history entry when present"
    - "An 'Add KB Entry' sidebar link navigates to /knowledge-base"
    - "source_trace is displayed on each entry card (satisfying KB-03 visibility requirement)"
  artifacts:
    - path: "bigpanda-app/app/knowledge-base/page.tsx"
      provides: "Knowledge Base list page with add button"
      exports: ["default (KnowledgeBasePage)"]
    - path: "bigpanda-app/components/AddKbEntryModal.tsx"
      provides: "Modal for creating new KB entry — title, content, project selector, source_trace"
      exports: ["AddKbEntryModal"]
    - path: "bigpanda-app/components/KnowledgeBaseEntry.tsx"
      provides: "Entry card with source_trace, linked items, and link controls"
      exports: ["KnowledgeBaseEntry"]
  key_links:
    - from: "bigpanda-app/app/knowledge-base/page.tsx"
      to: "/api/knowledge-base"
      via: "client-side fetch on mount"
      pattern: "fetch.*knowledge-base"
    - from: "bigpanda-app/components/KnowledgeBaseEntry.tsx"
      to: "/api/knowledge-base/[id]"
      via: "PATCH call on link action"
      pattern: "PATCH.*knowledge-base"
---

<objective>
Build the Knowledge Base UI: a global /knowledge-base page for browsing and creating entries, and an entry card component that shows source_trace and linked records. Adds a sidebar navigation link.

Purpose: Implements KB-01/02/03 from the user's perspective. API layer built in plan 08-04.
Output: /knowledge-base page + AddKbEntryModal + KnowledgeBaseEntry card + sidebar link.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@bigpanda-app/app/layout.tsx
</context>

<interfaces>
<!-- layout.tsx already has a sidebar with nav links — add "Knowledge Base" link pointing to /knowledge-base -->
<!-- shadcn/ui Dialog component available for modal (same as ActionEditModal pattern in Phase 3) -->
<!-- Entry list: Client Component, fetch on mount from /api/knowledge-base -->
<!-- Link action: PATCH /api/knowledge-base/[id] with { linked_risk_id: X } or { linked_history_id: X } -->
<!-- source_trace display: show as a badge or small secondary text under the entry title -->

From established modal pattern (ActionEditModal, Phase 3):
- Dialog from "@/components/ui/dialog"
- DialogContent, DialogHeader, DialogTitle, DialogFooter
- Form fields in a div with space-y-4
- Submit calls POST /api/knowledge-base, then refreshes list

data-testid requirements (from E2E stubs in plan 08-01):
- data-testid="add-kb-entry-btn"   — the Add Entry button on the page
- data-testid="source-trace"       — the source_trace display element per entry
- data-testid="link-risk-btn"      — button to link to a risk (on entry card)

SearchResult shape for KB entries (from /api/knowledge-base GET):
```typescript
interface KBEntryWithProject {
  id: number;
  project_id: number | null;
  title: string;
  content: string;
  source_trace: string | null;
  linked_risk_id: number | null;
  linked_history_id: number | null;
  linked_date: string | null;
  created_at: string;
  project_name: string | null;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Knowledge Base page and Add Entry modal</name>
  <files>bigpanda-app/app/knowledge-base/page.tsx, bigpanda-app/components/AddKbEntryModal.tsx</files>
  <action>
**bigpanda-app/app/knowledge-base/page.tsx:**
Client Component ("use client"). State: `entries: KBEntryWithProject[]`, `loading: boolean`, `showModal: boolean`.

Fetch on mount: `GET /api/knowledge-base`. Render list of KnowledgeBaseEntry cards.

Header: "Knowledge Base" title + `<button data-testid="add-kb-entry-btn">Add Entry</button>` that opens AddKbEntryModal.

On modal success (new entry created): re-fetch list.

Empty state: "No knowledge base entries yet. Add the first one."

**bigpanda-app/components/AddKbEntryModal.tsx:**
Client Component. Props: `open: boolean`, `onClose: () => void`, `onCreated: () => void`.

Uses shadcn Dialog. Form fields:
- Title (required text input)
- Content (required textarea, min 3 rows)
- Project (optional select — list projects fetched from GET /api/projects on modal open, or pass as prop)
- Source Trace (optional text input, placeholder "e.g. KAISER risk R-001, 2026-03-24 — leave blank to auto-generate")

On submit: POST /api/knowledge-base with form values. On success: call onCreated() and close. Show inline error on failure.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | grep "knowledge-base\|KnowledgeBase\|AddKb" | head -10</automated>
  </verify>
  <done>page.tsx and AddKbEntryModal.tsx exist. data-testid="add-kb-entry-btn" present. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create KnowledgeBaseEntry card and add sidebar link</name>
  <files>bigpanda-app/components/KnowledgeBaseEntry.tsx, bigpanda-app/app/layout.tsx</files>
  <action>
**bigpanda-app/components/KnowledgeBaseEntry.tsx:**
Client Component. Props: `entry: KBEntryWithProject`, `onUpdated: () => void`.

Renders a card with:
- Title (font-medium)
- Project name badge (if present)
- `<span data-testid="source-trace" className="text-xs text-zinc-500">{entry.source_trace ?? 'No source trace'}</span>`
- Content preview (first 150 chars with "..." truncation)
- Linked items section: if `linked_risk_id` is set, show "Linked Risk: #{linked_risk_id}". If `linked_history_id`, show "Linked History: #{linked_history_id}". If `linked_date`, show the date.
- Link actions row: `<button data-testid="link-risk-btn" onClick={handleLinkRisk}>Link to Risk</button>` and a similar "Link to History" button.

`handleLinkRisk`: show a simple prompt-style inline input for a risk ID (not a full modal — keep scope small). On submit: PATCH /api/knowledge-base/{entry.id} with `{ linked_risk_id: parseInt(value) }`. On success: call onUpdated().

**bigpanda-app/app/layout.tsx:**
Read the current sidebar nav links. Add a "Knowledge Base" link (`href="/knowledge-base"`) to the sidebar navigation list, below the existing nav items. Use the same link styling pattern as the existing sidebar items (typically an `<Link>` with className matching the others, possibly with a database or book icon from lucide-react).
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | grep -v node_modules | grep "error" | head -10</automated>
  </verify>
  <done>KnowledgeBaseEntry.tsx exists with data-testid="source-trace" and data-testid="link-risk-btn". layout.tsx has Knowledge Base link. TypeScript compiles without new errors.</done>
</task>

</tasks>

<verification>
- Files exist: `ls bigpanda-app/app/knowledge-base/page.tsx bigpanda-app/components/KnowledgeBaseEntry.tsx bigpanda-app/components/AddKbEntryModal.tsx`
- data-testids present: `grep -rn "data-testid" bigpanda-app/components/KnowledgeBaseEntry.tsx bigpanda-app/app/knowledge-base/page.tsx` shows add-kb-entry-btn, source-trace, link-risk-btn
- Sidebar link: `grep "knowledge-base" bigpanda-app/app/layout.tsx`
- TypeScript: `cd bigpanda-app && npx tsc --noEmit 2>&1 | grep -v node_modules | grep error | wc -l` returns 0
</verification>

<success_criteria>
/knowledge-base page renders entry list with Add Entry button. Each entry shows source_trace. Link to Risk action calls PATCH API. Knowledge Base appears in sidebar nav. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-project-features-+-polish/08-06-SUMMARY.md`
</output>
