---
phase: 32-time-tracking-global-view
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/time-tracking-global/api-endpoint.test.ts
  - tests/time-tracking-global/global-view.test.ts
  - tests/time-tracking-global/workspace-tabs.test.ts
autonomous: true
requirements:
  - TIME-01
  - TIME-02
  - TIME-03

must_haves:
  truths:
    - "Three test files exist that fail RED (stubs reference modules not yet implemented)"
    - "api-endpoint.test.ts describes GET /api/time-entries shape with project_name in response"
    - "global-view.test.ts describes cross-project fetch and ?project= param pre-fill"
    - "workspace-tabs.test.ts asserts 'time' subtab no longer in TAB_GROUPS"
  artifacts:
    - path: "tests/time-tracking-global/api-endpoint.test.ts"
      provides: "Wave 0 failing stubs for TIME-01 API shape"
      min_lines: 30
    - path: "tests/time-tracking-global/global-view.test.ts"
      provides: "Wave 0 failing stubs for TIME-01/TIME-02 component behavior"
      min_lines: 40
    - path: "tests/time-tracking-global/workspace-tabs.test.ts"
      provides: "Wave 0 failing stubs for TIME-03 tab removal"
      min_lines: 20
  key_links:
    - from: "tests/time-tracking-global/workspace-tabs.test.ts"
      to: "components/WorkspaceTabs.tsx"
      via: "import { TAB_GROUPS }"
      pattern: "TAB_GROUPS.*time"
---

<objective>
Create Wave 0 test scaffolds — failing RED stubs — for all three test files required by the Nyquist rule before any implementation begins.

Purpose: Establish test contracts that turn GREEN when later plans implement the production code, providing a feedback loop across the entire phase.
Output: Three test files in tests/time-tracking-global/ that fail RED with meaningful assertion messages (not import errors).
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-time-tracking-global-view/32-CONTEXT.md
@.planning/phases/32-time-tracking-global-view/32-RESEARCH.md
@.planning/phases/32-time-tracking-global-view/32-VALIDATION.md

<interfaces>
<!-- Key patterns from existing test infrastructure -->

From tests/api/projects-patch.test.ts (established vi.mock pattern):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
vi.mock('@/db', () => ({ db: { ... } }))
vi.mock('@/lib/auth-server', () => ({ requireSession: mockRequireSession }))
```

From vitest.config.ts (path aliases):
```typescript
// '@' resolves to bigpanda-app/
// 'server-only' → tests/__mocks__/server-only.ts (already stubbed)
```

From components/WorkspaceTabs.tsx (TAB_GROUPS export):
```typescript
export const TAB_GROUPS: TabGroup[] = [
  // ... includes { id: 'admin', children: [{ id: 'time', ... }, ...] }
]
```

From lib/time-tracking.ts (types used in tests):
```typescript
export type EntryStatus = 'draft' | 'submitted' | 'approved' | 'rejected' | 'locked'
export type GroupBy = 'project' | 'team_member' | 'status' | 'date'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create api-endpoint.test.ts and workspace-tabs.test.ts stubs</name>
  <files>tests/time-tracking-global/api-endpoint.test.ts, tests/time-tracking-global/workspace-tabs.test.ts</files>
  <behavior>
    api-endpoint.test.ts:
    - Test 1: "GET /api/time-entries returns entries with project_name field" — stub that expects a `project_name` property on entries, fails RED because module doesn't exist yet
    - Test 2: "GET /api/time-entries accepts project_id filter param" — stub that expects handler to be defined, fails RED
    - Test 3: "GET /api/time-entries accepts from/to date filter params" — stub that expects handler to be defined, fails RED

    workspace-tabs.test.ts:
    - Test 1: "TAB_GROUPS admin group does not contain a 'time' subtab" — directly imports TAB_GROUPS from components/WorkspaceTabs.tsx, asserts no child with id === 'time' exists. This CURRENTLY FAILS RED because the tab still exists.
  </behavior>
  <action>
    Create tests/time-tracking-global/api-endpoint.test.ts:
    - Use vi.mock('@/db') with minimal Drizzle chain mocks returning { entries: [] }
    - Use vi.mock('@/lib/auth-server') returning { session: { user: { id: 1, role: 'admin' } } }
    - Import GET handler from '@/app/api/time-entries/route' (module does not exist yet — test MUST fail RED on import or assertion)
    - Write 3 describe/it blocks with assertions that the endpoint returns `{ entries: [...] }` shape with `project_name` on each entry
    - Use Wave 0 stub pattern: const handler: any = undefined; expect(handler).toBeDefined() — fails RED without brittle import errors. Comment: "STUB — fails RED until 32-02 implements app/api/time-entries/route.ts"

    Create tests/time-tracking-global/workspace-tabs.test.ts:
    - Import { TAB_GROUPS } from '@/components/WorkspaceTabs' (module exists)
    - Test: find the admin group, then assert none of its children have id === 'time'
    - This test fails RED NOW (time tab exists) and turns GREEN after 32-03 removes it
    - No mocking needed — it is a pure data structure assertion
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/time-tracking-global/api-endpoint.test.ts tests/time-tracking-global/workspace-tabs.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both files exist; workspace-tabs.test.ts fails with an assertion error (not a syntax error); api-endpoint.test.ts fails with "handler is not defined" or similar stub failure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create global-view.test.ts stub</name>
  <files>tests/time-tracking-global/global-view.test.ts</files>
  <behavior>
    - Test 1: "GlobalTimeView exports a component" — stub, fails RED (module doesn't exist)
    - Test 2: "?project= query param initializes project filter from URL" — stub asserting the exported component can receive a project param, fails RED
    - Test 3: "TimeEntryModal accepts optional projectId prop" — stub asserting the modal interface supports optional projectId, fails RED
    - Test 4: "weekly grouping produces week bucket headers in 'Mar 31 – Apr 6, 2026' format" — pure unit test of getMondayOfWeek + formatWeekHeader helpers, fails RED (functions not yet exported)
  </behavior>
  <action>
    Create tests/time-tracking-global/global-view.test.ts:
    - Use Wave 0 stub pattern for component tests: const GlobalTimeView: any = undefined; expect(GlobalTimeView).toBeDefined()
    - For the weekly grouping test, write an actual logic test:
      - Import getMondayOfWeek from '@/components/GlobalTimeView' (fails RED — module doesn't exist)
      - Use stub pattern: const getMondayOfWeek: any = undefined; expect(getMondayOfWeek).toBeDefined()
      - Add a separate test block that, once implemented, checks: getMondayOfWeek('2026-04-01') returns '2026-03-30' (Monday of that week)
    - Comment all stubs: "STUB — fails RED until 32-04 implements GlobalTimeView"
    - Do NOT mock React — environment is 'node' (vitest.config.ts); keep tests to pure logic / export shape assertions
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/time-tracking-global/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All three test files exist; all fail RED with stub failures (not syntax/parse errors); running the suite shows meaningful failure messages for each test case</done>
</task>

</tasks>

<verification>
Run full test suite to confirm only the new time-tracking-global tests fail (no regressions):
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run --reporter=verbose 2>&1 | tail -30
</verification>

<success_criteria>
- tests/time-tracking-global/ directory contains exactly 3 test files
- All 3 files fail RED with meaningful stub failure messages
- No existing passing tests broken
- workspace-tabs.test.ts fails on the actual assertion (TAB_GROUPS contains 'time' — which is the current state)
</success_criteria>

<output>
After completion, create `.planning/phases/32-time-tracking-global-view/32-01-SUMMARY.md`
</output>
