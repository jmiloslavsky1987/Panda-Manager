---
phase: 32-time-tracking-global-view
plan: "05"
type: execute
wave: 4
depends_on:
  - "32-04"
files_modified: []
autonomous: false
requirements:
  - TIME-01
  - TIME-02
  - TIME-03

must_haves:
  truths:
    - "Full test suite is green with no regressions"
    - "Production build completes without errors or warnings"
    - "User can navigate to /time-tracking and see their entries grouped by week"
    - "Sidebar shows Time Tracking link; workspace no longer has Time tab"
    - "Old /customer/:id/time URL redirects to /time-tracking?project=:id"
    - "Add form project dropdown pre-fills when arriving via redirect"
    - "Bulk move reassigns entries across projects correctly"
  artifacts:
    - path: "bigpanda-app/app/time-tracking/page.tsx"
      provides: "Accessible, functional global time tracking page"
  key_links:
    - from: "Human verification"
      to: "All TIME-01, TIME-02, TIME-03 behaviors"
      via: "Browser walkthrough"
      pattern: "manual"
---

<objective>
Full verification of Phase 32: automated test suite, TypeScript build check, then human UAT walkthrough to confirm all five success criteria from the roadmap are met.

Purpose: Gate before marking Phase 32 complete. Automated checks catch regressions; human UAT confirms real browser behavior for routing, redirects, and UI interactions that cannot be unit-tested.
Output: Phase 32 marked complete or a list of bugs to fix.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/32-time-tracking-global-view/32-CONTEXT.md
@.planning/phases/32-time-tracking-global-view/32-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated verification — test suite + TypeScript build</name>
  <files></files>
  <action>
    Run the full Vitest test suite. Any failures must be fixed before proceeding to human UAT.

    Run order:
    1. npx vitest run tests/time-tracking-global/ --reporter=verbose (Phase 32 specific tests — all must be GREEN)
    2. npx vitest run --reporter=verbose (full suite — no regressions allowed)
    3. npx tsc --noEmit (TypeScript check — zero errors allowed)
    4. npm run build (if TSC passes — production build must succeed)

    Fix any failures found before marking this task complete. Document each fix in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run --reporter=verbose 2>&1 | grep -E "Tests:|PASS|FAIL" | tail -10 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite green; TypeScript compiles without errors; production build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 32 implementation:
    - /time-tracking — standalone top-level route with cross-project time entries
    - Entries grouped by week with date range headers
    - Project filter dropdown + date range filters
    - Full CRUD: add (with project dropdown), edit, delete from global view
    - Approval workflow: submit, approve, reject with status badges (role-gated)
    - Bulk actions: approve, reject, delete, move (cross-project reassignment)
    - CSV/XLSX export respecting active filters
    - Calendar import with per-event project selection
    - Sidebar Time Tracking link (Clock icon, after Scheduler)
    - WorkspaceTabs: Time tab removed from Admin group
    - Old /customer/:id/time redirects to /time-tracking?project=:id
  </what-built>
  <how-to-verify>
    Start the dev server: cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run next-only

    Then verify each item:

    1. SIDEBAR LINK
       - Open the app at http://localhost:3000
       - Confirm "Time Tracking" link with clock icon appears in the sidebar bottom section (after Scheduler)
       - Click it — should navigate to /time-tracking

    2. GLOBAL VIEW — BASIC DISPLAY (TIME-01)
       - At /time-tracking: confirm you see your time entries from all projects
       - Entries should be grouped by week with headers like "Mar 31 – Apr 6, 2026"
       - Each entry shows: date, project badge, hours, description, status badge

    3. FILTERS (TIME-01)
       - Use the project dropdown to filter to a single project — entries should narrow
       - Use the date range pickers (from / to) — entries should narrow to that range
       - Clear all filters — all entries reappear

    4. REDIRECT AND PRE-FILL (TIME-01, TIME-02)
       - Navigate to /customer/1/time (or any valid project ID)
       - Should redirect to /time-tracking?project=1
       - Project filter should be pre-selected to that project
       - Click "Add Entry" — project dropdown in the form should show that project pre-selected

    5. ADD ENTRY WITH PROJECT DROPDOWN (TIME-02)
       - Click "Add Entry" at /time-tracking (no redirect, no pre-fill)
       - Add entry form should show a required project dropdown (no default)
       - Select a project, fill date/hours/description, save
       - New entry should appear in the list with the correct project badge

    6. EDIT AND DELETE (TIME-02)
       - Click edit on any entry — modal opens with correct values
       - Change a value and save — entry updates
       - Delete an entry — confirm dialog appears, entry removed after confirm

    7. BULK ACTIONS (TIME-02)
       - Select 2+ entries using checkboxes — bulk action bar appears
       - Try bulk delete — entries removed after confirm
       - Try bulk move (if entries exist from multiple projects) — select entries from different projects, choose move, pick target project — entries should reassign

    8. WORKSPACE TAB REMOVAL (TIME-03)
       - Navigate to any customer workspace (e.g., /customer/1/overview)
       - Click the "Admin" tab group — confirm "Time" subtab is NOT in the sub-navigation
       - Admin group should show only: Artifacts and Review Queue

    9. EXPORT
       - Apply a project filter at /time-tracking
       - Click Export — file should download with only entries matching current filters
       - Open the file — should include a "Project" column

    10. CALENDAR IMPORT (if Google Calendar is connected)
       - Click calendar import — modal should open
       - Each event row should have a project dropdown for assignment
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 items pass. Or describe specific issues found (e.g., "Item 4 failed — no pre-fill on redirect"). Claude will fix issues and re-run verification.</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 32 success criteria from ROADMAP.md confirmed:
1. User can access /time-tracking from main navigation showing all time entries across all projects
2. Each time entry displays project attribution and user can filter by project
3. Time entries are grouped by week with date range headers
4. Per-project Time Tracking tab no longer appears in customer workspace tabs
5. Old /customer/[id]/time route redirects to /time-tracking with project filter preserved
</verification>

<success_criteria>
- Full test suite green, TypeScript clean, build succeeds
- Human UAT: all 10 verification items pass
- Phase 32 requirements TIME-01, TIME-02, TIME-03 all confirmed in browser
- Phase 32 marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/32-time-tracking-global-view/32-05-SUMMARY.md`
</output>
