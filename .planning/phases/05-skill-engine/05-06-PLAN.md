---
phase: 05-skill-engine
plan: 06
type: execute
wave: 4
depends_on:
  - 05-05
files_modified:
  - tests/e2e/phase5.spec.ts
autonomous: false
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
  - SKILL-11
  - SKILL-12
  - SKILL-13
  - SKILL-14
  - DASH-09
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04

must_haves:
  truths:
    - "All 13 E2E tests in phase5.spec.ts pass (GREEN)"
    - "Skills tab visible at /customer/1/skills with skill list and Run buttons"
    - "Running a skill triggers BullMQ job and creates a skill_runs row in DB"
    - "SSE stream delivers output chunks to the run page in real-time"
    - "Drafts Inbox section is visible on the Dashboard"
    - "Output Library page is accessible at /outputs with filter UI"
    - "Human has verified live SSE streaming in browser and navigate-away/return behavior"
  artifacts:
    - path: "tests/e2e/phase5.spec.ts"
      provides: "All 13 tests GREEN — stubs replaced with real assertions"
      contains: "expect(page.locator"
  key_links:
    - from: "tests/e2e/phase5.spec.ts"
      to: "all Phase 5 features"
      via: "Playwright assertions verifying each requirement"
      pattern: "SKILL-0[1-4]|SKILL-1[1-4]|DASH-09|OUT-0[1-4]"
---

<objective>
Turn all 13 Phase 5 E2E tests GREEN by replacing stub assertions with real Playwright assertions, then perform human verification of SSE streaming and the navigate-away/return deduplication behavior.

Purpose: Phase gate. All automated tests green + human eye verification = Phase 5 complete.
Output: Updated phase5.spec.ts with 13 passing tests. Human sign-off on SSE streaming and Drafts Inbox.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-skill-engine/05-CONTEXT.md
@.planning/phases/05-skill-engine/05-VALIDATION.md
@.planning/phases/05-skill-engine/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace E2E stubs with real assertions — run full suite to GREEN</name>
  <files>tests/e2e/phase5.spec.ts</files>
  <action>
    Read the current tests/e2e/phase5.spec.ts (from 05-01). For each test:
    1. Remove `expect(false, 'stub').toBe(true);` (the first line of every test body)
    2. Keep or update the remaining assertions to match the actual implemented UI

    Run `npx playwright test tests/e2e/phase5.spec.ts` after removing stubs. Fix any assertion failures by adjusting selectors to match actual rendered DOM. Use --grep to target individual tests during debugging.

    Key assertion adjustments based on actual implementation (adjust after reading actual rendered HTML):

    SKILL-01: Navigate to /customer/1/skills. Assert Skills link in nav is visible. Click first Run button. Assert URL changes to /customer/1/skills/{uuid}.

    SKILL-02: POST /api/skills/weekly-customer-status/run with projectId. Assert response.ok() and body.runId is truthy. (Token count is logged server-side — E2E verifies the API responds correctly, not the console log.)

    SKILL-03: Trigger weekly-customer-status via API, navigate to run page, wait for data-testid="skill-output" to be non-empty with timeout 45000ms (live Claude call). Then go to Dashboard, assert data-testid="drafts-inbox" visible and data-testid="draft-item" count > 0.

    SKILL-04: Trigger meeting-summary with transcript input. Navigate to run page. Wait for skill-output non-empty.

    SKILL-11: Trigger morning-briefing via API. Navigate to Dashboard. Assert data-testid="morning-briefing-panel" visible. (Add this panel to Dashboard page.tsx if not yet present — uses getLatestMorningBriefing query.)

    SKILL-12: Trigger context-updater with transcript. Navigate to run page. Wait for skill-output.

    SKILL-13: Trigger handoff-doc-generator. Navigate to run page. Wait for skill-output.

    SKILL-14: Navigate to /customer/1/skills. If a skill has no SKILL.md on disk (test by temporarily checking for a non-existent skill name), the POST /api/skills/[name]/run returns 422 and the UI shows error badge. Use a known-missing skill name in the test (e.g., 'elt-external-status' which is grayed out and has no SKILL.md).

    DASH-09: Navigate to /. Assert data-testid="drafts-inbox" visible. Assert either draft-item present OR "No pending drafts" text visible.

    OUT-01: After running a skill (or if outputs exist), navigate to /outputs. Assert data-testid="output-library" visible. Assert at least one output row if DB has data (assert-if-present pattern from STATE.md).

    OUT-02: Navigate to /outputs. Assert filter-account, filter-skill-type, filter-date-range all visible.

    OUT-03: Navigate to /outputs. If a completed output with type='html' or HTML content exists, assert data-output-type="html" element visible. (assert-if-present pattern.)

    OUT-04: Navigate to /outputs. If regenerate-btn exists (data exists), click it, wait for output-archived-badge. (assert-if-present pattern.)

    For tests that require live Claude API calls (SKILL-03, SKILL-04, SKILL-11, SKILL-12, SKILL-13): use timeout: 60000 on assertions waiting for skill output. The ANTHROPIC_API_KEY must be in environment for tests to succeed. If key not available, tests will timeout — this is expected behavior (not a test failure to fix in code).

    After updating assertions, run:
    ```
    npx playwright test tests/e2e/phase5.spec.ts
    ```

    If any tests fail, debug with `--grep "SKILL-01"` etc. to isolate. Adjust selectors to match actual DOM — do not change the underlying feature implementations.

    For SKILL-03, SKILL-14, SKILL-11 which touch Dashboard (page.tsx) features:
    - If data-testid="morning-briefing-panel" doesn't exist yet, add it to the Dashboard page. The morning briefing panel should show the latest morning briefing output from getLatestMorningBriefing(). Add it as a section above or near the Drafts Inbox. If no briefing exists: show "No briefing yet — will run at 8am".
    - This may require updating app/page.tsx to import and call getLatestMorningBriefing() and render a `<section data-testid="morning-briefing-panel">` section.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code && npx playwright test tests/e2e/phase5.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>npx playwright test tests/e2e/phase5.spec.ts shows 13 passed (0 failed). All stubs replaced with real assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 Skill Engine — all features:
    - Skills tab (11th workspace tab) with skill launcher list and Recent Runs
    - Real-time SSE streaming on skill run page
    - Navigate-away mid-stream deduplication (returns to completed output)
    - Drafts Inbox on Dashboard with inline editor and dismiss
    - Output Library at /outputs with filters, HTML iframe, Open button, Regenerate
    - 5 wired skills: Weekly Customer Status, Meeting Summary, Morning Briefing, Context Updater, Handoff Doc Generator
    - SKILL.md files read from disk at invocation time (hot-reload)
    - Token budget guard (logged to console before every Claude call)
  </what-built>
  <how-to-verify>
    Prerequisites: Server running (`npm run dev` in bigpanda-app/), Redis running, PostgreSQL running with migration 0004 applied, ANTHROPIC_API_KEY set in environment.

    1. Navigate to any customer workspace (e.g. /customer/1/overview). Verify "Skills" appears as the 11th tab in the nav bar.

    2. Click the Skills tab. Verify:
       - All 15 skills appear in a list
       - 5 skills have active "Run" buttons (Weekly Customer Status, Meeting Summary, Morning Briefing, Context Updater, Handoff Doc Generator)
       - 10 skills are grayed out with "Coming in a future update" tooltip on hover
       - A "Recent Runs" section appears below (may be empty)

    3. Click "Run" on Weekly Customer Status. Verify:
       - Browser navigates to /customer/1/skills/{uuid}
       - Text begins streaming in the output area in real-time (tokens appear one by one)
       - Status badge shows "Streaming..."

    4. While streaming, navigate to Dashboard (/). Then navigate back to the run URL. Verify:
       - Run completes in the background while you're away
       - Returning to the URL shows the full completed output (not a blank page)
       - No second Claude API call is made (check server logs — only one "input_tokens:" log line)

    5. Navigate to Dashboard (/). Verify:
       - "Drafts Inbox" section is visible
       - A draft from the Weekly Customer Status run appears with Copy/Gmail Draft/Slack/Dismiss buttons
       - Click "Dismiss" on the draft — it disappears from the inbox

    6. Navigate to /outputs. Verify:
       - Output Library page shows the completed Weekly Customer Status output
       - Account and Skill Type filter dropdowns are visible
       - "Regenerate" button is visible on the output row

    7. Verify SKILL.md hot-reload: Open bigpanda-app/skills/weekly-customer-status.md, add "Test change" to the end. Run Weekly Customer Status again. Verify the new output reflects the changed prompt content (server restart NOT required).

    8. Check the terminal/console where the server is running. Verify log lines like:
       `[skill-orchestrator] weekly-customer-status input_tokens: XXXX / budget: 80000`
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 verification steps pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
`npx playwright test tests/e2e/phase5.spec.ts` — all 13 tests GREEN
Human verification approved (all 8 manual verification steps pass)
</verification>

<success_criteria>
- All 13 Phase 5 E2E tests pass (npx playwright test tests/e2e/phase5.spec.ts shows 13 passed)
- Human verifies live SSE streaming — tokens appear in real-time in browser
- Human verifies navigate-away/return — no duplicate skill run triggered
- Human verifies Drafts Inbox on Dashboard — draft appears after skill run
- Human verifies Output Library — completed outputs visible with filters
- Human verifies SKILL.md hot-reload — no server restart required for prompt changes
- Human verifies token count in console — "input_tokens:" logged before every Claude call
</success_criteria>

<output>
After completion, create `.planning/phases/05-skill-engine/05-06-SUMMARY.md`
Update .planning/STATE.md: Phase 5 complete (6/6 plans)
Update .planning/ROADMAP.md: Phase 5 status = Complete
</output>
