---
phase: 82-chat-write-operations-tool-use-mutations-for-actions-milestones-risks-teams-and-architecture-nodes-with-confirmation-ux
plan: "05"
type: execute
wave: 5
depends_on: ["82-04"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "User can create a new action item by typing a request in chat and confirming the green confirmation card"
    - "User can update a risk by typing a request in chat and confirming the blue confirmation card"
    - "User can delete a milestone by typing a request in chat, the red card appears, type 'delete' is required, then confirming"
    - "Batch operation: 'mark all Sarah's open actions as in progress' produces one card per action, each confirmable independently"
    - "Cancel button on any card removes the pending operation with no DB write"
    - "After confirmation, Claude replies with a short status line only"
    - "All existing read-only chat behavior is unchanged"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete chat write operations flow across all entity types and operation variants.

Purpose: Confirm that the full interaction — natural language intent detection, confirmation card rendering, approval/cancel flow, batch operations, and DB persistence — works end-to-end in a real browser session.
Output: Human-verified go/no-go signal. Phase 82 complete.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verify: chat write operations end-to-end</name>
  <files>none — verification only</files>
  <action>
    Run full test suite before presenting to user:
    `cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run --reporter=verbose`
    Then start dev server: `npm run next-only`
    Then present verification steps to user.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/chat/ --reporter=verbose</automated>
  </verify>
  <done>User types "approved" confirming all 9 in-browser scenarios pass.</done>
  <what-built>
    Full chat write operations: 30+ write tools covering 13 entity types, MutationConfirmCard with create/update/delete variants, inline confirmation flow, delete type-to-confirm friction, batch operation support, activeTab context passing.
  </what-built>
  <how-to-verify>
    Start the dev server: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run next-only`
    Open a project in the browser and navigate to the Chat tab.

    Test 1 — Create (green card):
    Type: "Create an action: schedule demo with the infrastructure team, owner John, due next Friday"
    Expected: Green-bordered card appears showing the proposed action fields. Confirm → record created in Actions tab. Claude replies with short status line.

    Test 2 — Update (blue card):
    Type: "Update the first open risk — set its status to mitigated"
    Expected: Blue-bordered card appears showing the risk record and proposed change. Confirm → risk status updated in Risks tab.

    Test 3 — Delete (red card with type-to-confirm):
    Type: "Delete the milestone named [pick any real milestone]"
    Expected: Red-bordered card appears. Confirm button is disabled. Type "delete" into the confirmation input → Confirm button activates. Confirm → milestone removed from Milestones tab. Claude replies with status.

    Test 4 — Cancel flow:
    Trigger any mutation. Click Cancel on the card. Expected: no DB change, Claude replies "Cancelled — no changes made."

    Test 5 — Batch operation:
    Type: "Mark all open actions as in_progress" (with a project that has 2+ open actions)
    Expected: One confirmation card per action appears in the same message. Each can be confirmed or cancelled independently.

    Test 6 — Active tab context:
    While on the Risks tab, type: "Add a new one about data migration performance"
    Expected: Claude proposes creating a risk (not an action or milestone) — defaults to current tab's entity type.

    Test 7 — Regression check (read-only):
    Type: "What are the open actions?"
    Expected: Text response with action list (no confirmation card, no mutation).

    Test 8 — Teams entity:
    Type: "Create a team pathway for the Analytics team"
    Expected: Green card with team_name field pre-filled. Confirm → record in Teams tab.

    Test 9 — Architecture entity:
    Type: "Add an architecture node called 'Data Lake' on the [existing track name] track"
    Expected: Green card with name and track_name fields. Confirm → node appears in Architecture tab.
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 tests pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 9 verification scenarios behave as specified.
Full test suite: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- Chat write operations work end-to-end for create, update, delete
- Color-coded cards render correctly per operation type
- Delete type-to-confirm friction works
- Batch operations produce one card per record
- Cancel produces no DB write
- Active tab context influences entity type defaults
- Existing read-only chat behavior unchanged
- All tests GREEN, production build clean, git committed and pushed
</success_criteria>

<output>
After completion, create `.planning/phases/82-chat-write-operations-tool-use-mutations-for-actions-milestones-risks-teams-and-architecture-nodes-with-confirmation-ux/82-05-SUMMARY.md`
</output>
