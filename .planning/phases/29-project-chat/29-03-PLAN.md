---
phase: 29-project-chat
plan: "03"
type: execute
wave: 2
depends_on:
  - "29-01"
  - "29-02"
files_modified: []
autonomous: false
requirements:
  - CHAT-01
  - CHAT-02
must_haves:
  truths:
    - "Chat tab is visible and clickable in the project workspace nav bar"
    - "Typing a question and pressing Send streams words to the browser progressively"
    - "Typing indicator appears before the first word arrives and disappears when streaming completes"
    - "A follow-up question in the same session references the prior exchange correctly"
    - "A count question (open actions) returns the exact number from the DB — no invented numbers"
    - "Responses cite specific record IDs inline (e.g. A-KAISER-012)"
    - "Clear conversation button resets the panel without page refresh"
  artifacts:
    - path: "bigpanda-app/components/chat/ChatPanel.tsx"
      provides: "Verified streaming chat UI"
    - path: "bigpanda-app/app/api/projects/[projectId]/chat/route.ts"
      provides: "Verified streaming API endpoint"
  key_links:
    - from: "browser Chat tab"
      to: "POST /api/projects/[projectId]/chat"
      via: "useChat DefaultChatTransport SSE stream"
      pattern: "streaming words appear progressively"
    - from: "POST /api/projects/[projectId]/chat"
      to: "DB via buildChatContext"
      via: "system prompt context injection"
      pattern: "response cites exact DB record count"
---

<objective>
Human verification of the complete E2E chat flow — streaming, multi-turn context, and hallucination constraints.

Purpose: Confirm CHAT-01 and CHAT-02 are satisfied in a live browser with real DB data. Automated tests verify structure; this checkpoint verifies behavior. The hallucination audit (CHAT-02) cannot be automated — requires a human to cross-reference the chat response against the actual DB.
Output: Both requirements CHAT-01 and CHAT-02 marked verified in REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-project-chat/29-CONTEXT.md
@.planning/phases/29-project-chat/29-01-SUMMARY.md
@.planning/phases/29-project-chat/29-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and production build</name>
  <files></files>
  <action>
    Run the full test suite and production build before requesting human verification.

    1. Full test suite:
    ```
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run
    ```
    All tests must be GREEN. Fix any failures before proceeding.

    2. Production build (catches SSR/hydration issues — critical for streaming routes per VALIDATION.md):
    ```
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run build 2>&1 | tail -30
    ```
    Build must complete without errors. Common issues to watch for:
    - Missing `export const dynamic = 'force-dynamic'` on chat route (causes static prerender error)
    - useChat/DefaultChatTransport imported in server component (causes RSC error)
    - ChatPanel not marked 'use client' (causes hydration error)

    Fix any build errors before checkpoint.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | grep -E "(PASS|FAIL|Tests:)" | tail -10</automated>
  </verify>
  <done>All tests GREEN, production build completes with exit code 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — streaming, multi-turn, hallucination audit</name>
  <what-built>
    Complete AI chat panel integrated into every project workspace. Three capabilities to verify:
    1. CHAT-01: Chat tab accessible from nav bar; questions stream to browser in real time with typing indicator
    2. CHAT-01 (multi-turn): Follow-up questions reference prior conversation context
    3. CHAT-02: Responses cite actual DB record counts and record IDs — no invented facts

    To start the app: `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run dev`
    Then open any project workspace (e.g., http://localhost:3000/customer/1).
  </what-built>
  <how-to-verify>
    **Test 1 — Tab and streaming (CHAT-01):**
    1. Navigate to any project workspace
    2. Click "Chat" in the navigation bar — confirm it appears between Skills and Admin
    3. Verify the panel shows 4 starter question chips ("What are the open actions?", "Any overdue milestones?", etc.)
    4. Verify the header shows "Answers are based on this project's live data"
    5. Click a starter question chip OR type a question and press Enter/Send
    6. Confirm: words appear progressively in the browser (not all at once after a wait)
    7. Confirm: a pulsing typing indicator appears BEFORE the first word arrives
    8. Confirm: typing indicator disappears when the response finishes streaming

    **Test 2 — Multi-turn follow-up (CHAT-01):**
    1. Ask a first question: "What is the current project status?"
    2. Wait for full response
    3. Ask a follow-up: "What about the risks?"
    4. Confirm: the response correctly addresses risks (references risk data, not a generic answer)
    5. Ask another follow-up: "Which of those are high severity?"
    6. Confirm: response narrows to high-severity risks (not re-listing everything) — demonstrates multi-turn context

    **Test 3 — Hallucination audit / grounding verification (CHAT-02):**
    1. Open a new terminal and run: `psql $DATABASE_URL -c "SELECT COUNT(*) FROM actions WHERE project_id = 1 AND status NOT IN ('completed','cancelled');"` (replace project_id with the project you're testing)
    2. Note the actual open action count (e.g., 7)
    3. Ask in chat: "How many open actions does this project have?"
    4. Confirm: the response states the EXACT same number from the DB query (e.g., "7 open actions")
    5. Confirm: the response cites at least one specific record ID inline (e.g., "including A-KAISER-012")
    6. Ask: "What is the phase 99 completion percentage?" (something that doesn't exist)
    7. Confirm: response says "I don't see that information in this project's current data" — NOT an invented answer

    **Test 4 — Clear conversation:**
    1. After having a conversation, click "Clear conversation" in the panel header
    2. Confirm: message list clears and starter question chips reappear
    3. Confirm: page did not refresh (URL unchanged)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 4 tests pass.
    Or describe any issues found (e.g., "streaming works but typing indicator not appearing", "multi-turn: responses ignore prior context").
  </resume-signal>
  <files></files>
  <action>Human verifies the complete E2E chat flow per how-to-verify steps above. No code changes required at this step.</action>
  <verify>
    <automated>MANUAL — see how-to-verify steps above for streaming, multi-turn, and hallucination audit procedures</automated>
  </verify>
  <done>Human types "approved" after all 4 verification tests pass</done>
</task>

</tasks>

<verification>
All tests GREEN (automated). All 4 manual tests pass (human verification). CHAT-01 and CHAT-02 behaviors confirmed in browser.
</verification>

<success_criteria>
- Full test suite GREEN (npm test -- --run)
- Production build succeeds (npm run build)
- Chat tab accessible in workspace nav bar between Skills and Admin
- Streaming confirmed: words appear progressively with typing indicator
- Multi-turn confirmed: follow-up questions reference prior conversation
- Hallucination audit passed: count questions return exact DB count; record IDs cited; unknown queries return "I don't see that information"
- Clear conversation resets panel without page refresh
- Human has typed "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/29-project-chat/29-03-SUMMARY.md`

After human approval, update REQUIREMENTS.md:
- CHAT-01: change `- [ ]` to `- [x]`
- CHAT-02: change `- [ ]` to `- [x]`

Update ROADMAP.md Phase 29 progress row: `| 29. Project Chat | 4/4 | Complete | {date} |`
</output>
