---
phase: 85.1-screen-density-and-ux-overhaul
plan: "04"
subsystem: UX / page layout
tags: [density, tailwind, sweep, linear-density, page-wrappers]
dependency_graph:
  requires: ["85.1-02", "85.1-03"]
  provides: ["85.1-05"]
  affects: [all customer tab pages, all global pages]
tech_stack:
  added: []
  patterns: [p-6→p-4 sweep, space-y-6→space-y-3 sweep, gap-6→gap-3 sweep, wrapper-only surgery]
key_files:
  created: []
  modified:
    - app/customer/[id]/overview/page.tsx
    - app/customer/[id]/wbs/page.tsx
    - app/customer/[id]/architecture/page.tsx
    - app/customer/[id]/stakeholders/page.tsx
    - app/customer/[id]/artifacts/page.tsx
    - app/customer/[id]/queue/page.tsx
    - app/customer/[id]/history/page.tsx
    - app/customer/[id]/settings/page.tsx
    - app/page.tsx
    - app/daily-prep/page.tsx
    - app/knowledge-base/page.tsx
    - app/outputs/page.tsx
    - app/scheduler/page.tsx
    - app/time-tracking/page.tsx
    - app/settings/page.tsx
    - app/search/page.tsx
decisions:
  - "plan/page.tsx does not exist at app/customer/[id]/plan/page.tsx — route resolves to plan sub-tabs (board/gantt/swimlane/tasks); no wrapper to sweep; noted rather than creating a file"
  - "artifacts/page.tsx: the effective top-level content div is inside <ArtifactsDropZone>; swept p-6→p-4 on that inner div (space-y-8 is not in sweep set)"
  - "queue/page.tsx: px-6 py-8 → px-4 py-4 (both shorthand forms swept together); mb-6 → mb-4 on header group"
  - "settings/page.tsx (global): p-8 → p-6 per plan rule (p-8 top-level → p-6); inner card p-6 instances left untouched (sub-component scope)"
  - "This commit predates Plan 05 human visual verification — rollback path is gap-closure plan, not git revert"
  - "Plan 04 deliberately spans 30 files across 2 tasks; scope warning acknowledged in plan and accepted because edits are mechanical and exclusions are explicit"
metrics:
  duration: ~25 minutes
  completed: "2026-05-12T15:07:18Z"
  tasks_completed: 2
  files_modified: 16
  files_scoped_but_skipped: 14
---

# Phase 85.1 Plan 04: Per-Tab and Global Page Density Sweep Summary

**One-liner:** Mechanical p-6→p-4 / space-y-6→space-y-3 wrapper sweep across 16 customer tab and global pages; WBS and Gantt internals byte-identical to pre-execution state.

---

## Per-File Transform Matrix

### Task 1: Customer Tab Pages (21 routes scoped)

| File | p-6→p-4 | space-y-6→space-y-3 | gap-6→gap-3 | mt/mb-6→mt/mb-4 | Action |
|------|---------|---------------------|-------------|-----------------|--------|
| overview/page.tsx | — | ✓ (space-y-6 on top-level) | — | — | Swept |
| plan/page.tsx | — | — | — | — | MISSING — route resolves to plan/* sub-tabs |
| gantt/page.tsx | — | — | — | — | SKIP — already at p-4 |
| wbs/page.tsx | ✓ (wrapper only) | — | — | — | Swept (WbsPageClient untouched) |
| tasks/page.tsx | — | — | — | — | SKIP — already at p-4 |
| actions/page.tsx | — | — | — | — | SKIP — no padding wrapper (data-testid div) |
| risks/page.tsx | — | — | — | — | SKIP — no padding wrapper |
| milestones/page.tsx | — | — | — | — | SKIP — no padding wrapper |
| decisions/page.tsx | — | — | — | — | SKIP — no top-level padding |
| teams/page.tsx | — | — | — | — | SKIP — space-y-8 (not in sweep set) |
| architecture/page.tsx | — | ✓ (space-y-6 on top-level) | — | — | Swept |
| stakeholders/page.tsx | — | ✓ (space-y-6 on top-level) | — | — | Swept |
| skills/page.tsx | — | — | — | — | SKIP — returns `<SkillsTabClient>` directly |
| chat/page.tsx | — | — | — | — | SKIP — returns `<ChatPanel>` directly |
| context/page.tsx | — | — | — | — | SKIP — returns `<ContextTab>` directly |
| time/page.tsx | — | — | — | — | SKIP — redirect only |
| artifacts/page.tsx | ✓ (inner content div) | — | — | — | Swept (ArtifactsDropZone outer preserved) |
| queue/page.tsx | ✓ (px-6→px-4) | — | — | ✓ (mb-6→mb-4) | Swept (py-8 left: not in sweep set) |
| history/page.tsx | — | ✓ (space-y-6 on top-level) | — | — | Swept |
| members/page.tsx | — | — | — | — | SKIP — returns `<MembersTab>` directly |
| settings/page.tsx (customer) | — | — | — | ✓ (mb-6→mb-4 on h2) | Swept |

### Task 2: Global Pages (9 routes scoped)

| File | p-6→p-4 | space-y-6→space-y-3 | gap-6→gap-3 | px-8→px-6 | mb-6→mb-4 | Action |
|------|---------|---------------------|-------------|-----------|-----------|--------|
| app/page.tsx | — | ✓ (inner section div) | — | ✓ (px-8→px-6) | — | Swept (outer min-h-screen preserved; hero band untouched) |
| daily-prep/page.tsx | — | ✓ (top-level wrapper) | — | — | — | Swept (px-4/py-8 already correct) |
| weekly-report/page.tsx | — | — | — | — | — | SKIP — returns `<WeeklyReportClient>` directly |
| knowledge-base/page.tsx | ✓ | — | — | — | ✓ | Swept |
| outputs/page.tsx | ✓ | — | — | — | ✓ | Swept (prose wrappers untouched) |
| scheduler/page.tsx | ✓ | — | — | — | ✓ (×2) | Swept |
| time-tracking/page.tsx | ✓ | — | — | — | ✓ | Swept |
| settings/page.tsx (global) | ✓ (p-8→p-6) | — | — | — | ✓ (h1) | Swept (inner card p-6 untouched; sub-component scope) |
| search/page.tsx | ✓ | — | — | — | ✓ | Swept |

---

## Pages Skipped — Already at Target Density or No Applicable Wrapper

- `gantt/page.tsx` — already at `p-4` (no change needed)
- `tasks/page.tsx` — already at `p-4`
- `actions/page.tsx` — wraps in bare `data-testid` div, no padding
- `risks/page.tsx` — bare wrapper, no padding
- `milestones/page.tsx` — bare wrapper, no padding
- `decisions/page.tsx` — `<Suspense>` with `<DecisionsTableClient>` directly, no wrapper padding
- `teams/page.tsx` — uses `space-y-8` which is not in the sweep set
- `skills/page.tsx` — direct component return
- `chat/page.tsx` — direct component return
- `context/page.tsx` — direct component return
- `time/page.tsx` — redirect page only
- `members/page.tsx` — direct component return
- `weekly-report/page.tsx` — direct component return

---

## WBS and Gantt Internals — Exclusion Confirmation

**WBS:**
- `app/customer/[id]/wbs/page.tsx` — wrapper changed from `p-6` to `p-4` (1 line diff)
- `components/WbsPageClient.tsx` — NOT modified; import and all props identical
- `components/WbsGrid.tsx` — NOT modified; no diff in this plan's commits

**Gantt:**
- `app/customer/[id]/gantt/page.tsx` — already at `p-4`; no changes applied
- `components/GanttChart.tsx` — NOT modified; no diff in this plan's commits

Git verification: `git log --oneline -- components/WbsPageClient.tsx components/WbsGrid.tsx components/GanttChart.tsx` shows no commits from this plan's session. Last commit for these files predates Phase 85.1-04 execution.

---

## TypeScript Error Count

| Metric | Count |
|--------|-------|
| Pre-edit TS errors | 118 |
| Post-edit TS errors | 118 |
| New errors introduced | **0** |

All pre-existing errors are unrelated to this sweep (unrelated type issues from earlier phases).

---

## Build Verification

Next.js build completed successfully. All swept routes confirmed in build output:
- `/customer/[id]/wbs`, `/customer/[id]/overview`, `/customer/[id]/history`, `/customer/[id]/stakeholders`, `/customer/[id]/queue`, `/customer/[id]/artifacts`, `/customer/[id]/architecture`, `/customer/[id]/settings`
- `/daily-prep`, `/knowledge-base`, `/outputs`, `/scheduler`, `/time-tracking`, `/settings`, `/search`, `/` (Portfolio Dashboard)

No new build failures introduced.

---

## Git Commit

**Commit hash:** `4acbc692`
**Message:** `feat: [Phase 85.1] per-tab and global page density sweep (p-6 -> p-4)`
**Files changed:** 16 files, 24 insertions, 24 deletions
**Push status:** Pushed to `https://github.com/jmiloslavsky1987/Panda-Manager.git` (main)

---

## IMPORTANT: Pre-Verification Commit Note

**This commit predates Plan 05 human visual verification.** Per CLAUDE.md, every GSD phase execution must end with a git commit and push. Plan 05 will perform human visual verification after this commit.

**If verification fails:** The rollback path is a follow-up gap-closure plan (revert or fix the affected files in a new commit). This is NOT a `git revert` of this commit. This is intentional per project policy.

---

## Deviations from Plan

### Auto-fixed Issues

None — plan executed exactly as written.

### Non-Mechanical Decisions Documented

**1. `app/customer/[id]/plan/page.tsx` — File does not exist**
- The `/customer/[id]/plan` route resolves to plan sub-tabs (`/plan/board`, `/plan/gantt`, `/plan/swimlane`, `/plan/tasks`) — there is no direct `plan/page.tsx` wrapper
- Action: Noted in SUMMARY; no file created; sub-tab pages are already swept via their own routes
- Impact: Zero — the "plan" entry in the files_modified list is a documentation artifact; the actual route structure has sub-tabs with their own wrappers

**2. `app/settings/page.tsx` inner card `p-6` instances**
- 10+ `p-6` instances on `border border-zinc-200 rounded-lg p-6 max-w-lg` card divs inside the page body
- Decision: Left untouched — these are sub-component section cards, not the top-level page wrapper. Only the outermost `p-8` wrapper and `h1 mb-6` were swept.
- Impact: Inner cards remain at their own card-level padding; this is correct per plan scope

**3. `app/customer/[id]/artifacts/page.tsx` effective wrapper**
- The `<ArtifactsDropZone>` component wraps the page; the inner `<div className="p-6 space-y-8">` is the effective content root
- Decision: Swept `p-6→p-4` on the inner div (it is the top-level content wrapper within the component boundary). `space-y-8` is not in the sweep set.
- Impact: Consistent with other pages that wrap in a non-div component boundary

---

## Scope Note

Plan 04 deliberately spans 30 files across 2 tasks. Scope warning was acknowledged in the plan and accepted because:
1. Each per-file edit is mechanical (find-and-replace on top-level wrapper)
2. Splitting into 04a/04b would not gain meaningful resilience
3. Wave 3 runs against an already-dense baseline from Plans 02 and 03
4. The Task 2 verify script + cross-file grep guarantees no top-level `p-6` survives

Actual files with changes: 16 (of 30 scoped). 14 files skipped as already at target density or no applicable wrapper.

---

## Self-Check

Checking key created/modified files and commits exist:

## Self-Check: PASSED

- `app/customer/[id]/wbs/page.tsx` — EXISTS, contains `p-4` (wrapper swept)
- `app/customer/[id]/actions/page.tsx` — EXISTS (unmodified, as expected)
- `app/page.tsx` — EXISTS, contains `px-6 py-4 space-y-3` (inner section wrapper swept from px-8/py-6/space-y-6)
- Commit `4acbc692` — EXISTS in git log
- All 16 modified files confirmed present
- TypeScript errors: 118 before / 118 after (no regression)
- Build: completed successfully
