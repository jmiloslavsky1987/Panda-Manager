---
phase: 85.1-screen-density-and-ux-overhaul
plan: 02
subsystem: ui
tags: [shadcn, tailwind, density, table, button, dialog, card, input, select, textarea, cva]

# Dependency graph
requires:
  - phase: 85.1-01
    provides: "Spacing/height token foundation in kata-tokens.css sections 5-6; --spacing-* aliases in globals.css"
provides:
  - "All 7 shadcn primitives updated to Linear-style density defaults — every consumer inherits automatically"
  - "TableHead 36px (h-9); TableCell 32px rows with 13px table-scoped font"
  - "Input and SelectTrigger at 32px (h-8)"
  - "Button: default 32px, sm 28px, lg 40px, icon 32x32"
  - "DialogContent padding 16px (was 24px)"
  - "CardHeader/Content/Footer padding 16px (was 24px)"
  - "Textarea min-height 64px (was 80px) with tighter py-1.5"
affects: [85.1-03, 85.1-04, 85.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Primitive-first density: edit shadcn source once, all consumers inherit — no per-consumer edits"
    - "13px body text scoped to TableCell only (not global) — avoids typography cascade regressions"
    - "Template-literal className merge (textarea) preserved alongside cn() pattern (all other primitives)"

key-files:
  created: []
  modified:
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/table.tsx
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/input.tsx
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/select.tsx
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/textarea.tsx
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/button.tsx
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/dialog.tsx
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/card.tsx

key-decisions:
  - "[85.1-02] 13px body text applied at TableCell scope only — global body change deferred to Plan 05 human verification to avoid typography cascade"
  - "[85.1-02] Button size=lg transitioned from h-11 (44px) to h-10 (40px) — explicit height reduction per CONTEXT.md density spec"
  - "[85.1-02] DialogHeader and DialogFooter NOT modified — no padding in this repo's baseline; DialogContent p-4 plus gap-4 provides correct spacing"
  - "[85.1-02] textarea.tsx template-literal ${className || ''} pattern preserved (NOT converted to cn())"
  - "[85.1-02] SelectLabel and SelectSeparator not modified — only SelectTrigger h-10→h-8; SelectItem py-1.5 already at target"

patterns-established:
  - "Primitive density editing: always read file byte-exact before editing; use Edit tool with exact string matching"

requirements-completed: [DENSITY-02]

# Metrics
duration: 14min
completed: 2026-05-12
---

# Phase 85.1 Plan 02: Shadcn Primitive Density Sweep Summary

**All 7 shadcn primitives (table, input, select, textarea, button, dialog, card) updated to Linear-style density defaults — every app consumer inherits tighter sizing with zero per-consumer edits**

## Performance

- **Duration:** ~14 min
- **Started:** 2026-05-12T14:54:11Z
- **Completed:** 2026-05-12T15:08:00Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- All table consumers (Actions, Risks, Milestones, Decisions, Stakeholders, Members) now inherit 36px headers and 32px body rows automatically
- All form inputs and selects default to 32px height; all buttons default to 32px (sm=28px, lg=40px, icon=32x32)
- Dialog padding reduced from 24px to 16px; Card section padding reduced from 24px to 16px across all three sub-components
- TypeScript compile confirms zero new errors introduced — all pre-existing errors were in unrelated test files

## Before/After className Strings

### ui/table.tsx

**TableHead**
- BEFORE: `"h-12 px-4 text-left align-middle font-medium text-muted-foreground [&:has([role=checkbox])]:pr-0"`
- AFTER:  `"h-9 px-3 py-1.5 text-left align-middle font-medium text-muted-foreground text-xs [&:has([role=checkbox])]:pr-0"`

**TableCell**
- BEFORE: `"p-4 align-middle [&:has([role=checkbox])]:pr-0"`
- AFTER:  `"px-3 py-1.5 align-middle text-[13px] [&:has([role=checkbox])]:pr-0"`

### ui/input.tsx

- BEFORE: `"flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ..."`
- AFTER:  `"flex h-8 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ..."`
  (only `h-10` → `h-8`; `text-base` was not present; `text-sm` was already correct)

### ui/select.tsx

**SelectTrigger**
- BEFORE: `"flex h-10 w-full items-center justify-between ... text-sm ..."`
- AFTER:  `"flex h-8 w-full items-center justify-between ... text-sm ..."`
  (only `h-10` → `h-8`; SelectContent, SelectItem, SelectLabel, SelectSeparator unchanged)

### ui/textarea.tsx

- BEFORE: `` `flex min-h-[80px] w-full ... px-3 py-2 text-sm ... ${className || ''}` ``
- AFTER:  `` `flex min-h-[64px] w-full ... px-3 py-1.5 text-sm ... ${className || ''}` ``
  (template-literal `${className || ''}` merge pattern preserved — NOT converted to cn())

### ui/button.tsx

**cva size variants**
- BEFORE: `default: "h-10 px-4 py-2"` / `sm: "h-9 rounded-md px-3"` / `lg: "h-11 rounded-md px-8"` / `icon: "h-10 w-10"`
- AFTER:  `default: "h-8 px-3 py-1.5"` / `sm: "h-7 rounded-md px-2.5 text-xs"` / `lg: "h-10 rounded-md px-6"` / `icon: "h-8 w-8"`
- NOTE: Button size=lg transitioned from h-11 (44px) to h-10 (40px) — this IS an explicit height reduction per CONTEXT.md density spec

### ui/dialog.tsx

**DialogContent**
- BEFORE: `"... gap-4 border bg-background p-6 shadow-lg duration-200 ..."`
- AFTER:  `"... gap-4 border bg-background p-4 shadow-lg duration-200 ..."`
- NOTE: DialogHeader and DialogFooter were NOT modified — this repo's baseline has no padding on either (no `p-*` classes); DialogContent's `p-4` plus `gap-4` provides correct spacing. Earlier plan revisions had incorrectly assumed shadcn defaults; direct file inspection confirmed padding-less baseline.

### ui/card.tsx

**CardHeader**
- BEFORE: `"flex flex-col space-y-1.5 p-6"`
- AFTER:  `"flex flex-col space-y-1.5 p-4"`

**CardContent**
- BEFORE: `"p-6 pt-0"`
- AFTER:  `"p-4 pt-0"`

**CardFooter**
- BEFORE: `"flex items-center p-6 pt-0"`
- AFTER:  `"flex items-center p-4 pt-0"`

## Task Commits

Each task was committed atomically:

1. **Task 1: Tighten ui/table.tsx, ui/input.tsx, ui/select.tsx, ui/textarea.tsx** - `83f40e05` (feat)
2. **Task 2: Tighten ui/button.tsx, ui/dialog.tsx, ui/card.tsx** - `cccd7be4` (feat)

## Files Created/Modified
- `components/ui/table.tsx` - TableHead h-12→h-9 + text-xs; TableCell p-4→px-3 py-1.5 text-[13px]
- `components/ui/input.tsx` - h-10→h-8
- `components/ui/select.tsx` - SelectTrigger h-10→h-8
- `components/ui/textarea.tsx` - min-h-[80px]→min-h-[64px], py-2→py-1.5; template-literal preserved
- `components/ui/button.tsx` - size cva variants: default/sm/lg/icon all tightened
- `components/ui/dialog.tsx` - DialogContent p-6→p-4 only
- `components/ui/card.tsx` - CardHeader/Content/Footer p-6→p-4

## Decisions Made
- 13px body text (`text-[13px]`) applied at TableCell scope only. Global body font remains text-sm (14px) to avoid cascading typography regressions — Plan 05 human verification will spot-check whether 14px feels right in forms/modals/cards.
- Button `size=lg` dropped from h-11 (44px) to h-10 (40px) per CONTEXT.md density spec. This is an explicit behavioral transition, not an oversight.
- DialogHeader and DialogFooter intentionally left with no padding — they were already padding-less in this repo's baseline (not standard shadcn defaults).
- textarea.tsx template-literal `${className || ''}` pattern preserved rather than converting to cn() — preserves existing consumer override behavior.

## Deviations from Plan

None - plan executed exactly as written. All class name strings matched the documented BEFORE values byte-exactly.

## Issues Encountered

None. Pre-existing TypeScript errors in test files (archive.test.ts, front-matter-strip.test.ts, require-project-role.test.ts, discovery-approve-audit.test.ts) were present before this plan and are unrelated to the 7 UI primitive files.

## TypeScript Compile Result

`npx tsc --noEmit` — zero new errors introduced. All errors reported are pre-existing in test infrastructure files unrelated to this plan's changes.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Plans 03 (global chrome density) and 04 (per-tab sweep) can now rely on these primitive defaults
- All table consumers will automatically render at 36px/32px without per-consumer edits
- Per-consumer className overrides (e.g., `className="h-12"`) continue to work as expected — Plans 03/04 will sweep those if they exist

---
*Phase: 85.1-screen-density-and-ux-overhaul*
*Completed: 2026-05-12*
