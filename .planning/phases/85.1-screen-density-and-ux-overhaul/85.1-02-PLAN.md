---
phase: 85.1-screen-density-and-ux-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["85.1-01"]
files_modified:
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/table.tsx
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/input.tsx
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/select.tsx
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/button.tsx
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/dialog.tsx
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/card.tsx
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/textarea.tsx
autonomous: true
requirements:
  - DENSITY-02
must_haves:
  truths:
    - "Every Table consumer in the app (Actions, Risks, Milestones, Decisions, Stakeholders, Members) renders denser rows automatically — 32px body, 36px header — with no per-consumer edit"
    - "Every form Input / Select default-renders at 32px height (h-8), with text-sm everywhere"
    - "Every Button default-renders at 32px height; Button size='sm' renders at 28px; Button size='lg' stays at 40px"
    - "Every Dialog renders with p-4 padding (16px) instead of p-6 (24px)"
    - "Every Card renders with p-4 padding (16px) instead of p-6 (24px)"
    - "Every Textarea defaults to min-h-[64px] with px-3 py-1.5"
  artifacts:
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/components/ui/table.tsx"
      provides: "Tightened TableHead (h-9 px-3 py-1.5) + TableCell (px-3 py-1.5)"
      contains: "h-9 px-3 py-1.5"
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/components/ui/button.tsx"
      provides: "size: default=h-8 / sm=h-7 / lg=h-10 cva variants"
      contains: "h-8 px-3"
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/components/ui/dialog.tsx"
      provides: "DialogContent/Header/Footer at p-4 (was p-6)"
      contains: "p-4"
  key_links:
    - from: "/Users/jmiloslavsky/Documents/Panda-Manager/components/ui/table.tsx"
      to: "every consumer of <TableHead> and <TableCell>"
      via: "default className strings inside TableHead/TableCell forwardRef"
      pattern: "h-9 px-3 py-1.5|px-3 py-1.5"
    - from: "/Users/jmiloslavsky/Documents/Panda-Manager/components/ui/button.tsx"
      to: "cva size variants consumed by every <Button> in the app"
      via: "buttonVariants size: { default, sm, lg }"
      pattern: "h-8 px-3"
---

<objective>
Tighten the shadcn primitive layer at the source so the entire app inherits Linear-style density with zero per-consumer edits.

Purpose:
- These 7 files are the multipliers. Editing `ui/table.tsx` alone tightens every workspace tab that renders a table (Actions, Risks, Milestones, Decisions, Stakeholders, Members). Editing `ui/button.tsx` tightens every button in every toolbar and form. The primitive pass is the highest-leverage work in Phase 85.1.
- Lock in the density spec from CONTEXT.md "Shared Primitives (`components/ui/*`)" exactly — body rows 32px, headers 36px, inputs 32px, dialog padding 16px, card padding 16px.
- Preserve all behavioral and accessibility properties of the primitives. Only height, padding, and text-size defaults change. No prop signatures change.

Output:
- 7 shadcn primitive files updated with the new size defaults.
- App still builds. Every existing consumer still works.
- Tables, dialogs, cards, and buttons across the app visibly tighten.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/PROJECT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/ROADMAP.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-CONTEXT.md

@/Users/jmiloslavsky/Documents/Panda-Manager/components/ui/table.tsx
@/Users/jmiloslavsky/Documents/Panda-Manager/components/ui/button.tsx

<interfaces>
<!-- Confirmed via direct file inspection 2026-05-12 -->

components/ui/table.tsx (118 lines):
  - TableHead className: "h-12 px-4 text-left align-middle font-medium text-muted-foreground [&:has([role=checkbox])]:pr-0"
  - TableCell className: "p-4 align-middle [&:has([role=checkbox])]:pr-0"
  - Both wrap forwardRef; className prop is merged via cn(). Defaults are the second argument to cn().

components/ui/button.tsx (57 lines):
  - cva buttonVariants size variants:
      default: "h-10 px-4 py-2"
      sm:      "h-9 rounded-md px-3"
      lg:      "h-11 rounded-md px-8"
      icon:    "h-10 w-10"
  - defaultVariants: { variant: "default", size: "default" }

components/ui/input.tsx, select.tsx, dialog.tsx, card.tsx, textarea.tsx — read directly with the Read tool before editing; each is a small shadcn primitive with height/padding in className strings.

Phase 81 KDS Phase 81 baseline still in effect:
  - Inter is body typeface (text-sm = 14px → still default for primitives)
  - JetBrains Mono is for numerals only; primitives use Inter
  - Material Symbols replaced lucide-react; ui/button leaves SVG sizing at `[&_svg]:size-4` (do not change)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten ui/table.tsx, ui/input.tsx, ui/select.tsx, ui/textarea.tsx</name>
  <files>
    /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/table.tsx,
    /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/input.tsx,
    /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/select.tsx,
    /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/textarea.tsx
  </files>
  <action>
**Read each file first with the Read tool to capture current className strings byte-exact. Then edit each per the spec below.**

### `components/ui/table.tsx`

- `TableHead` default className:
  - BEFORE: `"h-12 px-4 text-left align-middle font-medium text-muted-foreground [&:has([role=checkbox])]:pr-0"`
  - AFTER:  `"h-9 px-3 py-1.5 text-left align-middle font-medium text-muted-foreground text-xs [&:has([role=checkbox])]:pr-0"`
  - Notes: 36px header (matches `--kata-row-lg`); add `text-xs` (12px) per CONTEXT.md "metadata 12px"; rest unchanged.

- `TableCell` default className:
  - BEFORE: `"p-4 align-middle [&:has([role=checkbox])]:pr-0"`
  - AFTER:  `"px-3 py-1.5 align-middle text-[13px] [&:has([role=checkbox])]:pr-0"`
  - Notes: yields 32px row height (8px top/bottom + 14px line + 2 implicit). `text-[13px]` from CONTEXT.md "Body text: 13px". Rest unchanged.

- DO NOT touch `Table`, `TableHeader`, `TableBody`, `TableFooter`, `TableRow`, `TableCaption`. They are correct as-is.

### `components/ui/input.tsx`

- Locate the default className string (shadcn pattern; class includes `h-10`).
- Change `h-10` → `h-8` (32px, matches `--kata-input`).
- Change `text-base` → `text-sm` if present (some shadcn templates ship `text-base md:text-sm`; standardize to `text-sm`).
- Leave `px-3 py-2` if present (px is already at 12px target; py is fine for h-8).
- Leave all `placeholder:`, `focus-visible:`, `disabled:` modifiers byte-identical.

### `components/ui/select.tsx`

- `SelectTrigger` default className currently includes `h-10` (shadcn default).
- Change `h-10` → `h-8`.
- Change `text-sm` (verify present; if `text-base` is used, replace with `text-sm`).
- Leave the chevron icon `[&_svg]:size-4` alone — Phase 81 still owns that.
- Leave `SelectContent`, `SelectItem`, `SelectLabel`, `SelectSeparator` paddings unchanged unless a `py-2` exists on `SelectItem` — if it does, leave it (8px is correct for option rows).

### `components/ui/textarea.tsx`

- Default className currently includes `min-h-[80px]` and `px-3 py-2`.
- Change `min-h-[80px]` → `min-h-[64px]`.
- Change `py-2` → `py-1.5`.
- Leave `px-3`, `text-sm`, `flex w-full rounded-md`, focus/disabled modifiers unchanged.

**Across all four files:**
- DO NOT change prop signatures, displayName, or export shape.
- DO NOT add new variants or new size props. Defaults only.
- DO NOT remove the `cn()` wrapper or the `className` merge — consumers still need to override.
- If any `text-base` defaults remain after this pass, replace with `text-sm` to match CONTEXT.md "text-sm everywhere by default".

After editing all four, run:

```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx tsc --noEmit 2>&1 | head -40
```

Acceptable: zero errors, or pre-existing errors unrelated to these four files.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager &amp;&amp; grep -q -- "h-9 px-3 py-1.5" components/ui/table.tsx &amp;&amp; grep -q -- "px-3 py-1.5 align-middle" components/ui/table.tsx &amp;&amp; ! grep -q -- "h-12 px-4" components/ui/table.tsx &amp;&amp; grep -q -- "h-8" components/ui/input.tsx &amp;&amp; ! grep -q -- "h-10" components/ui/input.tsx &amp;&amp; grep -q -- "h-8" components/ui/select.tsx &amp;&amp; grep -q -- "min-h-\\[64px\\]" components/ui/textarea.tsx</automated>
  </verify>
  <done>
- `ui/table.tsx`: `TableHead` is `h-9 px-3 py-1.5 ... text-xs`. `TableCell` is `px-3 py-1.5 ... text-[13px]`. No `h-12` or bare `p-4` defaults remain.
- `ui/input.tsx`: default height is `h-8`; default size is `text-sm`.
- `ui/select.tsx`: `SelectTrigger` default height is `h-8`.
- `ui/textarea.tsx`: `min-h-[64px]` and `py-1.5` present.
- TypeScript compile shows no new errors in any of these four files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten ui/button.tsx, ui/dialog.tsx, ui/card.tsx</name>
  <files>
    /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/button.tsx,
    /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/dialog.tsx,
    /Users/jmiloslavsky/Documents/Panda-Manager/components/ui/card.tsx
  </files>
  <action>
**Read each file first with the Read tool. Then edit each per the spec below.**

### `components/ui/button.tsx`

Update the cva `size` variants object in `buttonVariants`:

- BEFORE:
  ```ts
  size: {
    default: "h-10 px-4 py-2",
    sm: "h-9 rounded-md px-3",
    lg: "h-11 rounded-md px-8",
    icon: "h-10 w-10",
  },
  ```
- AFTER:
  ```ts
  size: {
    default: "h-8 px-3 py-1.5",
    sm: "h-7 rounded-md px-2.5 text-xs",
    lg: "h-10 rounded-md px-6",
    icon: "h-8 w-8",
  },
  ```

Notes:
- `default` = 32px (matches `--kata-input`). `px-3 py-1.5` keeps tap target.
- `sm` = 28px (matches `--kata-row-sm`); add `text-xs` per CONTEXT.md "consistent 28px chip height".
- `lg` = 40px (CONTEXT.md says "`lg` stays at `h-10`" — drop from h-11 to h-10 to fit the spec literally).
- `icon` = 32×32 to match the new `default` height (was 40×40).
- Do NOT touch the base className string (the long ring/transition/svg block).
- Do NOT touch `variant` definitions.
- Do NOT touch `defaultVariants`.
- Do NOT touch `Button.displayName`, `ButtonProps`, or the export.

**Accessibility note (CONTEXT.md "Risks"):** h-7 (28px) on text-only `sm` buttons is below the 24×24 minimum tap target only on width if `px-2.5 text-xs` produces a narrow string. Most button text in the app is ≥24px wide at 12px font size. h-7 height is acceptable for chip-style controls but verify with the human in Plan 05 verification.

### `components/ui/dialog.tsx`

Locate three className strings:

- `DialogContent` default className contains `p-6`. Change `p-6` → `p-4`.
- `DialogHeader` default className contains `p-6 pb-4` (or similar). Change to `p-4 pb-2`.
- `DialogFooter` default className contains `p-6 pt-4` (or similar). Change to `p-4 pt-2`.

If `DialogTitle` and `DialogDescription` have explicit `space-y-*` or `gap-*` between them, leave Phase 81 spacing. If unchanged-shadcn-default, leave alone — the parent `DialogHeader` `p-4 pb-2` is enough.

Do NOT touch `Dialog`, `DialogTrigger`, `DialogPortal`, `DialogOverlay`, `DialogClose`. Their classNames concern positioning/animation, not density.

### `components/ui/card.tsx`

- `CardHeader` default className contains `p-6` (shadcn default). Change to `p-4`.
- `CardContent` default className contains `p-6 pt-0`. Change to `p-4 pt-0`.
- `CardFooter` default className contains `p-6 pt-0`. Change to `p-4 pt-0`.
- Preserve `flex flex-col space-y-1.5` on `CardHeader` if present (CONTEXT.md says `gap-1.5` kept).
- DO NOT touch `Card`, `CardTitle`, `CardDescription`.

**Across both files:**
- Behavior, prop signatures, display names, exports — all unchanged.
- Run `npx tsc --noEmit` after editing.

```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx tsc --noEmit 2>&1 | head -40
```

Acceptable: zero errors, or pre-existing errors unrelated to these three files.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager &amp;&amp; grep -q -- "h-8 px-3 py-1.5" components/ui/button.tsx &amp;&amp; grep -q -- "h-7 rounded-md px-2.5 text-xs" components/ui/button.tsx &amp;&amp; grep -q -- "h-10 rounded-md px-6" components/ui/button.tsx &amp;&amp; ! grep -q -E -- "h-1(0|1) px-4 py-2|h-11 rounded-md px-8" components/ui/button.tsx &amp;&amp; grep -q -- "p-4" components/ui/dialog.tsx &amp;&amp; ! grep -q -- "p-6" components/ui/dialog.tsx &amp;&amp; grep -q -- "p-4" components/ui/card.tsx &amp;&amp; ! grep -q -- "p-6" components/ui/card.tsx</automated>
  </verify>
  <done>
- `ui/button.tsx`: cva size object has exactly four keys (default=h-8 px-3 py-1.5; sm=h-7 …; lg=h-10 …; icon=h-8 w-8). No `h-10 px-4 py-2` or `h-11 …` strings remain in size variants.
- `ui/dialog.tsx`: no `p-6` strings remain; `DialogContent` is `p-4`; `DialogHeader` is `p-4 pb-2`; `DialogFooter` is `p-4 pt-2`.
- `ui/card.tsx`: no `p-6` strings remain; `CardHeader/Content/Footer` are `p-4` (with `pt-0` where applicable).
- TypeScript compile shows no new errors.
  </done>
</task>

</tasks>

<verification>
- Run `npx tsc --noEmit` from Panda-Manager root: no new errors.
- Run `npm run lint` if a lint script exists; expect no new lint errors in the 7 files.
- Manually load the app's `/customer/{id}/actions` page (Actions table). Visually confirm rows are visibly denser (32px) and headers are 36px with smaller font.
- Manually load `/customer/{id}/risks`. Same density inheritance — no per-consumer edits should be needed for the table to look right.
- Open any modal that uses `Dialog` (e.g., "Add Action" from a tab toolbar). Confirm internal padding shrunk from 24px to 16px.
- Click any `Button` and visually confirm it is 32px tall.
- This plan touches NO consumer code. If a consumer overrides the new defaults via className prop (`className="h-12 ..."`), that consumer continues to render at its overridden height — that is expected. Plan 04 will sweep page-level wrappers; per-consumer height overrides will be addressed there if they exist.
</verification>

<success_criteria>
- All goal-backward truths satisfied: tables, inputs, buttons, dialogs, cards, textareas inherit denser defaults from the primitives.
- Zero TypeScript errors introduced.
- Zero prop-signature changes — every existing consumer still type-checks and renders.
- Plans 03 (chrome) and 04 (per-tab sweep) can now rely on these primitive defaults; their work is residual cleanup, not redundant pixel-pushing.
</success_criteria>

<output>
After completion, create `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-02-SUMMARY.md` describing:
- Exact before/after className strings for each of the 7 primitives.
- Confirmation `npx tsc --noEmit` passes (or only pre-existing errors).
- Any class names that were already at the target density and therefore skipped.
- Git commit hash for the change.
</output>
</content>
