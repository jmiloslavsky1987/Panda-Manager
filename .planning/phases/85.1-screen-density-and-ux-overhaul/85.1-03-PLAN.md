---
phase: 85.1-screen-density-and-ux-overhaul
plan: 03
type: execute
wave: 2
depends_on: ["85.1-01"]
files_modified:
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/Sidebar.tsx
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/SidebarProjectItem.tsx
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/PageBar.tsx
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/WorkspaceTabs.tsx
  - /Users/jmiloslavsky/Documents/Panda-Manager/components/SubTabBar.tsx
autonomous: true
requirements:
  - DENSITY-03
must_haves:
  truths:
    - "Sidebar (Command Rail) total vertical footprint shrinks visibly: header 32px, nav rows 28px, search pill 24px, project list rows 28px"
    - "Sidebar width stays 240px (KDS-03 honored)"
    - "PageBar is 36px tall (down from 44px) on every non-suppressed route — amends KDS-04"
    - "WorkspaceTabs top row is px-4 py-1.5 text-sm"
    - "SubTabBar is py-1 text-sm"
    - "data-theme='dark' isolation on Sidebar still works — Command Rail stays dark when html.dark is removed"
    - "All sidebar links (Daily Prep, Weekly Report, KB, Outputs, Settings, Scheduler, Time Tracking) and their data-testid attributes remain functional"
  artifacts:
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/components/Sidebar.tsx"
      provides: "Denser Command Rail (header py-2, nav py-1, project rows 28px, search h-6)"
      contains: "py-1"
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/components/PageBar.tsx"
      provides: "36px page bar (was 44px)"
      contains: "height: 36"
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/components/WorkspaceTabs.tsx"
      provides: "px-4 py-1.5 text-sm tab links"
      contains: "py-1.5"
  key_links:
    - from: "/Users/jmiloslavsky/Documents/Panda-Manager/components/PageBar.tsx"
      to: "every page wrapped by AppChrome (which is most non-customer routes)"
      via: "inline style={{ height: 36 }} on the bar's root div"
      pattern: "height: 36"
    - from: "/Users/jmiloslavsky/Documents/Panda-Manager/components/Sidebar.tsx"
      to: "all sidebar nav data-testid hooks consumed by Playwright/Vitest source-scan tests"
      via: "data-testid attributes preserved on every Link"
      pattern: "data-testid=\"sidebar-"
---

<objective>
Tighten the always-visible global chrome (Sidebar + PageBar + WorkspaceTabs + SubTabBar) to the Linear-density spec from CONTEXT.md, while preserving every functional/data-testid hook so existing tests and the data-theme="dark" isolation continue to work.

Purpose:
- These four (well, five — Sidebar has a co-edited child component) files are the only chrome surfaces touched in Phase 85.1. Every page renders inside this chrome. A tightening here is a tightening everywhere.
- This is the single KDS spec amendment in Phase 85.1: KDS-04 PageBar drops from 44px to 36px. Document the amendment in the SUMMARY.
- Sidebar width (240px, KDS-03) is NOT amended. Width stays; internal rows tighten.
- All sidebar `data-testid` attributes MUST be preserved — they are consumed by source-scan tests from Phases 79, 81, 84 (e.g., `sidebar-daily-prep-link`, `sidebar-weekly-report-link`, `sidebar-knowledge-base-link`, etc.). Breaking these silently breaks tests in Plans 02 and downstream.

Output:
- 5 chrome files updated to the densities specified in CONTEXT.md "Global Chrome Trim".
- App still builds. Every existing test that source-scans these files still passes.
- The visible app chrome shrinks; canvas gains vertical real estate.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-CONTEXT.md

@/Users/jmiloslavsky/Documents/Panda-Manager/components/Sidebar.tsx
@/Users/jmiloslavsky/Documents/Panda-Manager/components/PageBar.tsx

<interfaces>
<!-- Confirmed via direct file inspection 2026-05-12 -->

components/Sidebar.tsx (165 lines, server component):
  - Root <aside data-theme="dark"> w-60 (240px) — width and dark-isolation MUST stay.
  - Header: `flex items-center gap-2 px-4 py-3.5 border-b`. Change py-3.5 → py-2.
  - Search pill wrapper: `px-3 py-2.5`. Inner pill: `h-7`. Change inner pill h-7 → h-6.
  - Top nav (Portfolio + Daily Prep): `px-2 space-y-0.5` on <nav>; each Link is `px-2 py-1.5 rounded text-sm ...`. Change py-1.5 → py-1, space-y-0.5 → space-y-0.
  - Projects label: `px-4 mt-4 mb-1` — leave mt-4 (section gap is intentional); leave mb-1.
  - Project list <ul>: `space-y-0.5 px-2`. Each row rendered by <SidebarProjectItem/>. SidebarProjectItem.tsx must be opened separately to confirm/change its row height (target 28px).
  - Archived <details>: inner Link uses `px-2 py-2`. Change py-2 → py-1 for parity.
  - Additional nav strip (Weekly Report through Time Tracking): each Link is `px-2 py-1.5 ... data-testid="sidebar-{slug}-link"`. Change py-1.5 → py-1. data-testid attributes MUST stay byte-identical.
  - User footer: <SidebarUserIsland/> client component — NOT modified in this plan (separate concern; leave as-is).

components/PageBar.tsx (93 lines, client component):
  - Root div: inline `style={{ height: 44, background: 'var(--kata-surface-container)', borderBottom: ... }}`.
  - className: "flex items-center justify-between px-4". Keep px-4.
  - Title span: text-sm font-medium truncate. Keep.
  - Right-side: ctaSlot + theme-toggle button width:28 height:28 — already correct for h-9 bar.
  - SUPPRESSED_PATHS: ['/login', '/setup', '/customer/'] — KEEP unchanged.

components/WorkspaceTabs.tsx (152 lines, client component, per CONTEXT.md):
  - Tab Link className strings include `px-4 py-2 text-sm ...` on both active and inactive variants.
  - Change py-2 → py-1.5 on both. Keep px-4. Keep text-sm.

components/SubTabBar.tsx:
  - Sub-tab Link strings include `py-1.5`.
  - Change py-1.5 → py-1. Keep text-sm.

Phase 81 baselines that MUST be honored:
  - <aside data-theme="dark"> attribute on Sidebar root — DO NOT remove.
  - Inline style background/border via var(--kata-gray-*) — DO NOT replace with Tailwind classes (data-theme isolation depends on inline vars).
  - PageBar SUPPRESSED_PATHS list — DO NOT modify; /customer/ routes intentionally render their own page bar.
  - MutationObserver on document.documentElement for theme sync — DO NOT touch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim Sidebar.tsx + SidebarProjectItem.tsx + PageBar.tsx</name>
  <files>
    /Users/jmiloslavsky/Documents/Panda-Manager/components/Sidebar.tsx,
    /Users/jmiloslavsky/Documents/Panda-Manager/components/SidebarProjectItem.tsx,
    /Users/jmiloslavsky/Documents/Panda-Manager/components/PageBar.tsx
  </files>
  <action>
**Read each file first with the Read tool to capture current className strings byte-exact. Then make these edits.**

### `components/Sidebar.tsx`

Apply these className changes in-place (preserve everything else byte-identical):

1. Header div:
   - BEFORE: `className="flex items-center gap-2 px-4 py-3.5 border-b"`
   - AFTER:  `className="flex items-center gap-2 px-4 py-2 border-b"`

2. Search pill outer wrapper:
   - BEFORE: `className="px-3 py-2.5"`
   - AFTER:  `className="px-3 py-2"`

3. Search pill inner div:
   - BEFORE: `className="flex items-center gap-2 h-7 px-2 rounded-md"`
   - AFTER:  `className="flex items-center gap-2 h-6 px-2 rounded-md"`

4. Top <nav> (Portfolio + Daily Prep):
   - BEFORE: `className="px-2 space-y-0.5"`
   - AFTER:  `className="px-2 space-y-0"`

5. Portfolio Link:
   - BEFORE: `className="flex items-center gap-2 px-2 py-1.5 rounded text-sm text-white/70 hover:text-white hover:bg-white/5 transition-colors"`
   - AFTER:  `className="flex items-center gap-2 px-2 py-1 rounded text-sm text-white/70 hover:text-white hover:bg-white/5 transition-colors"`

6. Daily Prep Link (same change, KEEP `data-testid="sidebar-daily-prep-link"` byte-identical):
   - py-1.5 → py-1.

7. Project list <ul>:
   - BEFORE: `className="space-y-0.5 px-2"`
   - AFTER:  `className="space-y-0 px-2"`

8. Archived <details> summary:
   - BEFORE: `className="px-2 py-1.5 text-xs uppercase tracking-wider cursor-pointer select-none hover:text-white/50 list-none"`
   - AFTER:  `className="px-2 py-1 text-xs uppercase tracking-wider cursor-pointer select-none hover:text-white/50 list-none"`

9. Archived items <ul>: `space-y-0.5 mt-1` → `space-y-0 mt-1`.

10. Archived items inner Link:
    - BEFORE: `className="flex items-center gap-2.5 px-2 py-2 rounded text-sm transition-colors"`
    - AFTER:  `className="flex items-center gap-2 px-2 py-1 rounded text-sm transition-colors"`

11. Bottom additional-nav strip <div>:
    - BEFORE: `className="px-2 pb-1 space-y-0.5 border-t"`
    - AFTER:  `className="px-2 pb-1 space-y-0 border-t"`

12. Each of the 6 additional-nav Links (Weekly Report, Knowledge Base, Outputs, Settings, Scheduler, Time Tracking):
    - py-1.5 → py-1. KEEP `data-testid="sidebar-{slug}-link"` BYTE-IDENTICAL.

**MUST NOT CHANGE:**
- `<aside data-theme="dark" className="fixed left-0 top-0 h-screen w-60 flex flex-col z-40 overflow-hidden" style={{ ... }}>` — width w-60 (240px) and data-theme are LOCKED.
- Inline `style={{ background: 'var(--kata-gray-950)', borderRight: '1px solid var(--kata-gray-800)' }}` on <aside> — LOCKED (dark isolation depends on it).
- Any `data-testid="sidebar-*"` attribute.
- The Projects section label (mt-4 mb-1 stays — that's the section gap, not row spacing).
- The `<SidebarUserIsland />` import and render.

### `components/SidebarProjectItem.tsx`

Open the file and locate the row className (likely the <Link> wrapping the project name + RAG dot + go-live date).

- If the current padding is `px-3 py-2` (~36px), change to `px-2 py-1.5` (28px effective with text-sm).
- If the current padding is already `py-1` or `py-1.5`, leave the smaller value alone; just ensure the resulting row height computes to roughly 28px (`h-7` equivalent).
- Preserve the RAG dot and go-live date layout exactly.
- Preserve any `data-testid` attribute.
- Preserve the `font-mono` (JBM) on the go-live date.
- Do NOT remove or change the active/hover state classes.

Goal: a project list row that visibly aligns to ~28px height.

### `components/PageBar.tsx`

Single change:

- Inline style on the root div:
  - BEFORE: `style={{ height: 44, background: 'var(--kata-surface-container)', borderBottom: '1px solid var(--kata-stroke-subtle)' }}`
  - AFTER:  `style={{ height: 36, background: 'var(--kata-surface-container)', borderBottom: '1px solid var(--kata-stroke-subtle)' }}`

Add a one-line code comment above the style:

```tsx
// Phase 85.1 amends KDS-04 from 44px to 36px page-bar height (Linear density target).
```

**DO NOT CHANGE:**
- Theme toggle button (`width: 28, height: 28`) — already correct for 36px bar.
- SUPPRESSED_PATHS list.
- usePageBar() hook usage.
- MutationObserver block.
- Title span `text-sm font-medium truncate`.

After all three files are edited, run:

```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx tsc --noEmit 2>&1 | head -40
```

Acceptable: zero new errors.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager &amp;&amp; grep -q -- "px-4 py-2 border-b" components/Sidebar.tsx &amp;&amp; grep -q -- "h-6 px-2 rounded-md" components/Sidebar.tsx &amp;&amp; grep -q -- 'data-testid="sidebar-daily-prep-link"' components/Sidebar.tsx &amp;&amp; grep -q -- 'data-testid="sidebar-weekly-report-link"' components/Sidebar.tsx &amp;&amp; grep -q -- 'data-testid="sidebar-knowledge-base-link"' components/Sidebar.tsx &amp;&amp; grep -q -- 'data-theme="dark"' components/Sidebar.tsx &amp;&amp; grep -q -- "w-60" components/Sidebar.tsx &amp;&amp; ! grep -q -- "py-3.5" components/Sidebar.tsx &amp;&amp; ! grep -q -- "h-7 px-2 rounded-md" components/Sidebar.tsx &amp;&amp; grep -q -- "height: 36" components/PageBar.tsx &amp;&amp; ! grep -q -- "height: 44" components/PageBar.tsx</automated>
  </verify>
  <done>
- Sidebar header is `px-4 py-2` (was `px-4 py-3.5`).
- Sidebar search pill is `h-6` (was `h-7`).
- All `data-testid="sidebar-*"` attributes byte-identical to pre-edit state (Daily Prep, Weekly Report, Knowledge Base, Outputs, Settings, Scheduler, Time Tracking).
- `data-theme="dark"` attribute on the root `<aside>` byte-identical.
- `w-60` (240px) preserved.
- No `py-3.5` or `h-7 px-2 rounded-md` strings remain in Sidebar.tsx.
- PageBar root has `height: 36` (was `height: 44`).
- SidebarProjectItem rows compute to ~28px height.
- TypeScript compile shows no new errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Trim WorkspaceTabs.tsx + SubTabBar.tsx</name>
  <files>
    /Users/jmiloslavsky/Documents/Panda-Manager/components/WorkspaceTabs.tsx,
    /Users/jmiloslavsky/Documents/Panda-Manager/components/SubTabBar.tsx
  </files>
  <action>
**Read each file first.**

### `components/WorkspaceTabs.tsx`

Locate every Link className string that contains `px-4 py-2`. Both active and inactive tab variants will match this pattern (CONTEXT.md confirms `"px-4 py-2"` on each Link).

- Replace every `px-4 py-2` (within a Link className) with `px-4 py-1.5`. Keep `px-4`.
- Verify `text-sm` is present on each Link className; if not, add it (CONTEXT.md: `text-sm`).
- Preserve all `data-testid`, `data-state`, and active/inactive style branches.
- Preserve the bottom-border underline used to indicate active tab (KDS pattern from Phase 81).
- Preserve the `aria-current`, `aria-selected`, or similar accessibility props.

### `components/SubTabBar.tsx`

Locate every Link / button className string that contains `py-1.5`.

- Replace `py-1.5` → `py-1` on each.
- Verify `text-sm` is present; add if missing (CONTEXT.md target).
- Preserve `data-testid` and active-state classes.

**Both files:**

- Behavior, navigation, prop signatures unchanged.
- After editing, run:

```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx tsc --noEmit 2>&1 | head -40
```

If a `vitest` source-scan test for tab structure exists (Phase 81 added several), run:

```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run lib/__tests__/ 2>&1 | tail -40
```

Pre-existing failures are acceptable (Phase 85.1 does not have its own Wave 0 tests, per the planning context). New failures caused by missing `data-testid` or removed structural classes are NOT acceptable — restore the byte-identical attribute and re-run.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager &amp;&amp; grep -q -- "px-4 py-1.5" components/WorkspaceTabs.tsx &amp;&amp; ! grep -q -- "px-4 py-2 " components/WorkspaceTabs.tsx &amp;&amp; grep -q -- "py-1" components/SubTabBar.tsx &amp;&amp; ! grep -q -E -- "py-1\\.5 " components/SubTabBar.tsx</automated>
  </verify>
  <done>
- WorkspaceTabs.tsx: every tab Link className uses `px-4 py-1.5`; no `px-4 py-2 ` substrings remain.
- SubTabBar.tsx: every sub-tab Link/button uses `py-1`; no `py-1.5` substrings remain.
- `text-sm` present on tab classNames in both files.
- All `data-testid` attributes preserved byte-identical.
- TypeScript and existing test suite show no new failures attributable to these two files.
  </done>
</task>

</tasks>

<verification>
- Boot the dev server (`cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run next-only` or equivalent) and visually load `/` (Portfolio Dashboard).
- Confirm: Sidebar is visibly more compact; PageBar at top measures 36px (DevTools); WorkspaceTabs row is shorter.
- Toggle dark mode via the PageBar theme toggle. Confirm the Sidebar stays dark — `data-theme="dark"` isolation still works.
- Navigate to a `/customer/{id}/overview` route. Confirm:
  - The /customer/ PageBar suppression is still working (no global PageBar visible on workspace).
  - WorkspaceTabs row is denser.
  - SubTabBar (if visible for that tab) is denser.
- Run any phase-81 sidebar source-scan tests; confirm no new failures.
- Confirm in DevTools: the global PageBar (where visible) has `height: 36px` literal.
</verification>

<success_criteria>
- All goal-backward truths satisfied (sidebar denser; PageBar 36px; tabs denser; data-theme isolation intact; testids preserved).
- Zero TypeScript or behavioral regressions.
- The single KDS amendment (KDS-04 44px → 36px) is documented in the plan's SUMMARY and ready to surface in Plan 05 verification.
- Plans 02 (primitives) and 04 (sweep) can run/landed independently of this plan.
</success_criteria>

<output>
After completion, create `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-03-SUMMARY.md` describing:
- Exact before/after className strings for each chrome surface (Sidebar header, search pill, nav rows, project rows, additional-nav rows; PageBar height; WorkspaceTabs; SubTabBar).
- Explicit confirmation that the KDS-04 amendment (44px → 36px) is the only spec change in this phase.
- Confirmation that `data-theme="dark"` isolation, w-60 width, and every `data-testid="sidebar-*"` attribute are preserved.
- Git commit hash.
</output>
</content>
