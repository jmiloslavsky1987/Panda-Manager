---
phase: 85.1-screen-density-and-ux-overhaul
plan: "03"
subsystem: ui
tags: [tailwind, sidebar, pagebar, workspace-tabs, density, kds, linear-density]

# Dependency graph
requires:
  - phase: 85.1-01
    provides: Kata spacing tokens and semantic height tokens (--kata-row-sm, --kata-space-*)
provides:
  - Denser Command Rail (Sidebar): header 32px, nav rows 28px, project rows 28px, search pill 24px
  - PageBar trimmed from 44px to 36px (KDS-04 amended)
  - WorkspaceTabs top row py-1.5 text-sm (was py-2)
  - SubTabBar py-1 text-sm (was py-2)
  - All data-testid="sidebar-*" attributes preserved byte-identical
  - data-theme="dark" Sidebar isolation contract intact
affects: [85.1-04, 85.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Linear-density Tailwind class swap: py-1.5->py-1 on sub-tabs, py-2->py-1.5 on top tabs"
    - "KDS-04 amendment documented via inline comment in PageBar.tsx"

key-files:
  created: []
  modified:
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/Sidebar.tsx
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/SidebarProjectItem.tsx
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/PageBar.tsx
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/WorkspaceTabs.tsx
    - /Users/jmiloslavsky/Documents/Panda-Manager/components/SubTabBar.tsx

key-decisions:
  - "[85.1-03] KDS-04 PageBar amended from 44px to 36px — the only KDS spec change in Phase 85.1; documented inline in PageBar.tsx with comment referencing the amendment"
  - "[85.1-03] SubTabBar actual baseline was py-2 (not py-1.5 as stated in plan interfaces) — changed to py-1 per target spec; no behavioral regression"
  - "[85.1-03] WorkspaceTabs active tab className lacked text-sm — added alongside py-2->py-1.5 change for spec compliance"

patterns-established:
  - "data-theme='dark' isolation on Command Rail: preserved via <aside data-theme='dark'> attribute; DO NOT remove"
  - "Sidebar data-testid='sidebar-{slug}-link' on all nav Links: source-scan tests in Phases 79/81/84 depend on these byte-identical"

requirements-completed: [DENSITY-03]

# Metrics
duration: 2min
completed: 2026-05-12
---

# Phase 85.1 Plan 03: Global Chrome Density Trim Summary

**Five chrome files (Sidebar, SidebarProjectItem, PageBar, WorkspaceTabs, SubTabBar) trimmed to Linear density, with KDS-04 PageBar amended from 44px to 36px as the only spec change in Phase 85.1.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-12T14:58:44Z
- **Completed:** 2026-05-12T15:01:02Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Command Rail (Sidebar) vertical footprint visibly shrinks: header 32px, nav rows 28px, search pill 24px, project list rows 28px
- Sidebar width stays 240px (w-60, KDS-03 honored); only internal rows tighten
- PageBar drops from 44px to 36px (KDS-04 amendment, the single spec change in Phase 85.1)
- WorkspaceTabs top row: `px-4 py-1.5 text-sm` (was `px-4 py-2`)
- SubTabBar: `py-1 text-sm` (was `py-2`)
- All `data-testid="sidebar-*"` attributes preserved byte-identical (Daily Prep, Weekly Report, Knowledge Base, Outputs, Settings, Scheduler, Time Tracking)
- `data-theme="dark"` isolation on Sidebar root `<aside>` unchanged

## Before/After className Reference

### Sidebar.tsx
| Surface | Before | After |
|---|---|---|
| Header div | `px-4 py-3.5 border-b` | `px-4 py-2 border-b` |
| Search pill outer | `px-3 py-2.5` | `px-3 py-2` |
| Search pill inner | `h-7 px-2 rounded-md` | `h-6 px-2 rounded-md` |
| Top nav `<nav>` | `px-2 space-y-0.5` | `px-2 space-y-0` |
| Portfolio / Daily Prep Links | `px-2 py-1.5 rounded text-sm ...` | `px-2 py-1 rounded text-sm ...` |
| Project list `<ul>` | `space-y-0.5 px-2` | `space-y-0 px-2` |
| Archived summary | `px-2 py-1.5 text-xs uppercase ...` | `px-2 py-1 text-xs uppercase ...` |
| Archived items `<ul>` | `space-y-0.5 mt-1` | `space-y-0 mt-1` |
| Archived items Link | `gap-2.5 px-2 py-2 rounded text-sm ...` | `gap-2 px-2 py-1 rounded text-sm ...` |
| Additional nav div | `px-2 pb-1 space-y-0.5 border-t` | `px-2 pb-1 space-y-0 border-t` |
| Additional nav Links (x6) | `px-2 py-1.5 rounded text-sm ...` | `px-2 py-1 rounded text-sm ...` |

### SidebarProjectItem.tsx
| Before | After |
|---|---|
| `flex items-start gap-2.5 px-2 py-2 rounded transition-colors group` | `flex items-start gap-2 px-2 py-1 rounded transition-colors group` |

### PageBar.tsx
| Before | After |
|---|---|
| `height: 44` | `height: 36` (KDS-04 amendment) |

### WorkspaceTabs.tsx
| Before | After |
|---|---|
| Active: `px-4 py-2 font-medium ...` | `px-4 py-1.5 text-sm font-medium ...` |
| Inactive: `px-4 py-2 ...` | `px-4 py-1.5 text-sm ...` |

### SubTabBar.tsx
| Before | After |
|---|---|
| Active: `px-4 py-2 text-sm font-medium ...` | `px-4 py-1 text-sm font-medium ...` |
| Inactive: `px-4 py-2 text-sm ...` | `px-4 py-1 text-sm ...` |

## Task Commits

1. **Task 1: Trim Sidebar.tsx + SidebarProjectItem.tsx + PageBar.tsx** - `74971f6f` (feat)
2. **Task 2: Trim WorkspaceTabs.tsx + SubTabBar.tsx** - `764ceac1` (feat)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified
- `/Users/jmiloslavsky/Documents/Panda-Manager/components/Sidebar.tsx` — All nav rows py-1.5->py-1, header py-3.5->py-2, search pill h-7->h-6, space-y-0.5->space-y-0 throughout
- `/Users/jmiloslavsky/Documents/Panda-Manager/components/SidebarProjectItem.tsx` — Link gap-2.5 py-2 -> gap-2 py-1
- `/Users/jmiloslavsky/Documents/Panda-Manager/components/PageBar.tsx` — height 44->36, KDS-04 amendment comment added
- `/Users/jmiloslavsky/Documents/Panda-Manager/components/WorkspaceTabs.tsx` — px-4 py-2->px-4 py-1.5, text-sm added to active variant
- `/Users/jmiloslavsky/Documents/Panda-Manager/components/SubTabBar.tsx` — py-2->py-1 on both active/inactive variants

## Decisions Made

1. **KDS-04 amendment to 36px is the only KDS spec change in Phase 85.1.** Documented inline in PageBar.tsx with a comment: `// Phase 85.1 amends KDS-04 from 44px to 36px page-bar height (Linear density target).` Plan 05 human verification will confirm the visual result.

2. **SubTabBar baseline discrepancy.** The plan interfaces stated the SubTabBar used `py-1.5` but direct file inspection showed it used `py-2`. Applied the change to `py-1` per the density target regardless. The behavior and structure are identical; only padding changed.

3. **WorkspaceTabs active tab missing text-sm.** The active tab variant lacked `text-sm` in the original code while the inactive variant already had it. Added `text-sm` to the active variant alongside the `py-2->py-1.5` change, satisfying the CONTEXT.md spec that both variants use `text-sm`.

## Deviations from Plan

None — plan executed as written. The two noted discrepancies (SubTabBar py-2 not py-1.5, missing text-sm on active tab) were anticipated by the plan's inline note to "verify text-sm is present; add if missing" and did not require deviation-rule handling.

## Issues Encountered

None. Pre-existing TypeScript errors in test files (`archive.test.ts`, `require-project-role.test.ts`, `discovery-approve-audit.test.ts`) and pre-existing vitest failures in `lib/__tests__/require-project-role.test.ts` were confirmed to be unrelated to this plan's changes.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Global chrome surfaces (Sidebar, PageBar, WorkspaceTabs, SubTabBar) are now at Linear density.
- Plan 85.1-04 (per-tab density sweep) can proceed — these chrome changes cascade into every tab view automatically.
- Plan 85.1-05 (human verification) has what it needs to verify: Sidebar compactness, PageBar 36px height, tab row density, dark theme isolation, all data-testid hooks still present.
- KDS-04 amendment (44px -> 36px) is documented and ready to surface in Plan 05 verification report.

---
*Phase: 85.1-screen-density-and-ux-overhaul*
*Completed: 2026-05-12*
