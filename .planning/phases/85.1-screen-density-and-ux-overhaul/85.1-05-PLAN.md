---
phase: 85.1-screen-density-and-ux-overhaul
plan: 05
type: execute
wave: 4
depends_on: ["85.1-01", "85.1-02", "85.1-03", "85.1-04"]
files_modified: []
autonomous: false
requirements:
  - DENSITY-VERIFY
must_haves:
  truths:
    - "Human has loaded the running app and visually confirmed Linear-style density across all primary surfaces"
    - "PageBar measures 36px in DevTools (KDS-04 amendment)"
    - "Sidebar measures 240px wide and rows are visibly tighter (KDS-03 width unchanged)"
    - "Table rows are 32px, headers 36px, across at least 3 sampled tabs (Actions, Risks, Stakeholders)"
    - "Buttons render at 32px default / 28px sm / 40px lg"
    - "Dialogs / modals render with 16px internal padding (was 24px)"
    - "WBS grid is byte-identical to Phase 85 — no internal regression"
    - "Gantt is byte-identical to Phase 85 — no internal regression"
    - "data-theme='dark' isolation on Sidebar still works — light mode flips canvas but not rail"
    - "No 24x24 click-target violations on h-7 sm buttons that the human encounters in normal use"
  artifacts:
    - path: "/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-05-SUMMARY.md"
      provides: "Human verification record"
      contains: "VERIFIED"
  key_links:
    - from: "human verification"
      to: "Plans 01-04 must_haves"
      via: "Each must_have truth confirmed visually or via DevTools by the human"
      pattern: "VERIFIED|FAILED"
---

<objective>
Block until the human visually confirms Phase 85.1 has hit the Linear-density target across every primary surface and that the two hard exclusions (WBS, Gantt) are intact.

Purpose:
- Phase 85.1 ran without RESEARCH.md and without VALIDATION.md (user opted to skip both). There is no Nyquist validation gate, no automated visual regression. This human checkpoint is the ONLY backstop against visual regressions or accidental over-tightening.
- Verify the single KDS amendment (KDS-04 44px -> 36px page-bar) is intentional and looks correct next to Phase 81's 64px hero band and 28px KPI numerals (CONTEXT.md "Risks: KDS visual rhythm").
- Verify the WBS and Gantt exclusions held — these are the most likely regressions because of how easy it is to accidentally tighten a wrapper that affects layout.
- Verify h-7 (28px) sm buttons still meet a reasonable click-target threshold (24x24 min, per CONTEXT.md "Dialog/Modal forms" risk).
- Verify the data-theme="dark" isolation on the Command Rail still works after Sidebar edits.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-CONTEXT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-01-SUMMARY.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-02-SUMMARY.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-03-SUMMARY.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of Phase 85.1 density across the app</name>
  <files>(human verification only; no files modified)</files>
  <action>
**This is a blocking human-verify checkpoint. Pause execution and surface the script below to the user. Resume only when the user types one of the resume signals.**

### What Plans 01-04 built

- Plan 01 added spacing/height tokens to `components/kata-tokens.css` and wired them through `@theme inline` in `app/globals.css` (transparent override — `p-3` still = 12px).
- Plan 02 tightened 7 shadcn primitives (`ui/table`, `ui/input`, `ui/select`, `ui/button`, `ui/dialog`, `ui/card`, `ui/textarea`).
- Plan 03 tightened global chrome (Sidebar header/search/nav/project rows, PageBar 44px -> 36px, WorkspaceTabs `px-4 py-1.5`, SubTabBar `py-1`).
- Plan 04 swept ~21 customer tab pages + 9 global pages (`p-6` -> `p-4`, `space-y-6` -> `space-y-3`).
- WBS grid (`WbsGrid.tsx`, `WbsPageClient.tsx`) and Gantt (`GanttChart.tsx`) internals explicitly NOT touched.

### Human verification script

**Setup:**

1. Boot the dev server. From the Panda-Manager root:
   ```bash
   cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run next-only
   ```
   (Or `npm run dev` if Redis is required for the pages you sample.)

2. Open the app in a browser at `http://localhost:3000`.

3. Log in if needed (admin@localhost.dev / admin123 per memory).

**A. Chrome density (~2 minutes):**

1. Open DevTools (Cmd+Option+I).
2. Inspect the top PageBar element. Confirm `height: 36px` (not 44px). This is the single KDS-04 amendment.
3. Inspect the left `<aside data-theme="dark">`. Confirm `width: 240px` (w-60). KDS-03 unchanged.
4. Confirm `data-theme="dark"` attribute is present on the `<aside>` element.
5. Click the theme toggle in the PageBar. Confirm:
   - The main canvas inverts (background changes).
   - The Command Rail STAYS DARK regardless of light/dark canvas state.
   - Toggling back works.
6. Visually scan the sidebar: header, search pill, top nav (Portfolio + Daily Prep), project list rows, additional nav strip. Confirm rows are tight (~28px each). Compare against a screenshot from before Phase 85.1 if available.

**B. Primitive density inheritance (~3 minutes):**

1. Navigate to `/customer/{any-project-id}/actions`.
2. Inspect a `<th>` element. Confirm `height: 36px` (was 48px). Font is `text-xs` (12px).
3. Inspect a `<td>` element. Confirm height yields ~32px row (`px-3 py-1.5`; body text 13px).
4. Navigate to `/customer/{id}/risks`. Same table density — no per-consumer edit was needed; confirm it inherited from `ui/table.tsx`.
5. Navigate to `/customer/{id}/stakeholders`. Same.
6. Trigger a Dialog (e.g., "Add Action" or "Edit Risk" button). Inspect `DialogContent`. Confirm `padding: 16px` (was 24px).
7. Inspect a default `<Button>` (any toolbar). Confirm `height: 32px`. Inspect a small button (chip / pill area). Confirm `height: 28px`. Inspect a primary "Save" / "Submit" Button at size lg. Confirm `height: 40px`.
8. Inspect an `<Input>` field. Confirm `height: 32px`. Tab focus ring is still visible.

**C. WBS and Gantt exclusion verification (~2 minutes):**

1. Navigate to `/customer/{id}/wbs`. Confirm:
   - The OUTER page wrapper is tighter (16px padding around the grid).
   - The WBS grid INTERNALS look IDENTICAL to before Phase 85.1 — row heights, cell paddings, column widths, header style all preserved from Phase 85.
   - Try editing a cell — inline edit still works (Tab, Enter, indent/outdent).
2. Navigate to `/customer/{id}/gantt`. Confirm:
   - Outer wrapper tighter.
   - Bar heights, milestone markers, baseline ghost bars, dependency arrows all IDENTICAL to before Phase 85.1.
   - Scrolling and zoom still work.

**D. Accessibility / click-target spot check (~1 minute):**

1. Find an h-7 (28px) button in the app — these are typically chip/pill style controls or small "Cancel" buttons. Filter chips in /search or in any tab toolbar are good candidates.
2. Try clicking it. If it feels too small or you miss-click more than once in normal use, note it in the SUMMARY as a 24x24 violation that needs a small targeted fix (Plan 05 SUMMARY records it as a follow-up).
3. Confirm inputs inside a "Add Action" or "Edit Risk" modal are still clickable; tap targets feel reasonable.

**E. Per-tab regression spot check (~3 minutes):**

Cycle through 6-8 tabs and confirm nothing looks broken:

- `/` (Portfolio Dashboard): hero stat band still has 64px JBM numerals intact; project grid renders.
- `/customer/{id}/overview`: KPI strip still 28px JBM numerals (Phase 81 KDS-06); focus + risks 2-column grid present.
- `/customer/{id}/plan`: renders without layout collapse.
- `/customer/{id}/chat`: ChatPanel renders; messages render with new tighter padding; tool confirm cards still appear correctly.
- `/customer/{id}/skills`: skill list + output area render.
- `/customer/{id}/context`: artifact previews render.
- `/daily-prep`: event cards render; multi-select toolbar works.
- `/outputs`: output preview renders; docx-preview inline still functions.

**F. Confirm the original pain is gone:**

The CONTEXT.md preamble says: "screen real-estate pain is hurting daily use." Subjectively answer: do you now fit visibly more content on screen than before Phase 85.1? If yes, the phase succeeded. If no, note which surfaces still feel loose.

### Resume signal

Type one of:

- `approved` — Phase 85.1 verified, ready to close.
- `approved with notes: {notes}` — verified, but with follow-up items captured in the SUMMARY (e.g., one h-7 button feels too small, one tab missed the sweep).
- `failed: {description}` — a real regression. Specify which plan to revisit (most likely Plan 04 if a sweep over-tightened, or Plan 03 if chrome broke a testid).
  </action>
  <verify>
    <automated>test -f "/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-05-SUMMARY.md" &amp;&amp; grep -qE "approved|approved with notes|failed" "/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-05-SUMMARY.md"</automated>
  </verify>
  <done>
- Human typed `approved` or `approved with notes` (or `failed: ...` which routes to gap closure).
- `85.1-05-SUMMARY.md` exists and records the outcome plus any follow-up notes.
- Each `must_haves.truths` entry from Plan 05 is tagged VERIFIED or FAILED in the SUMMARY.
  </done>
</task>

</tasks>

<verification>
- Plan 05 is a human gate; verification is the human's response captured in the SUMMARY.
- If `approved` or `approved with notes`, the orchestrator marks Phase 85.1 complete in ROADMAP.md and STATE.md.
- If `failed`, the orchestrator routes to gap closure (`/gsd:plan-phase 85.1 --gaps`) with the failure notes as the gap source.
</verification>

<success_criteria>
- Human typed `approved` (or `approved with notes`).
- 85.1-05-SUMMARY.md records the verification outcome plus any notes.
- Phase 85.1 marked complete in ROADMAP.md and STATE.md.
- Any deferred follow-ups (e.g., chip-size accessibility fix) added to ROADMAP.md as a future micro-phase if substantive.
</success_criteria>

<output>
After completion, create `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/85.1-screen-density-and-ux-overhaul/85.1-05-SUMMARY.md` describing:
- Verification outcome (approved / approved with notes / failed).
- Each truth from Plans 01-04 must_haves with a VERIFIED or FAILED tag.
- KDS-04 amendment confirmation (PageBar measured 36px in DevTools).
- WBS and Gantt internals confirmed visually identical to Phase 85 baseline.
- Any follow-up items captured.
- Final Phase 85.1 disposition (complete / gap-closure-needed).
</output>
</content>
