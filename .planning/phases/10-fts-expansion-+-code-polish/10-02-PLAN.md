---
phase: 10-fts-expansion-+-code-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - tests/e2e/phase10.spec.ts
  - bigpanda-app/tests/skill-run-settings.test.ts
autonomous: false
requirements:
  - SRCH-01
  - SET-02

must_haves:
  truths:
    - "E2E SRCH-01 tests pass: search returns results from onboarding_steps and time_entries tables"
    - "E2E INT-UI-01 test passes: /skills/custom link is absent from the rendered skills tab"
    - "Unit SET-02 tests pass: skill-run.ts resolves SKILLS_DIR from settings when set, falls back when not"
    - "Full Playwright suite passes (no regressions on 23+ previously green tests)"
    - "User confirms search results show onboarding and time-entry data in the UI"
  artifacts:
    - path: "tests/e2e/phase10.spec.ts"
      provides: "Active E2E tests for SRCH-01 + INT-UI-01 (not stubs)"
    - path: "bigpanda-app/tests/skill-run-settings.test.ts"
      provides: "Active vitest unit tests for SET-02 (not stubs)"
  key_links:
    - from: "tests/e2e/phase10.spec.ts SRCH-01 tests"
      to: "/search page search results"
      via: "Playwright page.goto('/search?q=...') + result assertion"
      pattern: "data-testid.*search-result"
    - from: "bigpanda-app/tests/skill-run-settings.test.ts"
      to: "skill-run.ts SKILLS_DIR resolution logic"
      via: "vi.mock('../../lib/settings-core') + direct function test"
      pattern: "readSettings.*mockResolvedValue"
---

<objective>
Activate the Wave 0 RED test stubs from plan 10-01 into green passing tests, then run human verification to confirm the three gaps (FTS coverage, skill path, broken link) are fully closed.

Purpose: Provide automated and human proof that SRCH-01, SET-02, and INT-UI-01 are resolved. Phase gate before marking Phase 10 complete.
Output: Active GREEN test suite for phase10; human sign-off recorded.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-fts-expansion-+-code-polish/10-RESEARCH.md
@.planning/phases/10-fts-expansion-+-code-polish/10-VALIDATION.md
@.planning/phases/10-fts-expansion-+-code-polish/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate E2E tests + unit tests (RED → GREEN)</name>
  <files>tests/e2e/phase10.spec.ts, bigpanda-app/tests/skill-run-settings.test.ts</files>
  <behavior>
    - SRCH-01 E2E: navigating to /search?q={term} where term exists in onboarding_steps returns at least one result card showing section "Onboarding Steps"
    - SRCH-01 E2E: navigating to /search?q={term} where term exists in time_entries returns at least one result card showing section "Time Entries"
    - INT-UI-01 E2E: navigating to /customer/{id}/skills shows no anchor element with href containing "/skills/custom"
    - SET-02 unit: when readSettings() resolves to { skill_path: '/custom/skills/path' }, skillRunJob resolves SKILLS_DIR to '/custom/skills/path'
    - SET-02 unit: when readSettings() resolves to { skill_path: '' }, skillRunJob resolves SKILLS_DIR to the __dirname-relative fallback (path ends with '/skills')
  </behavior>
  <action>
Replace all stub `expect(false, 'stub').toBe(true)` assertions with real test bodies.

**tests/e2e/phase10.spec.ts — activate 3 tests:**

Test 1 (SRCH-01 — onboarding_steps):
- Use assert-if-present pattern (consistent with phase8/phase9): navigate to `/search?q=onboarding` (or a term likely to match onboarding step data)
- Wait for the search results container (data-testid or selector from Phase 8 search page implementation)
- If results are present, assert that at least one result has text matching "Onboarding Steps" or "onboarding_steps"
- If no results (empty DB / migration not applied), test passes structurally — log a warning but do not fail
- Before writing this test, read `bigpanda-app/app/search/page.tsx` or the search results component to confirm the correct data-testid selectors

Test 2 (SRCH-01 — time_entries):
- Same assert-if-present pattern; navigate to `/search?q=hours` or a term likely in time_entries.description
- Assert result card shows "Time Entries" section label if results exist

Test 3 (INT-UI-01 — /skills/custom absent):
- Navigate to a project's skills tab: `/customer/1/skills` (use project id 1; assert-if-page-loads pattern)
- Assert that no `<a>` element with href containing "/skills/custom" is present in the DOM
- This test should ALWAYS pass after the link is removed (not an assert-if-present test; the absence of the link is structural)

**bigpanda-app/tests/skill-run-settings.test.ts — activate 2 unit tests:**

Use vitest `vi.mock` to mock `../../lib/settings-core` and test the SKILLS_DIR resolution logic. Since SKILLS_DIR is now resolved inside `skillRunJob()`, extract the resolution logic OR test it by checking what path is passed to `orchestrator.run()`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.mock('../../lib/settings-core', () => ({
  readSettings: vi.fn(),
}));
vi.mock('../../lib/skill-orchestrator', () => ({
  SkillOrchestrator: vi.fn().mockImplementation(() => ({
    run: vi.fn().mockResolvedValue(undefined),
  })),
}));
// Also mock db and any other imports skill-run.ts uses

// Test 1: absolute skill_path honored
const { readSettings } = await import('../../lib/settings-core');
vi.mocked(readSettings).mockResolvedValue({ skill_path: '/custom/skills', ... });
// Call skillRunJob with a minimal job object, capture orchestrator.run args
// Assert skillsDir arg passed to orchestrator.run is '/custom/skills'

// Test 2: empty skill_path falls back to __dirname-relative
vi.mocked(readSettings).mockResolvedValue({ skill_path: '', ... });
// Assert skillsDir arg ends with '/skills'
```

If mocking the full skill-run.ts module is complex (many deps), use a simpler approach: extract the path-resolution logic into a pure helper function `resolveSkillsDir(skillPath: string): string` in skill-run.ts, export it, and test only that helper. This is cleaner and avoids mocking the entire dependency chain.

Note: If the pure helper extraction approach is used, also update skill-run.ts to call `resolveSkillsDir(settings.skill_path)` instead of the inline logic. This does not change observable behavior.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase10.spec.ts --reporter=list 2>&1 | tail -10</automated>
  </verify>
  <done>All 3 E2E tests in phase10.spec.ts pass (or skip gracefully via assert-if-present). Both vitest unit tests pass. `npx playwright test --reporter=list` full suite shows no regressions from prior passing tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — FTS expansion + SET-02 + link removal</name>
  <what-built>
    Phase 10 implementation complete:
    1. Migration 0009 — adds FTS (search_vec + GIN + trigger + backfill) to onboarding_steps, onboarding_phases, integrations, time_entries
    2. searchAllRecords() in queries.ts — 4 new UNION ALL arms; search now covers 12 tables
    3. skill-run.ts — SKILLS_DIR resolved at runtime from settings.skill_path with __dirname fallback (SET-02)
    4. SkillsTabClient.tsx — "Run custom skill" link targeting /skills/custom removed
    5. E2E tests passing for all 3 behaviors; unit tests passing for SET-02
  </what-built>
  <how-to-verify>
    Before verifying, ensure migration 0009 has been applied:
    ```
    cd bigpanda-app && DATABASE_URL=postgresql://localhost:5432/bigpanda_app npx drizzle-kit migrate
    ```

    1. **FTS expansion — onboarding steps:**
       - Visit http://localhost:3000/search
       - Search for the name of any onboarding step you know exists in a project (e.g. "Discovery", "kickoff", or "Integration")
       - Verify: at least one result card appears with section label "Onboarding Steps"

    2. **FTS expansion — time entries:**
       - Search for text from a time entry description (e.g. "meeting", "call", "review")
       - Verify: at least one result card appears with section label "Time Entries"

    3. **FTS expansion — integrations:**
       - Search for an integration tool name you've added (e.g. "Slack", "Salesforce")
       - Verify: result card appears with section label "Integrations"

    4. **SET-02 — skill path setting honored:**
       - Visit http://localhost:3000/settings
       - Confirm the "Skill file location" field shows the current path (default: ~/.claude/get-shit-done/ or similar)
       - Change it to a test path like `/tmp/test-skills`
       - Trigger an on-demand skill run (any skill on any project)
       - Verify in the Next.js server log: the skill attempts to read SKILL.md from `/tmp/test-skills/{skill-name}/SKILL.md`
       - Reset the path back to the correct value

    5. **/skills/custom link removed:**
       - Visit any project's skills tab (e.g. http://localhost:3000/customer/1/skills)
       - Scroll to the bottom of the skill list
       - Verify: no "Run custom skill" link is visible anywhere on the page

    6. **Full E2E suite:**
       - Run: `npx playwright test --reporter=list`
       - Verify: all previously passing tests remain green; no regressions
  </how-to-verify>
  <resume-signal>Type "approved" when all 6 checks pass, or describe any issues found</resume-signal>
  <action>Human reviews the 6 verification steps above in a running application instance and confirms all behaviors match expected outcomes.</action>
  <verify><automated>CHECKPOINT — human verification required; no automated command</automated></verify>
  <done>User types "approved" confirming all 6 checks pass; phase 10 marked complete.</done>
</task>

</tasks>

<verification>
Phase 10 complete when:
1. `npx playwright test tests/e2e/phase10.spec.ts --reporter=list` — all 3 tests pass (or skip gracefully)
2. `cd bigpanda-app && npx vitest run tests/skill-run-settings.test.ts --reporter=verbose` — both SET-02 tests pass
3. `npx playwright test --reporter=list` full suite — no regressions from prior phases
4. Human verification: FTS returns results from onboarding and time-entry tables; skill path setting honored; /skills/custom link absent
5. REQUIREMENTS.md SRCH-01 and SET-02 can be marked complete
</verification>

<success_criteria>
- E2E phase10.spec.ts: 3/3 tests green (assert-if-present pattern; pass even on empty DB)
- Vitest skill-run-settings.test.ts: 2/2 tests green (SET-02 path resolution logic verified)
- Full playwright suite: no regressions
- Human confirmed: FTS covers all 12 tables including Phase 5.1/5.2 additions; skill_path honored; broken nav link gone
- SRCH-01 and SET-02 marked Complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/10-fts-expansion-+-code-polish/10-02-SUMMARY.md`
</output>
