---
phase: 03-write-surface-+-plan-builder
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/phase3.spec.ts
autonomous: true
requirements: [WORK-02, PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06, PLAN-07, PLAN-08, PLAN-09, PLAN-10, PLAN-11]

must_haves:
  truths:
    - "tests/e2e/phase3.spec.ts exists and all tests run (all fail RED — that is correct at Wave 0)"
    - "Every Phase 3 user-observable behavior has a named test with a requirement ID in the test name"
    - "Action edit tests cover: opening a modal, changing fields, saving, and verifying xlsx sync signal"
    - "Plan Builder tests cover: creating a task, Phase Board, Task Board, Gantt, Swimlane, import, export, templates"
  artifacts:
    - path: "tests/e2e/phase3.spec.ts"
      provides: "Playwright E2E spec covering all Phase 3 behaviors"
      min_lines: 100
  key_links:
    - from: "tests/e2e/phase3.spec.ts"
      to: "http://localhost:3000"
      via: "Playwright page.goto()"
      pattern: "page\\.goto"
---

<objective>
Create the Phase 3 E2E test stub file. All tests are written first (RED) — they will remain failing until implementation plans complete.

Purpose: Nyquist compliance — every subsequent plan's verify command must have a real test to point at. Writing stubs now means implementation plans can use `npx playwright test tests/e2e/phase3.spec.ts --grep "WORK-02"` as their verify commands.
Output: `tests/e2e/phase3.spec.ts` with named tests for all 12 Phase 3 requirements. All tests fail — RED baseline established.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-write-surface-+-plan-builder/03-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 3 E2E test stubs</name>
  <files>tests/e2e/phase3.spec.ts</files>
  <action>
Create `tests/e2e/phase3.spec.ts` at project root level (same location as phase2.spec.ts). Playwright and Chromium are already installed from Phase 2. Do NOT reinstall.

The base URL is `http://localhost:3000`. All tests navigate to project ID 1 where applicable (matches Phase 2 convention). Every test body must call `expect(false, 'stub — implement Phase 3').toBe(true)` so tests are RED and visible in the report (not silently skipped).

Organize the spec into describe blocks:

**describe('WORK-02: Action Editing with xlsx dual-write'):**
- `'WORK-02: Action edit modal opens when clicking an action row'` — navigate to `/customer/1/actions`, click the first row with `[data-testid="action-row"]`, assert `[data-testid="action-edit-modal"]` appears
- `'WORK-02: Saving action change reflects in the actions list'` — open edit modal, change the status field to 'completed', click save, assert modal closes and the action row shows updated status
- `'WORK-02: Action save returns 200 and no error toast is shown'` — intercept `PATCH /api/actions/*`, verify 200 response, assert no `[data-testid="error-toast"]` is visible

**describe('PLAN-01: Task Creation'):**
- `'PLAN-01: Create task modal opens from Plan Tasks page'` — navigate to `/customer/1/plan/tasks`, click `[data-testid="create-task-btn"]`, assert `[data-testid="task-edit-modal"]` is visible
- `'PLAN-01: New task appears in task list after creation'` — fill task form (title, owner, due, priority), click save, assert the new task title appears in `[data-testid="task-list"]`

**describe('PLAN-02/03: Phase Board and Task Board'):**
- `'PLAN-02: Phase Board renders at /customer/1/plan/board'` — navigate to `/customer/1/plan/board`, assert `[data-testid="phase-board"]` is visible with at least one column
- `'PLAN-03: Task Board renders at /customer/1/plan/tasks'` — navigate to `/customer/1/plan/tasks`, assert `[data-testid="task-board"]` has columns: To Do, In Progress, Blocked, Done
- `'PLAN-02: Dragging a card to a different phase column updates the task phase'` — drag `[data-testid="task-card"]:first-child` to a different column droppable, assert PATCH to `/api/tasks/*` is called

**describe('PLAN-04/PLAN-06: Gantt Timeline and Dependencies'):**
- `'PLAN-04: Gantt tab renders at /customer/1/plan/gantt'` — navigate to `/customer/1/plan/gantt`, assert `[data-testid="gantt-container"]` is visible and contains an SVG element
- `'PLAN-06: Task with blocked_by shows dependency arrow in Gantt'` — assert SVG contains `.dependency-arrow` or `.arrow` element when tasks with blocked_by exist

**describe('PLAN-05: Swimlane View'):**
- `'PLAN-05: Swimlane view renders at /customer/1/plan/swimlane'` — navigate to `/customer/1/plan/swimlane`, assert `[data-testid="swimlane-view"]` is visible with at least one swim lane row

**describe('PLAN-07: Bulk Operations'):**
- `'PLAN-07: Selecting multiple tasks enables bulk action toolbar'` — on `/customer/1/plan/tasks`, check two task checkboxes, assert `[data-testid="bulk-toolbar"]` appears with at least one action button
- `'PLAN-07: Bulk reassign owner applies to all selected tasks'` — select 2 tasks, click "Reassign Owner" in bulk toolbar, fill new owner, confirm, assert PATCH to `/api/tasks-bulk` is called

**describe('PLAN-08: Task Templates'):**
- `'PLAN-08: Template button opens template picker on Phase Board'` — navigate to `/customer/1/plan/board`, click `[data-testid="template-btn"]`, assert `[data-testid="template-picker"]` is visible
- `'PLAN-08: Instantiating Biggy Activation template creates multiple tasks'` — click "Biggy Activation" template, confirm, assert multiple new task rows appear in the task list

**describe('PLAN-09: Progress Rollup'):**
- `'PLAN-09: Completing a task updates workstream percent_complete'` — mark a task as done via PATCH, navigate to `/customer/1/overview`, assert workstream progress bar value has changed

**describe('PLAN-10/11: Excel Import and Export'):**
- `'PLAN-10: Importing a KAISER-format xlsx populates tasks'` — POST to `/api/plan-import` with a test xlsx file fixture, assert 201 response and tasks appear in `/customer/1/plan/tasks`
- `'PLAN-11: Exporting plan produces a downloadable xlsx'` — navigate to `/customer/1/plan/board`, click `[data-testid="export-btn"]`, assert download is triggered (response Content-Type is application/vnd.openxmlformats-officedocument.spreadsheetml.sheet)

Note: All tests must have `expect(false, 'stub — implement Phase 3').toBe(true)` as the FIRST assertion so they are visibly RED. The navigations and selectors above describe what WILL be true after implementation — they are aspirational stubs.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase3.spec.ts --reporter=list 2>&1 | head -40 || true</automated>
  </verify>
  <done>
    - `tests/e2e/phase3.spec.ts` exists with all named tests organized in describe blocks
    - Every test body calls `expect(false, 'stub — implement Phase 3').toBe(true)` so all tests fail RED
    - All 12 requirement IDs (WORK-02, PLAN-01 through PLAN-11) appear in test names
    - Running the spec exits with failures (RED) — this is the correct Wave 0 state
  </done>
</task>

</tasks>

<verification>
Run: `cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase3.spec.ts --reporter=list 2>&1 | tail -10`
Expected: All tests fail (RED) — this is the correct Wave 0 state.
</verification>

<success_criteria>
`tests/e2e/phase3.spec.ts` exists with named tests for all Phase 3 requirements. All tests fail — the RED baseline is established. Subsequent plans will flip tests GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/03-write-surface-+-plan-builder/03-01-SUMMARY.md`
</output>
