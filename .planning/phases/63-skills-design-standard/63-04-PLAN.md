---
phase: 63-skills-design-standard
plan: 04
type: execute
wave: 3
depends_on:
  - 63-02
  - 63-03
files_modified: []
autonomous: false

requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-04

must_haves:
  truths:
    - "Skills tab renders without error with server-passed skill data"
    - "No ALL_SKILLS / WIRED_SKILLS / INPUT_REQUIRED_SKILLS hardcodes remain in the UI"
    - "Skills with valid front-matter render normally — no badge"
    - "Any non-compliant skill (if any remain) shows Fix required badge with opacity-60 treatment"
    - "Previously grayed-out skills (elt-*, team-engagement-map, workflow-diagram, risk-assessment, qbr-prep, biggy-weekly-briefing) are now runnable"
    - "stakeholder-comms and onboarding-checklist do not appear anywhere on the Skills tab"
    - "Running a functional skill (e.g. morning-briefing) completes without front-matter content appearing in the output"
  artifacts:
    - path: "bigpanda-app/app/customer/[id]/skills/page.tsx"
      provides: "Server-side skill loading confirmed functional"
    - path: "bigpanda-app/components/SkillsTabClient.tsx"
      provides: "Prop-driven rendering confirmed in browser"
  key_links:
    - from: "Skills tab"
      to: "SKILLS-DESIGN-STANDARD.md"
      via: "Fix required badge tooltip text references it"
      pattern: "SKILLS-DESIGN-STANDARD"
---

<objective>
Human verification gate. Confirm the Skills tab renders correctly with server-sourced skill data, previously non-functional skills appear as runnable, removed skills are gone, and the "Fix required" badge system is wired correctly.

Purpose: Plans 02 and 03 are code-only. This checkpoint ensures the end-to-end result — from .md files through server parsing through UI rendering — is visually and functionally correct before the phase closes.

Output: Human approval that all Phase 63 success criteria are met.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/63-skills-design-standard/63-CONTEXT.md
@.planning/phases/63-skills-design-standard/63-02-SUMMARY.md
@.planning/phases/63-skills-design-standard/63-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification — TypeScript clean and structural checks</name>
  <files></files>
  <action>
Run a full TypeScript check and confirm the build is clean before presenting for human verification:

```bash
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx tsc --noEmit
```

If TypeScript errors exist, fix them before proceeding to the human checkpoint. Do not present a broken build for human review.

Also confirm:
- grep -r "ALL_SKILLS" bigpanda-app/components/SkillsTabClient.tsx returns no matches
- grep -r "WIRED_SKILLS" bigpanda-app/components/SkillsTabClient.tsx returns no matches
- grep -r "INPUT_REQUIRED_SKILLS" bigpanda-app/components/SkillsTabClient.tsx returns no matches
- grep "startsWith.*---\|replace.*---" bigpanda-app/lib/skill-orchestrator.ts shows the front-matter strip
- All 7 new job handler files exist in bigpanda-app/worker/jobs/
- worker/index.ts has all 7 new JOB_HANDLERS entries
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx tsc --noEmit && echo "BUILD_CLEAN"</automated>
  </verify>
  <done>TypeScript build clean. Hardcode constants confirmed absent. Front-matter strip confirmed present. All 7 handler files confirmed present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Skills tab UI and skill execution</name>
  <files></files>
  <action>
Present the following verification steps to the user. Wait for their response before marking this plan complete.

What was built (summary for user context):
- SKILLS-DESIGN-STANDARD.md written in bigpanda-app/skills/
- 14 skill .md files have YAML front-matter (11 migrated + 3 new)
- SkillOrchestrator strips front-matter before sending to Claude
- skills/page.tsx parses front-matter server-side and passes SkillMeta[] to SkillsTabClient
- SkillsTabClient renders from props — no hardcoded ALL_SKILLS / WIRED_SKILLS arrays
- "Fix required" badge + opacity-60 for non-compliant skills
- 7 new BullMQ handlers for previously non-functional skills
- stakeholder-comms and onboarding-checklist removed from catalog

Steps to verify:
1. Start the dev server: cd bigpanda-app && npm run next-only
2. Navigate to any project's Skills tab (e.g. /customer/1/skills)
3. Confirm skill catalog: Weekly Customer Status, Meeting Summary, Morning Briefing, Handoff Doc Generator, ELT External Status, ELT Internal Status, Team Engagement Map, Workflow Diagram, Risk Assessment, QBR Prep, Biggy Weekly Briefing, Customer Project Tracker — all visible
4. Confirm ABSENT: "Stakeholder Comms" and "Onboarding Checklist" do NOT appear
5. Confirm no "Fix required" badges (all skills compliant after Plan 01 migration)
6. Run Morning Briefing — output should NOT start with "---" or YAML content
7. Optional: Try Risk Assessment or QBR Prep — should queue and run without SKILL_NOT_FOUND error
  </action>
  <verify>
    <automated>MISSING — human visual verification required for UI rendering and skill output content</automated>
  </verify>
  <done>Human types "approved". Skills tab renders all expected skills. Removed skills absent. Morning Briefing output contains no YAML front-matter. Risk Assessment and QBR Prep are visible.</done>
</task>

</tasks>

<verification>
Phase 63 success criteria (from ROADMAP.md):
1. SKILLS-DESIGN-STANDARD.md exists — verified in Task 1 build check
2. Runtime validation checks front-matter on load — verified by Fix required badge logic in page.tsx
3. Skills tab displays "Fix required" badges for non-compliant skills — verified in human checkpoint step 5
4. Non-compliant skills are grayed out (opacity-60) with tooltip — verified in human checkpoint step 5
5. Previously grayed-out skills are functional or marked — verified in human checkpoint steps 3 and 7
</verification>

<success_criteria>
- Human confirms Skills tab renders with all expected skills present
- Human confirms stakeholder-comms and onboarding-checklist are absent
- Human confirms Morning Briefing output does not start with YAML front-matter
- Human confirms Risk Assessment and QBR Prep are visible (new skills)
- No "Fix required" badges appear (all skills migrated in Plan 01)
- TypeScript build clean
</success_criteria>

<output>
After completion, create `.planning/phases/63-skills-design-standard/63-04-SUMMARY.md`
</output>
