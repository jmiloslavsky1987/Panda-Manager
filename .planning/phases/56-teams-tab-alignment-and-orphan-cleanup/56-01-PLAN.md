---
phase: 56-teams-tab-alignment-and-orphan-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bigpanda-app/components/teams/TeamEngagementMap.tsx
  - bigpanda-app/components/teams/TeamsPageTabs.tsx
  - bigpanda-app/components/teams/TeamEngagementOverview.tsx
  - bigpanda-app/components/teams/ArchOverviewSection.tsx
  - bigpanda-app/tests/teams/engagement-overview.test.tsx
  - bigpanda-app/tests/teams/warn-banner-trigger.test.tsx
  - bigpanda-app/.planning/REQUIREMENTS.md
autonomous: true
requirements: [TEAM-01]

must_haves:
  truths:
    - "Teams tab displays 4 sections (Business Value, E2E Workflows, Teams Engagement, Focus Areas) without Architecture section"
    - "Section headers have no numbered circle badges - plain text headers only"
    - "Architecture content is only viewable in the Architecture tab, not Teams tab"
  artifacts:
    - path: "bigpanda-app/components/teams/TeamEngagementMap.tsx"
      provides: "4-section Teams tab without Architecture (Section 2 removed)"
      min_lines: 60
    - path: "bigpanda-app/.planning/REQUIREMENTS.md"
      provides: "TEAM-01 marked as satisfied with 4-section design documented"
      contains: "4-section editable in-place map"
  key_links:
    - from: "bigpanda-app/components/teams/TeamEngagementMap.tsx"
      to: "Section headers"
      via: "Plain h2 elements instead of SectionHeader component"
      pattern: "<h2.*Business Value"
---

<objective>
Align Teams tab to final 4-section design and remove orphaned read-only Overview infrastructure.

Purpose: Close TEAM-01 gap by removing abandoned read-only Overview components (TeamsPageTabs, TeamEngagementOverview) that were never wired, removing Architecture section from TeamEngagementMap (duplicates Architecture tab content), and updating REQUIREMENTS.md to reflect the final 4-section editable in-place design.

Output: Clean Teams tab with 4 sections (no Architecture duplication), no orphaned components, TEAM-01 requirement satisfied.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-teams-tab-alignment-and-orphan-cleanup/56-CONTEXT.md
@.planning/phases/56-teams-tab-alignment-and-orphan-cleanup/56-RESEARCH.md

# Current Teams tab structure (5 sections with Architecture duplication)
@bigpanda-app/components/teams/TeamEngagementMap.tsx

# Orphaned components built for abandoned read-only Overview direction
@bigpanda-app/components/teams/TeamsPageTabs.tsx
@bigpanda-app/components/teams/TeamEngagementOverview.tsx

# Architecture section that will be orphaned after Section 2 removal
@bigpanda-app/components/teams/ArchOverviewSection.tsx

# Tests that reference deleted components
@bigpanda-app/tests/teams/engagement-overview.test.tsx
@bigpanda-app/tests/teams/warn-banner-trigger.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Architecture section from TeamEngagementMap and drop numbered badges</name>
  <files>bigpanda-app/components/teams/TeamEngagementMap.tsx</files>
  <action>
Edit TeamEngagementMap.tsx to implement the final 4-section design:

1. **Remove Section 2 (Architecture Overview):**
   - Delete lines 46-47 (SectionHeader n={2} + ArchOverviewSection component)
   - Remove the import for ArchOverviewSection (line 5)

2. **Replace SectionHeader component with plain h2 elements:**
   - Delete the SectionHeader function definition (lines 11-23)
   - Replace all `<SectionHeader n={1} title="..." />` with plain h2 headers:
   ```tsx
   <h2 className="text-xl font-bold text-zinc-900 border-b border-zinc-200 pb-2 mb-6">
     Business Value &amp; Expected Outcomes
   </h2>
   ```
   - Apply same pattern for all 4 remaining sections (Business Value, E2E Workflows, Teams Engagement, Focus Areas)

3. **Update section spacing:**
   - Keep the `space-y-10` wrapper for consistent vertical spacing
   - Each section gets the h2 header followed by its content component

Result: 4 sections with clean text headers, no numbered badges, no Architecture duplication.
  </action>
  <verify>
    <automated>npm test tests/teams/ --run 2>&1 | grep -E "(PASS|FAIL|Error)"</automated>
  </verify>
  <done>TeamEngagementMap.tsx renders 4 sections with plain text headers; no SectionHeader component usage; no ArchOverviewSection import or render; TypeScript compilation clean</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned TeamsPageTabs, TeamEngagementOverview, ArchOverviewSection components and update tests</name>
  <files>
    bigpanda-app/components/teams/TeamsPageTabs.tsx
    bigpanda-app/components/teams/TeamEngagementOverview.tsx
    bigpanda-app/components/teams/ArchOverviewSection.tsx
    bigpanda-app/tests/teams/engagement-overview.test.tsx
    bigpanda-app/tests/teams/warn-banner-trigger.test.tsx
  </files>
  <action>
1. **Delete orphaned component files:**
   - Delete `components/teams/TeamsPageTabs.tsx` (never wired to any page)
   - Delete `components/teams/TeamEngagementOverview.tsx` (only imported by TeamsPageTabs)
   - Delete `components/teams/ArchOverviewSection.tsx` (orphaned after Task 1 removes Section 2)

2. **Delete obsolete test files:**
   - Delete `tests/teams/engagement-overview.test.tsx` (tests deleted TeamEngagementOverview component)
   - Delete `tests/teams/warn-banner-trigger.test.tsx` (tests deleted TeamEngagementOverview component)

3. **Verify no remaining imports:**
   - Run `grep -r "TeamsPageTabs\|TeamEngagementOverview\|ArchOverviewSection" bigpanda-app/components/ bigpanda-app/app/` to confirm no orphaned imports remain

Note: app/customer/[id]/teams/page.tsx already imports TeamEngagementMap directly (correct), so no page updates needed.
  </action>
  <verify>
    <automated>test ! -f bigpanda-app/components/teams/TeamsPageTabs.tsx && test ! -f bigpanda-app/components/teams/TeamEngagementOverview.tsx && test ! -f bigpanda-app/components/teams/ArchOverviewSection.tsx && echo "Orphaned components deleted" || echo "ERROR: Files still exist"</automated>
  </verify>
  <done>3 orphaned component files deleted; 2 obsolete test files deleted; grep shows no remaining imports of deleted components</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md to document final 4-section design and mark TEAM-01 satisfied</name>
  <files>bigpanda-app/.planning/REQUIREMENTS.md</files>
  <action>
Edit .planning/REQUIREMENTS.md to update TEAM-01 and mark it as complete:

1. **Update TEAM-01 description (line 43):**
   - Replace: "Teams sub-tab (renamed "Team Engagement Overview") displays a 4-section engagement map: Business Outcomes, E2E Workflows, Teams & Engagement, and Top Focus Areas (Architecture section is covered by the dedicated Architecture tab — excluded from Overview per scope decision)"
   - With: "Teams tab displays a 4-section editable in-place map: Business Value & Expected Outcomes, End-to-End Workflows, Teams & Engagement Status, and Top Focus Areas. Architecture section is excluded (covered by dedicated Architecture tab). Section headers use plain text styling without numbered badges."

2. **Mark TEAM-01 as complete:**
   - Change checkbox from `- [ ]` to `- [x]` (line 43)

3. **Update traceability table:**
   - Find TEAM-01 entry in traceability section (line 136)
   - Change Status from "Pending" to "Complete"

4. **Update coverage note if present:**
   - If "Pending (gap closure): 3 (TEAM-01, TEAM-02 → Phase 56...)" exists, update to show TEAM-01 complete

Result: REQUIREMENTS.md reflects the final 4-section Teams tab design as the documented requirement.
  </action>
  <verify>
    <automated>grep -E "^\- \[x\] \*\*TEAM-01\*\*" .planning/REQUIREMENTS.md && grep "4-section editable in-place map" .planning/REQUIREMENTS.md && echo "TEAM-01 documented and satisfied" || echo "ERROR: REQUIREMENTS.md not updated"</automated>
  </verify>
  <done>REQUIREMENTS.md line 43 shows TEAM-01 checked and describes 4-section editable map; traceability table shows TEAM-01 Complete; no mention of numbered badges or read-only Overview in TEAM-01 description</done>
</task>

</tasks>

<verification>
Run full test suite for Teams components:
```bash
cd bigpanda-app && npm test tests/teams/ --run
```

Verify no orphaned imports:
```bash
grep -r "TeamsPageTabs\|TeamEngagementOverview\|ArchOverviewSection" bigpanda-app/components/ bigpanda-app/app/
```

Check TeamEngagementMap structure:
```bash
grep -n "SectionHeader\|ArchOverviewSection" bigpanda-app/components/teams/TeamEngagementMap.tsx
# Should return 0 results
```

Verify REQUIREMENTS.md:
```bash
grep -A 2 "TEAM-01" .planning/REQUIREMENTS.md | grep "\[x\]"
```
</verification>

<success_criteria>
- TeamEngagementMap renders 4 sections (Business Value, E2E Workflows, Teams Engagement, Focus Areas) without Architecture section
- Section headers are plain h2 elements with no numbered circle badges
- TeamsPageTabs.tsx, TeamEngagementOverview.tsx, and ArchOverviewSection.tsx files deleted
- No remaining imports of deleted components in codebase
- engagement-overview.test.tsx and warn-banner-trigger.test.tsx deleted
- REQUIREMENTS.md TEAM-01 marked [x] and describes final 4-section editable design
- TEAM-01 traceability status shows "Complete"
- TypeScript compilation clean with no Teams-related errors
</success_criteria>

<output>
After completion, create `.planning/phases/56-teams-tab-alignment-and-orphan-cleanup/56-01-SUMMARY.md`
</output>
