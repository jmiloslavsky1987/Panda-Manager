---
phase: 85.2
slug: daily-briefing-tab
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-14
---

# Phase 85.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest |
| **Config file** | `/Users/jmiloslavsky/Documents/Panda-Manager/vitest.config.ts` |
| **Quick run command** | `cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/schema/daily-briefings.test.ts tests/components/daily-briefing*.test.ts tests/api/daily-briefing*.test.ts` |
| **Full suite command** | `cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run` |
| **Estimated runtime** | ~30 seconds (quick) / ~120 seconds (full) |

**Test directory note:** `tests/` is gitignored by project design (STATE.md [79-00]). Test files exist on-disk only (except `lib/__tests__/` which is tracked). Follow source-scan pattern from `tests/components/daily-prep-page.test.ts` for page/layout tests.

---

## Sampling Rate

- **After every task commit:** Run quick run command
- **After every plan wave:** Run full suite command
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds (quick) / 120 seconds (full)

---

## Per-Task Verification Map

| Req ID | Behavior | Test Type | Automated Command | File Exists | Status |
|--------|----------|-----------|-------------------|-------------|--------|
| BRIEF-01 | `daily_briefings` table exists with correct columns + unique index | unit (source scan) | `npx vitest run tests/schema/daily-briefings.test.ts` | ❌ W0 | ⬜ pending |
| BRIEF-02 | `app/daily-prep/layout.tsx` exports layout with SubTabBar items (Calendar + Today's Briefing) | unit (source scan) | `npx vitest run tests/components/daily-briefing-layout.test.ts` | ❌ W0 | ⬜ pending |
| BRIEF-03 | `app/daily-prep/briefing/page.tsx` exports `force-dynamic`, renders empty state when no briefing | unit (source scan) | `npx vitest run tests/components/daily-briefing-page.test.ts` | ❌ W0 | ⬜ pending |
| BRIEF-04 | `POST /api/daily-prep/briefing` returns SSE stream with `data: { text }` + `event: done` | unit (route mock) | `npx vitest run tests/api/daily-briefing-route.test.ts` | ❌ W0 | ⬜ pending |
| BRIEF-05 | `parseFlexibleDate` returns `Date` for ISO, `null` for 'TBD'/'2026-Q3'/'' | unit | `npx vitest run tests/api/daily-briefing-route.test.ts` | ❌ W0 | ⬜ pending |
| BRIEF-06 | `app/daily-prep/page.tsx` chains to `/api/daily-prep/briefing` after `event: done` for all per-event streams | unit (source scan) | `npx vitest run tests/components/daily-prep-page.test.ts` (extend existing) | ✅ extend | ⬜ pending |
| BRIEF-07 | `daily_briefings` schema export exists in `db/schema.ts` | unit (source scan) | `npx vitest run tests/schema/daily-briefings.test.ts` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/schema/daily-briefings.test.ts` — stubs for BRIEF-01, BRIEF-07 (migration file + schema export scan)
- [ ] `tests/components/daily-briefing-layout.test.ts` — stubs for BRIEF-02 (source scan for SubTabBar items + usePathname)
- [ ] `tests/components/daily-briefing-page.test.ts` — stubs for BRIEF-03 (force-dynamic, empty state text, ReactMarkdown usage)
- [ ] `tests/api/daily-briefing-route.test.ts` — stubs for BRIEF-04, BRIEF-05 (route mock + parseFlexibleDate unit tests)
- [ ] Extend `tests/components/daily-prep-page.test.ts` — stub for BRIEF-06 (source scan for chain-to-briefing logic)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| SubTabBar visual offset (`sticky top-[Npx]`) sits flush under 36px PageBar | (Visual UX) | Visual rendering only — no DOM/text assertion can validate pixel alignment | 1. `pnpm dev` 2. Visit `/daily-prep` 3. Confirm SubTabBar sticks directly below PageBar with no gap or overlap |
| Today's Briefing markdown rendering layout (3 sections, readable) | BRIEF-03 (UX side) | LLM output is non-deterministic; render quality is a visual judgment | Visit `/daily-prep/briefing` after generating; confirm three section headings render and content is readable |
| SSE streaming smoothness (no buffering hitches) | BRIEF-04 (UX side) | Streaming feel is perceptual | Visit `/daily-prep`, click Generate, watch briefing tab — text should append progressively |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (quick) / 120s (full)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
