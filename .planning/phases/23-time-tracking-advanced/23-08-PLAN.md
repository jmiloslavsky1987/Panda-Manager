---
phase: 23-time-tracking-advanced
plan: "08"
type: execute
wave: 3
depends_on: ["23-02", "23-03", "23-05", "23-06", "23-07"]
files_modified:
  - bigpanda-app/components/TimeTab.tsx
autonomous: false
requirements:
  - TTADV-01
  - TTADV-02
  - TTADV-03
  - TTADV-04
  - TTADV-05
  - TTADV-06
  - TTADV-07
  - TTADV-08
  - TTADV-09
  - TTADV-10
  - TTADV-11
  - TTADV-12
  - TTADV-13
  - TTADV-14
  - TTADV-15
  - TTADV-16
  - TTADV-17
  - TTADV-18
  - TTADV-19

must_haves:
  truths:
    - "All Phase 23 time tracking features are verified working end-to-end in the browser"
    - "Admin settings persist correctly"
    - "Submit/approve/reject workflow completes without errors"
    - "Bulk operations process multiple entries"
    - "Export downloads a file with audit columns"
    - "Grouping renders correctly with subtotals"
    - "Notification banner appears after approve/reject"
---

<objective>
Human verification checkpoint for Phase 23 Time Tracking Advanced. Verify all TTADV features work end-to-end in the browser.

Purpose: Catch any integration failures before closing Phase 23.
Output: Phase 23 verified COMPLETE or issues filed for gap closure.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: End-to-end verification of all TTADV features</name>
  <files>bigpanda-app/components/TimeTab.tsx</files>
  <action>Human verifies all 19 TTADV requirements are working end-to-end in the browser. See how-to-verify for step-by-step instructions.</action>
  <what-built>
    Full Time Tracking Advanced system:
    - Admin settings at /settings/time-tracking (enable TT, capacity, categories, exemptions, locking)
    - Approval workflow: submit week, approve/reject per-entry with status badges in TimeTab
    - Google Calendar OAuth + event import with project auto-match + override
    - Bulk operations: multi-select in TimeTab, bulk approve/reject/move/delete toolbar
    - Export: CSV and Excel with audit fields (submitted_on, approved_on, rejected_on, etc.)
    - Table grouping: Group by project/status/date with billable/non-billable subtotals
    - In-app notifications for approval/rejection outcomes
  </what-built>
  <how-to-verify>
    Run the app: `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run dev`

    1. Admin settings (TTADV-01 to 06):
       - Visit http://localhost:3000/settings/time-tracking
       - Toggle "Enable Time Tracking" on → save → reload → confirm it persists as enabled
       - Set weekly capacity to 36 hours, change due day to Thursday → save
       - Add category "Research" to the categories list → save → confirm it appears
       - Add a user email to exempt users → save

    2. Approval workflow (TTADV-07 to 10):
       - Visit any project's Time tab
       - Confirm entries show status badge (gray "Draft" for existing entries)
       - Click "Submit Week" → confirm submitted_on is set (entries turn blue "Submitted")
       - Visit the same tab with ?role=approver appended to URL
       - Approve one entry → confirm it turns green "Approved" and shows lock icon
       - Reject one entry with reason "Wrong project" → confirm it turns red "Rejected"
       - Confirm approved entry cannot be edited (edit button hidden or disabled)

    3. Google Calendar import (TTADV-11 to 14):
       - Click "Import from Calendar" button in TimeTab
       - If not connected: "Connect Google Calendar" button appears (OAuth flow works even without completing auth)
       - If connected: week selector + event list with matched projects appears
       - Confirm duration is computed from event start/end times
       - Confirm imported entries use event date (not today's date)

    4. Bulk operations (TTADV-15):
       - With ?role=approver: confirm checkboxes appear in entry table
       - Select 2+ entries → bulk toolbar appears with Approve/Reject/Move/Delete buttons
       - Bulk approve 2 submitted entries → confirm both become Approved
       - Bulk delete 1 draft entry → confirm deletion after confirm dialog

    5. Export (TTADV-16):
       - Click Export → Export CSV → confirm file downloads
       - Open CSV in text editor → confirm Submitted_On, Approved_On, Rejected_On columns present
       - Export Excel → confirm .xlsx file downloads and opens in Excel/Numbers

    6. Grouping (TTADV-17):
       - Change "Group by" to "Status" → confirm entries grouped by Draft/Submitted/Approved/Rejected
       - Each group shows total hours and billable/non-billable breakdown
       - Change to "Date" → entries grouped by date with subtotals

    7. Notifications (TTADV-18, 19):
       - After approving an entry, confirm yellow notification banner appears at top of TimeTab
       - Expand notifications → see "Time Entry Approved" message
       - Click Dismiss → notification removed from list
  </how-to-verify>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run __tests__/time-tracking-advanced/ 2>&1 | tail -3</automated>
  </verify>
  <done>All 19 TTADV requirements verified working in browser. Human types "approved" to close Phase 23.</done>
  <resume-signal>Type "approved" if all features work. Describe any specific failures to trigger gap closure.</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all TTADV features verified in-browser.
</verification>

<success_criteria>
All 19 TTADV requirements (TTADV-01 through TTADV-19) verified functional. Phase 23 COMPLETE.
</success_criteria>

<output>
After completion, create `.planning/phases/23-time-tracking-advanced/23-08-SUMMARY.md`
Update REQUIREMENTS.md traceability: mark TTADV-01 through TTADV-19 as Complete (Phase 23).
Update STATE.md: Phase 23 COMPLETE.
</output>
