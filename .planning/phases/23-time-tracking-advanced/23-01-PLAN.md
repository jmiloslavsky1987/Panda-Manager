---
phase: 23-time-tracking-advanced
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/__tests__/time-tracking-advanced/approval-state.test.ts
  - bigpanda-app/__tests__/time-tracking-advanced/entry-locking.test.ts
  - bigpanda-app/__tests__/time-tracking-advanced/grouping.test.ts
autonomous: true
requirements:
  - TTADV-07
  - TTADV-08
  - TTADV-10
  - TTADV-15
  - TTADV-17

must_haves:
  truths:
    - "RED tests exist for approval state transitions and fail (unexported symbols or wrong behavior)"
    - "RED tests exist for entry locking logic and fail"
    - "RED tests exist for grouping/subtotal logic and fail"
  artifacts:
    - path: "bigpanda-app/__tests__/time-tracking-advanced/approval-state.test.ts"
      provides: "TDD scaffold for approval workflow state machine"
    - path: "bigpanda-app/__tests__/time-tracking-advanced/entry-locking.test.ts"
      provides: "TDD scaffold for locked entry guard logic"
    - path: "bigpanda-app/__tests__/time-tracking-advanced/grouping.test.ts"
      provides: "TDD scaffold for group-by + subtotal logic"
  key_links:
    - from: "bigpanda-app/__tests__/time-tracking-advanced/approval-state.test.ts"
      to: "bigpanda-app/lib/time-tracking.ts"
      via: "import of approval helpers (not yet written)"
      pattern: "from '@/lib/time-tracking'"
---

<objective>
Wave 0: Write failing TDD RED tests for the three stateful logic modules in Phase 23 — approval state machine, entry locking guard, and time entry grouping.

Purpose: Establish test contracts before implementation so Wave 1 plans implement against known behavior expectations, not discovered ones.
Output: Three test files that fail with "Cannot find module" or assertion failures. Implementation comes in Wave 1 plans.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Key types already in schema — executor uses these directly -->
<interfaces>
From bigpanda-app/db/schema.ts (time_entries table — Phase 5.2 + SCHEMA-03):
```typescript
export const timeEntries = pgTable('time_entries', {
  id:          serial('id').primaryKey(),
  project_id:  integer('project_id').notNull(),
  date:        text('date').notNull(),         // 'YYYY-MM-DD'
  hours:       text('hours').notNull(),        // decimal string '1.5'
  description: text('description').notNull(),
  created_at:  timestamp('created_at').defaultNow().notNull(),
  updated_at:  timestamp('updated_at').defaultNow().notNull(),
  submitted_on:  timestamp('submitted_on'),
  submitted_by:  text('submitted_by'),
  approved_on:   timestamp('approved_on'),
  approved_by:   text('approved_by'),
  rejected_on:   timestamp('rejected_on'),
  rejected_by:   text('rejected_by'),
  locked:        boolean('locked').default(false).notNull(),
})
export type TimeEntry = typeof timeEntries.$inferSelect
```

Vitest config: existing test runner — import from '@/' with tsconfig paths.
Pattern from Phase 17: import unexported symbol from future module → toBeDefined() fails RED.
Pattern from Phase 19.1: server-only mock in vitest.config.ts already in place.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD RED — Approval state machine tests</name>
  <files>bigpanda-app/__tests__/time-tracking-advanced/approval-state.test.ts</files>
  <behavior>
    - getEntryStatus(entry: TimeEntry): 'draft' | 'submitted' | 'approved' | 'rejected' | 'locked'
      - entry with no submitted_on → 'draft'
      - entry with submitted_on set, no approved_on or rejected_on → 'submitted'
      - entry with approved_on set → 'approved'
      - entry with rejected_on set → 'rejected'
      - entry with locked=true → 'locked' (highest priority)
    - canEdit(entry: TimeEntry): boolean
      - draft → true
      - submitted → false (pending approval)
      - approved/locked → false
      - rejected → true (can be corrected and resubmitted)
    - canSubmit(entry: TimeEntry): boolean
      - draft → true
      - rejected → true (re-submit after correction)
      - submitted/approved/locked → false
  </behavior>
  <action>
    Create bigpanda-app/__tests__/time-tracking-advanced/ directory.
    Import { getEntryStatus, canEdit, canSubmit } from '@/lib/time-tracking'.
    Write tests using the behavior cases above. The module does not exist yet — tests MUST fail RED.
    Use vitest describe/it/expect pattern matching existing tests in __tests__/.
    Commit: `test(23-01): add RED approval-state tests`
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run __tests__/time-tracking-advanced/approval-state.test.ts 2>&1 | grep -E "FAIL|Cannot find|Error" | head -5</automated>
  </verify>
  <done>Tests run and fail RED (import error or assertion failure). No passing tests in this file yet.</done>
</task>

<task type="tdd">
  <name>Task 2: TDD RED — Entry locking guard tests</name>
  <files>bigpanda-app/__tests__/time-tracking-advanced/entry-locking.test.ts</files>
  <behavior>
    - isLocked(entry: TimeEntry): boolean
      - locked=true → true
      - approved_on set + locked=false → false (locking is explicit, not automatic on approval)
      - no fields set → false
    - canOverrideLock(role: 'admin' | 'approver' | 'user'): boolean
      - 'admin' → true
      - 'approver' → true
      - 'user' → false
    - buildLockPayload(): Partial<TimeEntryInsert>
      - returns { locked: true }
    - buildUnlockPayload(): Partial<TimeEntryInsert>
      - returns { locked: false }
  </behavior>
  <action>
    Import { isLocked, canOverrideLock, buildLockPayload, buildUnlockPayload } from '@/lib/time-tracking'.
    Write tests for all behavior cases. Module does not exist — tests MUST fail RED.
    Commit: `test(23-01): add RED entry-locking tests`
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run __tests__/time-tracking-advanced/entry-locking.test.ts 2>&1 | grep -E "FAIL|Cannot find|Error" | head -5</automated>
  </verify>
  <done>Tests fail RED. Import error acceptable (module not yet created).</done>
</task>

<task type="tdd">
  <name>Task 3: TDD RED — Grouping and subtotal logic tests</name>
  <files>bigpanda-app/__tests__/time-tracking-advanced/grouping.test.ts</files>
  <behavior>
    - groupEntries(entries: TimeEntry[], by: 'project' | 'status' | 'date'): Record<string, TimeEntry[]>
      - groups entries by the specified key; project → group by project_id (string key)
      - status → group by getEntryStatus result
      - date → group by entry.date
    - computeSubtotals(group: TimeEntry[]): { total_hours: number; billable_hours: number; non_billable_hours: number }
      - total_hours: sum of parseFloat(entry.hours) for all entries
      - billable_hours: sum where description does NOT contain '[non-billable]' (case insensitive)
      - non_billable_hours: total_hours - billable_hours
    - Edge: empty group → all zeros
    - Edge: single entry '2.5 hours' → total_hours: 2.5
  </behavior>
  <action>
    Import { groupEntries, computeSubtotals } from '@/lib/time-tracking'.
    Write tests covering behavior cases including edge cases. Module does not exist — tests MUST fail RED.
    Commit: `test(23-01): add RED grouping tests`
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run __tests__/time-tracking-advanced/grouping.test.ts 2>&1 | grep -E "FAIL|Cannot find|Error" | head -5</automated>
  </verify>
  <done>Tests fail RED. All three test files committed and failing.</done>
</task>

</tasks>

<verification>
All three test files exist in bigpanda-app/__tests__/time-tracking-advanced/.
Running vitest on each shows FAIL status (not PASS).
No production code in lib/time-tracking.ts yet (that comes in Wave 1).
</verification>

<success_criteria>
Three test files committed with failing RED tests.
Test contracts define the exact function signatures Wave 1 plans will implement.
</success_criteria>

<output>
After completion, create `.planning/phases/23-time-tracking-advanced/23-01-SUMMARY.md`
</output>
