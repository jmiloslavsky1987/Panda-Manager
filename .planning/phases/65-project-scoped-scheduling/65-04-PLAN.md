---
phase: 65-project-scoped-scheduling
plan: 04
type: execute
wave: 4
depends_on:
  - 65-03
files_modified: []
autonomous: false
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05

must_haves:
  truths:
    - "Global Scheduler page shows only non-project jobs"
    - "Project workspace Skills tab has Project Scheduler section"
    - "Project scheduler only shows jobs for that project"
    - "Navigating away and returning to Skills tab preserves expanded row state"
    - "Manually triggered job run appears in run history on expand"
    - "Sidebar Scheduler link has no notification badge"
  artifacts:
    - path: "bigpanda-app/app/scheduler/page.tsx"
      provides: "Global scheduler with IS NULL filter and scope subtitle"
    - path: "bigpanda-app/app/customer/[id]/skills/page.tsx"
      provides: "Project-scoped jobs passed as initialJobs to SkillsTabClient"
    - path: "bigpanda-app/components/SkillsTabClient.tsx"
      provides: "Project Scheduler section rendered below skills catalog"
    - path: "bigpanda-app/components/Sidebar.tsx"
      provides: "Scheduler link without NotificationBadge"
  key_links:
    - from: "Skills tab"
      to: "Project Scheduler section"
      via: "visible below skills catalog on any project workspace"
      pattern: "Project Scheduler"
---

<objective>
Human verification gate — confirm all Phase 65 SCHED requirements are working correctly in the browser.

Purpose: Automated TypeScript checks confirm compilation but cannot verify runtime behavior, UI rendering, or cross-navigation state persistence. Human eyes needed to close the verification loop.
Output: All 5 SCHED requirements confirmed passing or issues documented for gap closure.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 65: Project-Scoped Scheduling</name>
  <action>Human verifies all SCHED requirements in browser using the steps in how-to-verify.</action>
  <verify>All 6 browser verification steps pass</verify>
  <done>All SCHED-01 through SCHED-05 requirements confirmed working in browser</done>
  <what-built>
    Phase 65 complete implementation:
    - DB schema: scheduled_jobs.project_id nullable FK to projects
    - Global /scheduler page: filtered to project_id IS NULL jobs; descriptive subtitle added
    - /api/projects/[projectId]/jobs: GET + POST with requireProjectRole('admin')
    - Skills tab: Project Scheduler section below skills catalog, fetched server-side
    - URL-param state persistence for expanded row state (sched_expanded)
    - Run history visible in expanded rows
    - Sidebar: Scheduler link has no notification badge
  </what-built>
  <how-to-verify>
    Start dev server: `cd bigpanda-app && npm run next-only`

    **Step 1 — SCHED-05: Nav badge removed**
    1. Navigate to any page
    2. Confirm the Scheduler link in the sidebar has NO orange/red badge next to it
    Expected: Clean "Scheduler" text with calendar icon, no badge

    **Step 2 — SCHED-01: Global scheduler shows non-project jobs only**
    1. Navigate to /scheduler
    2. Confirm the page subtitle reads something like "Global scheduled jobs — not scoped to any project"
    3. If any scheduled jobs exist, confirm none have a project association (or that project-scoped jobs are absent)
    Expected: Page loads; subtitle present; only global jobs shown

    **Step 3 — SCHED-02: Project scheduler section in Skills tab**
    1. Navigate to any project workspace → Skills tab
    2. Scroll down past the skills catalog
    3. Confirm a "Project Scheduler" section is visible with its own heading and job table (or empty state)
    Expected: Section visible below skills catalog; "Create Job" button present for admin users

    **Step 4 — SCHED-02: Create a project-scoped job**
    1. In the Project Scheduler section, click "Create Job"
    2. Complete the wizard (select a skill, set a schedule)
    3. Confirm the job appears in the project scheduler table
    4. Navigate to /scheduler (global) and confirm the job does NOT appear there
    Expected: Job visible in project scheduler, absent from global scheduler

    **Step 5 — SCHED-03: State persistence**
    1. In the Project Scheduler section, click a job row to expand it
    2. Navigate to a different tab in the project workspace (e.g., Overview)
    3. Navigate back to the Skills tab
    4. Confirm the same job row is still expanded (URL param sched_expanded preserved)
    Expected: Expanded row state restored on return

    **Step 6 — SCHED-04: Run history visible**
    1. In the Project Scheduler section, trigger a job manually (click Run)
    2. Expand the job row
    3. Confirm the Run History section shows the manual run with timestamp, outcome badge, and duration
    (If run_history_json is empty for new jobs, expand any existing job on /scheduler to verify the run history renders correctly)
    Expected: Run history entries visible in expanded panel; outcome badge (success/failure/partial) present
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 steps pass, or describe any failures for gap closure</resume-signal>
</task>

</tasks>

<verification>
All verification is human-driven in this plan. See checkpoint task above.
</verification>

<success_criteria>
All 5 SCHED requirements verified in browser:
- SCHED-01: Global /scheduler shows only non-project jobs
- SCHED-02: Project Skills tab has functional project scheduler (create, list, manage jobs)
- SCHED-03: Expanded row state persists across navigation via URL params
- SCHED-04: Run history visible in expanded job rows
- SCHED-05: Sidebar Scheduler link has no notification badge
</success_criteria>

<output>
After completion, create `.planning/phases/65-project-scoped-scheduling/65-04-SUMMARY.md`
</output>
