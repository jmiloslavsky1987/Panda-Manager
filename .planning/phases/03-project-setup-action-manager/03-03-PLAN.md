---
phase: 03-project-setup-action-manager
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - server/routes/workstreams.js
  - server/routes/workstreams.test.js
  - server/index.js
autonomous: true
requirements: [ACT-08]

must_haves:
  truths:
    - "PATCH /api/customers/:id/workstreams returns 200 with updated workstreams object"
    - "writeYamlFile called exactly once (atomic write verified)"
    - "Returns 422 when adr group is missing from request body"
    - "Returns 422 when YAML validation fails on read"
    - "Other YAML fields unchanged after PATCH workstreams (only workstreams key replaced)"
    - "node --test server/routes/workstreams.test.js exits 0 with all 5 assertions green"
  artifacts:
    - path: "server/routes/workstreams.js"
      provides: "PATCH /api/customers/:id/workstreams endpoint with group validation + atomic write"
    - path: "server/routes/workstreams.test.js"
      provides: "5 assertions filled (replacing t.todo stubs)"
    - path: "server/index.js"
      provides: "Route mounted: app.use('/api/customers/:id/workstreams', require('./routes/workstreams'))"
  key_links:
    - from: "server/routes/workstreams.js"
      to: "server/services/yamlService"
      via: "require('../services/yamlService')"
      pattern: "yamlService.validateYaml|serializeYaml"
    - from: "server/index.js"
      to: "server/routes/workstreams.js"
      via: "app.use('/api/customers/:id/workstreams', ...)"
      pattern: "require.*workstreams"
---

<objective>
Wave 1: Create server/routes/workstreams.js with PATCH handler, fill workstreams.test.js assertions,
and mount the route in server/index.js.

Purpose: Project Setup view depends on PATCH /api/customers/:id/workstreams to save the full
workstreams nested object atomically to Drive. This route does not exist yet.

Output: New workstreams.js route file, all workstreams.test.js assertions green, route mounted.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-project-setup-action-manager/03-01-SUMMARY.md
@server/index.js
@server/routes/workstreams.test.js
@server/routes/risks.js
@server/services/yamlService.js
@server/fixtures/sample.yaml

<interfaces>
From server/index.js — current mount list (workstreams NOT yet present):
  app.use('/api/customers/:id/actions', require('./routes/actions'));
  app.use('/api/customers/:id/risks', require('./routes/risks'));
  app.use('/api/customers/:id/milestones', require('./routes/milestones'));
  app.use('/api/customers/:id/artifacts', require('./routes/artifacts'));
  app.use('/api/customers/:id/history', require('./routes/history'));
  app.use('/api/customers/:id/reports', require('./routes/reports'));

Add after history mount:
  app.use('/api/customers/:id/workstreams', require('./routes/workstreams'));

From server/routes/risks.js (atomic pattern to replicate):
  router = require('express').Router({ mergeParams: true })
  const content = await driveService.readYamlFile(fileId);
  const data = yamlService.parseYaml(content);
  yamlService.validateYaml(data);
  // modify data...
  const normalized = yamlService.normalizeForSerialization(data);
  const yamlString = yamlService.serializeYaml(normalized);
  await driveService.writeYamlFile(fileId, yamlString);

sample.yaml after 03-01: workstreams has groups adr and biggy
REQUIRED_GROUPS validation: ['adr', 'biggy'] must both be present in req.body
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create workstreams.js route and mount in index.js</name>
  <files>server/routes/workstreams.js, server/index.js</files>
  <behavior>
    - PATCH with valid {adr: {...}, biggy: {...}} body returns 200 with {fileId, workstreams}
    - Returns 422 when adr group is missing from body
    - Returns 422 when biggy group is missing from body
    - writeYamlFile called exactly once
    - Other YAML fields (customer, actions, risks) unchanged after write
  </behavior>
  <action>
Create server/routes/workstreams.js:

  // server/routes/workstreams.js
  // mergeParams: true required — req.params.id is the customer file ID from parent route
  'use strict';
  const router = require('express').Router({ mergeParams: true });
  const asyncWrapper = require('../middleware/asyncWrapper');
  const driveService = require('../services/driveService');
  const yamlService = require('../services/yamlService');

  const REQUIRED_GROUPS = ['adr', 'biggy'];

  router.patch('/', asyncWrapper(async (req, res) => {
    const { id: fileId } = req.params;
    const newWorkstreams = req.body;

    // Validate required groups before reading Drive (fast-fail on bad body)
    for (const group of REQUIRED_GROUPS) {
      if (typeof newWorkstreams[group] !== 'object' || newWorkstreams[group] === null) {
        return res.status(422).json({ error: `Missing required workstream group: ${group}` });
      }
    }

    // Atomic: read -> replace workstreams key -> validate -> write
    const content = await driveService.readYamlFile(fileId);
    const data = yamlService.parseYaml(content);
    yamlService.validateYaml(data);

    data.workstreams = newWorkstreams;

    const normalized = yamlService.normalizeForSerialization(data);
    const yamlString = yamlService.serializeYaml(normalized);
    await driveService.writeYamlFile(fileId, yamlString);

    res.json({ fileId, workstreams: data.workstreams });
  }));

  module.exports = router;

In server/index.js, add this line after the history route mount and before the error handler:
  app.use('/api/customers/:id/workstreams', require('./routes/workstreams'));
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node -e "
const app = require('./index');
console.log('server/index.js loads without error');
process.exit(0);
" 2>&1  </verify>
  <done>workstreams.js created; route mounted in index.js; server loads without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill workstreams.test.js assertions</name>
  <files>server/routes/workstreams.test.js</files>
  <behavior>
    - PATCH with valid body returns 200 with workstreams in response
    - writeYamlFile called exactly once
    - Missing adr group returns 422
    - YAML validation failure returns 422
    - Other YAML fields unchanged after patch
  </behavior>
  <action>
Replace all t.todo() stubs in server/routes/workstreams.test.js with real assertions.

Build a valid workstreams body for test use (use just the shape, not all 11 sub-workstreams):
  const VALID_WORKSTREAMS = {
    adr: {
      inbound_integrations: { status: 'in_progress', percent_complete: 50, progress_notes: 'updated', blockers: '', scope: [] },
    },
    biggy: {
      biggy_app_integration: { status: 'not_started', percent_complete: 0, progress_notes: '', blockers: '' },
    },
  };

Test assertions:

  - 'returns 200 with updated workstreams when given valid body':
      const res = await request
        .patch(`/api/customers/${FAKE_FILE_ID}/workstreams`)
        .send(VALID_WORKSTREAMS)
        .set('Content-Type', 'application/json');
      assert.equal(res.status, 200);
      assert.ok(res.body.workstreams);
      assert.ok(res.body.workstreams.adr);
      assert.ok(res.body.workstreams.biggy);

  - 'writes updated YAML to Drive (writeYamlFile called once)':
      mockWriteYamlFile.mock.resetCalls();
      await request.patch(`/api/customers/${FAKE_FILE_ID}/workstreams`)
        .send(VALID_WORKSTREAMS).set('Content-Type', 'application/json');
      assert.equal(mockWriteYamlFile.mock.calls.length, 1);

  - 'returns 422 when workstreams body is missing required group adr':
      const res = await request
        .patch(`/api/customers/${FAKE_FILE_ID}/workstreams`)
        .send({ biggy: {} })
        .set('Content-Type', 'application/json');
      assert.equal(res.status, 422);
      assert.ok(res.body.error.includes('adr'));

  - 'returns 422 when YAML validation fails on read':
      mockReadYamlFile.mock.mockImplementationOnce(() =>
        Promise.resolve('customer:\n  name: "Broken"\nstatus: "on_track"\n')
      );
      const res = await request
        .patch(`/api/customers/${FAKE_FILE_ID}/workstreams`)
        .send(VALID_WORKSTREAMS).set('Content-Type', 'application/json');
      assert.equal(res.status, 422);

  - 'replaces workstreams atomically - other YAML fields unchanged':
      const res = await request
        .patch(`/api/customers/${FAKE_FILE_ID}/workstreams`)
        .send(VALID_WORKSTREAMS).set('Content-Type', 'application/json');
      assert.equal(res.status, 200);
      // Verify by reading what writeYamlFile was called with (it received the full YAML string)
      // The simplest check: response has fileId and workstreams keys only
      assert.ok(res.body.fileId);
      assert.ok(res.body.workstreams);
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/workstreams.test.js 2>&1 | tail -8</automated>
  </verify>
  <done>All 5 workstreams.test.js assertions pass (exit 0); no t.todo skips remain; 422 for missing adr group confirmed.</done>
</task>

</tasks>

<verification>
Run full server suite:
cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/risks.test.js routes/milestones.test.js routes/actions.test.js routes/workstreams.test.js
</verification>

<success_criteria>
- server/routes/workstreams.js exists with PATCH / handler (mergeParams: true)
- REQUIRED_GROUPS validation: 422 if adr or biggy missing from body
- Atomic write: read -> replace workstreams key -> validate -> serialize -> writeYamlFile
- server/index.js mounts route BEFORE error handler
- All 5 workstreams.test.js assertions pass
- Full server suite green (no regressions in risks, milestones)
</success_criteria>

<output>
After completion, create .planning/phases/03-project-setup-action-manager/03-03-SUMMARY.md
</output>
