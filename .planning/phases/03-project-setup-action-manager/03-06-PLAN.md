---
phase: 03-project-setup-action-manager
plan: 06
type: execute
wave: 4
depends_on: [03-05]
files_modified: []
autonomous: false
requirements: [ACT-01, ACT-02, ACT-03, ACT-04, ACT-05, ACT-06, ACT-07, ACT-08, ACT-09, ACT-10, ACT-11, ACT-12]

must_haves:
  truths:
    - "All automated tests pass before checkpoint is reached"
    - "Action Manager table renders with sort and filter controls"
    - "Checking a checkbox moves the action to Completed table with Saving... indicator"
    - "Inline editing description/owner/due date works and shows Saving... indicator"
    - "Status badge cycles correctly on click"
    - "Add Action row creates a new action with A-### ID after save"
    - "Project Setup saves workstream changes that immediately appear in CustomerOverview scope tags"
  artifacts:
    - path: "none"
      provides: "Human verification that Phase 3 e2e functionality works end-to-end"
  key_links: []
---

<objective>
Wave 4: Run full test suite and perform human visual verification of all Phase 3 functionality.

Purpose: Automated tests verify server behavior; this checkpoint verifies the full client-server
integration: checkbox complete, inline edit, add action, status cycle, Project Setup save, and
CustomerOverview scope tag reflection.

Output: Phase 3 signed off by user; phase marked complete.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-project-setup-action-manager/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite — must be green before checkpoint</name>
  <files>none</files>
  <action>
Run the full server and client test suite. All tests must pass before the human checkpoint proceeds.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/risks.test.js routes/milestones.test.js routes/actions.test.js routes/workstreams.test.js && cd "/Users/jmiloslavsky/Documents/Project Assistant Code/client" && node --test src/lib/deriveCustomer.test.js 2>&1 | tail -10</automated>
  </verify>
  <done>All server route tests and client unit tests pass (exit 0); no failing assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual checkpoint — Phase 3 end-to-end verification</name>
  <action>Human verifies Phase 3 functionality in browser with npm run dev running.</action>
  <verify>See how-to-verify steps below.</verify>
  <done>User types "approved" confirming all Phase 3 functionality works end-to-end.</done>
  <what-built>
Phase 3 complete implementation:
- PATCH /api/customers/:id/actions (edit, complete, reopen, delay) + POST (add action)
- PATCH /api/customers/:id/workstreams (atomic workstreams replacement)
- ActionManager.jsx: sortable/filterable table, inline edit, checkbox complete, add action, completed table with reopen
- ProjectSetup.jsx: 11-subworkstream form with TagInput for scope sub-workstreams
- Route /customer/:id/setup registered; CustomerOverview Project Setup link fixed (no full page reload)
  </what-built>
  <how-to-verify>
With npm run dev running (both Express + Vite), test the following:

1. ACTION MANAGER (navigate to any customer, then Manage Actions):
   a. Table shows open actions with columns: checkbox, ID, description, owner, due, status badge, workstream
   b. Click a column header — rows reorder; click again — reverses direction
   c. Use the workstream filter dropdown — only matching workstream rows shown
   d. Click a status filter toggle (Delayed) — only delayed actions shown; click All — all back
   e. Click an action's checkbox — action immediately disappears from open table; Saving... appears briefly; action appears in Completed table after refetch
   f. Click on a description cell — input appears, type new text, press Enter — saved, Saving... indicator shown
   g. Click a status badge — cycles Open > Delayed > In Review > Open
   h. Click "Mark Delayed" button — status changes to Delayed
   i. In Add Action row: type description, owner, due date, choose workstream, click Save — new A-### action appears in open table
   j. Expand Completed table (click "Completed Actions") — shows completed actions; click Reopen — action moves back to open table

2. PROJECT SETUP (click "Project Setup" link in CustomerOverview workstream header):
   a. No full page reload — sidebar stays active (React Router navigation, not full reload)
   b. All 11 sub-workstreams visible grouped in ADR and Biggy cards
   c. Scope sub-workstreams (Inbound Integrations, Outbound Integrations, UDC, Real-Time Integrations) show tag input
   d. Non-scope sub-workstreams do NOT show tag input
   e. Add a tool name to a scope tag input (type name, press Enter) — tag chip appears
   f. Click Save — Saving... appears; after save, navigate to CustomerOverview — scope tags visible in workstream section
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
Full automated suite green + human visual verification of end-to-end flows.
</verification>

<success_criteria>
- All server tests pass (actions.test.js 12/12, workstreams.test.js 5/5, risks and milestones still green)
- All client unit tests pass (deriveCustomer.test.js passes all tests)
- Human verifies: checkbox complete, inline edit, add action, status cycle, completed table reopen
- Human verifies: Project Setup renders all 11 sub-workstreams, TagInput on scope sub-workstreams, Save writes to Drive and CustomerOverview reflects changes
- Phase 3 marked complete
</success_criteria>

<output>
After completion, create .planning/phases/03-project-setup-action-manager/03-06-SUMMARY.md
</output>
