---
phase: 01-data-foundation
plan: 03
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - bigpanda-app/lib/settings.ts
  - bigpanda-app/app/api/settings/route.ts
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-04

must_haves:
  truths:
    - "Reading settings with no config file returns defaults including workspace_path='/Documents/PM Application'"
    - "Writing and reading back any settings value round-trips correctly"
    - "The Anthropic API key is stored in .env.local only — never appears in ~/.bigpanda-app/settings.json"
    - "Settings API route responds to GET and POST without error"
    - "All 6 schedule cron strings are present in default settings"
  artifacts:
    - path: "bigpanda-app/lib/settings.ts"
      provides: "readSettings() and writeSettings() backed by ~/.bigpanda-app/settings.json"
      exports: ["readSettings", "writeSettings", "AppSettings"]
    - path: "bigpanda-app/app/api/settings/route.ts"
      provides: "GET returns current settings; POST writes partial settings update; handles ANTHROPIC_API_KEY via .env.local separately"
  key_links:
    - from: "bigpanda-app/app/api/settings/route.ts"
      to: "bigpanda-app/lib/settings.ts"
      via: "readSettings() / writeSettings()"
      pattern: "readSettings|writeSettings"
    - from: "SET-04 API key storage"
      to: ".env.local"
      via: "fs.writeFileSync to .env.local path"
      pattern: "ANTHROPIC_API_KEY"
---

<objective>
Implement the settings service (lib/settings.ts) backed by a user-home config file and the settings API route. This plan is independent of the DB schema (settings are file-based) and can run in parallel with Plan 02.

Purpose: Phase 2+ reads workspace_path and skill_path from settings. The API key must be provably absent from committed files from the first commit.

Output: Settings read/write working, API key stored in .env.local only, all 4 SET requirements satisfied.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md

<interfaces>
<!-- Settings patterns from RESEARCH.md Pattern 6 -->

Storage locations:
- ~/.bigpanda-app/settings.json  — workspace_path, skill_path, schedule times (all non-sensitive)
- bigpanda-app/.env.local        — DATABASE_URL, ANTHROPIC_API_KEY (sensitive, gitignored)

Default values (LOCKED — from CONTEXT.md):
- workspace_path: '/Documents/PM Application'  (NOT ~/Documents/BigPanda Projects/)
- skill_path: path.join(os.homedir(), '.claude', 'get-shit-done')
- schedule cron strings: morning_briefing '0 8 * * *', health_check '0 8 * * *',
  slack_sweep '0 9 * * *', tracker_weekly '0 7 * * 1',
  weekly_status '0 16 * * 4', biggy_briefing '0 9 * * 5'

AppSettings interface:
```typescript
interface AppSettings {
  workspace_path: string;
  skill_path: string;
  schedule: {
    morning_briefing: string;
    health_check: string;
    slack_sweep: string;
    tracker_weekly: string;
    weekly_status: string;
    biggy_briefing: string;
  };
}
```

API key pattern: The GET /api/settings response MUST NOT include the actual key value.
Return { has_api_key: boolean } for the key field. The POST handler for api_key writes
to .env.local only. Use fs to append/replace the ANTHROPIC_API_KEY line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement settings service and API route</name>
  <files>
    bigpanda-app/lib/settings.ts
    bigpanda-app/app/api/settings/route.ts
  </files>
  <behavior>
    - readSettings() with no file returns AppSettings with workspace_path='/Documents/PM Application'
    - writeSettings({ workspace_path: '/tmp/test' }) + readSettings() returns '/tmp/test'
    - settings JSON at ~/.bigpanda-app/settings.json never contains key named 'api_key' or 'ANTHROPIC_API_KEY'
    - GET /api/settings returns JSON with workspace_path, skill_path, schedule — and has_api_key (boolean), NOT the key value
    - POST /api/settings with body { workspace_path: '/new/path' } writes to settings.json
    - POST /api/settings with body { api_key: 'sk-ant-xxx' } writes ONLY to .env.local, NOT to settings.json
    - writeSettings is atomic: uses write-to-temp-then-rename to avoid partial writes
  </behavior>
  <action>
    Create `bigpanda-app/lib/settings.ts` with `import 'server-only'` at the top. Implement:

    1. `SETTINGS_PATH` constant: `path.join(os.homedir(), '.bigpanda-app', 'settings.json')`
    2. `DEFAULTS` object with exact values from the interfaces block (workspace_path='/Documents/PM Application')
    3. `readSettings(): AppSettings` — reads SETTINGS_PATH, merges with DEFAULTS (DEFAULTS win for missing keys), catches file-not-found and returns DEFAULTS
    4. `writeSettings(partial: Partial<AppSettings>): void` — reads current, merges, writes to a temp file then renames atomically (use `fs.renameSync(tempPath, SETTINGS_PATH)`)
    5. Export `AppSettings` interface

    Create `bigpanda-app/app/api/settings/route.ts`:

    `GET` handler:
    - Call `readSettings()`
    - Check if `process.env.ANTHROPIC_API_KEY` is set — return `{ has_api_key: true/false }` NOT the key value
    - Return `NextResponse.json({ ...settings, has_api_key })`

    `POST` handler:
    - Parse request body as JSON
    - If body contains `api_key` field: write `ANTHROPIC_API_KEY=<value>` to `.env.local` using fs (read current .env.local, replace/append the line, write back). Remove `api_key` from the body before passing to `writeSettings`.
    - For all non-api_key fields: call `writeSettings(body)`
    - Return `NextResponse.json({ ok: true })`

    For .env.local API key write: read existing .env.local content, use regex to replace existing `ANTHROPIC_API_KEY=.*` line or append if not present, write back. The .env.local path is `path.join(process.cwd(), '.env.local')`.

    Use Zod for input validation in the POST handler. Define a `settingsUpdateSchema` with all AppSettings fields optional plus optional `api_key: z.string()`.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && node --import tsx/esm --test tests/settings.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/settings.test.ts passes (all settings round-trip assertions green)
    - Default workspace_path is '/Documents/PM Application' (not ~/Documents/BigPanda Projects/)
    - settings.json never contains api_key field
    - lib/settings.ts uses 'server-only' import
  </done>
</task>

</tasks>

<verification>
`node --import tsx/esm --test tests/settings.test.ts` — all assertions pass.

Also verify manually: confirm `~/.bigpanda-app/settings.json` (if created) contains no `ANTHROPIC_API_KEY` or `api_key` field by reading the file.
</verification>

<success_criteria>
- tests/settings.test.ts is GREEN
- readSettings() default workspace_path is '/Documents/PM Application'
- GET /api/settings returns { has_api_key: boolean } — key value is never exposed
- POST /api/settings with api_key writes to .env.local only
- Atomic write: writeSettings uses write-to-temp + rename (no partial write risk)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-03-SUMMARY.md`
</output>
