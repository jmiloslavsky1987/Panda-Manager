---
phase: 01-data-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - "01-04"
files_modified:
  - bigpanda-app/scripts/migrate-local.ts
autonomous: true
requirements:
  - DATA-03
  - DATA-06

must_haves:
  truths:
    - "Running the migration script imports KAISER and AMEX projects from YAML frontmatter with all fields parsed correctly"
    - "Merck is imported as a stub project record (no YAML frontmatter) with a logged warning — NOT silently skipped"
    - "All source_file fields preserve the original filename (source tracing)"
    - "Running the migration script a second time produces no duplicate rows (idempotency)"
    - "projects table has exactly 3 rows after one run: KAISER, AMEX, Merck"
    - "actions, risks, milestones imported from YAML have external_id matching A-CUSTOMER-NNN / R-CUSTOMER-NNN / M-CUSTOMER-NNN format"
  artifacts:
    - path: "bigpanda-app/scripts/migrate-local.ts"
      provides: "Idempotent migration script for YAML context docs (KAISER, AMEX, Merck stub)"
      contains: "extractFrontmatter"
  key_links:
    - from: "bigpanda-app/scripts/migrate-local.ts"
      to: "/Documents/PM Application/*.md"
      via: "fs.readFileSync"
      pattern: "SOURCE_DIR.*Documents/PM Application"
    - from: "bigpanda-app/scripts/migrate-local.ts"
      to: "bigpanda-app/lib/yaml-export.ts"
      via: "parseYaml import"
      pattern: "parseYaml"
    - from: "bigpanda-app/scripts/migrate-local.ts"
      to: "bigpanda-app/db/index.ts"
      via: "db import for all inserts"
      pattern: "import.*db.*from"
---

<objective>
Implement the migration script for YAML context documents. This plan handles the three-case import logic: KAISER and AMEX (YAML frontmatter), and Merck (no frontmatter — stub project with logged warning).

Purpose: Establishes the three existing customer projects in PostgreSQL with source tracing preserved. The xlsx supplement import is handled in Plan 06 which depends on these project records existing.

Output: scripts/migrate-local.ts produces 3 project rows (KAISER, AMEX, Merck stub) plus all associated actions/risks/milestones/etc from YAML, idempotently.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-02-SUMMARY.md
@.planning/phases/01-data-foundation/01-04-SUMMARY.md

<interfaces>
<!-- Critical findings from RESEARCH.md — must be reflected in migration logic -->

Source path (LOCKED): '/Documents/PM Application'
Context doc filenames (from inspection):
  - KAISER_Project_Context.md (or similar) — HAS YAML frontmatter
  - AMEX_Project_Context.md (or similar) — HAS YAML frontmatter
  - MERCK_Project_Context_2026-03-16 copy.md — NO YAML frontmatter (starts with ## prose)

Frontmatter detection pattern (from RESEARCH.md Pitfall 3):
```typescript
function extractFrontmatter(content: string): { yaml: string | null; body: string } {
  if (!content.startsWith('---')) return { yaml: null, body: content };
  const end = content.indexOf('\n---', 3);
  if (end === -1) return { yaml: null, body: content };
  return { yaml: content.slice(4, end), body: content.slice(end + 4) };
}
```

YAML key mapping from source docs (RESEARCH.md Finding 3):
  Source: 'overall_status' → DB: overall_status
  Source: 'customer'       → DB: customer
  Source: 'project'        → DB: project fields (name, go_live_target, etc.)
  Source: 'workstreams'    → DB: workstreams table
  Source: 'risks'          → DB: risks table
  Source: 'milestones'     → DB: milestones table
  Missing in source: 'actions', 'artifacts', 'history' → empty arrays (YAML wins, xlsx supplements in Plan 06)

External ID format (from CONTEXT.md):
  actions: A-[CUSTOMER]-NNN
  risks: R-[CUSTOMER]-NNN
  milestones: M-[CUSTOMER]-NNN

Idempotency pattern (from CONTEXT.md):
  - Match projects by customer name (normalize to uppercase)
  - Match child records by external_id
  - If record exists: skip (YAML wins — do NOT update)
  - If record is new: insert

Merck stub:
  - Insert projects row: { name: 'Merck PA3.0', customer: 'MERCK', status: 'active', source_file: <merck_filename> }
  - Log: console.warn('[migrate] Merck context doc has no YAML frontmatter — stub project created. Manual data entry required.')
  - Do NOT crash or silently skip

Open Questions (Q-NNN IDs) from xlsx — handled in Plan 06.
In the YAML import: actions array in YAML source is empty (see Finding 3) so no action import from YAML.

js-yaml must use parseYaml() from lib/yaml-export.ts (which uses JSON_SCHEMA to prevent boolean coercion).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement YAML context doc migration script (KAISER, AMEX, Merck stub)</name>
  <files>
    bigpanda-app/scripts/migrate-local.ts
  </files>
  <behavior>
    - Script reads all *.md files from /Documents/PM Application/
    - For files with YAML frontmatter: parse with parseYaml() (JSON_SCHEMA), import project + workstreams + risks + milestones
    - For Merck (no frontmatter): create stub project row, emit console.warn, skip data import
    - After one run: SELECT COUNT(*) FROM projects = 3
    - After two runs: SELECT COUNT(*) FROM projects still = 3 (no duplicates)
    - All imported rows have source_file set to the source filename
    - KAISER project has actions = [] in DB (no YAML actions key present in source)
    - External IDs preserved: A-KAISER-001 not re-normalized
    - customer name stored as uppercase (KAISER, AMEX, MERCK) for consistent matching
  </behavior>
  <action>
    Create `bigpanda-app/scripts/migrate-local.ts`.

    Structure:

    ```typescript
    import fs from 'fs';
    import path from 'path';
    import { db } from '../db/index';
    import { projects, workstreams, risks, milestones, engagementHistory } from '../db/schema';
    import { parseYaml } from '../lib/yaml-export';
    import { eq, sql } from 'drizzle-orm';

    const SOURCE_DIR = '/Documents/PM Application';
    ```

    Implement `extractFrontmatter(content: string)` — exact implementation from interfaces block.

    Main function `runMigration()`:

    1. Read all *.md files from SOURCE_DIR using `fs.readdirSync`
    2. For each file:
       a. Read content with `fs.readFileSync`
       b. Call `extractFrontmatter(content)`
       c. If `yaml === null` (no frontmatter):
          - Extract customer from filename (e.g. 'MERCK' from 'MERCK_Project_Context...')
          - Check if project already exists: `db.select().from(projects).where(eq(projects.customer, 'MERCK'))`
          - If not exists: insert stub `{ name: \`${customer} PA3.0\`, customer: customer.toUpperCase(), status: 'active', source_file: filename }`
          - `console.warn(\`[migrate] ${filename} has no YAML frontmatter — stub project created. Manual data entry required.\`)`
          - Continue to next file
       d. If yaml string present:
          - `const data = parseYaml(yaml) as Record<string, unknown>`
          - Extract customer: `String(data.customer ?? '').toUpperCase()` — normalize to uppercase
          - Check if project exists (by customer, case-insensitive): `WHERE UPPER(customer) = ${customerUpper}`
          - If exists: skip (log `[migrate] Project ${customer} already exists — skipping`)
          - If not exists: insert project row, capture returned `id`
          - Import `workstreams` array: for each workstream, check by `(project_id, name)` — insert if not exists
          - Import `risks` array: check by `(project_id, external_id)` — insert if not exists
          - Import `milestones` array: check by `(project_id, external_id)` — insert if not exists
          - Map `data.history` array → `engagementHistory` table (if present)
          - Source tracing: every inserted row gets `source: 'yaml'` and the project gets `source_file: filename`

    3. After all files processed, log summary: `[migrate] Projects: N inserted, M skipped`

    Run the script with `npx tsx scripts/migrate-local.ts` for verification.

    Important: the script reads from `/Documents/PM Application` — this path is hardcoded for the migration. The settings workspace_path is for OUTPUT files (where generated files are saved), not the migration source. Do NOT use settings.workspace_path here.

    Handle the customer name case mismatch: xlsx uses 'Kaiser' (mixed case) but YAML uses customer field. Normalize ALL customer values to uppercase on insert. The external_id fields (A-KAISER-001) are preserved as-is.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && node --import tsx/esm --test tests/migration.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - tests/migration.test.ts YAML import assertions pass
    - SELECT COUNT(*) FROM projects = 3 after one run
    - Running migration twice produces same row count (idempotent)
    - console.warn appears for Merck: "[migrate] ... has no YAML frontmatter"
    - All KAISER/AMEX records have source='yaml'
  </done>
</task>

</tasks>

<verification>
`node --import tsx/esm --test tests/migration.test.ts` — YAML import assertions (3 projects, idempotency, external_id format) all GREEN.

Manual check: `SELECT name, customer, source_file FROM projects` returns 3 rows with correct filenames in source_file column.
</verification>

<success_criteria>
- 3 project rows after migration (KAISER, AMEX, MERCK)
- MERCK row has status='active', source_file set, and a console.warn was emitted during import
- KAISER and AMEX rows have workstreams, risks, milestones imported (from YAML arrays)
- Running migration twice: same 3 rows, no duplicates anywhere
- All imported rows have source='yaml'
- migration.test.ts YAML assertions GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-05-SUMMARY.md`
</output>
