---
phase: 01-data-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/schema.test.ts
  - tests/append-only.test.ts
  - tests/rls.test.ts
  - tests/pool.test.ts
  - tests/migration.test.ts
  - tests/yaml-roundtrip.test.ts
  - tests/outputs.test.ts
  - tests/settings.test.ts
  - tests/fixtures/seed.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - DATA-08
  - SET-01
  - SET-02
  - SET-03
  - SET-04

must_haves:
  truths:
    - "All test files exist with failing (RED) assertions before any implementation runs"
    - "Wave 0 is complete: every subsequent plan has an automated verify command available"
    - "tsx is installed so Node --test can execute TypeScript test files"
    - "Test fixtures define two projects (KAISER, AMEX) and minimal seed records"
  artifacts:
    - path: "tests/schema.test.ts"
      provides: "Table existence checks for all 12 domain tables (DATA-01)"
    - path: "tests/append-only.test.ts"
      provides: "DB trigger rejection assertions for engagement_history and key_decisions (DATA-02)"
    - path: "tests/rls.test.ts"
      provides: "Cross-project isolation assertions (DATA-06)"
    - path: "tests/pool.test.ts"
      provides: "Singleton pool identity assertion (DATA-08)"
    - path: "tests/migration.test.ts"
      provides: "YAML import + xlsx import + idempotency assertions (DATA-03, DATA-04)"
    - path: "tests/yaml-roundtrip.test.ts"
      provides: "Round-trip stability assertion (DATA-05)"
    - path: "tests/outputs.test.ts"
      provides: "Outputs table idempotency key + status=running assertions (DATA-07)"
    - path: "tests/settings.test.ts"
      provides: "Settings read/write assertions for all four settings (SET-01 through SET-04)"
    - path: "tests/fixtures/seed.ts"
      provides: "In-memory test fixtures with two projects and minimal records"
  key_links:
    - from: "tests/*.test.ts"
      to: "node --test"
      via: "tsx loader"
      pattern: "node --test --require tsx/cjs tests/"
---

<objective>
Create all test stub files before any implementation work begins (Wave 0).

Purpose: The Nyquist rule requires every subsequent task to have an `<automated>` verify command. All commands point to files in `tests/`. This plan creates those files as failing stubs so the verify commands are valid from Wave 1 forward.

Output: 8 test files + 1 fixture file, all failing (RED), all runnable via `node --test`.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install tsx and create test fixture + schema/pool/rls/append-only stubs</name>
  <files>
    tests/fixtures/seed.ts
    tests/schema.test.ts
    tests/append-only.test.ts
    tests/rls.test.ts
    tests/pool.test.ts
  </files>
  <behavior>
    - Test: schema.test.ts — asserts 12 tables exist: projects, workstreams, actions, risks, milestones, artifacts, engagement_history, key_decisions, stakeholders, tasks, outputs, plan_templates — must FAIL (tables don't exist yet)
    - Test: append-only.test.ts — asserts that UPDATE on engagement_history throws a PostgreSQL exception — must FAIL
    - Test: append-only.test.ts — asserts that DELETE on key_decisions throws a PostgreSQL exception — must FAIL
    - Test: rls.test.ts — asserts that querying actions without setting app.current_project_id returns 0 rows (not cross-project leak) — must FAIL
    - Test: pool.test.ts — asserts that importing db/index.ts twice returns the same db instance (referential equality via globalThis.__pgConnection) — must FAIL (file doesn't exist)
    - Fixture: seed.ts exports KAISER_PROJECT and AMEX_PROJECT objects matching the projects table schema, plus 2 actions each
  </behavior>
  <action>
    First, install tsx as a dev dependency at the project root (not inside server/ or client/):
    ```
    npm install -D tsx
    ```

    Create `tests/fixtures/seed.ts` exporting plain TypeScript objects:
    ```typescript
    export const KAISER_PROJECT = { name: 'Kaiser PA3.0', customer: 'KAISER', status: 'active', source_file: 'KAISER_Project_Context.md' };
    export const AMEX_PROJECT = { name: 'AMEX PA3.0', customer: 'AMEX', status: 'active', source_file: 'AMEX_Project_Context.md' };
    export const SAMPLE_ACTION = { external_id: 'A-KAISER-001', description: 'Test action', status: 'open', source: 'yaml' };
    ```

    Create `tests/schema.test.ts` using Node.js built-in test runner. Import the postgres connection using DATABASE_URL from env. For each of the 12 expected tables, run `SELECT 1 FROM information_schema.tables WHERE table_name = '{table}'` and assert the row count is 1. Use `assert.strictEqual`. Mark the test with `{ skip: false }` so it runs and fails.

    Create `tests/append-only.test.ts`. Import the db singleton. Insert a row into engagement_history, then attempt `UPDATE engagement_history SET content = 'x' WHERE id = $1`. Use `assert.rejects()` and verify the error message includes 'append-only'. Repeat for DELETE on key_decisions.

    Create `tests/rls.test.ts`. Connect as app_user role (or the role that has RLS active). Query `SELECT * FROM actions` WITHOUT setting app.current_project_id. Assert `result.length === 0`. Add a comment: `// RLS must ensure missing project scope = empty result, not full table`.

    Create `tests/pool.test.ts`. Import `db` from `'../db/index'` twice (use require() or two dynamic imports). Assert `globalThis.__pgConnection !== undefined` and that both imports return the same reference.

    All test files must NOT use watch flags. Tests must complete in under 15 seconds.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && node --import tsx/esm --test tests/schema.test.ts tests/pool.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 test files exist and execute (they FAIL — that is correct at Wave 0). tsx is in package.json devDependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create migration, yaml-roundtrip, outputs, and settings test stubs</name>
  <files>
    tests/migration.test.ts
    tests/yaml-roundtrip.test.ts
    tests/outputs.test.ts
    tests/settings.test.ts
  </files>
  <behavior>
    - Test: migration.test.ts — after running migrate-local.ts, asserts projects table has 3 rows (KAISER, AMEX, Merck) — must FAIL (no script yet)
    - Test: migration.test.ts — asserts idempotency: running migrate-local.ts twice produces same row count (no duplicates) — must FAIL
    - Test: migration.test.ts — asserts KAISER actions imported from xlsx supplement matches external_id format A-KAISER-NNN — must FAIL
    - Test: yaml-roundtrip.test.ts — serializes a sample project object to YAML, re-parses, asserts deep equality — must FAIL (no yaml-export.ts yet)
    - Test: yaml-roundtrip.test.ts — asserts output YAML contains all 9 REQUIRED_TOP_LEVEL_KEYS even when some have empty arrays — must FAIL
    - Test: outputs.test.ts — inserts an output row, asserts status='running', asserts idempotency_key is unique — must FAIL
    - Test: settings.test.ts — writes settings to temp path, reads back, asserts workspace_path round-trips correctly — must FAIL (no settings.ts yet)
    - Test: settings.test.ts — asserts default workspace_path is '/Documents/PM Application' (not ~/Documents/BigPanda Projects/) — must FAIL
    - Test: settings.test.ts — asserts API key is NOT exposed in settings JSON file (only in .env.local) — must FAIL
  </behavior>
  <action>
    Create `tests/migration.test.ts`. It should:
    1. Import `db` from `../db/index`
    2. Run `await import('../scripts/migrate-local')` (or exec the script via child_process)
    3. Query `SELECT COUNT(*) FROM projects` and assert count >= 3
    4. Run the import again and assert count did not increase (idempotency)
    5. Query actions WHERE external_id LIKE 'A-KAISER-%' and assert at least one exists

    Create `tests/yaml-roundtrip.test.ts`. Import `buildYamlDocument` and `parseYaml` from `../lib/yaml-export`. Build a sample project document, serialize to YAML, parse back, and assert:
    - All 9 REQUIRED_TOP_LEVEL_KEYS are present in the serialized string
    - Deep equality holds after parse (round-trip stable)
    - The string starts with `---\n` (frontmatter marker)

    Create `tests/outputs.test.ts`. Import `db` and insert a row into `outputs` with a unique idempotency_key. Assert `status === 'running'`. Then try to insert another row with the same idempotency_key and assert it throws a unique constraint violation.

    Create `tests/settings.test.ts`. Override the SETTINGS_PATH to a temp file path for isolation. Call `writeSettings({ workspace_path: '/tmp/test' })`, then `readSettings()` and assert the value round-trips. Also assert `readSettings().workspace_path` equals `/Documents/PM Application` when no settings file exists (default value). Assert there is no `api_key` or `ANTHROPIC_API_KEY` field in the settings JSON file.

    All files use Node.js built-in `{ test, describe }` from `'node:test'` and `assert` from `'node:assert'`.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && node --import tsx/esm --test tests/migration.test.ts tests/settings.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 test files exist and execute (they FAIL — correct at Wave 0). Running `node --import tsx/esm --test tests/*.test.ts` outputs test failures, not syntax errors or "file not found".</done>
</task>

</tasks>

<verification>
All 8 test files + fixture exist. `node --import tsx/esm --test tests/*.test.ts` runs without "Cannot find module" or syntax errors — tests fail on assertions (expected), not on import errors.
</verification>

<success_criteria>
- `npm list tsx` shows tsx in devDependencies at project root
- All 8 test files pass `node --import tsx/esm --test {file}` without crashing (assertion failures are OK — that is the Wave 0 state)
- `tests/fixtures/seed.ts` exports KAISER_PROJECT, AMEX_PROJECT, and SAMPLE_ACTION objects
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
