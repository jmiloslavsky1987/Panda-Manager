---
phase: 06-mcp-integrations
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/phase6.spec.ts
autonomous: true
requirements: [SKILL-10, DASH-04, DASH-05]
must_haves:
  truths:
    - "E2E test file exists with RED stubs for DASH-04, DASH-05, SKILL-10, and MCP Settings UI"
    - "Running the full suite shows 4 failing tests (RED baseline established)"
    - "Each test can be targeted individually via --grep with its requirement ID"
  artifacts:
    - path: "tests/e2e/phase6.spec.ts"
      provides: "Wave 0 RED stubs for all Phase 6 requirements"
      exports: ["DASH-04 test", "DASH-05 test", "SKILL-10 test", "MCP Settings UI test"]
  key_links:
    - from: "tests/e2e/phase6.spec.ts"
      to: "npx playwright test tests/e2e/phase6.spec.ts"
      via: "Playwright test runner"
      pattern: "expect\\(false"
---

<objective>
Create the Wave 0 RED baseline: Playwright E2E stub tests for all Phase 6 behaviors. Stubs are intentionally failing so implementation plans have a concrete GREEN target.

Purpose: Nyquist compliance — every requirement must have an automated verify command from day one. Stubs provide the RED side of RED→GREEN.
Output: tests/e2e/phase6.spec.ts with 4 stub tests.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mcp-integrations/06-CONTEXT.md
@.planning/phases/06-mcp-integrations/06-VALIDATION.md
</context>

<interfaces>
<!-- Existing E2E stub pattern from prior phases (must match exactly) -->
<!-- From tests/e2e/phase2.spec.ts and phase5.spec.ts: -->
```typescript
// Wave 0 stub pattern — established in 02-01, 03-01, 04-01, 05.1-01
// First line is the RED assertion — visibly fails without server running
// Requirement IDs in test names for --grep targeting
test('DASH-XX: description', async ({ page }) => {
  expect(false, 'stub').toBe(true);  // RED — first line, always fails
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 6 E2E stub file</name>
  <files>tests/e2e/phase6.spec.ts</files>
  <action>
Create `tests/e2e/phase6.spec.ts` following the exact stub pattern established in phases 2, 3, 4, 5, and 5.1:
- `expect(false, 'stub').toBe(true)` as FIRST line in each test body (makes RED visible without a server)
- Requirement ID in test name for `--grep` targeting
- 4 stubs total:

**DASH-04 stub:**
```typescript
test('DASH-04: Risk Heat Map panel visible on Dashboard', async ({ page }) => {
  expect(false, 'stub').toBe(true);
  // When implemented: navigate to /, assert data-testid="risk-heat-map" exists
});
```

**DASH-05 stub:**
```typescript
test('DASH-05: Cross-Account Watch List panel visible on Dashboard', async ({ page }) => {
  expect(false, 'stub').toBe(true);
  // When implemented: navigate to /, assert data-testid="watch-list" exists
});
```

**SKILL-10 stub (assert-if-present — skill may require live MCP):**
```typescript
test('SKILL-10: Customer Project Tracker skill exists and is triggerable', async ({ page }) => {
  expect(false, 'stub').toBe(true);
  // When implemented: navigate to Skills tab for any project, assert skill-card[data-skill="customer-project-tracker"] exists
});
```

**MCP Settings UI stub:**
```typescript
test('MCP: Settings page has MCP Servers tab', async ({ page }) => {
  expect(false, 'stub').toBe(true);
  // When implemented: navigate to /settings, click "MCP Servers" tab, assert form is visible
});
```

Use the same `import { test, expect } from '@playwright/test'` import as other phase spec files. Add `test.describe('Phase 6: MCP Integrations', () => { ... })` wrapper block.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/phase6.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>File exists; running `npx playwright test tests/e2e/phase6.spec.ts` shows 4 tests — all FAILED (RED baseline). Each test name contains its requirement ID for --grep targeting.</done>
</task>

</tasks>

<verification>
`npx playwright test tests/e2e/phase6.spec.ts --reporter=list` shows 4 failing tests.
`npx playwright test tests/e2e/phase6.spec.ts --grep "DASH-04"` shows 1 failing test.
`npx playwright test tests/e2e/phase6.spec.ts --grep "SKILL-10"` shows 1 failing test.
</verification>

<success_criteria>
tests/e2e/phase6.spec.ts exists with 4 RED stubs. All 4 fail with the stub message. --grep targeting works per requirement ID.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mcp-integrations/06-01-SUMMARY.md`
</output>
