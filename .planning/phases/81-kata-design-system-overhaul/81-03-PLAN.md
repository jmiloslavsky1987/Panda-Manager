---
phase: 81-kata-design-system-overhaul
plan: "03"
type: execute
wave: 2
depends_on:
  - "81-01"
files_modified:
  - components/CreateJobWizard.tsx
  - components/ui/dialog.tsx
  - components/ui/checkbox.tsx
  - components/IngestionStepper.tsx
  - components/GlobalTimeView.tsx
  - components/PortfolioTableClient.tsx
  - components/workspace/DangerZoneSection.tsx
  - components/PromptEditModal.tsx
  - components/SprintSummaryPanel.tsx
  - components/ExceptionsPanel.tsx
  - components/arch/IntegrationDetailDrawer.tsx
  - components/teams/NodeDetailDrawer.tsx
  - components/ArchivedBanner.tsx
  - components/SchedulerJobRow.tsx
  - components/WbsGeneratePlanModal.tsx
  - components/ProjectWizard.tsx
  - components/PortfolioExceptionsPanel.tsx
  - components/WbsNode.tsx
  - components/AiPlanPanel.tsx
  - components/ScanForUpdatesButton.tsx
autonomous: true
requirements:
  - KDS-02
  - KDS-07

must_haves:
  truths:
    - "No file in the codebase imports from 'lucide-react' except IntegrationDetailDrawer.tsx and NodeDetailDrawer.tsx (Architecture tab — these may retain custom icons but must still migrate lucide icons)"
    - "All icon usages that were lucide-react are now <Icon name='...' /> from components/Icon.tsx"
    - "Functional behavior of all migrated components is unchanged — buttons still trigger same handlers"
  artifacts:
    - path: "components/CreateJobWizard.tsx"
      provides: "lucide removed; Check → Icon check"
    - path: "components/ui/dialog.tsx"
      provides: "lucide removed; X → Icon close"
    - path: "components/ui/checkbox.tsx"
      provides: "lucide removed; Check → Icon check"
    - path: "components/IngestionStepper.tsx"
      provides: "lucide removed; Loader2/CheckCircle/XCircle/Circle → Icon equivalents"
    - path: "components/GlobalTimeView.tsx"
      provides: "lucide removed; all lucide icons → Icon equivalents"
    - path: "components/PortfolioTableClient.tsx"
      provides: "lucide removed; ChevronDown/Up/AlertCircle → Icon equivalents"
    - path: "components/ExceptionsPanel.tsx"
      provides: "lucide removed; all icons → Icon equivalents"
    - path: "components/PromptEditModal.tsx"
      provides: "lucide removed; Lock/Maximize2/Bold/etc → Icon equivalents"
    - path: "components/WbsNode.tsx"
      provides: "lucide removed; Trash2/Plus/ChevronRight/GripVertical/Sparkles → Icon equivalents"
  key_links:
    - from: "all 20 migrated files"
      to: "components/Icon.tsx"
      via: "import { Icon } from '@/components/Icon' or '../Icon'"
      pattern: "from.*Icon"
---

<objective>
Migrate all 20 non-Sidebar lucide-react files to the `<Icon>` wrapper component.
Sidebar.tsx, SidebarProjectItem.tsx, and SidebarUserIsland.tsx are migrated in Plan 02.
This plan covers the remaining 20 files using the icon map from RESEARCH.md.

NOTE: Plan 02 runs in parallel with this plan (both Wave 2). This plan does NOT touch
Sidebar.tsx, SidebarProjectItem.tsx, or SidebarUserIsland.tsx — those are Plan 02's ownership.

Purpose: Complete KDS-02 (lucide-react fully removed) and KDS-07 (no regressions from icon migration).
Output: 20 files with lucide removed, Icon wrapper imported, all icon usages migrated.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-kata-design-system-overhaul/81-CONTEXT.md
@.planning/phases/81-kata-design-system-overhaul/81-RESEARCH.md
@.planning/phases/81-kata-design-system-overhaul/81-01-SUMMARY.md

<interfaces>
<!-- Icon migration map (from RESEARCH.md — complete): -->
<!-- lucide Check        → <Icon name="check" />             -->
<!-- lucide X            → <Icon name="close" />             -->
<!-- lucide Loader2      → <Icon name="progress_activity" /> -->
<!-- lucide CheckCircle  → <Icon name="task_alt" />          -->
<!-- lucide XCircle      → <Icon name="cancel" />            -->
<!-- lucide Circle       → <Icon name="radio_button_unchecked" /> -->
<!-- lucide Clock        → <Icon name="schedule" />          -->
<!-- lucide Download     → <Icon name="download" />          -->
<!-- lucide Trash2       → <Icon name="delete" />            -->
<!-- lucide Plus         → <Icon name="add" />               -->
<!-- lucide ChevronDown  → <Icon name="expand_more" />       -->
<!-- lucide ChevronUp    → <Icon name="expand_less" />       -->
<!-- lucide AlertCircle  → <Icon name="error" />             -->
<!-- lucide AlertTriangle→ <Icon name="warning" />           -->
<!-- lucide Lock         → <Icon name="lock" />              -->
<!-- lucide Maximize2    → <Icon name="open_in_full" />      -->
<!-- lucide Minimize2    → <Icon name="close_fullscreen" />  -->
<!-- lucide Bold         → <Icon name="format_bold" />       -->
<!-- lucide Italic       → <Icon name="format_italic" />     -->
<!-- lucide Code         → <Icon name="code" />              -->
<!-- lucide Heading      → <Icon name="title" />             -->
<!-- lucide ChevronRight → <Icon name="chevron_right" />     -->
<!-- lucide RefreshCw    → <Icon name="refresh" />           -->
<!-- lucide Archive      → <Icon name="archive" />           -->
<!-- lucide GripVertical → <Icon name="drag_indicator" />    -->
<!-- lucide Sparkles     → <Icon name="auto_awesome" />      -->
<!-- lucide BookOpen     → <Icon name="menu_book" />         -->
<!-- lucide CalendarClock→ <Icon name="event_available" />   -->
<!-- lucide Library      → <Icon name="inventory_2" />       -->
<!-- lucide Settings     → <Icon name="settings" />          -->
<!-- lucide CalendarDays → <Icon name="event_note" />        -->
<!-- lucide LogOut       → <Icon name="logout" />            -->

<!-- IMPORTANT: lucide icon components used className for sizing (className="w-4 h-4") -->
<!-- <Icon> uses size prop (number, px). Common mappings: -->
<!--   className="w-4 h-4" → size={16}  -->
<!--   className="w-5 h-5" → size={20}  -->
<!--   className="w-6 h-6" → size={24}  -->
<!--   className="w-3 h-3" → size={12}  -->
<!-- Pass other className attributes (like color, opacity) directly to Icon's className prop -->

<!-- Architecture tab files: IntegrationDetailDrawer.tsx, NodeDetailDrawer.tsx -->
<!-- These DO still import lucide-react for the custom integration icons? -->
<!-- NO — they use lucide X icon (close button). Migrate the lucide X → Icon close. -->
<!-- The Architecture tab IMAGES/SVGs (BigPanda integration logos) are custom images, -->
<!-- NOT lucide icons — those don't need to change. -->

<!-- Import path for Icon: use '@/components/Icon' for app/ files, -->
<!-- relative path for components/ files (e.g., './Icon' or '../Icon') -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate shadcn UI components and core shared components</name>
  <files>components/ui/dialog.tsx, components/ui/checkbox.tsx, components/IngestionStepper.tsx, components/ArchivedBanner.tsx, components/ScanForUpdatesButton.tsx, components/SprintSummaryPanel.tsx, components/SchedulerJobRow.tsx, components/CreateJobWizard.tsx, components/ProjectWizard.tsx</files>
  <action>
    For each file:
    1. Remove the `import { ... } from 'lucide-react'` line (or remove specific named imports)
    2. Add `import { Icon } from '../Icon'` (or '@/components/Icon' for deeply nested files)
    3. Replace each lucide component JSX usage with `<Icon name="..." size={N} className="..." />`
       using the icon map in the interfaces section
    4. Convert size: `className="w-4 h-4"` → `size={16}`, `className="w-5 h-5"` → `size={20}`, etc.
    5. Keep all other logic, state, handlers, props unchanged

    File-by-file specifics:
    - dialog.tsx: X → Icon name="close" (used in DialogClose button)
    - checkbox.tsx: Check → Icon name="check" size={12} (inside the indicator)
    - IngestionStepper.tsx: Loader2→progress_activity, CheckCircle→task_alt, XCircle→cancel, Circle→radio_button_unchecked
    - ArchivedBanner.tsx: Archive → Icon name="archive"
    - ScanForUpdatesButton.tsx: RefreshCw → Icon name="refresh"
    - SprintSummaryPanel.tsx: ChevronDown→expand_more, ChevronRight→chevron_right, RefreshCw→refresh
    - SchedulerJobRow.tsx: ChevronDown→expand_more, ChevronRight→chevron_right
    - CreateJobWizard.tsx: Check → Icon name="check"
    - ProjectWizard.tsx: Check → Icon name="check"

    IMPORTANT for checkbox.tsx (shadcn UI): The Check icon in Radix's checkbox indicator
    renders inside `<CheckboxPrimitive.Indicator>`. Replace the lucide Check with Icon but
    keep it inside the Indicator. The size should be small (size={14} or size={12}).
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && grep -l "from 'lucide-react'" components/ui/dialog.tsx components/ui/checkbox.tsx components/IngestionStepper.tsx components/ArchivedBanner.tsx components/ScanForUpdatesButton.tsx components/SprintSummaryPanel.tsx components/SchedulerJobRow.tsx components/CreateJobWizard.tsx components/ProjectWizard.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <done>grep returns 0 (no lucide-react imports remain in these 9 files); npm run build still exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Migrate feature components and complete icon-migration test to GREEN</name>
  <files>components/GlobalTimeView.tsx, components/PortfolioTableClient.tsx, components/PortfolioExceptionsPanel.tsx, components/ExceptionsPanel.tsx, components/workspace/DangerZoneSection.tsx, components/PromptEditModal.tsx, components/WbsGeneratePlanModal.tsx, components/WbsNode.tsx, components/AiPlanPanel.tsx, components/arch/IntegrationDetailDrawer.tsx, components/teams/NodeDetailDrawer.tsx</files>
  <action>
    Apply the same lucide → Icon migration pattern to the remaining 11 files:

    - GlobalTimeView.tsx: CheckCircle→task_alt, XCircle→cancel, Clock→schedule, Download→download, Trash2→delete, Plus→add
    - PortfolioTableClient.tsx: ChevronDown→expand_more, ChevronUp→expand_less, AlertCircle→error
    - PortfolioExceptionsPanel.tsx: ChevronDown→expand_more, ChevronUp→expand_less
    - ExceptionsPanel.tsx: CheckCircle→task_alt, Clock→schedule, AlertTriangle→warning, RefreshCw→refresh
    - workspace/DangerZoneSection.tsx: AlertTriangle→warning
    - PromptEditModal.tsx: Lock→lock, Maximize2→open_in_full, Minimize2→close_fullscreen, Bold→format_bold, Italic→format_italic, Code→code, Heading→title, X→close
    - WbsGeneratePlanModal.tsx: Loader2→progress_activity, Sparkles→auto_awesome
    - WbsNode.tsx: Trash2→delete, Plus→add, ChevronRight→chevron_right, GripVertical→drag_indicator, Sparkles→auto_awesome
    - AiPlanPanel.tsx: Loader2→progress_activity, Sparkles→auto_awesome
    - arch/IntegrationDetailDrawer.tsx: X→close (ONLY migrate lucide X; do NOT touch any custom BigPanda integration images/SVGs)
    - teams/NodeDetailDrawer.tsx: X→close (ONLY migrate lucide X; preserve all React Flow / custom node content)

    For nested component paths (workspace/, arch/, teams/), use correct relative import path:
    `import { Icon } from '../Icon'` (two levels up) or `import { Icon } from '../../Icon'`
    as appropriate for each file's directory depth.

    After all 11 files are migrated, run the icon-migration test — Test 3 should now be GREEN
    (zero lucide-react imports remaining across all 22 files including the ones migrated in Task 1
    and in Plan 02's Sidebar.tsx/SidebarProjectItem/SidebarUserIsland).
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/kds/icon-migration.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>icon-migration.test.ts: all 3 tests GREEN (Icon.tsx exists, material-symbols-outlined in source, ZERO lucide-react imports in all 22 files); production build clean</done>
</task>

</tasks>

<verification>
Run: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/kds/icon-migration.test.ts --reporter=verbose`

All 3 tests GREEN. Then: `npm run build` exits 0.

Spot-check: grep for any remaining lucide imports:
`grep -r "from 'lucide-react'" /Users/jmiloslavsky/Documents/Panda-Manager/components/ /Users/jmiloslavsky/Documents/Panda-Manager/app/ --include="*.tsx"` returns no results.
</verification>

<success_criteria>
- All 20 files (this plan) + Sidebar files (Plan 02) have zero lucide-react imports
- icon-migration.test.ts: all 3 tests GREEN
- No functional regressions: components compile, handlers preserved, TypeScript clean
- Production build exits 0
- Architecture tab custom images/SVGs untouched (no BigPanda integration logos changed)
</success_criteria>

<output>
After completion, create `.planning/phases/81-kata-design-system-overhaul/81-03-SUMMARY.md`
</output>
