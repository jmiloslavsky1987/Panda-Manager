---
phase: 81-kata-design-system-overhaul
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/kds/token-import.test.ts
  - tests/kds/icon-migration.test.ts
  - tests/kds/command-rail.test.ts
  - tests/kds/page-bar.test.ts
  - tests/kds/portfolio-layout.test.ts
  - tests/kds/workspace-kpi.test.ts
  - tests/kds/theme-persistence.test.ts
autonomous: true
requirements:
  - KDS-01
  - KDS-02
  - KDS-03
  - KDS-04
  - KDS-05
  - KDS-06
  - KDS-07
  - KDS-08

must_haves:
  truths:
    - "All 7 KDS test files exist in tests/kds/ and run with vitest"
    - "Each test is RED (stub behavior returns wrong values so assertion fails)"
    - "Full test suite still passes (no existing tests broken by adding new files)"
  artifacts:
    - path: "tests/kds/token-import.test.ts"
      provides: "KDS-01 gate: kata-tokens.css import + @theme inline aliases"
    - path: "tests/kds/icon-migration.test.ts"
      provides: "KDS-02 + KDS-07 gate: Icon.tsx existence + no lucide-react imports in 22 files"
    - path: "tests/kds/command-rail.test.ts"
      provides: "KDS-03 gate: data-theme=dark, search pill, top nav links"
    - path: "tests/kds/page-bar.test.ts"
      provides: "KDS-04 gate: PageBarContext.tsx and PageBar.tsx existence + theme toggle"
    - path: "tests/kds/portfolio-layout.test.ts"
      provides: "KDS-05 gate: portfolio page imports hero, briefing strip, project grid"
    - path: "tests/kds/workspace-kpi.test.ts"
      provides: "KDS-06 gate: WorkspaceKpiStrip.tsx with 5-column grid"
    - path: "tests/kds/theme-persistence.test.ts"
      provides: "KDS-08 gate: ThemeProvider localStorage key + flash-prevention script"
  key_links:
    - from: "tests/kds/*.test.ts"
      to: "components/Sidebar.tsx, components/Icon.tsx, components/PageBar.tsx, etc."
      via: "fs.readFileSync source-scan pattern (established in Phase 79)"
      pattern: "fs.readFileSync.*Sidebar\\.tsx"
---

<objective>
Create all 7 Wave 0 test scaffolds for Phase 81. Each test uses the source-scan pattern
(fs.readFileSync) established in Phase 79 sidebar-daily-prep.test.ts. Tests must be RED
now (targets do not exist yet) and GREEN after each implementation plan completes.

Purpose: Nyquist compliance — every KDS requirement has an automated gate before
implementation begins. Executor cannot skip validation.
Output: 7 test files in tests/kds/, all RED, full suite green.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-kata-design-system-overhaul/81-CONTEXT.md
@.planning/phases/81-kata-design-system-overhaul/81-RESEARCH.md

<interfaces>
<!-- Source-scan pattern from tests/components/sidebar-daily-prep.test.ts -->
<!-- All KDS tests use fs.readFileSync to read component source files. -->
<!-- Tests are intentionally RED because target files do not yet exist. -->

```typescript
// Pattern: read source file, assert on string content
import * as fs from 'fs';
import * as path from 'path';

function readSource(relPath: string): string {
  return fs.readFileSync(path.join(process.cwd(), relPath), 'utf-8');
}
```

<!-- Lucide-react files to check (22 total, from RESEARCH.md): -->
<!-- components/CreateJobWizard.tsx, components/ui/dialog.tsx,     -->
<!-- components/ui/checkbox.tsx, components/IngestionStepper.tsx,  -->
<!-- components/GlobalTimeView.tsx, components/PortfolioTableClient.tsx, -->
<!-- components/workspace/DangerZoneSection.tsx, components/PromptEditModal.tsx, -->
<!-- components/SprintSummaryPanel.tsx, components/ExceptionsPanel.tsx, -->
<!-- components/arch/IntegrationDetailDrawer.tsx,                   -->
<!-- components/teams/NodeDetailDrawer.tsx, components/ArchivedBanner.tsx, -->
<!-- components/SchedulerJobRow.tsx, components/WbsGeneratePlanModal.tsx, -->
<!-- components/Sidebar.tsx, components/ProjectWizard.tsx,          -->
<!-- components/PortfolioExceptionsPanel.tsx, components/WbsNode.tsx, -->
<!-- components/SidebarUserIsland.tsx, components/AiPlanPanel.tsx,  -->
<!-- components/ScanForUpdatesButton.tsx                            -->
<!-- NOTE: Architecture tab files (IntegrationDetailDrawer.tsx,     -->
<!-- NodeDetailDrawer.tsx) ARE in this list — they must have lucide  -->
<!-- removed too (Architecture TAB ICONS = custom SVGs, not lucide). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create KDS-01 and KDS-08 test scaffolds</name>
  <files>tests/kds/token-import.test.ts, tests/kds/theme-persistence.test.ts</files>
  <behavior>
    - token-import Test 1: globals.css contains `@import` line referencing `kata-tokens.css` → RED (file doesn't exist yet)
    - token-import Test 2: globals.css contains `@theme inline` block with `--color-background` aliased to a kata var → RED
    - token-import Test 3: globals.css contains `--color-primary` alias in @theme inline block → RED
    - theme-persistence Test 1: ThemeProvider.tsx exists at components/ThemeProvider.tsx → RED (file doesn't exist)
    - theme-persistence Test 2: ThemeProvider.tsx source contains `kata-theme` localStorage key → RED
    - theme-persistence Test 3: layout.tsx source contains inline script with `kata-theme` flash-prevention → RED
  </behavior>
  <action>
    Create tests/kds/ directory. Use the source-scan pattern: import fs and path, read
    files via fs.readFileSync(path.join(process.cwd(), relPath), 'utf-8'), assert on
    string content with .includes().

    token-import.test.ts: reads app/globals.css. Test 1 checks for `kata-tokens.css`
    in the file content. Test 2 checks for `--color-background: var(--kata-` pattern.
    Test 3 checks for `--color-primary: var(--kata-` pattern.

    theme-persistence.test.ts: Test 1 checks that
    components/ThemeProvider.tsx exists using fs.existsSync (not readFileSync — file
    won't exist yet; use try/catch or existsSync to get a clean RED). Test 2 reads
    ThemeProvider.tsx and checks for `kata-theme` string. Test 3 reads app/layout.tsx
    and checks for `kata-theme` in an inline script tag.

    Use stub pattern: for tests that read files that don't exist yet, wrap
    fs.readFileSync in a try/catch that returns '' — this gives a clean assertion
    failure (empty string doesn't include the expected pattern) rather than a throw.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/kds/token-import.test.ts tests/kds/theme-persistence.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist; token-import has 3 RED tests; theme-persistence has 3 RED tests; no crashes (tests FAIL with assertion errors, not throws)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create KDS-02, KDS-03, KDS-04, KDS-05, KDS-06, KDS-07 test scaffolds</name>
  <files>tests/kds/icon-migration.test.ts, tests/kds/command-rail.test.ts, tests/kds/page-bar.test.ts, tests/kds/portfolio-layout.test.ts, tests/kds/workspace-kpi.test.ts</files>
  <behavior>
    icon-migration.test.ts (KDS-02 + KDS-07):
    - Test 1: components/Icon.tsx exists → RED (doesn't exist)
    - Test 2: components/Icon.tsx contains `material-symbols-outlined` → RED
    - Test 3: none of the 22 lucide files contain `from 'lucide-react'` → RED (all 22 currently import lucide)

    command-rail.test.ts (KDS-03):
    - Test 1: Sidebar.tsx contains `data-theme="dark"` → RED (current Sidebar uses bg-zinc-900)
    - Test 2: Sidebar.tsx contains `href="/daily-prep"` → GREEN (already present — preserve this)
    - Test 3: Sidebar.tsx contains `uppercase tracking-wider` and the /daily-prep href appears before it → GREEN (preserved from Phase 79)
    - Test 4: Sidebar.tsx contains the ⌘K search pill (check for `⌘K` or `Jump to`) → RED

    page-bar.test.ts (KDS-04):
    - Test 1: components/PageBarContext.tsx exists → RED
    - Test 2: components/PageBar.tsx exists → RED
    - Test 3: PageBar.tsx source contains `ctaSlot` → RED
    - Test 4: PageBar.tsx source contains theme toggle (check for `toggleTheme` or `kata-theme`) → RED

    portfolio-layout.test.ts (KDS-05):
    - Test 1: app/page.tsx source imports `PortfolioHeroStats` or contains `hero` section → RED
    - Test 2: app/page.tsx source imports `PortfolioBriefingStrip` → RED
    - Test 3: app/page.tsx source imports `PortfolioProjectGrid` or references a project grid → RED

    workspace-kpi.test.ts (KDS-06):
    - Test 1: components/WorkspaceKpiStrip.tsx exists → RED
    - Test 2: WorkspaceKpiStrip.tsx source contains `gridTemplateColumns` or `repeat(5` → RED
  </behavior>
  <action>
    Create all 5 test files in tests/kds/ using the source-scan + stub pattern.

    For icon-migration Test 3: build an array of the 22 lucide file paths (relative
    to process.cwd()), read each, check if any still contains `from 'lucide-react'`.
    The test asserts that ZERO files contain the import. This will be RED now because
    all 22 currently import lucide. Write it as:
    ```typescript
    const remaining = lucideFiles.filter(f => {
      try { return fs.readFileSync(path.join(process.cwd(), f), 'utf-8').includes("from 'lucide-react'"); }
      catch { return false; }
    });
    expect(remaining).toHaveLength(0); // RED: all 22 files currently import lucide
    ```

    For command-rail Tests 2 and 3: these should ALREADY be GREEN from Phase 79
    (sidebar-daily-prep.test.ts covers them). Mirror those assertions here for
    completeness, confirm they pass.

    For portfolio-layout Test 1: check for `PortfolioHeroStats` OR the string
    `hero-stat` in page.tsx source (either component name or data-testid).

    Use existsSync wrapped try/catch for file existence checks.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/kds/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 5 new test files exist; RED tests are RED (assertion failures, not throws); command-rail Tests 2+3 are GREEN (Phase 79 work preserved); full tests/kds/ suite runs cleanly</done>
</task>

<task type="auto">
  <name>Task 3: Verify full test suite baseline</name>
  <files></files>
  <action>
    Run the complete vitest suite. Confirm:
    1. All pre-existing passing tests still pass
    2. The only failing tests are the newly added RED Wave 0 tests
    3. tests/components/sidebar-daily-prep.test.ts is still GREEN

    If any pre-existing test broke (unrelated to Wave 0 scaffolds), investigate and fix.
    Wave 0 tests themselves are EXPECTED to be RED — do not "fix" them.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>
  <done>Full suite shows: pre-existing passing tests still pass; only the Wave 0 KDS tests are failing; sidebar-daily-prep.test.ts is GREEN</done>
</task>

</tasks>

<verification>
Run: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/kds/ tests/components/sidebar-daily-prep.test.ts --reporter=verbose`

Expected: 7 new test files present in tests/kds/; tests/kds/ tests are RED (most failing); sidebar-daily-prep.test.ts is GREEN; no crashes or parse errors.
</verification>

<success_criteria>
- tests/kds/ directory exists with all 7 test files
- All Wave 0 tests run (no syntax errors or import crashes)
- RED tests fail with assertion errors (not thrown exceptions)
- Existing test suite baseline preserved (sidebar-daily-prep still GREEN)
- No pre-existing passing tests broken by adding scaffolds
</success_criteria>

<output>
After completion, create `.planning/phases/81-kata-design-system-overhaul/81-00-SUMMARY.md`
</output>
