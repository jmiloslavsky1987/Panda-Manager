---
phase: 81-kata-design-system-overhaul
plan: "06"
type: execute
wave: 4
depends_on:
  - "81-04"
  - "81-05"
files_modified: []
autonomous: false
requirements:
  - KDS-07
  - KDS-08

must_haves:
  truths:
    - "All 11 workspace tabs load and display data correctly after the visual rebuild"
    - "Command Rail stays dark gray-950 in both light and dark mode"
    - "JBM numerals render correctly in the portfolio hero and workspace KPI strip"
    - "Material Symbols icons render as glyphs (not text) across all migrated components"
    - "Theme toggle persists to localStorage and reapplies on page refresh"
    - "No regression in form submissions, navigation, or data display"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the complete Kata Design System rebuild.

Phase 81 is a pure visual-layer migration — automated tests verify structural correctness
but cannot verify visual rendering, font loading, icon glyph rendering, or functional
regressions across all 11 workspace tabs. This checkpoint is the final gate before
marking Phase 81 complete.

Purpose: KDS-07 (all tabs functional) and KDS-08 (theme persistence in-browser) require
human visual confirmation that automated source-scan tests cannot provide.
Output: Human approval or list of issues for gap-closure.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-kata-design-system-overhaul/81-04-SUMMARY.md
@.planning/phases/81-kata-design-system-overhaul/81-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite and build verification</name>
  <files></files>
  <action>
    Run the complete test suite and production build. Fix any failures before presenting
    the checkpoint.

    1. Full vitest suite: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run --reporter=verbose`
       - All KDS tests must be GREEN
       - All pre-existing tests must pass
       - Fix any regressions introduced by the visual rebuild

    2. Production build: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run build`
       - Must exit 0 with no TypeScript errors

    3. Start dev server: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run next-only`
       (background process — present URL to user for verification)

    Document the test results in the checkpoint message to the user.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>
  <done>All tests pass; production build clean; dev server running</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of complete KDS rebuild</name>
  <action>Present the verification checklist to the user and wait for their approval or issue report.</action>
  <what-built>
    Complete Kata Design System visual overhaul:
    - Token layer: kata-tokens.css with palette + semantic + [data-theme=dark] isolation
    - Typography: Inter (body) + JetBrains Mono (numerals/dates) via next/font/google
    - Icons: Material Symbols Outlined replacing lucide-react across all 22 files
    - Chrome: 240px dark Command Rail (Sidebar.tsx) + 44px PageBar with theme toggle
    - Portfolio Dashboard: hero stat band (JBM 64px) + briefing strip + 2-col project grid
    - Project Workspace: KPI strip (JBM 28px) + page-bar with health badge
    - Theme persistence: localStorage kata-theme key, flash-prevention script
  </what-built>
  <how-to-verify>
    Start dev server if not already running: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run next-only`

    **1. Portfolio Dashboard (KDS-05)**
    - Navigate to http://localhost:3000
    - Verify: JBM 64px project count numeral in the hero band
    - Verify: 3 briefing strip cards appear (Go-Lives, Risks, Attention)
    - Click each briefing card — it should expand inline showing a list
    - Verify: 2-column project grid with health-accented left borders and progress bars

    **2. Command Rail (KDS-03)**
    - Verify sidebar is 240px dark gray (gray-950) with white text
    - Verify: logo + "Panda Manager" header visible
    - Verify: ⌘K search pill visible
    - Verify: Portfolio / Today / Daily Prep top nav links present
    - Verify: Project list visible with RAG dots

    **3. Theme Toggle (KDS-04, KDS-08)**
    - Click the sun/moon icon in the top page-bar
    - Verify: main canvas inverts (light ↔ dark)
    - Verify: Command Rail stays dark gray-950 in BOTH light and dark mode (it must NOT invert)
    - Refresh the page — verify the theme preference is remembered (no flash)

    **4. Icons (KDS-02)**
    - Verify icons render as glyphs (not raw text like "search" or "settings")
    - Check sidebar icons, dialog close buttons, action buttons throughout

    **5. Workspace — all 11 tabs (KDS-07)**
    - Navigate to any project workspace (click a project in the sidebar)
    - Verify: page-bar shows project name + health badge
    - Verify: 5-column KPI strip appears below page-bar
    - Verify: all tabs load without errors:
      - Overview: data displays, exceptions panel visible
      - Plan: workstreams/tasks render
      - Gantt: chart renders
      - Stakeholders: table renders
      - Risks: table renders
      - Decisions: table renders
      - Artifacts: table renders
      - Skills: skill list renders
      - Time: time entries render
      - Daily Prep: page loads
      - Settings: form renders

    **6. JBM Fonts (KDS-02)**
    - In workspace KPI strip: numerals should appear in JetBrains Mono (monospaced)
    - In project go-live dates in sidebar: should appear in JetBrains Mono
    - Right-click → Inspect any KPI numeral → confirm font-family is "JetBrains Mono"
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found (e.g., "Command Rail turns light in light mode — needs fix")</resume-signal>
</task>

</tasks>

<verification>
Automated: full vitest suite GREEN + production build clean.
Manual: human confirms all 11 workspace tabs functional, Command Rail stays dark in light mode, theme persists, icons render as glyphs, JBM numerals visible in KPI strip.
</verification>

<success_criteria>
- Full vitest suite passes (all KDS tests GREEN, no regressions)
- Production build exits 0
- Human confirms: Command Rail dark in both modes, theme toggle persists, icons render as glyphs, all 11 workspace tabs functional, JBM numerals visible
- User types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/81-kata-design-system-overhaul/81-06-SUMMARY.md`

Also commit and push all Phase 81 changes to the remote:
`cd /Users/jmiloslavsky/Documents/Panda-Manager && git add -A && git commit -m "feat: [Phase 81] Kata Design System visual overhaul — tokens, chrome, portfolio, workspace" && git push`
</output>
