---
phase: 30-context-hub
plan: 06
type: execute
wave: 5
depends_on:
  - 30-05
files_modified: []
autonomous: false
requirements:
  - CTX-01
  - CTX-02
  - CTX-03
  - CTX-04

must_haves:
  truths:
    - "Context tab appears in workspace navigation between Chat and Admin"
    - "Uploading a document opens IngestionModal and extraction preview groups items by entity type"
    - "Approved items appear in the correct workspace tabs after approval"
    - "Upload history shows the uploaded document with processed status"
    - "Analyze Completeness button returns specific gap descriptions referencing real record IDs"
    - "Template placeholder records are not counted as complete"
    - "Full test suite passes GREEN before human review"
  artifacts:
    - path: "bigpanda-app/app/customer/[id]/context/page.tsx"
      provides: "Context tab page accessible in browser"
    - path: "bigpanda-app/components/ContextTab.tsx"
      provides: "Fully wired Context Hub UI"
  key_links:
    - from: "Context tab nav item"
      to: "app/customer/[id]/context/page.tsx"
      via: "?tab=context URL pattern"
      pattern: "tab=context"
---

<objective>
Full-suite test gate followed by human verification of the end-to-end Context Hub flow.

Purpose: Confirm all 4 CTX requirements are verifiably working in the browser before marking Phase 30 complete.
Output: Phase 30 verified and signed off.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/30-context-hub/30-CONTEXT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/30-context-hub/30-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite gate and production build</name>
  <files></files>
  <action>
Run the full test suite and confirm all tests pass GREEN before proceeding to human verification:

```bash
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | tail -30
```

If any tests are RED that were not previously RED (regressions), fix them before proceeding.
If only pre-existing RED tests remain (from earlier phases), that is acceptable — document in the summary.

Also verify the production build compiles cleanly (per VALIDATION.md and Phase 28/29 decisions — build verification required before browser checkpoint):

```bash
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run build 2>&1 | tail -20
```

Fix any build errors before proceeding to checkpoint.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm test -- --run 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>
  <done>Full test suite passes with no new regressions. Production build compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — end-to-end Context Hub flows</name>
  <files></files>
  <action>Human verifies end-to-end Context Hub flows in the browser per the instructions below.</action>
  <verify>
    <automated>MANUAL — see how-to-verify section</automated>
  </verify>
  <done>User types "approved" confirming all 3 verification flows work as expected.</done>
  <what-built>
Context Hub — Phase 30 complete implementation:
- Context tab in workspace navigation (between Chat and Admin)
- Upload section with IngestionModal integration for PDF/DOCX/PPTX
- Three new entity types routed by extraction (workstream, onboarding_step, integration)
- Upload history list showing previous ingestion documents
- Completeness analysis panel with per-tab status badges and specific gap descriptions
  </what-built>
  <how-to-verify>
Run the dev server: `cd bigpanda-app && npm run next-only`

**Flow 1 — Context tab navigation (CTX-01):**
1. Open any project workspace in the browser
2. Confirm "Context" tab appears in the nav bar between Chat and Admin
3. Click the Context tab — confirm ?tab=context in URL
4. Confirm the page shows: Upload section, Upload History section, Workspace Completeness section

**Flow 2 — Document upload and extraction (CTX-02):**
1. From the Context tab, click "Upload Document"
2. Upload any PDF or DOCX test file (meeting notes format works well)
3. Confirm IngestionModal opens and extraction runs
4. Confirm extraction preview groups items by entity type (including the 3 new types if present in the document)
5. Approve some items — confirm they appear in the correct workspace tabs afterward
6. Return to Context tab — confirm the uploaded document appears in Upload History with "processed" status

**Flow 3 — Completeness analysis (CTX-03 + CTX-04):**
1. From the Context tab, click "Analyze Completeness"
2. Confirm loading state appears while analysis runs (typically 5–15 seconds)
3. Confirm 11 tab rows appear in the panel, all collapsed by default
4. Confirm each row shows a status badge (complete / partial / empty)
5. Click any "partial" row — confirm specific gap descriptions appear with record IDs (e.g. "[A-KAISER-003] missing owner") — NOT generic text
6. For a new project (template placeholders only), confirm tabs show "empty" not "complete"

**Verify deferred items are NOT present:**
- No "Analyze after every approval" behavior
- No re-extract/retry button on history rows
- No scheduled completeness jobs
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 flows work as expected, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Manual in-browser verification per the checkpoint instructions above.
All 4 CTX requirements verified end-to-end.
</verification>

<success_criteria>
- Full test suite GREEN with no new regressions
- Production build compiles cleanly
- All 3 browser verification flows confirmed by user
- CTX-01 through CTX-04 marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After human approval, update:
1. /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/.planning/REQUIREMENTS.md — mark CTX-01, CTX-02, CTX-03, CTX-04 as [x]
2. /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/.planning/STATE.md — Phase 30 complete
3. Create /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/.planning/phases/30-context-hub/30-06-SUMMARY.md
</output>
