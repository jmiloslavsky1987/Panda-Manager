---
phase: 73-multi-tenant-isolation
plan: "05"
type: execute
wave: 2
depends_on:
  - "73-02"
  - "73-03"
  - "73-04"
files_modified: []
autonomous: false
requirements:
  - TENANT-01
  - TENANT-02
  - TENANT-03
  - TENANT-04
  - TENANT-05

must_haves:
  truths:
    - "All 5 TENANT requirements are verified green by automated tests"
    - "Full test suite (148+ tests) passes with no regressions"
    - "Production build is clean (no TypeScript errors)"
    - "User has manually verified cross-user isolation using two browser sessions"
  artifacts:
    - path: "bigpanda-app/tests/auth/portfolio-isolation.test.ts"
      provides: "TENANT-01 automated verification"
    - path: "bigpanda-app/tests/auth/query-param-403.test.ts"
      provides: "TENANT-02 automated verification"
    - path: "bigpanda-app/tests/auth/cache-isolation.test.ts"
      provides: "TENANT-03 automated verification"
    - path: "bigpanda-app/tests/auth/job-isolation.test.ts"
      provides: "TENANT-04 automated verification"
    - path: "bigpanda-app/tests/auth/invite-empty-state.test.ts"
      provides: "TENANT-05 automated verification"
  key_links:
    - from: "tests/auth/ (all 5 files)"
      to: "Phase 73 requirements (TENANT-01 through TENANT-05)"
      via: "Automated test coverage"
      pattern: "TENANT-0[1-5]"
---

<objective>
Verify the complete multi-tenant isolation implementation is correct and production-ready. Run the full automated test suite, perform a production build, and gate on human verification of cross-user isolation using two real browser sessions.

Purpose: Final quality gate before Phase 74 (Deployment Readiness). Multi-tenant correctness must be verified before deployment.
Output: All 5 TENANT requirements verified. Phase 73 marked complete.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-multi-tenant-isolation/73-02-SUMMARY.md
@.planning/phases/73-multi-tenant-isolation/73-03-SUMMARY.md
@.planning/phases/73-multi-tenant-isolation/73-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + production build verification</name>
  <files></files>
  <action>
    Run the complete verification sequence:

    1. Run all auth isolation tests:
    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run test tests/auth/ 2>&1 | tail -30
    ```
    All 5 new test files must be GREEN. If any are RED, identify which PLAN (02/03/04) needs a fix and surface the failure.

    2. Run the full test suite:
    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run test 2>&1 | tail -40
    ```
    Must show 148+ tests passing with 0 failures.

    3. Run production build:
    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run build 2>&1 | tail -20
    ```
    Must complete without TypeScript errors. "Compiled successfully" or equivalent.

    If any step fails, investigate and fix before proceeding to the human verification checkpoint. Failures here indicate an incomplete implementation in Plans 02-04.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run test 2>&1 | grep -E "(Tests|passed|failed|TENANT)" | tail -20</automated>
  </verify>
  <done>Full test suite passes with 0 failures. All 5 TENANT requirement tests are GREEN. Production build is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — cross-user isolation in browser</name>
  <what-built>
    Multi-tenant isolation across all API and UI layers:
    - Portfolio page (GET /api/projects) — filtered to user's own projects
    - Dashboard watch list (GET /api/dashboard/watch-list) — filtered to user's projects
    - Drafts inbox (GET /api/drafts) — filtered to user's projects
    - Artifacts API (GET+POST /api/artifacts?projectId) — 403 for non-members
    - Tasks API (GET+POST /api/tasks?projectId) — 403 for non-members
    - Ingestion upload (POST /api/ingestion/upload) — 403 for non-members
    - Ingestion extract (POST /api/ingestion/extract) — 403 for non-members
    - Ingestion approve (POST /api/ingestion/approve) — 403 for non-members
    - Artifact PATCH (PATCH /api/artifacts/[id]) — 403 for non-members
    - New user invite onboarding — clean empty portfolio
  </what-built>
  <how-to-verify>
    Start the dev server: `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run next-only`

    **Test 1 — Portfolio isolation (TENANT-01):**
    1. Log in as User A (who has existing projects)
    2. Confirm the portfolio page shows ONLY User A's projects
    3. Open an incognito window, log in as User B (a different user with different projects)
    4. Confirm User B sees ONLY their own projects — NOT User A's

    **Test 2 — Direct URL 403 (TENANT-02):**
    1. While logged in as User B, copy a project URL from User A's portfolio (e.g., /projects/1/workspace)
    2. Paste it into User B's browser
    3. Confirm: the workspace loads (path-param routes already had requireProjectRole from Phase 58)
    4. In User B's browser, open DevTools → Network tab
    5. Navigate to User A's project workspace — confirm that any API call for data User B shouldn't see returns 403

    **Test 3 — Invite onboarding empty state (TENANT-05):**
    1. Create a fresh test user account (via invite or signup)
    2. Log in as that fresh user
    3. Confirm: portfolio page is empty (no other users' projects visible)
    4. Confirm: dashboard watch list is empty (no other users' risks)

    **Expected results:**
    - User A and User B each see only their own projects in portfolio
    - Fresh user sees empty portfolio on first login
    - API calls for non-member projects return 403 in Network tab
  </how-to-verify>
  <resume-signal>Type "approved" if all isolation checks pass, or describe any failures observed</resume-signal>
</task>

</tasks>

<verification>
Phase complete when:
1. All 5 tests/auth/ test files are GREEN (automated)
2. Full test suite passes with 0 failures (automated)
3. Production build is clean (automated)
4. Human has approved cross-user isolation in browser (manual checkpoint)
</verification>

<success_criteria>
- TENANT-01: Portfolio shows only user's own projects
- TENANT-02: Non-member API access returns 403 (confirmed in Network tab)
- TENANT-03: Redis cache read requires membership (test green)
- TENANT-04: BullMQ job results scoped to project (test green)
- TENANT-05: New user sees empty portfolio on first login
- Full test suite: 148+ tests green, 0 failures
- Production build: clean TypeScript compilation
- Human verification: approved
</success_criteria>

<output>
After completion, create `.planning/phases/73-multi-tenant-isolation/73-05-SUMMARY.md`
</output>
