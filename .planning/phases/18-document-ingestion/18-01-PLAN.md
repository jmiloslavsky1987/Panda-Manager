---
phase: 18-document-ingestion
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/tests/ingestion/upload.test.ts
  - bigpanda-app/tests/ingestion/validation.test.ts
  - bigpanda-app/tests/ingestion/extractor.test.ts
  - bigpanda-app/tests/ingestion/preview.test.ts
  - bigpanda-app/tests/ingestion/dedup.test.ts
  - bigpanda-app/tests/ingestion/write.test.ts
  - bigpanda-app/db/migrations/0012_ingestion_source_attribution.sql
autonomous: true
requirements:
  - ING-01
  - ING-02
  - ING-03
  - ING-04
  - ING-05
  - ING-06
  - ING-07
  - ING-08
  - ING-09
  - ING-10
  - ING-11
  - ING-12

must_haves:
  truths:
    - "Test stubs exist for all 12 ING requirements so subsequent plans can go RED then GREEN"
    - "source_artifact_id and ingested_at columns exist on all entity tables before any write logic is built"
  artifacts:
    - path: "bigpanda-app/tests/ingestion/upload.test.ts"
      provides: "Upload + artifact record tests (ING-01, ING-03)"
    - path: "bigpanda-app/tests/ingestion/validation.test.ts"
      provides: "File type/size rejection tests (ING-02)"
    - path: "bigpanda-app/tests/ingestion/extractor.test.ts"
      provides: "Claude extraction output shape tests (ING-04)"
    - path: "bigpanda-app/tests/ingestion/preview.test.ts"
      provides: "Preview UI state tests (ING-05, ING-06, ING-07)"
    - path: "bigpanda-app/tests/ingestion/dedup.test.ts"
      provides: "Conflict detection and dedup tests (ING-08, ING-11, ING-12)"
    - path: "bigpanda-app/tests/ingestion/write.test.ts"
      provides: "Source attribution write tests (ING-09, ING-10)"
    - path: "bigpanda-app/db/migrations/0012_ingestion_source_attribution.sql"
      provides: "source_artifact_id + ingested_at on all entity tables"
  key_links:
    - from: "test files"
      to: "production modules"
      via: "import path stubs — tests import the to-be-created modules and immediately fail (RED)"
      pattern: "expect\\(false, 'stub'\\)\\.toBe\\(true\\)"
---

<objective>
Create Wave 0 Nyquist test scaffolds for all 12 ING requirements and run the DB migration that adds source_artifact_id + ingested_at to every entity table.

Purpose: Establishes the TDD RED baseline so each subsequent plan can go GREEN. Also unblocks ING-09 writes by adding the missing columns before any write logic is built.
Output: 6 test files (all RED), 1 migration SQL file.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-document-ingestion/18-CONTEXT.md
@.planning/phases/18-document-ingestion/18-RESEARCH.md
@.planning/phases/18-document-ingestion/18-VALIDATION.md

<interfaces>
<!-- Key schema exports the test files will import against -->
<!-- bigpanda-app/db/schema.ts (relevant exports): -->

```typescript
export const ingestionStatusEnum = pgEnum('ingestion_status', ['pending','extracting','preview','approved','failed']);
export const artifacts = pgTable('artifacts', {
  id: serial('id').primaryKey(),
  project_id: integer('project_id').references(() => projects.id, { onDelete: 'cascade' }),
  external_id: text('external_id'),
  name: text('name').notNull(),
  source: text('source').notNull(),        // 'upload' for ingested files
  ingestion_status: ingestionStatusEnum('ingestion_status'),
  ingestion_log_json: jsonb('ingestion_log_json'),
  created_at: timestamp('created_at').defaultNow(),
});

// Entity tables with existing 'source' column (target for source_artifact_id + ingested_at migration):
// actions, risks, milestones, key_decisions, engagement_history, stakeholders, tasks,
// business_outcomes, focus_areas, architecture_integrations, e2e_workflows
```

<!-- After migration 0012 runs, these columns will exist on each entity table: -->
```sql
source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL
ingested_at        TIMESTAMP
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing test stubs for all 6 test files</name>
  <files>
    bigpanda-app/tests/ingestion/upload.test.ts,
    bigpanda-app/tests/ingestion/validation.test.ts,
    bigpanda-app/tests/ingestion/extractor.test.ts,
    bigpanda-app/tests/ingestion/preview.test.ts,
    bigpanda-app/tests/ingestion/dedup.test.ts,
    bigpanda-app/tests/ingestion/write.test.ts
  </files>
  <action>
Create `bigpanda-app/tests/ingestion/` directory and write 6 test files. Each file MUST have `expect(false, 'stub').toBe(true)` as the first assertion in every test so all tests fail RED immediately without needing implementation.

**upload.test.ts** — covers ING-01, ING-03:
```typescript
import { describe, it, expect } from 'vitest';
describe('Upload route (ING-01, ING-03)', () => {
  it('ING-01: accepts supported file types', () => { expect(false, 'stub').toBe(true); });
  it('ING-03: creates Artifact record with ingestion_status pending', () => { expect(false, 'stub').toBe(true); });
  it('ING-03: stores file on disk at workspace path', () => { expect(false, 'stub').toBe(true); });
});
```

**validation.test.ts** — covers ING-02:
```typescript
import { describe, it, expect } from 'vitest';
describe('Upload validation (ING-02)', () => {
  it('ING-02: rejects files over 50 MB', () => { expect(false, 'stub').toBe(true); });
  it('ING-02: rejects unsupported file types', () => { expect(false, 'stub').toBe(true); });
  it('ING-02: returns clear error message on rejection', () => { expect(false, 'stub').toBe(true); });
});
```

**extractor.test.ts** — covers ING-04:
```typescript
import { describe, it, expect } from 'vitest';
describe('Document extractor (ING-04)', () => {
  it('ING-04: extracted items have entityType field', () => { expect(false, 'stub').toBe(true); });
  it('ING-04: extracted items have confidence 0.0-1.0', () => { expect(false, 'stub').toBe(true); });
  it('ING-04: extracted items have sourceExcerpt string', () => { expect(false, 'stub').toBe(true); });
  it('ING-04: extracted items have fields object', () => { expect(false, 'stub').toBe(true); });
  it('ING-04: supports all entity types: action/risk/decision/milestone/stakeholder/task/architecture/history/businessOutcome/team', () => { expect(false, 'stub').toBe(true); });
});
```

**preview.test.ts** — covers ING-05, ING-06, ING-07:
```typescript
import { describe, it, expect } from 'vitest';
describe('Extraction preview (ING-05, ING-06, ING-07)', () => {
  it('ING-05: groups items by entityType into tabs', () => { expect(false, 'stub').toBe(true); });
  it('ING-05: only shows tabs that have items', () => { expect(false, 'stub').toBe(true); });
  it('ING-05: tab badge shows item count', () => { expect(false, 'stub').toBe(true); });
  it('ING-06: item default state is approved (checked)', () => { expect(false, 'stub').toBe(true); });
  it('ING-06: unchecking an item sets it to rejected', () => { expect(false, 'stub').toBe(true); });
  it('ING-06: editing an item preserves checkbox state', () => { expect(false, 'stub').toBe(true); });
  it('ING-07: bulk approve sets all items to approved', () => { expect(false, 'stub').toBe(true); });
});
```

**dedup.test.ts** — covers ING-08, ING-11, ING-12:
```typescript
import { describe, it, expect } from 'vitest';
describe('Dedup and conflict detection (ING-08, ING-11, ING-12)', () => {
  it('ING-08: detects conflict when item matches existing record', () => { expect(false, 'stub').toBe(true); });
  it('ING-08: non-conflicting items pass through without prompt', () => { expect(false, 'stub').toBe(true); });
  it('ING-11: re-upload of same file triggers preview flow', () => { expect(false, 'stub').toBe(true); });
  it('ING-12: already-ingested items are filtered from preview', () => { expect(false, 'stub').toBe(true); });
  it('ING-12: net-new items from incremental upload are surfaced', () => { expect(false, 'stub').toBe(true); });
});
```

**write.test.ts** — covers ING-09, ING-10:
```typescript
import { describe, it, expect } from 'vitest';
describe('Ingestion write and logging (ING-09, ING-10)', () => {
  it('ING-09: written items have source set to ingestion', () => { expect(false, 'stub').toBe(true); });
  it('ING-09: written items have source_artifact_id set', () => { expect(false, 'stub').toBe(true); });
  it('ING-09: written items have ingested_at timestamp set', () => { expect(false, 'stub').toBe(true); });
  it('ING-10: artifact ingestion_log_json updated after write', () => { expect(false, 'stub').toBe(true); });
  it('ING-10: log contains filename, upload_time, items_extracted, items_approved, items_rejected', () => { expect(false, 'stub').toBe(true); });
});
```

Run `npx vitest run --reporter=verbose bigpanda-app/tests/ingestion/` and confirm all tests FAIL (RED). If any pass, the stub is wrong — fix it.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx vitest run --reporter=verbose bigpanda-app/tests/ingestion/ 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 test files exist, all tests report FAILED — 0 passing, N failing. No test passes without implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0012 — source_artifact_id + ingested_at on entity tables</name>
  <files>bigpanda-app/db/migrations/0012_ingestion_source_attribution.sql</files>
  <action>
Create `bigpanda-app/db/migrations/0012_ingestion_source_attribution.sql`. Add `source_artifact_id` (nullable FK → artifacts.id, SET NULL on delete) and `ingested_at` (nullable timestamp) to all entity tables that will receive ingested data.

Target tables: `actions`, `risks`, `milestones`, `key_decisions`, `engagement_history`, `stakeholders`, `tasks`, `business_outcomes`, `focus_areas`, `architecture_integrations`, `e2e_workflows`.

```sql
-- Migration 0012: Add ingestion source attribution columns to all entity tables
-- Required by ING-09: source_artifact_id links each ingested record back to its source artifact
-- ingested_at records when the item was written from the ingestion pipeline

ALTER TABLE actions                  ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE actions                  ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
ALTER TABLE risks                    ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE risks                    ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
ALTER TABLE milestones               ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE milestones               ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
ALTER TABLE key_decisions            ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE key_decisions            ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
ALTER TABLE engagement_history       ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE engagement_history       ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
ALTER TABLE stakeholders             ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE stakeholders             ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
ALTER TABLE tasks                    ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE tasks                    ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
ALTER TABLE business_outcomes        ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE business_outcomes        ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
ALTER TABLE focus_areas              ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE focus_areas              ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
ALTER TABLE architecture_integrations ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE architecture_integrations ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
ALTER TABLE e2e_workflows            ADD COLUMN IF NOT EXISTS source_artifact_id INTEGER REFERENCES artifacts(id) ON DELETE SET NULL;
ALTER TABLE e2e_workflows            ADD COLUMN IF NOT EXISTS ingested_at TIMESTAMP;
```

Also update `bigpanda-app/db/schema.ts` to add these two optional columns to each of the 11 entity table Drizzle definitions:
- `source_artifact_id: integer('source_artifact_id').references(() => artifacts.id, { onDelete: 'set null' })`
- `ingested_at: timestamp('ingested_at')`

Note: `key_decisions` and `engagement_history` are append-only (no UPDATE/DELETE triggers) — adding columns does not violate append-only enforcement.
  </action>
  <verify>
    <automated>grep -c "source_artifact_id" "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app/db/migrations/0012_ingestion_source_attribution.sql" && grep -c "source_artifact_id" "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app/db/schema.ts"</automated>
  </verify>
  <done>Migration file exists with ADD COLUMN statements for all 11 entity tables (22 ALTER TABLE statements total). Drizzle schema.ts updated with source_artifact_id + ingested_at on each entity table. TypeScript compiler accepts the schema: `cd bigpanda-app && npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose bigpanda-app/tests/ingestion/` — all stubs RED (0 passing)
- Migration SQL file has 22 ALTER TABLE statements (2 per table × 11 tables)
- `npx tsc --noEmit` in bigpanda-app exits 0 (schema.ts changes accepted by TypeScript)
</verification>

<success_criteria>
6 RED test files committed. Migration 0012 SQL file committed and schema.ts updated. The foundation is in place for all subsequent Wave 1–4 plans to implement against.
</success_criteria>

<output>
After completion, create `.planning/phases/18-document-ingestion/18-01-SUMMARY.md`
</output>
