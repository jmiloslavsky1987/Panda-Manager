---
phase: 64-editable-prompts-ui
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - bigpanda-app/__tests__/skills/prompt-settings.test.ts
  - bigpanda-app/__tests__/skills/front-matter-strip.test.ts
  - bigpanda-app/tests/skills/prompt-edit-api.test.ts
autonomous: true
requirements:
  - SKILL-03a
  - SKILL-03b
must_haves:
  truths:
    - "RED stubs exist for all 6 SKILL-03a/03b behaviors"
    - "Running the test suite shows failures (not errors) for all new stubs"
    - "front-matter-strip.test.ts extended with body extraction cases"
  artifacts:
    - path: "bigpanda-app/__tests__/skills/prompt-settings.test.ts"
      provides: "SKILL-03a: settings round-trip + admin guard stubs"
      exports: []
    - path: "bigpanda-app/tests/skills/prompt-edit-api.test.ts"
      provides: "SKILL-03b: GET/PATCH route behavior stubs (4 stubs)"
      exports: []
    - path: "bigpanda-app/__tests__/skills/front-matter-strip.test.ts"
      provides: "Extended with body extraction regex cases"
      exports: []
  key_links:
    - from: "prompt-settings.test.ts"
      to: "lib/settings-core.ts"
      via: "round-trip test calls readSettings/writeSettings"
      pattern: "readSettings.*writeSettings"
    - from: "prompt-edit-api.test.ts"
      to: "app/api/skills/[skillName]/prompt/route.ts"
      via: "route handler import stubs"
      pattern: "skills.*prompt.*route"
---

<objective>
Write RED TDD stubs that define the contracts for SKILL-03a (global toggle) and SKILL-03b (prompt read/write API) before any implementation exists.

Purpose: Establish verifiable acceptance criteria that implementation must drive to GREEN. Every save to the prompt API and every settings toggle must satisfy these contracts.
Output: Three test files — two new, one extended — all failing (RED) with meaningful failure messages.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@bigpanda-app/__tests__/skills/front-matter-strip.test.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From bigpanda-app/types/skills.ts:
```typescript
export interface SkillMeta {
  name: string;
  label: string;
  description: string;
  inputRequired: boolean;
  inputLabel: string;
  schedulable: boolean;
  errorBehavior: 'retry' | 'fail';
  compliant: boolean;
}
```

From bigpanda-app/lib/settings-core.ts:
```typescript
export interface AppSettings {
  workspace_path: string;
  skill_path: string;
  schedule: { morning_briefing: string; health_check: string; slack_sweep: string; tracker_weekly: string; weekly_status: string; biggy_briefing: string; };
  mcp_servers: MCPServerConfig[];
  source_credentials?: SourceCredentials;
  // Phase 64 will add: prompt_editing_enabled?: boolean
}

export async function readSettings(settingsPath?: string): Promise<AppSettings>;
export async function writeSettings(partial: Partial<AppSettings>, settingsPath?: string): Promise<void>;
```

Test framework: Vitest 4.x, config at bigpanda-app/vitest.config.ts
Quick test run: `cd bigpanda-app && npx vitest run __tests__/skills/ tests/skills/`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED stubs — prompt-settings.test.ts (SKILL-03a)</name>
  <files>bigpanda-app/__tests__/skills/prompt-settings.test.ts</files>
  <behavior>
    - Test 1: `prompt_editing_enabled` persists through writeSettings/readSettings round-trip — set to `true`, re-read, expect `true`
    - Test 2: `prompt_editing_enabled` defaults to `false` (undefined coerced) when not in settings file
    - Test 3: Settings POST endpoint rejects `prompt_editing_enabled` update when session role is not 'admin' — expect 403
    - Test 4: Settings POST accepts `prompt_editing_enabled: true` when session role is 'admin' — expect 200/ok
  </behavior>
  <action>
    Create `bigpanda-app/__tests__/skills/prompt-settings.test.ts` with four `it.todo` or stubbed `expect` calls that will FAIL RED before implementation.

    For Tests 1+2 (settings round-trip): import from `../../lib/settings-core`, use a tmp file path via `os.tmpdir()` to avoid polluting real settings. Call `writeSettings({ prompt_editing_enabled: true }, tmpPath)` then `readSettings(tmpPath)` and assert. These tests will fail because `AppSettings` does not yet have the field.

    For Tests 3+4 (admin guard): Import the settings POST route handler or use `fetch` to a mocked URL. Use Vitest mock for `requireSession` to return a non-admin session. Expect 403. These can be implemented as `it.todo('Settings POST rejects prompt_editing_enabled from non-admin')` stubs to ensure RED without import errors.

    Do NOT implement the actual feature — stubs only. Goal: `npx vitest run __tests__/skills/prompt-settings.test.ts` shows failures or todos, not passes.
  </action>
  <verify>
    <automated>cd bigpanda-app && npx vitest run __tests__/skills/prompt-settings.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, vitest reports the tests as failing or pending (not all passing)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED stubs — prompt-edit-api.test.ts (SKILL-03b) + extend front-matter-strip.test.ts</name>
  <files>bigpanda-app/tests/skills/prompt-edit-api.test.ts, bigpanda-app/__tests__/skills/front-matter-strip.test.ts</files>
  <behavior>
    prompt-edit-api.test.ts stubs (4 behaviors):
    - Test 1: GET `/api/skills/[skillName]/prompt` returns `{ frontMatter: string, body: string }` split at second `---` delimiter
    - Test 2: PATCH `/api/skills/[skillName]/prompt` with valid body writes atomically and returns `{ ok: true }`
    - Test 3: PATCH rejects with 422/400 when Design Standard validation fails on the reassembled file
    - Test 4: PATCH rejects with 403 when session is not admin

    front-matter-strip.test.ts additions (2 new cases):
    - Body extraction from file with trailing newline returns body without leading newline
    - Body extraction from file without trailing newline after second `---` returns empty string
  </behavior>
  <action>
    Create `bigpanda-app/tests/skills/prompt-edit-api.test.ts` (note: `tests/` not `__tests__/` — matches path from VALIDATION.md).

    Use `it.todo` stubs for all 4 route behaviors — these will show as pending/failing RED.

    For the front-matter tests: read the existing `__tests__/skills/front-matter-strip.test.ts` first. Add the two new body-extraction test cases using the regex pattern from RESEARCH.md: `const bodyMatch = content.match(/^---\n[\s\S]*?\n---\n?([\s\S]*)$/)`. These should be real `expect` assertions that will FAIL before implementation (because the helper function doesn't exist yet), or use `it.todo`.

    Ensure `tests/skills/` directory exists (create it with the test file).
  </action>
  <verify>
    <automated>cd bigpanda-app && npx vitest run __tests__/skills/ tests/skills/ 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist; vitest run shows test count increased with failures/todos; no import errors crash the suite</done>
</task>

</tasks>

<verification>
Run: `cd bigpanda-app && npx vitest run __tests__/skills/ tests/skills/`
Expected: All new stubs are RED (failing) or pending (todos). No import-level crashes. Existing `front-matter-strip.test.ts` passing tests remain GREEN.
</verification>

<success_criteria>
- `__tests__/skills/prompt-settings.test.ts` exists with 4 stubs (SKILL-03a behaviors)
- `tests/skills/prompt-edit-api.test.ts` exists with 4 stubs (SKILL-03b behaviors)
- `__tests__/skills/front-matter-strip.test.ts` extended with 2 body-extraction cases
- All new stubs are RED or pending — none accidentally pass
- Existing passing tests remain GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/64-editable-prompts-ui/64-01-SUMMARY.md`
</output>
