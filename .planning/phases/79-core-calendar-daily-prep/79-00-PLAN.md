---
phase: 79-core-calendar-daily-prep
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/components/calendar-import-modal.test.ts
  - tests/api/calendar-import-global.test.ts
  - tests/components/daily-prep-card.test.ts
  - tests/components/daily-prep-page.test.ts
  - tests/components/sidebar-daily-prep.test.ts
  - lib/__tests__/meeting-prep-context.test.ts
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - PREP-02
  - PREP-03
  - PREP-04
  - PREP-05
  - PREP-06
  - PREP-07
  - SKILL-01
  - SKILL-02
  - NAV-01

must_haves:
  truths:
    - "All 6 test files exist with failing stubs that describe expected behavior"
    - "Running npx vitest run --reporter=verbose tests/ produces RED results (not missing-file errors)"
    - "Existing test suite still passes (no regressions from adding stub files)"
  artifacts:
    - path: "tests/components/calendar-import-modal.test.ts"
      provides: "Failing stubs for CAL-01 (global mode, no projectId)"
    - path: "tests/api/calendar-import-global.test.ts"
      provides: "Failing stubs for CAL-02 (extended interface) and CAL-03 (title+attendee matching)"
    - path: "tests/components/daily-prep-card.test.ts"
      provides: "Failing stubs for PREP-02 (card renders fields) and PREP-03 (unmatched dropdown)"
    - path: "tests/components/daily-prep-page.test.ts"
      provides: "Failing stubs for PREP-04 through PREP-07 (multi-select, inline output, copy, date picker)"
    - path: "tests/components/sidebar-daily-prep.test.ts"
      provides: "Failing stub for NAV-01 (Daily Prep link below Dashboard)"
    - path: "lib/__tests__/meeting-prep-context.test.ts"
      provides: "Extended with failing stubs for SKILL-01 (calendarMeta param) and SKILL-02 (structured sections)"
  key_links:
    - from: "tests/api/calendar-import-global.test.ts"
      to: "app/api/time-entries/calendar-import/route.ts"
      via: "imports CalendarEventItem type and matching algorithm"
      pattern: "CalendarEventItem"
    - from: "lib/__tests__/meeting-prep-context.test.ts"
      to: "lib/meeting-prep-context.ts"
      via: "imports buildMeetingPrepContext"
      pattern: "buildMeetingPrepContext"
---

<objective>
Create the Wave 0 test scaffolds for Phase 79. All 6 test files must exist with failing (RED) stubs before any implementation begins. This ensures every implementation task has automated verification from the first commit.

Purpose: Nyquist compliance — no implementation task without a pre-existing automated test target.
Output: 6 test files with failing stubs; existing suite still green.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-core-calendar-daily-prep/79-CONTEXT.md
@.planning/phases/79-core-calendar-daily-prep/79-VALIDATION.md

<interfaces>
<!-- Existing test file to extend (not replace): -->
<!-- /Users/jmiloslavsky/Documents/Panda-Manager/lib/__tests__/meeting-prep-context.test.ts -->
<!-- Contains: existing tests for buildMeetingPrepContext(projectId, input?) -->
<!-- Extend by appending new describe block for CalendarMetadata parameter -->

<!-- Test framework: Vitest (node environment) -->
<!-- Config: /Users/jmiloslavsky/Documents/Panda-Manager/vitest.config.ts -->
<!-- Run: npx vitest run --reporter=verbose tests/ -->
<!-- All tests/ files use: import { describe, it, expect, vi } from 'vitest' -->

<!-- Existing CalendarEventItem interface (current, before extension): -->
<!-- app/api/time-entries/calendar-import/route.ts:60 -->
export interface CalendarEventItem {
  event_id: string;
  summary: string;
  date: string;
  start_time: string;
  end_time: string;
  duration_hours: string;
  matched_project_id: number | null;
  matched_project_name: string | null;
  match_confidence: 'high' | 'low' | 'none';
}

<!-- buildMeetingPrepContext current signature: -->
export async function buildMeetingPrepContext(projectId: number, input?: string): Promise<string>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calendar import and modal test stubs (CAL-01, CAL-02, CAL-03)</name>
  <files>
    tests/components/calendar-import-modal.test.ts
    tests/api/calendar-import-global.test.ts
  </files>
  <behavior>
    tests/components/calendar-import-modal.test.ts:
    - Test 1 (CAL-01): When CalendarImportModal is rendered with no projectId prop, it does not throw and renders successfully
    - Test 2 (CAL-01): Fetch call uses /api/time-entries/calendar-import (global) URL, not a project-scoped URL, when projectId is undefined

    tests/api/calendar-import-global.test.ts:
    - Test 1 (CAL-02): CalendarEventItem response includes attendee_names (string[]), recurrence_flag (boolean), event_description (string | null)
    - Test 2 (CAL-02): recurrence_flag is true when recurringEventId is set on the Google event
    - Test 3 (CAL-03): Title match scores 2 points; attendee match scores 1 point per email overlap
    - Test 4 (CAL-03): score >= 2 → confidence 'high'; score === 1 → confidence 'low'; score === 0 → confidence 'none'
    - Test 5 (CAL-03): Project names shorter than 4 chars are NOT used for title matching (guard against false positives)

    All tests must be FAILING stubs (the implementations don't exist yet).
  </behavior>
  <action>
    Create tests/components/calendar-import-modal.test.ts as a Vitest unit test. Since CalendarImportModal is a React component that uses fetch, mock fetch globally with vi.fn(). The test for CAL-01 (no projectId) should verify the component accepts an optional projectId prop — write a stub test with `expect(true).toBe(false)` body if the real assertion would require the full implementation to exist. The goal is RED tests with descriptive names, not passing tests.

    Create tests/api/calendar-import-global.test.ts as a pure logic test. Do NOT import the Next.js route handler directly (it will fail due to DB dependencies). Instead, test the pure scoring/confidence logic in isolation by extracting the algorithm as a helper function OR writing tests that document the expected behavior with `expect(false).toBe(true)` stubs labeled with the specific assertion.

    Preferred pattern: write actual failing assertions using stub functions that return wrong values:
    ```typescript
    function computeConfidence(score: number): 'high' | 'low' | 'none' {
      // stub — returns wrong value intentionally
      return 'none';
    }
    it('score >= 2 → high confidence', () => {
      expect(computeConfidence(2)).toBe('high'); // FAILS until real implementation
    });
    ```
    This gives RED with a clear assertion failure, not a thrown error.

    Code root: /Users/jmiloslavsky/Documents/Panda-Manager
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run --reporter=verbose tests/components/calendar-import-modal.test.ts tests/api/calendar-import-global.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist. Tests run (no import errors). Tests FAIL with assertion failures (not with "cannot find module" errors).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Daily prep page and sidebar test stubs (NAV-01, PREP-02 through PREP-07)</name>
  <files>
    tests/components/daily-prep-card.test.ts
    tests/components/daily-prep-page.test.ts
    tests/components/sidebar-daily-prep.test.ts
  </files>
  <behavior>
    tests/components/sidebar-daily-prep.test.ts:
    - Test 1 (NAV-01): Sidebar renders a link to /daily-prep
    - Test 2 (NAV-01): The /daily-prep link appears in the nav before the Projects section

    tests/components/daily-prep-card.test.ts:
    - Test 1 (PREP-02): EventCard renders event time, title, duration, matched project name, attendees
    - Test 2 (PREP-03): EventCard with matched_project_id null shows a project selector dropdown
    - Test 3 (PREP-03): EventCard with matched_project_id set does NOT show the project selector dropdown

    tests/components/daily-prep-page.test.ts:
    - Test 1 (PREP-04): Selecting 2 event cards and clicking Generate Prep fires 2 fetch calls to /api/daily-prep/generate
    - Test 2 (PREP-05): After generation, brief content renders inside .prose element per card
    - Test 3 (PREP-06): Copy button calls navigator.clipboard.writeText with the brief content
    - Test 4 (PREP-07): date input defaults to today in ISO format (YYYY-MM-DD)
    - Test 5 (PREP-07): Changing the date input triggers a new fetch to the calendar-import endpoint

    All tests must be FAILING stubs.
  </behavior>
  <action>
    Create three test files in tests/components/. Follow the same stub pattern as Task 1 — use stub functions or vi.fn() mocks that return wrong values so tests FAIL with assertion errors, not import errors.

    For sidebar-daily-prep.test.ts: The Sidebar component is a Server Component (async function). Do not try to render it with React Testing Library — instead, write tests that import a hypothetical sidebar HTML string and assert on it, using stubs. OR write simple tests that document expected behavior with a stub like `const hasLink = false; expect(hasLink).toBe(true)`. The test names must clearly identify what they verify.

    For daily-prep-card.test.ts and daily-prep-page.test.ts: These components don't exist yet. Write tests that import from their future paths (`@/components/DailyPrepCard` etc.) wrapped in try/catch or using vi.mock() to prevent import errors. The tests should document exactly what behavior they verify.

    Keep tests focused: each test verifies exactly one behavior from the behavior list above.

    Code root: /Users/jmiloslavsky/Documents/Panda-Manager
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run --reporter=verbose tests/components/daily-prep-card.test.ts tests/components/daily-prep-page.test.ts tests/components/sidebar-daily-prep.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 test files exist. Tests run. Tests FAIL with assertion failures. Test names match requirement IDs in comments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend meeting-prep-context tests for CalendarMetadata (SKILL-01, SKILL-02)</name>
  <files>lib/__tests__/meeting-prep-context.test.ts</files>
  <behavior>
    - Test 1 (SKILL-01): buildMeetingPrepContext called with calendarMeta {attendees: ['Alice'], durationHours: '1.00', recurrenceFlag: true} includes "Attendees:" in the returned context string
    - Test 2 (SKILL-01): buildMeetingPrepContext called with calendarMeta {eventDescription: 'Q1 review'} includes the event description in the returned context string
    - Test 3 (SKILL-01): buildMeetingPrepContext called WITHOUT calendarMeta (third param omitted) still returns a valid context string (backward compatibility)
    - Test 4 (SKILL-02): meeting-prep.md skill file contains the header "## Context" (not "## Open Items")
    - Test 5 (SKILL-02): meeting-prep.md skill file contains "## Desired Outcome" header
    - Test 6 (SKILL-02): meeting-prep.md skill file contains "## Agenda" header

    Tests 1-3 must FAIL (signature doesn't accept calendarMeta yet).
    Tests 4-6 must FAIL (meeting-prep.md still has old headers).
  </behavior>
  <action>
    READ the existing lib/__tests__/meeting-prep-context.test.ts first to understand current test structure. Then APPEND a new describe block at the bottom:

    ```typescript
    describe('CalendarMetadata parameter (SKILL-01, SKILL-02)', () => {
      // Tests 1-3: calendarMeta injection into context
      // Tests 4-6: meeting-prep.md section headers
    });
    ```

    For Tests 4-6 (skill file headers): Use Node's fs.readFileSync to read skills/meeting-prep.md from process.cwd(). Assert on string content. These tests will FAIL until the skill file is updated.

    For Tests 1-3: Mock the DB queries (use the same mock pattern already in the file — check existing vi.mock() calls). Call buildMeetingPrepContext(projectId, undefined, calendarMeta) — this will throw/fail because the function signature doesn't yet accept a third param.

    Do NOT modify existing tests. Only append the new describe block.

    Code root: /Users/jmiloslavsky/Documents/Panda-Manager
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run --reporter=verbose lib/__tests__/meeting-prep-context.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>Existing tests still pass. New CalendarMetadata describe block exists with 6 failing tests. No regressions.</done>
</task>

</tasks>

<verification>
Full test run after all tasks:
`cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run --reporter=verbose tests/ 2>&1 | tail -30`

Expected: New stub tests FAIL with assertion failures (not import/module errors). Existing tests continue to pass.
</verification>

<success_criteria>
- 6 test files exist (5 new + 1 extended)
- `npx vitest run --reporter=verbose tests/` runs all new tests without "cannot find module" errors
- New tests are RED (failing assertions documenting expected behavior)
- Pre-existing tests remain GREEN
- Each test name includes the requirement ID it covers
</success_criteria>

<output>
After completion, create `.planning/phases/79-core-calendar-daily-prep/79-00-SUMMARY.md`
</output>
