---
phase: 79-core-calendar-daily-prep
plan: "05"
type: execute
wave: 4
depends_on:
  - "79-01"
  - "79-02"
  - "79-03"
  - "79-04"
files_modified: []
autonomous: false
requirements:
  - CAL-01
  - PREP-01
  - PREP-05
  - NAV-01

must_haves:
  truths:
    - "All 13 Phase 79 requirements are verifiable by a human in a running browser"
    - "No 404 on /daily-prep"
    - "CalendarImportModal opens from GlobalTimeView"
    - "Daily Prep sidebar link is visible and navigates correctly"
    - "SSE streaming renders brief text progressively in the browser"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for Phase 79. Automated tests validate logic; this checkpoint validates the end-to-end experience in a running browser including OAuth-gated flows and SSE streaming that require live network.

Purpose: Catch UI/UX issues that automated tests cannot — visual layout, streaming behavior, OAuth integration, interactive flows.
Output: Phase 79 marked ready for /gsd:verify-work.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-core-calendar-daily-prep/79-CONTEXT.md
@.planning/phases/79-core-calendar-daily-prep/79-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated gate — full Vitest suite + TypeScript compile</name>
  <files></files>
  <action>
    Run the full test suite and TypeScript compile. Both must be clean before proceeding to human verification.

    ```bash
    cd /Users/jmiloslavsky/Documents/Panda-Manager
    npx vitest run --reporter=verbose 2>&1 | tail -20
    npx tsc --noEmit 2>&1 | tail -10
    ```

    If any tests fail or TypeScript reports errors, stop and fix before proceeding.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run --reporter=verbose 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Full Vitest suite green. TypeScript compiles with no errors. Ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 79 complete implementation:
    - CalendarImportModal wired into GlobalTimeView (CAL-01)
    - Title+attendee hybrid matching with confidence badges (CAL-02, CAL-03)
    - Daily Prep sidebar link (NAV-01)
    - /daily-prep page with event cards, date picker, project dropdown (PREP-01, PREP-02, PREP-03, PREP-07)
    - Multi-select + parallel SSE generation + inline output + copy button (PREP-04, PREP-05, PREP-06)
    - Meeting Prep skill enhanced with calendar metadata + restructured sections (SKILL-01, SKILL-02)
  </what-built>
  <how-to-verify>
    Start the dev server: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run next-only`

    **CAL-01, CAL-02, CAL-03: Calendar Import from GlobalTimeView**
    1. Navigate to /time-tracking (GlobalTimeView)
    2. Confirm "Import from Calendar" button appears in the toolbar
    3. Click it — CalendarImportModal should open
    4. Verify events show confidence badges (High/Low/None) per event
    5. (If Google Calendar is connected) Verify events with matching project names show HIGH confidence badge

    **NAV-01: Sidebar Navigation**
    6. Confirm "Daily Prep" link appears in the sidebar directly below "Dashboard" and above "Projects"
    7. Click the Daily Prep link — should navigate to /daily-prep

    **PREP-01, PREP-02, PREP-03, PREP-07: Daily Prep Page**
    8. Confirm /daily-prep loads without errors
    9. (If Google Calendar connected) Verify today's event cards appear with: time, title, duration, matched project, attendees
    10. Verify events with no matched project show a project dropdown
    11. Change the date picker to a different day — verify page refetches events for that date
    12. Verify "No meetings on [date]" appears for a day with no events

    **PREP-04, PREP-05, PREP-06: Generation Flow**
    13. Select one or more event cards using checkboxes
    14. Click "Generate Prep" — button should become disabled while generating
    15. Verify each selected card shows a loading state ("Generating brief…")
    16. Verify the brief text streams in progressively (text appears character by character or in chunks)
    17. Verify the brief contains "## Context", "## Desired Outcome", "## Agenda" sections rendered as formatted markdown
    18. Click "Copy" on an expanded brief — paste into a text editor and verify content is correct
    19. Navigate away from /daily-prep and back — verify generated briefs are still expanded (LocalStorage persistence)

    **SKILL-01, SKILL-02: Skills Tab Compatibility**
    20. Navigate to any project → Skills tab
    21. Run "Meeting Prep" skill
    22. Verify the output uses Context / Desired Outcome / Agenda section headers (not the old Open Items / Suggested Agenda headers)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Or describe issues found (e.g., "step 15 - no loading state appears", "step 7 - sidebar link missing").</resume-signal>
</task>

</tasks>

<verification>
Phase 79 is complete when:
1. Full Vitest suite is green (Task 1)
2. Human approves all 22 verification steps above (Task 2)
</verification>

<success_criteria>
- All 22 verification steps confirmed by human
- No 404 on /daily-prep
- SSE streaming renders progressively in browser
- Brief persistence survives navigation
- Meeting Prep skill output uses new section headers
- Git commit and push to remote completed
</success_criteria>

<output>
After completion, create `.planning/phases/79-core-calendar-daily-prep/79-05-SUMMARY.md`
</output>
