---
phase: 13-skill-ux-+-draft-polish
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/phase13.spec.ts
autonomous: true
requirements:
  - SKILL-03
  - SKILL-04
  - SKILL-05
  - SKILL-06
  - SKILL-07
  - SKILL-08
  - SKILL-12
  - SKILL-13
  - DASH-09
  - SRCH-01
  - SRCH-02

must_haves:
  truths:
    - "All 11 Phase 13 E2E behaviors have failing stub tests before any implementation runs"
    - "Each stub uses expect(false, 'stub').toBe(true) so the failure is visible and intentional"
    - "Test file imports and test IDs match the grep patterns in VALIDATION.md"
  artifacts:
    - path: "tests/e2e/phase13.spec.ts"
      provides: "11 RED stub tests for all Phase 13 behaviors"
      contains: "phase13"
  key_links:
    - from: "tests/e2e/phase13.spec.ts"
      to: "VALIDATION.md grep patterns"
      via: "test description strings"
      pattern: "history.*skill|stakeholders.*skill|navigate.*skills|draft.*modal|draft.*fields|draft.*save|draft.*dismiss|search.*date|date.*filter.*empty|template.*modal|template.*count"
---

<objective>
Create the Wave 0 E2E test stub file for Phase 13. All 11 behaviors must be RED (failing) before implementation begins — this is the Nyquist requirement for this phase.

Purpose: Establishes a verifiable RED baseline before any implementation. Each subsequent plan drives specific tests GREEN.
Output: tests/e2e/phase13.spec.ts with 11 failing stub tests.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-skill-ux-+-draft-polish/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase13.spec.ts with 11 RED stub tests</name>
  <files>tests/e2e/phase13.spec.ts</files>
  <action>
Create tests/e2e/phase13.spec.ts with 11 stub tests that all fail immediately using `expect(false, 'stub').toBe(true)`. Follow the exact stub pattern established in phase12.spec.ts (and earlier phases): no `test.fixme()`, just the hard-fail assertion.

The test file must use `test.describe` groupings and description strings that match the VALIDATION.md grep patterns exactly. Use `baseURL` from playwright.config.ts (http://localhost:3000).

Tests to create (description strings must include the grep-matchable substrings):

Group: "History tab skill launch"
1. `"history tab has Generate Meeting Summary skill button"` — visits /customer/[any-project-id]/history, expects button with text "Generate Meeting Summary"
2. `"clicking history skill button navigates to skills tab"` — clicks "Generate Meeting Summary", expects URL to include "/skills"

Group: "Stakeholders tab skill launch"
3. `"stakeholders tab has Create Handoff Doc skill button"` — visits /customer/[any-project-id]/stakeholders, expects button with text "Create Handoff Doc"
4. `"clicking stakeholders skill button navigates to skills tab"` — clicks "Create Handoff Doc", expects URL to include "/skills"

Group: "Draft edit modal"
5. `"clicking draft card opens draft modal dialog"` — visits /dashboard, expects [role="dialog"] to be visible after clicking a draft card
6. `"draft modal exposes subject content and recipient fields"` — modal contains inputs for subject, content (textarea), and recipient
7. `"saving draft modal updates draft card content"` — edits content field, saves, modal closes, card shows updated text
8. `"dismiss button inside draft modal dismisses the draft"` — modal has Dismiss button; clicking it removes draft from list

Group: "Search date filter"
9. `"search page has from and to date inputs"` — visits /search, expects two date inputs present
10. `"date filter with past to-date returns empty results"` — sets to=2020-01-01, submits search with query "a", expects no results (or empty state)

Group: "Template browser modal"
11. `"templates button in PhaseBoard opens modal dialog"` — visits /customer/[any-project-id]/plan, clicks "Templates" button, expects [role="dialog"] to be visible
12. (note: "template count" test — if templates exist, each item shows task count in parentheses) — add as stub 12: `"template list shows task count per template"`

Wait — VALIDATION.md lists 11 tests. Create exactly 11 stubs matching the grep patterns listed in VALIDATION.md. Use project ID from the first active project in DB (or a hardcoded known ID from seed data — check prior phase specs for the project ID used). Use the same project ID pattern as phase12.spec.ts.

All 11 stubs use: `expect(false, 'stub').toBe(true)` as the only assertion. Do not add any real selectors or navigation — pure stubs.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/phase13.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 11 tests exist and ALL FAIL with "stub" message. Output shows "11 failed" (not "passed"). Running `npx playwright test tests/e2e/phase13.spec.ts --list` shows 11 tests. No test passes.
  </done>
</task>

</tasks>

<verification>
npx playwright test tests/e2e/phase13.spec.ts --list
Expected output: 11 tests listed
npx playwright test tests/e2e/phase13.spec.ts 2>&1 | grep -E "passed|failed"
Expected output: "11 failed" (all stubs RED)
</verification>

<success_criteria>
- tests/e2e/phase13.spec.ts exists with 11 failing stub tests
- Every test description matches the grep patterns in VALIDATION.md
- Zero tests pass — all stubs intentionally RED
- File committed to git
</success_criteria>

<output>
After completion, create .planning/phases/13-skill-ux-+-draft-polish/13-01-SUMMARY.md
</output>
