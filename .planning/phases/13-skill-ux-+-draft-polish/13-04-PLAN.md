---
phase: 13-skill-ux-+-draft-polish
plan: "04"
type: execute
wave: 2
depends_on:
  - "13-02"
  - "13-03"
files_modified: []
autonomous: false
requirements:
  - SKILL-03
  - SKILL-04
  - SKILL-05
  - SKILL-06
  - SKILL-07
  - SKILL-08
  - SKILL-12
  - SKILL-13
  - DASH-09
  - SRCH-01
  - SRCH-02

must_haves:
  truths:
    - "Full E2E suite passes with all 11 phase13.spec.ts tests GREEN"
    - "Human has verified the four UX improvements work end-to-end in a running app"
  artifacts:
    - path: "tests/e2e/phase13.spec.ts"
      provides: "11 GREEN tests (all stubs driven to passing)"
      contains: "11 passed"
  key_links:
    - from: "tests/e2e/phase13.spec.ts"
      to: "bigpanda-app (running dev server)"
      via: "Playwright browser automation against localhost:3000"
      pattern: "npx playwright test tests/e2e/phase13.spec.ts"
---

<objective>
Drive all 11 phase13.spec.ts tests from RED to GREEN, then human-verify the four UX improvements in a running app session.

Purpose: Confirms that the four targeted improvements (skill launch buttons, draft edit modal, search date filter, template picker modal) all work correctly together from the user's perspective.
Output: 11/11 GREEN tests + human sign-off.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-skill-ux-+-draft-polish/13-VALIDATION.md
@.planning/phases/13-skill-ux-+-draft-polish/13-02-SUMMARY.md
@.planning/phases/13-skill-ux-+-draft-polish/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive all 11 E2E stubs to GREEN</name>
  <files></files>
  <action>
Run the full phase13 test suite and diagnose any failing tests. For each failure:

1. Read the relevant source file to understand the current implementation
2. Identify the gap between the stub expectation and actual behavior
3. Make the minimal fix — do not rewrite working code

Common issues to check if tests are still failing after Plans 02 and 03:
- Navigation tests (`navigate.*skills`): Playwright needs to actually click the Link and wait for URL change. Ensure the test waits for `page.waitForURL('**/skills')` after click.
- Draft modal tests: Verify the project used in tests has at least one draft in 'pending' status. If the test DB has no pending drafts, the modal will never open. Add a test fixture draft if needed (via API call in `beforeEach`).
- Date filter empty test: Requires submitting search with `to=2020-01-01`. Verify the test sets the date input value correctly (Playwright `fill()` on date inputs requires ISO format `YYYY-MM-DD`).
- Template modal tests: Verify the test project has plan_templates in DB. If not, add a `beforeEach` fixture that creates one via POST to `/api/plan-templates`.

Fix each failing test either by:
a) Updating the stub test body (replacing `expect(false, 'stub').toBe(true)` with real selectors), OR
b) Making a minimal code fix if the implementation has a bug

Run after each fix: `npx playwright test tests/e2e/phase13.spec.ts`

Target: 11/11 GREEN. If a test cannot be made GREEN without significant new work, document the blocker in the SUMMARY.md.

Also run the full suite to check for regressions: `npx playwright test`
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/phase13.spec.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 11 phase13 tests pass. Full Playwright suite shows no regressions from prior phases.
    Output includes: "11 passed" (or "11 passed, 0 failed").
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — four UX improvements</name>
  <action>
Human verifies all four Phase 13 UX improvements work end-to-end in the running app. Start dev server with `cd bigpanda-app && npm run dev`, then follow the verification steps in how-to-verify below.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/phase13.spec.ts 2>&1 | grep -E "passed|failed"</automated>
  </verify>
  <done>Human types "approved" confirming all 5 verification scenarios pass.</done>
  <what-built>
Four UX improvements implemented and E2E-verified:
1. Skill launch buttons on History tab ("Generate Meeting Summary") and Stakeholders tab ("Create Handoff Doc") — both navigate to /customer/[id]/skills
2. Draft edit modal in Drafts Inbox — click any draft card opens Dialog with subject/body/recipient editable fields + Dismiss inside modal
3. Search date range filter — from/to inputs on /search correctly filter results by date
4. Templates button in PhaseBoard opens Dialog modal showing template name + task count; selecting a template applies it
  </what-built>
  <how-to-verify>
Start the dev server: `cd bigpanda-app && npm run dev`

**Test 1 — History skill button:**
1. Navigate to any project workspace → Engagement History tab
2. Confirm "Generate Meeting Summary" button appears in the heading row (top-right)
3. Click it — confirm navigation to the Skills tab (/customer/[id]/skills)

**Test 2 — Stakeholders skill button:**
1. Navigate to any project workspace → Stakeholders tab
2. Confirm "Create Handoff Doc" button appears next to "+ Add Stakeholder"
3. Click it — confirm navigation to the Skills tab

**Test 3 — Draft edit modal:**
1. Navigate to Dashboard
2. If Drafts Inbox has any pending drafts, click on a draft card
3. Confirm a Dialog modal opens (not inline textarea expansion)
4. Confirm modal has Subject, Recipient, and Body/Content fields
5. Confirm "Dismiss Draft" button is visible inside the modal
6. Edit the Subject field and click Save — confirm modal closes and card reflects updated subject
7. (Optional) Click Dismiss Draft — confirm draft is removed from the inbox

**Test 4 — Search date filter:**
1. Navigate to /search
2. Enter a search query (e.g. "a")
3. Set the "To" date to a past date like 2020-01-01
4. Submit the search
5. Confirm results list is empty (no records before 2020)
6. Change "To" to today's date and resubmit — confirm results appear

**Test 5 — Templates modal in PhaseBoard:**
1. Navigate to any project workspace → Plan tab
2. Locate the "Templates" button
3. Click it — confirm a Dialog modal opens (not a dropdown popover)
4. If templates exist, confirm each one shows name + task count in parentheses
5. Select a template — confirm it applies and the modal closes
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
npx playwright test tests/e2e/phase13.spec.ts 2>&1 | grep -E "passed|failed"
Expected: "11 passed"

npx playwright test 2>&1 | grep -E "passed|failed" | tail -3
Expected: full suite passes with no new failures
</verification>

<success_criteria>
- 11/11 phase13.spec.ts tests GREEN
- Full Playwright suite shows no regressions
- Human has approved all 5 verification scenarios
- Phase 13 marked COMPLETE in STATE.md
</success_criteria>

<output>
After completion, create .planning/phases/13-skill-ux-+-draft-polish/13-04-SUMMARY.md
</output>
