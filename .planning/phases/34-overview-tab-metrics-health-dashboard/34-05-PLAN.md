---
phase: 34-overview-tab-metrics-health-dashboard
plan: 05
type: execute
wave: 5
depends_on:
  - 34-04
files_modified: []
autonomous: false
requirements:
  - METR-01
  - HLTH-01
  - TMLN-01

must_haves:
  truths:
    - "Full automated test suite passes GREEN with no failures"
    - "Production build completes without errors"
    - "Milestone timeline renders as visual chart above the onboarding section"
    - "Metrics section shows two ProgressRings (ADR and Biggy), a risk donut, and a weekly hours bar chart"
    - "Health Dashboard shows overall RAG badge in correct color, per-track badges, and active blocker count"
    - "Charts are responsive and do not overflow their containers"
    - "All three requirements METR-01, HLTH-01, TMLN-01 are visually confirmed in browser"
  artifacts:
    - path: "bigpanda-app/tests/overview/metrics-health.test.ts"
      provides: "GREEN passing tests for METR-01, HLTH-01, TMLN-01"
    - path: "bigpanda-app/tests/api/overview-metrics.test.ts"
      provides: "GREEN passing tests for overview-metrics API"
    - path: "bigpanda-app/tests/overview/timeline-replacement.test.ts"
      provides: "GREEN passing smoke test for old timeline section removal"
  key_links:
    - from: "human verification"
      to: "visual layout in browser"
      via: "manual UAT steps"
      pattern: "N/A"
---

<objective>
Run full automated test suite, execute production build, and complete human UAT of all three visual sections added in Phase 34.

Purpose: Gate the phase on both automated correctness and human visual confirmation before marking complete.
Output: All tests GREEN, build clean, UAT approval for METR-01, HLTH-01, TMLN-01.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-overview-tab-metrics-health-dashboard/34-CONTEXT.md
@.planning/phases/34-overview-tab-metrics-health-dashboard/34-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated verification</name>
  <files></files>
  <action>
    Run the following automated checks in order. Fix any failures before proceeding to the human checkpoint.

    Step 1 — Phase-specific tests:
    ```bash
    cd bigpanda-app && npm test -- tests/overview/ tests/api/overview-metrics.test.ts --reporter=verbose
    ```
    All tests must pass GREEN. If any fail, diagnose and fix the root cause (not the test).

    Step 2 — Full test suite:
    ```bash
    cd bigpanda-app && npm test
    ```
    Zero failures. If any previously passing tests have regressed, fix them before continuing.

    Step 3 — TypeScript check:
    ```bash
    cd bigpanda-app && npx tsc --noEmit
    ```
    Zero TypeScript errors. Fix all type errors.

    Step 4 — Production build:
    ```bash
    cd bigpanda-app && npm run build 2>&1 | tail -30
    ```
    Build must complete without errors. Warnings are acceptable if they are pre-existing.

    Document all checks and their outcomes in the task completion notes.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npm test -- tests/overview/ tests/api/overview-metrics.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All Phase 34 tests GREEN, full suite green, tsc clean, build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — visual verification of all three Overview sections</name>
  <action>
    Pause for human verification. The user will start the dev server and visually inspect all three sections added in Phase 34. Claude should wait for the user's response before proceeding.
  </action>
  <verify>
    <automated>echo "Human verification required — see how-to-verify steps below"</automated>
  </verify>
  <done>User types "approved" confirming all 6 visual checks pass</done>
  <what-built>
    Three new sections added to the Overview tab:
    1. MilestoneTimeline — Recharts BarChart showing project milestones with status colors, positioned ABOVE the onboarding section
    2. OverviewMetrics — Metrics section with ADR/Biggy ProgressRings, risk distribution donut, and weekly hours bar chart
    3. HealthDashboard — Overall RAG badge (rule-based: critical risk → red, high risk OR completion less than 50 pct → yellow, else green), per-track ADR/Biggy health badges, active blocker count
  </what-built>
  <how-to-verify>
    1. Start the dev server:
       ```bash
       cd bigpanda-app && npm run next-only
       ```
    2. Navigate to any project's Overview tab (e.g., http://localhost:3000/customer/1/overview)

    3. Verify TMLN-01 — Milestone Timeline:
       - The first section rendered is a visual chart (bars or scatter dots) — NOT a text list
       - Timeline appears ABOVE the ADR/Biggy onboarding columns
       - If milestones exist: bars are colored by status (green = complete, blue = in-progress, gray = upcoming, red = blocked)
       - "View all →" link is present
       - Old dot-on-spine horizontal scroller is GONE (no duplicate milestone section lower in the page)

    4. Verify METR-01 — Metrics:
       - "Metrics" section heading visible
       - Two circular progress rings labeled ADR and Biggy showing completion percentages
       - Risk distribution donut chart (if risks exist: colored slices for critical/high/medium/low)
       - Weekly hours bar chart (last 8 weeks, or "No time entries" if none)
       - Stat cards do not overflow their containers; resize browser to verify responsive layout

    5. Verify HLTH-01 — Health Dashboard:
       - "Health Dashboard" section heading visible
       - Overall health badge shows correct color: red if critical risks exist, yellow if high risks or either track below 50%, green otherwise
       - ADR health badge and Biggy health badge visible with correct colors
       - Active blocker count shows number of steps with status='blocked' (0 is acceptable, shows "0 active blockers")

    6. Visual quality checks:
       - Charts do not overflow their containers at 1280px viewport
       - Charts do not overflow at 768px (tablet) viewport
       - No duplicate sections (only ONE milestone section, ONE metrics section, ONE health dashboard)
       - Sections separate cleanly with spacing

    Expected: All 6 checks pass visually.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass. Or describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npm test` GREEN (zero failures, zero skips in Phase 34 tests)
- `npx tsc --noEmit` clean
- `npm run build` succeeds
- Human UAT: all three sections render correctly in browser with correct visual hierarchy
</verification>

<success_criteria>
Phase 34 is complete when: automated suite is green, build is clean, and the user has visually confirmed MilestoneTimeline at top (Recharts chart, not text list), OverviewMetrics section (ProgressRings + risk donut + hours chart), and HealthDashboard (overall RAG + per-track badges + blocker count). Requirements METR-01, HLTH-01, TMLN-01 all verified.
</success_criteria>

<output>
After completion, create `.planning/phases/34-overview-tab-metrics-health-dashboard/34-05-SUMMARY.md`
</output>
