---
phase: 22-source-badges-+-audit-log
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - bigpanda-app/tests/audit/audit-helper.test.ts
  - bigpanda-app/tests/audit/source-badge.test.ts
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
must_haves:
  truths:
    - "writeAuditLog() test file exists with RED failing tests before lib/audit.ts is created"
    - "SourceBadge test file exists with RED failing tests before component is created"
    - "Tests cover Manual / Ingested / Discovered badge label derivation"
    - "Tests cover writeAuditLog() correct field mapping (entity_type, entity_id, action, actor_id, before_json, after_json)"
    - "Tests cover DELETE audit ordering — before-state captured before removal"
  artifacts:
    - path: "bigpanda-app/tests/audit/audit-helper.test.ts"
      provides: "Failing unit tests for writeAuditLog() helper"
      contains: "writeAuditLog"
    - path: "bigpanda-app/tests/audit/source-badge.test.ts"
      provides: "Failing unit tests for SourceBadge label derivation"
      contains: "SourceBadge"
  key_links:
    - from: "bigpanda-app/tests/audit/audit-helper.test.ts"
      to: "bigpanda-app/lib/audit.ts"
      via: "import { writeAuditLog }"
      pattern: "import.*writeAuditLog.*lib/audit"
    - from: "bigpanda-app/tests/audit/source-badge.test.ts"
      to: "bigpanda-app/components/SourceBadge.tsx"
      via: "import { SourceBadge }"
      pattern: "import.*SourceBadge.*components"
---

<objective>
Write RED failing tests for the two new modules Phase 22 introduces: the `writeAuditLog()` helper and the `SourceBadge` component.

Purpose: TDD Wave 0 — tests define the contract that Wave 1 implementation must satisfy. Tests MUST fail before implementation exists.
Output: Two test files under `bigpanda-app/tests/audit/` with RED status confirmed.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-source-badges-+-audit-log/22-RESEARCH.md
@.planning/phases/22-source-badges-+-audit-log/22-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for writeAuditLog() helper</name>
  <files>bigpanda-app/tests/audit/audit-helper.test.ts</files>
  <behavior>
    - Test 1: writeAuditLog() inserts a row with correct entity_type, entity_id, action='update', actor_id='default', before_json, after_json
    - Test 2: writeAuditLog() inserts a row for action='create' with null before_json and a valid after_json
    - Test 3: writeAuditLog() inserts a row for action='delete' with a valid before_json and null after_json
    - Test 4: writeAuditLog() is callable with a numeric entity_id
    - Test 5: writeAuditLog() is callable with a null entity_id (for create-before-insert cases)
  </behavior>
  <action>
    Create `bigpanda-app/tests/audit/audit-helper.test.ts`.

    Import `writeAuditLog` from `@/lib/audit`. Because `lib/audit.ts` does not exist yet, the import will resolve to `undefined` — all tests that call it will fail with TypeError or "not a function". This is the valid RED state.

    Use Vitest (`describe`, `it`, `expect`, `vi`). Mock the `db` layer using `vi.mock('@/db', () => ({ db: { insert: vi.fn().mockReturnValue({ values: vi.fn() }) } }))` so tests exercise the helper's argument-mapping logic without a live DB.

    Write 5 `it()` blocks as described in `<behavior>`. Each should call `writeAuditLog()` with specific args and assert `db.insert` was called with the matching field mapping (`entity_type`, `entity_id`, `action`, `actor_id`, `before_json`, `after_json`).

    Do NOT write the implementation. Confirm tests are RED by running the verify command.

    Note: The Vitest config at `bigpanda-app/vitest.config.ts` has a `server-only` mock already configured — no changes to config needed.
  </action>
  <verify>
    <automated>cd bigpanda-app && npx vitest run tests/audit/audit-helper.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Test file exists. Running vitest shows failures (RED) — not "no test files found". Output should show 5 failing tests, not 5 passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED tests for SourceBadge label derivation</name>
  <files>bigpanda-app/tests/audit/source-badge.test.ts</files>
  <behavior>
    - Test 1: source='manual' renders badge text containing "Manual"
    - Test 2: source='ingestion', artifactName='SOW.pdf' renders badge text containing "Ingested — SOW.pdf"
    - Test 3: source='discovery', discoverySource='Slack' renders badge text containing "Discovered — Slack"
    - Test 4: source='discovery' with null discoverySource renders "Discovered" (no crash, no "undefined")
    - Test 5: source='ingestion' with null artifactName falls back to "Manual" badge (safety path per research)
  </behavior>
  <action>
    Create `bigpanda-app/tests/audit/source-badge.test.ts`.

    Import `SourceBadge` from `@/components/SourceBadge`. Because `components/SourceBadge.tsx` does not exist yet, the import will be undefined — tests will fail. Valid RED state.

    Use Vitest + `@testing-library/react` (`render`, `screen`). Write 5 `it()` blocks as described in `<behavior>`. Each calls `render(<SourceBadge source={...} artifactName={...} discoverySource={...} />)` and asserts `screen.getByText(...)` finds the expected label substring.

    Note: The label for test 5 (ingestion + null artifactName) should show "Manual" as a safe fallback — this matches the SourceBadge design where an incomplete ingestion attribution should not show a broken badge.

    Do NOT write the component. Confirm tests are RED by running the verify command.
  </action>
  <verify>
    <automated>cd bigpanda-app && npx vitest run tests/audit/source-badge.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Test file exists. Running vitest shows failures (RED). Output shows 5 failing tests.</done>
</task>

</tasks>

<verification>
Both test files exist at `bigpanda-app/tests/audit/`. Running the full audit suite returns failures (RED), not "no test files found".

```bash
cd bigpanda-app && npx vitest run tests/audit/
```

Expected: ~10 failing tests across both files. Zero passing.
</verification>

<success_criteria>
- `bigpanda-app/tests/audit/audit-helper.test.ts` exists with 5 RED tests covering writeAuditLog() field mapping
- `bigpanda-app/tests/audit/source-badge.test.ts` exists with 5 RED tests covering Manual / Ingested / Discovered badge labels
- Both files are importable (no syntax errors, only runtime failures from missing modules)
- Wave 1 can now implement against these contracts
</success_criteria>

<output>
After completion, create `.planning/phases/22-source-badges-+-audit-log/22-01-SUMMARY.md`
</output>
