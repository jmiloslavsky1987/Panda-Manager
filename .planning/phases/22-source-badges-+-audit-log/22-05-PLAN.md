---
phase: 22-source-badges-+-audit-log
plan: 05
type: execute
wave: 4
depends_on:
  - "22-03"
  - "22-04"
autonomous: false
files_modified: []
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
must_haves:
  truths:
    - "Source badge is visible on every entity row across all 9 workspace tabs in the browser"
    - "Manually-entered records show grey 'Manual' badge"
    - "Document-ingested records show blue 'Ingested — [filename]' badge"
    - "Discovery-approved records show purple 'Discovered — [tool]' badge"
    - "Clicking delete on any record opens a confirmation dialog before deletion fires"
    - "After a create/edit/delete, audit_log contains the correct row with entity_type, action, actor_id, and before/after JSON"
  artifacts:
    - path: "bigpanda-app/app/customer/[id]/actions/page.tsx"
      provides: "Actions tab with visible badges"
      contains: "SourceBadge"
    - path: "bigpanda-app/lib/audit.ts"
      provides: "writeAuditLog helper wired into all routes"
      contains: "writeAuditLog"
  key_links:
    - from: "browser UI delete button"
      to: "DeleteConfirmDialog"
      via: "click opens dialog"
      pattern: "DeleteConfirmDialog"
    - from: "audit_log table"
      to: "API route mutations"
      via: "transaction-wrapped insert"
      pattern: "writeAuditLog"
---

<objective>
Human verification checkpoint: confirm source badges render correctly in the browser, deletion confirmation dialogs work end-to-end, and the audit log captures all mutations correctly.

Purpose: Visual and functional proof that AUDIT-01, AUDIT-02, and AUDIT-03 are satisfied before Phase 22 closes.
Output: Phase 22 approved or issues documented for gap closure.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-source-badges-+-audit-log/22-03-SUMMARY.md
@.planning/phases/22-source-badges-+-audit-log/22-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - SourceBadge component on all 9 workspace tab pages showing Manual / Ingested / Discovered provenance
    - DeleteConfirmDialog wrapping all delete buttons across all workspace tabs
    - writeAuditLog() wired into all PATCH/POST/DELETE API routes — every mutation produces an audit_log row
    - Migration 0017 adding discovery_source column to 12 entity tables
    - Discovery approve route populates discovery_source so "Discovered — Slack" style badges work
  </what-built>
  <how-to-verify>
    Run the app: `cd bigpanda-app && npm run dev`

    **AUDIT-01: Source badges**
    1. Open the ACME project (or any seeded project)
    2. Visit the Actions tab — confirm every row has a grey "Manual" badge (existing manually-entered records)
    3. Visit the Artifacts tab — confirm uploaded/ingested artifacts show "Ingested — [filename]" badge
    4. If any discovery-approved items exist in any tab, confirm they show "Discovered — [tool]" badge
    5. Repeat badge check on Risks, Milestones, Decisions, History, Stakeholders, Teams, Architecture tabs

    **AUDIT-03: Delete confirmation dialog**
    6. On the Teams tab, find a business outcome or focus area card with a delete button
    7. Click delete — confirm a dialog opens asking "Delete this [entity]?" with Cancel and Delete buttons
    8. Click Cancel — confirm no deletion occurred
    9. Click delete again → confirm → record is removed from the UI

    **AUDIT-02: Audit log**
    10. After step 9 (deletion), run in a terminal:
        `psql $DATABASE_URL -c "SELECT entity_type, entity_id, action, actor_id, before_json IS NOT NULL as has_before, after_json IS NULL as after_null, created_at FROM audit_log ORDER BY created_at DESC LIMIT 5;"`
    11. Confirm the delete event appears with: correct entity_type, action='delete', actor_id='default', has_before=true, after_null=true
    12. Edit any action (change the title) — confirm a new row appears in audit_log with action='update', before_json showing old title, after_json showing new title

    Expected results:
    - Every tab row has a source badge (no rows without a badge)
    - Delete dialog always appears before deletion fires
    - audit_log has rows for all mutations performed during testing
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 22, or describe any issues found for gap closure</resume-signal>
</task>

</tasks>

<verification>
Human-verified: badges visible, delete confirmation working, audit log populated with correct entries.
</verification>

<success_criteria>
- "approved" received from human verification
- All three AUDIT requirements confirmed satisfied in the running application
- Phase 22 can be marked COMPLETE in STATE.md
</success_criteria>

<output>
After approval, create `.planning/phases/22-source-badges-+-audit-log/22-05-SUMMARY.md` and update STATE.md to mark Phase 22 COMPLETE.
</output>
