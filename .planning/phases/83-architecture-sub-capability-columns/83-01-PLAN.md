---
phase: 83-architecture-sub-capability-columns
plan: "01"
type: execute
wave: 1
depends_on: ["83-00"]
files_modified:
  - db/migrations/0046_arch_nodes_parent_id.sql
  - db/schema.ts
autonomous: true
requirements:
  - ARCH-SCHEMA

must_haves:
  truths:
    - "arch_nodes table has parent_id (nullable integer FK → arch_nodes.id) and node_type (text, DEFAULT 'sub-capability') columns"
    - "Existing projects have section nodes (node_type=section) and 10 sub-capability nodes (node_type=sub-capability) per ADR Track"
    - "Console node has node_type=console in existing projects"
    - "architecture_integrations rows with phase='Alert Intelligence' are remapped to 'Monitoring Integrations'; phase='Incident Intelligence' and 'Workflow Automation' rows are deleted"
    - "ArchNode TypeScript type includes parent_id and node_type fields"
    - "section-grouping tests turn GREEN after this plan"
  artifacts:
    - path: "db/migrations/0046_arch_nodes_parent_id.sql"
      provides: "Schema migration adding parent_id and node_type; full existing-project data migration"
    - path: "db/schema.ts"
      provides: "Updated archNodes table definition with parent_id and node_type columns"
  key_links:
    - from: "db/schema.ts archNodes"
      to: "lib/queries.ts ArchNode type"
      via: "typeof archNodes.$inferSelect"
      pattern: "inferSelect"
    - from: "db/migrations/0046_arch_nodes_parent_id.sql"
      to: "arch_nodes table in PostgreSQL"
      via: "ALTER TABLE + PL/pgSQL block"
      pattern: "ALTER TABLE arch_nodes ADD COLUMN"
---

<objective>
Apply the 0046 DB migration that adds parent_id and node_type to arch_nodes, migrates existing project data to the new section/sub-capability structure, and updates the Drizzle schema type so TypeScript picks up the new fields.

Purpose: This is the foundational schema change. Everything else in Phase 83 — rendering, seed scripts, downstream consumers — depends on these two new columns existing and being populated correctly.
Output: Migration SQL file, updated schema.ts. The ArchNode TypeScript type automatically gains parent_id and node_type via $inferSelect.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-architecture-sub-capability-columns/83-CONTEXT.md
@.planning/phases/83-architecture-sub-capability-columns/83-RESEARCH.md
@.planning/phases/83-architecture-sub-capability-columns/83-00-SUMMARY.md

<interfaces>
<!-- Current archNodes table definition in db/schema.ts lines 866-880 -->
export const archNodes = pgTable('arch_nodes', {
  id: serial('id').primaryKey(),
  track_id: integer('track_id').notNull().references(() => archTracks.id),
  project_id: integer('project_id').notNull().references(() => projects.id),
  name: text('name').notNull(),
  display_order: integer('display_order').default(0).notNull(),
  status: archNodeStatusEnum('status').default('planned').notNull(),
  notes: text('notes'),
  source_trace: text('source_trace'),
  created_at: timestamp('created_at').defaultNow().notNull(),
}, (t) => [
  uniqueIndex('arch_nodes_project_track_name_idx').on(t.project_id, t.track_id, t.name),
]);
export type ArchNode = typeof archNodes.$inferSelect;

<!-- Latest migration is: db/migrations/0045_daily_prep_tables.sql -->
<!-- Next migration number: 0046 -->

<!-- Unique index constraint to be aware of: arch_nodes_project_track_name_idx on (project_id, track_id, name) -->
<!-- Migration must DELETE old section node names BEFORE re-inserting them with node_type='section' -->

<!-- display_order strategy from RESEARCH.md open question resolution: -->
<!-- Use 10, 20, 30 for sections; Console at 25. All under 100 filter. Sort: AI(10) → II(20) → Console(25) → WA(30) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0046 — ADD COLUMN parent_id + node_type + existing project data migration</name>
  <files>db/migrations/0046_arch_nodes_parent_id.sql</files>
  <behavior>
    - Migration adds parent_id (nullable integer FK → arch_nodes.id)
    - Migration adds node_type (text NOT NULL DEFAULT 'sub-capability')
    - Existing ADR Track section nodes are deleted, then re-inserted with node_type='section'
    - Console node gets node_type='console' via UPDATE
    - 10 sub-capability nodes are inserted under each section per project
    - architecture_integrations phase='Alert Intelligence' remapped to 'Monitoring Integrations'
    - architecture_integrations phase IN ('Incident Intelligence','Workflow Automation') rows deleted
    - Unique index constraint not violated (DELETE before INSERT within same PL/pgSQL block)
  </behavior>
  <action>
Create db/migrations/0046_arch_nodes_parent_id.sql exactly as specified in RESEARCH.md with the following adjustments:

1. ALTER TABLE statements first:
```sql
ALTER TABLE arch_nodes ADD COLUMN parent_id integer REFERENCES arch_nodes(id);
ALTER TABLE arch_nodes ADD COLUMN node_type text NOT NULL DEFAULT 'sub-capability';
```

2. PL/pgSQL DO $$ block for existing project migration. Use display_order values:
   - Alert Intelligence section: display_order = 10
   - Incident Intelligence section: display_order = 20
   - Console: UPDATE SET node_type = 'console' (display_order stays as-is)
   - Workflow Automation section: display_order = 30
   
   This ordering (10, 20, 25-ish Console, 30) ensures correct left-to-right visual order after the renderer sorts by display_order.

3. Sub-capability node display_order values (1-4 within each section — these do NOT conflict with section display_orders because the renderer groups sub-capabilities under their parent):
   - Alert Intelligence sub-capabilities: display_order 1, 2, 3
   - Incident Intelligence sub-capabilities: display_order 1, 2, 3, 4
   - Workflow Automation sub-capabilities: display_order 1, 2, 3, 4

4. Copy the exact sub-capability names from CONTEXT.md (exact casing matters for integration phase matching):
   - Alert Intelligence: 'Monitoring Integrations', 'Alert Normalization', 'Alert Enrichment'
   - Incident Intelligence: 'Alert Correlation', 'Incident Enrichment', 'Incident Classification', 'Suggested Root Cause'
   - Workflow Automation: 'Environments', 'Automated Incident Creation', 'Automated Incident Notification', 'Automated Incident Remediation'

5. Wrap the entire DO $$ block in proper error handling. The RETURNING...INTO pattern requires STRICT keyword for single-row safety — use it.

6. After the DO $$ block, the phase remapping statements:
```sql
UPDATE architecture_integrations SET phase = 'Monitoring Integrations' WHERE phase = 'Alert Intelligence';
DELETE FROM architecture_integrations WHERE phase IN ('Incident Intelligence', 'Workflow Automation');
```

CRITICAL: The unique index is on (project_id, track_id, name). The migration deletes old 'Alert Intelligence', 'Incident Intelligence', 'Workflow Automation' rows THEN inserts them as section nodes with node_type='section'. The delete happens inside the PL/pgSQL loop before the inserts — this is safe.

DO NOT wrap in a BEGIN/COMMIT — run-migrations.ts handles transaction management externally.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && ls db/migrations/0046_arch_nodes_parent_id.sql && grep -c "ADD COLUMN" db/migrations/0046_arch_nodes_parent_id.sql</automated>
  </verify>
  <done>File db/migrations/0046_arch_nodes_parent_id.sql exists, contains two ADD COLUMN statements, a PL/pgSQL block iterating over ADR Track projects, and the phase remapping UPDATE/DELETE statements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: schema.ts — add parent_id and node_type to archNodes table definition</name>
  <files>db/schema.ts</files>
  <behavior>
    - archNodes pgTable definition gains parent_id column (integer, nullable, self-FK reference)
    - archNodes pgTable definition gains node_type column (text, NOT NULL, default 'sub-capability')
    - ArchNode TypeScript type automatically includes new fields via $inferSelect
    - No other schema changes — additive only
  </behavior>
  <action>
Edit db/schema.ts — find the archNodes pgTable definition (around line 866) and add two new columns inside the column object, BEFORE the closing `}, (t) => [` line:

```typescript
parent_id: integer('parent_id').references((): AnyPgColumn => archNodes.id),
node_type: text('node_type').notNull().default('sub-capability'),
```

Notes:
- parent_id is a nullable self-referential FK. Use `.references((): AnyPgColumn => archNodes.id)` (the `(): AnyPgColumn =>` return type annotation is required by Drizzle for circular references — see how wbsItems.parent_id is defined in the same schema file for the exact pattern).
- node_type is text (not a pgEnum) to match the SQL migration. The constraint is enforced at the application layer via TypeScript.
- Import `AnyPgColumn` from 'drizzle-orm/pg-core' if not already imported — check the existing imports at the top of schema.ts first.

After adding the columns, verify TypeScript compiles cleanly:
```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx tsc --noEmit 2>&1 | grep -i "schema" | head -10
```

Also run section-grouping tests to verify they turn GREEN:
```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/arch/section-grouping.test.ts
```
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/arch/section-grouping.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>tests/arch/section-grouping.test.ts passes (all 5 tests GREEN). TypeScript compiles with no errors in schema.ts. ArchNode type now includes parent_id and node_type fields.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/arch/section-grouping.test.ts && npx vitest run tests/arch/arch-nodes-wiring.test.ts
```
section-grouping: all GREEN. arch-nodes-wiring: still GREEN (mock objects need parent_id/node_type added if TypeScript now requires them — update mock shapes in arch-nodes-wiring.test.ts if tsc complains).
</verification>

<success_criteria>
- db/migrations/0046_arch_nodes_parent_id.sql exists with correct ALTER TABLE and PL/pgSQL block
- db/schema.ts archNodes definition includes parent_id and node_type
- tests/arch/section-grouping.test.ts passes (5/5 GREEN)
- Existing arch tests remain GREEN
- TypeScript compiles cleanly after schema.ts update
</success_criteria>

<output>
After completion, create `.planning/phases/83-architecture-sub-capability-columns/83-01-SUMMARY.md`
</output>
