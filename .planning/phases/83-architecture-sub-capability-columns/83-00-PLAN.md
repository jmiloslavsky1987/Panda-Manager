---
phase: 83-architecture-sub-capability-columns
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/arch/section-grouping.test.ts
  - tests/arch/integration-modal-optgroup.test.ts
  - tests/arch/arch-context-builder.test.ts
autonomous: true
requirements:
  - ARCH-SCHEMA
  - ARCH-RENDER
  - ARCH-DOWNSTREAM

must_haves:
  truths:
    - "Wave 0 test files exist with RED failing tests gating the schema, rendering, and downstream changes"
    - "section-grouping tests assert node_type and parent_id fields on ArchNode mocks"
    - "integration-modal-optgroup tests assert <optgroup> structure in rendered ADR phase selector"
    - "arch-context-builder tests assert buildArchPhasesContext returns only sub-capability names"
  artifacts:
    - path: "tests/arch/section-grouping.test.ts"
      provides: "Test gate for schema migration (node_type, parent_id fields on ArchNode)"
    - path: "tests/arch/integration-modal-optgroup.test.ts"
      provides: "Test gate for IntegrationEditModal grouped optgroup rendering"
    - path: "tests/arch/arch-context-builder.test.ts"
      provides: "Test gate for buildArchPhasesContext sub-capability filter"
  key_links:
    - from: "tests/arch/section-grouping.test.ts"
      to: "db/schema.ts ArchNode"
      via: "mock object shape assertion"
      pattern: "node_type.*section|parent_id"
    - from: "tests/arch/arch-context-builder.test.ts"
      to: "worker/jobs/document-extraction.ts buildArchPhasesContext"
      via: "direct import mock"
      pattern: "buildArchPhasesContext"
---

<objective>
Create three failing test files that will gate the Wave 1, Wave 2, and Wave 3 implementation tasks. These stubs establish the expected contracts before any production code is written.

Purpose: RED gate enforcement — tests must fail now, then pass after each subsequent wave completes. Follows the established Phase 79/80/82 stub pattern.
Output: Three new test files in tests/arch/ (gitignored). Existing tests/arch/arch-nodes-wiring.test.ts, column-reorder.test.ts, and status-cycle.test.ts are NOT modified.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-architecture-sub-capability-columns/83-CONTEXT.md
@.planning/phases/83-architecture-sub-capability-columns/83-RESEARCH.md

<interfaces>
<!-- Current ArchNode shape in db/schema.ts (before migration) -->
<!-- Missing: parent_id, node_type — these are the new fields tests assert -->
Current ArchNode fields: id, track_id, project_id, name, display_order, status, notes, source_trace, created_at
Post-migration additions: parent_id (integer | null), node_type ('section' | 'sub-capability' | 'console')

<!-- Existing arch tests mock pattern (arch-nodes-wiring.test.ts) -->
nodes mock: { id, track_id, project_id, name, display_order, status, notes, source_trace, created_at }
// After Wave 1 these mocks must include parent_id and node_type

<!-- buildArchPhasesContext signature (worker/jobs/document-extraction.ts line 726) -->
export async function buildArchPhasesContext(projectId: number): Promise<string>
// Currently queries archNodes + archTracks with no node_type filter
// After Wave 3: must filter WHERE node_type = 'sub-capability'

<!-- IntegrationEditModal ADR_PHASES (components/arch/IntegrationEditModal.tsx line 5-10) -->
const ADR_PHASES = ['Alert Intelligence', 'Incident Intelligence', 'Console', 'Workflow Automation']
// After Wave 2: replaced with <optgroup> structure (no ADR_PHASES array)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: section-grouping.test.ts — node_type and parent_id shape assertions</name>
  <files>tests/arch/section-grouping.test.ts</files>
  <action>
Create tests/arch/section-grouping.test.ts with failing stubs that gate the schema migration (Wave 1) and rendering overhaul (Wave 2).

Use the vi.mock + stub pattern from tests/arch/arch-nodes-wiring.test.ts. All tests should be RED now (ArchNode mock does not have node_type/parent_id yet).

Test cases to write:

1. "ArchNode has node_type field" — mock getArchNodes to return a node with `node_type: 'sub-capability'`, assert `result.nodes[0].node_type` equals `'sub-capability'`. This is RED now because ArchNode type has no node_type.

2. "ArchNode has parent_id field" — mock returns node with `parent_id: null`, assert `result.nodes[0].parent_id` equals `null`. RED now.

3. "Section nodes have node_type=section and parent_id=null" — mock returns one section node `{ id: 1, node_type: 'section', parent_id: null, name: 'Alert Intelligence', display_order: 10, ... }` and one sub-capability node `{ id: 2, node_type: 'sub-capability', parent_id: 1, name: 'Monitoring Integrations', display_order: 1, ... }`. Assert section node has node_type==='section', parent_id===null. Assert sub-capability node has parent_id===1. RED now.

4. "Console node has node_type=console" — mock returns `{ id: 99, node_type: 'console', parent_id: null, name: 'Console', ... }`. Assert node_type==='console'. RED now.

5. "Sub-capability nodes can be grouped by parent_id" — given nodes array from mock with mixed section/sub-capability nodes, write inline grouping logic (same as renderer will use) and assert grouped structure. This tests the renderer's grouping logic before the component exists.

Use stub pattern: mocks return objects with the NEW expected shape. TypeScript will flag the mocks as invalid until schema.ts is updated — that's the RED signal.

NOTE: tests/ is gitignored per [79-00] project decision. These files exist on-disk only.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/arch/section-grouping.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>File exists at tests/arch/section-grouping.test.ts. Running the test file produces failures (TypeScript type errors or assertion failures) confirming RED state. Tests are not syntax errors — they are valid Vitest tests that fail on assertion.</done>
</task>

<task type="auto">
  <name>Task 2: integration-modal-optgroup.test.ts — ADR phase picker optgroup assertions</name>
  <files>tests/arch/integration-modal-optgroup.test.ts</files>
  <action>
Create tests/arch/integration-modal-optgroup.test.ts with failing stubs that gate the IntegrationEditModal optgroup rendering (Wave 2).

Do NOT use React component rendering (no @testing-library/react needed). Instead, test the data structure that drives the grouped select — this keeps the test lightweight and avoids JSDOM setup.

Test cases to write:

1. "ADR sub-capability names are not in flat ADR_PHASES list" — import (or inline) the new ADR sub-capability names constant. Assert that 'Monitoring Integrations' is a valid ADR phase name. This is RED now because IntegrationEditModal only has flat ADR_PHASES = ['Alert Intelligence', 'Incident Intelligence', 'Console', 'Workflow Automation'].

2. "ADR optgroup structure covers all 10 sub-capabilities" — define the expected optgroup structure inline as a typed object:
```ts
const ADR_OPTGROUPS = [
  { label: 'Alert Intelligence', options: ['Monitoring Integrations', 'Alert Normalization', 'Alert Enrichment'] },
  { label: 'Incident Intelligence', options: ['Alert Correlation', 'Incident Enrichment', 'Incident Classification', 'Suggested Root Cause'] },
  { label: 'Workflow Automation', options: ['Environments', 'Automated Incident Creation', 'Automated Incident Notification', 'Automated Incident Remediation'] },
]
```
Assert ADR_OPTGROUPS has 3 groups, total option count is 10. This test passes immediately (it's a static data structure test) — that is intentional; the RED test is #3.

3. "IntegrationEditModal does not use flat ADR_PHASES with section names" — use fs.readFileSync to read components/arch/IntegrationEditModal.tsx source. Assert the source does NOT contain `'Alert Intelligence'` as a direct option value (i.e., the flat list is gone). RED now (old ADR_PHASES still contains 'Alert Intelligence').

4. "IntegrationEditModal uses optgroup for ADR track" — read source, assert it contains `<optgroup` string. RED now (no optgroup in current source).
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/arch/integration-modal-optgroup.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>File exists. Test #3 and #4 are RED (source-scan assertions fail on current IntegrationEditModal). Test #2 passes (static data). Tests are syntactically valid Vitest files.</done>
</task>

<task type="auto">
  <name>Task 3: arch-context-builder.test.ts — buildArchPhasesContext sub-capability filter assertions</name>
  <files>tests/arch/arch-context-builder.test.ts</files>
  <action>
Create tests/arch/arch-context-builder.test.ts with failing stubs that gate the document-extraction.ts and chat-context-builder.ts downstream updates (Wave 3).

Test cases to write:

1. "buildArchPhasesContext output does not contain section node names" — mock db with rows returning BOTH section nodes (node_type='section', name='Alert Intelligence') AND sub-capability nodes (node_type='sub-capability', name='Monitoring Integrations'). Call buildArchPhasesContext with mocked db. Assert output does NOT contain 'Alert Intelligence' as a stage name. Assert output DOES contain 'Monitoring Integrations'. RED now (current function has no node_type filter, so 'Alert Intelligence' appears in output).

2. "buildArchPhasesContext stage guide uses sub-capability names" — Assert the returned string contains 'Monitoring Integrations:' in the stage assignment guide section. RED now (current guide uses section names like 'Alert Intelligence stage:').

3. "buildArchPhasesContext stage guide covers all 10 sub-capabilities" — Assert returned string contains all of: 'Alert Normalization', 'Alert Enrichment', 'Alert Correlation', 'Incident Enrichment', 'Incident Classification', 'Suggested Root Cause', 'Environments', 'Automated Incident Creation', 'Automated Incident Notification', 'Automated Incident Remediation'. RED now.

4. "chat-context-builder architecture section groups by section->sub-column" — use fs.readFileSync on lib/chat-context-builder.ts. Assert source contains 'getArchNodes' import or call (i.e., node structure context is added). RED now (current file only calls getArchTabData, no getArchNodes call).

Mocking approach for test 1-3: mock the db module and the archNodes/archTracks imports. Use vi.mock pattern. buildArchPhasesContext does a db.select().from(archNodes).innerJoin(archTracks)... mock this to return a controlled rows array with node_type field included.

IMPORTANT: buildArchPhasesContext is in worker/jobs/document-extraction.ts — import it directly for tests 1-3.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/arch/arch-context-builder.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>File exists. Tests 1-3 and test 4 are RED (filter not applied, getArchNodes not called in context builder). Tests are syntactically valid Vitest files with clear assertion failure messages.</done>
</task>

</tasks>

<verification>
Run full arch test suite — new stubs are RED, existing tests stay GREEN:
```
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/arch/
```
Expected: section-grouping = FAIL (5 tests), integration-modal-optgroup = FAIL (3 tests), arch-context-builder = FAIL (4 tests), arch-nodes-wiring = PASS, column-reorder = PASS, status-cycle = PASS.
</verification>

<success_criteria>
- All three new test files exist at tests/arch/
- New tests are RED (failing assertions, not syntax errors)
- Existing arch tests (arch-nodes-wiring, column-reorder, status-cycle) remain GREEN
- Each failing test has a clear assertion message pointing to the specific production code that must be written
</success_criteria>

<output>
After completion, create `.planning/phases/83-architecture-sub-capability-columns/83-00-SUMMARY.md`
</output>
