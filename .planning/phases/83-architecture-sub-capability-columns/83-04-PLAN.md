---
phase: 83-architecture-sub-capability-columns
plan: "04"
type: execute
wave: 4
depends_on: ["83-02", "83-03"]
files_modified: []
autonomous: false

requirements:
  - ARCH-SCHEMA
  - ARCH-RENDER
  - ARCH-DOWNSTREAM
  - ARCH-SEED

must_haves:
  truths:
    - "ADR Track architecture diagram shows 3 colored section headers (blue, amber, green) with sub-capability columns beneath each"
    - "Console node renders as a special narrow node between Incident Intelligence and Workflow Automation sections"
    - "AI Assistant Track renders flat and unchanged"
    - "Integration Add/Edit modal shows grouped optgroup phase picker for ADR track"
    - "Existing project architecture data shows section structure (not old flat 4-node layout)"
    - "New project creation produces correct 3-section + Console + 10-sub-capability structure"
    - "Full Vitest suite passes with no regressions"
    - "Production build succeeds"
  artifacts:
    - path: "db/migrations/0046_arch_nodes_parent_id.sql"
      provides: "Applied migration"
    - path: "components/arch/InteractiveArchGraph.tsx"
      provides: "Section header rendering verified in browser"
    - path: "components/arch/IntegrationEditModal.tsx"
      provides: "Optgroup phase picker verified"
  key_links:
    - from: "architecture tab (browser)"
      to: "ADR Track section headers"
      via: "visual verification"
      pattern: "section-header"
    - from: "full vitest suite"
      to: "zero regressions"
      via: "npx vitest run"
      pattern: "passing"
---

<objective>
Human verification of the complete Phase 83 implementation. Claude runs migration, builds the app, runs the full test suite, then pauses for the user to verify the architecture diagram visually in the browser.

Purpose: This is the Phase 83 gate — all automated tests must be GREEN and the visual diagram must match the BigPanda Future State Alert Pipeline reference before the phase is marked complete.
Output: Approved visual UX + clean test suite + git push.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/83-architecture-sub-capability-columns/83-CONTEXT.md
@.planning/phases/83-architecture-sub-capability-columns/83-02-SUMMARY.md
@.planning/phases/83-architecture-sub-capability-columns/83-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migration, run full test suite, build, and git push</name>
  <files></files>
  <action>
Run all automated validation steps in order:

1. Apply the 0046 migration against the running PostgreSQL database:
```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && node -e "require('./run-migrations').runMigrations()" 2>&1
```
If run-migrations.ts requires compilation: `npx tsx run-migrations.ts` or check package.json scripts for a `db:migrate` script.

2. Run the full Vitest test suite:
```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run 2>&1 | tail -20
```
All tests must pass. If any tests fail, fix before proceeding to the checkpoint.

3. Run TypeScript compilation check:
```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx tsc --noEmit 2>&1 | grep "error TS" | head -20
```
Zero TypeScript errors required.

4. Run production build:
```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run build 2>&1 | tail -20
```
Build must succeed with no errors.

5. Git commit all Phase 83 changes and push:
```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && git add db/migrations/0046_arch_nodes_parent_id.sql db/schema.ts components/arch/InteractiveArchGraph.tsx components/arch/IntegrationEditModal.tsx app/api/projects/route.ts scripts/seed-projects.ts lib/chat-context-builder.ts worker/jobs/document-extraction.ts "app/api/projects/[projectId]/chat/tools/arch-tools.ts" && git commit -m "feat: [Phase 83] architecture sub-capability columns — grouped ADR Track layout" && git push
```
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>
  <done>Full test suite GREEN. TypeScript compiles. Production build succeeds. Git push succeeded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of architecture diagram in browser</name>
  <action>User verifies the ADR Track section headers, sub-capability columns, Console node, and Integration modal optgroup in a running dev server.</action>
  <what-built>
Complete Phase 83 implementation:
- ADR Track architecture diagram now renders with 3 colored section headers (Alert Intelligence/blue, Incident Intelligence/amber, Workflow Automation/green), each containing their sub-capability columns
- Console node renders as a special narrow node between Incident Intelligence and Workflow Automation
- AI Assistant Track unchanged (flat rendering)
- Integration Add/Edit modal uses grouped optgroup phase picker for ADR track
- All downstream consumers (extraction pipeline, chat tools, context builder) use sub-capability names
- DB migration applied: existing project arch nodes now have section + sub-capability structure
  </what-built>
  <how-to-verify>
1. Start the dev server: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run next-only`

2. Open an existing project → Architecture tab → ADR Track section

3. Verify the diagram shows:
   - "Alert Intelligence" colored section header (blue left-border bar) above 3 sub-columns: Monitoring Integrations, Alert Normalization, Alert Enrichment
   - Console node (narrow circle/badge) between Incident Intelligence and Workflow Automation sections
   - "Incident Intelligence" colored section header (amber) above 4 sub-columns: Alert Correlation, Incident Enrichment, Incident Classification, Suggested Root Cause
   - "Workflow Automation" colored section header (green) above 4 sub-columns: Environments, Automated Incident Creation, Automated Incident Notification, Automated Incident Remediation
   - AI Assistant Track below still renders flat (no section headers)

4. Click "Add Integration" button → phase picker shows grouped optgroup structure with sub-capability names under section labels

5. Try dragging a sub-capability column within its section — verify it reorders correctly and stays within the section

6. Create a new project → Architecture tab → verify 3 sections + Console + 10 sub-columns appear immediately

7. Confirm no visual regressions in the AI Assistant Track, Teams map, or any other architecture diagram elements
  </how-to-verify>
  <resume-signal>Type "approved" if the diagram looks correct, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
All tests pass + build succeeds + user approves visual UX.
</verification>

<success_criteria>
- Full Vitest suite GREEN (zero failures)
- TypeScript compiles with no errors
- Production build succeeds
- User confirms ADR Track renders with section headers and sub-capability columns
- User confirms Integration modal shows optgroup picker
- User confirms AI Assistant Track unchanged
- Git push to remote succeeded
</success_criteria>

<output>
After completion, create `.planning/phases/83-architecture-sub-capability-columns/83-04-SUMMARY.md`
Update ROADMAP.md to mark Phase 83 complete.
Update STATE.md to reflect Phase 83 done and v12.0 milestone shipped.
</output>
