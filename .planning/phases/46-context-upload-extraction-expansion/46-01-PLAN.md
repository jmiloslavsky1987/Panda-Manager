---
phase: 46-context-upload-extraction-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bigpanda-app/tests/ingestion/extractor.test.ts
  - bigpanda-app/tests/ingestion/extraction-types.test.ts
  - bigpanda-app/worker/jobs/document-extraction.ts
autonomous: true
requirements: [WBS-03, TEAM-02, ARCH-04]

must_haves:
  truths:
    - "Extraction system recognizes wbs_task entity type from uploaded documents"
    - "Extraction system recognizes team_engagement entity type from uploaded documents"
    - "Extraction system recognizes arch_node entity type from uploaded documents"
    - "Extracted wbs_task items include track, parent_section_name, level, title, status, description fields"
    - "Extracted team_engagement items include section_name and content fields"
    - "Extracted arch_node items include track, node_name, status, notes fields"
  artifacts:
    - path: "bigpanda-app/worker/jobs/document-extraction.ts"
      provides: "Extended EXTRACTION_SYSTEM prompt with 3 new entity types"
      exports: ["EXTRACTION_SYSTEM", "EntityType"]
    - path: "bigpanda-app/tests/ingestion/extractor.test.ts"
      provides: "Test coverage for new entity type extraction"
      min_lines: 50
    - path: "bigpanda-app/tests/ingestion/extraction-types.test.ts"
      provides: "Test coverage for new entity type deduplication"
      min_lines: 50
  key_links:
    - from: "bigpanda-app/worker/jobs/document-extraction.ts"
      to: "EXTRACTION_SYSTEM prompt"
      via: "Claude API streaming call"
      pattern: "anthropic\\.messages\\.create"
---

<objective>
Extend Claude extraction prompt to recognize and extract three new entity types (wbs_task, team_engagement, arch_node) from uploaded documents with hierarchical and track-based classification guidance.

Purpose: Enable AI-driven document extraction to auto-populate WBS, Team Engagement, and Architecture features created in Phase 45
Output: Extended extraction prompt + EntityType union + Wave 0 RED test scaffolds for extraction AND deduplication
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-context-upload-extraction-expansion/46-RESEARCH.md
@.planning/phases/46-context-upload-extraction-expansion/46-VALIDATION.md
@.planning/phases/45-database-schema-foundation/45-01-SUMMARY.md
@.planning/phases/45-database-schema-foundation/45-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types executors will use from existing codebase -->

From bigpanda-app/worker/jobs/document-extraction.ts (lines 24-57):
```typescript
export const EXTRACTION_SYSTEM = `You are a project data extractor...
{
  "entityType": "action" | "risk" | "decision" | "milestone" | "stakeholder" | "task" | "architecture" | "history" | "businessOutcome" | "team" | "note" | "workstream" | "onboarding_step" | "integration",
  "fields": { /* entity-specific key-value pairs as strings */ },
  "confidence": 0.85,
  "sourceExcerpt": "verbatim text this was extracted from (max 200 chars)"
}`;

export type EntityType =
  | 'action' | 'risk' | 'decision' | 'milestone' | 'stakeholder'
  | 'task' | 'architecture' | 'history' | 'businessOutcome' | 'team'
  | 'note' | 'team_pathway' | 'workstream' | 'onboarding_step' | 'integration';
```

From Phase 45 schema (45-01-SUMMARY.md):
```typescript
// WBS structure: 3-level hierarchy with ADR (10+25 items) and Biggy (5+9 items) templates
// Team Engagement: 5 sections (Business Outcomes, Architecture, E2E Workflows, Teams & Engagement, Top Focus Areas)
// Architecture: 2 tracks (ADR Track, AI Assistant Track) with nodes per track
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Wave 0 — RED test scaffolds for new entity type extraction and deduplication</name>
  <files>bigpanda-app/tests/ingestion/extractor.test.ts, bigpanda-app/tests/ingestion/extraction-types.test.ts</files>
  <behavior>
    - Test 1: wbs_task extraction returns items with required fields (track, parent_section_name, level, title, status, description)
    - Test 2: team_engagement extraction returns items with required fields (section_name, content)
    - Test 3: arch_node extraction returns items with required fields (track, node_name, status, notes)
    - Test 4: wbs_task dedup matches existing wbsItems by project + track + title prefix
    - Test 5: team_engagement dedup matches existing sections by project + section_name + content prefix
    - Test 6: arch_node dedup matches existing nodes by project + track + node_name prefix
  </behavior>
  <action>
Create TWO RED test files following Phase 45 test patterns:

**File 1: bigpanda-app/tests/ingestion/extractor.test.ts**
Create with RED test stubs for three new entity type extractions. Follow existing test pattern from tests/schema/ and tests/seeding/.

Write three describe blocks:
1. describe('wbs_task extraction') — test that mock document with WBS-like tasks extracts wbs_task items with track='ADR' or 'Biggy', parent_section_name, level (1-3), title, status, description
2. describe('team_engagement extraction') — test that mock document with engagement content extracts team_engagement items with section_name matching one of 5 valid sections, content field populated
3. describe('arch_node extraction') — test that mock document with architecture details extracts arch_node items with track ('ADR Track'|'AI Assistant Track'), node_name, status ('planned'|'in_progress'|'live'), notes

Use mock fixtures inline (no external fixture files needed yet). Tests MUST fail (RED) because EXTRACTION_SYSTEM prompt doesn't yet include new entity types.

**File 2: bigpanda-app/tests/ingestion/extraction-types.test.ts**
Create with RED test stubs for isAlreadyIngested() deduplication logic (Plan 02 will implement). Follow pattern from existing extraction-types tests.

Write three describe blocks:
1. describe('isAlreadyIngested - wbs_task') — insert a wbsItem, call isAlreadyIngested with matching wbs_task extraction item, expect true
2. describe('isAlreadyIngested - team_engagement') — insert a teamEngagementSection with content, call isAlreadyIngested with matching team_engagement item, expect true
3. describe('isAlreadyIngested - arch_node') — insert an archNode, call isAlreadyIngested with matching arch_node item, expect true

Tests MUST fail (RED) because isAlreadyIngested() doesn't yet have cases for these entity types.

Reference existing test patterns from Phase 45 (tests/seeding/wbs-templates.test.ts, tests/seeding/team-engagement.test.ts, tests/seeding/architecture.test.ts) for assertion style.
  </action>
  <verify>
    <automated>npm test tests/ingestion/extractor.test.ts tests/ingestion/extraction-types.test.ts --run</automated>
    Expected: 6 tests RED (fail) because new entity types not yet in extraction prompt or dedup logic
  </verify>
  <done>Both test files exist with 6 failing RED tests total (3 extraction + 3 dedup) for wbs_task, team_engagement, arch_node</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Extend EXTRACTION_SYSTEM prompt with wbs_task, team_engagement, arch_node guidance</name>
  <files>bigpanda-app/worker/jobs/document-extraction.ts</files>
  <behavior>
    - Claude extraction recognizes wbs_task with hierarchical classification (track + parent_section_name + level)
    - Claude extraction recognizes team_engagement with 5-section routing (section_name validation)
    - Claude extraction recognizes arch_node with track-based classification (ADR Track vs AI Assistant Track)
  </behavior>
  <action>
Extend EXTRACTION_SYSTEM prompt string (lines 24-57) with three new entity types. Follow existing entity type guidance format exactly.

Add to entityType union on line 28:
- "wbs_task"
- "team_engagement"
- "arch_node"

Add to Entity type guidance section (after line 46):
```
- wbs_task: { title, track ("ADR" or "Biggy"), parent_section_name (exact match from WBS template — e.g., "Solution Design", "Platform Configuration", "Integrations"), level (1, 2, or 3 — 1 for top-level sections, 2 for sub-items, 3 for leaf tasks), status ("not_started", "in_progress", or "complete"), description (task details or null) } — task that belongs in WBS structure; extract track and parent section verbatim as they appear in document; use level to indicate hierarchy depth
- team_engagement: { section_name ("Business Outcomes" | "Architecture" | "E2E Workflows" | "Teams & Engagement" | "Top Focus Areas"), content (markdown text for this section) } — content for Team Engagement Map sections; extract verbatim section names; use for engagement data, team details, business outcomes, workflow descriptions
- arch_node: { track ("ADR Track" | "AI Assistant Track"), node_name (tool or capability name — e.g., "Event Ingest", "Alert Intelligence", "Knowledge Sources"), status ("planned" | "in_progress" | "live"), notes (integration details, status notes, or null) } — architecture capability or tool node; extract track verbatim; use for system components, tools, integrations mentioned in architecture context
```

CRITICAL disambiguation guidance (add after existing "IMPORTANT disambiguation:" section on line 49-50):
```
- wbs_task vs task: wbs_task = hierarchical WBS template items with track + parent section; task = generic project tasks. If document mentions WBS structure, ADR/Biggy tracks, or explicit template sections → wbs_task. Otherwise → task.
- team_engagement vs team: team_engagement = section content for Team Engagement Map (5 specific sections); team = team metadata (name, track, ingest_status). Use team_engagement for prose/paragraph content about engagement, outcomes, workflows.
```

Also extend EntityType union type definition (lines 61-76) to include:
```typescript
  | 'wbs_task'
  | 'team_engagement'
  | 'arch_node'
```

IMPORTANT: Maintain existing prompt structure, formatting, and all existing entity types. Do NOT remove or modify existing guidance. Only ADD new types.

After changes, GREEN the RED extraction tests from Task 0 (extractor.test.ts). Tests should pass because prompt now includes extraction guidance for new entity types. Deduplication tests (extraction-types.test.ts) will remain RED until Plan 02.
  </action>
  <verify>
    <automated>npm test tests/ingestion/extractor.test.ts --run</automated>
    Expected: 3 extraction tests GREEN (pass) after prompt expansion. Dedup tests still RED (expected — Plan 02 implements).
  </verify>
  <done>EXTRACTION_SYSTEM prompt includes wbs_task, team_engagement, arch_node with field-level guidance; EntityType union includes new types; extraction tests GREEN; dedup tests RED (awaiting Plan 02)</done>
</task>

</tasks>

<verification>
Run targeted ingestion tests:
```bash
npm test tests/ingestion/ --run
```

Extraction tests GREEN. Deduplication tests RED (expected — will be greened in Plan 02). No regressions on existing entity types.
</verification>

<success_criteria>
- [ ] EXTRACTION_SYSTEM prompt includes wbs_task entity type with track + parent_section_name + level fields
- [ ] EXTRACTION_SYSTEM prompt includes team_engagement entity type with section_name + content fields
- [ ] EXTRACTION_SYSTEM prompt includes arch_node entity type with track + node_name + status + notes fields
- [ ] EntityType union in document-extraction.ts includes 3 new types
- [ ] Prompt guidance includes disambiguation rules (wbs_task vs task, team_engagement vs team)
- [ ] Test file tests/ingestion/extractor.test.ts exists with 3 GREEN extraction tests
- [ ] Test file tests/ingestion/extraction-types.test.ts exists with 3 RED dedup tests (Plan 02 will green)
- [ ] Claude API can extract new entity types from sample documents
</success_criteria>

<output>
After completion, create `.planning/phases/46-context-upload-extraction-expansion/46-01-SUMMARY.md`
</output>
