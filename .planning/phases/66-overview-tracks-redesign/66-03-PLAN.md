---
phase: 66-overview-tracks-redesign
plan: 03
type: execute
wave: 3
depends_on:
  - 66-01
  - 66-02
files_modified: []
autonomous: false
requirements:
  - OVRVW-01
  - OVRVW-02
  - OVRVW-03
  - OVRVW-04
  - OVRVW-05

must_haves:
  truths:
    - "Overview tab shows exactly 3 ADR static tracks and 3 Biggy static tracks"
    - "Dynamic summary cards show live integration counts"
    - "Trash icon deletes an integration without a confirmation dialog"
    - "Generate Now button is always visible as small outline button"
    - "Weekly Focus empty state shows quiet auto-schedule message"
  artifacts:
    - path: "bigpanda-app/components/OnboardingDashboard.tsx"
      provides: "Verified hybrid static/dynamic rendering"
    - path: "bigpanda-app/components/WeeklyFocus.tsx"
      provides: "Verified always-visible Generate Now"
  key_links:
    - from: "static track cards"
      to: "STATIC_ADR_TRACKS / STATIC_BIGGY_TRACKS config"
      via: "visual confirmation — only 3 cards per column, correct names"
      pattern: "Discovery.*Platform.*UAT|Discovery.*Platform.*Validation"
---

<objective>
Human verification gate for all Phase 66 requirements: visual/functional confirmation that static tracks, dynamic summary cards, integration delete, and WeeklyFocus UX redesign all work correctly in the browser.

Purpose: The five OVRVW requirements span UI changes that require human eyes to verify layout, UX labels, and delete behavior.
Output: All 5 requirements confirmed or issues documented for gap closure.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/66-overview-tracks-redesign/66-CONTEXT.md
@.planning/phases/66-overview-tracks-redesign/66-01-SUMMARY.md
@.planning/phases/66-overview-tracks-redesign/66-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification</name>
  <files></files>
  <action>
    Run TypeScript compilation check to confirm no errors before human verification:

    ```bash
    cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app"
    npx tsc --noEmit
    ```

    If any errors exist, fix them before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1; echo "Exit: $?"</automated>
  </verify>
  <done>TypeScript compilation passes with 0 errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all 5 OVRVW requirements</name>
  <files></files>
  <action>
    Start dev server and verify all 5 OVRVW requirements in the browser. See how-to-verify steps below.
  </action>
  <verify>
    <automated>MISSING — visual verification required in browser</automated>
  </verify>
  <done>All 5 OVRVW requirements confirmed passing in browser.</done>
  <what-built>
    Phase 66 complete implementation:
    - Static onboarding tracks (OVRVW-01): ADR shows 3 cards (Discovery and Kickoff, Platform Config, UAT); Biggy shows 3 cards (Discovery and Kickoff, Platform Config, Validation) — hardcoded names, not from DB phase list
    - Dynamic track summary cards (OVRVW-02): "ADR — Live Tracks" and "Biggy — Live Tracks" sections above the Integration Tracker with integration count stats
    - Weekly Focus auto-schedule (OVRVW-03): Project creation now registers weekly-focus repeatable BullMQ job (0 6 * * 1) — verified by checking new project creation flow
    - Weekly Focus Generate Now UX (OVRVW-04): Small outline button always visible in Weekly Focus header, not just in empty state; empty state shows quiet text instead of large CTA
    - Integration delete (OVRVW-05): Trash icon on each integration card in Integration Tracker — immediate delete, no confirmation
  </what-built>
  <how-to-verify>
    Start dev server: `cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npm run next-only`

    **OVRVW-01 — Static Tracks:**
    1. Open any project's Overview tab
    2. Confirm the ADR onboarding column shows exactly 3 tracks: "Discovery & Kickoff", "Platform Config", "UAT" (no others)
    3. Confirm the Biggy onboarding column shows exactly 3 tracks: "Discovery & Kickoff", "Platform Config", "Validation" (no others)
    4. Confirm step status badges still work (click to cycle status)

    **OVRVW-02 — Dynamic Summary Cards:**
    5. Scroll down past the phase cards — confirm an "ADR — Live Tracks" section appears with "Integrations" and "Teams" stat cards showing counts
    6. Confirm "Biggy — Live Tracks" section with "IT Knowledge Graph" and "Teams" stat cards

    **OVRVW-04 — Generate Now UX:**
    7. In the Weekly Focus section, confirm "Generate Now" button is visible even when weekly focus bullets ARE populated (not hidden behind data)
    8. Confirm the button is small and outline/secondary style (grey border, not blue)
    9. On a project with no weekly focus data yet, confirm the empty state shows: "Weekly focus generates automatically every Monday at 6am." as quiet text

    **OVRVW-05 — Integration Delete:**
    10. In the Integration Tracker section, confirm each integration card has a trash icon button (top-right area of card)
    11. Click a trash icon — confirm the integration disappears immediately with no confirmation dialog
    12. Verify the deletion persists on page refresh (check Integrations tab — the deleted item should be gone)

    **OVRVW-03 — Weekly Focus Auto-Schedule (optional if no test project available):**
    13. Create a new test project — inspect browser network tab on POST /api/projects to confirm 201 response
    14. If Redis is running, verify the weekly-focus repeatable job was registered (check Redis KEYS or BullMQ scheduler)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found for gap closure.</resume-signal>
</task>

</tasks>

<verification>
All 5 OVRVW requirements confirmed through visual browser verification:
- OVRVW-01: 3 static ADR tracks, 3 static Biggy tracks visible
- OVRVW-02: Dynamic summary cards present with live data
- OVRVW-03: Weekly-focus job registered on new project creation
- OVRVW-04: Generate Now always-visible, outline style, quiet empty state
- OVRVW-05: Trash icon deletes integration immediately, no confirmation
</verification>

<success_criteria>
- All 5 OVRVW requirements pass human verification
- No regressions in existing onboarding step status cycling
- No regressions in integration status/notes/track editing
- TypeScript build clean (0 errors)
</success_criteria>

<output>
After completion, create `.planning/phases/66-overview-tracks-redesign/66-03-SUMMARY.md`
</output>
