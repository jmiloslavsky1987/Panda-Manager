---
phase: 59-project-lifecycle-management
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/__tests__/lifecycle/archive.test.ts
  - bigpanda-app/__tests__/lifecycle/delete.test.ts
  - bigpanda-app/__tests__/lifecycle/restore.test.ts
  - bigpanda-app/__tests__/lifecycle/portfolio.test.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-04, PORTF-01, PORTF-02]
must_haves:
  truths:
    - "Four RED test files exist covering archive, delete, restore, and portfolio exclusion"
    - "All tests fail (RED) because no production code has been written yet"
    - "Test file structure matches the Vitest pattern used by existing __tests__/ files"
  artifacts:
    - path: "bigpanda-app/__tests__/lifecycle/archive.test.ts"
      provides: "PROJ-01 test contract — archive PATCH: admin required, status set, non-admin 403"
      exports: []
    - path: "bigpanda-app/__tests__/lifecycle/delete.test.ts"
      provides: "PROJ-02 test contract — DELETE pre-flight blocks running jobs, requires archived status first"
      exports: []
    - path: "bigpanda-app/__tests__/lifecycle/restore.test.ts"
      provides: "PROJ-04 test contract — restore PATCH sets status to active, seedProjectFromRegistry idempotency"
      exports: []
    - path: "bigpanda-app/__tests__/lifecycle/portfolio.test.ts"
      provides: "PORTF-01/PORTF-02 test contract — getActiveProjects excludes archived, deleted row excluded"
      exports: []
  key_links:
    - from: "bigpanda-app/__tests__/lifecycle/archive.test.ts"
      to: "bigpanda-app/app/api/projects/[projectId]/route.ts"
      via: "mocked Drizzle db + requireProjectRole"
      pattern: "vi\\.mock.*requireProjectRole"
    - from: "bigpanda-app/__tests__/lifecycle/portfolio.test.ts"
      to: "bigpanda-app/lib/queries.ts"
      via: "mocked db returning filtered project rows"
      pattern: "getActiveProjects"
---

<objective>
Create four RED TDD test stubs that define the behavioral contracts for the lifecycle API handlers and queries. These tests MUST fail before implementation begins.

Purpose: Establish contracts so that Plan 02 (Wave 1) drives implementation to GREEN.
Output: 4 test files in `__tests__/lifecycle/` — all failing RED.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-project-lifecycle-management/59-VALIDATION.md

<interfaces>
<!-- Existing test pattern from the codebase — use as structural template -->
<!-- Vitest + node environment. Tests live in __tests__/ with vi.mock for db and auth -->

From bigpanda-app/vitest.config.ts:
  environment: 'node'
  test runner: vitest

From bigpanda-app/app/api/projects/[projectId]/route.ts (current state):
```typescript
// PATCH uses requireProjectRole(numericId, 'user') — WILL BE upgraded in Plan 02
// No DELETE handler exists yet
export async function GET(...) // exists
export async function PATCH(...) // exists, uses 'user' role (bug)
// DELETE — MISSING
```

From bigpanda-app/lib/queries.ts:
```typescript
export async function getActiveProjects(opts?: ProjectQueryOpts): Promise<ProjectWithHealth[]>
// filters: inArray(projects.status, ['active', 'draft'])
// 'archived' is excluded because it is not in ['active', 'draft']
```

From bigpanda-app/db/schema.ts (confirmed):
  projectStatusEnum values: 'active', 'draft', 'archived'
  skillRuns.status enum includes: 'pending', 'running', 'completed', 'failed'
  extractionJobs.status: similar enum with 'pending', 'running'
  projects table has FK children with onDelete: 'cascade' on most tables
</interfaces>
</context>

<feature>
  <name>Project Lifecycle Behavioral Contracts</name>
  <files>
    bigpanda-app/__tests__/lifecycle/archive.test.ts,
    bigpanda-app/__tests__/lifecycle/delete.test.ts,
    bigpanda-app/__tests__/lifecycle/restore.test.ts,
    bigpanda-app/__tests__/lifecycle/portfolio.test.ts
  </files>
  <behavior>
    archive.test.ts contracts:
    - PATCH /api/projects/[projectId] with {status: 'archived'} returns 200 when caller is admin
    - PATCH with {status: 'archived'} returns 403 when caller is not admin (requireProjectRole fails)
    - After successful archive PATCH, project.status is set to 'archived' in DB

    delete.test.ts contracts:
    - DELETE /api/projects/[projectId] returns 409 if project.status is 'active' (not archived)
    - DELETE returns 409 if there are pending/running skill_runs for the project
    - DELETE returns 409 if there are pending/running extraction_jobs for the project
    - DELETE returns 200 and removes project row when project is 'archived' and no active jobs

    restore.test.ts contracts:
    - PATCH with {status: 'active'} on an archived project returns 200
    - After successful restore, project.status is 'active'
    - seedProjectFromRegistry is called on restore (status === 'active' transition)
    - seedProjectFromRegistry is called with the numeric project ID

    portfolio.test.ts contracts:
    - getActiveProjects returns projects with status 'active'
    - getActiveProjects returns projects with status 'draft'
    - getActiveProjects does NOT return projects with status 'archived'
    - After a project row is deleted from DB, getActiveProjects does not return it
  </behavior>
  <implementation>
    Each test file mocks the Drizzle db and requireProjectRole using vi.mock.
    Use describe/it/expect from vitest.
    Write tests that CALL the route handler functions (PATCH, DELETE) or query functions (getActiveProjects) directly.
    Do NOT import the actual route.ts as a module — use handler function imports.

    RED cycle: Write tests → run `npx vitest run __tests__/lifecycle/ -x` → confirm all fail → commit.

    Stub pattern for route handler tests:
    - vi.mock('@/db', ...) returning controlled db query chains
    - vi.mock('@/lib/auth-server', ...) controlling requireProjectRole return value
    - vi.mock('@/lib/seed-project', ...) spying on seedProjectFromRegistry

    Stub pattern for query tests:
    - vi.mock('../db', ...) or use actual DB with test transaction if DB available
    - Preferred: mock approach matching existing __tests__/portfolio/ pattern
  </implementation>
</feature>

<verification>
  <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run __tests__/lifecycle/ -x --reporter=verbose 2>&1 | tail -20</automated>
</verification>

<success_criteria>
All 4 test files exist. Running `npx vitest run __tests__/lifecycle/ -x` shows tests FAILING RED (not erroring on import — the tests run but assertions fail or functions-under-test don't exist yet). At least 2 failing tests per file.
</success_criteria>

<output>
After completion, create `.planning/phases/59-project-lifecycle-management/59-01-SUMMARY.md`
</output>
