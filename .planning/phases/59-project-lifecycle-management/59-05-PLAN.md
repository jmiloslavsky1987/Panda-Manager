---
phase: 59-project-lifecycle-management
plan: 05
type: execute
wave: 3
depends_on: [59-03, 59-04]
files_modified: []
autonomous: false
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, AUTH-01, PORTF-01, PORTF-02]
must_haves:
  truths:
    - "Admin can archive a project from the Settings sub-tab Danger Zone"
    - "Archived project appears in the sidebar's collapsed Archived section"
    - "Archived project workspace shows amber 'Archived — read only' banner"
    - "Admin-only Restore button in banner restores the project to active"
    - "Admin can permanently delete an archived project from the Danger Zone"
    - "User logs out via sidebar logout button and lands on /login"
    - "Non-admin cannot see the Danger Zone or Restore button"
    - "Portfolio dashboard active table does not show archived projects"
  artifacts: []
  key_links: []
---

<objective>
Human verification gate for all Phase 59 functionality. Confirms that the complete project lifecycle (archive, delete, restore) and sidebar logout work correctly in the browser with real sessions and role checks.

Purpose: Visual and interactive behaviors cannot be verified by automated tests — this gate confirms the full UX is correct before the phase is marked complete.
Output: User approval that all 7 success criteria are met.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-project-lifecycle-management/59-03-SUMMARY.md
@.planning/phases/59-project-lifecycle-management/59-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — run full test suite and confirm build is clean</name>
  <files></files>
  <action>
    Run the full Vitest suite and production build to confirm no regressions before handing off to human verification.
    Command: `cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run --reporter=verbose 2>&1 | tail -30`
    Then: `cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npm run build 2>&1 | tail -20`

    If any lifecycle tests are RED, report specifically which tests failed and what errors occurred — do not proceed to checkpoint.
    If build fails, report the error — do not proceed to checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run __tests__/lifecycle/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All lifecycle tests GREEN. Build passes with no errors. Proceed to human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of full lifecycle UX</name>
  <files></files>
  <action>
    Pause execution. Present the verification steps below to the user and wait for sign-off.
    Do not create any files or run any commands during this task — it is a human verification gate.
  </action>
  <verify>
    <automated>MISSING — this task requires human browser verification; no automated command available</automated>
  </verify>
  <done>User has typed "approved" confirming all 8 verification tests passed.</done>
  <what-built>
    Complete Project Lifecycle Management:
    - Archive action in Admin > Settings tab Danger Zone (admin only)
    - Archived banner with Restore button in archived project workspace (admin only)
    - Permanent Delete action in archived project's Admin > Settings (admin only)
    - Collapsed sidebar 'Archived (N)' section below active projects
    - Sidebar logout button (bottom of sidebar, all users)
    - Portfolio dashboard excludes archived projects from active view
  </what-built>
  <how-to-verify>
    Start dev server: `cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npm run next-only`

    **Test 1 — Sidebar logout (AUTH-01):**
    1. Log in and navigate to any page
    2. Confirm bottom of sidebar shows your name + logout icon button
    3. Click logout — confirm redirect to /login and session cleared

    **Test 2 — Archive a project (PROJ-01):**
    1. Log in as a project Admin
    2. Navigate to any active project workspace
    3. Click Admin tab > Settings sub-tab
    4. Confirm Danger Zone section is visible with "Archive project" button
    5. Click "Archive project" > confirm dialog > confirm
    6. Confirm redirect to dashboard
    7. Confirm the project no longer appears in active sidebar project list

    **Test 3 — Archived sidebar section (PROJ-03, PORTF-01):**
    1. After archiving in Test 2, confirm "Archived (1)" section appears at bottom of sidebar projects list
    2. Confirm section is collapsed by default
    3. Click to expand — confirm project name appears as a link

    **Test 4 — Archived workspace banner (PROJ-03):**
    1. Click the archived project link in the sidebar
    2. Confirm amber "Archived — read only" banner appears at top of workspace
    3. Confirm "Restore project" button is visible (as Admin)

    **Test 5 — Restore project (PROJ-04):**
    1. In the archived project workspace, click "Restore project" in the amber banner
    2. Confirm redirect to dashboard
    3. Confirm project reappears in active sidebar list
    4. Confirm Archived sidebar section no longer shows this project
    5. Navigate back to restored project — confirm no amber banner appears

    **Test 6 — Non-admin visibility (PROJ-01, PROJ-04):**
    1. Log in as a User role member (non-admin) for a project
    2. Navigate to Admin > Settings — confirm Danger Zone is NOT visible
    3. Navigate to an archived project — confirm NO Restore button in the amber banner

    **Test 7 — Delete permanently (PROJ-02):**
    1. Archive a test project (repeat Test 2)
    2. Navigate to the archived project via sidebar
    3. Go to Admin > Settings > Danger Zone
    4. Confirm "Archive project" button is gone (already archived)
    5. Confirm "Delete permanently" button is now active
    6. Click "Delete permanently" > confirm dialog > confirm
    7. Confirm redirect to dashboard
    8. Confirm project is gone from sidebar entirely

    **Test 8 — Portfolio exclusion (PORTF-01, PORTF-02):**
    1. Navigate to Portfolio dashboard (/)
    2. Confirm archived projects do not appear in the active projects table
    3. After permanent delete in Test 7, confirm that project is not visible anywhere in portfolio
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 tests pass, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all 8 verification tests.
</verification>

<success_criteria>
All 7 phase success criteria confirmed by human:
1. Admin can archive a project making it read-only and hidden from active list
2. Admin can permanently delete a project with pre-flight validation
3. User can view archived projects in dedicated archived sidebar section (read-only)
4. Admin can restore archived project back to active status
5. User can log out from sidebar
6. Portfolio dashboard separates archived projects
7. Portfolio dashboard excludes permanently deleted projects
</success_criteria>

<output>
After completion, create `.planning/phases/59-project-lifecycle-management/59-05-SUMMARY.md`
</output>
