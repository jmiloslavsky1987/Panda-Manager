---
phase: 41-ux-polish-consistency
plan: 04
type: execute
wave: 3
depends_on:
  - 41-02
  - 41-03
files_modified: []
autonomous: false
requirements:
  - UXPOL-01
  - UXPOL-02
  - UXPOL-03

must_haves:
  truths:
    - "Full automated test suite passes GREEN (zero failures)"
    - "User confirms empty states are visible and CTAs are correct across all tabs"
    - "User confirms overdue rows are visually red in Actions and Milestones tabs"
    - "User confirms skeletons appear on slow networks before data loads in Overview"
  artifacts:
    - path: "bigpanda-app/tests/ui/empty-states.test.tsx"
      provides: "All 9 UXPOL-01 empty state behaviors verified"
    - path: "bigpanda-app/tests/ui/overdue-highlighting.test.tsx"
      provides: "All 4 UXPOL-02 overdue behaviors verified"
    - path: "bigpanda-app/tests/ui/loading-skeletons.test.tsx"
      provides: "All 3 UXPOL-03 skeleton behaviors verified"
  key_links:
    - from: "automated test suite"
      to: "production components"
      via: "vitest + @testing-library/react"
      pattern: "npm test.*GREEN"
---

<objective>
Phase gate: automated test suite must be GREEN, then human verifies all three UXPOL requirements visually in the running application.

Purpose: Confirm all polish changes are correct before marking Phase 41 complete.
Output: Go/no-go decision. If approved, phase ships. If issues found, gaps are documented for correction.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-ux-polish-consistency/41-CONTEXT.md
@.planning/phases/41-ux-polish-consistency/41-02-SUMMARY.md
@.planning/phases/41-ux-polish-consistency/41-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated test suite gate</name>
  <files></files>
  <action>
    Run the full test suite. All tests from Phases 37–41 must pass. Report results.

    Run:
    ```bash
    cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npm test -- --run 2>&1 | tail -40
    ```

    Expected:
    - tests/ui/empty-states.test.tsx: 9/9 GREEN
    - tests/ui/overdue-highlighting.test.tsx: 4/4 GREEN
    - tests/ui/loading-skeletons.test.tsx: 3/3 GREEN
    - All prior phase tests still GREEN (no regressions)

    If any tests fail: fix the failures before proceeding to the checkpoint. Do NOT proceed to human verification with a RED test suite.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npm test -- --run 2>&1 | grep -E "Tests:|FAIL|failed" | tail -10</automated>
  </verify>
  <done>npm test shows zero failures. All Phase 41 test files GREEN. No regressions from prior phases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — UXPOL-01, UXPOL-02, UXPOL-03</name>
  <action>Human verifies all three UXPOL improvements are visually correct in the running application. 18-step verification checklist covering empty states, overdue rows, and loading skeletons.</action>
  <what-built>
    Three UX polish improvements across all project tabs:
    1. Empty states (UXPOL-01): 9 tabs now show actionable empty states with CTA buttons instead of blank areas or generic text
    2. Overdue highlighting (UXPOL-02): Actions and Milestones tables now show red border + red background on overdue rows (consistent with Tasks established in Phase 39)
    3. Loading skeletons (UXPOL-03): OverviewMetrics shows 3-card skeleton grid, HealthDashboard shows expanded skeleton, SkillsTabClient shows skeleton on initial load
  </what-built>
  <how-to-verify>
    Start the dev server: `cd bigpanda-app && npm run next-only`
    Open a project in the browser.

    **UXPOL-01: Empty States**
    For a project with no data (or use browser DevTools to inspect components):
    1. Go to Actions tab with no actions → confirm empty state shows "No actions yet" description + "Add Action" button (not "No actions found." text)
    2. Go to Risks tab with no risks → confirm "No risks logged" description + "Add Risk" button
    3. Go to Milestones tab with no milestones → confirm "No milestones yet" description + "Add Milestone" button
    4. Go to Decisions tab with no decisions → confirm "No decisions logged" description + "Log a Decision" button
    5. Go to Stakeholders tab with no stakeholders → confirm empty state description + "Add Stakeholder" button (or equivalent)
    6. Go to Artifacts tab with no artifacts → confirm "No artifacts yet" description + "Upload a document" button (clicking should navigate to Context Hub)
    7. Go to Engagement History tab with no history → confirm description text only ("Activity will appear here automatically...") — NO button
    8. Teams tab: if data exists, no empty state visible — that is correct; Teams is pre-populated

    **UXPOL-02: Overdue Highlighting**
    9. Create or find an action with a past due date and status of "open" or "in_progress" → open Actions tab → confirm that row has a red background and red border (visually distinct from normal rows)
    10. Create or find an action with status "completed" and past due date → confirm row is NOT red
    11. Open Milestones tab → find an overdue incomplete milestone → confirm row is red AND the existing "Overdue" badge is still present in the row
    12. Confirm overdue badge text is readable (dark red badge on light red background should have sufficient contrast)

    **UXPOL-03: Loading Skeletons**
    13. Throttle network in Chrome DevTools (Network tab → "Slow 3G")
    14. Navigate to Overview tab → confirm skeleton appears (3 animated grey cards) before data loads
    15. Confirm no blank white area or spinner — animated skeleton is visible
    16. Confirm no significant layout shift when data loads (page doesn't jump)
    17. Navigate to Skills tab → confirm skeleton appears briefly before skill list loads

    **Regression Check**
    18. Navigate to Plan tab → TaskBoard → find an overdue task → confirm it still shows red border/background (Phase 39 regression check)
  </how-to-verify>
  <verify>
    <automated>MISSING — visual verification required by human</automated>
  </verify>
  <done>User has completed 18-step checklist and typed "approved".</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (e.g., "History has a button when it shouldn't", "Overdue row on Actions not red")</resume-signal>
</task>

</tasks>

<verification>
Phase is complete when:
1. npm test shows zero failures
2. Human verifies all 18 checklist items above
3. User types "approved"
</verification>

<success_criteria>
- Full automated suite GREEN (all phases 37–41 tests pass)
- User has visually confirmed empty states, overdue rows, and skeletons match UXPOL-01/02/03 requirements
- No regression on Phase 39 TaskBoard overdue highlighting
- Phase 41 marked complete in STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/41-ux-polish-consistency/41-04-SUMMARY.md`
</output>
