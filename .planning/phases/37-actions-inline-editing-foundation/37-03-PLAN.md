---
phase: 37-actions-inline-editing-foundation
plan: "03"
type: execute
wave: 1
depends_on:
  - "37-01"
files_modified:
  - bigpanda-app/app/api/stakeholders/route.ts
  - bigpanda-app/app/api/actions/bulk-update/route.ts
  - bigpanda-app/app/api/actions/[id]/route.ts
  - bigpanda-app/app/api/risks/[id]/route.ts
  - bigpanda-app/app/api/milestones/[id]/route.ts
autonomous: true
requirements:
  - ACTN-05
  - IEDIT-01
  - IEDIT-02
  - IEDIT-03
  - IEDIT-04
  - FORM-02

must_haves:
  truths:
    - "GET /api/stakeholders?project_id=X returns array of { id, name, role } with 200"
    - "GET /api/stakeholders without project_id returns 400"
    - "POST /api/actions/bulk-update with valid action_ids + status returns 200 { ok: true, count: N }"
    - "PATCH /api/risks/[id] with status not in [open, mitigated, resolved, accepted] returns 400"
    - "PATCH /api/milestones/[id] with status not in [not_started, in_progress, completed, blocked] returns 400"
    - "All new/modified routes enforce requireSession() auth guard"
  artifacts:
    - path: "bigpanda-app/app/api/stakeholders/route.ts"
      provides: "GET handler for stakeholder name list by project"
      exports: ["GET", "POST"]
    - path: "bigpanda-app/app/api/actions/bulk-update/route.ts"
      provides: "Bulk status update endpoint for Actions table"
      exports: ["POST"]
    - path: "bigpanda-app/app/api/actions/[id]/route.ts"
      provides: "Existing PATCH with confirmed status enum acceptance"
    - path: "bigpanda-app/app/api/risks/[id]/route.ts"
      provides: "PATCH with enum-constrained risk status"
    - path: "bigpanda-app/app/api/milestones/[id]/route.ts"
      provides: "PATCH with enum-constrained milestone status"
  key_links:
    - from: "app/api/stakeholders/route.ts GET"
      to: "db.select stakeholders table"
      via: "drizzle query with eq(stakeholders.project_id, projectId)"
      pattern: "db.*select.*stakeholders.*project_id"
    - from: "app/api/actions/bulk-update/route.ts"
      to: "db.update actions"
      via: "inArray(actions.id, action_ids)"
      pattern: "inArray.*actions.*id"
    - from: "app/api/risks/[id]/route.ts PATCH"
      to: "zod RiskPatchSchema"
      via: "z.enum(['open','mitigated','resolved','accepted']) for status field"
      pattern: "z.enum.*open.*mitigated.*resolved.*accepted"
---

<objective>
Add the GET handler to the stakeholders route, create the actions bulk-update endpoint, and tighten Risk and Milestone PATCH schemas to enforce fixed status enums. These API changes make the Wave 0 test scaffolds turn GREEN.

Purpose: API contracts that back the inline-edit components. Plans 02 (components) and 03 (API) run in parallel (Wave 1) — no file overlap.
Output: 5 route files updated/created, Wave 0 tests go GREEN.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/PROJECT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/ROADMAP.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-CONTEXT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-RESEARCH.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-01-SUMMARY.md

<interfaces>
<!-- requireSession pattern — from bigpanda-app/app/api/actions/[id]/route.ts -->
import { requireSession } from '@/lib/auth'
const { session, redirectResponse } = await requireSession()
if (redirectResponse) return redirectResponse

<!-- Drizzle db patterns — from bigpanda-app/app/api/tasks-bulk/route.ts -->
import { db } from '@/db'
import { actions, stakeholders } from '@/db/schema'
import { eq, inArray } from 'drizzle-orm'

// Bulk update pattern:
await db.update(actions)
  .set({ status: patch.status })
  .where(inArray(actions.id, action_ids))

// Stakeholders select:
await db.select({ id: stakeholders.id, name: stakeholders.name, role: stakeholders.role })
  .from(stakeholders)
  .where(eq(stakeholders.project_id, projectId))

<!-- Zod enum validation pattern -->
import { z } from 'zod'
const RiskPatchSchema = z.object({
  status: z.enum(['open', 'mitigated', 'resolved', 'accepted']).optional(),
  owner: z.string().optional(),
  severity: z.string().optional(),
  // ... other fields
})
// On parse failure: return NextResponse.json({ error: 'Invalid status' }, { status: 400 })

<!-- NextRequest + NextResponse pattern -->
import { NextRequest, NextResponse } from 'next/server'
const projectId = parseInt(request.nextUrl.searchParams.get('project_id') ?? '', 10)
if (isNaN(projectId)) return NextResponse.json({ error: 'project_id required' }, { status: 400 })

<!-- Existing bulk update route structure — bigpanda-app/app/api/tasks-bulk/route.ts -->
// Mirror this exactly for actions/bulk-update/route.ts:
// - BulkUpdateSchema: { action_ids: z.array(z.number()).min(1), patch: z.object({ status: z.string().optional() }) }
// - No xlsx write (bulk is Actions-UI only per RESEARCH.md)
// - Returns: { ok: true, count: action_ids.length }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GET to stakeholders route + create actions bulk-update endpoint</name>
  <files>
    bigpanda-app/app/api/stakeholders/route.ts
    bigpanda-app/app/api/actions/bulk-update/route.ts
  </files>
  <behavior>
    stakeholders GET:
    - GET /api/stakeholders?project_id=1 → 200 [{ id, name, role }]
    - GET /api/stakeholders (no project_id) → 400 { error: 'project_id required' }
    - GET without session → 401/redirect

    actions bulk-update POST:
    - POST { action_ids: [1, 2, 3], patch: { status: 'completed' } } → 200 { ok: true, count: 3 }
    - POST { action_ids: [], patch: { status: 'open' } } → 400 (min(1) validation)
    - POST without session → 401/redirect
  </behavior>
  <action>
    For stakeholders/route.ts: READ the existing file first, then ADD the GET export before the existing POST export. Do not modify the existing POST handler. The GET handler follows Pattern 6 from RESEARCH.md exactly.

    For actions/bulk-update/route.ts: Create a NEW file at `bigpanda-app/app/api/actions/bulk-update/route.ts`. Mirror tasks-bulk/route.ts pattern exactly. Use BulkUpdateSchema = z.object({ action_ids: z.array(z.number()).min(1), patch: z.object({ status: z.string().optional() }) }). No xlsx write for bulk (per RESEARCH.md note on actions PATCH xlsx side-effect). Return { ok: true, count: action_ids.length }.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run tests/api/stakeholders-get.test.ts tests/api/actions-bulk.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <done>Both tests turn GREEN. GET /api/stakeholders?project_id=X passes. POST /api/actions/bulk-update passes. Zero TypeScript errors in modified files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce enum validation on Risks and Milestones PATCH schemas</name>
  <files>
    bigpanda-app/app/api/risks/[id]/route.ts
    bigpanda-app/app/api/milestones/[id]/route.ts
  </files>
  <behavior>
    risks PATCH:
    - PATCH with status 'open' → 200
    - PATCH with status 'mitigated' → 200
    - PATCH with status 'resolved' → 200
    - PATCH with status 'accepted' → 200
    - PATCH with status 'closed' → 400
    - PATCH with status 'Resolved' (wrong case) → 400

    milestones PATCH:
    - PATCH with status 'not_started' → 200
    - PATCH with status 'in_progress' → 200
    - PATCH with status 'completed' → 200
    - PATCH with status 'blocked' → 200
    - PATCH with status 'done' → 400
  </behavior>
  <action>
    READ both files first. For each, find the existing zod patchSchema. Change the `status` field from `z.string().optional()` to `z.enum(['open', 'mitigated', 'resolved', 'accepted']).optional()` for risks, and `z.enum(['not_started', 'in_progress', 'completed', 'blocked']).optional()` for milestones.

    Do NOT change the DB schema (risks.status and milestones.status remain text columns — no migration needed per RESEARCH.md). Do NOT change any other fields in the schema. Preserve all existing logic including the audit log writes and xlsx handling if any.

    Important: The actions PATCH route already accepts status strings. Check if it should also be updated to validate the 4 action statuses (open/in_progress/completed/cancelled). If actions patchSchema has status as z.string(), tighten it to z.enum(['open','in_progress','completed','cancelled']).optional() — this makes the ACTN-02 tests green.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run tests/api/risks-patch.test.ts tests/api/milestones-patch.test.ts tests/api/actions-patch.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 test files turn GREEN. Invalid enum values return 400. Valid enum values return 200. No other existing tests broken — run `npx vitest run tests/api/` to confirm.</done>
</task>

</tasks>

<verification>
Run `cd bigpanda-app && npx vitest run tests/api/` — all 5 Wave 0 test files should now be GREEN:
- tests/api/actions-patch.test.ts GREEN
- tests/api/actions-bulk.test.ts GREEN
- tests/api/risks-patch.test.ts GREEN
- tests/api/milestones-patch.test.ts GREEN
- tests/api/stakeholders-get.test.ts GREEN
</verification>

<success_criteria>
All 5 Wave 0 tests GREEN. New/modified API routes enforce requireSession(). Enum validation in place on risks and milestones. Bulk endpoint created. Stakeholders GET added. Zero regressions in existing tests.
</success_criteria>

<output>
After completion, create `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-03-SUMMARY.md`
</output>
