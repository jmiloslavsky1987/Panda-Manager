---
phase: 37-actions-inline-editing-foundation
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/tests/api/actions-patch.test.ts
  - bigpanda-app/tests/api/actions-bulk.test.ts
  - bigpanda-app/tests/api/risks-patch.test.ts
  - bigpanda-app/tests/api/milestones-patch.test.ts
  - bigpanda-app/tests/api/stakeholders-get.test.ts
autonomous: true
requirements:
  - ACTN-02
  - ACTN-05
  - IEDIT-01
  - IEDIT-02
  - FORM-02

must_haves:
  truths:
    - "Running npx vitest run tests/api/ fails (RED) before any implementation exists — confirming tests are valid contracts, not vacuous"
    - "Each test file imports the correct route handler and asserts on specific request/response shapes"
    - "All 5 test files exist with failing tests"
  artifacts:
    - path: "bigpanda-app/tests/api/actions-patch.test.ts"
      provides: "Contract for PATCH /api/actions/[id] accepting status, owner, due_date as inline edit fields"
    - path: "bigpanda-app/tests/api/actions-bulk.test.ts"
      provides: "Contract for POST /api/actions/bulk-update accepting action_ids + patch"
    - path: "bigpanda-app/tests/api/risks-patch.test.ts"
      provides: "Contract for PATCH /api/risks/[id] accepting enum status values only"
    - path: "bigpanda-app/tests/api/milestones-patch.test.ts"
      provides: "Contract for PATCH /api/milestones/[id] accepting enum status values only"
    - path: "bigpanda-app/tests/api/stakeholders-get.test.ts"
      provides: "Contract for GET /api/stakeholders?project_id=X returning name list"
  key_links:
    - from: "tests/api/actions-patch.test.ts"
      to: "app/api/actions/[id]/route.ts"
      via: "direct import of PATCH handler"
      pattern: "PATCH.*status.*owner.*due"
    - from: "tests/api/risks-patch.test.ts"
      to: "app/api/risks/[id]/route.ts"
      via: "direct import of PATCH handler — validates enum rejection"
      pattern: "z.enum.*open.*mitigated.*resolved.*accepted"
---

<objective>
Create Wave 0 test scaffolds for all API behaviors that require automated validation in Phase 37. These tests must FAIL before implementation (RED state), confirming they are real contracts. Plans 02–05 make them GREEN.

Purpose: Nyquist compliance — every code-producing task in this phase has an automated verify command referencing a test file that exists.
Output: 5 test files under `bigpanda-app/tests/api/`, all failing (RED) before Wave 1 work begins.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/PROJECT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/ROADMAP.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/STATE.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-CONTEXT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-RESEARCH.md

<interfaces>
<!-- Existing test pattern — from bigpanda-app/tests/api/projects-patch.test.ts -->
<!-- Use this as the structural template for all 5 new test files -->

Pattern (confirmed from codebase):
- Import handler directly: `import { PATCH } from '@/app/api/...'`
- Mock requireSession: `vi.mock('@/lib/auth', () => ({ requireSession: vi.fn() }))`
- Mock db: `vi.mock('@/db', () => ({ db: { update: vi.fn(), select: vi.fn(), ... } }))`
- Create NextRequest: `new NextRequest('http://localhost/api/...', { method: 'PATCH', body: JSON.stringify({...}) })`
- Assert response status and body

From bigpanda-app/app/api/actions/[id]/route.ts (existing PATCH):
- Accepts: status, owner, due_date, description fields
- Uses requireSession() security guard
- Validates with zod ActionPatchSchema
- Returns: Response.json({ ok: true })

From bigpanda-app/app/api/tasks-bulk/route.ts (bulk pattern):
- Body: { task_ids: number[], patch: { status?: string } }
- Uses requireSession() security guard
- Returns: Response.json({ ok: true, count: N })
- Mirror this exactly for actions bulk endpoint

Enum contracts to enforce in tests:
- Risk status: 'open' | 'mitigated' | 'resolved' | 'accepted'
- Milestone status: 'not_started' | 'in_progress' | 'completed' | 'blocked'
- A test MUST assert that sending status: 'closed' (invalid) returns 400

From bigpanda-app/vitest.config.ts:
- Uses path alias @/ → bigpanda-app/
- Test files: tests/**/*.test.ts pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Actions API test scaffolds (PATCH single + bulk)</name>
  <files>
    bigpanda-app/tests/api/actions-patch.test.ts
    bigpanda-app/tests/api/actions-bulk.test.ts
  </files>
  <behavior>
    actions-patch.test.ts:
    - Test: PATCH with valid status 'open' returns 200 { ok: true }
    - Test: PATCH with valid status 'in_progress' returns 200
    - Test: PATCH with valid status 'completed' returns 200
    - Test: PATCH with valid status 'cancelled' returns 200
    - Test: PATCH with owner string returns 200
    - Test: PATCH with due_date ISO string returns 200
    - Test: Unauthenticated PATCH returns 401/redirect (requireSession check)

    actions-bulk.test.ts:
    - Test: POST with { action_ids: [1, 2], patch: { status: 'completed' } } returns 200 { ok: true, count: 2 }
    - Test: POST with empty action_ids array returns 400
    - Test: Unauthenticated POST returns 401/redirect
  </behavior>
  <action>
    Create both test files using the codebase's existing test pattern (vi.mock for requireSession + db, direct handler import, NextRequest construction). Tests must import from paths that do not yet exist (bulk endpoint) — this ensures RED state. Mock the db at module level. Do NOT use watch mode flags.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run tests/api/actions-patch.test.ts tests/api/actions-bulk.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Both files exist. Tests run and fail (RED) because the bulk endpoint does not exist yet and the PATCH enum enforcement is not yet in place. Test output shows import or assertion failures, not syntax errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Risks, Milestones, and Stakeholders API test scaffolds</name>
  <files>
    bigpanda-app/tests/api/risks-patch.test.ts
    bigpanda-app/tests/api/milestones-patch.test.ts
    bigpanda-app/tests/api/stakeholders-get.test.ts
  </files>
  <behavior>
    risks-patch.test.ts:
    - Test: PATCH with status 'open' returns 200
    - Test: PATCH with status 'mitigated' returns 200
    - Test: PATCH with status 'resolved' returns 200
    - Test: PATCH with status 'accepted' returns 200
    - Test: PATCH with status 'closed' (invalid) returns 400 (enum rejection)
    - Test: PATCH with status 'Resolved' (wrong case) returns 400

    milestones-patch.test.ts:
    - Test: PATCH with status 'not_started' returns 200
    - Test: PATCH with status 'in_progress' returns 200
    - Test: PATCH with status 'completed' returns 200
    - Test: PATCH with status 'blocked' returns 200
    - Test: PATCH with status 'done' (invalid) returns 400

    stakeholders-get.test.ts:
    - Test: GET /api/stakeholders?project_id=1 returns 200 with array of { id, name, role }
    - Test: GET without project_id returns 400
    - Test: Unauthenticated GET returns 401/redirect
  </behavior>
  <action>
    Create all 3 test files using the same mock pattern. For risks-patch and milestones-patch, the critical contract is that invalid enum values return 400 — these tests will be RED until Plan 03 adds enum validation to those PATCH handlers. For stakeholders-get, the test will be RED until the GET handler is added in Plan 03.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run tests/api/risks-patch.test.ts tests/api/milestones-patch.test.ts tests/api/stakeholders-get.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 files exist. Enum-rejection tests fail (current handlers accept any string). Stakeholders-get test fails (no GET handler). No syntax errors — pure assertion failures.</done>
</task>

</tasks>

<verification>
All 5 test files exist in bigpanda-app/tests/api/. Running `cd bigpanda-app && npx vitest run tests/api/` shows all new tests FAILING (RED) — confirming they are real contracts that will be satisfied by Wave 1 implementation.
</verification>

<success_criteria>
5 test scaffold files created. All fail (RED) before Wave 1. No syntax errors (only import/assertion failures). Vitest can parse and execute them.
</success_criteria>

<output>
After completion, create `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/37-actions-inline-editing-foundation/37-01-SUMMARY.md`
</output>
