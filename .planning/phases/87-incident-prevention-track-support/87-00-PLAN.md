---
phase: 87-incident-prevention-track-support
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jmiloslavsky/Documents/Panda-Manager/tests/ui/onboarding-config.test.ts
  - /Users/jmiloslavsky/Documents/Panda-Manager/tests/api/projects.test.ts
  - /Users/jmiloslavsky/Documents/Panda-Manager/tests/api/project-settings.test.ts
  - /Users/jmiloslavsky/Documents/Panda-Manager/tests/arch/interactive-arch-graph.test.ts
  - /Users/jmiloslavsky/Documents/Panda-Manager/tests/schema/active-tracks-type.test.ts
  - /Users/jmiloslavsky/Documents/Panda-Manager/tests/extraction/ip-track-cues.test.ts
  - /Users/jmiloslavsky/Documents/Panda-Manager/tests/ui/seed-project.test.ts
autonomous: true
requirements: [IP-03, IP-04, IP-05, IP-06, IP-07, IP-08, IP-09, IP-10, IP-11, IP-12, IP-13, IP-14]

must_haves:
  truths:
    - "All IP-03..IP-14 test files exist and execute (RED is fine)"
    - "Running the quick suite produces a clean test summary with named failing tests for every automated requirement"
    - "No test references a yet-to-be-built artifact in a way that throws before the assertion (try/catch + ENOENT swallow per [85.2-00])"
  artifacts:
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/tests/ui/onboarding-config.test.ts"
      provides: "RED stubs for IP-04, IP-05"
      min_lines: 30
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/tests/api/projects.test.ts"
      provides: "RED stubs for IP-06, IP-07"
      min_lines: 60
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/tests/api/project-settings.test.ts"
      provides: "RED stubs for IP-08, IP-09"
      min_lines: 60
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/tests/arch/interactive-arch-graph.test.ts"
      provides: "RED stub for IP-12 (source-scan)"
      min_lines: 25
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/tests/schema/active-tracks-type.test.ts"
      provides: "RED stubs for IP-03, IP-13"
      min_lines: 30
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/tests/extraction/ip-track-cues.test.ts"
      provides: "RED stub for IP-14"
      min_lines: 25
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/tests/ui/seed-project.test.ts"
      provides: "Extended with IP-10, IP-11 cases"
      contains: "Incident Prevention"
  key_links:
    - from: "tests/schema/active-tracks-type.test.ts"
      to: "db/schema.ts"
      via: "fs.readFileSync source-scan"
      pattern: "incident_prevention.*boolean"
    - from: "tests/extraction/ip-track-cues.test.ts"
      to: "worker/jobs/document-extraction.ts"
      via: "fs.readFileSync source-scan"
      pattern: "Incident Prevention Track"
    - from: "tests/arch/interactive-arch-graph.test.ts"
      to: "components/arch/InteractiveArchGraph.tsx"
      via: "fs.readFileSync source-scan"
      pattern: "isIP|Incident Prevention Track"
---

<objective>
Create Wave 0 RED test scaffolds for every automated Phase 87 requirement (IP-03..IP-14) so that subsequent plans have failing tests to drive GREEN. Tests live in the Panda-Manager `tests/` directory (gitignored per [79-00] convention — created on disk only, no git commit needed for tests under tests/).

Purpose: Establishes Nyquist validation contract before any implementation. Every downstream plan's `<verify><automated>` command targets a test file created here.
Output: 6 new test files + 1 extended test file. Each runs and fails with a clear assertion (not crashes on import).
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/PROJECT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/ROADMAP.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/STATE.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-CONTEXT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-VALIDATION.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-RESEARCH.md

<interfaces>
<!-- Existing scaffolding to mirror. Source-scan pattern established in [79-00], [80-00], [82-00], [85.2-00], [86-00]. -->

Existing seed-project test (extend, do NOT replace):
- `/Users/jmiloslavsky/Documents/Panda-Manager/tests/ui/seed-project.test.ts`

Files this Wave 0 references (read-only — source-scans against these):
- `/Users/jmiloslavsky/Documents/Panda-Manager/db/schema.ts` (active_tracks type at line 114)
- `/Users/jmiloslavsky/Documents/Panda-Manager/lib/onboarding-config.ts`
- `/Users/jmiloslavsky/Documents/Panda-Manager/app/api/projects/route.ts`
- `/Users/jmiloslavsky/Documents/Panda-Manager/app/api/projects/[projectId]/settings/route.ts`
- `/Users/jmiloslavsky/Documents/Panda-Manager/components/arch/InteractiveArchGraph.tsx`
- `/Users/jmiloslavsky/Documents/Panda-Manager/worker/jobs/document-extraction.ts`

Test runner: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run <file>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source-scan stubs (schema, arch graph, extraction)</name>
  <files>
    /Users/jmiloslavsky/Documents/Panda-Manager/tests/schema/active-tracks-type.test.ts
    /Users/jmiloslavsky/Documents/Panda-Manager/tests/arch/interactive-arch-graph.test.ts
    /Users/jmiloslavsky/Documents/Panda-Manager/tests/extraction/ip-track-cues.test.ts
  </files>
  <action>
    Create three source-scan tests using the `fs.readFileSync` + try/catch returning `''` on ENOENT pattern established in [79-02] / [85.2-00].

    **tests/schema/active-tracks-type.test.ts** (IP-03, IP-13):
    - Test "IP-13: active_tracks type includes incident_prevention boolean": read `db/schema.ts`, assert the substring `incident_prevention: boolean` appears in the file.
    - Test "IP-13: active_tracks default JSON includes incident_prevention key": assert the file contains the literal string `incident_prevention` within a `.default(` call OR in the JSONB default expression.
    - Test "IP-03: migration 0052 preserves adr/biggy keys via additive `||` operator": read `db/migrations/0052_incident_prevention_track.sql`, assert the file contains both `active_tracks ||` AND `incident_prevention` AND `NOT (active_tracks ? 'incident_prevention')`. If the migration file does not exist yet, fail with "MISSING — Plan 01 must create migration 0052" (use expect.fail with message).
    - Wrap all reads in `try { ... } catch { return '' }` so vitest does not crash on missing files in RED state.

    **tests/arch/interactive-arch-graph.test.ts** (IP-12):
    - Test "IP-12: InteractiveArchGraph extends section-grouped path to Incident Prevention Track": read `components/arch/InteractiveArchGraph.tsx`, assert the file contains both `Incident Prevention Track` AND a section-grouped condition pattern (regex: `isIP|isSectionGrouped|isADR \|\| isIP`). RED until Plan 06 lands.
    - Test "IP-12: ConsoleNode supports Change Risk Console label": assert the file contains the literal `Change Risk Console`. RED until Plan 06.

    **tests/extraction/ip-track-cues.test.ts** (IP-14):
    - Test "IP-14: document-extraction.ts lists Incident Prevention Track as valid arch_node track": read `worker/jobs/document-extraction.ts`, assert the file contains the literal `Incident Prevention Track`. RED until Plan 02.
    - Test "IP-14: document-extraction.ts includes strong IP cue 'change ticket'": assert the file contains `change ticket` (case-insensitive). RED until Plan 02.
    - Test "IP-14: document-extraction.ts includes strong IP cue 'CAB'": assert the file contains `CAB` AND `change advisory board` (case-insensitive). RED until Plan 02.

    Each test file uses `import { describe, it, expect } from 'vitest'` and `import { readFileSync } from 'fs'`. No vitest mocks needed.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/schema/active-tracks-type.test.ts tests/arch/interactive-arch-graph.test.ts tests/extraction/ip-track-cues.test.ts 2>&1 | tail -30</automated>
    Expected: vitest exits non-zero (RED), shows named failing tests for IP-03, IP-12, IP-13, IP-14. No crashes from missing files.
  </verify>
  <done>Three test files exist, each runs to a clean RED state with assertion messages naming the requirement ID.</done>
</task>

<task type="auto">
  <name>Task 2: Route + config stubs (onboarding, project create, settings) + seed-project extension</name>
  <files>
    /Users/jmiloslavsky/Documents/Panda-Manager/tests/ui/onboarding-config.test.ts
    /Users/jmiloslavsky/Documents/Panda-Manager/tests/api/projects.test.ts
    /Users/jmiloslavsky/Documents/Panda-Manager/tests/api/project-settings.test.ts
    /Users/jmiloslavsky/Documents/Panda-Manager/tests/ui/seed-project.test.ts
  </files>
  <action>
    Create four test files (one is an extension of existing). Use direct-import where the module is small (config files) and source-scan pattern where the module pulls in DB/Next runtime.

    **tests/ui/onboarding-config.test.ts** (IP-04, IP-05):
    - `import { INCIDENT_PREVENTION_ONBOARDING_CONFIG, ALL_STANDARD_STEP_NAMES } from '@/lib/onboarding-config'`.
    - Test "IP-04: INCIDENT_PREVENTION_ONBOARDING_CONFIG exports 4 phases": assert `INCIDENT_PREVENTION_ONBOARDING_CONFIG.length === 4`. RED until Plan 02.
    - Test "IP-04: 4 phase names match spec": assert phase names are exactly `['Discovery & Kickoff', 'Platform Configuration', 'Validation', 'Go-Live']` in order.
    - Test "IP-04: total step count is 13": assert `INCIDENT_PREVENTION_ONBOARDING_CONFIG.flatMap(p => p.steps).length === 13`.
    - Test "IP-04: display_order values are [1, 3, 5, 6]": assert phase display_orders in order.
    - Test "IP-05: ALL_STANDARD_STEP_NAMES contains all 13 IP step names": for each of the 13 step names (Kickoff, Change Process Discovery, ITSM Audit, Single Sign-On, ITSM Integration, Data Source Connectors, Risk Categories & Weights, Write-Back Setup, Historical Backtest, Live Scoring UAT, Threshold Tuning, Go Live, CAB Enablement), assert `ALL_STANDARD_STEP_NAMES.includes(name)`.
    - Wrap the import in dynamic `try { await import(...) } catch { return [] }` so RED state shows assertion failures, not module-not-found crashes. Actually: use a synchronous fallback constant `const cfg = (() => { try { return require('@/lib/onboarding-config') } catch { return { INCIDENT_PREVENTION_ONBOARDING_CONFIG: [], ALL_STANDARD_STEP_NAMES: [] } } })()`.

    **tests/api/projects.test.ts** (IP-06, IP-07) — source-scan only to avoid Next route runtime:
    - Test "IP-06: POST /api/projects accepts active_tracks parameter": read `app/api/projects/route.ts`, assert the file contains both `active_tracks` AND a Zod schema or destructure pattern parsing it from request body. RED until Plan 04.
    - Test "IP-06: POST /api/projects gates IP WBS seeding on active_tracks.incident_prevention": assert the file contains a pattern like `active_tracks.incident_prevention` OR `tracks.incident_prevention` AND a conditional (if/ternary) gating WBS insertion. RED until Plan 04.
    - Test "IP-06: POST /api/projects gates ADR/Biggy WBS seeding on active_tracks": assert file contains both `tracks.adr` (or `active_tracks.adr`) AND `tracks.biggy` AND `if` near WBS insertion (regex tolerant). Catches the unconditional-seed regression.
    - Test "IP-07: Wizard / new-project UI requires at least one track checked": read the new-project form component (search for the form that POSTs to /api/projects — likely `components/NewProjectModal.tsx` or `components/NewProjectForm.tsx` or `app/page.tsx` modal). For the source-scan version: assert any client-side new-project form file contains both an "Incident Prevention" checkbox label AND a `disabled` attribute conditional on track-selection state. Implementer of this test must grep the codebase first to find the right file path; if not found, mark TODO with `expect.fail('MISSING: new-project form not located — Plan 04 must add Incident Prevention checkbox')`.

    **tests/api/project-settings.test.ts** (IP-08, IP-09) — source-scan:
    - Test "IP-08: settings PATCH route accepts incident_prevention key in Zod schema": read `app/api/projects/[projectId]/settings/route.ts`, assert the file contains `incident_prevention` AND `z.boolean()`. RED until Plan 05.
    - Test "IP-08: settings PATCH triggers retroactive seeding when track flips false→true": assert the file contains a function call like `seedIncidentPreventionForProject` OR `seedTrack` AND a diff check pattern (e.g. `!current` AND `newTracks.incident_prevention` OR `flippedOn`).
    - Test "IP-08: settings PATCH wraps update + seeding in db.transaction": assert the file contains `db.transaction` OR `await tx`.
    - Test "IP-09: retroactive seeding uses idempotency guards": read the dedicated seeding helper file (e.g. `lib/seed-incident-prevention.ts` or wherever Plan 05 places it). Source-scan for either `WHERE NOT EXISTS` OR `onConflictDoNothing` OR `NOT EXISTS`. RED until Plan 05 ships.

    **tests/ui/seed-project.test.ts** (IP-10, IP-11) — EXTEND existing file (already exists in lib/__tests__ — check both locations):
    - First, READ the existing test file to understand its structure (it lives at either `/Users/jmiloslavsky/Documents/Panda-Manager/tests/ui/seed-project.test.ts` or `/Users/jmiloslavsky/Documents/Panda-Manager/lib/__tests__/seed-project.test.ts` — check both, prefer extending the one that exists).
    - Append two new test cases (do NOT delete existing tests):
      - "IP-10: seedProjectFromRegistry inserts Team Gamma when incident_prevention is true": mock active_tracks with `{adr: false, biggy: false, incident_prevention: true}`; call seeder; assert the insert payload contains an object with `team_name: 'Team Gamma'` and `track: 'Incident Prevention'`.
      - "IP-11: seedProjectFromRegistry does NOT insert Team Gamma when incident_prevention is false": mock active_tracks with `{adr: true, biggy: true, incident_prevention: false}`; call seeder; assert no insert payload contains `team_name: 'Team Gamma'`.
    - Both tests RED until Plan 03.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/ui/onboarding-config.test.ts tests/api/projects.test.ts tests/api/project-settings.test.ts tests/ui/seed-project.test.ts 2>&1 | tail -40</automated>
    Expected: vitest runs all four files; named test failures for IP-04, IP-05, IP-06, IP-07, IP-08, IP-09, IP-10, IP-11. No import-resolution crashes (existing seed-project test continues to pass).
  </verify>
  <done>All four test files exist and run to clean RED state with named failing tests per requirement ID. Existing seed-project test cases continue to pass (no regression from the extension).</done>
</task>

</tasks>

<verification>
- All 7 test files exist on disk (1 extended, 6 new).
- `npx vitest run tests/schema/ tests/arch/ tests/extraction/ tests/ui/onboarding-config.test.ts tests/api/projects.test.ts tests/api/project-settings.test.ts tests/ui/seed-project.test.ts` runs cleanly (non-zero exit, no import crashes).
- Each failing test message names its IP-XX requirement.
</verification>

<success_criteria>
- 6 new test files + 1 extended test file written.
- Combined Wave 0 vitest run completes within ~60s; ≥12 failing test cases with named IP-IDs.
- No test crashes on missing module imports (source-scan pattern shields against ENOENT).
- Plan committable to git (test files under `tests/` are gitignored — only `lib/__tests__/` is tracked; this plan creates only `tests/` files so no git commit step is required for the test code itself per [79-00] / [86-00]).
</success_criteria>

<output>
After completion, create `.planning/phases/87-incident-prevention-track-support/87-00-SUMMARY.md` documenting:
- Each test file's path and what RED assertions it carries
- Confirmation that no tests crash on import (key Nyquist invariant)
- Mapping table: IP-XX → test file → test name(s)
- Note any tests that pre-pass on existing artifacts (acceptable if key gating assertion remains RED)
</output>
