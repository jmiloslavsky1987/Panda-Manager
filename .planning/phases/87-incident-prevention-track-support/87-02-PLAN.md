---
phase: 87-incident-prevention-track-support
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jmiloslavsky/Documents/Panda-Manager/lib/onboarding-config.ts
  - /Users/jmiloslavsky/Documents/Panda-Manager/worker/jobs/document-extraction.ts
  - /Users/jmiloslavsky/Documents/Panda-Manager/lib/discovery-scanner.ts
autonomous: true
requirements: [IP-04, IP-05, IP-14]

must_haves:
  truths:
    - "lib/onboarding-config.ts exports INCIDENT_PREVENTION_ONBOARDING_CONFIG with 4 phases, 13 steps"
    - "ALL_STANDARD_STEP_NAMES contains all 13 IP step names (deduplicated against ADR/Biggy)"
    - "Extraction Pass 2 prompt lists 'Incident Prevention Track' as a valid arch_node track name"
    - "Extraction Pass 0/3 prompt includes IP cues (change ticket, change request, CHG-, RFC, CAB, change advisory board, risk score, change risk, ServiceNow change, JSM change) for incident_prevention track routing"
    - "DISCOVERY_SYSTEM_TEMPLATE existing-structure builder includes Incident Prevention sections when track is active"
  artifacts:
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/lib/onboarding-config.ts"
      provides: "PhaseConfig export for IP track + deduplicated step name registry"
      contains: "INCIDENT_PREVENTION_ONBOARDING_CONFIG"
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/worker/jobs/document-extraction.ts"
      provides: "IP cues in extraction prompts (Pass 0 classification + Pass 2/3 arch_node track names + wbs_task inference)"
      contains: "Incident Prevention Track"
    - path: "/Users/jmiloslavsky/Documents/Panda-Manager/lib/discovery-scanner.ts"
      provides: "Existing-structure block includes IP when track active"
      contains: "Incident Prevention"
  key_links:
    - from: "lib/onboarding-config.ts"
      to: "ALL_STANDARD_STEP_NAMES export"
      via: "INCIDENT_PREVENTION_ONBOARDING_CONFIG.flatMap(p => p.steps) spread"
      pattern: "INCIDENT_PREVENTION_ONBOARDING_CONFIG.flatMap"
    - from: "worker/jobs/document-extraction.ts PASS_2_PROMPT"
      to: "Claude classification logic"
      via: "Inlined valid track names list"
      pattern: "Incident Prevention Track.*valid"
---

<objective>
Add Incident Prevention awareness to the configuration and extraction layers — both are independent of the DB migration and can land in parallel with Plan 01. Three files, three concerns: (a) onboarding step registry, (b) document-extraction Claude prompts, (c) discovery-scanner existing-structure block.

Purpose: Without this plan, retroactive seeding (Plan 05) cannot find IP phase/step names, and document extraction will reject IP arch_node entities with the current "ONLY valid values are ADR Track and AI Assistant Track" guard rule.
Output: ~50 LOC added to onboarding-config.ts; ~30 LOC added/edited across document-extraction.ts; ~10 LOC in discovery-scanner.ts to surface IP in existing-structure block.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/PROJECT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/STATE.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-CONTEXT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-RESEARCH.md
@/Users/jmiloslavsky/Documents/Panda-Manager/lib/onboarding-config.ts

<interfaces>
<!-- Existing exports in lib/onboarding-config.ts:13, 36, 60 (confirmed via grep): -->

```typescript
export const ADR_ONBOARDING_CONFIG: PhaseConfig[] = [ ... ]   // line 13
export const BIGGY_ONBOARDING_CONFIG: PhaseConfig[] = [ ... ] // line 36
export const ALL_STANDARD_STEP_NAMES = [                       // line 60
  ...ADR_ONBOARDING_CONFIG.flatMap(p => p.steps),
  ...BIGGY_ONBOARDING_CONFIG.flatMap(p => p.steps),
].filter((v, i, a) => a.indexOf(v) === i)
```

**PhaseConfig shape (assume from existing usage):**
```typescript
type PhaseConfig = { name: string; display_order: number; steps: string[] }
```

**Document-extraction prompt constants** (per RESEARCH.md):
- PASS_0_PROMPT (Pass 0 classification — predicts entity types and track context)
- PASS_2_PROMPT (entity extraction — enforces arch_node track name allowlist)
- PASS_3_PROMPT (disambiguation pass — re-asserts arch_node track allowlist and wbs_task track inference)

The literal string `"AI Assistant Track"` appears in both PASS_2 and PASS_3 — search and update ALL occurrences (per Pitfall 6 in RESEARCH.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend lib/onboarding-config.ts with INCIDENT_PREVENTION_ONBOARDING_CONFIG</name>
  <files>/Users/jmiloslavsky/Documents/Panda-Manager/lib/onboarding-config.ts</files>
  <action>
    Read the existing `lib/onboarding-config.ts` to confirm the PhaseConfig shape and the exact ADR/BIGGY exports.

    Append a third config export immediately after `BIGGY_ONBOARDING_CONFIG`:

    ```typescript
    export const INCIDENT_PREVENTION_ONBOARDING_CONFIG: PhaseConfig[] = [
      {
        name: 'Discovery & Kickoff',
        display_order: 1,
        steps: ['Kickoff', 'Change Process Discovery', 'ITSM Audit', 'Single Sign-On'],
      },
      {
        name: 'Platform Configuration',
        display_order: 3,
        steps: ['ITSM Integration', 'Data Source Connectors', 'Risk Categories & Weights', 'Write-Back Setup'],
      },
      {
        name: 'Validation',
        display_order: 5,
        steps: ['Historical Backtest', 'Live Scoring UAT', 'Threshold Tuning'],
      },
      {
        name: 'Go-Live',
        display_order: 6,
        steps: ['Go Live', 'CAB Enablement'],
      },
    ]
    ```

    Then extend the `ALL_STANDARD_STEP_NAMES` export to include the new steps. The existing definition (line ~60):
    ```typescript
    export const ALL_STANDARD_STEP_NAMES = [
      ...ADR_ONBOARDING_CONFIG.flatMap(p => p.steps),
      ...BIGGY_ONBOARDING_CONFIG.flatMap(p => p.steps),
    ].filter((v, i, a) => a.indexOf(v) === i)
    ```

    Change to:
    ```typescript
    export const ALL_STANDARD_STEP_NAMES = [
      ...ADR_ONBOARDING_CONFIG.flatMap(p => p.steps),
      ...BIGGY_ONBOARDING_CONFIG.flatMap(p => p.steps),
      ...INCIDENT_PREVENTION_ONBOARDING_CONFIG.flatMap(p => p.steps),
    ].filter((v, i, a) => a.indexOf(v) === i)
    ```

    **Important detail:** Some IP step names overlap with ADR/Biggy ('Kickoff', 'Single Sign-On', 'Go Live' may already appear). The `.filter` already deduplicates — this is intentional. IP-05 only requires that all 13 IP names ARE PRESENT in the array, not that all 13 are unique entries.

    **Why phase display_orders are [1, 3, 5, 6] (not [1, 2, 3, 4]):** This mirrors the established cadence in ADR/Biggy configs — gaps allow inserted phases between Discovery/Config/Validation/Go-Live in the future without renumbering. Confirm by reading existing ADR_ONBOARDING_CONFIG display_orders before writing.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && grep -q 'INCIDENT_PREVENTION_ONBOARDING_CONFIG' lib/onboarding-config.ts && grep -q 'Change Process Discovery' lib/onboarding-config.ts && grep -q 'CAB Enablement' lib/onboarding-config.ts && npx vitest run tests/ui/onboarding-config.test.ts 2>&1 | tail -10</automated>
    Expected: file contains the new export; IP-04 + IP-05 GREEN.
  </verify>
  <done>INCIDENT_PREVENTION_ONBOARDING_CONFIG exported with exactly 4 phases / 13 steps; ALL_STANDARD_STEP_NAMES includes the new step names; IP-04 and IP-05 vitest cases GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add IP cues to worker/jobs/document-extraction.ts prompts</name>
  <files>/Users/jmiloslavsky/Documents/Panda-Manager/worker/jobs/document-extraction.ts</files>
  <action>
    Read the full `worker/jobs/document-extraction.ts` file and find every occurrence of the literal string `"AI Assistant Track"` (per Pitfall 6 in RESEARCH.md, it appears in BOTH PASS_2 and PASS_3 disambiguation sections).

    **Edit 1 — arch_node valid-track-names rule (in PASS_2_PROMPT):**
    Find the line resembling:
    > `arch_node track names: ONLY valid values are "ADR Track" and "AI Assistant Track". If the document mentions a different track name, do NOT extract an arch_node entity.`

    Change to:
    > `arch_node track names: ONLY valid values are "ADR Track", "AI Assistant Track", and "Incident Prevention Track". If the document mentions a different track name, do NOT extract an arch_node entity.`

    **Edit 2 — same rule in PASS_3_PROMPT (disambiguation):**
    Same find/replace as Edit 1 — search and update the second occurrence.

    **Edit 3 — wbs_task track inference (in PASS_3_PROMPT):**
    Find the line resembling:
    > `INFER from document context: "ADR" if BigPanda/enterprise deployment, "Biggy" if startup/SMB. Default to "ADR" if unclear.`

    Change to:
    > `INFER from document context: "ADR" if BigPanda/enterprise deployment, "Biggy" if startup/SMB, "Incident Prevention" if document contains change-ticket / change-request / CHG- / RFC / CAB / change advisory board / risk score / change risk / ServiceNow change / JSM change / blast radius / freeze window / 5-category / weighted risk references. Default to "ADR" if unclear.`

    **Edit 4 — Pass 0 classification cues (in PASS_0_PROMPT):**
    Add a new bullet under whatever existing track-classification guidance exists. Example pattern (adapt to actual prompt structure):
    > `If the document mentions change tickets (CHG-, RFC), ServiceNow / JSM changes, change risk prediction, CAB / change advisory board, blast radius, CI criticality, freeze windows, or a 5-category weighted risk engine, classify as "Incident Prevention" track. Strong cues: "change ticket", "change request", "ServiceNow change", "JSM change", "risk score", "change risk", "CAB", "change advisory board". Supporting cues: "blast radius", "configuration item / CI", "freeze window", "blackout window", "5-category", "weighted risk", "risk engine", "approval workflow", "change approval".`

    Place this cue block adjacent to any existing ADR/Biggy classification guidance so the prompt's classification section reads cohesively.

    **Sanity check after edits:** Run `grep -c 'AI Assistant Track' worker/jobs/document-extraction.ts` — note the count before/after; you want every occurrence to ALSO include "Incident Prevention Track" within ~50 chars. Run `grep -c 'Incident Prevention Track' worker/jobs/document-extraction.ts` — expect ≥2.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && grep -q 'Incident Prevention Track' worker/jobs/document-extraction.ts && grep -ci 'change ticket' worker/jobs/document-extraction.ts | grep -v '^0$' && grep -ci 'change advisory board' worker/jobs/document-extraction.ts | grep -v '^0$' && grep -ci 'risk score' worker/jobs/document-extraction.ts | grep -v '^0$' && npx vitest run tests/extraction/ip-track-cues.test.ts 2>&1 | tail -10</automated>
    Expected: all greps non-zero; IP-14 vitest GREEN.
  </verify>
  <done>Pass 0 prompt includes IP cue list; Pass 2 and Pass 3 prompts include "Incident Prevention Track" in arch_node valid-track-names; Pass 3 prompt includes IP inference rule for wbs_task; IP-14 GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Update lib/discovery-scanner.ts DISCOVERY_SYSTEM_TEMPLATE existing-structure block</name>
  <files>/Users/jmiloslavsky/Documents/Panda-Manager/lib/discovery-scanner.ts</files>
  <action>
    Read `lib/discovery-scanner.ts` and find the `runDiscoveryScan()` function (or whichever function builds the `existingStructureBlock` value substituted into DISCOVERY_SYSTEM_TEMPLATE — per [84.1-01] this is a runtime substitution).

    The existing-structure builder currently iterates archTracks and listing each track + its sections. There is no hardcoded "ADR" / "Biggy" string — it's generic. Verify this by reading the builder.

    **Two possible cases:**

    **Case A (preferred — builder is already generic):** Then no code change is needed in this file at all. The Plan 01 migration seeds the IP arch_track and the generic builder will naturally surface it when scanning an IP-active project. In this case, this task becomes a "verify and document" task: confirm via grep that the builder does NOT hardcode track names; document the finding in 87-02-SUMMARY.md.

    **Case B (builder hardcodes track lists):** If grep finds `'ADR'` / `'Biggy'` / `'AI Assistant'` literals in the builder function, add `'Incident Prevention'` to the list alongside.

    After confirming which case applies, also add an IP-aware comment in the DISCOVERY_SYSTEM_TEMPLATE itself (the template literal) near the `{existingStructureBlock}` placeholder:
    > `Three product tracks exist in this codebase: ADR, Biggy (AI Assistant), and Incident Prevention. Match entities to the appropriate track based on document content.`

    This ensures Claude is aware of the third track at the prompt level even before the existing-structure block expands.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && grep -q 'Incident Prevention' lib/discovery-scanner.ts</automated>
    Expected: literal "Incident Prevention" appears at least once (either as a track listing addition in Case B, or as part of the new comment block in either case).
  </verify>
  <done>discovery-scanner.ts surfaces IP awareness to Claude at the discovery-scan prompt level. If the builder was already generic, that finding is documented in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/ui/onboarding-config.test.ts tests/extraction/ip-track-cues.test.ts` → all GREEN (IP-04, IP-05, IP-14).
- No regression in existing extraction tests (`tests/extraction/` other files).
- Document-extraction.ts compiles (no syntax errors from edits inside template literals).
</verification>

<success_criteria>
- IP-04, IP-05, IP-14 GREEN.
- Existing onboarding-config, document-extraction, and discovery-scanner consumer tests continue to pass.
- Git commit referencing Phase 87.
</success_criteria>

<output>
After completion, create `.planning/phases/87-incident-prevention-track-support/87-02-SUMMARY.md` documenting:
- The 13 IP step names and which ones overlap with ADR/Biggy (for deduplication context)
- The exact count of `"Incident Prevention Track"` insertions in document-extraction.ts
- Which Case (A or B) applied to discovery-scanner.ts and what was added
- Confirmation that IP-04, IP-05, IP-14 are GREEN
</output>
