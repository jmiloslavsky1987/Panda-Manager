---
phase: 87-incident-prevention-track-support
plan: 08
type: execute
wave: 3
depends_on: [87-00, 87-01, 87-02, 87-03, 87-04, 87-05, 87-06, 87-07]
files_modified:
  - /Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-VERIFICATION.md
  - /Users/jmiloslavsky/Documents/Project Assistant Code/.planning/STATE.md
  - /Users/jmiloslavsky/Documents/Project Assistant Code/.planning/ROADMAP.md
autonomous: false
requirements: [IP-01, IP-02, IP-15, IP-16]

must_haves:
  truths:
    - "Migration 0052 applies cleanly against a Docker DB with pre-existing ADR/Biggy projects (IP-01)"
    - "Re-running migration 0052 produces zero duplicate arch_tracks / arch_nodes rows (IP-02)"
    - "Architecture diagram renders the Incident Prevention Track as a third column with 3 sections + Change Risk Console centerpiece + 13 sub-capabilities (IP-15)"
    - "OnboardingDashboard renders 3 columns in ADR -> Biggy -> Incident Prevention order for an IP-active project (IP-16)"
    - "An existing ADR/Biggy project that flips active_tracks.incident_prevention false->true via Settings is fully seeded (WBS L1+L2, arch sections, console, sub-caps, onboarding phases/steps, Team Gamma)"
    - "Existing ADR/Biggy projects retain their adr/biggy active_tracks values verbatim after migration 0052"
    - "New project created via the wizard with only Incident Prevention checked has NO ADR / Biggy data seeded"
  artifacts:
    - path: "/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-VERIFICATION.md"
      provides: "Verification record of all 16 IP-XX requirements"
      contains: "PASSED"
  key_links:
    - from: "user browser session"
      to: "Phase 87 deliverables"
      via: "Manual browser verification + Docker DB sanity SQL"
      pattern: "all-features-working"
---

<objective>
Final human-verification checkpoint for Phase 87. Plans 00-07 ship code; this plan verifies end-to-end against a running Docker stack plus three live test projects covering: (a) the migration apply path (IP-01, IP-02), (b) the visual rendering surfaces (IP-15, IP-16), and (c) the three lifecycle paths (new-project IP-only, existing-project Settings flip, two-track regression).

Purpose: Phase 87 is high-impact (third product track across all customer-facing surfaces) — automated tests cover code-level invariants but the user-facing pixel-level layout and the Docker migration round-trip require human eyes. This plan packages those into a single structured verification.

Output: A `87-VERIFICATION.md` document with PASS/FAIL per requirement and any inline UAT fixes (following [86-05] precedent for inline fixes under 50 LoC).
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/STATE.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-CONTEXT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-VALIDATION.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-01-SUMMARY.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-04-SUMMARY.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-05-SUMMARY.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-06-SUMMARY.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-07-SUMMARY.md

<interfaces>
<!-- This plan does NOT modify any code. It runs the Docker stack, exercises the feature end-to-end via the browser, and records observations. -->

**Run before checkpoint:**
1. `cd /Users/jmiloslavsky/Documents/Panda-Manager && ~/bin/panda-rebuild.sh` to rebuild Docker with all Plan 01-07 changes applied
2. Full vitest suite: `cd Panda-Manager && npx vitest run` — must be GREEN before browser verification

**Docker DB sanity SQL** (run via `docker compose exec postgres psql -U postgres -d bigpanda_app -c "..."`):
- `SELECT id, active_tracks FROM projects LIMIT 5;`  -> verify backfill (existing rows have all three keys, adr/biggy preserved)
- `SELECT count(*) FROM arch_tracks WHERE name = 'Incident Prevention Track';`  -> matches project count
- `SELECT count(*) FROM arch_nodes WHERE node_type = 'sub-capability' AND name IN ('ITSM Connectors','Blast Radius Risk','CAB Notifications');`  -> >= 3 x project count (one per project)
- Re-apply migration 0052 (psql -f db/migrations/0052_incident_prevention_track.sql) and re-run the count queries — values must be identical (idempotency)

**Test project matrix:**
- **Project A (regression):** existing ADR + Biggy project. Confirm 2-column OnboardingDashboard, ADR + Biggy arch tracks visible, no IP UI surfaces. After migration, active_tracks should be `{adr:true, biggy:true, incident_prevention:false}`.
- **Project B (IP-only new):** create via wizard with only Incident Prevention checked. Confirm IP-only WBS, IP-only arch, single-column OnboardingDashboard, Team Gamma, no ADR/Biggy data.
- **Project C (mid-engagement flip):** start with `{adr:true, biggy:false, incident_prevention:false}`; in Admin > Settings, toggle Incident Prevention ON; Save. Confirm retroactive seeding, render-layer toggle behavior, idempotency on double-toggle.

**Visual checkpoints (IP-15, IP-16):**
- Section colors per Plan 06's choices (recommended violet/red/green)
- Change Risk Console centerpiece visually placed between Data Ingestion and Risk Engine
- OnboardingDashboard column ordering: ADR -> Biggy -> Incident Prevention
- WorkspaceKpiStrip aggregate totals include IP step counts when IP active
- HealthDashboard third badge appears with appropriate health color

**Known auto-skipped:**
- Migration 0052 against the Docker container's postgres is the IP-01/IP-02 verification — CANNOT be unit-tested.
- If run-migrations.ts trips on the DO block (per the bug noted in [83-04]), apply via direct psql per the [85.2-01] precedent. Acceptable and matches established phase patterns.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Docker apply + DB sanity SQL (IP-01, IP-02)</name>
  <files>/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-VERIFICATION.md</files>
  <action>
**What was built (for the human verifier):**
- Migration 0052 from Plan 01 (schema default change + JSONB backfill + arch seed for existing projects)
- All downstream code from Plans 02-07 (config, extraction, seeding, routes, UI)
- Docker stack has been rebuilt with all Phase 87 changes applied

**How to verify (executor presents this checklist; waits for resume signal):**

1. Rebuild Docker:
   ```bash
   cd /Users/jmiloslavsky/Documents/Panda-Manager && ~/bin/panda-rebuild.sh
   ```
   Wait for build + migrations to complete (~3-5 min).

2. Confirm migration applied (active_tracks JSONB shape):
   ```bash
   docker compose -f install/docker-compose.local.yml exec postgres \
     psql -U postgres -d bigpanda_app \
     -c "SELECT id, active_tracks FROM projects ORDER BY id LIMIT 10;"
   ```
   Expected: every row's `active_tracks` JSONB contains all three keys (`adr`, `biggy`, `incident_prevention`). Pre-existing projects have their `adr` and `biggy` values preserved verbatim (NOT both flipped to false — only NEW projects start at all-false). `incident_prevention` is `false` for all backfilled rows.

3. Confirm arch_tracks seed (1 row per existing project):
   ```bash
   docker compose -f install/docker-compose.local.yml exec postgres \
     psql -U postgres -d bigpanda_app \
     -c "SELECT count(*) FROM arch_tracks WHERE name = 'Incident Prevention Track';"
   ```
   Expected: count matches the number of existing projects (1:1).

4. Confirm arch_nodes seeded (17 per project = 3 sections + 1 console + 13 sub-caps):
   ```bash
   docker compose -f install/docker-compose.local.yml exec postgres \
     psql -U postgres -d bigpanda_app \
     -c "SELECT count(*) FROM arch_nodes a JOIN arch_tracks t ON a.track_id=t.id WHERE t.name='Incident Prevention Track';"
   ```
   Expected: 17 × project_count.

5. **Idempotency check (IP-02):** re-apply the migration:
   ```bash
   docker compose -f install/docker-compose.local.yml exec postgres \
     psql -U postgres -d bigpanda_app \
     -f /migrations/0052_incident_prevention_track.sql
   ```
   Re-run steps 3 and 4 — values MUST be identical (no duplicates).

6. **Backfill protection check:** pick one existing project, manually flip its IP key to true:
   ```bash
   docker compose -f install/docker-compose.local.yml exec postgres \
     psql -U postgres -d bigpanda_app \
     -c "UPDATE projects SET active_tracks = active_tracks || '{\"incident_prevention\":true}'::jsonb WHERE id=<some-id>;"
   ```
   Re-apply migration 0052. Confirm the project's `incident_prevention` is still `true` (the `NOT (active_tracks ? 'incident_prevention')` guard protects it). Revert the manual change after the test.

If run-migrations.ts fails on the DO block (known bug per [83-04]), apply via direct psql per the [85.2-01] precedent and document the workaround in 87-VERIFICATION.md.

Verifier task: confirm each step's expected output and record findings.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — IP-01 and IP-02 require Docker postgres SQL output review by user."</automated>
  </verify>
  <done>Human verifier approves migration apply + idempotency check. IP-01 and IP-02 marked PASSED in 87-VERIFICATION.md. Any inline fixes (per [86-05]) documented.</done>
  <resume-signal>Type "approved" or describe issues (with SQL output snippets)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser end-to-end verification — three test projects (IP-15, IP-16)</name>
  <files>/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-VERIFICATION.md</files>
  <action>
**What was built (for the human verifier):**
- Full Phase 87 visual surface area:
  - Architecture diagram with section-grouped IP track + Change Risk Console (Plan 06)
  - OnboardingDashboard with conditional third column (Plan 07)
  - Settings UI with third checkbox + retroactive seeding (Plan 05)
  - Project-create wizard with three checkboxes + ≥1 validation (Plan 04)
  - Supporting dashboards (WorkspaceKpiStrip / OverviewMetrics / HealthDashboard — Plan 07)

**How to verify (executor presents this checklist; waits for resume signal):**

Run the app: `cd /Users/jmiloslavsky/Documents/Panda-Manager && open http://localhost:3000`. Log in with admin@localhost.dev / admin123.

**Project A (regression check — 2-track project):**
Open one of the pre-existing projects. Confirm:
- [ ] OnboardingDashboard shows 2 columns (ADR + Biggy), no IP column
- [ ] Architecture tab shows ADR Track + AI Assistant Track only
- [ ] No "Incident Prevention" string appears in the workspace UI
- [ ] WorkspaceKpiStrip totals match pre-Phase-87 values (sanity — IP contributes 0 when off)
- [ ] HealthDashboard shows 2 health badges (ADR + Biggy)
Active tracks of Project A should be `{adr:true, biggy:true, incident_prevention:false}`.

**Project B (new IP-only project — wizard creation):**
Click "+ New Project" (or whatever creates a new project). Confirm:
- [ ] Three track checkboxes visible (ADR, Biggy, Incident Prevention) in that order, all unchecked by default
- [ ] Submit button disabled when all three are unchecked
- [ ] Submit button enabled when at least one is checked
- [ ] Check ONLY Incident Prevention, fill in project metadata, click Submit
- [ ] Project is created and routes to its workspace
- [ ] WBS tab shows 10 IP L1 phases ordered by display_order, each with ~3-4 L2 children
- [ ] No ADR or Biggy WBS rows present
- [ ] Architecture tab shows ONLY "Incident Prevention Track" column with 3 colored section bands + Change Risk Console centerpiece + 13 sub-cap cards
- [ ] Sub-cap names match the spec:
  - Data Ingestion: ITSM Connectors / CMDB Connectors / Monitoring Connectors / Deployment History Connectors
  - Risk Engine: Change History Risk / Blast Radius Risk / CI Criticality Risk / Timing & Freeze Window Risk / Team Performance Risk
  - Decision & Write-Back: Risk Threshold Rules / ITSM Write-Back (ServiceNow / JSM) / CAB Notifications / Reporting & Dashboards
- [ ] OnboardingDashboard shows 1 column (Incident Prevention) with 4 phases (Discovery & Kickoff, Platform Configuration, Validation, Go-Live) and 13 steps total
- [ ] Teams tab shows Team Gamma (track: Incident Prevention)
- [ ] HealthDashboard shows 1 health badge (Incident Prevention)

**Project C (mid-engagement flip):**
Either reuse Project A or create a fresh ADR-only project. Then:
- [ ] Navigate to Admin > Project Settings
- [ ] Confirm "Incident Prevention" checkbox is present (unchecked, third in order)
- [ ] Check the box, click Save
- [ ] Watch for success toast / inline feedback
- [ ] Hard refresh the page
- [ ] WBS tab now shows ADR rows AND IP rows (10 IP L1 phases + L2 children appear)
- [ ] Architecture tab shows ADR Track + Incident Prevention Track (no Biggy)
- [ ] OnboardingDashboard shows 2 columns: ADR (left) + IP (right) — no Biggy
- [ ] Team Gamma appears in Teams tab
- [ ] Toggle IP OFF, Save, refresh — IP column / arch track / WBS rows DISAPPEAR from the UI (render-layer hide per ADMIN-04)
- [ ] In psql, verify the DB still contains the IP arch_nodes / wbs_items (data is hidden by render, not deleted):
  ```sql
  SELECT count(*) FROM arch_nodes a JOIN arch_tracks t ON a.track_id=t.id WHERE t.project_id=<C-id> AND t.name='Incident Prevention Track';
  ```
  Count should be unchanged (17).
- [ ] Toggle IP back ON, Save, refresh — IP column reappears; verify no duplicate arch_tracks:
  ```sql
  SELECT count(*) FROM arch_tracks WHERE project_id=<C-id> AND name='Incident Prevention Track';
  ```
  Returns exactly 1.

**Extraction sanity:**
- [ ] Upload a sample document to Project B (IP-only) that mentions "CHG-1234", "ServiceNow change", "CAB approval", "blast radius". Run extraction. Confirm extracted entities classify with IP-track context (wbs_task track='Incident Prevention', arch_node track_id maps to Incident Prevention Track). If extraction misclassifies, the cues in Plan 02 may need tuning — escalate inline (under 50 LoC per [86-05]) or as a follow-up gap-closure plan.

**Visual quality checks:**
- [ ] Section colors render distinctly (recommended: violet for Data Ingestion, red for Risk Engine, green for Decision & Write-Back — or whichever Plan 06 chose)
- [ ] Change Risk Console centerpiece is visually placed between Data Ingestion (display_order 10) and Risk Engine (display_order 20)
- [ ] Track ordering in every UI: ADR -> Biggy -> Incident Prevention

Verifier task: walk through all three projects in browser; record screenshots; confirm each checkbox.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — IP-15 and IP-16 visual rendering require browser review by user."</automated>
  </verify>
  <done>Human verifier approves all three test-project flows. IP-15 and IP-16 marked PASSED in 87-VERIFICATION.md. Any inline UAT fixes under 50 LoC applied + documented (per [86-05] precedent).</done>
  <resume-signal>Type "approved" or describe issues (with screenshots / unexpected output)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 87-VERIFICATION.md + close phase</name>
  <files>
    /Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/87-incident-prevention-track-support/87-VERIFICATION.md
    /Users/jmiloslavsky/Documents/Project Assistant Code/.planning/STATE.md
    /Users/jmiloslavsky/Documents/Project Assistant Code/.planning/ROADMAP.md
  </files>
  <action>
    After Task 1 and Task 2 both PASS (or after any inline UAT fixes under 50 LoC have been applied per [86-05] inline-fix pattern), create `87-VERIFICATION.md` with the following template:

    ```markdown
    ---
    phase: 87
    status: PASSED | PASSED_WITH_DEFERRED_GAPS | FAILED
    verified: YYYY-MM-DD
    ---

    # Phase 87 — Incident Prevention Track Support — VERIFICATION

    ## Status: PASSED

    ## Requirement Verification

    | Req | Description | Status | Evidence |
    |-----|-------------|--------|----------|
    | IP-01 | Migration 0052 applies cleanly | PASSED | docker psql active_tracks JSONB shape verified |
    | IP-02 | Migration 0052 is idempotent | PASSED | re-applied migration; arch_tracks count unchanged |
    | IP-03 | Existing adr/biggy preserved post-migration | PASSED | tests/schema/active-tracks-type.test.ts GREEN + SQL |
    | IP-04 | INCIDENT_PREVENTION_ONBOARDING_CONFIG correct shape | PASSED | tests/ui/onboarding-config.test.ts GREEN |
    | IP-05 | ALL_STANDARD_STEP_NAMES includes 13 IP step names | PASSED | tests/ui/onboarding-config.test.ts GREEN |
    | IP-06 | POST /api/projects gates seeding on active_tracks | PASSED | tests/api/projects.test.ts GREEN + Project B visual |
    | IP-07 | Wizard submit disabled when no track checked | PASSED | tests/api/projects.test.ts GREEN + Project B visual |
    | IP-08 | Settings PATCH triggers retroactive seeding | PASSED | tests/api/project-settings.test.ts GREEN + Project C |
    | IP-09 | Retroactive seeding is idempotent | PASSED | double-toggle test GREEN + arch_tracks count check |
    | IP-10 | Team Gamma inserted when IP active | PASSED | tests/ui/seed-project.test.ts GREEN |
    | IP-11 | Team Gamma NOT inserted when IP inactive | PASSED | tests/ui/seed-project.test.ts GREEN |
    | IP-12 | InteractiveArchGraph renders section-grouped IP | PASSED | tests/arch/interactive-arch-graph.test.ts GREEN + visual |
    | IP-13 | active_tracks type includes incident_prevention | PASSED | tests/schema/active-tracks-type.test.ts GREEN |
    | IP-14 | Extraction prompts include IP cues | PASSED | tests/extraction/ip-track-cues.test.ts GREEN + Project B extraction |
    | IP-15 | Architecture diagram visual rendering | PASSED | screenshot in 87-VERIFICATION.md |
    | IP-16 | OnboardingDashboard 3-column layout | PASSED | screenshot in 87-VERIFICATION.md |

    ## Inline UAT Fixes (if any)
    [List any fixes under 50 LoC applied during verification with file paths + commit hash, per [86-05] pattern. None expected if Plans 00-07 land clean.]

    ## Deferred Gaps (if any)
    [If any gaps require their own gap-closure phase, list them here and indicate "deferred to phase 87.1"]

    ## Sign-Off
    Phase 87 CLOSED YYYY-MM-DD. All 16 phase-local requirements verified.
    ```

    Update STATE.md and ROADMAP.md per the standard phase-close procedure:
    - STATE.md: increment `completed_plans`, update `last_activity` to reference Phase 87 closure with key stats (project count migrated, IP arch nodes seeded, etc.), and append Phase 87 [87-XX] decisions to "Key Decisions (carry-forward)" — pull the most important findings from each 87-NN-SUMMARY.md
    - ROADMAP.md: mark Phase 87 as ✅ Complete with date; update Progress table; bump v12 milestone summary if applicable

    Git commit (in the GSD planning root for STATE/ROADMAP/VERIFICATION docs):
    ```bash
    cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(87): close Phase 87 — Incident Prevention Track Support" --files .planning/phases/87-incident-prevention-track-support/87-VERIFICATION.md .planning/STATE.md .planning/ROADMAP.md
    ```

    Confirm the Panda-Manager code-side has been pushed (Plans 01-07 each committed their own work; verify the latest push reached origin/main):
    ```bash
    cd /Users/jmiloslavsky/Documents/Panda-Manager && git status && git log --oneline -10
    ```
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code && test -f .planning/phases/87-incident-prevention-track-support/87-VERIFICATION.md && grep -q 'PASSED' .planning/phases/87-incident-prevention-track-support/87-VERIFICATION.md && grep -q '✅' .planning/ROADMAP.md && grep -q 'Phase 87' .planning/STATE.md</automated>
    Expected: VERIFICATION.md exists with PASSED status; ROADMAP.md and STATE.md reflect Phase 87 closure.
  </verify>
  <done>87-VERIFICATION.md written and committed; STATE.md and ROADMAP.md updated; Phase 87 CLOSED.</done>
</task>

</tasks>

<verification>
- Task 1: Docker DB SQL outputs confirm migration applied + idempotent (human approved)
- Task 2: Browser verification across three test projects all PASS (human approved)
- Task 3: 87-VERIFICATION.md exists with PASSED status; STATE.md and ROADMAP.md reflect closure; git committed
</verification>

<success_criteria>
- All 16 phase-local requirements (IP-01..IP-16) verified.
- Phase 87 marked complete in ROADMAP.md and STATE.md.
- Panda-Manager git push to remote succeeded (verified at start of Task 3).
- 87-VERIFICATION.md committed to GSD planning repo.
- Any inline UAT fixes under 50 LoC documented in VERIFICATION.md per [86-05] precedent.
</success_criteria>

<output>
After completion, create `.planning/phases/87-incident-prevention-track-support/87-08-SUMMARY.md` documenting:
- Final verification result (PASSED / PASSED_WITH_DEFERRED_GAPS / FAILED)
- List of inline UAT fixes (if any) with file paths, LoC counts, commit hashes, and rationale
- Confirmation that all Plan 00-07 SUMMARYs are present and consistent
- Total Phase 87 stats: # files touched, # LOC net, # tests added, # tests GREEN
- Acknowledgement of CONTEXT.md "briefing skill stays untouched" decision honored (zero changes to briefing skill in this phase)
</output>
