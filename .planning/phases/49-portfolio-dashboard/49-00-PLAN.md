---
phase: 49-portfolio-dashboard
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/__tests__/portfolio/getPortfolioData.test.ts
  - bigpanda-app/__tests__/portfolio/portfolioSummary.test.ts
  - bigpanda-app/__tests__/portfolio/portfolioFilters.test.ts
  - bigpanda-app/__tests__/portfolio/portfolioExceptions.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 4 test files exist with RED stub tests before implementation tasks run"
    - "Each test file exports at least one failing test that describes expected behavior"
    - "Test suite runs without errors (tests fail with 'not implemented' messages)"
  artifacts:
    - path: "bigpanda-app/__tests__/portfolio/getPortfolioData.test.ts"
      provides: "RED stubs for getPortfolioData query function"
      min_lines: 20
    - path: "bigpanda-app/__tests__/portfolio/portfolioSummary.test.ts"
      provides: "RED stubs for PortfolioSummaryChips component"
      min_lines: 20
    - path: "bigpanda-app/__tests__/portfolio/portfolioFilters.test.ts"
      provides: "RED stubs for PortfolioTableClient filtering"
      min_lines: 20
    - path: "bigpanda-app/__tests__/portfolio/portfolioExceptions.test.ts"
      provides: "RED stubs for PortfolioExceptionsPanel component"
      min_lines: 20
  key_links:
    - from: "bigpanda-app/__tests__/portfolio/*.test.ts"
      to: "npm test"
      via: "jest test runner"
      pattern: "npm test.*--testPathPattern=portfolio"
---

<objective>
Create RED test scaffolds for all Phase 49 portfolio dashboard components before implementation begins.

Purpose: Establish Nyquist compliance by creating failing test stubs that describe expected behavior. Each implementation task can then verify progress by running its specific test file. Following TDD RED-GREEN-REFACTOR cycle — this is the RED step.

Output: 4 test files with failing stubs, test suite runs without errors (but tests fail with "not implemented")
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-portfolio-dashboard/49-CONTEXT.md
@.planning/phases/49-portfolio-dashboard/49-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create getPortfolioData test scaffolds</name>
  <files>bigpanda-app/__tests__/portfolio/getPortfolioData.test.ts</files>
  <action>
Create test file at `bigpanda-app/__tests__/portfolio/getPortfolioData.test.ts` with RED stubs for the portfolio data query function.

Test structure:
1. Import types and setup:
   ```typescript
   import { describe, it, expect } from '@jest/globals';
   // Type imports only — no implementation imports yet
   ```

2. Stub tests (all should fail with "not implemented"):
   - `describe('getPortfolioData')`:
     - `it('returns array of PortfolioProject objects')` → `throw new Error('not implemented')`
     - `it('includes enriched fields: owner, tracks, currentPhase, percentComplete')` → `throw new Error('not implemented')`
     - `it('includes next milestone fields: nextMilestone, nextMilestoneDate')` → `throw new Error('not implemented')`
     - `it('includes risk/dependency fields: riskLevel, dependencyStatus')` → `throw new Error('not implemented')`
     - `it('includes health fields from computeHealth')` → `throw new Error('not implemented')`
     - `it('completes in <500ms with 20+ projects')` → `throw new Error('not implemented')`

3. Each test should:
   - Describe expected behavior in test name
   - Throw "not implemented" error (RED state)
   - Include a comment describing the verification approach

Example stub:
```typescript
it('returns array of PortfolioProject objects', () => {
  // Verify: call getPortfolioData(), check return type is PortfolioProject[]
  throw new Error('not implemented');
});
```

4. Create `__tests__/portfolio/` directory if it doesn't exist

5. Ensure file uses correct test framework (jest or vitest based on project config)

Why stub tests: Establishes the test infrastructure before implementation. Plan 49-01 Task 1 can run `npm test -- --testPathPattern=getPortfolioData` immediately and see these RED tests.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=getPortfolioData</automated>
  </verify>
  <done>
Test file exists at bigpanda-app/__tests__/portfolio/getPortfolioData.test.ts with 6 failing stub tests, each test throws "not implemented" error, test suite runs without syntax errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PortfolioSummaryChips test scaffolds</name>
  <files>bigpanda-app/__tests__/portfolio/portfolioSummary.test.ts</files>
  <action>
Create test file at `bigpanda-app/__tests__/portfolio/portfolioSummary.test.ts` with RED stubs for the summary chips component.

Test structure:
1. Import types and setup (React Testing Library or similar)

2. Stub tests for `describe('PortfolioSummaryChips')`:
   - `it('renders 6 stat chips: totalActive, onTrack, atRisk, offTrack, blocked, overdueCount')` → `throw new Error('not implemented')`
   - `it('computes totalActive count from projects array length')` → `throw new Error('not implemented')`
   - `it('computes onTrack count from projects with health === green')` → `throw new Error('not implemented')`
   - `it('computes atRisk count from projects with health === yellow')` → `throw new Error('not implemented')`
   - `it('computes offTrack count from projects with health === red')` → `throw new Error('not implemented')`
   - `it('computes blocked count from projects with dependencyStatus === Blocked')` → `throw new Error('not implemented')`
   - `it('computes overdueCount by summing project.overdueActions')` → `throw new Error('not implemented')`
   - `it('applies correct color classes: green-100, yellow-100, red-100, orange-100, blue-100')` → `throw new Error('not implemented')`

3. Each stub test throws "not implemented" and includes comment describing verification

Why 8 stubs: Covers DASH-01 (health distribution) and DASH-02 (summary stats) requirements with granular test cases.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=portfolioSummary</automated>
  </verify>
  <done>
Test file exists at bigpanda-app/__tests__/portfolio/portfolioSummary.test.ts with 8 failing stub tests, each test throws "not implemented" error, test suite runs without syntax errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Create PortfolioTableClient filter test scaffolds</name>
  <files>bigpanda-app/__tests__/portfolio/portfolioFilters.test.ts</files>
  <action>
Create test file at `bigpanda-app/__tests__/portfolio/portfolioFilters.test.ts` with RED stubs for table filtering.

Test structure:
1. Import types and setup (React Testing Library, mock useSearchParams)

2. Stub tests for `describe('PortfolioTableClient filtering')`:
   - `it('filters by status (health) param')` → `throw new Error('not implemented')`
   - `it('filters by owner param (text match)')` → `throw new Error('not implemented')`
   - `it('filters by track param')` → `throw new Error('not implemented')`
   - `it('filters by phase param')` → `throw new Error('not implemented')`
   - `it('filters by riskLevel param')` → `throw new Error('not implemented')`
   - `it('filters by dependency param (Clear/Blocked)')` → `throw new Error('not implemented')`
   - `it('filters by search param (project name text match)')` → `throw new Error('not implemented')`
   - `it('combines multiple filters with AND logic')` → `throw new Error('not implemented')`
   - `it('updates URL params when filter changed')` → `throw new Error('not implemented')`
   - `it('renders filter panel with toggle button')` → `throw new Error('not implemented')`
   - `it('collapses/expands filter panel on toggle')` → `throw new Error('not implemented')`

3. Each stub throws "not implemented" and describes expected behavior

Why 11 stubs: Covers DASH-03 (table columns) and DASH-04 (filtering) with granular filter dimension tests.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=portfolioFilters</automated>
  </verify>
  <done>
Test file exists at bigpanda-app/__tests__/portfolio/portfolioFilters.test.ts with 11 failing stub tests, each test throws "not implemented" error, test suite runs without syntax errors
  </done>
</task>

<task type="auto">
  <name>Task 4: Create PortfolioExceptionsPanel test scaffolds</name>
  <files>bigpanda-app/__tests__/portfolio/portfolioExceptions.test.ts</files>
  <action>
Create test file at `bigpanda-app/__tests__/portfolio/portfolioExceptions.test.ts` with RED stubs for exceptions panel.

Test structure:
1. Import types and setup (React Testing Library)

2. Stub tests for `describe('PortfolioExceptionsPanel')`:
   - `it('computes overdue milestone exceptions (nextMilestoneDate < today)')` → `throw new Error('not implemented')`
   - `it('computes stale update exceptions (updated_at > 14 days ago)')` → `throw new Error('not implemented')`
   - `it('computes open blocker exceptions (dependencyStatus === Blocked)')` → `throw new Error('not implemented')`
   - `it('computes missing ownership exceptions (owner === null)')` → `throw new Error('not implemented')`
   - `it('computes unresolved dependency exceptions')` → `throw new Error('not implemented')`
   - `it('sorts exceptions by severity: blockers → overdue → ownership → stale → dependencies')` → `throw new Error('not implemented')`
   - `it('renders exception rows with project name, type badge, description')` → `throw new Error('not implemented')`
   - `it('links exception rows to /customer/[id]')` → `throw new Error('not implemented')`
   - `it('renders empty state when no exceptions exist')` → `throw new Error('not implemented')`
   - `it('renders collapsible panel with expand/collapse toggle')` → `throw new Error('not implemented')`

3. Each stub throws "not implemented" and describes verification approach

Why 10 stubs: Covers DASH-05 (exceptions panel) and DASH-06 (drill-down) with granular exception type tests.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=portfolioExceptions</automated>
  </verify>
  <done>
Test file exists at bigpanda-app/__tests__/portfolio/portfolioExceptions.test.ts with 10 failing stub tests, each test throws "not implemented" error, test suite runs without syntax errors
  </done>
</task>

</tasks>

<verification>
After task completion:
- All 4 test files exist in bigpanda-app/__tests__/portfolio/
- Running `npm test -- --testPathPattern=portfolio` executes all 35 stub tests
- All tests fail with "not implemented" errors (RED state)
- No syntax errors or test infrastructure failures
- Wave 1 plans (49-01, 49-02) can now reference these test files in their automated verify commands
</verification>

<success_criteria>
Plan complete when:
- [x] getPortfolioData.test.ts exists with 6 RED stubs
- [x] portfolioSummary.test.ts exists with 8 RED stubs
- [x] portfolioFilters.test.ts exists with 11 RED stubs
- [x] portfolioExceptions.test.ts exists with 10 RED stubs
- [x] All 35 tests run and fail with "not implemented" (RED)
- [x] Test suite has no syntax errors or infrastructure failures
- [x] Files committed to git
- [x] VALIDATION.md Wave 0 checklist satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/49-portfolio-dashboard/49-00-SUMMARY.md` documenting:
- Test files created (4 files, 35 RED stubs total)
- Test framework configuration confirmed
- Sampling strategy validated (each implementation task has a test file)
- Any test infrastructure issues encountered
</output>
