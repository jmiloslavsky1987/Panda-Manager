---
phase: 85-wbs-ms-project-style-overhaul
plan: "05"
type: execute
wave: 4
depends_on:
  - "85-04"
files_modified: []
autonomous: false
requirements:
  - WBS-01
  - WBS-02
  - WBS-03
  - WBS-04

must_haves:
  truths:
    - "User can see the WBS grid with all columns rendered in a browser"
    - "Clicking a cell activates inline editing; Tab navigates to the next cell; Enter moves to the next row"
    - "Tab with no cell focused indents the selected row"
    - "Shift+Tab with no cell focused outdents the selected row"
    - "Predecessor column shows row numbers, not item IDs"
    - "Gantt tab shows dependency arrows between linked task bars"
    - "Gantt WBS summary bars reflect percent_complete (not just 0/50/100 from status)"
  artifacts:
    - path: "components/WbsGrid.tsx"
      provides: "Verified working spreadsheet grid"
    - path: "components/GanttChart.tsx"
      provides: "Verified dependency arrow SVG rendering"
  key_links:
    - from: "WBS tab in browser"
      to: "WbsGrid component"
      via: "Visual verification of grid layout, cell editing, keyboard nav"
      pattern: "manual"
---

<objective>
Human verification checkpoint: confirm the complete WBS overhaul is working end-to-end in the browser before committing the phase as complete.

Purpose: The keyboard behaviors (Tab indent/outdent, Tab cell navigation), predecessor row-number display, and Gantt dependency arrows cannot be fully verified by automated tests alone.
Output: Phase 85 approved and ready for git push.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-wbs-ms-project-style-overhaul/85-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build verification</name>
  <files></files>
  <action>
Run the complete test suite and TypeScript build check before human verification:

```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run 2>&1 | tail -20
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx tsc --noEmit 2>&1 | head -20
```

If any tests are failing, investigate and fix before proceeding to the checkpoint.

Also verify no pre-existing test regressions exist that are unrelated to Phase 85 (compare against STATE.md [83-04] note about pre-existing failures in status-cycle/column-reorder from Phase 48 and portfolio/lifecycle).
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/components/WbsGrid.test.tsx tests/api/wbs-dependencies.test.ts tests/components/GanttChart-deps.test.ts tests/schema/wbs-overhaul.test.ts tests/api/wbs-crud.test.ts 2>&1 | grep -E "Tests|passed|failed"</automated>
  </verify>
  <done>All Phase 85 tests GREEN; TypeScript compiles clean; pre-existing unrelated failures noted but not blocking</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete WBS MS Project-style overhaul:
    - WbsGrid replaces WbsTree on the WBS tab — spreadsheet grid with inline cell editing
    - Tab/Enter keyboard navigation between cells
    - Tab (no cell focused) = indent; Shift+Tab = outdent
    - Predecessor column shows row numbers; editing saves dependency pairs
    - Gantt: percent_complete drives WBS bar fill; SVG arrows for FS/SS dependencies
    - Migration 0050: duration_days, percent_complete, assignee columns + wbs_dependencies table
  </what-built>
  <how-to-verify>
1. Start dev server: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run next-only`
2. Open a project → WBS tab
3. Verify the spreadsheet grid renders (not the old tree). Columns visible: Task Name, Duration, Start, Due, %, Assigned To, Predecessors
4. Click a cell in the Task Name column — verify an input appears
5. Press Tab — verify focus moves to the Duration column (same row)
6. Press Tab repeatedly — verify focus wraps through all columns and then to the first column of the next row
7. Type a value and press Enter — verify focus moves to the same column on the next row
8. Click away to deselect the cell, then click on a row (not a cell) to select the row. Press Tab — verify the row indents (becomes child of the row above)
9. Press Shift+Tab on the indented row — verify it outdents back to root
10. Type "2" in the Predecessors column of row 3 (after creating at least 3 rows) — verify it saves and displays "2" (row number, not item ID)
11. Open the Gantt tab — verify WBS summary bar progress reflects the % Complete value set in the grid
12. If you have a predecessor dependency set, verify an arrow connects the two bars in the Gantt
13. Verify ADR and Biggy tabs in WBS both show separate grids
14. Verify "+ Add task" at the bottom of the grid creates a new root-level row
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe any issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Git commit and push</name>
  <files></files>
  <action>
After human approval, commit and push all Phase 85 changes:

```bash
cd /Users/jmiloslavsky/Documents/Panda-Manager
git add db/migrations/0050_wbs_overhaul.sql
git add db/schema.ts
git add lib/queries.ts
git add app/api/projects/\[projectId\]/wbs/route.ts
git add app/api/projects/\[projectId\]/wbs/\[itemId\]/route.ts
git add app/api/projects/\[projectId\]/wbs/reorder/route.ts
git add app/api/projects/\[projectId\]/wbs/dependencies/route.ts
git add "app/api/projects/[projectId]/wbs/dependencies/[depId]/route.ts"
git add components/WbsGrid.tsx
git add components/WbsGrid.types.ts
git add app/customer/\[id\]/wbs/page.tsx
git add app/customer/\[id\]/gantt/page.tsx
git status
git commit -m "feat: [Phase 85] WBS MS Project-style spreadsheet grid with predecessor dependencies and Gantt arrows"
git push
```

Verify git push succeeds.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && git log --oneline -3</automated>
  </verify>
  <done>Phase 85 commit pushed to remote; git log shows the Phase 85 commit as most recent</done>
</task>

</tasks>

<verification>
Final phase gate:
```
cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run 2>&1 | tail -5
```
Expected: Test suite passes (excluding known pre-existing failures from Phase 48).
</verification>

<success_criteria>
- Full test suite GREEN for all Phase 85 test files
- Human has verified: grid renders, cell editing works, Tab/Enter navigation works, Tab indents when no cell focused, Shift+Tab outdents, predecessor column shows row numbers, Gantt arrows render, Gantt percent_complete reflects grid values
- Git commit pushed to remote with Phase 85 tag in message
- No production build regressions
</success_criteria>

<output>
After completion, create `.planning/phases/85-wbs-ms-project-style-overhaul/85-05-SUMMARY.md`
</output>
