---
phase: 15-scheduler-ui-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 15-02
files_modified: []
autonomous: false
requirements:
  - SCHED-01
  - SCHED-03
  - DATA-05
  - OVER-04

must_haves:
  truths:
    - "All Phase 15 automated tests are GREEN"
    - "BullMQ queue shows morning-briefing and weekly-customer-status as scheduled jobs"
    - "BullMQ queue does NOT show action-sync or weekly-briefing as scheduled jobs"
    - "Search type filter in the UI shows Onboarding Steps, Onboarding Phases, Integrations, and Time Entries options"
  artifacts:
    - path: "bigpanda-app/tests/scheduler-map.test.ts"
      provides: "GREEN — all 6 assertions pass"
    - path: "bigpanda-app/tests/search-type-options.test.ts"
      provides: "GREEN — all 5 assertions pass"
  key_links:
    - from: "BullMQ Redis state"
      to: "registered schedulers"
      via: "BullMQ dashboard or queue.getJobSchedulers()"
      pattern: "morning-briefing.*morning_briefing"
---

<objective>
Human verification checkpoint for Phase 15. Confirms the worker-tier scheduler registration fixes are observable at runtime (not just in unit tests), and that the search UI visually exposes all 12 FTS type filter options.

Purpose: Unit tests verify code structure. This checkpoint verifies the running system reflects the intent — that scheduled job names match handler names, phantom entries are absent from Redis, and the search filter UI change is visible.
Output: Phase 15 marked complete; STATE.md updated.

NOTE on DATA-05 and OVER-04: These requirement IDs (YAML export round-trip and context doc write-back) were explicitly deferred by the user in 15-CONTEXT.md locked decisions. They are listed in this plan's requirements field for traceability only — no implementation work is required. They will be addressed in a future phase.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-scheduler-ui-fixes/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and TypeScript check</name>
  <files>bigpanda-app/tests/scheduler-map.test.ts, bigpanda-app/tests/search-type-options.test.ts</files>
  <action>
    Run the complete automated verification battery:

    1. Full Vitest suite: cd bigpanda-app && npx vitest run
       Expected: all tests GREEN, including the 2 new Phase 15 test files

    2. TypeScript compilation: cd bigpanda-app && npx tsc --noEmit
       Expected: 0 new errors

    3. Grep for hardcoded skill paths in the 3 migrated handlers:
       grep -n "path.join(__dirname" bigpanda-app/worker/jobs/morning-briefing.ts bigpanda-app/worker/jobs/weekly-customer-status.ts bigpanda-app/worker/jobs/context-updater.ts
       Expected: no output

    4. Grep for scheduler map correctness:
       grep -n "morning-briefing\|weekly-customer-status\|action-sync\|weekly-briefing" bigpanda-app/worker/scheduler.ts
       Expected: morning-briefing and weekly-customer-status present as JOB_SCHEDULE_MAP keys; action-sync and weekly-briefing only in removeJobScheduler calls (not as map keys)

    If any check fails, stop and fix before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>All automated checks pass. Full Vitest suite GREEN. TypeScript clean. No hardcoded __dirname skill paths. Scheduler map correct.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — scheduler runtime and search UI</name>
  <action>Human verifies scheduler job names in BullMQ and search type filter options in the UI.</action>
  <what-built>
    Four integration gap fixes from v1.0 audit:
    1. BullMQ scheduler registration fixed — morning-briefing and weekly-customer-status now mapped in JOB_SCHEDULE_MAP; phantom action-sync and weekly-briefing removed and cleaned from Redis on next startup
    2. Three job handlers migrated from hardcoded skill path to resolveSkillsDir(settings.skill_path)
    3. Search type filter extended with 4 missing FTS table options
  </what-built>
  <how-to-verify>
    **A. Search filter UI (visual, ~30 seconds)**
    1. Start the app: cd bigpanda-app && npm run dev
    2. Navigate to http://localhost:3000/search
    3. Open the Type filter dropdown
    4. Confirm you can see: "Onboarding Steps", "Onboarding Phases", "Integrations", "Time Entries" in the list
    5. Select "Onboarding Steps" — confirm the filter applies (results may be empty if no data matches, but no error should appear)

    **B. Scheduler registration (if worker is accessible)**
    1. If BullMQ Bull Board dashboard is running, navigate to it and check the Repeatable Jobs list
    2. Confirm: "morning-briefing" appears with cron "0 8 * * *"
    3. Confirm: "weekly-customer-status" appears with cron "0 16 * * 4"
    4. Confirm: "action-sync" and "weekly-briefing" do NOT appear
    5. If Bull Board is not available, start the worker process and check startup logs for "Registered scheduler: morning-briefing" messages

    NOTE: If you cannot verify scheduler runtime state right now, the unit tests in scheduler-map.test.ts provide structural verification. Type "approved with note: scheduler runtime unverified" in that case.
  </how-to-verify>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms search type filter shows 4 new options. Human confirms or notes scheduler runtime state. Phase 15 approved.</done>
  <resume-signal>Type "approved" to close Phase 15, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 15 complete when:
- cd bigpanda-app && npx vitest run shows 0 failures
- Human verifier confirms search type filter shows all 12 options
- Human verifier confirms (or notes inability to verify) scheduler job names in BullMQ
</verification>

<success_criteria>
- All automated tests GREEN
- Search UI visually shows 4 new type filter options
- Phase 15 marked COMPLETE in STATE.md and ROADMAP.md
- DATA-05 and OVER-04 remain as Pending in REQUIREMENTS.md (to be addressed in a future phase)
</success_criteria>

<output>
After completion, create `.planning/phases/15-scheduler-ui-fixes/15-03-SUMMARY.md`

Update STATE.md: set current phase to Phase 15 COMPLETE.
Update ROADMAP.md: check Phase 15 checkbox.
</output>
