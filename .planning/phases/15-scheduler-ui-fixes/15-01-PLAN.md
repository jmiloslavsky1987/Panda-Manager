---
phase: 15-scheduler-ui-fixes
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/tests/scheduler-map.test.ts
  - bigpanda-app/tests/search-type-options.test.ts
autonomous: true
requirements:
  - SCHED-01
  - SRCH-02

must_haves:
  truths:
    - "scheduler-map.test.ts fails because JOB_SCHEDULE_MAP still has phantom entries (RED)"
    - "search-type-options.test.ts fails because TYPE_OPTIONS still has only 8 entries (RED)"
    - "Both test files are importable by Vitest with no syntax errors"
  artifacts:
    - path: "bigpanda-app/tests/scheduler-map.test.ts"
      provides: "Unit test verifying JOB_SCHEDULE_MAP contains morning-briefing and weekly-customer-status; verifies action-sync and weekly-briefing are absent"
      exports: []
    - path: "bigpanda-app/tests/search-type-options.test.ts"
      provides: "Unit test verifying TYPE_OPTIONS contains all 12 FTS table value strings"
      exports: []
  key_links:
    - from: "bigpanda-app/tests/scheduler-map.test.ts"
      to: "bigpanda-app/worker/scheduler.ts"
      via: "direct import of JOB_SCHEDULE_MAP"
      pattern: "import.*JOB_SCHEDULE_MAP.*scheduler"
    - from: "bigpanda-app/tests/search-type-options.test.ts"
      to: "bigpanda-app/app/search/page.tsx"
      via: "direct import of TYPE_OPTIONS"
      pattern: "import.*TYPE_OPTIONS.*search"
---

<objective>
Create two failing test files (RED state) that define the acceptance criteria for the Phase 15 implementation tasks. These test scaffolds must fail against the current codebase to ensure the RED→GREEN cycle is honest.

Purpose: Nyquist compliance — implementation tasks cannot have an `<automated>` verify command without a test file that exists. Creating the tests first ensures the verify commands in Plan 02 point to real files that were failing before the fix.
Output: Two new test files in bigpanda-app/tests/, both RED against current source.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-scheduler-ui-fixes/15-CONTEXT.md
@.planning/phases/15-scheduler-ui-fixes/15-RESEARCH.md
@.planning/phases/15-scheduler-ui-fixes/15-VALIDATION.md

<interfaces>
<!-- Existing export to import in tests. Do not modify these files. -->

From bigpanda-app/worker/scheduler.ts:
```typescript
// Export JOB_SCHEDULE_MAP so tests can inspect it
export const JOB_SCHEDULE_MAP: Record<string, keyof AppSettings['schedule']> = { ... };
```
NOTE: If JOB_SCHEDULE_MAP is not currently exported, add `export` keyword to the const — this is required for testability and is part of the Wave 1 work in Plan 02. For Wave 0, write the test to import it and let the test FAIL if JOB_SCHEDULE_MAP is not yet exported (do not modify scheduler.ts in this plan).

From bigpanda-app/app/search/page.tsx:
```typescript
// Export TYPE_OPTIONS so tests can inspect it
export const TYPE_OPTIONS = [ ... ];
```
NOTE: Same pattern — if TYPE_OPTIONS is not currently exported, write the test to import it; let it fail (RED). Plan 02 adds the export.

Vitest config: bigpanda-app/vitest.config.ts
Test runner: cd bigpanda-app && npx vitest run
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing scheduler map test</name>
  <files>bigpanda-app/tests/scheduler-map.test.ts</files>
  <behavior>
    - Test: JOB_SCHEDULE_MAP keys include 'morning-briefing'
    - Test: JOB_SCHEDULE_MAP keys include 'weekly-customer-status'
    - Test: JOB_SCHEDULE_MAP keys do NOT include 'action-sync'
    - Test: JOB_SCHEDULE_MAP keys do NOT include 'weekly-briefing'
    - Test: JOB_SCHEDULE_MAP['morning-briefing'] === 'morning_briefing'
    - Test: JOB_SCHEDULE_MAP['weekly-customer-status'] === 'weekly_status'
  </behavior>
  <action>
    Create bigpanda-app/tests/scheduler-map.test.ts using Vitest (import { describe, it, expect } from 'vitest').

    Import JOB_SCHEDULE_MAP from '../worker/scheduler'. Write 6 tests per the behavior block above. The file must be syntactically valid — if the import fails at runtime because JOB_SCHEDULE_MAP is not exported yet, Vitest will report it as a test failure (RED), which is the intended state for Wave 0.

    Do NOT modify scheduler.ts in this task. The tests must be RED because the current code has the wrong entries, not because the test is broken.

    Run after creating: cd bigpanda-app && npx vitest run tests/scheduler-map.test.ts
    Confirm the suite shows FAILED (some or all tests fail against current code).
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run tests/scheduler-map.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>File exists; Vitest reports at least one failure (RED state confirmed). No syntax errors in the test file itself.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing search type-options test</name>
  <files>bigpanda-app/tests/search-type-options.test.ts</files>
  <behavior>
    - Test: TYPE_OPTIONS contains an entry with value 'onboarding_steps'
    - Test: TYPE_OPTIONS contains an entry with value 'onboarding_phases'
    - Test: TYPE_OPTIONS contains an entry with value 'integrations'
    - Test: TYPE_OPTIONS contains an entry with value 'time_entries'
    - Test: TYPE_OPTIONS total length is 13 (1 'All Types' + 12 FTS tables)
  </behavior>
  <action>
    Create bigpanda-app/tests/search-type-options.test.ts using Vitest.

    Import TYPE_OPTIONS from '../app/search/page'. Write 5 tests per the behavior block above. As with the scheduler test, if the import fails because TYPE_OPTIONS is not yet exported, the test will fail (RED) — intended.

    Run after creating: cd bigpanda-app && npx vitest run tests/search-type-options.test.ts
    Confirm the suite shows FAILED (4 missing entries not present in current TYPE_OPTIONS).
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx vitest run tests/search-type-options.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>File exists; Vitest reports failures for the 4 missing TYPE_OPTIONS entries (RED state confirmed). No syntax errors.</done>
</task>

</tasks>

<verification>
Both test files exist. Running cd bigpanda-app && npx vitest run tests/scheduler-map.test.ts tests/search-type-options.test.ts shows failures (not "no test files found" and not "0 tests"). Full suite remains unaffected: cd bigpanda-app && npx vitest run passes all pre-existing tests (new RED files do not break existing GREEN tests — Vitest treats each file independently).
</verification>

<success_criteria>
- bigpanda-app/tests/scheduler-map.test.ts exists and has 6 named tests; at least 4 fail against current scheduler.ts
- bigpanda-app/tests/search-type-options.test.ts exists and has 5 named tests; at least 4 fail against current search/page.tsx
- No pre-existing tests broken by importing these new files
</success_criteria>

<output>
After completion, create `.planning/phases/15-scheduler-ui-fixes/15-01-SUMMARY.md`
</output>
