---
phase: 20-project-initiation-wizard
plan: "06"
type: execute
wave: 5
depends_on:
  - "20-04"
  - "20-05"
files_modified: []
autonomous: false

requirements:
  - WIZ-01
  - WIZ-02
  - WIZ-03
  - WIZ-04
  - WIZ-05
  - WIZ-07
  - WIZ-08
  - WIZ-09

must_haves:
  truths:
    - "Clicking 'New Project' on the Dashboard opens the wizard as a full-screen Dialog"
    - "Step 1 form collects project info and creates the project — draft appears in Dashboard immediately"
    - "Completing the wizard and clicking Launch sets project status to Active and navigates to the project"
    - "Overview tab shows a completeness score bar and below-60% warning with working tab links"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Project Initiation Wizard end-to-end flow and the Overview tab completeness display.

Purpose: Automated tests verify logic but cannot verify visual rendering, Dialog focus trap behavior, drag-and-drop upload, and the full wizard flow. This checkpoint confirms the complete user experience.
Output: Verified wizard flow from Dashboard to Launch + completeness display confirmed working.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-project-initiation-wizard/20-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete 5-step Project Initiation Wizard accessible from the Dashboard. Creates a project, uploads collateral documents with AI extraction, allows manual data entry, and launches the project. Overview tab now shows a Project Completeness score bar and a below-60% warning banner.
  </what-built>
  <how-to-verify>
    Run `cd bigpanda-app && npm run dev` to start the app if not already running.

    **Test 1 — Dashboard button:**
    1. Open http://localhost:3000
    2. Confirm a "New Project" button appears in the Dashboard header row
    3. Click it — verify a full-screen Dialog opens with a 5-step progress indicator

    **Test 2 — Step 1: Project Info:**
    1. Fill in: Project Name = "Test Project", Customer = "Test Corp", Status = Draft
    2. Click Next — verify the API creates the project (console should show no errors)
    3. Verify step 2 (Upload Files) now shows

    **Test 3 — Step 1 → close → Draft in Dashboard:**
    1. Open a NEW project wizard
    2. Fill step 1, click Next — project is created
    3. Close the wizard via the X button
    4. Verify "Test Project 2" (or your project name) appears in the Dashboard as a Draft card

    **Test 4 — Step 2: Upload Collateral:**
    1. Advance to step 2
    2. Drag a file named "sow-draft.pdf" onto the upload zone
    3. Verify the "SOW" checklist item auto-checks
    4. Drag a file named "kickoff-presentation.pptx" — verify "Kickoff Deck" auto-checks
    5. Verify both files appear in the file list

    **Test 5 — Steps 3-5: Full wizard flow (requires actual files for extraction):**
    1. Skip step 3 if no real documents to extract (click Skip)
    2. On step 4 (Manual Entry): add one Action manually ("Title: Test action") and save it
    3. Advance to step 5 — verify the Launch step shows a summary including the manual action
    4. Click "Launch Project"
    5. Verify navigation to /customer/[id] page

    **Test 6 — Overview tab completeness:**
    1. Navigate to any project's Overview tab
    2. Verify a progress bar appears at the top showing "Project Completeness: X%"
    3. For a project with sparse data (< 60%): verify a yellow warning banner appears below the bar
    4. Verify the banner lists specific tab names as links (e.g., "Risks", "Milestones")
    5. Click one of the tab links — verify it navigates to the correct workspace tab

    **Test 7 — Regression check:**
    1. Run `cd bigpanda-app && npx vitest run --reporter=verbose` — confirm full suite green
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 20, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full end-to-end wizard flow verified by human. See checkpoint instructions above.
</verification>

<success_criteria>
- "New Project" button visible on Dashboard; wizard opens as full-screen Dialog
- Step 1 creates project; closing after step 1 leaves Draft in Dashboard
- File upload in step 2 auto-checks matching collateral categories
- Manual entry in step 4 persists items to DB
- "Launch Project" sets status to Active and navigates to project
- Overview tab shows completeness bar (always) + yellow banner when score < 60%
- Full vitest suite green
</success_criteria>

<output>
After completion, create `.planning/phases/20-project-initiation-wizard/20-06-SUMMARY.md`
</output>
