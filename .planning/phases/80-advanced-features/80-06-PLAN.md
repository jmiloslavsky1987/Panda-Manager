---
phase: 80-advanced-features
plan: "06"
type: execute
wave: 4
depends_on:
  - "80-05"
files_modified: []
autonomous: false
requirements:
  - RECUR-01
  - OUT-01
  - AVAIL-01
  - SCHED-01

must_haves:
  truths:
    - "All four Phase 80 requirements are visually verified as working end-to-end"
    - "Full test suite passes green before this checkpoint is reached"
    - "Git commit + push complete for all Phase 80 changes"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for all four Phase 80 features: recurring templates, PDF export, stakeholder availability, and auto-prep job.

Purpose: Confirm the power-user features work end-to-end in the browser before closing v10.0.
Output: Human sign-off on all four RECUR-01 / OUT-01 / AVAIL-01 / SCHED-01 behaviors.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-advanced-features/80-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated verification</name>
  <files></files>
  <action>
    Before presenting the checkpoint, run the full test suite and confirm it is green:

    ```bash
    cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run
    ```

    Also run TypeScript check:
    ```bash
    cd /Users/jmiloslavsky/Documents/Panda-Manager && npx tsc --noEmit
    ```

    Also confirm git commit + push for all Phase 80 changes:
    ```bash
    cd /Users/jmiloslavsky/Documents/Panda-Manager && git status && git log --oneline -5
    ```

    If any tests are failing or TypeScript errors exist, STOP and fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All vitest tests GREEN; no TypeScript errors; git clean and pushed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 80 complete — four power-user features layered on the Daily Prep foundation:
    1. RECUR-01: Recurring meeting prep templates (save/load per series ID)
    2. OUT-01: PDF export via window.print() — per-card and Export All
    3. AVAIL-01: Stakeholder availability chips (free/busy) on event cards
    4. SCHED-01: Auto-prep BullMQ job + DB-backed brief persistence (localStorage replaced)
  </what-built>
  <how-to-verify>
    Start dev server: `cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run dev`
    Open: http://localhost:3000/daily-prep

    **RECUR-01 — Recurring Templates:**
    1. Find a recurring calendar event (it should show the recurring badge from Phase 79)
    2. Generate a prep brief for it
    3. After brief appears, confirm "Save as template" button is visible
    4. Click "Save as template" — button should change to show "Template saved" badge
    5. Reload the page — the recurring event card should show "Template saved" badge without generating
    6. Click "Load template" — brief should expand inline with the saved content
    7. Confirm "Regenerate" button is still accessible after loading template

    **OUT-01 — PDF Export:**
    1. Generate a prep brief for any event
    2. Click "Export" on the card — browser print dialog should open
    3. In print preview, confirm: event header visible, brief text visible, navigation hidden, buttons hidden, dropdowns hidden
    4. Close print dialog
    5. Click "Export All" button (top of page) — print dialog shows ALL cards with generated briefs
    6. Confirm collapsed cards also appear expanded in the Export All print preview

    **AVAIL-01 — Stakeholder Availability:**
    1. On /daily-prep, events with matched project stakeholders who are also attendees should show colored chips below attendee list
    2. Green dot = free during meeting time, red dot = busy
    3. If Google Calendar is not re-authed with freebusy scope: confirm a soft banner appears ("Reconnect calendar...") — no crash
    4. Cards should show a loading state briefly while freebusy data is fetched on page load

    **SCHED-01 — Auto-Prep Job:**
    1. Navigate to /scheduler (global scheduler page)
    2. Click "Create Job" — confirm "Meeting Prep (Daily)" appears in the skill list
    3. Select it and proceed through wizard — confirm no project picker, just a description of the job
    4. Save the job — it should appear in the scheduler table
    5. Return to /daily-prep — confirm briefs generated previously persist across page reload (DB persistence, not localStorage)
    6. Open browser DevTools → Application → Local Storage — confirm no `daily-prep-briefs:` keys exist
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 80, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All four requirements verified by human inspection. Test suite green. Git pushed.
</verification>

<success_criteria>
- RECUR-01: Template save/load works end-to-end for recurring events
- OUT-01: Per-card Export and Export All produce clean print output
- AVAIL-01: Availability chips render; 403 degrades gracefully
- SCHED-01: meeting-prep-daily job available in scheduler; localStorage removed; briefs persist in DB
- Human approves all four features
</success_criteria>

<output>
After completion, create `.planning/phases/80-advanced-features/80-06-SUMMARY.md`
</output>
