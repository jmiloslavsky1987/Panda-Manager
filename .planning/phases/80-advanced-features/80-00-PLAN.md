---
phase: 80-advanced-features
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/__tests__/recur-template.test.ts
  - lib/__tests__/pdf-export.test.ts
  - lib/__tests__/availability.test.ts
  - lib/__tests__/meeting-prep-daily.test.ts
autonomous: true
requirements:
  - RECUR-01
  - OUT-01
  - AVAIL-01
  - SCHED-01

must_haves:
  truths:
    - "All four test files exist with failing stub tests before any implementation runs"
    - "Each test file uses readFileSync/existsSync source-check pattern matching lib/__tests__/daily-prep-generate.test.ts"
    - "Running `npx vitest run` on stub tests produces clear RED failure messages (assertion not satisfied)"
  artifacts:
    - path: "lib/__tests__/recur-template.test.ts"
      provides: "RECUR-01 source-check stubs"
    - path: "lib/__tests__/pdf-export.test.ts"
      provides: "OUT-01 source-check stubs"
    - path: "lib/__tests__/availability.test.ts"
      provides: "AVAIL-01 source-check stubs"
    - path: "lib/__tests__/meeting-prep-daily.test.ts"
      provides: "SCHED-01 source-check stubs"
  key_links:
    - from: "lib/__tests__/recur-template.test.ts"
      to: "app/api/daily-prep/templates/route.ts"
      via: "existsSync check"
      pattern: "existsSync.*templates/route"
    - from: "lib/__tests__/meeting-prep-daily.test.ts"
      to: "worker/jobs/meeting-prep-daily.ts"
      via: "existsSync check"
      pattern: "existsSync.*meeting-prep-daily"
---

<objective>
Create all four Wave 0 test scaffold files for Phase 80. All tests must be RED (failing stubs) — they check for artifacts that do not yet exist.

Purpose: Establish the Nyquist-compliant feedback loop so every Wave 1+ task has an automated verify command ready.
Output: Four test files in lib/__tests__/ following the established source-check pattern from lib/__tests__/daily-prep-generate.test.ts.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-advanced-features/80-CONTEXT.md
@.planning/phases/80-advanced-features/80-RESEARCH.md

<interfaces>
<!-- Established source-check test pattern from lib/__tests__/daily-prep-generate.test.ts -->
<!-- All Wave 0 tests use readFileSync + existsSync — no runtime code execution -->

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync, existsSync } from 'fs';
import path from 'path';

const targetPath = path.resolve(__dirname, '../../path/to/target.ts');

describe('REQ-XX: feature description', () => {
  it('REQ-XX Test 1: file exists', () => {
    expect(existsSync(targetPath)).toBe(true);  // STUB: returns false until file created
  });
  it('REQ-XX Test 2: source contains required pattern', () => {
    const source = readFileSync(targetPath, 'utf-8');
    expect(source).toContain('expectedString');  // STUB: throws until file exists
  });
});
```

Key: Use `path.resolve(__dirname, '../../...')` to navigate from lib/__tests__/ to project root.
Tests must FAIL (RED) before any implementation. Write stubs with wrong expected values only if needed to produce clear failure, but existsSync returning false is the cleanest RED state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 stubs for RECUR-01 and OUT-01</name>
  <files>lib/__tests__/recur-template.test.ts, lib/__tests__/pdf-export.test.ts</files>
  <behavior>
    - recur-template.test.ts: Tests FAIL because the artifacts don't yet exist
    - Test 1: CalendarEventItem interface exposes `recurring_event_id: string | null` (check calendar-import/route.ts source)
    - Test 2: /api/daily-prep/templates/route.ts file exists
    - Test 3: templates route exports GET, POST, DELETE functions
    - Test 4: templates route uses requireSession
    - Test 5: meeting_prep_templates referenced in db/schema.ts
    - Test 6: DailyPrepCard.tsx contains "Save as template" text
    - Test 7: DailyPrepCard.tsx contains "Load template" text
    - Test 8: DailyPrepCard.tsx contains "hasTemplate" in EventCardState
    - pdf-export.test.ts: Tests FAIL because artifacts don't yet exist
    - Test 1: daily-prep page.tsx contains window.print()
    - Test 2: daily-prep page.tsx contains "Export" button text
    - Test 3: globals.css or daily-prep page.tsx contains @media print
    - Test 4: daily-prep page.tsx contains "Export All" text
    - Test 5: DailyPrepCard.tsx contains data-print-visible or data-testid="brief-section"
  </behavior>
  <action>
    Create lib/__tests__/recur-template.test.ts and lib/__tests__/pdf-export.test.ts using the source-check pattern from lib/__tests__/daily-prep-generate.test.ts. Use readFileSync + existsSync. All tests must produce clear RED failures — the artifacts being checked do not exist yet.

    For recur-template.test.ts, check paths:
    - `../../app/api/time-entries/calendar-import/route.ts` for `recurring_event_id`
    - `../../app/api/daily-prep/templates/route.ts` (existsSync, then content checks)
    - `../../db/schema.ts` for `meeting_prep_templates`
    - `../../components/DailyPrepCard.tsx` for template UI strings

    For pdf-export.test.ts, check paths:
    - `../../app/daily-prep/page.tsx` for window.print(), "Export", "Export All"
    - `../../app/globals.css` OR `../../app/daily-prep/page.tsx` for `@media print`
    - `../../components/DailyPrepCard.tsx` for print-friendly attributes

    Include a comment block at top of each file listing the requirement IDs covered, matching the style of daily-prep-generate.test.ts.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run lib/__tests__/recur-template.test.ts lib/__tests__/pdf-export.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Both files exist; running vitest produces FAIL output (not errors/exceptions) with clear "expected false to be true" or "cannot readFileSync" messages confirming stubs are RED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 stubs for AVAIL-01 and SCHED-01</name>
  <files>lib/__tests__/availability.test.ts, lib/__tests__/meeting-prep-daily.test.ts</files>
  <behavior>
    - availability.test.ts: Tests FAIL because artifacts don't yet exist
    - Test 1: /api/calendar/freebusy/route.ts file exists
    - Test 2: freebusy route uses requireSession
    - Test 3: freebusy route exports POST function
    - Test 4: /api/oauth/calendar/route.ts contains `calendar.freebusy` scope string
    - Test 5: CalendarEventItem contains `start_datetime` field
    - Test 6: CalendarEventItem contains `end_datetime` field
    - Test 7: DailyPrepCard.tsx contains "availability" (the EventCardState field)
    - meeting-prep-daily.test.ts: Tests FAIL because artifacts don't yet exist
    - Test 1: lib/scheduler-skills.ts contains 'meeting-prep-daily'
    - Test 2: worker/jobs/meeting-prep-daily.ts file exists
    - Test 3: worker/index.ts imports meeting-prep-daily
    - Test 4: worker/index.ts JOB_HANDLERS contains 'meeting-prep-daily'
    - Test 5: db/schema.ts contains 'daily_prep_briefs'
    - Test 6: db/migrations/0045_daily_prep_tables.sql file exists
    - Test 7: app/daily-prep/page.tsx does NOT contain `localStorage.getItem` for briefs (LocalStorage removed)
  </behavior>
  <action>
    Create lib/__tests__/availability.test.ts and lib/__tests__/meeting-prep-daily.test.ts.

    For availability.test.ts, check paths:
    - `../../app/api/calendar/freebusy/route.ts` (existsSync + content)
    - `../../app/api/oauth/calendar/route.ts` for `calendar.freebusy` scope
    - `../../app/api/time-entries/calendar-import/route.ts` for `start_datetime`, `end_datetime`
    - `../../components/DailyPrepCard.tsx` for availability field

    For meeting-prep-daily.test.ts, check paths:
    - `../../lib/scheduler-skills.ts` for `meeting-prep-daily`
    - `../../worker/jobs/meeting-prep-daily.ts` (existsSync + content)
    - `../../worker/index.ts` for import and JOB_HANDLERS entry
    - `../../db/schema.ts` for `daily_prep_briefs`
    - `../../db/migrations/0045_daily_prep_tables.sql` (existsSync)
    - `../../app/daily-prep/page.tsx` for localStorage removal (check that it does NOT contain `localStorage.getItem` in brief-loading context — use `not.toContain('daily-prep-briefs')`)

    Note: For the localStorage test (Test 7), the existing page contains `localStorage.getItem` with the key `daily-prep-briefs:${selectedDate}`. After SCHED-01 implementation, this must be removed. Write the test as: `expect(source).not.toContain("daily-prep-briefs:")` — this is RED now (source contains it) and GREEN after implementation.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run lib/__tests__/availability.test.ts lib/__tests__/meeting-prep-daily.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Both files exist; running vitest produces FAIL output confirming all stubs are RED; meeting-prep-daily localStorage test correctly fails because page.tsx currently contains "daily-prep-briefs:" localStorage key</done>
</task>

</tasks>

<verification>
All four test files exist in lib/__tests__/. Running the full suite produces failures (not syntax errors):
`cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run lib/__tests__/recur-template.test.ts lib/__tests__/pdf-export.test.ts lib/__tests__/availability.test.ts lib/__tests__/meeting-prep-daily.test.ts`

Expect: 20+ test failures, 0 passes for the new files. No "cannot parse file" or TypeScript errors.
</verification>

<success_criteria>
- Four test files exist at lib/__tests__/{recur-template,pdf-export,availability,meeting-prep-daily}.test.ts
- All tests are RED (failing) with clear assertion messages
- No syntax errors or import failures
- Test source follows the readFileSync/existsSync pattern from daily-prep-generate.test.ts
- Each file has a comment block listing the requirement IDs it covers
</success_criteria>

<output>
After completion, create `.planning/phases/80-advanced-features/80-00-SUMMARY.md`
</output>
