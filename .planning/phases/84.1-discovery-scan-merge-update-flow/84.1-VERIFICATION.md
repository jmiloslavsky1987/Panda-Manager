---
phase: 84.1-discovery-scan-merge-update-flow
verified: 2026-05-07T18:20:00Z
status: human_needed
score: 6/6 must-haves verified
human_verification:
  - test: "Merge button visible in ReviewQueue for items with entity_match set"
    expected: "For any queue item that has entity_match set after a scan, a 'Merge' button appears alongside Approve/Edit/Dismiss"
    why_human: "React UI rendering and live scan output cannot be verified programmatically"
  - test: "Merge button label shows which entity will be updated"
    expected: "Button displays 'Merge' with title tooltip showing 'Merge into \"<entity name>\"'"
    why_human: "Visual/tooltip display requires browser observation"
  - test: "Clicking Merge updates the target entity (team_engagement content appended, workflow step inserted, arch_node notes appended)"
    expected: "After clicking Merge on a team_engagement item, navigate to Teams tab — old content is still visible with new text appended below it. Human confirmed this end-to-end in session."
    why_human: "Append semantics require browser + DB state observation. User confirmed this works."
  - test: "Approving an arch_node discovery item twice does not produce an error"
    expected: "Second approve call returns 200 with approved:1, errors:[] — no 500 or constraint error"
    why_human: "Requires live DB with existing arch_node item to duplicate. User confirmed this works."
  - test: "Existing Approve/Dismiss flow unaffected (regression)"
    expected: "Approving a non-merge item (action, risk, decision) and dismissing items work as before"
    why_human: "Full regression requires browser interaction with live data. User confirmed end-to-end merge worked (implying approve/dismiss unchanged)."
---

# Phase 84.1: Discovery Scan Merge/Update Flow Verification Report

**Phase Goal:** Discovered content can enrich existing entities rather than always creating new ones — merge/update flow for team_engagement, workflow_step, and arch_node types, plus fix the arch_node duplicate approve bug.
**Verified:** 2026-05-07T18:20:00Z
**Status:** human_needed — all automated checks passed; human confirmed end-to-end merge flow works
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                  | Status     | Evidence                                                                                  |
|----|----------------------------------------------------------------------------------------|------------|-------------------------------------------------------------------------------------------|
| 1  | discovery_items table has entity_match and suggested_position columns                 | VERIFIED   | db/migrations/0049_discovery_merge_fields.sql has both ALTER TABLE statements; db/schema.ts has both columns on discoveryItems |
| 2  | DISCOVERY_SYSTEM_TEMPLATE (exported) contains entity_match and suggested_position     | VERIFIED   | lib/discovery-scanner.ts line 42: `export const DISCOVERY_SYSTEM_TEMPLATE`; lines 52-53 document both fields; MERGE-01 tests pass (9/9 in scan.test.ts) |
| 3  | DiscoveryItem interface has entity_match? and suggested_position? optional fields     | VERIFIED   | lib/discovery-scanner.ts lines 29-30; approve/route.ts DiscoveryItem type lines 57-58    |
| 4  | scan/route.ts persists entity_match and suggested_position on insert                  | VERIFIED   | app/api/discovery/scan/route.ts lines 232-235: both fields included in db.insert values  |
| 5  | Approving arch_node twice returns approved:1 errors:[] (BUG-01 fix)                  | VERIFIED   | approve/route.ts line 359: `.onConflictDoNothing()` present on archNodes insert; BUG-01 test GREEN (15/15 approve tests pass) |
| 6  | mergeDiscoveredItem() handles team_engagement, workflow_step, arch_node               | VERIFIED   | approve/route.ts lines 95-191: full implementation present with all three switch cases; MERGE-03/04/05 tests GREEN |
| 7  | QueueItemRow shows Merge button only when item.entity_match is set                   | VERIFIED   | QueueItemRow.tsx lines 198-207: conditional `{item.entity_match && onMerge && ( <button...> )}` |
| 8  | ReviewQueue wires handleMerge() that calls approve POST with action:merge             | VERIFIED   | ReviewQueue.tsx lines 79-111: handleMerge() present with action:'merge', entity_match, res.ok guard, toast feedback; onMerge={handleMerge} wired at line 160 |
| 9  | queue/route.ts parses suggested_position JSON string to object before returning       | VERIFIED   | app/api/discovery/queue/route.ts lines 37-43: JSON.parse with try/catch on suggested_position |
| 10 | ScanForUpdatesButton uses window.location.href to avoid React 19 crash               | VERIFIED   | ScanForUpdatesButton.tsx line 187: `window.location.href = '/customer/${projectId}/queue'` |
| 11 | Gmail adapter fetches full email bodies (format: full, 8000 char cap)                | VERIFIED   | lib/source-adapters/gmail-adapter.ts line 5: `const MAX_BODY_CHARS = 8000`; line 69: `format: 'full'` |
| 12 | MERGE-02 test GREEN (QueueItemRow source contains entity_match and onMerge)          | VERIFIED   | queue.test.ts line 200: MERGE-02 test passes (1/1 in Phase 84.1 describe block; 5 pre-existing failures are unrelated Phase 19 issues) |

**Score:** 6/6 must-haves verified (all automated truths confirmed; items 7-12 are bonus confirmations of human-approved items)

### Required Artifacts

| Artifact                                         | Expected                                                          | Status     | Details                                                                             |
|--------------------------------------------------|-------------------------------------------------------------------|------------|-------------------------------------------------------------------------------------|
| `db/migrations/0049_discovery_merge_fields.sql` | ALTER TABLE statements for entity_match + suggested_position      | VERIFIED   | File exists with 2 ALTER TABLE statements; committed in feat(84.1-01)               |
| `db/schema.ts`                                   | discoveryItems table has entity_match + suggested_position columns | VERIFIED   | Lines 560-561: both columns present as nullable text                                |
| `lib/discovery-scanner.ts`                       | Exported DISCOVERY_SYSTEM_TEMPLATE + extended DiscoveryItem       | VERIFIED   | export keyword at line 42; entity_match at line 29; suggested_position at line 30   |
| `app/api/discovery/scan/route.ts`                | Persists entity_match + suggested_position on insert              | VERIFIED   | Lines 232-235: both fields in insert values block                                   |
| `app/api/discovery/approve/route.ts`             | mergeDiscoveredItem() + resolveStepPosition() + BUG-01 fix        | VERIFIED   | mergeDiscoveredItem() at line 95; resolveStepPosition() at line 69; .onConflictDoNothing() at line 359 |
| `components/QueueItemRow.tsx`                    | entity_match + onMerge prop + conditional Merge button            | VERIFIED   | entity_match in DiscoveryQueueItem (line 18); onMerge in QueueItemRowProps (line 78); Merge button at lines 198-207 |
| `components/ReviewQueue.tsx`                     | handleMerge() + onMerge wired to all queue rows                   | VERIFIED   | handleMerge() at line 79; onMerge={handleMerge} wired in QueueTable at line 160 (covers newItems + likelyDuplicates; dismissed rows get readonly prop which hides action column) |
| `app/api/discovery/queue/route.ts`               | JSON parses suggested_position before returning to client         | VERIFIED   | Lines 37-43: parse block present with JSON.parse + try/catch fallback                |

### Key Link Verification

| From                        | To                                         | Via                                      | Status   | Details                                                                                    |
|-----------------------------|--------------------------------------------|------------------------------------------|----------|--------------------------------------------------------------------------------------------|
| POST handler                | mergeDiscoveredItem()                      | action === 'merge' branch                | WIRED    | approve/route.ts lines 547-566: if (action === 'merge') branch calls mergeDiscoveredItem() |
| mergeDiscoveredItem()       | teamEngagementSections                     | db.update with SQL concat                | WIRED    | Lines 120-123: db.update with sql`COALESCE...` (note: uses SQL concat, not JS concat — correct for production atomicity; test mock verified GREEN) |
| mergeDiscoveredItem()       | workflowSteps                              | resolveStepPosition + db.insert          | WIRED    | Lines 143-148: resolveStepPosition() called, db.insert with computed position              |
| mergeDiscoveredItem()       | archNodes                                  | db.update notes with SQL concat          | WIRED    | Lines 165-169: db.update with sql`COALESCE(${archNodes.notes}, '') || ...`                 |
| ReviewQueue handleMerge()   | /api/discovery/approve                     | POST with action:merge                   | WIRED    | ReviewQueue.tsx lines 85-95: fetch with action:'merge', entity_match, suggested_position   |
| QueueItemRow Merge button   | onMerge callback                           | onClick conditional on entity_match      | WIRED    | Lines 198-207: button onClick calls onMerge(item.id, item.entity_match!, item.suggested_position) |
| lib/discovery-scanner.ts    | DiscoveryItem.entity_match                 | interface optional field                 | WIRED    | Line 29: `entity_match?: string` |
| app/api/discovery/scan      | entity_match in db.insert                  | item.entity_match ?? null                | WIRED    | scan/route.ts line 232: `entity_match: item.entity_match ?? null` |

### Requirements Coverage

| Requirement | Source Plan(s)     | Description                                                     | Status    | Evidence                                                                                     |
|-------------|--------------------|-----------------------------------------------------------------|-----------|----------------------------------------------------------------------------------------------|
| BUG-01      | 84.1-00, 84.1-02, 84.1-04 | Second approval of same arch_node does not error (unique constraint bug) | SATISFIED | .onConflictDoNothing() at approve/route.ts:359; BUG-01 test GREEN in approve.test.ts         |
| MERGE-01    | 84.1-01, 84.1-04   | DISCOVERY_SYSTEM_TEMPLATE includes entity_match + suggested_position field docs | SATISFIED | lib/discovery-scanner.ts lines 52-53; MERGE-01 tests GREEN (2/2) in scan.test.ts             |
| MERGE-02    | 84.1-03, 84.1-04   | QueueItemRow renders Merge button when item.entity_match is set | SATISFIED | QueueItemRow.tsx lines 198-207; MERGE-02 test GREEN in queue.test.ts; human confirmed         |
| MERGE-03    | 84.1-02, 84.1-04   | team_engagement merge appends content to existing section       | SATISFIED | mergeDiscoveredItem() team_engagement case in approve/route.ts:104-125; MERGE-03 test GREEN  |
| MERGE-04    | 84.1-02, 84.1-04   | workflow_step merge inserts step at anchor_position + 1         | SATISFIED | resolveStepPosition() + mergeDiscoveredItem() workflow_step case; MERGE-04 test GREEN        |
| MERGE-05    | 84.1-02, 84.1-04   | arch_node merge appends text to existing node's notes           | SATISFIED | mergeDiscoveredItem() arch_node case in approve/route.ts:152-170; MERGE-05 test GREEN        |

All 6 requirements from the ROADMAP are covered by plan frontmatter. No orphaned requirements found in REQUIREMENTS.md (the BUG/MERGE IDs are Phase 84.1-specific, not in the v10/v11 REQUIREMENTS.md document, which tracks calendar/KDS features).

### Anti-Patterns Found

| File                              | Line | Pattern                                    | Severity | Impact                                                                                     |
|-----------------------------------|------|-------------------------------------------|----------|--------------------------------------------------------------------------------------------|
| components/ReviewQueue.tsx        | 24   | `if (!res.ok) return` in handleApprove    | Info     | handleApprove silently swallows errors — pre-existing behavior, out of scope for this phase |
| components/ReviewQueue.tsx        | 40   | `if (!res.ok) return` in handleDismiss    | Info     | Same pattern for handleDismiss — pre-existing                                               |

No blockers found. The `handleMerge` function has the appropriate `res.ok` guard + toast errors (user confirmed this was added in this phase).

### Human Verification Required

The following items need browser confirmation. Per the session context provided, the user has already confirmed the merge flow end-to-end. These items are listed for completeness and to record what was human-validated.

**1. Merge button visibility and label**

**Test:** Run a scan on a project, inspect review queue items with entity_match set.
**Expected:** "Merge" button appears alongside Approve/Edit/Dismiss; title tooltip reads `Merge into "<entity name>"`
**Why human:** React UI rendering requires browser observation.
**Status:** User confirmed (session context: "Merge button works end-to-end").

**2. Merge action append semantics**

**Test:** Click Merge on a team_engagement item, navigate to Teams tab.
**Expected:** Old content is still present with new text appended below it (not replaced).
**Why human:** DB append semantics require observing actual data state.
**Status:** User confirmed.

**3. Arch_node duplicate approve fix**

**Test:** Approve an arch_node item, run scan again for same item, approve again.
**Expected:** Second approve returns success, no 500 error or constraint failure.
**Why human:** Requires live DB with an arch_node that can trigger a duplicate constraint.
**Status:** User confirmed.

**4. Existing Approve/Dismiss regression**

**Test:** Approve a non-merge item (action, risk, decision); dismiss an item.
**Expected:** Both actions work exactly as before Phase 84.1.
**Why human:** Full regression requires live data interaction.
**Status:** User confirmed end-to-end merge flow without reporting regressions.

### Gaps Summary

No gaps found. All automated checks pass and all 6 requirements (BUG-01, MERGE-01, MERGE-02, MERGE-03, MERGE-04, MERGE-05) are implemented and verified by passing unit tests.

**Note on git push:** At time of verification, `git status` shows the branch is 1 commit ahead of `origin/main`. The CLAUDE.md requires every phase to be pushed to remote. The final fix commit (`8f96125e`) is staged locally but not yet pushed. This should be addressed but is not a code gap — the implementation is complete.

**Note on pre-existing test failures:** `tests/discovery/queue.test.ts` shows 5 failures — these are pre-Phase 84.1 failures dating to Phase 19 (next/headers mock not restored in beforeEach). The 1 Phase 84.1 test in that file (MERGE-02) passes. `tests/discovery/dismiss.test.ts` has 3 pre-existing failures for the same reason. These are out of scope.

---

_Verified: 2026-05-07T18:20:00Z_
_Verifier: Claude (gsd-verifier)_
