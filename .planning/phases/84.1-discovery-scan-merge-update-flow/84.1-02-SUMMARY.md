---
phase: 84.1-discovery-scan-merge-update-flow
plan: "02"
subsystem: api
tags: [drizzle-orm, vitest, tdd, discovery, merge, team-engagement, workflow-step, arch-node]

# Dependency graph
requires:
  - phase: 84.1-00
    provides: Wave 0 RED tests for BUG-01 and MERGE-03/04/05 in approve.test.ts
  - phase: 84.1-01
    provides: entity_match/suggested_position DB columns and DiscoveryItem interface extension
provides:
  - mergeDiscoveredItem() function in approve/route.ts for team_engagement, workflow_step, arch_node
  - resolveStepPosition() function for anchor-based workflow step position computation
  - .onConflictDoNothing() on archNodes insert (BUG-01 fix)
  - Extended ApproveRequestSchema with action/entity_match/suggested_position fields
  - Merge branch in POST handler routing action=merge to mergeDiscoveredItem()
affects: [84.1-03, 84.1-04, discovery UI plans]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "mergeDiscoveredItem() uses sql template literals (not JS concat) for COALESCE-based content append — required for vitest mock compatibility (mocked sql returns {type:'sql'} object)"
    - "resolveStepPosition() uses anchor-label lookup with position+1 gap insertion — stable due to ORDER BY position ASC, id ASC on read"
    - "Merge branch in POST handler reads entity_match from request body, suggested_position from body or item.suggested_position (JSON parse fallback)"

key-files:
  created: []
  modified:
    - app/api/discovery/approve/route.ts

key-decisions:
  - "[84.1-02] mergeDiscoveredItem() uses sql template literals (drizzle sql) not JS string concat — tests assert {type:'sql'} object from mocked sql(), making sql-based approach the only way tests pass"
  - "[84.1-02] DISC-84-04 arch_node select mock updated to handle orderBy() chaining — sections query uses where()→this then orderBy()→Promise, not where()→Promise"
  - "[84.1-02] BUG-01 insert mock updated: values()→mockReturnThis(), onConflictDoNothing()→mockResolvedValue([]) — route now chains without throwing"

patterns-established:
  - "Stateful select mock pattern for multi-query test flows: queryCount variable + per-query chain method returns (where→this for non-terminal, where→Promise for terminal)"

requirements-completed: [BUG-01, MERGE-03, MERGE-04, MERGE-05]

# Metrics
duration: 25min
completed: 2026-05-06
---

# Phase 84.1 Plan 02: Approve Route Merge Logic Summary

**mergeDiscoveredItem() with team_engagement/workflow_step/arch_node merge cases, resolveStepPosition(), and onConflictDoNothing BUG-01 fix — all 15 approve tests GREEN**

## Performance

- **Duration:** 25 min
- **Started:** 2026-05-06T10:00:00Z
- **Completed:** 2026-05-06T10:25:00Z
- **Tasks:** 2 (both TDD)
- **Files modified:** 1 (route.ts in Panda-Manager; test file gitignored)

## Accomplishments
- Fixed BUG-01: `.onConflictDoNothing()` added to archNodes insert prevents duplicate-key errors on re-approve
- Implemented `mergeDiscoveredItem()` covering all three merge-capable entity types: team_engagement (COALESCE content append), workflow_step (anchor-based position insert), arch_node (COALESCE notes append)
- Implemented `resolveStepPosition()` for stable step ordering without requiring gap-free positions
- Extended Zod schema and DiscoveryItem type for action/entity_match/suggested_position fields
- Wired merge branch in POST handler with entity_match validation and suggested_position JSON parse fallback
- All 15 approve tests pass GREEN; pre-existing DISC-84-04 arch_node test mocks repaired as part of the fix

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix arch_node BUG-01 + extend Zod schema + DiscoveryItem type** - `eb65cdd5` (feat)
2. **Task 2: Add mergeDiscoveredItem() + resolveStepPosition() + wire merge branch** - `4057bddc` (feat)

_Note: TDD tasks (both tasks) had test updates in the same commit as production code since tests are gitignored._

## Files Created/Modified
- `/Users/jmiloslavsky/Documents/Panda-Manager/app/api/discovery/approve/route.ts` - Extended Zod schema, DiscoveryItem type, added onConflictDoNothing, added resolveStepPosition() and mergeDiscoveredItem(), wired merge branch in POST handler

## Decisions Made
- Used `sql` template literals (not JS concat) for content/notes append in mergeDiscoveredItem() — the Wave 0 tests assert `{ type: 'sql' }` objects from the mocked drizzle-orm sql function, so JS concat would fail the tests
- DISC-84-04 arch_node test mocks repaired (Rule 1 - Bug): select chain `where()` was returning Promise for sections query which then had `.orderBy()` chained on it — fixed by returning `this` from `where()` and resolving from `orderBy()` for the sections query
- `resolveStepPosition()` falls back to max+1 when anchor label not found — defensive for stale suggested_position values

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed select mock chain for arch_node sections query in DISC-84-04 tests**
- **Found during:** Task 1 (BUG-01 fix and mock update)
- **Issue:** DISC-84-04 arch_node tests had select mocks where `where()` returned `Promise.resolve([])` for the sections query, but the route chains `.orderBy()` after `.where()` — calling `.orderBy()` on a Promise throws TypeError, caught as an error, making `approved=0`
- **Fix:** Updated select mocks to return `this` from `where()` for sections query and resolve the Promise from `orderBy()` instead; BUG-01 test similarly restructured with named chain variable
- **Files modified:** tests/discovery/approve.test.ts (gitignored)
- **Verification:** DISC-84-04 arch_node tests now GREEN (were pre-existing failures)
- **Committed in:** eb65cdd5 (Task 1 commit; test changes not in git)

---

**Total deviations:** 1 auto-fixed (Rule 1 - Bug in test mock chain)
**Impact on plan:** Test mock fix was necessary to verify the BUG-01 fix and also unblocked 2 pre-existing DISC-84-04 arch_node test failures.

## Issues Encountered
- The RESEARCH.md in the plan recommended JS-side string concat for test compatibility, but the Wave 0 tests (ground truth in TDD) assert `{ type: 'sql' }` objects — confirming sql template literals are required

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- mergeDiscoveredItem() complete and tested — backend is ready for the Merge button UI (Plan 84.1-03)
- entity_match and suggested_position flow end-to-end through scanner → DB → approve handler
- Plans 84.1-03 and 84.1-04 can implement the QueueItemRow Merge button and UI wiring

---
*Phase: 84.1-discovery-scan-merge-update-flow*
*Completed: 2026-05-06*
