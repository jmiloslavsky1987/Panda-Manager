---
phase: 84.1-discovery-scan-merge-update-flow
plan: "04"
type: execute
wave: 5
depends_on: ["84.1-03"]
files_modified: []
autonomous: false

requirements: [BUG-01, MERGE-01, MERGE-02, MERGE-03, MERGE-04, MERGE-05]

must_haves:
  truths:
    - "All 7 Phase 84.1 tests are GREEN in the discovery test suite"
    - "Merge button appears in ReviewQueue for items that have entity_match set"
    - "Clicking Merge updates the target entity (team_engagement content appended, workflow step inserted, arch_node notes appended)"
    - "Approving an arch_node twice does not produce an error"
    - "Production build is clean"
  artifacts:
    - path: "app/api/discovery/approve/route.ts"
      provides: "mergeDiscoveredItem() + BUG-01 fix"
      contains: "mergeDiscoveredItem"
    - path: "components/QueueItemRow.tsx"
      provides: "Merge button with entity_match display"
      contains: "entity_match"
    - path: "components/ReviewQueue.tsx"
      provides: "handleMerge wired to approve POST"
      contains: "handleMerge"
  key_links:
    - from: "ReviewQueue Merge button"
      to: "/api/discovery/approve"
      via: "handleMerge POST with action:merge"
      pattern: "action.*merge"
---

<objective>
Human verification checkpoint — confirm the full merge/update flow works end-to-end in the browser,
all automated tests are GREEN, and production build passes.

Purpose: Visual and functional verification cannot be automated. The merge UX must be confirmed by
a human who can observe the Merge button appearing, confirm the entity was updated, and verify no
regressions in the existing Approve/Dismiss flow.
Output: Phase 84.1 approved and complete.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/PROJECT.md
@/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated gate — full test suite + production build</name>
  <files></files>
  <action>
    Run the complete verification sequence before presenting results to the user:

    1. Full discovery test suite:
    cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/discovery/ 2>&1 | tail -30

    2. TypeScript compilation:
    cd /Users/jmiloslavsky/Documents/Panda-Manager && npx tsc --noEmit 2>&1 | head -20

    3. Production build:
    cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run build 2>&1 | tail -20

    4. Confirm git status is clean:
    cd /Users/jmiloslavsky/Documents/Panda-Manager && git status && git log --oneline -3

    Report results clearly: how many tests pass/fail, any build errors.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Panda-Manager && npx vitest run tests/discovery/ 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>
  <done>
    All Phase 84.1 tests pass. TypeScript compiles. Production build succeeds.
    Git is clean and pushed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Merge/update flow for discovery scan review queue:
    - arch_node duplicate approve bug fixed (BUG-01) — approving same arch_node twice no longer errors
    - DISCOVERY_SYSTEM_TEMPLATE updated to instruct Claude to emit entity_match and suggested_position
    - DB migration 0049: discovery_items.entity_match + suggested_position columns added
    - approve route: new action:merge branch with mergeDiscoveredItem() for team_engagement, workflow_step, arch_node
    - ReviewQueue: Merge button appears alongside Approve for items where entity_match is set; shows entity name
  </what-built>
  <how-to-verify>
    Start dev server: cd /Users/jmiloslavsky/Documents/Panda-Manager && npm run next-only

    Test 1 — Merge button visibility:
    1. Navigate to any project's Discovery tab
    2. Run a scan (Scan for Updates)
    3. Inspect the review queue — for any item that has entity_match set, a
       "Merge into '<entity name>'" button should appear alongside Approve/Edit/Dismiss
    4. Items WITHOUT entity_match should show only Approve/Edit/Dismiss (no change)

    Test 2 — Merge button label is informative:
    5. If a team_engagement item has entity_match = "IT Team", the button should read:
       'Merge into "IT Team"'

    Test 3 — Merge action works (requires an item with entity_match):
    6. Click Merge on a team_engagement item
    7. Navigate to the Teams tab and verify the section content has the new text appended
       (not replaced — the old content should still be visible with new text below it)

    Test 4 — Arch_node duplicate fix:
    8. Approve an arch_node discovery item
    9. Run scan again so the same arch_node appears again
    10. Approve it a second time — should succeed without error (no 500 response)

    Test 5 — Regression check:
    11. Approve a non-merge item (action, risk, decision) — should work exactly as before
    12. Dismiss an item — should work exactly as before
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 7 Phase 84.1 tests GREEN in tests/discovery/ suite.
TypeScript compilation clean.
Production build passes.
Human verification approved.
</verification>

<success_criteria>
- All automated tests pass
- Merge button visible in ReviewQueue for items with entity_match
- Merge action updates the target entity (append semantics confirmed)
- arch_node duplicate approve no longer errors
- Existing Approve/Dismiss flow unaffected
- Phase 84.1 human-verified and complete
</success_criteria>

<output>
After completion, create `/Users/jmiloslavsky/Documents/Project Assistant Code/.planning/phases/84.1-discovery-scan-merge-update-flow/84.1-04-SUMMARY.md`

Also update STATE.md: mark Phase 84.1 complete. Update last_activity and stopped_at fields.
</output>
