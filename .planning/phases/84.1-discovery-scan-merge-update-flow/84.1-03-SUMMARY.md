---
phase: 84.1-discovery-scan-merge-update-flow
plan: "03"
subsystem: ui
tags: [react, typescript, discovery, merge, queue]

# Dependency graph
requires:
  - phase: 84.1-02
    provides: mergeDiscoveredItem() backend + approve route action:merge support
provides:
  - Extended DiscoveryQueueItem interface with entity_match and suggested_position fields
  - Conditional Merge button in QueueItemRow showing target entity name
  - handleMerge() in ReviewQueue wired to /api/discovery/approve with action:merge
  - onMerge prop connected to newItems and likelyDuplicates sections
affects: [84.1-04, discovery-ui, review-queue]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Conditional button rendering: {item.entity_match && onMerge && (<button>)} — renders Merge only when both data and handler exist"
    - "handleMerge mirrors handleApprove pattern with action:merge + entity_match payload"

key-files:
  created: []
  modified:
    - components/QueueItemRow.tsx
    - components/ReviewQueue.tsx

key-decisions:
  - "Dismissed items (readonly) intentionally omit onMerge — readonly prop already prevents action row rendering, double-protection is clean"
  - "Merge button placed between Approve and Edit — primary actions first, secondary edit last"
  - "onMerge is optional in QueueItemRowProps — component remains backward-compatible when not passed"

patterns-established:
  - "Entity merge UI pattern: conditional button rendering checks both item.entity_match (data) and onMerge (handler) before rendering"

requirements-completed: [MERGE-02]

# Metrics
duration: 3min
completed: 2026-05-06
---

# Phase 84.1 Plan 03: Discovery Queue Merge UI Summary

**Conditional Merge button in QueueItemRow surfacing entity_match items with emerald styling, wired through ReviewQueue handleMerge() to the approve API with action:merge**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-06T17:08:48Z
- **Completed:** 2026-05-06T17:12:14Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Extended DiscoveryQueueItem interface with `entity_match?: string` and `suggested_position?: { after: string }` fields
- Added optional `onMerge` callback to QueueItemRowProps; conditional Merge button rendered only when both `item.entity_match` and `onMerge` are set
- MERGE-02 test turned GREEN — QueueItemRow source scan confirms both `entity_match` and `onMerge` present
- Implemented `handleMerge()` in ReviewQueue that POSTs `action:merge`, `entity_match`, and `suggested_position` to `/api/discovery/approve`
- Wired `onMerge={handleMerge}` to pending items and likely-duplicates sections; dismissed (readonly) items intentionally excluded
- TypeScript clean; production build passes; git pushed to remote

## Task Commits

1. **Task 1: Extend QueueItemRow — DiscoveryQueueItem type + onMerge prop + Merge button** - `419d2a8a` (feat)
2. **Task 2: Wire handleMerge() in ReviewQueue.tsx** - `20e89758` (feat)

## Files Created/Modified

- `/Users/jmiloslavsky/Documents/Panda-Manager/components/QueueItemRow.tsx` — Added entity_match/suggested_position to DiscoveryQueueItem, onMerge to QueueItemRowProps, conditional Merge button in action row
- `/Users/jmiloslavsky/Documents/Panda-Manager/components/ReviewQueue.tsx` — Added handleMerge() function, wired onMerge prop to newItems and likelyDuplicates rows

## Decisions Made

- Dismissed items (readonly) intentionally omit onMerge — readonly prop already prevents action row rendering; double-protection is clean by design
- Merge button placed between Approve and Edit to maintain primary-action-first visual hierarchy
- onMerge is optional in QueueItemRowProps — component remains backward-compatible for all existing callers that don't pass the prop

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Pre-existing test failures in `queue.test.ts` (DISC-11, DISC-16) and `dismiss.test.ts` (DISC-15) due to `vi.resetAllMocks()` clearing the `next/headers` mock in `beforeEach`. These failures predate Plan 84.1-03 and are unrelated to this plan's changes (documented in KEY DECISIONS `[84-00]`). MERGE-02 — the gate test for this plan — is GREEN.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Merge UI fully wired: users can now click "Merge into [entity]" on any discovery item with entity_match set
- Plan 84.1-04 (end-to-end wiring/verification) can proceed — all UI and backend paths are now complete
- No blockers

---
*Phase: 84.1-discovery-scan-merge-update-flow*
*Completed: 2026-05-06*

## Self-Check: PASSED

- FOUND: /Users/jmiloslavsky/Documents/Panda-Manager/components/QueueItemRow.tsx
- FOUND: /Users/jmiloslavsky/Documents/Panda-Manager/components/ReviewQueue.tsx
- FOUND: .planning/phases/84.1-discovery-scan-merge-update-flow/84.1-03-SUMMARY.md
- FOUND commit: 419d2a8a (Task 1 - QueueItemRow extensions)
- FOUND commit: 20e89758 (Task 2 - ReviewQueue handleMerge wiring)
