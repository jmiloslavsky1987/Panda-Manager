---
phase: 58-per-project-rbac
plan: 02
type: execute
wave: 2
depends_on:
  - 58-01
files_modified:
  - bigpanda-app/app/api/projects/[projectId]/analytics/route.ts
  - bigpanda-app/app/api/projects/[projectId]/arch-nodes/[nodeId]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/arch-nodes/reorder/route.ts
  - bigpanda-app/app/api/projects/[projectId]/architecture-integrations/[id]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/architecture-integrations/route.ts
  - bigpanda-app/app/api/projects/[projectId]/artifacts/route.ts
  - bigpanda-app/app/api/projects/[projectId]/before-state/route.ts
  - bigpanda-app/app/api/projects/[projectId]/business-outcomes/[id]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/business-outcomes/route.ts
  - bigpanda-app/app/api/projects/[projectId]/chat/route.ts
  - bigpanda-app/app/api/projects/[projectId]/completeness/route.ts
  - bigpanda-app/app/api/projects/[projectId]/e2e-workflows/[workflowId]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/e2e-workflows/[workflowId]/steps/[stepId]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/e2e-workflows/[workflowId]/steps/route.ts
  - bigpanda-app/app/api/projects/[projectId]/e2e-workflows/route.ts
  - bigpanda-app/app/api/projects/[projectId]/extraction-status/route.ts
  - bigpanda-app/app/api/projects/[projectId]/focus-areas/[id]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/focus-areas/route.ts
  - bigpanda-app/app/api/projects/[projectId]/generate-plan/route.ts
  - bigpanda-app/app/api/projects/[projectId]/integrations/[integId]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/integrations/route.ts
  - bigpanda-app/app/api/projects/[projectId]/milestones/route.ts
  - bigpanda-app/app/api/projects/[projectId]/onboarding/route.ts
  - bigpanda-app/app/api/projects/[projectId]/onboarding/steps/[stepId]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/overview-metrics/route.ts
  - bigpanda-app/app/api/projects/[projectId]/risks/route.ts
  - bigpanda-app/app/api/projects/[projectId]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/sprint-summary/route.ts
  - bigpanda-app/app/api/projects/[projectId]/team-onboarding-status/[id]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/team-onboarding-status/route.ts
  - bigpanda-app/app/api/projects/[projectId]/team-pathways/[id]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/team-pathways/route.ts
  - bigpanda-app/app/api/projects/[projectId]/time-entries/[entryId]/approve/route.ts
  - bigpanda-app/app/api/projects/[projectId]/time-entries/[entryId]/reject/route.ts
  - bigpanda-app/app/api/projects/[projectId]/time-entries/[entryId]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/time-entries/bulk/route.ts
  - bigpanda-app/app/api/projects/[projectId]/time-entries/calendar-import/route.ts
  - bigpanda-app/app/api/projects/[projectId]/time-entries/export/route.ts
  - bigpanda-app/app/api/projects/[projectId]/time-entries/route.ts
  - bigpanda-app/app/api/projects/[projectId]/time-entries/submit/route.ts
  - bigpanda-app/app/api/projects/[projectId]/wbs/[itemId]/route.ts
  - bigpanda-app/app/api/projects/[projectId]/wbs/generate/route.ts
  - bigpanda-app/app/api/projects/[projectId]/wbs/reorder/route.ts
  - bigpanda-app/app/api/projects/[projectId]/wbs/route.ts
  - bigpanda-app/app/api/projects/[projectId]/weekly-focus/route.ts
  - bigpanda-app/app/api/projects/[projectId]/yaml-export/route.ts
  - bigpanda-app/app/api/projects/route.ts
  - bigpanda-app/app/api/jobs/route.ts
  - bigpanda-app/app/api/jobs/[id]/route.ts
  - bigpanda-app/app/api/jobs/trigger/route.ts
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "All 46 route handlers under [projectId]/ enforce requireProjectRole() — zero handlers left using raw requireSession() alone"
    - "New project creation (POST /api/projects) inserts creator as Admin in project_members inside the existing transaction"
    - "GET/POST /api/jobs and /api/jobs/[id] and /api/jobs/trigger require global admin role (resolveRole === 'admin')"
    - "Non-member hitting any [projectId] route gets 403"
    - "User-role member hitting an admin-only route gets 403"
  artifacts:
    - path: "bigpanda-app/app/api/projects/route.ts"
      provides: "POST adds creator to project_members inside transaction"
      contains: "projectMembers"
    - path: "bigpanda-app/app/api/jobs/route.ts"
      provides: "Global admin gate on POST"
      contains: "resolveRole"
  key_links:
    - from: "all 46 [projectId] route handlers"
      to: "bigpanda-app/lib/auth-server.ts"
      via: "import requireProjectRole"
      pattern: "requireProjectRole"
    - from: "bigpanda-app/app/api/projects/route.ts"
      to: "db transaction block"
      via: "tx.insert(projectMembers)"
      pattern: "tx.insert\\(projectMembers\\)"
---

<objective>
Migrate all 46 route handlers under `app/api/projects/[projectId]/` from `requireSession()` to `requireProjectRole()`. Add creator-insert to project creation transaction. Gate the global scheduler `/api/jobs` routes with `resolveRole === 'admin'` check.

Purpose: AUTH-05 requires ALL project route handlers to enforce role checks. A partial migration is a security hole. This plan completes the full sweep in one execution to guarantee no handlers are missed.
Output: 46 updated route handlers, updated POST /api/projects, updated /api/jobs routes.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-per-project-rbac/58-CONTEXT.md
@.planning/phases/58-per-project-rbac/58-RESEARCH.md
@.planning/phases/58-per-project-rbac/58-01-SUMMARY.md
@bigpanda-app/app/api/projects/route.ts
@bigpanda-app/app/api/projects/[projectId]/route.ts
@bigpanda-app/app/api/jobs/route.ts

<interfaces>
<!-- requireProjectRole() contract from Plan 01 -->

```typescript
// From bigpanda-app/lib/auth-server.ts (created in Plan 01)
export type ProjectRoleResult =
  | { session: NonNullable<SessionResult['session']>; redirectResponse: null; projectRole: 'admin' | 'user' }
  | { session: null; redirectResponse: NextResponse; projectRole: null };

export async function requireProjectRole(
  projectId: number,
  minRole: 'admin' | 'user' = 'user'
): Promise<ProjectRoleResult>
```

<!-- Migration pattern — BEFORE (current in all 46 handlers): -->
```typescript
const { session, redirectResponse } = await requireSession();
if (redirectResponse) return redirectResponse;
const { projectId } = await params;
const numericId = parseInt(projectId, 10);
```

<!-- AFTER pattern (general access — all 46 handlers): -->
```typescript
const { projectId } = await params;
const numericId = parseInt(projectId, 10);
if (isNaN(numericId)) return NextResponse.json({ error: 'Invalid projectId' }, { status: 400 });
const { session, redirectResponse } = await requireProjectRole(numericId, 'user');
if (redirectResponse) return redirectResponse;
```

<!-- Admin-only handlers that need minRole='admin':
  - DELETE handler in app/api/projects/[projectId]/route.ts (project delete — Phase 59 but guard now)
  - PATCH to status 'archived' in app/api/projects/[projectId]/route.ts
  Note: Most handlers use 'user' (general read/write). Only genuinely destructive ops need 'admin'.
  For this phase, use 'user' for all existing handlers. Admin gate on members API comes in Plan 04.
-->

<!-- jobs route — existing pattern to extend: -->
```typescript
// app/api/jobs/route.ts (current)
const { session, redirectResponse } = await requireSession();
if (redirectResponse) return redirectResponse;
// ADD AFTER: global admin check
if (resolveRole(session!) !== 'admin') {
  return NextResponse.json({ error: 'Forbidden: Admin role required' }, { status: 403 });
}
```

<!-- project creation transaction extension (app/api/projects/route.ts POST): -->
```typescript
// Inside the existing db.transaction(async (tx) => { ... }) block
// ADD BEFORE the final `return inserted` at end of transaction:
await tx.insert(projectMembers).values({
  project_id: inserted.id,
  user_id: session!.user.id,
  role: 'admin',
});
// return inserted  <-- this line already exists, keep it
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate all 46 [projectId] route handlers to requireProjectRole</name>
  <files>
    bigpanda-app/app/api/projects/[projectId]/analytics/route.ts,
    bigpanda-app/app/api/projects/[projectId]/arch-nodes/[nodeId]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/arch-nodes/reorder/route.ts,
    bigpanda-app/app/api/projects/[projectId]/architecture-integrations/[id]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/architecture-integrations/route.ts,
    bigpanda-app/app/api/projects/[projectId]/artifacts/route.ts,
    bigpanda-app/app/api/projects/[projectId]/before-state/route.ts,
    bigpanda-app/app/api/projects/[projectId]/business-outcomes/[id]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/business-outcomes/route.ts,
    bigpanda-app/app/api/projects/[projectId]/chat/route.ts,
    bigpanda-app/app/api/projects/[projectId]/completeness/route.ts,
    bigpanda-app/app/api/projects/[projectId]/e2e-workflows/[workflowId]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/e2e-workflows/[workflowId]/steps/[stepId]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/e2e-workflows/[workflowId]/steps/route.ts,
    bigpanda-app/app/api/projects/[projectId]/e2e-workflows/route.ts,
    bigpanda-app/app/api/projects/[projectId]/extraction-status/route.ts,
    bigpanda-app/app/api/projects/[projectId]/focus-areas/[id]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/focus-areas/route.ts,
    bigpanda-app/app/api/projects/[projectId]/generate-plan/route.ts,
    bigpanda-app/app/api/projects/[projectId]/integrations/[integId]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/integrations/route.ts,
    bigpanda-app/app/api/projects/[projectId]/milestones/route.ts,
    bigpanda-app/app/api/projects/[projectId]/onboarding/route.ts,
    bigpanda-app/app/api/projects/[projectId]/onboarding/steps/[stepId]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/overview-metrics/route.ts,
    bigpanda-app/app/api/projects/[projectId]/risks/route.ts,
    bigpanda-app/app/api/projects/[projectId]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/sprint-summary/route.ts,
    bigpanda-app/app/api/projects/[projectId]/team-onboarding-status/[id]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/team-onboarding-status/route.ts,
    bigpanda-app/app/api/projects/[projectId]/team-pathways/[id]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/team-pathways/route.ts,
    bigpanda-app/app/api/projects/[projectId]/time-entries/[entryId]/approve/route.ts,
    bigpanda-app/app/api/projects/[projectId]/time-entries/[entryId]/reject/route.ts,
    bigpanda-app/app/api/projects/[projectId]/time-entries/[entryId]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/time-entries/bulk/route.ts,
    bigpanda-app/app/api/projects/[projectId]/time-entries/calendar-import/route.ts,
    bigpanda-app/app/api/projects/[projectId]/time-entries/export/route.ts,
    bigpanda-app/app/api/projects/[projectId]/time-entries/route.ts,
    bigpanda-app/app/api/projects/[projectId]/time-entries/submit/route.ts,
    bigpanda-app/app/api/projects/[projectId]/wbs/[itemId]/route.ts,
    bigpanda-app/app/api/projects/[projectId]/wbs/generate/route.ts,
    bigpanda-app/app/api/projects/[projectId]/wbs/reorder/route.ts,
    bigpanda-app/app/api/projects/[projectId]/wbs/route.ts,
    bigpanda-app/app/api/projects/[projectId]/weekly-focus/route.ts,
    bigpanda-app/app/api/projects/[projectId]/yaml-export/route.ts
  </files>
  <action>
In EVERY one of the 46 route handler files listed above, perform this transformation for EACH exported HTTP method function (GET, POST, PUT, PATCH, DELETE):

**BEFORE (current pattern):**
```typescript
import { requireSession } from "@/lib/auth-server";
// ...
const { session, redirectResponse } = await requireSession();
if (redirectResponse) return redirectResponse;
const { projectId } = await params;
const numericId = parseInt(projectId, 10);
```

**AFTER (new pattern):**
```typescript
import { requireProjectRole } from "@/lib/auth-server";
// (remove requireSession import if no longer used in the file)
// ...
const { projectId } = await params;
const numericId = parseInt(projectId, 10);
if (isNaN(numericId)) return NextResponse.json({ error: 'Invalid projectId' }, { status: 400 });
const { session, redirectResponse } = await requireProjectRole(numericId, 'user');
if (redirectResponse) return redirectResponse;
```

**IMPORTANT — order matters:** `params` must be awaited BEFORE calling `requireProjectRole` because `numericId` is needed as its first argument. Some handlers currently await `params` AFTER `requireSession()` — reorder so params is awaited first.

**IMPORTANT — numericId guard:** If a handler already has `if (isNaN(numericId)) return ...` keep it; if not, add it before `requireProjectRole`.

**IMPORTANT — session usage:** Some handlers use `session.user.id` or `session.user.name` later in the function. The destructured `session` from `requireProjectRole` is the same shape — these usages continue to work unchanged.

**DO NOT change** the `requireSession` import in `app/api/projects/route.ts` (the non-[projectId] route) — it uses `requireSession` and that is correct. Only handlers under `[projectId]/` change.

Use `grep -r "requireSession" bigpanda-app/app/api/projects/\[projectId\]/ --include="route.ts" -l` after editing to verify zero remaining `requireSession` imports in [projectId] handlers.
  </action>
  <verify>
    <automated>cd bigpanda-app && grep -r "from \"@/lib/auth-server\"" app/api/projects/\[projectId\] --include="route.ts" | grep -v "requireProjectRole" | wc -l</automated>
  </verify>
  <done>Command output is 0 — every [projectId] route handler imports requireProjectRole (not requireSession alone). TypeScript compiles: `cd bigpanda-app && npx tsc --noEmit 2>&1 | grep -v node_modules | wc -l` returns 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update project creation and global scheduler routes</name>
  <files>
    bigpanda-app/app/api/projects/route.ts,
    bigpanda-app/app/api/jobs/route.ts,
    bigpanda-app/app/api/jobs/[id]/route.ts,
    bigpanda-app/app/api/jobs/trigger/route.ts
  </files>
  <action>
**1. Update `app/api/projects/route.ts` POST handler:**
Add `projectMembers` import from `@/db/schema` and `resolveRole` from `@/lib/auth-utils`.
Inside the existing `db.transaction(async (tx) => { ... })` block, BEFORE the `return inserted` line at the end of the transaction callback, insert:
```typescript
// Seed creator as Admin in project_members
await tx.insert(projectMembers).values({
  project_id: inserted.id,
  user_id: session!.user.id,
  role: 'admin',
});
```
The session is already available (requireSession() is called at the top of POST). The insert must be INSIDE the transaction — not after it.

**2. Update `app/api/jobs/route.ts` — both GET and POST:**
Add `resolveRole` import from `@/lib/auth-utils`.
After the existing `if (redirectResponse) return redirectResponse;` line in both GET and POST handlers, add:
```typescript
if (resolveRole(session!) !== 'admin') {
  return NextResponse.json({ error: 'Forbidden: Admin role required' }, { status: 403 });
}
```

**3. Update `app/api/jobs/[id]/route.ts`:**
Read the file first. Add the same `resolveRole` admin gate after the session check in each exported method.

**4. Update `app/api/jobs/trigger/route.ts`:**
Read the file first. Add the same `resolveRole` admin gate after the session check.

These are GLOBAL role checks (not per-project), matching the existing pattern in `app/api/settings/users/route.ts`.
  </action>
  <verify>
    <automated>cd bigpanda-app && npx vitest run && npx tsc --noEmit 2>&1 | grep -v node_modules | wc -l</automated>
  </verify>
  <done>Full test suite passes. TypeScript compiles clean. `grep -n "resolveRole" bigpanda-app/app/api/jobs/route.ts` shows the admin gate present in GET and POST handlers. `grep -n "projectMembers" bigpanda-app/app/api/projects/route.ts` shows the creator insert inside the transaction.</done>
</task>

</tasks>

<verification>
After both tasks:
```
cd bigpanda-app && npx vitest run
```
Full suite passes.

```
cd bigpanda-app && grep -r "requireSession" app/api/projects/\[projectId\] --include="route.ts" | wc -l
```
Returns 0 — no [projectId] handler still uses raw requireSession.

```
cd bigpanda-app && npx tsc --noEmit 2>&1 | grep -v node_modules
```
No TypeScript errors.
</verification>

<success_criteria>
- All 46 route handlers under [projectId]/ use requireProjectRole (zero use requireSession alone)
- POST /api/projects inserts creator into project_members inside the transaction
- /api/jobs GET, POST, [id], trigger all return 403 for non-admin global role
- Full vitest suite passes
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/58-per-project-rbac/58-02-SUMMARY.md`
</output>
