---
phase: 68-gantt-bi-directional-sync
plan: 05
type: execute
wave: 4
depends_on:
  - 68-02
  - 68-04
files_modified: []
autonomous: false

requirements:
  - DLVRY-01
  - DLVRY-02
  - DLVRY-03
  - DLVRY-04

must_haves:
  truths:
    - "Gantt tab shows WBS level-1 items as rows with a structural skeleton visible on page load"
    - "User can drag a task bar left edge to change only start date"
    - "User can drag a task bar right edge to change only end date"
    - "User can drag a milestone vertical line to reposition the date marker"
    - "User can click a date cell in the left panel to open a date picker and save a new date"
    - "WBS summary row edge drag shifts all child task dates by the same delta"
    - "After editing a milestone date in the Milestones tab, navigating to Gantt shows updated marker"
    - "After editing a task date in Task Board, navigating to Gantt shows updated bars"
    - "Production build compiles with zero errors"
  artifacts:
    - path: "bigpanda-app/components/GanttChart.tsx"
      provides: "Complete WBS row model + edge drag + milestone drag + inline date cells"
    - path: "bigpanda-app/app/customer/[id]/gantt/page.tsx"
      provides: "WBS-structured server data fetch"
    - path: "bigpanda-app/app/api/milestones/[id]/route.ts"
      provides: "date field in patchSchema"
    - path: "bigpanda-app/components/MilestonesTableClient.tsx"
      provides: "Fixed date field alignment"
  key_links:
    - from: "Milestones tab DatePickerCell"
      to: "Gantt milestone marker position"
      via: "patchMilestone({ date: v }) + router.refresh() + server component re-fetch"
      pattern: "patchMilestone.*date"
    - from: "Task Board date edit"
      to: "Gantt task bar position"
      via: "router.refresh() in TaskEditModal + server component re-fetch"
      pattern: "router.refresh"
---

<objective>
Build verification and human verification gate for all Phase 68 requirements.

Purpose: Confirm that the production build is clean, all automated tests pass, and all 4 DLVRY requirements are visually and functionally verified in the browser before the phase is marked complete.
Output: Signed-off phase with all DLVRY-01 through DLVRY-04 confirmed.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-gantt-bi-directional-sync/68-CONTEXT.md
@.planning/phases/68-gantt-bi-directional-sync/68-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build verification + full test suite</name>
  <files></files>
  <action>
    Run the full vitest suite:
    `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run 2>&1 | tail -40`

    All tests must pass GREEN. If any tests fail:
    - Check if it is a Phase 68 test file — fix the implementation (not the test)
    - Check if it is a regression in a pre-existing test — fix the regression

    Run the TypeScript check:
    `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx tsc --noEmit 2>&1 | head -30`

    Zero TypeScript errors required.

    Run the Next.js production build:
    `cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run build 2>&1 | tail -30`

    Build must exit with code 0. If build fails, diagnose and fix before proceeding to human verification.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | wc -l && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All tests GREEN. TypeScript: 0 errors. Build: exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all 4 DLVRY requirements in browser</name>
  <files></files>
  <action>
    Pause for human browser verification. The user must perform the 20-step verification sequence below and confirm all steps pass before execution continues.
  </action>
  <verify>
    <automated>MISSING — this is a human-only verification step requiring browser interaction</automated>
  </verify>
  <done>User types "approved" confirming all 20 verification steps pass in the browser.</done>
  <what-built>
    Complete Phase 68 implementation across 4 plans:
    - Plan 01: TDD RED stubs for all 4 requirements
    - Plan 02: Milestones PATCH API `date` field + MilestonesTableClient field fix
    - Plan 03: GanttChart WBS row model redesign (level-1 items as rows, milestone markers as vertical lines)
    - Plan 04: Edge drag handles + milestone drag + inline date cells
  </what-built>
  <how-to-verify>
    Start the dev server: `cd bigpanda-app && npm run next-only`
    Open a project that has WBS items and tasks.

    **DLVRY-01: Structural skeleton on page load**
    1. Navigate to a project → Delivery tab → Gantt sub-tab
    2. Verify: Each WBS level-1 item (phase name) appears as a row header
    3. Verify: WBS rows with no tasks show a greyed dashed placeholder bar
    4. Verify: Milestone markers appear as dashed vertical lines (not row containers)
    5. Verify: Tasks are nested under their WBS parent (click expand triangle to see)

    **DLVRY-02: Drag-edge date adjustment + manual date entry**
    6. Hover a task bar — verify left and right edge handles appear (narrow resize cursor area)
    7. Drag the LEFT edge of a task bar — verify only the START date changes; end date stays fixed
    8. Drag the RIGHT edge of a task bar — verify only the END date changes; start date stays fixed
    9. Drag the FULL bar (middle) — verify both dates shift together (existing behavior preserved)
    10. Drag a milestone vertical dashed line left/right — verify the marker repositions
    11. Click a Start or Due date cell in the left panel — verify date picker opens; save a date; verify bar repositions immediately

    **DLVRY-02: WBS summary bar edge drag**
    12. Expand a WBS row to see its child tasks
    13. Drag the edge of the WBS summary bar — verify ALL child task bars shift by the same delta
    14. Verify child task dates in the database updated (refresh page; bars stay in new position)

    **DLVRY-03: Date propagation to records**
    15. Drag a task bar edge to new dates — navigate to Task Board; verify task shows new dates
    16. Drag a milestone marker to a new date — navigate to Milestones tab; verify milestone shows new date

    **DLVRY-04: Reverse sync from workspace tabs**
    17. In Milestones tab, click the date cell for a milestone — change the date — save
    18. Navigate to Gantt tab — verify the milestone vertical marker is at the new date position
    19. In Task Board, open a task modal — change start or end date — save
    20. Navigate to Gantt tab — verify the task bar reflects the new dates
  </how-to-verify>
  <resume-signal>Type "approved" if all 20 verification steps pass, or describe any failures found.</resume-signal>
</task>

</tasks>

<verification>
All 20 browser verification steps passing. Full vitest suite GREEN. Production build clean.
</verification>

<success_criteria>
- Production build exits with code 0
- All vitest tests pass GREEN (no regressions)
- TypeScript build clean (0 errors)
- Human verification gate approved (all 20 steps passing)
- DLVRY-01: WBS rows visible as structural skeleton on Gantt load
- DLVRY-02: Edge drag, milestone drag, inline date cells all functional
- DLVRY-03: Date changes persist to DB and visible in workspace tabs
- DLVRY-04: Date changes in workspace tabs visible in Gantt on navigation
</success_criteria>

<output>
After completion, create `.planning/phases/68-gantt-bi-directional-sync/68-05-SUMMARY.md`
</output>
