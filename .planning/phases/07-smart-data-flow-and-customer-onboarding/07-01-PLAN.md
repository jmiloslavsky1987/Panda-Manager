---
phase: 07-smart-data-flow-and-customer-onboarding
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - server/routes/customers.test.js
autonomous: true
requirements:
  - MGT-01

must_haves:
  truths:
    - "The test runner exits 0 on customers.test.js after Wave 0 (all new tests are t.todo() stubs)"
    - "POST /api/customers stub tests describe expected request/response shape for happy path and error cases"
  artifacts:
    - path: "server/routes/customers.test.js"
      provides: "POST /api/customers test stubs (t.todo) appended to existing test file"
      contains: "POST /api/customers"
  key_links:
    - from: "server/routes/customers.test.js"
      to: "server/routes/customers.js"
      via: "supertest on loaded app"
      pattern: "request\\.post.*api/customers"
---

<objective>
Add t.todo() stub test cases for POST /api/customers to the existing customers.test.js.
The POST endpoint already exists in customers.js but has no tests yet.
Stubs make the Nyquist verify path runnable before Plan 02 fills in assertions.

Purpose: Establish automated verify gate for Wave 1 server work (MGT-01).
Output: Updated customers.test.js with POST describe block containing 3 t.todo() stubs.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-smart-data-flow-and-customer-onboarding/07-CONTEXT.md

@server/routes/customers.test.js
@server/routes/customers.js
</context>

<interfaces>
<!-- Existing POST /api/customers in customers.js (lines 46-58): -->
```js
router.post('/', asyncWrapper(async (req, res) => {
  const { customerName, projectName, goLiveDate } = req.body || {};
  if (!customerName || !customerName.trim()) {
    return res.status(422).json({ error: 'customerName is required' });
  }
  // ...builds template YAML, calls driveService.createYamlFile...
  res.status(201).json({ fileId: file.id, ...data });
}));
```

Mock in existing test file already includes:
```js
createYamlFile: mock.fn(() => Promise.resolve({ id: 'new-file-id' })),
```
This mock is already present in the `before()` block — no changes needed to mocks.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append POST /api/customers describe block with t.todo() stubs to customers.test.js</name>
  <files>server/routes/customers.test.js</files>
  <action>
Append a new `describe('POST /api/customers', ...)` block at the end of server/routes/customers.test.js (after the PUT /api/customers/:id/yaml describe block, before module.exports if any).

The block must contain exactly these 3 stubs using `it('...', { todo: true })` syntax (not t.todo — node:test uses `{ todo: true }` as second arg to `it`):

```js
// ── POST /api/customers ───────────────────────────────────────────────────────

describe('POST /api/customers', () => {
  it('returns 201 with fileId and customer data when customerName is provided', { todo: true });
  it('returns 422 with error when customerName is missing or empty', { todo: true });
  it('returns 201 and seeds customer from yamlContent when base64 YAML string is provided', { todo: true });
});
```

IMPORTANT: Do NOT modify any existing describe blocks or mocks. Only append. The file already has the `createYamlFile` mock in the `before()` setup block — no changes needed there.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/server && node --test routes/customers.test.js 2>&1 | tail -8</automated>
  </verify>
  <done>Test runner exits 0; output shows "POST /api/customers" describe block with 3 todo items; existing 4 tests still pass.</done>
</task>

</tasks>

<verification>
Run full server suite to confirm no regressions:
`cd server && node --test --test-reporter spec routes/*.test.js services/*.test.js 2>&1 | tail -10`

Expected: All previously passing tests still pass; 3 new todos reported (not failures).
</verification>

<success_criteria>
- customers.test.js has POST /api/customers describe block with 3 t.todo() stubs
- `node --test routes/customers.test.js` exits 0
- Full server test suite still exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-smart-data-flow-and-customer-onboarding/07-01-SUMMARY.md`
</output>
