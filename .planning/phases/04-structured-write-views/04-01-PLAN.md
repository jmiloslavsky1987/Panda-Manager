---
phase: 04-structured-write-views
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - server/routes/artifacts.test.js
  - server/routes/history.test.js
autonomous: true
requirements: [ART-02, ART-03, ART-04, ART-05, UPD-04]

must_haves:
  truths:
    - "Running `cd server && node --test routes/artifacts.test.js` exits 0 (all stubs pass)"
    - "Running `cd server && node --test routes/history.test.js` exits 0 (all stubs pass)"
    - "artifacts.test.js covers: POST assigns X-### ID, PATCH updates fields, PATCH 404 for unknown ID, writeYamlFile called once per mutation"
    - "history.test.js covers: POST prepends entry as history[0], POST returns 201, writeYamlFile called once"
  artifacts:
    - path: "server/routes/artifacts.test.js"
      provides: "Test stubs for ART-02, ART-03, ART-04, ART-05"
      contains: "t.todo"
    - path: "server/routes/history.test.js"
      provides: "Test stubs for UPD-04"
      contains: "t.todo"
  key_links:
    - from: "server/routes/artifacts.test.js"
      to: "server/routes/artifacts.js"
      via: "supertest + require('../index')"
      pattern: "require\\(.*index"
    - from: "server/routes/history.test.js"
      to: "server/routes/history.js"
      via: "supertest + require('../index')"
      pattern: "require\\(.*index"
---

<objective>
Create Wave 0 test stubs for the two new server routes so all subsequent tasks have a runnable verify command from the start.

Purpose: Nyquist compliance — every implementation task in this phase must have an `<automated>` verify command pointing to a file that exists. These stubs establish the test file structure using `t.todo()` so the runner exits 0 before implementation.
Output: server/routes/artifacts.test.js (4 stub tests), server/routes/history.test.js (3 stub tests)
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-structured-write-views/04-VALIDATION.md

<interfaces>
<!-- Test harness pattern established in Phase 3 — replicate exactly -->

From server/routes/actions.test.js (canonical test pattern):
```javascript
'use strict';
const { describe, it, before, mock } = require('node:test');
const assert = require('node:assert/strict');
const path = require('path');
const fs = require('fs');

const sampleYamlPath = path.join(__dirname, '..', 'fixtures', 'sample.yaml');
const sampleYamlContent = fs.readFileSync(sampleYamlPath, 'utf8');

const mockReadYamlFile = mock.fn(() => Promise.resolve(sampleYamlContent));
const mockWriteYamlFile = mock.fn(() => Promise.resolve());

before(() => {
  require.cache[require.resolve('../services/driveService')] = {
    id: require.resolve('../services/driveService'),
    filename: require.resolve('../services/driveService'),
    loaded: true,
    exports: {
      readYamlFile: mockReadYamlFile,
      writeYamlFile: mockWriteYamlFile,
      listCustomerFiles: mock.fn(() => Promise.resolve([])),
      checkDriveHealth: mock.fn(() => Promise.resolve({ ok: true })),
    },
  };
});

let request;
before(async () => {
  const supertest = require('supertest');
  const app = require('../index');
  request = supertest(app);
});

const FAKE_FILE_ID = 'fake-file-id-123';
```

From server/fixtures/sample.yaml (IDs relevant to these tests):
- artifacts: max is X-001 → assignNextId('X', artifacts) must return 'X-002'
- history: 1 existing entry with week_ending: "2026-03-07" → unshift puts new entry at [0]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create artifacts.test.js with t.todo() stubs</name>
  <files>server/routes/artifacts.test.js</files>
  <action>
Create `server/routes/artifacts.test.js` using the canonical test pattern from actions.test.js (same mock setup, same driveService injection, same supertest pattern). All test bodies must use `it('description', (t) => { t.todo(); })` so the runner exits 0 immediately.

Include these four describe/it blocks:

describe('POST /api/customers/:id/artifacts')
  it('returns 201 with new artifact including server-assigned X-002 id', (t) => { t.todo(); })
  it('initializes related_topics: [] and linked_actions: [] in new artifact', (t) => { t.todo(); })
  it('calls writeYamlFile exactly once', (t) => { t.todo(); })

describe('PATCH /api/customers/:id/artifacts/:artifactId')
  it('returns 200 and updates artifact fields', (t) => { t.todo(); })
  it('auto-updates last_updated to today', (t) => { t.todo(); })
  it('returns 404 for unknown artifactId', (t) => { t.todo(); })
  it('calls writeYamlFile exactly once', (t) => { t.todo(); })

The mock setup must inject into require.cache BEFORE requiring app — use the same `before()` pattern from actions.test.js verbatim. Use `FAKE_FILE_ID = 'fake-file-id-123'`.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/artifacts.test.js</automated>
  </verify>
  <done>Runner exits 0; all 7 tests show as TODO (not failed); file exists at server/routes/artifacts.test.js</done>
</task>

<task type="auto">
  <name>Task 2: Create history.test.js with t.todo() stubs</name>
  <files>server/routes/history.test.js</files>
  <action>
Create `server/routes/history.test.js` using the identical canonical mock pattern. All test bodies use `t.todo()`.

Include these describe/it blocks:

describe('POST /api/customers/:id/history')
  it('returns 201 with the submitted entry', (t) => { t.todo(); })
  it('prepends entry so history[0] is the new entry (not push)', (t) => { t.todo(); })
  it('calls writeYamlFile exactly once', (t) => { t.todo(); })

Same driveService mock injection, same `FAKE_FILE_ID = 'fake-file-id-123'`.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/history.test.js</automated>
  </verify>
  <done>Runner exits 0; all 3 tests show as TODO; file exists at server/routes/history.test.js</done>
</task>

</tasks>

<verification>
Both test files run without errors:
`cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/artifacts.test.js routes/history.test.js`

Full suite still green (no regressions):
`cd "/Users/jmiloslavsky/Documents/Project Assistant Code/server" && node --test routes/*.test.js`
</verification>

<success_criteria>
- server/routes/artifacts.test.js exists with 7 t.todo() stubs covering ART-02, ART-03, ART-04, ART-05
- server/routes/history.test.js exists with 3 t.todo() stubs covering UPD-04
- Both files use identical mock injection pattern from actions.test.js
- `node --test routes/artifacts.test.js routes/history.test.js` exits 0
- Full suite `node --test routes/*.test.js` still green (65+ tests pass)
</success_criteria>

<output>
After completion, create `.planning/phases/04-structured-write-views/04-01-SUMMARY.md`
</output>
