---
phase: 67-delivery-tab-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bigpanda-app/components/ActionsTableClient.tsx
  - bigpanda-app/components/RisksTableClient.tsx
  - bigpanda-app/components/MilestonesTableClient.tsx
  - bigpanda-app/components/AddDecisionModal.tsx
autonomous: true
requirements:
  - DLVRY-07
  - DLVRY-08
  - DLVRY-09
  - DLVRY-10

must_haves:
  truths:
    - "Actions table has no ID column and no Source column"
    - "Risks table has no ID column"
    - "Milestones table has no ID column"
    - "Add Decision form labels and placeholders reflect operational impact scope"
  artifacts:
    - path: "bigpanda-app/components/ActionsTableClient.tsx"
      provides: "Actions table without ID/Source columns"
      contains: "colSpan={5}"
    - path: "bigpanda-app/components/RisksTableClient.tsx"
      provides: "Risks table without ID column"
      contains: "colSpan={6}"
    - path: "bigpanda-app/components/MilestonesTableClient.tsx"
      provides: "Milestones table without ID column"
      contains: "colSpan={6}"
    - path: "bigpanda-app/components/AddDecisionModal.tsx"
      provides: "Decision form with operational impact labels"
      contains: "Decision / Action"
  key_links:
    - from: "ActionsTableClient.tsx TableHeader"
      to: "ActionsTableClient.tsx TableBody colSpan"
      via: "column count must match"
      pattern: "colSpan=\\{5\\}"
    - from: "RisksTableClient.tsx TableHeader"
      to: "RisksTableClient.tsx TableBody colSpan"
      via: "column count must match"
      pattern: "colSpan=\\{6\\}"
---

<objective>
Remove noisy ID and Source columns from delivery tabs, and update Decisions entry form labels to scope input toward operational impact documentation.

Purpose: Reduce visual noise in the delivery tables; keep only actionable columns. Scope the Decisions modal to its intended use (operational transformation events).
Output: Three modified table components with columns removed; updated AddDecisionModal labels and placeholders.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-delivery-tab-cleanup/67-CONTEXT.md
</context>

<interfaces>
<!-- Extracted from source files for executor context. No codebase exploration needed. -->

From bigpanda-app/components/ActionsTableClient.tsx (TableHeader at ~line 289):
```tsx
<TableHead className="w-12">{/* Checkbox */}</TableHead>
<TableHead className="w-20">ID</TableHead>          {/* DELETE this TableHead */}
<TableHead>Description</TableHead>
<TableHead className="w-40">Owner</TableHead>
<TableHead className="w-32">Due Date</TableHead>
<TableHead className="w-32">Status</TableHead>
<TableHead className="w-32">Source</TableHead>      {/* DELETE this TableHead */}
```

From bigpanda-app/components/ActionsTableClient.tsx (TableBody row at ~line 323):
```tsx
<TableCell>                                               {/* Checkbox */}
<TableCell className="font-mono text-xs text-zinc-400">  {/* ID — DELETE */}
  {action.external_id}
</TableCell>
<TableCell>                                               {/* Description */}
...
<TableCell>                                               {/* Source — DELETE */}
  <SourceBadge ... />
</TableCell>
```
Empty state: `<TableCell colSpan={7}` → change to `colSpan={5}` (removed 2 columns)

From bigpanda-app/components/RisksTableClient.tsx (TableHeader at ~line 303):
```tsx
<TableHead className="w-[50px]">{/* Checkbox */}</TableHead>
<TableHead className="w-[130px]">ID</TableHead>      {/* DELETE this TableHead */}
<TableHead>Description</TableHead>
...
```
Body row at ~line 342:
```tsx
<TableCell className="font-mono text-xs text-zinc-500">{risk.external_id}</TableCell>  {/* DELETE */}
```
Empty state: `<TableCell colSpan={7}` → change to `colSpan={6}` (removed 1 column)

From bigpanda-app/components/MilestonesTableClient.tsx (TableHeader at ~line 299):
```tsx
<TableHead className="w-12">{/* Checkbox */}</TableHead>
<TableHead className="w-[130px]">ID</TableHead>      {/* DELETE this TableHead */}
<TableHead>Name</TableHead>
...
```
Body row at ~line 339:
```tsx
<TableCell className="font-mono text-xs text-zinc-500">{m.external_id}</TableCell>  {/* DELETE */}
```
Empty state: `<TableCell colSpan={7}` → change to `colSpan={6}` (removed 1 column)

From bigpanda-app/components/AddDecisionModal.tsx (lines ~77-99):
```tsx
<label ...>Decision <span ...>*</span></label>
<textarea placeholder="Describe the decision made..." />
...
<label ...>Context <span ...>(optional)</span></label>
<textarea placeholder="Why was this decision made? What alternatives were considered?" />
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove ID/Source columns from Actions, Risks, Milestones tables</name>
  <files>bigpanda-app/components/ActionsTableClient.tsx, bigpanda-app/components/RisksTableClient.tsx, bigpanda-app/components/MilestonesTableClient.tsx</files>
  <action>
    Permanently remove columns — no toggle UI. Simple JSX deletion only.

    ActionsTableClient.tsx:
    - Delete `<TableHead className="w-20">ID</TableHead>` (line ~297)
    - Delete the `{/* ID */}` comment + `<TableCell className="font-mono text-xs text-zinc-400">{action.external_id}</TableCell>` (line ~330-333)
    - Delete `<TableHead className="w-32">Source</TableHead>` (line ~302)
    - Delete the `{/* Source */}` comment + `<TableCell><SourceBadge .../></TableCell>` (lines ~373-380)
    - Update empty-state `colSpan={7}` to `colSpan={5}` (two columns removed)

    RisksTableClient.tsx:
    - Delete `<TableHead className="w-[130px]">ID</TableHead>` (line ~310)
    - Delete `<TableCell className="font-mono text-xs text-zinc-500">{risk.external_id}</TableCell>` (line ~342)
    - Update empty-state `colSpan={7}` to `colSpan={6}` (one column removed)

    MilestonesTableClient.tsx:
    - Delete `<TableHead className="w-[130px]">ID</TableHead>` (line ~306)
    - Delete `<TableCell className="font-mono text-xs text-zinc-500">{m.external_id}</TableCell>` (line ~339)
    - Update empty-state `colSpan={7}` to `colSpan={6}` (one column removed)

    Note: external_id field is removed from display only. The column remains in the DB and query results — only the table rendering changes. Do NOT remove external_id from queries or types.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>No TypeScript errors. All three files have no ID TableHead or external_id TableCell. colSpan values match remaining column counts (5 for Actions, 6 for Risks, 6 for Milestones).</done>
</task>

<task type="auto">
  <name>Task 2: Update Decisions entry form labels and placeholders (DLVRY-10)</name>
  <files>bigpanda-app/components/AddDecisionModal.tsx</files>
  <action>
    Update label text and placeholder text in AddDecisionModal. No schema change, no new fields.

    Changes:
    1. Label "Decision" → "Decision / Action" (keep the required asterisk)
    2. Placeholder for decision textarea → "What was decided or agreed operationally? (e.g. routing rule change, automation enabled, process formalized)"
    3. Label "Context" → "Operational Impact / Rationale" (keep the "(optional)" suffix)
    4. Placeholder for context textarea → "Why this decision? What does it affect operationally? What was the alternative?"

    Do NOT change DialogTitle, form submission logic, field names, or any other part of the component.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript clean. AddDecisionModal shows "Decision / Action" label and "Operational Impact / Rationale" label with updated placeholders. No other changes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Actions table renders 5 columns (checkbox, description, owner, due date, status) — no ID, no Source
3. Risks table renders 6 columns (checkbox, description, severity, owner, status, mitigation) — no ID
4. Milestones table renders 6 columns (checkbox, name, status, target/date, owner, notes) — no ID
5. Add Decision modal shows "Decision / Action" label and "Operational Impact / Rationale" label
</verification>

<success_criteria>
- DLVRY-07: Actions tab ID and Source columns absent from rendered table
- DLVRY-08: Risks tab ID column absent from rendered table
- DLVRY-09: Milestones tab ID column absent from rendered table
- DLVRY-10: Add Decision form scoped to operational impact (updated labels/placeholders)
- Build is clean (TypeScript passes)
</success_criteria>

<output>
After completion, create `.planning/phases/67-delivery-tab-cleanup/67-01-SUMMARY.md` following the summary template.
</output>
