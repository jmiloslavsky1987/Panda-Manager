---
phase: 67-delivery-tab-cleanup
plan: "04"
type: execute
wave: 2
depends_on:
  - "67-01"
  - "67-02"
  - "67-03"
files_modified: []
autonomous: false
requirements:
  - DLVRY-05
  - DLVRY-06
  - DLVRY-07
  - DLVRY-08
  - DLVRY-09
  - DLVRY-10
  - TEAM-01
  - TEAM-02

must_haves:
  truths:
    - "All 8 phase requirements are visually confirmed working in the browser"
    - "Build is clean before verification"
  artifacts:
    - path: "bigpanda-app/components/ActionsTableClient.tsx"
      provides: "Verified: no ID or Source column visible"
    - path: "bigpanda-app/app/customer/[id]/tasks/page.tsx"
      provides: "Verified: Generate Plan button above task list"
    - path: "bigpanda-app/components/StakeholderEditModal.tsx"
      provides: "Verified: Delete and Move buttons in edit modal"
  key_links:
    - from: "Human verification"
      to: "All 8 requirements"
      via: "Browser visual confirmation"
      pattern: "approved"
---

<objective>
Run a production build and gate all 8 Phase 67 requirements through human browser verification.

Purpose: Confirm every delivery tab cleanup change is working correctly before marking Phase 67 complete.
Output: All requirements approved by user; Phase 67 closed.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-delivery-tab-cleanup/67-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build verification</name>
  <files></files>
  <action>
    Run the Next.js production build to confirm no build errors across all changes from Plans 01-03.

    ```bash
    cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npm run build 2>&1 | tail -30
    ```

    If build fails:
    - Fix TypeScript/compilation errors
    - Re-run build until clean
    - Do NOT proceed to checkpoint until build passes
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js build completes with no errors. "Build complete" or equivalent success output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human browser verification — all 8 Phase 67 requirements</name>
  <files></files>
  <action>Pause for human to verify all 8 requirements in the browser.</action>
  <verify>
    <automated>MISSING — human verification required, no automated command</automated>
  </verify>
  <done>User confirms all 8 verification steps pass and types "approved".</done>
  <what-built>
    Phase 67 changes across 4 plans:
    - DLVRY-07: Actions tab — ID and Source columns removed
    - DLVRY-08: Risks tab — ID column removed
    - DLVRY-09: Milestones tab — ID column removed
    - DLVRY-10: Add Decision form — updated labels ("Decision / Action", "Operational Impact / Rationale")
    - DLVRY-05: Plan sub-tab removed from Delivery navigation; Generate Plan button on Task Board
    - DLVRY-06: Generate Plan de-duplicates against existing tasks (greyed-out "Already exists"); committed tasks write to WBS tree
    - TEAM-01: Move button in Stakeholder edit modal (toggles BigPanda/customer section)
    - TEAM-02: Delete button in Stakeholder edit modal (no confirmation dialog)
  </what-built>
  <how-to-verify>
    Start the dev server: `cd bigpanda-app && npm run next-only`
    Open any project with existing data.

    Step 1 — DLVRY-07 (Actions): Go to Delivery > Actions tab. Confirm no "ID" column and no "Source" column visible in the table header or rows.

    Step 2 — DLVRY-08 (Risks): Go to Delivery > Risks tab. Confirm no "ID" column visible.

    Step 3 — DLVRY-09 (Milestones): Go to Delivery > Milestones tab. Confirm no "ID" column visible.

    Step 4 — DLVRY-10 (Decisions): Go to Delivery > Decisions tab. Click "Add Decision". Confirm the modal shows "Decision / Action" label and "Operational Impact / Rationale" label (not the old "Decision" and "Context" labels).

    Step 5 — DLVRY-05 (Plan tab removed): Confirm "Plan" is NOT in the Delivery sub-tab navigation. Confirm WBS, Task Board, Gantt, Actions, Risks, Milestones, Decisions are still present.

    Step 6 — DLVRY-05 (Generate Plan on Task Board): Go to Delivery > Task Board. Confirm "Generate plan" button appears above the task board. Click it and confirm plan generation works (or shows loading spinner).

    Step 7 — DLVRY-06 (De-dup + WBS): Generate a plan. Confirm any tasks with titles similar to existing tasks appear greyed-out with "Already exists" label and are unchecked by default. Select some non-duplicate tasks and commit them. Navigate to WBS tab and confirm committed tasks appear as level-3 items under the appropriate parent (if the parent WBS node exists).

    Step 8 — TEAM-01 + TEAM-02 (Stakeholder operations): Go to Team > Stakeholders. Open an existing stakeholder's edit modal. Confirm "Delete" (red button, left-aligned) and "Move to [section]" buttons appear. Test Delete: click Delete, confirm stakeholder disappears without a confirmation dialog. Test Move: open another stakeholder, click Move, confirm the stakeholder moves to the other section.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps pass, or describe specific issues found.</resume-signal>
</task>

</tasks>

<verification>
Build passes. All 8 requirements confirmed passing by user in browser.
</verification>

<success_criteria>
All 8 Phase 67 requirements confirmed passing:
- DLVRY-05: Plan tab removed, Generate Plan on Task Board
- DLVRY-06: De-dup display + WBS commit
- DLVRY-07: Actions ID/Source columns hidden
- DLVRY-08: Risks ID column hidden
- DLVRY-09: Milestones ID column hidden
- DLVRY-10: Decisions form scoped to operational impact
- TEAM-01: Stakeholder move between sections
- TEAM-02: Stakeholder delete (no confirmation)
</success_criteria>

<output>
After completion, create `.planning/phases/67-delivery-tab-cleanup/67-04-SUMMARY.md` following the summary template.
</output>
