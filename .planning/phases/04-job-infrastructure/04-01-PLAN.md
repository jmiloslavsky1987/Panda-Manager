---
phase: 04-job-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/phase4.spec.ts
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05, SCHED-06, SCHED-07, SCHED-08]

must_haves:
  truths:
    - "All 8 SCHED-xx E2E tests exist and fail (RED) before any implementation starts"
    - "Each test is identifiable by grep on its requirement ID"
  artifacts:
    - path: "tests/e2e/phase4.spec.ts"
      provides: "Phase 4 E2E test stubs, one per SCHED requirement"
      min_lines: 60
  key_links:
    - from: "tests/e2e/phase4.spec.ts"
      to: "/settings"
      via: "playwright page.goto"
      pattern: "goto.*settings"
---

<objective>
Wave 0: Create Phase 4 E2E test stubs — one test per SCHED requirement, all RED.

Purpose: Establishes the acceptance criteria before implementation begins. Follows the same RED baseline pattern used in Phase 2 (02-01) and Phase 3 (03-01).
Output: tests/e2e/phase4.spec.ts with 8 failing tests.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-job-infrastructure/04-CONTEXT.md
@.planning/phases/04-job-infrastructure/04-VALIDATION.md
</context>

<interfaces>
<!-- Pattern from Phase 2/3 Wave 0 stubs. Each test:
  - Has the requirement ID in its title (e.g. "SCHED-01")
  - Uses expect(false, 'stub').toBe(true) as the FIRST line — stays RED without a server
  - Groups related tests in describe blocks
-->

<!-- From playwright.config.ts (existing):
  webServer: { command: 'cd bigpanda-app && npm run dev', url: 'http://localhost:3000', ... }
  baseURL: 'http://localhost:3000'
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 4 E2E test stubs</name>
  <files>tests/e2e/phase4.spec.ts</files>
  <action>
Create tests/e2e/phase4.spec.ts with 8 tests following the exact RED stub pattern from phase2.spec.ts and phase3.spec.ts.

Each test title MUST contain the requirement ID exactly (e.g., "SCHED-01") — grep depends on this.
First line inside each test MUST be: `expect(false, 'stub').toBe(true)` — this keeps it RED even without a server running.

Test structure:

```typescript
import { test, expect } from '@playwright/test';

test.describe('Phase 4 — Job Infrastructure', () => {

  test('SCHED-01: BullMQ worker process starts alongside Next.js and job queue is accessible', async ({ page }) => {
    expect(false, 'stub').toBe(true);
    // Will verify: /api/job-runs returns 200; job_runs table exists in DB
  });

  test('SCHED-02: Morning briefing scheduler (action-sync) is registered in BullMQ', async ({ page }) => {
    expect(false, 'stub').toBe(true);
    // Will verify: Jobs tab shows 'action-sync' row with schedule '0 8 * * *'
  });

  test('SCHED-03: Health refresh scheduler (health-refresh) is registered in BullMQ', async ({ page }) => {
    expect(false, 'stub').toBe(true);
    // Will verify: Jobs tab shows 'health-refresh' row
  });

  test('SCHED-04: Slack sweep scheduler (context-updater) is registered in BullMQ', async ({ page }) => {
    expect(false, 'stub').toBe(true);
    // Will verify: Jobs tab shows 'context-updater' row with schedule '0 9 * * *'
  });

  test('SCHED-05: Tracker weekly scheduler (gantt-snapshot) is registered in BullMQ', async ({ page }) => {
    expect(false, 'stub').toBe(true);
    // Will verify: Jobs tab shows 'gantt-snapshot' row with schedule '0 7 * * 1'
  });

  test('SCHED-06: Weekly status scheduler (weekly-briefing) is registered in BullMQ', async ({ page }) => {
    expect(false, 'stub').toBe(true);
    // Will verify: Jobs tab shows 'weekly-briefing' row with schedule '0 16 * * 4'
  });

  test('SCHED-07: Biggy briefing scheduler (risk-monitor) is registered in BullMQ', async ({ page }) => {
    expect(false, 'stub').toBe(true);
    // Will verify: Jobs tab shows 'risk-monitor' row with schedule '0 9 * * 5'
  });

  test('SCHED-08: /settings Jobs tab renders all 6 jobs; Trigger Now button enqueues a manual run visible in job_runs', async ({ page }) => {
    expect(false, 'stub').toBe(true);
    // Will verify: page.goto('/settings'); 6 job rows visible; click Trigger Now; new job_runs row appears
  });

});
```

Note: The 6 job names in Phase 4 context are: action-sync, health-refresh, weekly-briefing, context-updater, gantt-snapshot, risk-monitor. Map these to SCHED-02 through SCHED-07 as shown above. The comments map requirements to the actual job names used in worker/scheduler.ts.
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code && npx playwright test tests/e2e/phase4.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>tests/e2e/phase4.spec.ts exists with 8 tests, all failing (RED) with "stub" message. Running the playwright command shows 8 failed tests, none pass.</done>
</task>

</tasks>

<verification>
Run: `npx playwright test tests/e2e/phase4.spec.ts --reporter=list`
Expected: 8 tests FAILED (all stubs)
</verification>

<success_criteria>
- tests/e2e/phase4.spec.ts exists with 8 tests
- All 8 tests are RED (fail immediately on `expect(false, 'stub').toBe(true)`)
- Each test title contains exactly: SCHED-01, SCHED-02, ..., SCHED-08
- `npx playwright test tests/e2e/phase4.spec.ts --grep "SCHED-08"` runs exactly 1 test and it fails
</success_criteria>

<output>
After completion, create `.planning/phases/04-job-infrastructure/04-01-SUMMARY.md`
</output>
