---
phase: 07-file-generation-remaining-skills
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/phase7.spec.ts
  - bigpanda-app/lib/file-gen/__tests__/pptx-generator.test.ts
  - bigpanda-app/lib/file-gen/__tests__/html-generator.test.ts
  - bigpanda-app/worker/jobs/__tests__/skill-run-file.test.ts
  - bigpanda-app/app/api/__tests__/ai-plan.test.ts
  - bigpanda-app/app/api/__tests__/sprint-summary.test.ts
autonomous: true
requirements: [SKILL-05, SKILL-06, SKILL-07, SKILL-08, PLAN-12, PLAN-13]

must_haves:
  truths:
    - "All 6 E2E stubs exist in tests/e2e/phase7.spec.ts and fail visibly (RED) without server running"
    - "All 5 vitest unit stub files exist in bigpanda-app and fail visibly (RED)"
    - "vitest is installed in bigpanda-app as a dev dependency"
  artifacts:
    - path: "tests/e2e/phase7.spec.ts"
      provides: "Playwright E2E stubs for SKILL-05, SKILL-06, SKILL-07, SKILL-08, PLAN-12, PLAN-13"
    - path: "bigpanda-app/lib/file-gen/__tests__/pptx-generator.test.ts"
      provides: "vitest stubs for PPTX generation"
    - path: "bigpanda-app/lib/file-gen/__tests__/html-generator.test.ts"
      provides: "vitest stubs for HTML generation"
    - path: "bigpanda-app/worker/jobs/__tests__/skill-run-file.test.ts"
      provides: "vitest stubs for skill-run.ts file gen extension"
    - path: "bigpanda-app/app/api/__tests__/ai-plan.test.ts"
      provides: "vitest stubs for PLAN-12 API"
    - path: "bigpanda-app/app/api/__tests__/sprint-summary.test.ts"
      provides: "vitest stubs for PLAN-13 API"
  key_links:
    - from: "tests/e2e/phase7.spec.ts"
      to: "implementation plans 07-02 through 07-06"
      via: "--grep flags matching requirement IDs"
      pattern: "SKILL-05|SKILL-06|SKILL-07|SKILL-08|PLAN-12|PLAN-13"
---

<objective>
Wave 0: Establish all test stubs (Playwright E2E + vitest unit) that subsequent implementation plans will turn GREEN. Install vitest as a dev dependency in bigpanda-app.

Purpose: RED baseline ensures every implementation plan has a concrete pass/fail target before any production code is written.
Output: 1 Playwright E2E file + 5 vitest unit stub files + vitest installed.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-file-generation-remaining-skills/07-CONTEXT.md
@.planning/phases/07-file-generation-remaining-skills/07-RESEARCH.md
@.planning/phases/07-file-generation-remaining-skills/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest + create Playwright E2E stubs</name>
  <files>bigpanda-app/package.json, tests/e2e/phase7.spec.ts</files>
  <action>
Install vitest in bigpanda-app as a dev dependency. Use --no-package-lock (established project pattern — invalid esbuild semver in package-lock.json):

```
cd bigpanda-app && npm install --save-dev vitest --no-package-lock
```

Then create `tests/e2e/phase7.spec.ts` at the project root with 6 test stubs. Use `expect(false, 'stub').toBe(true)` as the FIRST line in each test body — keeps tests RED and visible in report (established pattern from 02-01, 03-01, 04-01, 05-01).

Test structure:
```typescript
import { test, expect } from '@playwright/test';

test('SKILL-05: ELT External Status skill card is visible and enabled in Skills tab', async ({ page }) => {
  expect(false, 'stub').toBe(true);
  // Navigate to any project's Skills tab
  // Verify 'ELT External Status' card is visible and NOT grayed out (i.e., in WIRED_SKILLS)
});

test('SKILL-06: ELT Internal Status skill card is visible and enabled in Skills tab', async ({ page }) => {
  expect(false, 'stub').toBe(true);
  // Navigate to any project's Skills tab
  // Verify 'ELT Internal Status' card is visible and NOT grayed out
});

test('SKILL-07: Team Engagement Map skill card is visible and enabled in Skills tab', async ({ page }) => {
  expect(false, 'stub').toBe(true);
  // Navigate to any project's Skills tab
  // Verify 'Team Engagement Map' card is visible and NOT grayed out
});

test('SKILL-08: Workflow Diagram skill card is visible and enabled in Skills tab', async ({ page }) => {
  expect(false, 'stub').toBe(true);
  // Navigate to any project's Skills tab
  // Verify 'Workflow Diagram' card is visible and NOT grayed out
});

test('PLAN-12: Generate plan button visible on Plan tab; proposed tasks panel renders on click', async ({ page }) => {
  expect(false, 'stub').toBe(true);
  // Navigate to any project's Plan tab (board)
  // Verify 'Generate plan' button is visible
  // Click it; verify AiPlanPanel / proposed tasks area appears
});

test('PLAN-13: Sprint summary panel visible at top of Plan tab layout', async ({ page }) => {
  expect(false, 'stub').toBe(true);
  // Navigate to any project's Plan tab
  // Verify SprintSummaryPanel is rendered (data-testid='sprint-summary-panel')
});
```

Use `assert-if-present` pattern for Anthropic API-dependent assertions: structural UI assertions (card visible, button visible, panel renders) always pass without API key.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/phase7.spec.ts --reporter=line 2>&1 | tail -15</automated>
  </verify>
  <done>tests/e2e/phase7.spec.ts has 6 tests; all 6 fail with 'stub' message when run. vitest appears in bigpanda-app/package.json devDependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create vitest unit stub files</name>
  <files>
    bigpanda-app/lib/file-gen/__tests__/pptx-generator.test.ts,
    bigpanda-app/lib/file-gen/__tests__/html-generator.test.ts,
    bigpanda-app/worker/jobs/__tests__/skill-run-file.test.ts,
    bigpanda-app/app/api/__tests__/ai-plan.test.ts,
    bigpanda-app/app/api/__tests__/sprint-summary.test.ts
  </files>
  <action>
Create 5 vitest unit stub files. Each stub uses `assert.fail('stub')` as the first line (consistent with project's node:test pattern, but adapted for vitest syntax — use `expect(false, 'stub').toBe(true)` for vitest).

Create directories as needed with `mkdir -p`.

**bigpanda-app/lib/file-gen/__tests__/pptx-generator.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('pptx-generator', () => {
  it('SKILL-05: generatePptx writes a file to disk given valid EltSlideJson', () => {
    expect(false, 'stub').toBe(true);
  });
  it('SKILL-06: generatePptx for internal status uses direct tone JSON structure', () => {
    expect(false, 'stub').toBe(true);
  });
  it('strips markdown fences from Claude output before JSON.parse', () => {
    expect(false, 'stub').toBe(true);
  });
});
```

**bigpanda-app/lib/file-gen/__tests__/html-generator.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('html-generator', () => {
  it('SKILL-07: generateHtml writes .html file to disk at expected path', () => {
    expect(false, 'stub').toBe(true);
  });
  it('SKILL-08: generateHtml for workflow-diagram creates per-customer directory if absent', () => {
    expect(false, 'stub').toBe(true);
  });
});
```

**bigpanda-app/worker/jobs/__tests__/skill-run-file.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('skill-run file generation extension', () => {
  it('SKILL-05: FILE_SKILLS set includes elt-external-status', () => {
    expect(false, 'stub').toBe(true);
  });
  it('outputs row sets filepath and filename when FILE_SKILLS match', () => {
    expect(false, 'stub').toBe(true);
  });
});
```

**bigpanda-app/app/api/__tests__/ai-plan.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('generate-plan API route', () => {
  it('PLAN-12: POST /api/projects/[id]/generate-plan returns 200 with tasks array', () => {
    expect(false, 'stub').toBe(true);
  });
  it('proposed tasks are not written to DB until commit', () => {
    expect(false, 'stub').toBe(true);
  });
});
```

**bigpanda-app/app/api/__tests__/sprint-summary.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('sprint-summary API route', () => {
  it('PLAN-13: GET /api/projects/[id]/sprint-summary returns stored text or empty', () => {
    expect(false, 'stub').toBe(true);
  });
  it('POST /api/projects/[id]/sprint-summary triggers new Claude call and stores result', () => {
    expect(false, 'stub').toBe(true);
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>5 vitest stub files exist under their respective __tests__ directories. Running vitest shows all stubs failing with the 'stub' message. Zero passing tests (all RED).</done>
</task>

</tasks>

<verification>
- `tests/e2e/phase7.spec.ts` exists with 6 Playwright stubs, all RED
- All 5 vitest stub files exist under bigpanda-app directories
- `cd bigpanda-app && npx vitest run` exits non-zero (stubs fail)
- `npx playwright test tests/e2e/phase7.spec.ts` exits non-zero (stubs fail)
- `vitest` appears in bigpanda-app/package.json devDependencies
</verification>

<success_criteria>
Wave 0 RED baseline established: 6 Playwright stubs + 5 vitest stubs all failing. Every implementation plan (07-02 through 07-06) has a concrete test target to turn GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/07-file-generation-remaining-skills/07-01-SUMMARY.md`
</output>
