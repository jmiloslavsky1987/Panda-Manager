---
phase: 07-file-generation-remaining-skills
plan: 07
type: execute
wave: 4
depends_on: [07-05, 07-06]
files_modified:
  - tests/e2e/phase7.spec.ts
autonomous: false
requirements: [SKILL-05, SKILL-06, SKILL-07, SKILL-08, PLAN-12, PLAN-13]

must_haves:
  truths:
    - "All 6 Playwright E2E tests for phase 7 pass GREEN (SKILL-05, SKILL-06, SKILL-07, SKILL-08, PLAN-12, PLAN-13)"
    - "Skills tab shows all 4 new skills as enabled (not grayed out)"
    - "Plan tab shows 'Generate plan' button and SprintSummaryPanel"
    - "Running an ELT skill produces a file on disk and shows 'Open in app' button on completion"
  artifacts:
    - path: "tests/e2e/phase7.spec.ts"
      provides: "6 active Playwright tests, all GREEN"
  key_links:
    - from: "tests/e2e/phase7.spec.ts"
      to: "bigpanda-app/components/SkillsTabClient.tsx"
      via: "WIRED_SKILLS check — skill card not grayed out"
      pattern: "SKILL-05|SKILL-06|SKILL-07|SKILL-08"
    - from: "tests/e2e/phase7.spec.ts"
      to: "bigpanda-app/components/AiPlanPanel.tsx"
      via: "data-testid='generate-plan-btn'"
      pattern: "PLAN-12"
---

<objective>
Activate all 6 Playwright E2E stubs by replacing `expect(false, 'stub').toBe(true)` with real assertions, run the full suite to GREEN, then pause for human verification of the file outputs and UI.

Purpose: Confirms the entire Phase 7 implementation is functionally correct — structural UI passes without API key, file generation verified by human (PowerPoint open without corruption, HTML iframe rendering).
Output: tests/e2e/phase7.spec.ts with 6 active tests + human sign-off.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-file-generation-remaining-skills/07-CONTEXT.md
@.planning/phases/07-file-generation-remaining-skills/07-VALIDATION.md
@.planning/phases/07-file-generation-remaining-skills/07-05-SUMMARY.md
@.planning/phases/07-file-generation-remaining-skills/07-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate E2E tests — replace stubs with real assertions</name>
  <files>tests/e2e/phase7.spec.ts</files>
  <action>
Replace all 6 stub bodies with real Playwright assertions. Use the `assert-if-present` pattern established in phases 5, 5.1, 6: structural UI assertions (element visible) always pass without API key; live-call assertions (file generated) skip gracefully when infra unavailable.

Navigate to the first active project found on the Dashboard for each test. Use `page.goto('/')` then find and click a project card.

**SKILL-05, SKILL-06, SKILL-07, SKILL-08 tests** — check that:
1. Navigate to `/customer/{id}/skills`
2. Find the skill card by skill display name
3. Assert the skill card is visible AND does NOT have a disabled/grayed-out class (i.e., it's in WIRED_SKILLS)
4. Assert the "Run" or "Trigger" button is enabled for that skill card

Use data-testid attributes already present in SkillsTabClient.tsx. If no data-testid attributes target individual skill cards, use text content matching: `page.getByText('ELT External Status')`.

**PLAN-12 test** — check that:
1. Navigate to `/customer/{id}/plan/board`
2. Assert `data-testid="generate-plan-btn"` is visible
3. (assert-if-present) If DB is seeded, click it and assert `data-testid="ai-plan-panel"` transitions to show a task list

**PLAN-13 test** — check that:
1. Navigate to `/customer/{id}/plan/board` (or any plan sub-route)
2. Assert `data-testid="sprint-summary-panel"` is visible
3. Assert `data-testid="sprint-summary-toggle"` is visible
4. Assert `data-testid="sprint-summary-refresh"` is visible

Helper to navigate to first project:
```typescript
async function gotoFirstProject(page: Page): Promise<string> {
  await page.goto('/');
  const firstCard = page.locator('[data-testid="project-card"]').first();
  if (await firstCard.count() === 0) {
    return '';
  }
  const href = await firstCard.locator('a').getAttribute('href') ?? '';
  const idMatch = href.match(/\/customer\/(\d+)/);
  return idMatch?.[1] ?? '';
}
```

Run the full suite after activation and confirm all 6 tests pass.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/phase7.spec.ts --reporter=line 2>&1 | tail -15</automated>
  </verify>
  <done>All 6 phase7 E2E tests pass GREEN. No stubs remain (no "stub" in test failure output). `npx playwright test tests/e2e/phase7.spec.ts` exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 7 outputs</name>
  <action>Human verifies file generation, UI panels, and skill card states as described in how-to-verify.</action>
  <what-built>
    Phase 7 is complete:
    - FileGenerationService (lib/file-gen/) — converts Claude JSON to .pptx and .html
    - 4 SKILL.md files — elt-external-status, elt-internal-status, team-engagement-map, workflow-diagram
    - SkillsTabClient updated — all 4 new skills enabled; biggy-weekly-briefing remains grayed out
    - skill-run.ts extended — FILE_SKILLS post-orchestrator file generation with filepath in outputs table
    - Skill run page — "Open in app" button when file output exists
    - AiPlanPanel — "Generate plan" button + proposed tasks checklist with commit/discard on Plan tab
    - SprintSummaryPanel — collapsible panel at top of Plan layout with Refresh
    - DB migration 0007 — sprint_summary + sprint_summary_at on projects table
    - All 6 E2E tests GREEN
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd bigpanda-app && npm run dev`

    2. **Skills tab — new skill cards visible:**
       - Navigate to any customer → Skills tab
       - Confirm "ELT External Status", "ELT Internal Status", "Team Engagement Map", "Workflow Diagram" cards are visible and NOT grayed out
       - Confirm "Biggy Weekly Briefing" is NOT shown or remains grayed out

    3. **Plan tab — AI panels visible:**
       - Navigate to any customer → Plan tab → Board
       - Confirm "Generate plan" button is visible above the Phase Board
       - Confirm the Sprint Summary panel is visible at the top of the page (with "Refresh" button)

    4. **If Anthropic API key is configured — file generation:**
       - Trigger "ELT External Status" from the Skills tab
       - Wait for run to complete (streaming output visible)
       - Confirm "Open in PowerPoint" button appears on the run completion page
       - Click it — confirm a .pptx file opens in PowerPoint without a corruption dialog
       - Check `~/Documents/BigPanda Projects/[CustomerName]/` for the generated file

    5. **Plan generation (if API key configured):**
       - Click "Generate plan" on the Plan tab board
       - Wait for proposed tasks to appear with checkboxes
       - Check one or more tasks, click "Commit selected"
       - Confirm the committed tasks appear in the Phase Board

    6. **Sprint summary persistence:**
       - Click "Refresh" on the Sprint Summary panel
       - Wait for generation to complete
       - Reload the page
       - Confirm the summary text still shows (without re-generating)
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `npx playwright test tests/e2e/phase7.spec.ts` exits 0 (all 6 GREEN)
- Human approval received for all 6 verification steps
- No regression in prior phases: `npx playwright test tests/e2e/ --grep "DASH|WORK|PLAN|SKILL|SCHED|OVER|TIME" 2>&1 | tail -10`
</verification>

<success_criteria>
All 6 phase 7 E2E tests pass GREEN. Human has verified: 4 new skill cards enabled in Skills tab, AiPlanPanel and SprintSummaryPanel visible on Plan tab, file generation produces uncorrupted output (when API key available), proposed tasks commit flow works, sprint summary persists across reloads.
</success_criteria>

<output>
After completion, create `.planning/phases/07-file-generation-remaining-skills/07-07-SUMMARY.md`
</output>
