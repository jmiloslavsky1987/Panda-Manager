---
phase: 19-external-discovery-scan
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - bigpanda-app/app/api/discovery/queue/route.ts
  - bigpanda-app/app/api/discovery/approve/route.ts
  - bigpanda-app/app/api/discovery/dismiss/route.ts
  - bigpanda-app/app/api/discovery/dismiss-history/route.ts
  - bigpanda-app/tests/discovery/queue.test.ts
  - bigpanda-app/tests/discovery/approve.test.ts
  - bigpanda-app/tests/discovery/dismiss.test.ts
autonomous: true
requirements:
  - DISC-10
  - DISC-11
  - DISC-12
  - DISC-13
  - DISC-14
  - DISC-15
  - DISC-16

must_haves:
  truths:
    - "GET /api/discovery/queue returns all pending items with required display fields"
    - "POST /api/discovery/approve writes item to correct entity table with source='discovery' attribution"
    - "POST /api/discovery/dismiss sets status=dismissed — record preserved, not deleted"
    - "GET /api/discovery/dismiss-history returns status=dismissed items"
    - "Bulk approve via POST /api/discovery/approve with array of IDs works"
  artifacts:
    - path: "bigpanda-app/app/api/discovery/queue/route.ts"
      provides: "GET ?projectId=N — returns DiscoveryQueueItem[] (all pending, no expiry)"
    - path: "bigpanda-app/app/api/discovery/approve/route.ts"
      provides: "POST — writes approved items to entity table, marks discovery_item approved"
    - path: "bigpanda-app/app/api/discovery/dismiss/route.ts"
      provides: "POST — sets status=dismissed, handles bulk IDs array"
    - path: "bigpanda-app/app/api/discovery/dismiss-history/route.ts"
      provides: "GET ?projectId=N — returns dismissed items"
  key_links:
    - from: "bigpanda-app/app/api/discovery/approve/route.ts"
      to: "entity tables (actions, risks, decisions, milestones)"
      via: "db.insert() per suggested_field value"
      pattern: "db\\.insert\\("
    - from: "bigpanda-app/app/api/discovery/queue/route.ts"
      to: "bigpanda-app/db/schema.ts discoveryItems"
      via: "db.select().where(eq(discoveryItems.status, 'pending'))"
      pattern: "discoveryItems.*pending"
---

<objective>
Wave 1 (parallel with 19-02): Implement all four discovery CRUD API routes — GET queue, POST approve (with entity write), POST dismiss, GET dismiss-history. These are the server routes the Review Queue UI calls. Approve route mirrors the ingestion approve route pattern (18-04) but writes with source='discovery' instead of source='ingestion'.

Purpose: Closes DISC-10–16. Enables the Review Queue page (19-04) to fetch, approve, and dismiss items without any placeholder data.

Output: 4 API route files + 3 test files GREEN.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

@.planning/phases/18-document-ingestion/18-04-SUMMARY.md
</context>

<interfaces>
<!-- discovery_items table columns -->
```typescript
// id, project_id, source (tool name), content (extracted value),
// suggested_field (entity type), suggested_value, status ('pending'|'approved'|'dismissed'),
// scan_timestamp, source_url, source_excerpt, scan_id, created_at
```

<!-- entity write pattern from 18-04-SUMMARY.md -->
// insertItem() pattern: db.insert(table).values({ ...fields, source: 'discovery', created_at: new Date() })
// For actions: external_id = 'DISC-ACT-{discoveryItemId}-{timestamp}' (actions table has external_id NOT NULL)
// Append-only tables (key_decisions, engagement_history): auto-skip on conflict

<!-- Drizzle query pattern from project codebase -->
```typescript
import db from '@/db';
import { discoveryItems } from '@/db/schema';
import { eq, and } from 'drizzle-orm';

// Pending queue
await db.select().from(discoveryItems)
  .where(and(eq(discoveryItems.project_id, projectId), eq(discoveryItems.status, 'pending')));
```

<!-- suggested_field values → entity table mapping -->
// 'action' → actions
// 'risk' → risks
// 'decision' → key_decisions (append-only)
// 'milestone' → milestones
// 'stakeholder' → stakeholders
// 'history' → engagement_history (append-only)
// Others: store content in engagement_history as catch-all

<!-- Zod v4 note from 18-04-SUMMARY.md -->
// z.record() requires two args in Zod v4: z.record(z.string(), z.string())
// Single-arg form crashes at runtime
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/discovery/queue + GET /api/discovery/dismiss-history</name>
  <files>bigpanda-app/app/api/discovery/queue/route.ts, bigpanda-app/app/api/discovery/dismiss-history/route.ts, bigpanda-app/tests/discovery/queue.test.ts</files>
  <behavior>
    - GET /api/discovery/queue?projectId=N returns { items: DiscoveryQueueItem[] } where each item has: id, source, content, suggested_field, source_excerpt, source_url, scan_timestamp, created_at, status='pending'
    - Returns ALL pending items regardless of age (DISC-16 — no expiry)
    - Returns 400 if projectId missing
    - GET /api/discovery/dismiss-history?projectId=N returns { items: DiscoveryQueueItem[] } where status='dismissed'
    - queue.test.ts GREEN: (a) returns items with all required display fields, (b) items remain pending across scans (no expiry logic removes them)
  </behavior>
  <action>
    Create bigpanda-app/app/api/discovery/queue/route.ts:
    - GET handler: parse projectId from searchParams, query discoveryItems WHERE project_id = projectId AND status = 'pending', order by created_at DESC
    - Return { items } as JSON

    Create bigpanda-app/app/api/discovery/dismiss-history/route.ts:
    - GET handler: same but status = 'dismissed'

    Update bigpanda-app/tests/discovery/queue.test.ts — replace stubs with real tests:
    - Mock @/db with vi.mock
    - Test GET queue returns items with all 8 display fields present
    - Test that GET queue includes source_excerpt field (from migration 0013)
    - Test no time-based filtering applied (all pending items returned)

    Use vi.mock('@/db', ...) with inline vi.fn() factories (not top-level const — hoisting issue per 18-04 decision).
    Run: cd bigpanda-app && npx vitest run tests/discovery/queue.test.ts
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/discovery/queue.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>queue.test.ts passes GREEN. Both GET routes exist with correct Drizzle queries. No TypeScript errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/discovery/approve + POST /api/discovery/dismiss</name>
  <files>bigpanda-app/app/api/discovery/approve/route.ts, bigpanda-app/app/api/discovery/dismiss/route.ts, bigpanda-app/tests/discovery/approve.test.ts, bigpanda-app/tests/discovery/dismiss.test.ts</files>
  <behavior>
    approve route:
    - POST body: { projectId: number, itemIds: number[] } — supports single and bulk (DISC-13)
    - For each itemId: fetch discovery_item, route to entity table by suggested_field, insert with source='discovery' + scan_timestamp from discovery_item
    - After successful insert: UPDATE discovery_items SET status='approved' WHERE id=itemId
    - For append-only types (key_decisions→decision, engagement_history→history): use INSERT only, no conflict detection
    - For actions: external_id = `DISC-ACT-${itemId}-${Date.now()}`
    - Returns { approved: N, errors: [] }
    - approve.test.ts GREEN: (a) action written to actions with source='discovery', (b) risk written to risks with source='discovery', (c) discovery_item status set to 'approved', (d) bulk array of IDs all processed

    dismiss route:
    - POST body: { projectId: number, itemIds: number[] }
    - UPDATE discovery_items SET status='dismissed' WHERE id IN (itemIds) AND project_id = projectId
    - Does NOT delete records (DISC-15)
    - Returns { dismissed: N }
    - dismiss.test.ts GREEN: (a) status set to dismissed not deleted, (b) bulk dismiss works, (c) dismissed item can be fetched from dismiss-history endpoint
  </behavior>
  <action>
    Create bigpanda-app/app/api/discovery/approve/route.ts:
    Zod schema: z.object({ projectId: z.number(), itemIds: z.array(z.number()) })
    For each itemId:
    - SELECT discovery_item from discovery_items WHERE id=itemId AND project_id=projectId
    - Call insertDiscoveredItem(item) — entity router:
      switch(item.suggested_field):
        'action' → db.insert(actions).values({ title: item.content, status: 'open', source: 'discovery', external_id: `DISC-ACT-${item.id}-${Date.now()}`, project_id: item.project_id, created_at: new Date() })
        'risk' → db.insert(risks).values({ title: item.content, source: 'discovery', project_id: item.project_id, created_at: new Date() })
        'decision' → db.insert(key_decisions).values({ text: item.content, source: 'discovery', project_id: item.project_id, created_at: new Date() })
        'milestone' → db.insert(milestones).values({ name: item.content, source: 'discovery', project_id: item.project_id, created_at: new Date() })
        'stakeholder' → db.insert(stakeholders).values({ name: item.content, source: 'discovery', project_id: item.project_id, created_at: new Date() })
        default → db.insert(engagement_history).values({ text: item.content, source: 'discovery', project_id: item.project_id, created_at: new Date() })
    - UPDATE discoveryItems status='approved'

    Create bigpanda-app/app/api/discovery/dismiss/route.ts:
    Zod schema: z.object({ projectId: z.number(), itemIds: z.array(z.number()) })
    UPDATE discoveryItems SET status='dismissed' WHERE id IN itemIds AND project_id = projectId
    Use Drizzle: db.update(discoveryItems).set({ status: 'dismissed' }).where(and(inArray(discoveryItems.id, itemIds), eq(discoveryItems.project_id, projectId)))

    Update both test files — replace stubs with mocked assertions.
    Run: cd bigpanda-app && npx vitest run tests/discovery/approve.test.ts tests/discovery/dismiss.test.ts
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/discovery/approve.test.ts tests/discovery/dismiss.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>approve.test.ts (4 tests) GREEN. dismiss.test.ts (3 tests) GREEN. Both routes compile with 0 TypeScript errors. Approve route covers all 5 entity types + catch-all.</done>
</task>

</tasks>

<verification>
- cd bigpanda-app && npx vitest run tests/discovery/ → all tests passing (queue + approve + dismiss)
- ls bigpanda-app/app/api/discovery/ → queue/, approve/, dismiss/, dismiss-history/ all present
- grep "source='discovery'\|source: 'discovery'" bigpanda-app/app/api/discovery/approve/route.ts → present
- grep "status.*dismissed\|dismissed.*status" bigpanda-app/app/api/discovery/dismiss/route.ts → present (UPDATE not DELETE)
</verification>

<success_criteria>
4 API routes exist and compile. queue.test.ts, approve.test.ts, dismiss.test.ts all GREEN. Approve route writes to 5+ entity tables with source='discovery'. Dismiss route sets status without deleting.
</success_criteria>

<output>
After completion, create `.planning/phases/19-external-discovery-scan/19-03-SUMMARY.md`
</output>
