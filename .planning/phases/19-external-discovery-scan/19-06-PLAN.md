---
phase: 19-external-discovery-scan
plan: 06
type: execute
wave: 4
depends_on:
  - 19-04
  - 19-05
files_modified: []
autonomous: false
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
  - DISC-06
  - DISC-07
  - DISC-08
  - DISC-09
  - DISC-10
  - DISC-11
  - DISC-12
  - DISC-13
  - DISC-14
  - DISC-15
  - DISC-16
  - DISC-17

must_haves:
  truths:
    - "All 5 DISC test files pass in cd bigpanda-app && npx vitest run tests/discovery/"
    - "Review Queue tab visible in workspace navigation"
    - "Scan for Updates button visible and triggers SSE scan without error"
    - "Queue items display all DISC-11 required fields"
    - "Approve/dismiss/bulk-approve all functional"
    - "DiffView appears on conflict (409 response from approve route)"
    - "Dismissed items appear in history, not active queue"
  artifacts:
    - path: "bigpanda-app/tests/discovery/"
      provides: "All 5 test files GREEN"
    - path: "bigpanda-app/app/customer/[id]/queue/page.tsx"
      provides: "Review Queue page accessible at /customer/:id/queue"
    - path: "bigpanda-app/components/ScanForUpdatesButton.tsx"
      provides: "Scan trigger button wired to workspace layout"
  key_links:
    - from: "Human"
      to: "Review Queue page"
      via: "Browser navigation to /customer/:id/queue"
      pattern: "manual"
---

<objective>
Wave 3: Full verification checkpoint. All 17 DISC requirements verified — automated test suite pass + human functional verification of the complete scan-to-review-to-approve flow.

Purpose: Confirms Phase 19 is complete and production-ready before marking the phase done. All automated tests must pass first; then human verification of the live UI flow.

Output: All tests GREEN + human-confirmed end-to-end flow.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated test suite pass</name>
  <files></files>
  <action>
    Run all discovery tests and verify all pass GREEN:

    cd bigpanda-app && npx vitest run tests/discovery/

    If any tests fail, diagnose and fix before proceeding. Common failure modes:
    - vi.mock hoisting errors: use inline vi.fn() in factory (per 18-04 decision)
    - @/ alias resolution: must run from bigpanda-app/ not project root (per 18-03 decision)
    - Zod v4 z.record single-arg crash: use z.record(z.string(), z.string()) (per 18-04 decision)
    - vi.resetAllMocks() needed (not vi.clearAllMocks()) when mocks leak between tests

    Also run TypeScript check:
    cd bigpanda-app && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep "discovery\|queue\|scan" | head -20

    Fix any TypeScript errors in Phase 19 files (pre-existing errors in other files are out of scope).
  </action>
  <verify>
    <automated>cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npx vitest run tests/discovery/ 2>&1 | tail -20</automated>
  </verify>
  <done>All 13+ discovery tests passing GREEN. 0 TypeScript errors introduced by Phase 19 files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete scan-to-review-to-approve flow</name>
  <action>Human verifies all DISC requirements end-to-end in the running app.</action>
  <what-built>
    Complete Phase 19 External Discovery Scan:
    - "Scan for Updates" button on all workspace tabs — source selector — SSE scan progress — toast notification
    - Review Queue tab (14th tab) with pending item badge
    - Review Queue page: per-item rows with source badge, date, excerpt, destination, extracted value
    - Approve / Edit then Approve / Dismiss per item, Bulk Approve All
    - DiffView: side-by-side comparison triggered when approve returns conflict
    - Dismissal history toggle: view dismissed items
    - Scheduled daily discovery-scan BullMQ job registered
  </what-built>
  <how-to-verify>
    1. Start dev server: `cd bigpanda-app && npm run dev`
    2. Navigate to any project workspace — confirm "Scan for Updates" button visible in the bar below the tab navigation
    3. Click "Scan for Updates" — source selector appears with 4 checkboxes (Slack, Gmail, Glean, Gong) — select at least 2 — click "Start Scan"
    4. Confirm SSE progress messages appear ("Scanning Slack...", "Scanning Gmail...")
    5. When scan completes: confirm toast notification fires if items found, OR "No new items" message if none
    6. Navigate to "Review Queue" tab (14th tab in workspace nav) — confirm it loads
    7. If items pending: verify each item shows: source tool name, date found, source excerpt (collapsible), suggested destination field, Claude's extracted value
    8. Click "Show source excerpt" on an item — confirm excerpt text appears
    9. Click "Dismiss" on one item — item disappears from queue
    10. Click "View Dismissal History" — dismissed item appears in history section
    11. Run scan again — dismissed item does NOT reappear in active queue
    12. Click "Approve" on an item — item disappears from queue, navigate to the destination tab (e.g., Actions) to verify the record was written with correct content
    13. Click "Approve All" with multiple items — all approved in bulk
    14. Confirm pending count badge on "Review Queue" tab shows correct number (updates after approve/dismiss)
  </how-to-verify>
  <resume-signal>Type "approved" if all verification steps pass, or describe any failures (include step number)</resume-signal>
</task>

</tasks>

<verification>
cd bigpanda-app && npx vitest run tests/discovery/ → all GREEN
Human: all 14 verification steps confirmed
</verification>

<success_criteria>
All 13+ discovery tests GREEN. Human verified: scan triggers without error, Review Queue displays all DISC-11 fields, approve writes to DB, dismiss preserved in history, dismissed items don't reappear, bulk approve works, DiffView appears on conflict.
</success_criteria>

<output>
After completion, create `.planning/phases/19-external-discovery-scan/19-06-SUMMARY.md`
</output>
