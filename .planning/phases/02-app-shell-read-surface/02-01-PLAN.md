---
phase: 02-app-shell-read-surface
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/phase2.spec.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-06, DASH-07, DASH-08, WORK-01, WORK-03, WORK-04, WORK-05, WORK-06, WORK-07, WORK-08, WORK-09]

must_haves:
  truths:
    - "Playwright is installed and `npx playwright test tests/e2e/phase2.spec.ts` runs (all tests fail RED — that's correct at Wave 0)"
    - "Test file covers every Phase 2 user-observable behavior with a named test"
    - "Dashboard health card tests assert at least 1 project card renders"
    - "Each of the 9 workspace tab URLs is covered by its own test"
    - "Add Notes modal test covers open, type, and save interactions"
    - "Notification badge test asserts the badge element exists when overdue data is present"
  artifacts:
    - path: "tests/e2e/phase2.spec.ts"
      provides: "Playwright E2E spec covering all Phase 2 behaviors"
      min_lines: 80
  key_links:
    - from: "tests/e2e/phase2.spec.ts"
      to: "http://localhost:3000"
      via: "Playwright page.goto()"
      pattern: "page\\.goto"
---

<objective>
Install Playwright and create the Phase 2 E2E test stub file. All tests are written first (RED) — they will remain failing until implementation plans complete.

Purpose: Nyquist compliance — every subsequent plan's `<verify>` command must have a real test to point at. Writing stubs now means implementation plans can use `npx playwright test tests/e2e/phase2.spec.ts --grep "..."` as their verify commands.
Output: `tests/e2e/phase2.spec.ts` with named tests for all 14 requirements.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shell-read-surface/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and write E2E test stubs</name>
  <files>tests/e2e/phase2.spec.ts</files>
  <action>
From the project root (`/Users/jmiloslavsky/Documents/Project Assistant Code`):

1. Install Playwright as a dev dependency at project root:
   ```
   npm install --save-dev @playwright/test
   npx playwright install chromium
   ```

2. Create `tests/e2e/phase2.spec.ts` with the following test structure. All tests use `test.fixme()` or `expect(true).toBe(false)` so they are RED stubs (they exist and run, but they fail). Do NOT write assertions that accidentally pass. The base URL is `http://localhost:3000`.

   Organize the spec into describe blocks:

   **describe('Dashboard'):**
   - 'DASH-02/03: health cards render for all active projects' — navigate to `/`, assert `.data-testid="health-card"` count >= 1, assert each card has a RAG badge element
   - 'DASH-01: Today Briefing panel is visible' — navigate to `/`, assert `[data-testid="briefing-panel"]` is visible
   - 'DASH-06: Recent Activity Feed shows entries' — assert `[data-testid="activity-feed"]` is visible and contains at least one row
   - 'DASH-07: Quick Action Bar buttons are visible' — assert `[data-testid="quick-action-bar"]` exists, assert buttons for "Run Tracker", "Generate Briefing", "Weekly Status Draft" are present (they may be disabled)
   - 'DASH-08: Notification badge appears for overdue actions' — assert `[data-testid="notification-badge"]` exists anywhere on the page (it lives in the page content area in Phase 2, not in a header element)

   **describe('Workspace tabs'):**
   - 'WORK-01: Overview tab renders workstream progress and milestone timeline' — navigate to `/customer/1/overview`, assert `[data-testid="overview-tab"]` is visible
   - 'WORK-03: Risks tab renders risk register' — navigate to `/customer/1/risks`, assert `[data-testid="risks-tab"]` is visible
   - 'WORK-04: Milestones tab renders milestone tracker' — navigate to `/customer/1/milestones`, assert `[data-testid="milestones-tab"]` is visible
   - 'WORK-05: Teams tab renders onboarding status table' — navigate to `/customer/1/teams`, assert `[data-testid="teams-tab"]` is visible
   - 'WORK-06: Architecture tab renders architecture state' — navigate to `/customer/1/architecture`, assert `[data-testid="architecture-tab"]` is visible
   - 'WORK-07: Decisions tab renders key decisions' — navigate to `/customer/1/decisions`, assert `[data-testid="decisions-tab"]` is visible
   - 'WORK-08: Engagement History tab renders history entries' — navigate to `/customer/1/history`, assert `[data-testid="history-tab"]` is visible
   - 'WORK-09: Stakeholders tab renders contacts roster' — navigate to `/customer/1/stakeholders`, assert `[data-testid="stakeholders-tab"]` is visible

   **describe('Add Notes modal'):**
   - 'DASH engagement: Add Notes modal opens and accepts input' — navigate to `/customer/1/overview`, click `[data-testid="add-notes-btn"]`, assert modal appears, fill textarea, click save, assert modal closes without console error

   **describe('No console errors on tab load'):**
   - Parameterized test for each of the 9 tab paths — navigate to each, assert no `page.on('console')` errors of type 'error' appear

   Each stub test body should call `test.fail()` or use `expect(false, 'stub — implement Phase 2').toBe(true)` so that the test runner reports them as failing (not as skip). This makes the RED state explicit.

3. Create `playwright.config.ts` at project root if it does not exist:
   ```typescript
   import { defineConfig } from '@playwright/test';
   export default defineConfig({
     testDir: './tests/e2e',
     use: {
       baseURL: 'http://localhost:3000',
     },
     webServer: {
       command: 'cd bigpanda-app && npm run dev',
       url: 'http://localhost:3000',
       reuseExistingServer: true,
     },
   });
   ```

Note: Tests will all be RED at this point — this is correct and expected.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase2.spec.ts --reporter=list 2>&1 | head -30 || true</automated>
  </verify>
  <done>
    - `tests/e2e/phase2.spec.ts` exists with all named tests
    - `playwright.config.ts` exists at project root
    - Running the spec exits with failures (RED) — all tests exist but fail because the app routes don't exist yet
    - All 9 workspace tabs have a named test
    - Dashboard has 5 named tests covering DASH-01/02/03/06/07/08
    - Add Notes modal has 1 named test
  </done>
</task>

</tasks>

<verification>
Run: `npx playwright test tests/e2e/phase2.spec.ts --reporter=list 2>&1 | head -40`
Expected: All tests fail (RED) — this is the correct Wave 0 state.
</verification>

<success_criteria>
Playwright is installed. `tests/e2e/phase2.spec.ts` exists with named tests for all Phase 2 requirements. All tests fail — the RED baseline is established. Subsequent plans will flip tests GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell-read-surface/02-01-SUMMARY.md`
</output>
