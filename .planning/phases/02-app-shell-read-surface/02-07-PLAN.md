---
phase: 02-app-shell-read-surface
plan: 07
type: execute
wave: 4
depends_on: [02-05, 02-06]
files_modified: []
autonomous: false

requirements: [DASH-01, DASH-02, DASH-03, DASH-06, DASH-07, DASH-08, WORK-01, WORK-03, WORK-04, WORK-05, WORK-06, WORK-07, WORK-08, WORK-09]

must_haves:
  truths:
    - "All 9 workspace tabs load and display real data with no console errors"
    - "Dashboard health cards show correct RAG status for each active project"
    - "Add Notes modal opens, accepts input, and saves to the database"
    - "No tab shows placeholder or mock data"
  artifacts:
    - path: "bigpanda-app/app"
      provides: "Complete Phase 2 application"
  key_links:
    - from: "tests/e2e/phase2.spec.ts"
      to: "http://localhost:3000"
      via: "Playwright test run"
      pattern: "playwright test"
---

<objective>
Human visual and functional verification of the complete Phase 2 read surface. Run the Playwright E2E suite and manually verify key interactions that automated tests cannot cover (visual layout quality, RAG dot colors, modal usability).

Purpose: Phase 2 is a UI phase. The automated E2E tests verify structure and behavior, but human eyes confirm the app is actually usable as the daily driver it's designed to be.
Output: Confirmed working Phase 2 app — approved or issues logged for gap closure.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-app-shell-read-surface/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run E2E suite and fix any blocking failures</name>
  <files></files>
  <action>
Start the Next.js dev server (if not already running):
```bash
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code/bigpanda-app && npm run dev &
sleep 5
```

Run the full Playwright E2E suite:
```bash
cd /Users/jmiloslavsky/Documents/Project\ Assistant\ Code && npx playwright test tests/e2e/phase2.spec.ts --reporter=list
```

Capture the output. For each failing test:
1. If it's a structural failure (data-testid not found, page returns 500) → fix it before the checkpoint
2. If it's a data failure (no rows because DB is empty) → note it in the summary; the user will seed the DB
3. If it's a visual/interaction failure that requires human judgment → leave for the checkpoint

After fixing any blocking structural failures, re-run:
```bash
npx playwright test tests/e2e/phase2.spec.ts --reporter=list
```
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase2.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>
    - E2E suite runs to completion (even if some tests fail due to empty DB — that's acceptable)
    - No structural failures (missing pages, 500 errors, TypeScript build errors)
    - Output captured and ready for checkpoint review
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 2 App Shell + Read Surface:
- Dashboard (/) with health cards, activity feed, quick action buttons (disabled), notification badge, briefing panel
- Left sidebar with project list, RAG dots, navigation
- All 9 workspace tabs for each customer project:
  Overview | Actions | Risks | Milestones | Teams | Architecture | Decisions | Engagement History | Stakeholders
- "Add Notes" modal that writes to engagement_history
- All data from PostgreSQL (requires DB to be populated via migration script)
  </what-built>
  <how-to-verify>
**Prerequisites:** Ensure PostgreSQL is running and the migration has been run:
```bash
cd bigpanda-app
DATABASE_URL=postgresql://localhost:5432/bigpanda_app npx drizzle-kit migrate
DATABASE_URL=postgresql://localhost:5432/bigpanda_app npx tsx scripts/migrate-local.ts
```
Then start the app: `npm run dev` in `bigpanda-app/`

**Verify these behaviors:**

1. Visit `http://localhost:3000` — confirm:
   - Sidebar is visible on the left (dark background)
   - At least one project card appears in the main content area
   - Each project card shows a colored RAG dot/badge (Green, Yellow, or Red)
   - "Quick Actions" section shows disabled buttons for each project

2. Click a project in the sidebar — confirm:
   - URL changes to `/customer/[id]/overview`
   - A tab bar appears with all 9 tabs (Overview through Stakeholders)
   - Overview tab shows workstream progress and milestone summary

3. Click each tab and confirm it loads without a browser console error:
   - Actions: shows a table of actions; overdue ones are highlighted in red
   - Risks: shows risk register sorted by severity
   - Milestones: shows milestone status
   - Teams: shows team tracks with stall indicator if applicable
   - Architecture: shows integration state content
   - Decisions: shows key decisions (no edit button visible)
   - Engagement History: shows history entries (no edit button visible)
   - Stakeholders: shows contacts table

4. Click "+ Add Notes" button (bottom right corner) — confirm:
   - Modal opens with a textarea
   - Type "Test note from Phase 2 verification — [today's date]"
   - Click Save — modal closes
   - Navigate to the Engagement History tab — the new note appears at the top with source "manual_entry"

5. Check browser DevTools console — confirm no red errors on any page (warnings about React are acceptable)
  </how-to-verify>
  <resume-signal>
Type "approved" if all behaviors confirmed.
Type "issues: [description]" to log specific problems for gap closure.
  </resume-signal>
</task>

</tasks>

<verification>
Human verification complete — user has approved or logged issues.
</verification>

<success_criteria>
All 9 workspace tabs load with real data, the dashboard shows project health cards with computed RAG status, the Add Notes modal writes to the database and the entry appears in Engagement History, and no console errors appear during normal navigation. Phase 2 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell-read-surface/02-07-SUMMARY.md`
Update STATE.md: Phase 2 status → COMPLETE (7/7 plans).
</output>
