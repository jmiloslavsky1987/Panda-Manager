---
phase: 02-app-shell-read-surface
plan: 05
type: execute
wave: 3
depends_on: [02-04]
files_modified:
  - bigpanda-app/app/customer/[id]/overview/page.tsx
  - bigpanda-app/app/customer/[id]/actions/page.tsx
  - bigpanda-app/app/customer/[id]/risks/page.tsx
  - bigpanda-app/app/customer/[id]/milestones/page.tsx
  - bigpanda-app/app/customer/[id]/teams/page.tsx
autonomous: true
requirements: [WORK-01, WORK-03, WORK-04, WORK-05]

must_haves:
  truths:
    - "Navigating to /customer/1/overview shows workstream progress bars for ADR and Biggy tracks, a milestone timeline section, and auto-derived health status"
    - "Navigating to /customer/1/actions shows a table of all actions filterable by status with open high-priority actions highlighted"
    - "Navigating to /customer/1/risks shows the risk register with severity badges and mitigation text per risk"
    - "Navigating to /customer/1/milestones shows a milestone tracker with status indicators and target dates"
    - "Navigating to /customer/1/teams shows the team onboarding status table with ADR and Biggy track columns and stall detection (14+ days)"
    - "No page in this plan shows placeholder or mock data — all rows come from PostgreSQL"
  artifacts:
    - path: "bigpanda-app/app/customer/[id]/overview/page.tsx"
      provides: "Overview tab — workstreams, milestones summary, health signal"
      exports: ["default (OverviewPage)"]
    - path: "bigpanda-app/app/customer/[id]/actions/page.tsx"
      provides: "Actions tab — full action list with status filter"
      exports: ["default (ActionsPage)"]
    - path: "bigpanda-app/app/customer/[id]/risks/page.tsx"
      provides: "Risks tab — risk register with severity and mitigation"
      exports: ["default (RisksPage)"]
    - path: "bigpanda-app/app/customer/[id]/milestones/page.tsx"
      provides: "Milestones tab — milestone tracker"
      exports: ["default (MilestonesPage)"]
    - path: "bigpanda-app/app/customer/[id]/teams/page.tsx"
      provides: "Teams tab — onboarding status with stall detection"
      exports: ["default (TeamsPage)"]
  key_links:
    - from: "bigpanda-app/app/customer/[id]/overview/page.tsx"
      to: "bigpanda-app/lib/queries.ts"
      via: "getWorkspaceData(projectId)"
      pattern: "getWorkspaceData"
    - from: "bigpanda-app/app/customer/[id]/actions/page.tsx"
      to: "bigpanda-app/lib/queries.ts"
      via: "getWorkspaceData(projectId).actions"
      pattern: "getWorkspaceData"
---

<objective>
Implement five workspace tab pages: Overview, Actions, Risks, Milestones, Teams. All are RSC pages using `getWorkspaceData()` from lib/queries — no client-side fetching.

Purpose: This plan and plan 02-06 run in parallel (Wave 3) — both depend on the workspace layout shell (02-04) but not on each other. Together they deliver all 9 workspace tabs.
Output: Five tab pages at their respective routes, all displaying live PostgreSQL data.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-app-shell-read-surface/02-CONTEXT.md
@bigpanda-app/lib/queries.ts
@bigpanda-app/db/schema.ts

<interfaces>
<!-- WorkspaceData from getWorkspaceData(projectId) -->
WorkspaceData: {
  project: ProjectWithHealth,
  workstreams: Workstream[],
  actions: Action[],
  risks: Risk[],
  milestones: Milestone[],
  artifacts: Artifact[],
  history: HistoryEntry[],
  keyDecisions: KeyDecision[],
  stakeholders: Stakeholder[]
}

Workstream: { id, project_id, name, track ('ADR'|'Biggy'), current_status, lead, last_updated, state }
Action: { id, project_id, external_id, description, owner, due, status, notes, type }
Risk: { id, project_id, external_id, description, severity, owner, mitigation, status }
Milestone: { id, project_id, external_id, name, status, target, date, notes, owner }
Stakeholder: { id, name, role, company, email, slack_id, notes }

<!-- shadcn/ui components available (installed in 02-02) -->
Badge, Card, Table (if added), Separator, Button, Tabs (shadcn)
Note: if shadcn Table component not yet installed, add it:
  cd bigpanda-app && npx shadcn@latest add table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overview tab and Actions tab</name>
  <files>bigpanda-app/app/customer/[id]/overview/page.tsx, bigpanda-app/app/customer/[id]/actions/page.tsx</files>
  <action>
Create directory `app/customer/[id]/overview/` and `app/customer/[id]/actions/`.

**Overview page (`overview/page.tsx`)** — RSC:
```tsx
import { getWorkspaceData } from '../../../../lib/queries'

export default async function OverviewPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  const data = await getWorkspaceData(parseInt(id, 10))
  const { workstreams, milestones, project } = data
  // ...
}
```

Sections to render:
1. **Health Status** (from project.health): a colored banner "Project is Healthy / At Risk / Critical" with signal breakdown (N overdue actions, N high risks, N stalled milestones)
2. **Workstreams** (`data-testid="overview-tab"`): for each workstream, show name, track badge (ADR = blue, Biggy = purple), current_status text, lead, last_updated. Group by track (ADR track first, then Biggy). A progress bar metaphor: show state as text ("In Progress", "Blocked", "Done") mapped to a colored dot, not an actual percentage bar since there's no numeric percent in the schema yet. Use `<Badge>` for track labels.
3. **Milestone Summary**: show the 5 most recent milestones (by date descending) with name, status badge, and target date. Link "View all →" to the milestones tab (just a `<Link>`).
4. **Go-live target**: prominently show `project.go_live_target` at the top.

**Actions page (`actions/page.tsx`)** — RSC:
- `data-testid="actions-tab"` on root div
- Install shadcn Table if not present: `npx shadcn@latest add table`
- Render all actions (not just open ones) in a shadcn `<Table>` with columns: ID (`external_id`), Description, Owner, Due, Status (Badge)
- Status badge colors: open=blue, in_progress=orange, completed=green, cancelled=zinc
- Overdue indicator: if action.due is a parseable ISO date < today AND status is open or in_progress, highlight the row with `bg-red-50` and add a "Overdue" badge
- For 'TBD'/'N/A'/'–' due strings, show as-is without overdue logic
- Filter by status: since this is RSC, use URL searchParams. Read `searchParams.status` from the page props (`{ params, searchParams }`). If `searchParams.status` is set, filter the actions array before rendering.
- Add filter links at the top: "All | Open | In Progress | Completed | Cancelled" — each is a `<Link>` with `?status=...` query param (shadcn `<Badge>` styled as buttons)
- Pagination: show first 50 rows only. Show "Showing X of Y actions" count at the top.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | grep -E "overview/page|actions/page" | head -10 || echo "no errors"</automated>
  </verify>
  <done>
    - `app/customer/[id]/overview/page.tsx` exists with data-testid="overview-tab", renders workstreams grouped by track and milestone summary
    - `app/customer/[id]/actions/page.tsx` exists with data-testid="actions-tab", renders actions table with overdue highlighting
    - Both call getWorkspaceData() as async RSC — no 'use client'
    - TypeScript reports no errors in these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Risks, Milestones, and Teams tabs</name>
  <files>bigpanda-app/app/customer/[id]/risks/page.tsx, bigpanda-app/app/customer/[id]/milestones/page.tsx, bigpanda-app/app/customer/[id]/teams/page.tsx</files>
  <action>
Create directories for each tab under `app/customer/[id]/`.

**Risks page (`risks/page.tsx`)** — RSC:
- `data-testid="risks-tab"` on root div
- Render risks in a table: columns — ID (external_id), Description (truncated at 100 chars), Severity (Badge), Owner, Status, Mitigation (full text in a collapsed div or just show first 150 chars with "...")
- Severity badge colors: critical=red, high=orange, medium=yellow, low=green (use shadcn `<Badge>` with className override)
- Sort by severity (critical first, then high, medium, low)
- Unresolved high/critical risks get a highlighted row (`bg-orange-50`)
- Append-only mitigation note: below the risk table, add a small info callout: "Mitigation entries are append-only — editing available in Phase 3"

**Milestones page (`milestones/page.tsx`)** — RSC:
- `data-testid="milestones-tab"` on root div
- Render milestones in a table: columns — ID (external_id), Name, Status (Badge), Target/Date, Owner, Notes
- Status badge: completed=green, in_progress=blue, not_started=zinc, blocked=red
- Sort: incomplete milestones first (by target date ascending), completed last
- A milestone with a parseable date in the past and status != 'completed' gets a "Overdue" badge in red alongside the status badge

**Teams page (`teams/page.tsx`)** — RSC:
- `data-testid="teams-tab"` on root div
- Workstreams from the data represent team tracks. Group workstreams by track: ADR track and Biggy track.
- For each group, render a section header and a table of workstream rows: Name, State/Status, Lead, Last Updated, Stall Indicator
- Stall detection: if workstream.last_updated is a parseable date and it's more than 14 days ago, AND workstream.state/status is not 'complete'/'done'/'closed', add a "Stalled 14+ days" warning badge in amber
- Note: the stakeholders table is available in workspace data and contains the contacts. For Phase 2 Teams tab, focus on workstream status (stakeholder management is the Stakeholders tab). Do not duplicate stakeholder data here.
- Add a heading at top: "Team Onboarding Velocity" with a brief description of the stall detection rule.

For all three pages (and all tab pages throughout this phase), use the async params pattern — Next.js 15+ requires it:
```tsx
export default async function RisksPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  const data = await getWorkspaceData(parseInt(id, 10))
  // ...
}
```
Each page is a standalone RSC that receives the same WorkspaceData — they are independent routes.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app" && npx tsc --noEmit 2>&1 | grep -E "risks/page|milestones/page|teams/page" | head -10 || echo "no errors"</automated>
  </verify>
  <done>
    - `app/customer/[id]/risks/page.tsx` exists with data-testid="risks-tab", severity-sorted risk table
    - `app/customer/[id]/milestones/page.tsx` exists with data-testid="milestones-tab", overdue milestone detection
    - `app/customer/[id]/teams/page.tsx` exists with data-testid="teams-tab", 14-day stall detection
    - All three call getWorkspaceData() — no mock data
    - TypeScript reports no errors in these files
  </done>
</task>

</tasks>

<verification>
```bash
ls "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app/app/customer/[id]/"
```
Expected: overview/, actions/, risks/, milestones/, teams/ directories (plus layout.tsx and page.tsx from plan 02-04).

Check data-testid presence:
```bash
grep -r "data-testid" "/Users/jmiloslavsky/Documents/Project Assistant Code/bigpanda-app/app/customer/" | grep -E "overview-tab|actions-tab|risks-tab|milestones-tab|teams-tab"
```
</verification>

<success_criteria>
Five workspace tab pages exist and render real PostgreSQL data: Overview (workstreams + milestone summary + health), Actions (filterable table with overdue detection), Risks (severity-sorted register), Milestones (timeline with overdue detection), Teams (stall detection). All are RSC — no client-side data fetching.
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell-read-surface/02-05-SUMMARY.md`
</output>
