---
phase: 12-complete-workspace-write-surface
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/phase12.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 12 Phase 12 behaviors have a failing Playwright test stub (RED baseline)"
    - "Each stub fails with a clear description, not a skip or fixme"
    - "Tests are organized by grep tag matching the VALIDATION.md map"
  artifacts:
    - path: "tests/e2e/phase12.spec.ts"
      provides: "RED E2E test stubs for all write-surface behaviors"
      min_lines: 60
      contains: "expect(false, 'stub').toBe(true)"
  key_links:
    - from: "tests/e2e/phase12.spec.ts"
      to: "playwright.config.ts"
      via: "standard spec file import"
      pattern: "test\\("
---

<objective>
Create the RED baseline E2E test stubs for all Phase 12 behaviors. Every write-surface behavior that will be implemented in Plans 02 and 03 has a corresponding failing test before any implementation begins.

Purpose: Establish the Nyquist verification signal for the phase — tests are RED now and must turn GREEN by Plan 04.
Output: tests/e2e/phase12.spec.ts with 12 stubs, all failing.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-complete-workspace-write-surface/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase12.spec.ts with 12 failing stubs</name>
  <files>tests/e2e/phase12.spec.ts</files>
  <action>
Create `tests/e2e/phase12.spec.ts` with one test per behavior from the VALIDATION.md map. Every test must:

1. Use `expect(false, 'stub').toBe(true)` as the first and only assertion — same pattern established in Phase 2 (02-01 decision in STATE.md).
2. Use exactly the grep tag shown in the VALIDATION.md map embedded in the test name.
3. Be grouped under descriptive `test.describe` blocks that mirror the feature area.

The 12 stubs and their required grep tags:

```typescript
import { test, expect } from '@playwright/test'

test.describe('Phase 12 — Artifacts tab', () => {
  test('artifacts-tab: /customer/1/artifacts renders', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
  test('ArtifactEditModal: New Artifact button opens create modal', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
  test('artifact-create: artifact create saves and appears in table', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
  test('artifact-edit: row click opens ArtifactEditModal in edit mode', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
  test('artifacts-tab-nav: 13th tab "Artifacts" visible in WorkspaceTabs', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
})

test.describe('Phase 12 — Decisions write surface', () => {
  test('add-decision: Add Decision button opens modal', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
  test('decision-save: new decision appears at top of list after save', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
})

test.describe('Phase 12 — Architecture inline edit', () => {
  test('architecture-edit: Edit button opens workstream modal', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
  test('architecture-save: architecture save persists state+lead change', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
})

test.describe('Phase 12 — Teams progress slider', () => {
  test('teams-progress: Teams tab shows Progress column with slider', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
  test('teams-save: Teams slider save updates percent_complete', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
})

test.describe('Phase 12 — Banner cleanup', () => {
  test('banner-removed: Phase 3 placeholder banners are removed', async ({ page }) => {
    expect(false, 'stub').toBe(true)
  })
})
```

Do NOT add any navigation code, page.goto(), or assertions beyond the stub pattern — stubs must be as thin as possible. This matches the Phase 2 Wave 0 pattern (02-01-PLAN.md).
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase12.spec.ts --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <done>All 12 tests appear in the Playwright run output and all 12 fail with "stub" as the failure message. Zero passes.</done>
</task>

</tasks>

<verification>
Run: `cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase12.spec.ts --reporter=line`

Expected: 12 failed, 0 passed. Each failure shows "stub" as the reason.
</verification>

<success_criteria>
- tests/e2e/phase12.spec.ts exists with exactly 12 test stubs
- Every stub uses `expect(false, 'stub').toBe(true)` — no skips, no fixme, no real assertions
- All grep tags from VALIDATION.md are represented in test names
- npx playwright test reports 12 failures (RED baseline confirmed)
</success_criteria>

<output>
After completion, create `.planning/phases/12-complete-workspace-write-surface/12-01-SUMMARY.md`
</output>
