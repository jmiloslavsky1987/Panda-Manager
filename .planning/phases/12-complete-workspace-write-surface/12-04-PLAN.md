---
phase: 12-complete-workspace-write-surface
plan: 04
type: execute
wave: 3
depends_on:
  - 12-03
files_modified:
  - tests/e2e/phase12.spec.ts
autonomous: false

requirements: []

must_haves:
  truths:
    - "All 12 Phase 12 E2E tests are GREEN"
    - "Artifacts tab is usable end-to-end: create, view, edit"
    - "Decisions append-only write surface works and new decisions appear in timeline"
    - "Architecture inline edit works and state text preserves whitespace"
    - "Teams progress slider saves and the health score reflects the change on next dashboard visit"
    - "No stale placeholder banners visible anywhere in the workspace"
  artifacts:
    - path: "tests/e2e/phase12.spec.ts"
      provides: "12 passing E2E tests"
      contains: "12 passed"
  key_links:
    - from: "tests/e2e/phase12.spec.ts"
      to: "all Phase 12 write surfaces"
      via: "Playwright browser automation"
      pattern: "test\\("
---

<objective>
Drive all 12 Phase 12 E2E stubs from RED to GREEN, then present the complete write surface to the user for manual verification of the behaviors that Playwright cannot assert (visual layout, health score badge update, optimistic saving indicator).

Purpose: Phase gate — no phase is done without GREEN tests + human sign-off.
Output: All 12 tests passing + human approval.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-complete-workspace-write-surface/12-VALIDATION.md
@.planning/phases/12-complete-workspace-write-surface/12-02-SUMMARY.md
@.planning/phases/12-complete-workspace-write-surface/12-03-SUMMARY.md
@tests/e2e/phase12.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive E2E stubs from RED to GREEN</name>
  <files>tests/e2e/phase12.spec.ts</files>
  <action>
Run the Phase 12 E2E suite first to confirm current state:

```bash
cd "/Users/jmiloslavsky/Documents/Project Assistant Code"
npx playwright test tests/e2e/phase12.spec.ts --reporter=line
```

For each failing test, replace the `expect(false, 'stub').toBe(true)` stub with real assertions. The implementation across Plans 02 and 03 is complete — stubs just need to be wired to the actual UI.

**Test implementation guide per behavior:**

**artifacts-tab** — navigate to /customer/1/artifacts, assert the page title "Artifacts" and the `[data-testid="artifacts-table"]` is present.

**ArtifactEditModal** — navigate to /customer/1/artifacts, click `[data-testid="new-artifact-btn"]` (or "New Artifact" button), assert the Dialog appears with a name input visible.

**artifact-create** — navigate to /customer/1/artifacts, click New Artifact, fill in name input with a unique test name, click Save, assert the new row appears in the table by searching for the unique name.

**artifact-edit** — navigate to /customer/1/artifacts, if artifacts exist click the first row `[data-testid^="artifact-row-"]`, assert the modal opens with the artifact's name pre-populated.

**artifacts-tab-nav** — navigate to /customer/1/overview (or any workspace tab), assert the tab bar contains a link/tab labeled "Artifacts".

**add-decision** — navigate to /customer/1/decisions, click `[data-testid="add-decision-btn"]` (or "Add Decision" button), assert the Dialog with `decision` textarea is visible.

**decision-save** — navigate to /customer/1/decisions, click Add Decision, type a unique decision text, click Save, assert the decision text appears in the decisions list (top of the list — newest-first sort).

**architecture-edit** — navigate to /customer/1/architecture, click `[data-testid="architecture-edit-trigger"]` on the first workstream card (or "Edit" button), assert the modal opens with a textarea for state.

**architecture-save** — navigate to /customer/1/architecture, open edit modal for first workstream, change the lead field value, click Save, assert the updated lead name is visible in the card.

**teams-progress** — navigate to /customer/1/teams, assert the table has a column with a range `input[type="range"]` (data-testid pattern: `slider-*`).

**teams-save** — navigate to /customer/1/teams, find a slider for the first workstream, change its value (dispatchEvent or fill), assert the Save button appears (`[data-testid^="save-progress-"]`).

**banner-removed** — navigate to /customer/1/decisions and /customer/1/architecture in sequence, assert neither page contains text "Phase 3" or "Inline editing available" or "available in Phase 3".

Run the full suite after all stubs are replaced:

```bash
npx playwright test tests/e2e/phase12.spec.ts --reporter=line
```

If tests fail due to implementation gaps (not stub issues), fix the underlying implementation in the relevant page/component/route file before proceeding. The objective is 12/12 GREEN.

Note: The app must be running locally for E2E tests. Check `playwright.config.ts` for the baseURL and whether `webServer` is configured.
  </action>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase12.spec.ts --reporter=line 2>&1 | tail -5</automated>
  </verify>
  <done>Output shows "12 passed, 0 failed". All Phase 12 write-surface behaviors have automated verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all write surfaces</name>
  <files></files>
  <action>
Pause for human verification. All 12 E2E tests are GREEN. The user must manually verify the behaviors Playwright cannot assert: visual layout, health score badge update after Teams slider save, and the optimistic "Saving…" indicator.

Verification steps are detailed in the `<how-to-verify>` block below.
  </action>
  <what-built>
Complete workspace write surface:
- Artifacts tab (13th tab) — table view, create modal, edit modal, X-NNN auto-ID
- Decisions tab — "Add Decision" modal (append-only, newest-first)
- Architecture tab — per-card Edit modal (state textarea + lead input, whitespace preserved)
- Teams tab — Progress column with inline range slider and conditional Save button
- All "available in Phase 3" placeholder banners removed
- 12/12 E2E tests GREEN
  </what-built>
  <how-to-verify>
Ensure the dev server is running (`npm run dev` in bigpanda-app/), then open http://localhost:3000 and verify each item:

**1. Artifacts tab**
- Open any project workspace, confirm "Artifacts" appears as the 13th tab in the tab bar (after "Time")
- Click Artifacts tab — confirm the artifacts table loads with columns: ID, Name, Status, Owner
- Click "New Artifact" — confirm the modal opens, fill in a name, save, confirm the new artifact appears in the table with a sequential X-NNN ID
- Click an existing artifact row — confirm the Edit modal opens with the artifact's current values pre-filled

**2. Decisions write surface**
- Open the Decisions tab, confirm no amber "available in Phase 3" banner is visible
- Click "Add Decision" — confirm the modal opens with a `decision` textarea (required) and `context` textarea (optional)
- Type a decision, click Save, confirm it appears at the TOP of the decisions list with today's date

**3. Architecture write surface**
- Open the Architecture tab, confirm no blue "Inline editing" banner is visible
- Find any workstream card, click the Edit button (top-right of card)
- Confirm the modal opens with the current state (multi-line textarea) and lead (text input) pre-filled
- Edit the lead field, save, confirm the updated lead name appears on the card

**4. Teams progress slider**
- Open the Teams tab, confirm a "Progress" column is visible in the workstream table
- Confirm each workstream row shows a range slider with a "XX%" label next to it
- Drag a slider to a new value — confirm a "Save" button appears next to that row
- Click Save — confirm the button disappears and the page refreshes with the new value shown

**5. Health score recalculation (manual check)**
- After saving a Teams slider value, navigate to the Dashboard
- Confirm the health card for that project shows an updated RAG status consistent with the new percent_complete value (no specific value expected — just confirm it loaded without error)

**6. Optimistic "Saving…" indicator (optional — requires DevTools throttle)**
- Open DevTools → Network → set to "Slow 3G"
- Trigger any save (artifact/decision/architecture/teams)
- Confirm "Saving…" text appears next to the Save button during the in-flight request
- Reset network to "No throttling"
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 12, or describe any issues found</resume-signal>
  <verify>
    <automated>cd "/Users/jmiloslavsky/Documents/Project Assistant Code" && npx playwright test tests/e2e/phase12.spec.ts --reporter=line 2>&1 | grep -E "passed|failed" | tail -3</automated>
  </verify>
  <done>User types "approved". Phase 12 is complete — all write surfaces verified working in the running app.</done>
</task>

</tasks>

<verification>
All 12 E2E tests GREEN: `npx playwright test tests/e2e/phase12.spec.ts --reporter=line`
Human verification approved for all 6 manual check areas.
</verification>

<success_criteria>
- npx playwright test tests/e2e/phase12.spec.ts reports 12 passed, 0 failed
- User confirms all 5 write surfaces work as expected in the running app
- No regressions in existing workspace tabs (visible in the same manual walkthrough)
- Phase 12 marked COMPLETE in ROADMAP.md and STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/12-complete-workspace-write-surface/12-04-SUMMARY.md`
Update ROADMAP.md to mark Phase 12 complete.
Update STATE.md to reflect Phase 12 completion and set next phase context.
</output>
