---
phase: 26-multi-user-auth
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - bigpanda-app/package.json
  - tests/auth/login.test.ts
  - tests/auth/login-page.test.tsx
  - tests/auth/setup-guard.test.ts
  - tests/auth/user-management.test.ts
  - tests/auth/self-mod-guard.test.ts
  - tests/auth/resolve-role.test.ts
  - tests/auth/schema.test.ts
  - tests/auth/require-session.test.ts
  - tests/auth/cve-2025-29927.test.ts
  - tests/auth/proxy.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "10 test stub files exist in tests/auth/ and run RED (failing)"
    - "better-auth, bcryptjs, and @types/bcryptjs are installed in package.json"
    - "npm test -- --run tests/auth/ exits non-zero (all stubs fail as expected)"
  artifacts:
    - path: "bigpanda-app/tests/auth/login.test.ts"
      provides: "RED stub for signIn.email() happy path + wrong password (AUTH-01)"
    - path: "bigpanda-app/tests/auth/login-page.test.tsx"
      provides: "RED stub for login page renders without Sidebar/SearchBar (AUTH-01)"
    - path: "bigpanda-app/tests/auth/setup-guard.test.ts"
      provides: "RED stub for /setup redirect when users exist (AUTH-01)"
    - path: "bigpanda-app/tests/auth/user-management.test.ts"
      provides: "RED stub for admin create/edit/deactivate user (AUTH-02)"
    - path: "bigpanda-app/tests/auth/self-mod-guard.test.ts"
      provides: "RED stub for admin cannot deactivate own account (AUTH-02)"
    - path: "bigpanda-app/tests/auth/resolve-role.test.ts"
      provides: "RED stub for resolveRole() with credential + OIDC session shapes (AUTH-03, AUTH-04)"
    - path: "bigpanda-app/tests/auth/schema.test.ts"
      provides: "RED stub for external_id nullable TEXT + role default 'user' via DB query (AUTH-04)"
    - path: "bigpanda-app/tests/auth/require-session.test.ts"
      provides: "RED stub for requireSession() returns 401 with no session cookie (AUTH-05)"
    - path: "bigpanda-app/tests/auth/cve-2025-29927.test.ts"
      provides: "RED stub for route returns 401 when x-middleware-subrequest header present (AUTH-05)"
    - path: "bigpanda-app/tests/auth/proxy.test.ts"
      provides: "RED stub for proxy.ts redirects to /login with no session cookie (AUTH-05)"
  key_links:
    - from: "tests/auth/*.test.ts"
      to: "lib/auth.ts, lib/auth-server.ts, lib/auth-utils.ts"
      via: "imports that will resolve in Wave 1"
      pattern: "import.*lib/auth"
---

<objective>
Install better-auth packages and create all 10 RED test stubs matching 26-VALIDATION.md exactly.

Purpose: Nyquist compliance — no task in later waves proceeds without an automated verify command. These stubs establish the RED state; later waves turn them GREEN.
Output: 10 test files in tests/auth/, better-auth + bcryptjs installed.
</objective>

<execution_context>
@/Users/jmiloslavsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jmiloslavsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-multi-user-auth/26-CONTEXT.md
@.planning/phases/26-multi-user-auth/26-RESEARCH.md
@.planning/phases/26-multi-user-auth/26-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install better-auth, bcryptjs, and @types/bcryptjs</name>
  <files>bigpanda-app/package.json</files>
  <action>
    Run from inside bigpanda-app/:

    ```
    npm install better-auth bcryptjs @types/bcryptjs --legacy-peer-deps
    ```

    The --legacy-peer-deps flag is required because better-auth may have peer dependency mismatches with Next.js 16. After install, verify that `better-auth`, `bcryptjs`, and `@types/bcryptjs` appear in package.json dependencies/devDependencies.

    Do NOT run `npm install` without --legacy-peer-deps — it will fail.
  </action>
  <verify>
    <automated>cd bigpanda-app && node -e "require('bcryptjs'); console.log('bcryptjs OK')"</automated>
  </verify>
  <done>better-auth, bcryptjs, @types/bcryptjs present in node_modules and package.json</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create all 10 RED test stubs in tests/auth/</name>
  <files>
    bigpanda-app/tests/auth/login.test.ts,
    bigpanda-app/tests/auth/login-page.test.tsx,
    bigpanda-app/tests/auth/setup-guard.test.ts,
    bigpanda-app/tests/auth/user-management.test.ts,
    bigpanda-app/tests/auth/self-mod-guard.test.ts,
    bigpanda-app/tests/auth/resolve-role.test.ts,
    bigpanda-app/tests/auth/schema.test.ts,
    bigpanda-app/tests/auth/require-session.test.ts,
    bigpanda-app/tests/auth/cve-2025-29927.test.ts,
    bigpanda-app/tests/auth/proxy.test.ts
  </files>
  <behavior>
    Each test file must:
    - Import the target module (which does NOT exist yet — import will fail or return undefined)
    - Call `expect(thing).toBeDefined()` or similar assertion that FAILS because the module doesn't exist
    - Use `vi.mock()` for DB calls and next/headers to avoid runtime errors during stub phase

    Exact behaviors per file (these become GREEN in later waves):

    login.test.ts (AUTH-01):
    - Test 1: signIn.email({ email, password }) with correct credentials resolves to a session object (not undefined)
    - Test 2: signIn.email({ email, password }) with wrong password rejects with error message containing "Invalid"

    login-page.test.tsx (AUTH-01):
    - Test 1: LoginPage renders without throwing
    - Test 2: LoginPage renders an email input, password input, and submit button
    - Test 3: LoginPage does NOT render a Sidebar or SearchBar component

    setup-guard.test.ts (AUTH-01):
    - Test 1: When users table has 0 rows, setup page renders the "Create Admin Account" form
    - Test 2: When users table has 1+ rows, setup page calls redirect('/login')

    user-management.test.ts (AUTH-02):
    - Test 1: POST /api/settings/users creates a new user record (returns 201)
    - Test 2: PUT /api/settings/users/[id] updates email and role (returns 200)
    - Test 3: PUT /api/settings/users/[id] with active=false deactivates the user (returns 200, user.active is false)

    self-mod-guard.test.ts (AUTH-02):
    - Test 1: PUT /api/settings/users/[id] where id === session.user.id returns 403
    - Test 2: DELETE /api/settings/users/[id] where id === session.user.id returns 403

    resolve-role.test.ts (AUTH-03, AUTH-04):
    - Test 1: resolveRole({ user: { role: 'admin' } }) returns 'admin'
    - Test 2: resolveRole({ user: { role: 'user' } }) returns 'user'
    - Test 3: resolveRole({ user: {} }) returns 'user' (default)
    - Test 4: resolveRole({ claims: { groups: ['Admins'] } }) returns 'admin' (OIDC path)
    - Test 5: resolveRole({ claims: { roles: ['admin'] } }) returns 'admin' (OIDC path)
    - Test 6: resolveRole({ claims: {} }) returns 'user' (OIDC path, no matching groups)

    schema.test.ts (AUTH-04):
    - Test 1: users table column external_id is nullable (no NOT NULL constraint) — verify via DB introspection query
    - Test 2: users table column role has default value 'user'
    - Test 3: users table column active has default value true

    require-session.test.ts (AUTH-05):
    - Test 1: requireSession() when auth.api.getSession returns null → returns { session: null, redirectResponse: 401 NextResponse }
    - Test 2: requireSession() when auth.api.getSession returns a session → returns { session, redirectResponse: null }

    cve-2025-29927.test.ts (AUTH-05):
    - Test 1: A route handler that calls requireSession() returns 401 even when x-middleware-subrequest header is present
    - Note: This test verifies requireSession() is the security boundary, not the proxy layer

    proxy.test.ts (AUTH-05):
    - Test 1: proxy() with no session cookie → returns NextResponse.redirect to /login
    - Test 2: proxy() with session cookie present → returns NextResponse.next()
    - Test 3: proxy() on /login path → does not redirect (must match exclusion pattern)
    - Test 4: proxy() on /setup path → does not redirect (must match exclusion pattern)
    - Test 5: proxy() on /api/auth/... path → does not redirect (must match exclusion pattern)

    All stubs MUST fail (RED) because the implementation files (lib/auth.ts, lib/auth-server.ts, lib/auth-utils.ts, proxy.ts, app/login/page.tsx, app/setup/page.tsx, app/api/settings/users/route.ts) do not exist yet.

    Use vi.mock() to mock:
    - "@/db" → return { db: {} }
    - "next/headers" → return { headers: vi.fn().mockResolvedValue(new Headers()) }
    - "next/navigation" → return { redirect: vi.fn() }
    - "better-auth" → return { betterAuth: vi.fn() } (for login.test.ts only)
  </behavior>
  <action>
    Create the tests/auth/ directory and all 10 stub files. Each file imports its target module and asserts behaviors that will pass only after Wave 1-4 implementation.

    Use this skeleton pattern for each stub:

    ```typescript
    // tests/auth/[name].test.ts
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    vi.mock('@/db', () => ({ db: {} }));
    vi.mock('next/headers', () => ({ headers: vi.fn().mockResolvedValue(new Headers()) }));

    // Import the target (will be undefined / throw until implemented)
    // import { targetFn } from '@/lib/target-module';

    describe('[Feature name]', () => {
      it('[behavior description]', async () => {
        expect(false).toBe(true); // RED — remove when implementing
      });
    });
    ```

    Replace the `expect(false).toBe(true)` placeholder with the actual assertions listed in the behavior block above. The assertions will fail RED because the imported modules don't exist yet — that is the correct state.

    Commit message after this task: `test(26-01): add 10 RED auth test stubs — Wave 0`
  </action>
  <verify>
    <automated>cd bigpanda-app && npm test -- --run tests/auth/ 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 10 test files exist in tests/auth/. Running the test suite shows all 10 test files attempted, and they FAIL (RED) because implementation modules are missing. Non-zero exit code is expected and correct.
  </done>
</task>

</tasks>

<verification>
- `bigpanda-app/node_modules/better-auth` directory exists
- `bigpanda-app/node_modules/bcryptjs` directory exists
- All 10 files in `tests/auth/` exist (ls tests/auth/ shows 10 .ts/.tsx files)
- `npm test -- --run tests/auth/` exits non-zero (all RED)
- No existing tests broken by package install (run `npm test -- --run` and confirm other test files still pass)
</verification>

<success_criteria>
10 test stubs exist and are RED. better-auth and bcryptjs are installed. The test infrastructure is ready for Wave 1 implementation to turn stubs GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/26-multi-user-auth/26-01-SUMMARY.md`
</output>
